/**
 * @module Effect/WorkbenchLayout/Tag/WorkbenchLayoutServiceTag
 * @description
 * Type alias for `WorkbenchLayoutService`. The live service object is
 * available via the LandWorkbench registry.
 * @category Tag
 */

import type { WorkbenchLayoutService } from "../Interface/WorkbenchLayoutService.js";

export type WorkbenchLayoutServiceTag = WorkbenchLayoutService;

export type WorkbenchLayout = WorkbenchLayoutService;
