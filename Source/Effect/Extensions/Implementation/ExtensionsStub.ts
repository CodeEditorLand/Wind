import { Effect } from "effect";
import type { ExtensionsService } from "../Interface/ExtensionsService.js";

export const StubExtensionsService: ExtensionsService = {} as ExtensionsService;

export default StubExtensionsService;
