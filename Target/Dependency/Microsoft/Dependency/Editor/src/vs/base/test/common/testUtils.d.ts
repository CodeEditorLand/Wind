export declare function flakySuite(title: string, fn: () => void): Mocha.Suite;
/**
 * (pseudo)Random boolean generator.
 *
 * ## Examples
 *
 * ```typescript
 * randomBoolean(); // generates either `true` or `false`
 * ```
 *
 */
export declare const randomBoolean: () => boolean;
