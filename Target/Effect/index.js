var __defProp = Object.defineProperty;
var __name = (target, value2) => __defProp(target, "name", { value: value2, configurable: true });
var __export = (target, all12) => {
  for (var name in all12)
    __defProp(target, name, { get: all12[name], enumerable: true });
};

// ../../node_modules/.pnpm/@tauri-apps+api@2.10.1/node_modules/@tauri-apps/api/external/tslib/tslib.es6.js
function __classPrivateFieldGet(receiver, state, kind, f) {
  if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}
__name(__classPrivateFieldGet, "__classPrivateFieldGet");
function __classPrivateFieldSet(receiver, state, value2, kind, f) {
  if (kind === "m") throw new TypeError("Private method is not writable");
  if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f.call(receiver, value2) : f ? f.value = value2 : state.set(receiver, value2), value2;
}
__name(__classPrivateFieldSet, "__classPrivateFieldSet");
typeof SuppressedError === "function" ? SuppressedError : function(error2, suppressed, message) {
  var e = new Error(message);
  return e.name = "SuppressedError", e.error = error2, e.suppressed = suppressed, e;
};

// ../../node_modules/.pnpm/@tauri-apps+api@2.10.1/node_modules/@tauri-apps/api/core.js
var _Channel_onmessage, _Channel_nextMessageIndex, _Channel_pendingMessages, _Channel_messageEndIndex, _Resource_rid;
var SERIALIZE_TO_IPC_FN = "__TAURI_TO_IPC_KEY__";
function transformCallback(callback, once6 = false) {
  return window.__TAURI_INTERNALS__.transformCallback(callback, once6);
}
__name(transformCallback, "transformCallback");
var Channel = class {
  static {
    __name(this, "Channel");
  }
  constructor(onmessage) {
    _Channel_onmessage.set(this, void 0);
    _Channel_nextMessageIndex.set(this, 0);
    _Channel_pendingMessages.set(this, []);
    _Channel_messageEndIndex.set(this, void 0);
    __classPrivateFieldSet(this, _Channel_onmessage, onmessage || (() => {
    }), "f");
    this.id = transformCallback((rawMessage) => {
      const index = rawMessage.index;
      if ("end" in rawMessage) {
        if (index == __classPrivateFieldGet(this, _Channel_nextMessageIndex, "f")) {
          this.cleanupCallback();
        } else {
          __classPrivateFieldSet(this, _Channel_messageEndIndex, index, "f");
        }
        return;
      }
      const message = rawMessage.message;
      if (index == __classPrivateFieldGet(this, _Channel_nextMessageIndex, "f")) {
        __classPrivateFieldGet(this, _Channel_onmessage, "f").call(this, message);
        __classPrivateFieldSet(this, _Channel_nextMessageIndex, __classPrivateFieldGet(this, _Channel_nextMessageIndex, "f") + 1, "f");
        while (__classPrivateFieldGet(this, _Channel_nextMessageIndex, "f") in __classPrivateFieldGet(this, _Channel_pendingMessages, "f")) {
          const message2 = __classPrivateFieldGet(this, _Channel_pendingMessages, "f")[__classPrivateFieldGet(this, _Channel_nextMessageIndex, "f")];
          __classPrivateFieldGet(this, _Channel_onmessage, "f").call(this, message2);
          delete __classPrivateFieldGet(this, _Channel_pendingMessages, "f")[__classPrivateFieldGet(this, _Channel_nextMessageIndex, "f")];
          __classPrivateFieldSet(this, _Channel_nextMessageIndex, __classPrivateFieldGet(this, _Channel_nextMessageIndex, "f") + 1, "f");
        }
        if (__classPrivateFieldGet(this, _Channel_nextMessageIndex, "f") === __classPrivateFieldGet(this, _Channel_messageEndIndex, "f")) {
          this.cleanupCallback();
        }
      } else {
        __classPrivateFieldGet(this, _Channel_pendingMessages, "f")[index] = message;
      }
    });
  }
  cleanupCallback() {
    window.__TAURI_INTERNALS__.unregisterCallback(this.id);
  }
  set onmessage(handler) {
    __classPrivateFieldSet(this, _Channel_onmessage, handler, "f");
  }
  get onmessage() {
    return __classPrivateFieldGet(this, _Channel_onmessage, "f");
  }
  [(_Channel_onmessage = /* @__PURE__ */ new WeakMap(), _Channel_nextMessageIndex = /* @__PURE__ */ new WeakMap(), _Channel_pendingMessages = /* @__PURE__ */ new WeakMap(), _Channel_messageEndIndex = /* @__PURE__ */ new WeakMap(), SERIALIZE_TO_IPC_FN)]() {
    return `__CHANNEL__:${this.id}`;
  }
  toJSON() {
    return this[SERIALIZE_TO_IPC_FN]();
  }
};
var PluginListener = class {
  static {
    __name(this, "PluginListener");
  }
  constructor(plugin, event, channelId) {
    this.plugin = plugin;
    this.event = event;
    this.channelId = channelId;
  }
  async unregister() {
    return invoke(`plugin:${this.plugin}|remove_listener`, {
      event: this.event,
      channelId: this.channelId
    });
  }
};
async function addPluginListener(plugin, event, cb) {
  const handler = new Channel(cb);
  try {
    await invoke(`plugin:${plugin}|register_listener`, {
      event,
      handler
    });
    return new PluginListener(plugin, event, handler.id);
  } catch {
    await invoke(`plugin:${plugin}|registerListener`, { event, handler });
    return new PluginListener(plugin, event, handler.id);
  }
}
__name(addPluginListener, "addPluginListener");
async function checkPermissions(plugin) {
  return invoke(`plugin:${plugin}|check_permissions`);
}
__name(checkPermissions, "checkPermissions");
async function requestPermissions(plugin) {
  return invoke(`plugin:${plugin}|request_permissions`);
}
__name(requestPermissions, "requestPermissions");
async function invoke(cmd, args2 = {}, options) {
  return window.__TAURI_INTERNALS__.invoke(cmd, args2, options);
}
__name(invoke, "invoke");
function convertFileSrc(filePath, protocol = "asset") {
  return window.__TAURI_INTERNALS__.convertFileSrc(filePath, protocol);
}
__name(convertFileSrc, "convertFileSrc");
var Resource = class {
  static {
    __name(this, "Resource");
  }
  get rid() {
    return __classPrivateFieldGet(this, _Resource_rid, "f");
  }
  constructor(rid) {
    _Resource_rid.set(this, void 0);
    __classPrivateFieldSet(this, _Resource_rid, rid, "f");
  }
  /**
   * Destroys and cleans up this resource from memory.
   * **You should not call any method on this object anymore and should drop any reference to it.**
   */
  async close() {
    return invoke("plugin:resources|close", {
      rid: this.rid
    });
  }
};
_Resource_rid = /* @__PURE__ */ new WeakMap();
function isTauri() {
  return !!(globalThis || window).isTauri;
}
__name(isTauri, "isTauri");

// ../../node_modules/.pnpm/@tauri-apps+api@2.10.1/node_modules/@tauri-apps/api/event.js
var TauriEvent;
(function(TauriEvent2) {
  TauriEvent2["WINDOW_RESIZED"] = "tauri://resize";
  TauriEvent2["WINDOW_MOVED"] = "tauri://move";
  TauriEvent2["WINDOW_CLOSE_REQUESTED"] = "tauri://close-requested";
  TauriEvent2["WINDOW_DESTROYED"] = "tauri://destroyed";
  TauriEvent2["WINDOW_FOCUS"] = "tauri://focus";
  TauriEvent2["WINDOW_BLUR"] = "tauri://blur";
  TauriEvent2["WINDOW_SCALE_FACTOR_CHANGED"] = "tauri://scale-change";
  TauriEvent2["WINDOW_THEME_CHANGED"] = "tauri://theme-changed";
  TauriEvent2["WINDOW_CREATED"] = "tauri://window-created";
  TauriEvent2["WEBVIEW_CREATED"] = "tauri://webview-created";
  TauriEvent2["DRAG_ENTER"] = "tauri://drag-enter";
  TauriEvent2["DRAG_OVER"] = "tauri://drag-over";
  TauriEvent2["DRAG_DROP"] = "tauri://drag-drop";
  TauriEvent2["DRAG_LEAVE"] = "tauri://drag-leave";
})(TauriEvent || (TauriEvent = {}));
async function _unlisten(event, eventId) {
  window.__TAURI_EVENT_PLUGIN_INTERNALS__.unregisterListener(event, eventId);
  await invoke("plugin:event|unlisten", {
    event,
    eventId
  });
}
__name(_unlisten, "_unlisten");
async function listen(event, handler, options) {
  var _a;
  const target = typeof (options === null || options === void 0 ? void 0 : options.target) === "string" ? { kind: "AnyLabel", label: options.target } : (_a = options === null || options === void 0 ? void 0 : options.target) !== null && _a !== void 0 ? _a : { kind: "Any" };
  return invoke("plugin:event|listen", {
    event,
    target,
    handler: transformCallback(handler)
  }).then((eventId) => {
    return async () => _unlisten(event, eventId);
  });
}
__name(listen, "listen");
async function once(event, handler, options) {
  return listen(event, (eventData) => {
    void _unlisten(event, eventData.id);
    handler(eventData);
  }, options);
}
__name(once, "once");
async function emit(event, payload) {
  await invoke("plugin:event|emit", {
    event,
    payload
  });
}
__name(emit, "emit");
async function emitTo(target, event, payload) {
  const eventTarget = typeof target === "string" ? { kind: "AnyLabel", label: target } : target;
  await invoke("plugin:event|emit_to", {
    target: eventTarget,
    event,
    payload
  });
}
__name(emitTo, "emitTo");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Function.js
var isFunction = /* @__PURE__ */ __name((input) => typeof input === "function", "isFunction");
var dual = /* @__PURE__ */ __name(function(arity, body) {
  if (typeof arity === "function") {
    return function() {
      if (arity(arguments)) {
        return body.apply(this, arguments);
      }
      return (self) => body(self, ...arguments);
    };
  }
  switch (arity) {
    case 0:
    case 1:
      throw new RangeError(`Invalid arity ${arity}`);
    case 2:
      return function(a, b) {
        if (arguments.length >= 2) {
          return body(a, b);
        }
        return function(self) {
          return body(self, a);
        };
      };
    case 3:
      return function(a, b, c) {
        if (arguments.length >= 3) {
          return body(a, b, c);
        }
        return function(self) {
          return body(self, a, b);
        };
      };
    case 4:
      return function(a, b, c, d) {
        if (arguments.length >= 4) {
          return body(a, b, c, d);
        }
        return function(self) {
          return body(self, a, b, c);
        };
      };
    case 5:
      return function(a, b, c, d, e) {
        if (arguments.length >= 5) {
          return body(a, b, c, d, e);
        }
        return function(self) {
          return body(self, a, b, c, d);
        };
      };
    default:
      return function() {
        if (arguments.length >= arity) {
          return body.apply(this, arguments);
        }
        const args2 = arguments;
        return function(self) {
          return body(self, ...args2);
        };
      };
  }
}, "dual");
var apply = /* @__PURE__ */ __name((...a) => (self) => self(...a), "apply");
var identity = /* @__PURE__ */ __name((a) => a, "identity");
var satisfies = /* @__PURE__ */ __name(() => (b) => b, "satisfies");
var unsafeCoerce = identity;
var constant = /* @__PURE__ */ __name((value2) => () => value2, "constant");
var constTrue = /* @__PURE__ */ constant(true);
var constFalse = /* @__PURE__ */ constant(false);
var constNull = /* @__PURE__ */ constant(null);
var constUndefined = /* @__PURE__ */ constant(void 0);
var constVoid = constUndefined;
var flip = /* @__PURE__ */ __name((f) => (...b) => (...a) => f(...a)(...b), "flip");
var compose = /* @__PURE__ */ dual(2, (ab, bc) => (a) => bc(ab(a)));
var absurd = /* @__PURE__ */ __name((_) => {
  throw new Error("Called `absurd` function which should be uncallable");
}, "absurd");
var tupled = /* @__PURE__ */ __name((f) => (a) => f(...a), "tupled");
var untupled = /* @__PURE__ */ __name((f) => (...a) => f(a), "untupled");
function pipe(a, ab, bc, cd, de, ef, fg, gh, hi) {
  switch (arguments.length) {
    case 1:
      return a;
    case 2:
      return ab(a);
    case 3:
      return bc(ab(a));
    case 4:
      return cd(bc(ab(a)));
    case 5:
      return de(cd(bc(ab(a))));
    case 6:
      return ef(de(cd(bc(ab(a)))));
    case 7:
      return fg(ef(de(cd(bc(ab(a))))));
    case 8:
      return gh(fg(ef(de(cd(bc(ab(a)))))));
    case 9:
      return hi(gh(fg(ef(de(cd(bc(ab(a))))))));
    default: {
      let ret = arguments[0];
      for (let i = 1; i < arguments.length; i++) {
        ret = arguments[i](ret);
      }
      return ret;
    }
  }
}
__name(pipe, "pipe");
function flow(ab, bc, cd, de, ef, fg, gh, hi, ij) {
  switch (arguments.length) {
    case 1:
      return ab;
    case 2:
      return function() {
        return bc(ab.apply(this, arguments));
      };
    case 3:
      return function() {
        return cd(bc(ab.apply(this, arguments)));
      };
    case 4:
      return function() {
        return de(cd(bc(ab.apply(this, arguments))));
      };
    case 5:
      return function() {
        return ef(de(cd(bc(ab.apply(this, arguments)))));
      };
    case 6:
      return function() {
        return fg(ef(de(cd(bc(ab.apply(this, arguments))))));
      };
    case 7:
      return function() {
        return gh(fg(ef(de(cd(bc(ab.apply(this, arguments)))))));
      };
    case 8:
      return function() {
        return hi(gh(fg(ef(de(cd(bc(ab.apply(this, arguments))))))));
      };
    case 9:
      return function() {
        return ij(hi(gh(fg(ef(de(cd(bc(ab.apply(this, arguments)))))))));
      };
  }
  return;
}
__name(flow, "flow");
var hole = /* @__PURE__ */ unsafeCoerce(absurd);
var SK = /* @__PURE__ */ __name((_, b) => b, "SK");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Equivalence.js
var make = /* @__PURE__ */ __name((isEquivalent) => (self, that) => self === that || isEquivalent(self, that), "make");
var isStrictEquivalent = /* @__PURE__ */ __name((x, y) => x === y, "isStrictEquivalent");
var strict = /* @__PURE__ */ __name(() => isStrictEquivalent, "strict");
var string = /* @__PURE__ */ strict();
var number = /* @__PURE__ */ strict();
var boolean = /* @__PURE__ */ strict();
var bigint = /* @__PURE__ */ strict();
var symbol = /* @__PURE__ */ strict();
var combine = /* @__PURE__ */ dual(2, (self, that) => make((x, y) => self(x, y) && that(x, y)));
var combineMany = /* @__PURE__ */ dual(2, (self, collection) => make((x, y) => {
  if (!self(x, y)) {
    return false;
  }
  for (const equivalence2 of collection) {
    if (!equivalence2(x, y)) {
      return false;
    }
  }
  return true;
}));
var isAlwaysEquivalent = /* @__PURE__ */ __name((_x, _y) => true, "isAlwaysEquivalent");
var combineAll = /* @__PURE__ */ __name((collection) => combineMany(isAlwaysEquivalent, collection), "combineAll");
var mapInput = /* @__PURE__ */ dual(2, (self, f) => make((x, y) => self(f(x), f(y))));
var Date2 = /* @__PURE__ */ mapInput(number, (date) => date.getTime());
var product = /* @__PURE__ */ dual(2, (self, that) => make(([xa, xb], [ya, yb]) => self(xa, ya) && that(xb, yb)));
var all = /* @__PURE__ */ __name((collection) => {
  return make((x, y) => {
    const len = Math.min(x.length, y.length);
    let collectionLength = 0;
    for (const equivalence2 of collection) {
      if (collectionLength >= len) {
        break;
      }
      if (!equivalence2(x[collectionLength], y[collectionLength])) {
        return false;
      }
      collectionLength++;
    }
    return true;
  });
}, "all");
var productMany = /* @__PURE__ */ __name((self, collection) => {
  const equivalence2 = all(collection);
  return make((x, y) => !self(x[0], y[0]) ? false : equivalence2(x.slice(1), y.slice(1)));
}, "productMany");
var tuple = /* @__PURE__ */ __name((...elements) => all(elements), "tuple");
var array = /* @__PURE__ */ __name((item) => make((self, that) => {
  if (self.length !== that.length) {
    return false;
  }
  for (let i = 0; i < self.length; i++) {
    const isEq = item(self[i], that[i]);
    if (!isEq) {
      return false;
    }
  }
  return true;
}), "array");
var struct = /* @__PURE__ */ __name((fields) => {
  const keys7 = Object.keys(fields);
  return make((self, that) => {
    for (const key of keys7) {
      if (!fields[key](self[key], that[key])) {
        return false;
      }
    }
    return true;
  });
}, "struct");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/doNotation.js
var let_ = /* @__PURE__ */ __name((map36) => dual(3, (self, name, f) => map36(self, (a) => ({
  ...a,
  [name]: f(a)
}))), "let_");
var bindTo = /* @__PURE__ */ __name((map36) => dual(2, (self, name) => map36(self, (a) => ({
  [name]: a
}))), "bindTo");
var bind = /* @__PURE__ */ __name((map36, flatMap25) => dual(3, (self, name, f) => flatMap25(self, (a) => map36(f(a), (b) => ({
  ...a,
  [name]: b
})))), "bind");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/GlobalValue.js
var globalStoreId = `effect/GlobalValue`;
var globalStore;
var globalValue = /* @__PURE__ */ __name((id3, compute) => {
  if (!globalStore) {
    globalThis[globalStoreId] ??= /* @__PURE__ */ new Map();
    globalStore = globalThis[globalStoreId];
  }
  if (!globalStore.has(id3)) {
    globalStore.set(id3, compute());
  }
  return globalStore.get(id3);
}, "globalValue");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Predicate.js
var mapInput2 = /* @__PURE__ */ dual(2, (self, f) => (b) => self(f(b)));
var isTupleOf = /* @__PURE__ */ dual(2, (self, n) => self.length === n);
var isTupleOfAtLeast = /* @__PURE__ */ dual(2, (self, n) => self.length >= n);
var isTruthy = /* @__PURE__ */ __name((input) => !!input, "isTruthy");
var isSet = /* @__PURE__ */ __name((input) => input instanceof Set, "isSet");
var isMap = /* @__PURE__ */ __name((input) => input instanceof Map, "isMap");
var isString = /* @__PURE__ */ __name((input) => typeof input === "string", "isString");
var isNumber = /* @__PURE__ */ __name((input) => typeof input === "number", "isNumber");
var isBoolean = /* @__PURE__ */ __name((input) => typeof input === "boolean", "isBoolean");
var isBigInt = /* @__PURE__ */ __name((input) => typeof input === "bigint", "isBigInt");
var isSymbol = /* @__PURE__ */ __name((input) => typeof input === "symbol", "isSymbol");
var isPropertyKey = /* @__PURE__ */ __name((u) => isString(u) || isNumber(u) || isSymbol(u), "isPropertyKey");
var isFunction2 = isFunction;
var isUndefined = /* @__PURE__ */ __name((input) => input === void 0, "isUndefined");
var isNotUndefined = /* @__PURE__ */ __name((input) => input !== void 0, "isNotUndefined");
var isNull = /* @__PURE__ */ __name((input) => input === null, "isNull");
var isNotNull = /* @__PURE__ */ __name((input) => input !== null, "isNotNull");
var isNever = /* @__PURE__ */ __name((_) => false, "isNever");
var isUnknown = /* @__PURE__ */ __name((_) => true, "isUnknown");
var isRecordOrArray = /* @__PURE__ */ __name((input) => typeof input === "object" && input !== null, "isRecordOrArray");
var isObject = /* @__PURE__ */ __name((input) => isRecordOrArray(input) || isFunction2(input), "isObject");
var hasProperty = /* @__PURE__ */ dual(2, (self, property) => isObject(self) && property in self);
var isTagged = /* @__PURE__ */ dual(2, (self, tag) => hasProperty(self, "_tag") && self["_tag"] === tag);
var isNullable = /* @__PURE__ */ __name((input) => input === null || input === void 0, "isNullable");
var isNotNullable = /* @__PURE__ */ __name((input) => input !== null && input !== void 0, "isNotNullable");
var isError = /* @__PURE__ */ __name((input) => input instanceof Error, "isError");
var isUint8Array = /* @__PURE__ */ __name((input) => input instanceof Uint8Array, "isUint8Array");
var isDate = /* @__PURE__ */ __name((input) => input instanceof Date, "isDate");
var isIterable = /* @__PURE__ */ __name((input) => typeof input === "string" || hasProperty(input, Symbol.iterator), "isIterable");
var isRecord = /* @__PURE__ */ __name((input) => isRecordOrArray(input) && !Array.isArray(input), "isRecord");
var isReadonlyRecord = isRecord;
var isPromise = /* @__PURE__ */ __name((input) => hasProperty(input, "then") && "catch" in input && isFunction2(input.then) && isFunction2(input.catch), "isPromise");
var isPromiseLike = /* @__PURE__ */ __name((input) => hasProperty(input, "then") && isFunction2(input.then), "isPromiseLike");
var isRegExp = /* @__PURE__ */ __name((input) => input instanceof RegExp, "isRegExp");
var compose2 = /* @__PURE__ */ dual(2, (ab, bc) => (a) => ab(a) && bc(a));
var product2 = /* @__PURE__ */ __name((self, that) => ([a, b]) => self(a) && that(b), "product");
var all2 = /* @__PURE__ */ __name((collection) => {
  return (as15) => {
    let collectionIndex = 0;
    for (const p of collection) {
      if (collectionIndex >= as15.length) {
        break;
      }
      if (p(as15[collectionIndex]) === false) {
        return false;
      }
      collectionIndex++;
    }
    return true;
  };
}, "all");
var productMany2 = /* @__PURE__ */ __name((self, collection) => {
  const rest = all2(collection);
  return ([head10, ...tail5]) => self(head10) === false ? false : rest(tail5);
}, "productMany");
var tuple2 = /* @__PURE__ */ __name((...elements) => all2(elements), "tuple");
var struct2 = /* @__PURE__ */ __name((fields) => {
  const keys7 = Object.keys(fields);
  return (a) => {
    for (const key of keys7) {
      if (!fields[key](a[key])) {
        return false;
      }
    }
    return true;
  };
}, "struct");
var not = /* @__PURE__ */ __name((self) => (a) => !self(a), "not");
var or = /* @__PURE__ */ dual(2, (self, that) => (a) => self(a) || that(a));
var and = /* @__PURE__ */ dual(2, (self, that) => (a) => self(a) && that(a));
var xor = /* @__PURE__ */ dual(2, (self, that) => (a) => self(a) !== that(a));
var eqv = /* @__PURE__ */ dual(2, (self, that) => (a) => self(a) === that(a));
var implies = /* @__PURE__ */ dual(2, (antecedent, consequent) => (a) => antecedent(a) ? consequent(a) : true);
var nor = /* @__PURE__ */ dual(2, (self, that) => (a) => !(self(a) || that(a)));
var nand = /* @__PURE__ */ dual(2, (self, that) => (a) => !(self(a) && that(a)));
var every = /* @__PURE__ */ __name((collection) => (a) => {
  for (const p of collection) {
    if (!p(a)) {
      return false;
    }
  }
  return true;
}, "every");
var some = /* @__PURE__ */ __name((collection) => (a) => {
  for (const p of collection) {
    if (p(a)) {
      return true;
    }
  }
  return false;
}, "some");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/errors.js
var getBugErrorMessage = /* @__PURE__ */ __name((message) => `BUG: ${message} - please report an issue at https://github.com/Effect-TS/effect/issues`, "getBugErrorMessage");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Utils.js
var GenKindTypeId = /* @__PURE__ */ Symbol.for("effect/Gen/GenKind");
var isGenKind = /* @__PURE__ */ __name((u) => isObject(u) && GenKindTypeId in u, "isGenKind");
var GenKindImpl = class {
  static {
    __name(this, "GenKindImpl");
  }
  value;
  constructor(value2) {
    this.value = value2;
  }
  /**
   * @since 2.0.0
   */
  get _F() {
    return identity;
  }
  /**
   * @since 2.0.0
   */
  get _R() {
    return (_) => _;
  }
  /**
   * @since 2.0.0
   */
  get _O() {
    return (_) => _;
  }
  /**
   * @since 2.0.0
   */
  get _E() {
    return (_) => _;
  }
  /**
   * @since 2.0.0
   */
  [GenKindTypeId] = GenKindTypeId;
  /**
   * @since 2.0.0
   */
  [Symbol.iterator]() {
    return new SingleShotGen(this);
  }
};
var SingleShotGen = class _SingleShotGen {
  static {
    __name(this, "SingleShotGen");
  }
  self;
  called = false;
  constructor(self) {
    this.self = self;
  }
  /**
   * @since 2.0.0
   */
  next(a) {
    return this.called ? {
      value: a,
      done: true
    } : (this.called = true, {
      value: this.self,
      done: false
    });
  }
  /**
   * @since 2.0.0
   */
  return(a) {
    return {
      value: a,
      done: true
    };
  }
  /**
   * @since 2.0.0
   */
  throw(e) {
    throw e;
  }
  /**
   * @since 2.0.0
   */
  [Symbol.iterator]() {
    return new _SingleShotGen(this.self);
  }
};
var makeGenKind = /* @__PURE__ */ __name((kind) => new GenKindImpl(kind), "makeGenKind");
var adapter = /* @__PURE__ */ __name(() => function() {
  let x = arguments[0];
  for (let i = 1; i < arguments.length; i++) {
    x = arguments[i](x);
  }
  return new GenKindImpl(x);
}, "adapter");
var defaultIncHi = 335903614;
var defaultIncLo = 4150755663;
var MUL_HI = 1481765933 >>> 0;
var MUL_LO = 1284865837 >>> 0;
var BIT_53 = 9007199254740992;
var BIT_27 = 134217728;
var PCGRandom = class {
  static {
    __name(this, "PCGRandom");
  }
  _state;
  constructor(seedHi, seedLo, incHi, incLo) {
    if (isNullable(seedLo) && isNullable(seedHi)) {
      seedLo = Math.random() * 4294967295 >>> 0;
      seedHi = 0;
    } else if (isNullable(seedLo)) {
      seedLo = seedHi;
      seedHi = 0;
    }
    if (isNullable(incLo) && isNullable(incHi)) {
      incLo = this._state ? this._state[3] : defaultIncLo;
      incHi = this._state ? this._state[2] : defaultIncHi;
    } else if (isNullable(incLo)) {
      incLo = incHi;
      incHi = 0;
    }
    this._state = new Int32Array([0, 0, incHi >>> 0, ((incLo || 0) | 1) >>> 0]);
    this._next();
    add64(this._state, this._state[0], this._state[1], seedHi >>> 0, seedLo >>> 0);
    this._next();
    return this;
  }
  /**
   * Returns a copy of the internal state of this random number generator as a
   * JavaScript Array.
   *
   * @category getters
   * @since 2.0.0
   */
  getState() {
    return [this._state[0], this._state[1], this._state[2], this._state[3]];
  }
  /**
   * Restore state previously retrieved using `getState()`.
   *
   * @since 2.0.0
   */
  setState(state) {
    this._state[0] = state[0];
    this._state[1] = state[1];
    this._state[2] = state[2];
    this._state[3] = state[3] | 1;
  }
  /**
   * Get a uniformly distributed 32 bit integer between [0, max).
   *
   * @category getter
   * @since 2.0.0
   */
  integer(max10) {
    return Math.round(this.number() * Number.MAX_SAFE_INTEGER) % max10;
  }
  /**
   * Get a uniformly distributed IEEE-754 double between 0.0 and 1.0, with
   * 53 bits of precision (every bit of the mantissa is randomized).
   *
   * @category getters
   * @since 2.0.0
   */
  number() {
    const hi = (this._next() & 67108863) * 1;
    const lo = (this._next() & 134217727) * 1;
    return (hi * BIT_27 + lo) / BIT_53;
  }
  /** @internal */
  _next() {
    const oldHi = this._state[0] >>> 0;
    const oldLo = this._state[1] >>> 0;
    mul64(this._state, oldHi, oldLo, MUL_HI, MUL_LO);
    add64(this._state, this._state[0], this._state[1], this._state[2], this._state[3]);
    let xsHi = oldHi >>> 18;
    let xsLo = (oldLo >>> 18 | oldHi << 14) >>> 0;
    xsHi = (xsHi ^ oldHi) >>> 0;
    xsLo = (xsLo ^ oldLo) >>> 0;
    const xorshifted = (xsLo >>> 27 | xsHi << 5) >>> 0;
    const rot = oldHi >>> 27;
    const rot2 = (-rot >>> 0 & 31) >>> 0;
    return (xorshifted >>> rot | xorshifted << rot2) >>> 0;
  }
};
function mul64(out, aHi, aLo, bHi, bLo) {
  let c1 = (aLo >>> 16) * (bLo & 65535) >>> 0;
  let c0 = (aLo & 65535) * (bLo >>> 16) >>> 0;
  let lo = (aLo & 65535) * (bLo & 65535) >>> 0;
  let hi = (aLo >>> 16) * (bLo >>> 16) + ((c0 >>> 16) + (c1 >>> 16)) >>> 0;
  c0 = c0 << 16 >>> 0;
  lo = lo + c0 >>> 0;
  if (lo >>> 0 < c0 >>> 0) {
    hi = hi + 1 >>> 0;
  }
  c1 = c1 << 16 >>> 0;
  lo = lo + c1 >>> 0;
  if (lo >>> 0 < c1 >>> 0) {
    hi = hi + 1 >>> 0;
  }
  hi = hi + Math.imul(aLo, bHi) >>> 0;
  hi = hi + Math.imul(aHi, bLo) >>> 0;
  out[0] = hi;
  out[1] = lo;
}
__name(mul64, "mul64");
function add64(out, aHi, aLo, bHi, bLo) {
  let hi = aHi + bHi >>> 0;
  const lo = aLo + bLo >>> 0;
  if (lo >>> 0 < aLo >>> 0) {
    hi = hi + 1 | 0;
  }
  out[0] = hi;
  out[1] = lo;
}
__name(add64, "add64");
var YieldWrapTypeId = /* @__PURE__ */ Symbol.for("effect/Utils/YieldWrap");
var YieldWrap = class {
  static {
    __name(this, "YieldWrap");
  }
  /**
   * @since 3.0.6
   */
  #value;
  constructor(value2) {
    this.#value = value2;
  }
  /**
   * @since 3.0.6
   */
  [YieldWrapTypeId]() {
    return this.#value;
  }
};
function yieldWrapGet(self) {
  if (typeof self === "object" && self !== null && YieldWrapTypeId in self) {
    return self[YieldWrapTypeId]();
  }
  throw new Error(getBugErrorMessage("yieldWrapGet"));
}
__name(yieldWrapGet, "yieldWrapGet");
var structuralRegionState = /* @__PURE__ */ globalValue("effect/Utils/isStructuralRegion", () => ({
  enabled: false,
  tester: void 0
}));
var structuralRegion = /* @__PURE__ */ __name((body, tester) => {
  const current2 = structuralRegionState.enabled;
  const currentTester = structuralRegionState.tester;
  structuralRegionState.enabled = true;
  if (tester) {
    structuralRegionState.tester = tester;
  }
  try {
    return body();
  } finally {
    structuralRegionState.enabled = current2;
    structuralRegionState.tester = currentTester;
  }
}, "structuralRegion");
var standard = {
  effect_internal_function: /* @__PURE__ */ __name((body) => {
    return body();
  }, "effect_internal_function")
};
var forced = {
  effect_internal_function: /* @__PURE__ */ __name((body) => {
    try {
      return body();
    } finally {
    }
  }, "effect_internal_function")
};
var isNotOptimizedAway = /* @__PURE__ */ standard.effect_internal_function(() => new Error().stack)?.includes("effect_internal_function") === true;
var internalCall = isNotOptimizedAway ? standard.effect_internal_function : forced.effect_internal_function;
var genConstructor = function* () {
}.constructor;
var isGeneratorFunction = /* @__PURE__ */ __name((u) => isObject(u) && u.constructor === genConstructor, "isGeneratorFunction");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Hash.js
var randomHashCache = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Hash/randomHashCache"), () => /* @__PURE__ */ new WeakMap());
var symbol2 = /* @__PURE__ */ Symbol.for("effect/Hash");
var hash = /* @__PURE__ */ __name((self) => {
  if (structuralRegionState.enabled === true) {
    return 0;
  }
  switch (typeof self) {
    case "number":
      return number2(self);
    case "bigint":
      return string2(self.toString(10));
    case "boolean":
      return string2(String(self));
    case "symbol":
      return string2(String(self));
    case "string":
      return string2(self);
    case "undefined":
      return string2("undefined");
    case "function":
    case "object": {
      if (self === null) {
        return string2("null");
      } else if (self instanceof Date) {
        if (Number.isNaN(self.getTime())) {
          return string2("Invalid Date");
        }
        return hash(self.toISOString());
      } else if (self instanceof URL) {
        return hash(self.href);
      } else if (isHash(self)) {
        return self[symbol2]();
      } else {
        return random(self);
      }
    }
    default:
      throw new Error(`BUG: unhandled typeof ${typeof self} - please report an issue at https://github.com/Effect-TS/effect/issues`);
  }
}, "hash");
var random = /* @__PURE__ */ __name((self) => {
  if (!randomHashCache.has(self)) {
    randomHashCache.set(self, number2(Math.floor(Math.random() * Number.MAX_SAFE_INTEGER)));
  }
  return randomHashCache.get(self);
}, "random");
var combine2 = /* @__PURE__ */ __name((b) => (self) => self * 53 ^ b, "combine");
var optimize = /* @__PURE__ */ __name((n) => n & 3221225471 | n >>> 1 & 1073741824, "optimize");
var isHash = /* @__PURE__ */ __name((u) => hasProperty(u, symbol2), "isHash");
var number2 = /* @__PURE__ */ __name((n) => {
  if (n !== n || n === Infinity) {
    return 0;
  }
  let h = n | 0;
  if (h !== n) {
    h ^= n * 4294967295;
  }
  while (n > 4294967295) {
    h ^= n /= 4294967295;
  }
  return optimize(h);
}, "number");
var string2 = /* @__PURE__ */ __name((str) => {
  let h = 5381, i = str.length;
  while (i) {
    h = h * 33 ^ str.charCodeAt(--i);
  }
  return optimize(h);
}, "string");
var structureKeys = /* @__PURE__ */ __name((o, keys7) => {
  let h = 12289;
  for (let i = 0; i < keys7.length; i++) {
    h ^= pipe(string2(keys7[i]), combine2(hash(o[keys7[i]])));
  }
  return optimize(h);
}, "structureKeys");
var structure = /* @__PURE__ */ __name((o) => structureKeys(o, Object.keys(o)), "structure");
var array2 = /* @__PURE__ */ __name((arr) => {
  let h = 6151;
  for (let i = 0; i < arr.length; i++) {
    h = pipe(h, combine2(hash(arr[i])));
  }
  return optimize(h);
}, "array");
var cached = /* @__PURE__ */ __name(function() {
  if (arguments.length === 1) {
    const self2 = arguments[0];
    return function(hash3) {
      Object.defineProperty(self2, symbol2, {
        value() {
          return hash3;
        },
        enumerable: false
      });
      return hash3;
    };
  }
  const self = arguments[0];
  const hash2 = arguments[1];
  Object.defineProperty(self, symbol2, {
    value() {
      return hash2;
    },
    enumerable: false
  });
  return hash2;
}, "cached");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Equal.js
var symbol3 = /* @__PURE__ */ Symbol.for("effect/Equal");
function equals() {
  if (arguments.length === 1) {
    return (self) => compareBoth(self, arguments[0]);
  }
  return compareBoth(arguments[0], arguments[1]);
}
__name(equals, "equals");
function compareBoth(self, that) {
  if (self === that) {
    return true;
  }
  const selfType = typeof self;
  if (selfType !== typeof that) {
    return false;
  }
  if (selfType === "object" || selfType === "function") {
    if (self !== null && that !== null) {
      if (isEqual(self) && isEqual(that)) {
        if (hash(self) === hash(that) && self[symbol3](that)) {
          return true;
        } else {
          return structuralRegionState.enabled && structuralRegionState.tester ? structuralRegionState.tester(self, that) : false;
        }
      } else if (self instanceof Date && that instanceof Date) {
        const t1 = self.getTime();
        const t2 = that.getTime();
        return t1 === t2 || Number.isNaN(t1) && Number.isNaN(t2);
      } else if (self instanceof URL && that instanceof URL) {
        return self.href === that.href;
      }
    }
    if (structuralRegionState.enabled) {
      if (Array.isArray(self) && Array.isArray(that)) {
        return self.length === that.length && self.every((v, i) => compareBoth(v, that[i]));
      }
      if (Object.getPrototypeOf(self) === Object.prototype && Object.getPrototypeOf(that) === Object.prototype) {
        const keysSelf = Object.keys(self);
        const keysThat = Object.keys(that);
        if (keysSelf.length === keysThat.length) {
          for (const key of keysSelf) {
            if (!(key in that && compareBoth(self[key], that[key]))) {
              return structuralRegionState.tester ? structuralRegionState.tester(self, that) : false;
            }
          }
          return true;
        }
      }
      return structuralRegionState.tester ? structuralRegionState.tester(self, that) : false;
    }
  }
  return structuralRegionState.enabled && structuralRegionState.tester ? structuralRegionState.tester(self, that) : false;
}
__name(compareBoth, "compareBoth");
var isEqual = /* @__PURE__ */ __name((u) => hasProperty(u, symbol3), "isEqual");
var equivalence = /* @__PURE__ */ __name(() => equals, "equivalence");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Inspectable.js
var NodeInspectSymbol = /* @__PURE__ */ Symbol.for("nodejs.util.inspect.custom");
var toJSON = /* @__PURE__ */ __name((x) => {
  try {
    if (hasProperty(x, "toJSON") && isFunction2(x["toJSON"]) && x["toJSON"].length === 0) {
      return x.toJSON();
    } else if (Array.isArray(x)) {
      return x.map(toJSON);
    }
  } catch {
    return {};
  }
  return redact(x);
}, "toJSON");
var CIRCULAR = "[Circular]";
function formatDate(date) {
  try {
    return date.toISOString();
  } catch {
    return "Invalid Date";
  }
}
__name(formatDate, "formatDate");
function safeToString(input) {
  try {
    const s = input.toString();
    return typeof s === "string" ? s : String(s);
  } catch {
    return "[toString threw]";
  }
}
__name(safeToString, "safeToString");
function formatPropertyKey(name) {
  return isString(name) ? JSON.stringify(name) : String(name);
}
__name(formatPropertyKey, "formatPropertyKey");
function formatUnknown(input, options) {
  const space = options?.space ?? 0;
  const seen = /* @__PURE__ */ new WeakSet();
  const gap = !space ? "" : isNumber(space) ? " ".repeat(space) : space;
  const ind = /* @__PURE__ */ __name((d) => gap.repeat(d), "ind");
  const wrap = /* @__PURE__ */ __name((v, body) => {
    const ctor = v?.constructor;
    return ctor && ctor !== Object.prototype.constructor && ctor.name ? `${ctor.name}(${body})` : body;
  }, "wrap");
  const ownKeys = /* @__PURE__ */ __name((o) => {
    try {
      return Reflect.ownKeys(o);
    } catch {
      return ["[ownKeys threw]"];
    }
  }, "ownKeys");
  function go(v, d = 0) {
    if (Array.isArray(v)) {
      if (seen.has(v)) return CIRCULAR;
      seen.add(v);
      if (!gap || v.length <= 1) return `[${v.map((x) => go(x, d)).join(",")}]`;
      const inner = v.map((x) => go(x, d + 1)).join(",\n" + ind(d + 1));
      return `[
${ind(d + 1)}${inner}
${ind(d)}]`;
    }
    if (isDate(v)) return formatDate(v);
    if (!options?.ignoreToString && hasProperty(v, "toString") && isFunction2(v["toString"]) && v["toString"] !== Object.prototype.toString && v["toString"] !== Array.prototype.toString) {
      const s = safeToString(v);
      if (v instanceof Error && v.cause) {
        return `${s} (cause: ${go(v.cause, d)})`;
      }
      return s;
    }
    if (isString(v)) return JSON.stringify(v);
    if (isNumber(v) || v == null || isBoolean(v) || isSymbol(v)) return String(v);
    if (isBigInt(v)) return String(v) + "n";
    if (v instanceof Set || v instanceof Map) {
      if (seen.has(v)) return CIRCULAR;
      seen.add(v);
      return `${v.constructor.name}(${go(Array.from(v), d)})`;
    }
    if (isObject(v)) {
      if (seen.has(v)) return CIRCULAR;
      seen.add(v);
      const keys7 = ownKeys(v);
      if (!gap || keys7.length <= 1) {
        const body2 = `{${keys7.map((k) => `${formatPropertyKey(k)}:${go(v[k], d)}`).join(",")}}`;
        return wrap(v, body2);
      }
      const body = `{
${keys7.map((k) => `${ind(d + 1)}${formatPropertyKey(k)}: ${go(v[k], d + 1)}`).join(",\n")}
${ind(d)}}`;
      return wrap(v, body);
    }
    return String(v);
  }
  __name(go, "go");
  return go(input, 0);
}
__name(formatUnknown, "formatUnknown");
var format = /* @__PURE__ */ __name((x) => JSON.stringify(x, null, 2), "format");
var BaseProto = {
  toJSON() {
    return toJSON(this);
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  toString() {
    return format(this.toJSON());
  }
};
var Class = class {
  static {
    __name(this, "Class");
  }
  /**
   * @since 2.0.0
   */
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  /**
   * @since 2.0.0
   */
  toString() {
    return format(this.toJSON());
  }
};
var toStringUnknown = /* @__PURE__ */ __name((u, whitespace = 2) => {
  if (typeof u === "string") {
    return u;
  }
  try {
    return typeof u === "object" ? stringifyCircular(u, whitespace) : String(u);
  } catch {
    return String(u);
  }
}, "toStringUnknown");
var stringifyCircular = /* @__PURE__ */ __name((obj, whitespace) => {
  let cache = [];
  const retVal = JSON.stringify(obj, (_key, value2) => typeof value2 === "object" && value2 !== null ? cache.includes(value2) ? void 0 : cache.push(value2) && (redactableState.fiberRefs !== void 0 && isRedactable(value2) ? value2[symbolRedactable](redactableState.fiberRefs) : value2) : value2, whitespace);
  cache = void 0;
  return retVal;
}, "stringifyCircular");
var symbolRedactable = /* @__PURE__ */ Symbol.for("effect/Inspectable/Redactable");
var isRedactable = /* @__PURE__ */ __name((u) => typeof u === "object" && u !== null && symbolRedactable in u, "isRedactable");
var redactableState = /* @__PURE__ */ globalValue("effect/Inspectable/redactableState", () => ({
  fiberRefs: void 0
}));
var withRedactableContext = /* @__PURE__ */ __name((context11, f) => {
  const prev = redactableState.fiberRefs;
  redactableState.fiberRefs = context11;
  try {
    return f();
  } finally {
    redactableState.fiberRefs = prev;
  }
}, "withRedactableContext");
var redact = /* @__PURE__ */ __name((u) => {
  if (isRedactable(u) && redactableState.fiberRefs !== void 0) {
    return u[symbolRedactable](redactableState.fiberRefs);
  }
  return u;
}, "redact");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Pipeable.js
var pipeArguments = /* @__PURE__ */ __name((self, args2) => {
  switch (args2.length) {
    case 0:
      return self;
    case 1:
      return args2[0](self);
    case 2:
      return args2[1](args2[0](self));
    case 3:
      return args2[2](args2[1](args2[0](self)));
    case 4:
      return args2[3](args2[2](args2[1](args2[0](self))));
    case 5:
      return args2[4](args2[3](args2[2](args2[1](args2[0](self)))));
    case 6:
      return args2[5](args2[4](args2[3](args2[2](args2[1](args2[0](self))))));
    case 7:
      return args2[6](args2[5](args2[4](args2[3](args2[2](args2[1](args2[0](self)))))));
    case 8:
      return args2[7](args2[6](args2[5](args2[4](args2[3](args2[2](args2[1](args2[0](self))))))));
    case 9:
      return args2[8](args2[7](args2[6](args2[5](args2[4](args2[3](args2[2](args2[1](args2[0](self)))))))));
    default: {
      let ret = self;
      for (let i = 0, len = args2.length; i < len; i++) {
        ret = args2[i](ret);
      }
      return ret;
    }
  }
}, "pipeArguments");
var Prototype = {
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var Base = /* @__PURE__ */ (function() {
  function PipeableBase() {
  }
  __name(PipeableBase, "PipeableBase");
  PipeableBase.prototype = Prototype;
  return PipeableBase;
})();
var Class2 = /* @__PURE__ */ __name((klass) => klass ? class extends klass {
  pipe() {
    return pipeArguments(this, arguments);
  }
} : Base, "Class");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/effect.js
var OP_ASYNC = "Async";
var OP_COMMIT = "Commit";
var OP_FAILURE = "Failure";
var OP_ON_FAILURE = "OnFailure";
var OP_ON_SUCCESS = "OnSuccess";
var OP_ON_SUCCESS_AND_FAILURE = "OnSuccessAndFailure";
var OP_SUCCESS = "Success";
var OP_SYNC = "Sync";
var OP_TAG = "Tag";
var OP_UPDATE_RUNTIME_FLAGS = "UpdateRuntimeFlags";
var OP_WHILE = "While";
var OP_ITERATOR = "Iterator";
var OP_WITH_RUNTIME = "WithRuntime";
var OP_YIELD = "Yield";
var OP_REVERT_FLAGS = "RevertFlags";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/version.js
var moduleVersion = "3.19.16";
var getCurrentVersion = /* @__PURE__ */ __name(() => moduleVersion, "getCurrentVersion");
var setCurrentVersion = /* @__PURE__ */ __name((version) => {
  moduleVersion = version;
}, "setCurrentVersion");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/effectable.js
var EffectTypeId = /* @__PURE__ */ Symbol.for("effect/Effect");
var StreamTypeId = /* @__PURE__ */ Symbol.for("effect/Stream");
var SinkTypeId = /* @__PURE__ */ Symbol.for("effect/Sink");
var ChannelTypeId = /* @__PURE__ */ Symbol.for("effect/Channel");
var effectVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  _V: /* @__PURE__ */ getCurrentVersion()
};
var sinkVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _L: /* @__PURE__ */ __name((_) => _, "_L"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var channelVariance = {
  /* c8 ignore next */
  _Env: /* @__PURE__ */ __name((_) => _, "_Env"),
  /* c8 ignore next */
  _InErr: /* @__PURE__ */ __name((_) => _, "_InErr"),
  /* c8 ignore next */
  _InElem: /* @__PURE__ */ __name((_) => _, "_InElem"),
  /* c8 ignore next */
  _InDone: /* @__PURE__ */ __name((_) => _, "_InDone"),
  /* c8 ignore next */
  _OutErr: /* @__PURE__ */ __name((_) => _, "_OutErr"),
  /* c8 ignore next */
  _OutElem: /* @__PURE__ */ __name((_) => _, "_OutElem"),
  /* c8 ignore next */
  _OutDone: /* @__PURE__ */ __name((_) => _, "_OutDone")
};
var EffectPrototype = {
  [EffectTypeId]: effectVariance,
  [StreamTypeId]: effectVariance,
  [SinkTypeId]: sinkVariance,
  [ChannelTypeId]: channelVariance,
  [symbol3](that) {
    return this === that;
  },
  [symbol2]() {
    return cached(this, random(this));
  },
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var StructuralPrototype = {
  [symbol2]() {
    return cached(this, structure(this));
  },
  [symbol3](that) {
    const selfKeys = Object.keys(this);
    const thatKeys = Object.keys(that);
    if (selfKeys.length !== thatKeys.length) {
      return false;
    }
    for (const key of selfKeys) {
      if (!(key in that && equals(this[key], that[key]))) {
        return false;
      }
    }
    return true;
  }
};
var CommitPrototype = {
  ...EffectPrototype,
  _op: OP_COMMIT
};
var StructuralCommitPrototype = {
  ...CommitPrototype,
  ...StructuralPrototype
};
var Base2 = /* @__PURE__ */ (function() {
  function Base4() {
  }
  __name(Base4, "Base");
  Base4.prototype = CommitPrototype;
  return Base4;
})();
var StructuralBase = /* @__PURE__ */ (function() {
  function Base4() {
  }
  __name(Base4, "Base");
  Base4.prototype = StructuralCommitPrototype;
  return Base4;
})();

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/option.js
var TypeId = /* @__PURE__ */ Symbol.for("effect/Option");
var CommonProto = {
  ...EffectPrototype,
  [TypeId]: {
    _A: /* @__PURE__ */ __name((_) => _, "_A")
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  toString() {
    return format(this.toJSON());
  }
};
var SomeProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(CommonProto), {
  _tag: "Some",
  _op: "Some",
  [symbol3](that) {
    return isOption(that) && isSome(that) && equals(this.value, that.value);
  },
  [symbol2]() {
    return cached(this, combine2(hash(this._tag))(hash(this.value)));
  },
  toJSON() {
    return {
      _id: "Option",
      _tag: this._tag,
      value: toJSON(this.value)
    };
  }
});
var NoneHash = /* @__PURE__ */ hash("None");
var NoneProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(CommonProto), {
  _tag: "None",
  _op: "None",
  [symbol3](that) {
    return isOption(that) && isNone(that);
  },
  [symbol2]() {
    return NoneHash;
  },
  toJSON() {
    return {
      _id: "Option",
      _tag: this._tag
    };
  }
});
var isOption = /* @__PURE__ */ __name((input) => hasProperty(input, TypeId), "isOption");
var isNone = /* @__PURE__ */ __name((fa) => fa._tag === "None", "isNone");
var isSome = /* @__PURE__ */ __name((fa) => fa._tag === "Some", "isSome");
var none = /* @__PURE__ */ Object.create(NoneProto);
var some2 = /* @__PURE__ */ __name((value2) => {
  const a = Object.create(SomeProto);
  a.value = value2;
  return a;
}, "some");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/either.js
var TypeId2 = /* @__PURE__ */ Symbol.for("effect/Either");
var CommonProto2 = {
  ...EffectPrototype,
  [TypeId2]: {
    _R: /* @__PURE__ */ __name((_) => _, "_R")
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  toString() {
    return format(this.toJSON());
  }
};
var RightProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(CommonProto2), {
  _tag: "Right",
  _op: "Right",
  [symbol3](that) {
    return isEither(that) && isRight(that) && equals(this.right, that.right);
  },
  [symbol2]() {
    return combine2(hash(this._tag))(hash(this.right));
  },
  toJSON() {
    return {
      _id: "Either",
      _tag: this._tag,
      right: toJSON(this.right)
    };
  }
});
var LeftProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(CommonProto2), {
  _tag: "Left",
  _op: "Left",
  [symbol3](that) {
    return isEither(that) && isLeft(that) && equals(this.left, that.left);
  },
  [symbol2]() {
    return combine2(hash(this._tag))(hash(this.left));
  },
  toJSON() {
    return {
      _id: "Either",
      _tag: this._tag,
      left: toJSON(this.left)
    };
  }
});
var isEither = /* @__PURE__ */ __name((input) => hasProperty(input, TypeId2), "isEither");
var isLeft = /* @__PURE__ */ __name((ma) => ma._tag === "Left", "isLeft");
var isRight = /* @__PURE__ */ __name((ma) => ma._tag === "Right", "isRight");
var left = /* @__PURE__ */ __name((left3) => {
  const a = Object.create(LeftProto);
  a.left = left3;
  return a;
}, "left");
var right = /* @__PURE__ */ __name((right3) => {
  const a = Object.create(RightProto);
  a.right = right3;
  return a;
}, "right");
var getLeft = /* @__PURE__ */ __name((self) => isRight(self) ? none : some2(self.left), "getLeft");
var getRight = /* @__PURE__ */ __name((self) => isLeft(self) ? none : some2(self.right), "getRight");
var fromOption = /* @__PURE__ */ dual(2, (self, onNone) => isNone(self) ? left(onNone()) : right(self.value));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Either.js
var TypeId3 = TypeId2;
var right2 = right;
var void_ = /* @__PURE__ */ right2(void 0);
var left2 = left;
var fromNullable = /* @__PURE__ */ dual(2, (self, onNullable) => self == null ? left2(onNullable(self)) : right2(self));
var fromOption2 = fromOption;
var try_ = /* @__PURE__ */ __name((evaluate3) => {
  if (isFunction2(evaluate3)) {
    try {
      return right2(evaluate3());
    } catch (e) {
      return left2(e);
    }
  } else {
    try {
      return right2(evaluate3.try());
    } catch (e) {
      return left2(evaluate3.catch(e));
    }
  }
}, "try_");
var isEither2 = isEither;
var isLeft2 = isLeft;
var isRight2 = isRight;
var getRight2 = getRight;
var getLeft2 = getLeft;
var getEquivalence = /* @__PURE__ */ __name(({
  left: left3,
  right: right3
}) => make((x, y) => isLeft2(x) ? isLeft2(y) && left3(x.left, y.left) : isRight2(y) && right3(x.right, y.right)), "getEquivalence");
var mapBoth = /* @__PURE__ */ dual(2, (self, {
  onLeft,
  onRight
}) => isLeft2(self) ? left2(onLeft(self.left)) : right2(onRight(self.right)));
var mapLeft = /* @__PURE__ */ dual(2, (self, f) => isLeft2(self) ? left2(f(self.left)) : right2(self.right));
var map = /* @__PURE__ */ dual(2, (self, f) => isRight2(self) ? right2(f(self.right)) : left2(self.left));
var match = /* @__PURE__ */ dual(2, (self, {
  onLeft,
  onRight
}) => isLeft2(self) ? onLeft(self.left) : onRight(self.right));
var liftPredicate = /* @__PURE__ */ dual(3, (a, predicate, orLeftWith) => predicate(a) ? right2(a) : left2(orLeftWith(a)));
var filterOrLeft = /* @__PURE__ */ dual(3, (self, predicate, orLeftWith) => flatMap(self, (r) => predicate(r) ? right2(r) : left2(orLeftWith(r))));
var merge = /* @__PURE__ */ match({
  onLeft: identity,
  onRight: identity
});
var getOrElse = /* @__PURE__ */ dual(2, (self, onLeft) => isLeft2(self) ? onLeft(self.left) : self.right);
var getOrNull = /* @__PURE__ */ getOrElse(constNull);
var getOrUndefined = /* @__PURE__ */ getOrElse(constUndefined);
var getOrThrowWith = /* @__PURE__ */ dual(2, (self, onLeft) => {
  if (isRight2(self)) {
    return self.right;
  }
  throw onLeft(self.left);
});
var getOrThrow = /* @__PURE__ */ getOrThrowWith(() => new Error("getOrThrow called on a Left"));
var orElse = /* @__PURE__ */ dual(2, (self, that) => isLeft2(self) ? that(self.left) : right2(self.right));
var flatMap = /* @__PURE__ */ dual(2, (self, f) => isLeft2(self) ? left2(self.left) : f(self.right));
var andThen = /* @__PURE__ */ dual(2, (self, f) => flatMap(self, (a) => {
  const b = isFunction2(f) ? f(a) : f;
  return isEither2(b) ? b : right2(b);
}));
var zipWith = /* @__PURE__ */ dual(3, (self, that, f) => flatMap(self, (r) => map(that, (r2) => f(r, r2))));
var ap = /* @__PURE__ */ dual(2, (self, that) => zipWith(self, that, (f, a) => f(a)));
var all3 = /* @__PURE__ */ __name((input) => {
  if (Symbol.iterator in input) {
    const out2 = [];
    for (const e of input) {
      if (isLeft2(e)) {
        return e;
      }
      out2.push(e.right);
    }
    return right2(out2);
  }
  const out = {};
  for (const key of Object.keys(input)) {
    const e = input[key];
    if (isLeft2(e)) {
      return e;
    }
    out[key] = e.right;
  }
  return right2(out);
}, "all");
var flip2 = /* @__PURE__ */ __name((self) => isLeft2(self) ? right2(self.left) : left2(self.right), "flip");
var adapter2 = /* @__PURE__ */ adapter();
var gen = /* @__PURE__ */ __name((...args2) => {
  const f = args2.length === 1 ? args2[0] : args2[1].bind(args2[0]);
  const iterator = f(adapter2);
  let state = iterator.next();
  while (!state.done) {
    const current2 = isGenKind(state.value) ? state.value.value : yieldWrapGet(state.value);
    if (isLeft2(current2)) {
      return current2;
    }
    state = iterator.next(current2.right);
  }
  return right2(state.value);
}, "gen");
var Do = /* @__PURE__ */ right2({});
var bind2 = /* @__PURE__ */ bind(map, flatMap);
var bindTo2 = /* @__PURE__ */ bindTo(map);
var let_2 = /* @__PURE__ */ let_(map);
var transposeOption = /* @__PURE__ */ __name((self) => {
  return isNone(self) ? right2(none) : map(self.value, some2);
}, "transposeOption");
var transposeMapOption = /* @__PURE__ */ dual(2, (self, f) => isNone(self) ? right2(none) : map(f(self.value), some2));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/array.js
var isNonEmptyArray = /* @__PURE__ */ __name((self) => self.length > 0, "isNonEmptyArray");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Order.js
var make2 = /* @__PURE__ */ __name((compare) => (self, that) => self === that ? 0 : compare(self, that), "make");
var string3 = /* @__PURE__ */ make2((self, that) => self < that ? -1 : 1);
var number3 = /* @__PURE__ */ make2((self, that) => self < that ? -1 : 1);
var boolean2 = /* @__PURE__ */ make2((self, that) => self < that ? -1 : 1);
var bigint2 = /* @__PURE__ */ make2((self, that) => self < that ? -1 : 1);
var reverse = /* @__PURE__ */ __name((O) => make2((self, that) => O(that, self)), "reverse");
var combine3 = /* @__PURE__ */ dual(2, (self, that) => make2((a1, a2) => {
  const out = self(a1, a2);
  if (out !== 0) {
    return out;
  }
  return that(a1, a2);
}));
var combineMany2 = /* @__PURE__ */ dual(2, (self, collection) => make2((a1, a2) => {
  let out = self(a1, a2);
  if (out !== 0) {
    return out;
  }
  for (const O of collection) {
    out = O(a1, a2);
    if (out !== 0) {
      return out;
    }
  }
  return out;
}));
var empty = /* @__PURE__ */ __name(() => make2(() => 0), "empty");
var combineAll2 = /* @__PURE__ */ __name((collection) => combineMany2(empty(), collection), "combineAll");
var mapInput3 = /* @__PURE__ */ dual(2, (self, f) => make2((b1, b2) => self(f(b1), f(b2))));
var Date3 = /* @__PURE__ */ mapInput3(number3, (date) => date.getTime());
var product3 = /* @__PURE__ */ dual(2, (self, that) => make2(([xa, xb], [ya, yb]) => {
  const o = self(xa, ya);
  return o !== 0 ? o : that(xb, yb);
}));
var all4 = /* @__PURE__ */ __name((collection) => {
  return make2((x, y) => {
    const len = Math.min(x.length, y.length);
    let collectionLength = 0;
    for (const O of collection) {
      if (collectionLength >= len) {
        break;
      }
      const o = O(x[collectionLength], y[collectionLength]);
      if (o !== 0) {
        return o;
      }
      collectionLength++;
    }
    return 0;
  });
}, "all");
var productMany3 = /* @__PURE__ */ dual(2, (self, collection) => {
  const O = all4(collection);
  return make2((x, y) => {
    const o = self(x[0], y[0]);
    return o !== 0 ? o : O(x.slice(1), y.slice(1));
  });
});
var tuple3 = /* @__PURE__ */ __name((...elements) => all4(elements), "tuple");
var array3 = /* @__PURE__ */ __name((O) => make2((self, that) => {
  const aLen = self.length;
  const bLen = that.length;
  const len = Math.min(aLen, bLen);
  for (let i = 0; i < len; i++) {
    const o = O(self[i], that[i]);
    if (o !== 0) {
      return o;
    }
  }
  return number3(aLen, bLen);
}), "array");
var struct3 = /* @__PURE__ */ __name((fields) => {
  const keys7 = Object.keys(fields);
  return make2((self, that) => {
    for (const key of keys7) {
      const o = fields[key](self[key], that[key]);
      if (o !== 0) {
        return o;
      }
    }
    return 0;
  });
}, "struct");
var lessThan = /* @__PURE__ */ __name((O) => dual(2, (self, that) => O(self, that) === -1), "lessThan");
var greaterThan = /* @__PURE__ */ __name((O) => dual(2, (self, that) => O(self, that) === 1), "greaterThan");
var lessThanOrEqualTo = /* @__PURE__ */ __name((O) => dual(2, (self, that) => O(self, that) !== 1), "lessThanOrEqualTo");
var greaterThanOrEqualTo = /* @__PURE__ */ __name((O) => dual(2, (self, that) => O(self, that) !== -1), "greaterThanOrEqualTo");
var min = /* @__PURE__ */ __name((O) => dual(2, (self, that) => self === that || O(self, that) < 1 ? self : that), "min");
var max = /* @__PURE__ */ __name((O) => dual(2, (self, that) => self === that || O(self, that) > -1 ? self : that), "max");
var clamp = /* @__PURE__ */ __name((O) => dual(2, (self, options) => min(O)(options.maximum, max(O)(options.minimum, self))), "clamp");
var between = /* @__PURE__ */ __name((O) => dual(2, (self, options) => !lessThan(O)(self, options.minimum) && !greaterThan(O)(self, options.maximum)), "between");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Option.js
var TypeId4 = /* @__PURE__ */ Symbol.for("effect/Option");
var none2 = /* @__PURE__ */ __name(() => none, "none");
var some3 = some2;
var isOption2 = isOption;
var isNone2 = isNone;
var isSome2 = isSome;
var match2 = /* @__PURE__ */ dual(2, (self, {
  onNone,
  onSome
}) => isNone2(self) ? onNone() : onSome(self.value));
var toRefinement = /* @__PURE__ */ __name((f) => (a) => isSome2(f(a)), "toRefinement");
var fromIterable = /* @__PURE__ */ __name((collection) => {
  for (const a of collection) {
    return some3(a);
  }
  return none2();
}, "fromIterable");
var getRight3 = getRight;
var getLeft3 = getLeft;
var getOrElse2 = /* @__PURE__ */ dual(2, (self, onNone) => isNone2(self) ? onNone() : self.value);
var orElse2 = /* @__PURE__ */ dual(2, (self, that) => isNone2(self) ? that() : self);
var orElseSome = /* @__PURE__ */ dual(2, (self, onNone) => isNone2(self) ? some3(onNone()) : self);
var orElseEither = /* @__PURE__ */ dual(2, (self, that) => isNone2(self) ? map2(that(), right) : map2(self, left));
var firstSomeOf = /* @__PURE__ */ __name((collection) => {
  let out = none2();
  for (out of collection) {
    if (isSome2(out)) {
      return out;
    }
  }
  return out;
}, "firstSomeOf");
var fromNullable2 = /* @__PURE__ */ __name((nullableValue) => nullableValue == null ? none2() : some3(nullableValue), "fromNullable");
var liftNullable = /* @__PURE__ */ __name((f) => (...a) => fromNullable2(f(...a)), "liftNullable");
var getOrNull2 = /* @__PURE__ */ getOrElse2(constNull);
var getOrUndefined2 = /* @__PURE__ */ getOrElse2(constUndefined);
var liftThrowable = /* @__PURE__ */ __name((f) => (...a) => {
  try {
    return some3(f(...a));
  } catch {
    return none2();
  }
}, "liftThrowable");
var getOrThrowWith2 = /* @__PURE__ */ dual(2, (self, onNone) => {
  if (isSome2(self)) {
    return self.value;
  }
  throw onNone();
});
var getOrThrow2 = /* @__PURE__ */ getOrThrowWith2(() => new Error("getOrThrow called on a None"));
var map2 = /* @__PURE__ */ dual(2, (self, f) => isNone2(self) ? none2() : some3(f(self.value)));
var as = /* @__PURE__ */ dual(2, (self, b) => map2(self, () => b));
var asVoid = /* @__PURE__ */ as(void 0);
var void_2 = /* @__PURE__ */ some3(void 0);
var flatMap2 = /* @__PURE__ */ dual(2, (self, f) => isNone2(self) ? none2() : f(self.value));
var andThen2 = /* @__PURE__ */ dual(2, (self, f) => flatMap2(self, (a) => {
  const b = isFunction(f) ? f(a) : f;
  return isOption2(b) ? b : some3(b);
}));
var flatMapNullable = /* @__PURE__ */ dual(2, (self, f) => isNone2(self) ? none2() : fromNullable2(f(self.value)));
var flatten = /* @__PURE__ */ flatMap2(identity);
var zipRight = /* @__PURE__ */ dual(2, (self, that) => flatMap2(self, () => that));
var zipLeft = /* @__PURE__ */ dual(2, (self, that) => tap(self, () => that));
var composeK = /* @__PURE__ */ dual(2, (afb, bfc) => (a) => flatMap2(afb(a), bfc));
var tap = /* @__PURE__ */ dual(2, (self, f) => flatMap2(self, (a) => map2(f(a), () => a)));
var product4 = /* @__PURE__ */ __name((self, that) => isSome2(self) && isSome2(that) ? some3([self.value, that.value]) : none2(), "product");
var productMany4 = /* @__PURE__ */ __name((self, collection) => {
  if (isNone2(self)) {
    return none2();
  }
  const out = [self.value];
  for (const o of collection) {
    if (isNone2(o)) {
      return none2();
    }
    out.push(o.value);
  }
  return some3(out);
}, "productMany");
var all5 = /* @__PURE__ */ __name((input) => {
  if (Symbol.iterator in input) {
    const out2 = [];
    for (const o of input) {
      if (isNone2(o)) {
        return none2();
      }
      out2.push(o.value);
    }
    return some3(out2);
  }
  const out = {};
  for (const key of Object.keys(input)) {
    const o = input[key];
    if (isNone2(o)) {
      return none2();
    }
    out[key] = o.value;
  }
  return some3(out);
}, "all");
var zipWith2 = /* @__PURE__ */ dual(3, (self, that, f) => map2(product4(self, that), ([a, b]) => f(a, b)));
var ap2 = /* @__PURE__ */ dual(2, (self, that) => zipWith2(self, that, (f, a) => f(a)));
var reduceCompact = /* @__PURE__ */ dual(3, (self, b, f) => {
  let out = b;
  for (const oa of self) {
    if (isSome2(oa)) {
      out = f(out, oa.value);
    }
  }
  return out;
});
var toArray = /* @__PURE__ */ __name((self) => isNone2(self) ? [] : [self.value], "toArray");
var partitionMap = /* @__PURE__ */ dual(2, (self, f) => {
  if (isNone2(self)) {
    return [none2(), none2()];
  }
  const e = f(self.value);
  return isLeft(e) ? [some3(e.left), none2()] : [none2(), some3(e.right)];
});
var filterMap = flatMap2;
var filter = /* @__PURE__ */ dual(2, (self, predicate) => filterMap(self, (b) => predicate(b) ? some2(b) : none));
var getEquivalence2 = /* @__PURE__ */ __name((isEquivalent) => make((x, y) => isNone2(x) ? isNone2(y) : isNone2(y) ? false : isEquivalent(x.value, y.value)), "getEquivalence");
var getOrder = /* @__PURE__ */ __name((O) => make2((self, that) => isSome2(self) ? isSome2(that) ? O(self.value, that.value) : 1 : -1), "getOrder");
var lift2 = /* @__PURE__ */ __name((f) => dual(2, (self, that) => zipWith2(self, that, f)), "lift2");
var liftPredicate2 = /* @__PURE__ */ dual(2, (b, predicate) => predicate(b) ? some3(b) : none2());
var containsWith = /* @__PURE__ */ __name((isEquivalent) => dual(2, (self, a) => isNone2(self) ? false : isEquivalent(self.value, a)), "containsWith");
var _equivalence = /* @__PURE__ */ equivalence();
var contains = /* @__PURE__ */ containsWith(_equivalence);
var exists = /* @__PURE__ */ dual(2, (self, refinement) => isNone2(self) ? false : refinement(self.value));
var bindTo3 = /* @__PURE__ */ bindTo(map2);
var let_3 = /* @__PURE__ */ let_(map2);
var bind3 = /* @__PURE__ */ bind(map2, flatMap2);
var Do2 = /* @__PURE__ */ some3({});
var adapter3 = /* @__PURE__ */ adapter();
var gen2 = /* @__PURE__ */ __name((...args2) => {
  const f = args2.length === 1 ? args2[0] : args2[1].bind(args2[0]);
  const iterator = f(adapter3);
  let state = iterator.next();
  while (!state.done) {
    const current2 = isGenKind(state.value) ? state.value.value : yieldWrapGet(state.value);
    if (isNone2(current2)) {
      return current2;
    }
    state = iterator.next(current2.value);
  }
  return some3(state.value);
}, "gen");
var mergeWith = /* @__PURE__ */ __name((f) => (o1, o2) => {
  if (isNone2(o1)) {
    return o2;
  } else if (isNone2(o2)) {
    return o1;
  }
  return some3(f(o1.value, o2.value));
}, "mergeWith");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Tuple.js
var make3 = /* @__PURE__ */ __name((...elements) => elements, "make");
var getFirst = /* @__PURE__ */ __name((self) => self[0], "getFirst");
var getSecond = /* @__PURE__ */ __name((self) => self[1], "getSecond");
var map3 = /* @__PURE__ */ dual(2, (self, fn2) => self.map((element) => fn2(element)));
var mapBoth2 = /* @__PURE__ */ dual(2, (self, {
  onFirst,
  onSecond
}) => [onFirst(self[0]), onSecond(self[1])]);
var mapFirst = /* @__PURE__ */ dual(2, (self, f) => [f(self[0]), self[1]]);
var mapSecond = /* @__PURE__ */ dual(2, (self, f) => [self[0], f(self[1])]);
var swap = /* @__PURE__ */ __name((self) => [self[1], self[0]], "swap");
var getEquivalence3 = tuple;
var getOrder2 = tuple3;
var appendElement = /* @__PURE__ */ dual(2, (self, that) => [...self, that]);
var at = /* @__PURE__ */ dual(2, (self, index) => self[index]);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Iterable.js
var makeBy = /* @__PURE__ */ __name((f, options) => {
  const max10 = options?.length !== void 0 ? Math.max(1, Math.floor(options.length)) : Infinity;
  return {
    [Symbol.iterator]() {
      let i = 0;
      return {
        next() {
          if (i < max10) {
            return {
              value: f(i++),
              done: false
            };
          }
          return {
            done: true,
            value: void 0
          };
        }
      };
    }
  };
}, "makeBy");
var range = /* @__PURE__ */ __name((start3, end6) => {
  if (end6 === void 0) {
    return makeBy((i) => start3 + i);
  }
  return makeBy((i) => start3 + i, {
    length: start3 <= end6 ? end6 - start3 + 1 : 1
  });
}, "range");
var replicate = /* @__PURE__ */ dual(2, (a, n) => makeBy(() => a, {
  length: n
}));
var fromRecord = /* @__PURE__ */ __name((self) => ({
  *[Symbol.iterator]() {
    for (const key in self) {
      if (Object.prototype.hasOwnProperty.call(self, key)) {
        yield [key, self[key]];
      }
    }
  }
}), "fromRecord");
var prepend = /* @__PURE__ */ dual(2, (self, head10) => prependAll(self, [head10]));
var prependAll = /* @__PURE__ */ dual(2, (self, that) => appendAll(that, self));
var append = /* @__PURE__ */ dual(2, (self, last7) => appendAll(self, [last7]));
var appendAll = /* @__PURE__ */ dual(2, (self, that) => ({
  [Symbol.iterator]() {
    const iterA = self[Symbol.iterator]();
    let doneA = false;
    let iterB;
    return {
      next() {
        if (!doneA) {
          const r = iterA.next();
          if (r.done) {
            doneA = true;
            iterB = that[Symbol.iterator]();
            return iterB.next();
          }
          return r;
        }
        return iterB.next();
      }
    };
  }
}));
var scan = /* @__PURE__ */ dual(3, (self, b, f) => ({
  [Symbol.iterator]() {
    let acc = b;
    let iterator;
    function next4() {
      if (iterator === void 0) {
        iterator = self[Symbol.iterator]();
        return {
          done: false,
          value: acc
        };
      }
      const result = iterator.next();
      if (result.done) {
        return result;
      }
      acc = f(acc, result.value);
      return {
        done: false,
        value: acc
      };
    }
    __name(next4, "next");
    return {
      next: next4
    };
  }
}));
var isEmpty = /* @__PURE__ */ __name((self) => {
  const iterator = self[Symbol.iterator]();
  return iterator.next().done === true;
}, "isEmpty");
var size = /* @__PURE__ */ __name((self) => {
  const iterator = self[Symbol.iterator]();
  let count5 = 0;
  while (!iterator.next().done) {
    count5++;
  }
  return count5;
}, "size");
var head = /* @__PURE__ */ __name((self) => {
  const iterator = self[Symbol.iterator]();
  const result = iterator.next();
  return result.done ? none2() : some3(result.value);
}, "head");
var unsafeHead = /* @__PURE__ */ __name((self) => {
  const iterator = self[Symbol.iterator]();
  const result = iterator.next();
  if (result.done) throw new Error("unsafeHead: empty iterable");
  return result.value;
}, "unsafeHead");
var take = /* @__PURE__ */ dual(2, (self, n) => ({
  [Symbol.iterator]() {
    let i = 0;
    const iterator = self[Symbol.iterator]();
    return {
      next() {
        if (i < n) {
          i++;
          return iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var takeWhile = /* @__PURE__ */ dual(2, (self, predicate) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done || !predicate(result.value, i++)) {
          return {
            done: true,
            value: void 0
          };
        }
        return result;
      }
    };
  }
}));
var drop = /* @__PURE__ */ dual(2, (self, n) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        while (i < n) {
          const result = iterator.next();
          if (result.done) {
            return {
              done: true,
              value: void 0
            };
          }
          i++;
        }
        return iterator.next();
      }
    };
  }
}));
var findFirst = /* @__PURE__ */ dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some3(a);
      }
    } else {
      if (isSome2(o)) {
        return o;
      }
    }
    i++;
  }
  return none2();
});
var findLast = /* @__PURE__ */ dual(2, (self, f) => {
  let i = 0;
  let last7 = none2();
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        last7 = some3(a);
      }
    } else {
      if (isSome2(o)) {
        last7 = o;
      }
    }
    i++;
  }
  return last7;
});
var zip = /* @__PURE__ */ dual(2, (self, that) => zipWith3(self, that, make3));
var zipWith3 = /* @__PURE__ */ dual(3, (self, that, f) => ({
  [Symbol.iterator]() {
    const selfIterator = self[Symbol.iterator]();
    const thatIterator = that[Symbol.iterator]();
    return {
      next() {
        const selfResult = selfIterator.next();
        const thatResult = thatIterator.next();
        if (selfResult.done || thatResult.done) {
          return {
            done: true,
            value: void 0
          };
        }
        return {
          done: false,
          value: f(selfResult.value, thatResult.value)
        };
      }
    };
  }
}));
var intersperse = /* @__PURE__ */ dual(2, (self, middle) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let next4 = iterator.next();
    let emitted = false;
    return {
      next() {
        if (next4.done) {
          return next4;
        } else if (emitted) {
          emitted = false;
          return {
            done: false,
            value: middle
          };
        }
        emitted = true;
        const result = next4;
        next4 = iterator.next();
        return result;
      }
    };
  }
}));
var containsWith2 = /* @__PURE__ */ __name((isEquivalent) => dual(2, (self, a) => {
  for (const i of self) {
    if (isEquivalent(a, i)) {
      return true;
    }
  }
  return false;
}), "containsWith");
var _equivalence2 = /* @__PURE__ */ equivalence();
var contains2 = /* @__PURE__ */ containsWith2(_equivalence2);
var chunksOf = /* @__PURE__ */ dual(2, (self, n) => {
  const safeN = Math.max(1, Math.floor(n));
  return {
    [Symbol.iterator]() {
      let iterator = self[Symbol.iterator]();
      return {
        next() {
          if (iterator === void 0) {
            return {
              done: true,
              value: void 0
            };
          }
          const chunk4 = [];
          for (let i = 0; i < safeN; i++) {
            const result = iterator.next();
            if (result.done) {
              iterator = void 0;
              return chunk4.length === 0 ? {
                done: true,
                value: void 0
              } : {
                done: false,
                value: chunk4
              };
            }
            chunk4.push(result.value);
          }
          return {
            done: false,
            value: chunk4
          };
        }
      };
    }
  };
});
var groupWith = /* @__PURE__ */ dual(2, (self, isEquivalent) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let nextResult;
    return {
      next() {
        let result;
        if (nextResult !== void 0) {
          if (nextResult.done) {
            return {
              done: true,
              value: void 0
            };
          }
          result = nextResult;
          nextResult = void 0;
        } else {
          result = iterator.next();
          if (result.done) {
            return {
              done: true,
              value: void 0
            };
          }
        }
        const chunk4 = [result.value];
        while (true) {
          const next4 = iterator.next();
          if (next4.done || !isEquivalent(result.value, next4.value)) {
            nextResult = next4;
            return {
              done: false,
              value: chunk4
            };
          }
          chunk4.push(next4.value);
        }
      }
    };
  }
}));
var group = /* @__PURE__ */ groupWith(/* @__PURE__ */ equivalence());
var groupBy = /* @__PURE__ */ dual(2, (self, f) => {
  const out = {};
  for (const a of self) {
    const k = f(a);
    if (Object.prototype.hasOwnProperty.call(out, k)) {
      out[k].push(a);
    } else {
      out[k] = [a];
    }
  }
  return out;
});
var constEmpty = {
  [Symbol.iterator]() {
    return constEmptyIterator;
  }
};
var constEmptyIterator = {
  next() {
    return {
      done: true,
      value: void 0
    };
  }
};
var empty2 = /* @__PURE__ */ __name(() => constEmpty, "empty");
var of = /* @__PURE__ */ __name((a) => [a], "of");
var map4 = /* @__PURE__ */ dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        return {
          done: false,
          value: f(result.value, i++)
        };
      }
    };
  }
}));
var flatMap3 = /* @__PURE__ */ dual(2, (self, f) => flatten2(map4(self, f)));
var flatten2 = /* @__PURE__ */ __name((self) => ({
  [Symbol.iterator]() {
    const outerIterator = self[Symbol.iterator]();
    let innerIterator;
    function next4() {
      if (innerIterator === void 0) {
        const next5 = outerIterator.next();
        if (next5.done) {
          return next5;
        }
        innerIterator = next5.value[Symbol.iterator]();
      }
      const result = innerIterator.next();
      if (result.done) {
        innerIterator = void 0;
        return next4();
      }
      return result;
    }
    __name(next4, "next");
    return {
      next: next4
    };
  }
}), "flatten");
var filterMap2 = /* @__PURE__ */ dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        let result = iterator.next();
        while (!result.done) {
          const b = f(result.value, i++);
          if (isSome2(b)) {
            return {
              done: false,
              value: b.value
            };
          }
          result = iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var filterMapWhile = /* @__PURE__ */ dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        const b = f(result.value, i++);
        if (isSome2(b)) {
          return {
            done: false,
            value: b.value
          };
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var getSomes = /* @__PURE__ */ filterMap2(identity);
var getLefts = /* @__PURE__ */ __name((self) => filterMap2(self, getLeft2), "getLefts");
var getRights = /* @__PURE__ */ __name((self) => filterMap2(self, getRight2), "getRights");
var filter2 = /* @__PURE__ */ dual(2, (self, predicate) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        let result = iterator.next();
        while (!result.done) {
          if (predicate(result.value, i++)) {
            return {
              done: false,
              value: result.value
            };
          }
          result = iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var flatMapNullable2 = /* @__PURE__ */ dual(2, (self, f) => filterMap2(self, (a) => {
  const b = f(a);
  return b == null ? none2() : some3(b);
}));
var some4 = /* @__PURE__ */ dual(2, (self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (predicate(a, i++)) {
      return true;
    }
  }
  return false;
});
var unfold = /* @__PURE__ */ __name((b, f) => ({
  [Symbol.iterator]() {
    let next4 = b;
    return {
      next() {
        const o = f(next4);
        if (isNone2(o)) {
          return {
            done: true,
            value: void 0
          };
        }
        const [a, b2] = o.value;
        next4 = b2;
        return {
          done: false,
          value: a
        };
      }
    };
  }
}), "unfold");
var forEach = /* @__PURE__ */ dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    f(a, i++);
  }
});
var reduce = /* @__PURE__ */ dual(3, (self, b, f) => {
  if (Array.isArray(self)) {
    return self.reduce(f, b);
  }
  let i = 0;
  let result = b;
  for (const n of self) {
    result = f(result, n, i++);
  }
  return result;
});
var dedupeAdjacentWith = /* @__PURE__ */ dual(2, (self, isEquivalent) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let first4 = true;
    let last7;
    function next4() {
      const result = iterator.next();
      if (result.done) {
        return {
          done: true,
          value: void 0
        };
      }
      if (first4) {
        first4 = false;
        last7 = result.value;
        return result;
      }
      const current2 = result.value;
      if (isEquivalent(last7, current2)) {
        return next4();
      }
      last7 = current2;
      return result;
    }
    __name(next4, "next");
    return {
      next: next4
    };
  }
}));
var dedupeAdjacent = /* @__PURE__ */ dedupeAdjacentWith(/* @__PURE__ */ equivalence());
var cartesianWith = /* @__PURE__ */ dual(3, (self, that, f) => flatMap3(self, (a) => map4(that, (b) => f(a, b))));
var cartesian = /* @__PURE__ */ dual(2, (self, that) => cartesianWith(self, that, (a, b) => [a, b]));
var countBy = /* @__PURE__ */ dual(2, (self, f) => {
  let count5 = 0;
  let i = 0;
  for (const a of self) {
    if (f(a, i)) {
      count5++;
    }
    i++;
  }
  return count5;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Record.js
var empty3 = /* @__PURE__ */ __name(() => ({}), "empty");
var isEmptyRecord = /* @__PURE__ */ __name((self) => keys(self).length === 0, "isEmptyRecord");
var isEmptyReadonlyRecord = isEmptyRecord;
var fromIterableWith = /* @__PURE__ */ dual(2, (self, f) => {
  const out = empty3();
  for (const a of self) {
    const [k, b] = f(a);
    out[k] = b;
  }
  return out;
});
var fromIterableBy = /* @__PURE__ */ __name((items, f) => fromIterableWith(items, (a) => [f(a), a]), "fromIterableBy");
var fromEntries = Object.fromEntries;
var collect = /* @__PURE__ */ dual(2, (self, f) => {
  const out = [];
  for (const key of keys(self)) {
    out.push(f(key, self[key]));
  }
  return out;
});
var toEntries = /* @__PURE__ */ collect((key, value2) => [key, value2]);
var size2 = /* @__PURE__ */ __name((self) => keys(self).length, "size");
var has = /* @__PURE__ */ dual(2, (self, key) => Object.prototype.hasOwnProperty.call(self, key));
var get = /* @__PURE__ */ dual(2, (self, key) => has(self, key) ? some3(self[key]) : none2());
var modify = /* @__PURE__ */ dual(3, (self, key, f) => {
  if (!has(self, key)) {
    return {
      ...self
    };
  }
  return {
    ...self,
    [key]: f(self[key])
  };
});
var modifyOption = /* @__PURE__ */ dual(3, (self, key, f) => {
  if (!has(self, key)) {
    return none2();
  }
  return some3({
    ...self,
    [key]: f(self[key])
  });
});
var replaceOption = /* @__PURE__ */ dual(3, (self, key, b) => modifyOption(self, key, () => b));
var remove = /* @__PURE__ */ dual(2, (self, key) => {
  if (!has(self, key)) {
    return {
      ...self
    };
  }
  const out = {
    ...self
  };
  delete out[key];
  return out;
});
var pop = /* @__PURE__ */ dual(2, (self, key) => has(self, key) ? some3([self[key], remove(self, key)]) : none2());
var map5 = /* @__PURE__ */ dual(2, (self, f) => {
  const out = {
    ...self
  };
  for (const key of keys(self)) {
    out[key] = f(self[key], key);
  }
  return out;
});
var mapKeys = /* @__PURE__ */ dual(2, (self, f) => {
  const out = {};
  for (const key of keys(self)) {
    const a = self[key];
    out[f(key, a)] = a;
  }
  return out;
});
var mapEntries = /* @__PURE__ */ dual(2, (self, f) => {
  const out = {};
  for (const key of keys(self)) {
    const [k, b] = f(self[key], key);
    out[k] = b;
  }
  return out;
});
var filterMap3 = /* @__PURE__ */ dual(2, (self, f) => {
  const out = empty3();
  for (const key of keys(self)) {
    const o = f(self[key], key);
    if (isSome2(o)) {
      out[key] = o.value;
    }
  }
  return out;
});
var filter3 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const out = empty3();
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      out[key] = self[key];
    }
  }
  return out;
});
var getSomes2 = /* @__PURE__ */ filterMap3(identity);
var getLefts2 = /* @__PURE__ */ __name((self) => {
  const out = empty3();
  for (const key of keys(self)) {
    const value2 = self[key];
    if (isLeft2(value2)) {
      out[key] = value2.left;
    }
  }
  return out;
}, "getLefts");
var getRights2 = /* @__PURE__ */ __name((self) => {
  const out = empty3();
  for (const key of keys(self)) {
    const value2 = self[key];
    if (isRight2(value2)) {
      out[key] = value2.right;
    }
  }
  return out;
}, "getRights");
var partitionMap2 = /* @__PURE__ */ dual(2, (self, f) => {
  const left3 = empty3();
  const right3 = empty3();
  for (const key of keys(self)) {
    const e = f(self[key], key);
    if (isLeft2(e)) {
      left3[key] = e.left;
    } else {
      right3[key] = e.right;
    }
  }
  return [left3, right3];
});
var separate = /* @__PURE__ */ partitionMap2(identity);
var partition = /* @__PURE__ */ dual(2, (self, predicate) => {
  const left3 = empty3();
  const right3 = empty3();
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      right3[key] = self[key];
    } else {
      left3[key] = self[key];
    }
  }
  return [left3, right3];
});
var keys = /* @__PURE__ */ __name((self) => Object.keys(self), "keys");
var values = /* @__PURE__ */ __name((self) => collect(self, (_, a) => a), "values");
var set = /* @__PURE__ */ dual(3, (self, key, value2) => {
  return {
    ...self,
    [key]: value2
  };
});
var replace = /* @__PURE__ */ dual(3, (self, key, value2) => {
  if (has(self, key)) {
    return {
      ...self,
      [key]: value2
    };
  }
  return {
    ...self
  };
});
var isSubrecordBy = /* @__PURE__ */ __name((equivalence2) => dual(2, (self, that) => {
  for (const key of keys(self)) {
    if (!has(that, key) || !equivalence2(self[key], that[key])) {
      return false;
    }
  }
  return true;
}), "isSubrecordBy");
var isSubrecord = /* @__PURE__ */ isSubrecordBy(/* @__PURE__ */ equivalence());
var reduce2 = /* @__PURE__ */ dual(3, (self, zero2, f) => {
  let out = zero2;
  for (const key of keys(self)) {
    out = f(out, self[key], key);
  }
  return out;
});
var every2 = /* @__PURE__ */ dual(2, (self, refinement) => {
  for (const key of keys(self)) {
    if (!refinement(self[key], key)) {
      return false;
    }
  }
  return true;
});
var some5 = /* @__PURE__ */ dual(2, (self, predicate) => {
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      return true;
    }
  }
  return false;
});
var union = /* @__PURE__ */ dual(3, (self, that, combine18) => {
  if (isEmptyRecord(self)) {
    return {
      ...that
    };
  }
  if (isEmptyRecord(that)) {
    return {
      ...self
    };
  }
  const out = empty3();
  for (const key of keys(self)) {
    if (has(that, key)) {
      out[key] = combine18(self[key], that[key]);
    } else {
      out[key] = self[key];
    }
  }
  for (const key of keys(that)) {
    if (!has(out, key)) {
      out[key] = that[key];
    }
  }
  return out;
});
var intersection = /* @__PURE__ */ dual(3, (self, that, combine18) => {
  const out = empty3();
  if (isEmptyRecord(self) || isEmptyRecord(that)) {
    return out;
  }
  for (const key of keys(self)) {
    if (has(that, key)) {
      out[key] = combine18(self[key], that[key]);
    }
  }
  return out;
});
var difference = /* @__PURE__ */ dual(2, (self, that) => {
  if (isEmptyRecord(self)) {
    return {
      ...that
    };
  }
  if (isEmptyRecord(that)) {
    return {
      ...self
    };
  }
  const out = {};
  for (const key of keys(self)) {
    if (!has(that, key)) {
      out[key] = self[key];
    }
  }
  for (const key of keys(that)) {
    if (!has(self, key)) {
      out[key] = that[key];
    }
  }
  return out;
});
var getEquivalence4 = /* @__PURE__ */ __name((equivalence2) => {
  const is = isSubrecordBy(equivalence2);
  return (self, that) => is(self, that) && is(that, self);
}, "getEquivalence");
var singleton = /* @__PURE__ */ __name((key, value2) => ({
  [key]: value2
}), "singleton");
var findFirst2 = /* @__PURE__ */ dual(2, (self, f) => {
  const k = keys(self);
  for (let i = 0; i < k.length; i++) {
    const key = k[i];
    if (f(self[key], key)) {
      return some3([key, self[key]]);
    }
  }
  return none2();
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Array.js
var make4 = /* @__PURE__ */ __name((...elements) => elements, "make");
var allocate = /* @__PURE__ */ __name((n) => new Array(n), "allocate");
var makeBy2 = /* @__PURE__ */ dual(2, (n, f) => {
  const max10 = Math.max(1, Math.floor(n));
  const out = new Array(max10);
  for (let i = 0; i < max10; i++) {
    out[i] = f(i);
  }
  return out;
});
var range2 = /* @__PURE__ */ __name((start3, end6) => start3 <= end6 ? makeBy2(end6 - start3 + 1, (i) => start3 + i) : [start3], "range");
var replicate2 = /* @__PURE__ */ dual(2, (a, n) => makeBy2(n, () => a));
var fromIterable2 = /* @__PURE__ */ __name((collection) => Array.isArray(collection) ? collection : Array.from(collection), "fromIterable");
var ensure = /* @__PURE__ */ __name((self) => Array.isArray(self) ? self : [self], "ensure");
var fromRecord2 = toEntries;
var fromOption3 = toArray;
var match3 = /* @__PURE__ */ dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(self) : onEmpty());
var matchLeft = /* @__PURE__ */ dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(headNonEmpty(self), tailNonEmpty(self)) : onEmpty());
var matchRight = /* @__PURE__ */ dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(initNonEmpty(self), lastNonEmpty(self)) : onEmpty());
var prepend2 = /* @__PURE__ */ dual(2, (self, head10) => [head10, ...self]);
var prependAll2 = /* @__PURE__ */ dual(2, (self, that) => fromIterable2(that).concat(fromIterable2(self)));
var append2 = /* @__PURE__ */ dual(2, (self, last7) => [...self, last7]);
var appendAll2 = /* @__PURE__ */ dual(2, (self, that) => fromIterable2(self).concat(fromIterable2(that)));
var scan2 = /* @__PURE__ */ dual(3, (self, b, f) => {
  const out = [b];
  let i = 0;
  for (const a of self) {
    out[i + 1] = f(out[i], a);
    i++;
  }
  return out;
});
var scanRight = /* @__PURE__ */ dual(3, (self, b, f) => {
  const input = fromIterable2(self);
  const out = new Array(input.length + 1);
  out[input.length] = b;
  for (let i = input.length - 1; i >= 0; i--) {
    out[i] = f(out[i + 1], input[i]);
  }
  return out;
});
var isArray = Array.isArray;
var isEmptyArray = /* @__PURE__ */ __name((self) => self.length === 0, "isEmptyArray");
var isEmptyReadonlyArray = isEmptyArray;
var isNonEmptyArray2 = isNonEmptyArray;
var isNonEmptyReadonlyArray = isNonEmptyArray;
var length = /* @__PURE__ */ __name((self) => self.length, "length");
var isOutOfBounds = /* @__PURE__ */ __name((i, as15) => i < 0 || i >= as15.length, "isOutOfBounds");
var clamp2 = /* @__PURE__ */ __name((i, as15) => Math.floor(Math.min(Math.max(0, i), as15.length)), "clamp");
var get2 = /* @__PURE__ */ dual(2, (self, index) => {
  const i = Math.floor(index);
  return isOutOfBounds(i, self) ? none2() : some3(self[i]);
});
var unsafeGet = /* @__PURE__ */ dual(2, (self, index) => {
  const i = Math.floor(index);
  if (isOutOfBounds(i, self)) {
    throw new Error(`Index ${i} out of bounds`);
  }
  return self[i];
});
var unprepend = /* @__PURE__ */ __name((self) => [headNonEmpty(self), tailNonEmpty(self)], "unprepend");
var unappend = /* @__PURE__ */ __name((self) => [initNonEmpty(self), lastNonEmpty(self)], "unappend");
var head2 = /* @__PURE__ */ get2(0);
var headNonEmpty = /* @__PURE__ */ unsafeGet(0);
var last = /* @__PURE__ */ __name((self) => isNonEmptyReadonlyArray(self) ? some3(lastNonEmpty(self)) : none2(), "last");
var lastNonEmpty = /* @__PURE__ */ __name((self) => self[self.length - 1], "lastNonEmpty");
var tail = /* @__PURE__ */ __name((self) => {
  const input = fromIterable2(self);
  return isNonEmptyReadonlyArray(input) ? some3(tailNonEmpty(input)) : none2();
}, "tail");
var tailNonEmpty = /* @__PURE__ */ __name((self) => self.slice(1), "tailNonEmpty");
var init = /* @__PURE__ */ __name((self) => {
  const input = fromIterable2(self);
  return isNonEmptyReadonlyArray(input) ? some3(initNonEmpty(input)) : none2();
}, "init");
var initNonEmpty = /* @__PURE__ */ __name((self) => self.slice(0, -1), "initNonEmpty");
var take2 = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  return input.slice(0, clamp2(n, input));
});
var takeRight = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  const i = clamp2(n, input);
  return i === 0 ? [] : input.slice(-i);
});
var takeWhile2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  let i = 0;
  const out = [];
  for (const a of self) {
    if (!predicate(a, i)) {
      break;
    }
    out.push(a);
    i++;
  }
  return out;
});
var spanIndex = /* @__PURE__ */ __name((self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (!predicate(a, i)) {
      break;
    }
    i++;
  }
  return i;
}, "spanIndex");
var span = /* @__PURE__ */ dual(2, (self, predicate) => splitAt(self, spanIndex(self, predicate)));
var drop2 = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  return input.slice(clamp2(n, input), input.length);
});
var dropRight = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  return input.slice(0, input.length - clamp2(n, input));
});
var dropWhile = /* @__PURE__ */ dual(2, (self, predicate) => fromIterable2(self).slice(spanIndex(self, predicate)));
var findFirstIndex = /* @__PURE__ */ dual(2, (self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (predicate(a, i)) {
      return some3(i);
    }
    i++;
  }
  return none2();
});
var findLastIndex = /* @__PURE__ */ dual(2, (self, predicate) => {
  const input = fromIterable2(self);
  for (let i = input.length - 1; i >= 0; i--) {
    if (predicate(input[i], i)) {
      return some3(i);
    }
  }
  return none2();
});
var findFirst3 = findFirst;
var findLast2 = /* @__PURE__ */ dual(2, (self, f) => {
  const input = fromIterable2(self);
  for (let i = input.length - 1; i >= 0; i--) {
    const a = input[i];
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some3(a);
      }
    } else {
      if (isSome2(o)) {
        return o;
      }
    }
  }
  return none2();
});
var findFirstWithIndex = /* @__PURE__ */ dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some3([a, i]);
      }
    } else {
      if (isSome2(o)) {
        return some3([o.value, i]);
      }
    }
    i++;
  }
  return none2();
});
var countBy2 = /* @__PURE__ */ dual(2, (self, f) => {
  let count5 = 0;
  const as15 = fromIterable2(self);
  for (let i = 0; i < as15.length; i++) {
    const a = as15[i];
    if (f(a, i)) {
      count5++;
    }
  }
  return count5;
});
var insertAt = /* @__PURE__ */ dual(3, (self, i, b) => {
  const out = Array.from(self);
  if (i < 0 || i > out.length) {
    return none2();
  }
  out.splice(i, 0, b);
  return some3(out);
});
var replace2 = /* @__PURE__ */ dual(3, (self, i, b) => modify2(self, i, () => b));
var replaceOption2 = /* @__PURE__ */ dual(3, (self, i, b) => modifyOption2(self, i, () => b));
var modify2 = /* @__PURE__ */ dual(3, (self, i, f) => {
  const out = Array.from(self);
  if (isOutOfBounds(i, out)) {
    return out;
  }
  const b = f(out[i]);
  out[i] = b;
  return out;
});
var modifyOption2 = /* @__PURE__ */ dual(3, (self, i, f) => {
  const arr = fromIterable2(self);
  if (isOutOfBounds(i, arr)) {
    return none2();
  }
  const out = Array.isArray(self) ? self.slice() : arr;
  const b = f(arr[i]);
  out[i] = b;
  return some3(out);
});
var remove2 = /* @__PURE__ */ dual(2, (self, i) => {
  const out = Array.from(self);
  if (isOutOfBounds(i, out)) {
    return out;
  }
  out.splice(i, 1);
  return out;
});
var removeOption = /* @__PURE__ */ dual(2, (self, i) => {
  const arr = fromIterable2(self);
  if (isOutOfBounds(i, arr)) {
    return none2();
  }
  const out = Array.isArray(self) ? self.slice() : arr;
  out.splice(i, 1);
  return some3(out);
});
var reverse2 = /* @__PURE__ */ __name((self) => Array.from(self).reverse(), "reverse");
var sort = /* @__PURE__ */ dual(2, (self, O) => {
  const out = Array.from(self);
  out.sort(O);
  return out;
});
var sortWith = /* @__PURE__ */ dual(3, (self, f, order) => Array.from(self).map((a) => [a, f(a)]).sort(([, a], [, b]) => order(a, b)).map(([_]) => _));
var sortBy = /* @__PURE__ */ __name((...orders) => {
  const sortByAll = sort(combineAll2(orders));
  return (self) => {
    const input = fromIterable2(self);
    if (isNonEmptyReadonlyArray(input)) {
      return sortByAll(input);
    }
    return [];
  };
}, "sortBy");
var zip2 = /* @__PURE__ */ dual(2, (self, that) => zipWith4(self, that, make3));
var zipWith4 = /* @__PURE__ */ dual(3, (self, that, f) => {
  const as15 = fromIterable2(self);
  const bs = fromIterable2(that);
  if (isNonEmptyReadonlyArray(as15) && isNonEmptyReadonlyArray(bs)) {
    const out = [f(headNonEmpty(as15), headNonEmpty(bs))];
    const len = Math.min(as15.length, bs.length);
    for (let i = 1; i < len; i++) {
      out[i] = f(as15[i], bs[i]);
    }
    return out;
  }
  return [];
});
var unzip = /* @__PURE__ */ __name((self) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    const fa = [input[0][0]];
    const fb = [input[0][1]];
    for (let i = 1; i < input.length; i++) {
      fa[i] = input[i][0];
      fb[i] = input[i][1];
    }
    return [fa, fb];
  }
  return [[], []];
}, "unzip");
var intersperse2 = /* @__PURE__ */ dual(2, (self, middle) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    const out = [headNonEmpty(input)];
    const tail5 = tailNonEmpty(input);
    for (let i = 0; i < tail5.length; i++) {
      if (i < tail5.length) {
        out.push(middle);
      }
      out.push(tail5[i]);
    }
    return out;
  }
  return [];
});
var modifyNonEmptyHead = /* @__PURE__ */ dual(2, (self, f) => [f(headNonEmpty(self)), ...tailNonEmpty(self)]);
var setNonEmptyHead = /* @__PURE__ */ dual(2, (self, b) => modifyNonEmptyHead(self, () => b));
var modifyNonEmptyLast = /* @__PURE__ */ dual(2, (self, f) => append2(initNonEmpty(self), f(lastNonEmpty(self))));
var setNonEmptyLast = /* @__PURE__ */ dual(2, (self, b) => modifyNonEmptyLast(self, () => b));
var rotate = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    const len = input.length;
    const m = Math.round(n) % len;
    if (isOutOfBounds(Math.abs(m), input) || m === 0) {
      return copy(input);
    }
    if (m < 0) {
      const [f, s] = splitNonEmptyAt(input, -m);
      return appendAll2(s, f);
    } else {
      return rotate(self, m - len);
    }
  }
  return [];
});
var containsWith3 = /* @__PURE__ */ __name((isEquivalent) => dual(2, (self, a) => {
  for (const i of self) {
    if (isEquivalent(a, i)) {
      return true;
    }
  }
  return false;
}), "containsWith");
var _equivalence3 = /* @__PURE__ */ equivalence();
var contains3 = /* @__PURE__ */ containsWith3(_equivalence3);
var chop = /* @__PURE__ */ dual(2, (self, f) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    const [b, rest] = f(input);
    const out = [b];
    let next4 = rest;
    while (isNonEmptyArray(next4)) {
      const [b2, rest2] = f(next4);
      out.push(b2);
      next4 = rest2;
    }
    return out;
  }
  return [];
});
var splitAt = /* @__PURE__ */ dual(2, (self, n) => {
  const input = Array.from(self);
  const _n = Math.floor(n);
  if (isNonEmptyReadonlyArray(input)) {
    if (_n >= 1) {
      return splitNonEmptyAt(input, _n);
    }
    return [[], input];
  }
  return [input, []];
});
var splitNonEmptyAt = /* @__PURE__ */ dual(2, (self, n) => {
  const _n = Math.max(1, Math.floor(n));
  return _n >= self.length ? [copy(self), []] : [prepend2(self.slice(1, _n), headNonEmpty(self)), self.slice(_n)];
});
var split = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  return chunksOf2(input, Math.ceil(input.length / Math.floor(n)));
});
var splitWhere = /* @__PURE__ */ dual(2, (self, predicate) => span(self, (a, i) => !predicate(a, i)));
var copy = /* @__PURE__ */ __name((self) => self.slice(), "copy");
var pad = /* @__PURE__ */ dual(3, (self, n, fill) => {
  if (self.length >= n) {
    return take2(self, n);
  }
  return appendAll2(self, makeBy2(n - self.length, () => fill));
});
var chunksOf2 = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    return chop(input, splitNonEmptyAt(n));
  }
  return [];
});
var window2 = /* @__PURE__ */ dual(2, (self, n) => {
  const input = fromIterable2(self);
  if (n > 0 && isNonEmptyReadonlyArray(input)) {
    return Array.from({
      length: input.length - (n - 1)
    }, (_, index) => input.slice(index, index + n));
  }
  return [];
});
var groupWith2 = /* @__PURE__ */ dual(2, (self, isEquivalent) => chop(self, (as15) => {
  const h = headNonEmpty(as15);
  const out = [h];
  let i = 1;
  for (; i < as15.length; i++) {
    const a = as15[i];
    if (isEquivalent(a, h)) {
      out.push(a);
    } else {
      break;
    }
  }
  return [out, as15.slice(i)];
}));
var group2 = /* @__PURE__ */ groupWith2(/* @__PURE__ */ equivalence());
var groupBy2 = /* @__PURE__ */ dual(2, (self, f) => {
  const out = {};
  for (const a of self) {
    const k = f(a);
    if (Object.prototype.hasOwnProperty.call(out, k)) {
      out[k].push(a);
    } else {
      out[k] = [a];
    }
  }
  return out;
});
var unionWith = /* @__PURE__ */ dual(3, (self, that, isEquivalent) => {
  const a = fromIterable2(self);
  const b = fromIterable2(that);
  if (isNonEmptyReadonlyArray(a)) {
    if (isNonEmptyReadonlyArray(b)) {
      const dedupe3 = dedupeWith(isEquivalent);
      return dedupe3(appendAll2(a, b));
    }
    return a;
  }
  return b;
});
var union2 = /* @__PURE__ */ dual(2, (self, that) => unionWith(self, that, _equivalence3));
var intersectionWith = /* @__PURE__ */ __name((isEquivalent) => {
  const has10 = containsWith3(isEquivalent);
  return dual(2, (self, that) => {
    const bs = fromIterable2(that);
    return fromIterable2(self).filter((a) => has10(bs, a));
  });
}, "intersectionWith");
var intersection2 = /* @__PURE__ */ intersectionWith(_equivalence3);
var differenceWith = /* @__PURE__ */ __name((isEquivalent) => {
  const has10 = containsWith3(isEquivalent);
  return dual(2, (self, that) => {
    const bs = fromIterable2(that);
    return fromIterable2(self).filter((a) => !has10(bs, a));
  });
}, "differenceWith");
var difference2 = /* @__PURE__ */ differenceWith(_equivalence3);
var empty4 = /* @__PURE__ */ __name(() => [], "empty");
var of2 = /* @__PURE__ */ __name((a) => [a], "of");
var map6 = /* @__PURE__ */ dual(2, (self, f) => self.map(f));
var flatMap4 = /* @__PURE__ */ dual(2, (self, f) => {
  if (isEmptyReadonlyArray(self)) {
    return [];
  }
  const out = [];
  for (let i = 0; i < self.length; i++) {
    const inner = f(self[i], i);
    for (let j = 0; j < inner.length; j++) {
      out.push(inner[j]);
    }
  }
  return out;
});
var flatten3 = /* @__PURE__ */ flatMap4(identity);
var filterMap4 = /* @__PURE__ */ dual(2, (self, f) => {
  const as15 = fromIterable2(self);
  const out = [];
  for (let i = 0; i < as15.length; i++) {
    const o = f(as15[i], i);
    if (isSome2(o)) {
      out.push(o.value);
    }
  }
  return out;
});
var filterMapWhile2 = /* @__PURE__ */ dual(2, (self, f) => {
  let i = 0;
  const out = [];
  for (const a of self) {
    const b = f(a, i);
    if (isSome2(b)) {
      out.push(b.value);
    } else {
      break;
    }
    i++;
  }
  return out;
});
var partitionMap3 = /* @__PURE__ */ dual(2, (self, f) => {
  const left3 = [];
  const right3 = [];
  const as15 = fromIterable2(self);
  for (let i = 0; i < as15.length; i++) {
    const e = f(as15[i], i);
    if (isLeft2(e)) {
      left3.push(e.left);
    } else {
      right3.push(e.right);
    }
  }
  return [left3, right3];
});
var getSomes3 = /* @__PURE__ */ filterMap4(identity);
var getLefts3 = /* @__PURE__ */ __name((self) => {
  const out = [];
  for (const a of self) {
    if (isLeft2(a)) {
      out.push(a.left);
    }
  }
  return out;
}, "getLefts");
var getRights3 = /* @__PURE__ */ __name((self) => {
  const out = [];
  for (const a of self) {
    if (isRight2(a)) {
      out.push(a.right);
    }
  }
  return out;
}, "getRights");
var filter4 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const as15 = fromIterable2(self);
  const out = [];
  for (let i = 0; i < as15.length; i++) {
    if (predicate(as15[i], i)) {
      out.push(as15[i]);
    }
  }
  return out;
});
var partition2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const left3 = [];
  const right3 = [];
  const as15 = fromIterable2(self);
  for (let i = 0; i < as15.length; i++) {
    if (predicate(as15[i], i)) {
      right3.push(as15[i]);
    } else {
      left3.push(as15[i]);
    }
  }
  return [left3, right3];
});
var separate2 = /* @__PURE__ */ partitionMap3(identity);
var reduce3 = /* @__PURE__ */ dual(3, (self, b, f) => fromIterable2(self).reduce((b2, a, i) => f(b2, a, i), b));
var reduceRight = /* @__PURE__ */ dual(3, (self, b, f) => fromIterable2(self).reduceRight((b2, a, i) => f(b2, a, i), b));
var liftPredicate3 = /* @__PURE__ */ __name((predicate) => (b) => predicate(b) ? [b] : [], "liftPredicate");
var liftOption = /* @__PURE__ */ __name((f) => (...a) => fromOption3(f(...a)), "liftOption");
var fromNullable3 = /* @__PURE__ */ __name((a) => a == null ? empty4() : [a], "fromNullable");
var liftNullable2 = /* @__PURE__ */ __name((f) => (...a) => fromNullable3(f(...a)), "liftNullable");
var flatMapNullable3 = /* @__PURE__ */ dual(2, (self, f) => flatMap4(self, (a) => fromNullable3(f(a))));
var liftEither = /* @__PURE__ */ __name((f) => (...a) => {
  const e = f(...a);
  return isLeft2(e) ? [] : [e.right];
}, "liftEither");
var every3 = /* @__PURE__ */ dual(2, (self, refinement) => self.every(refinement));
var some6 = /* @__PURE__ */ dual(2, (self, predicate) => self.some(predicate));
var extend = /* @__PURE__ */ dual(2, (self, f) => self.map((_, i, as15) => f(as15.slice(i))));
var min2 = /* @__PURE__ */ dual(2, (self, O) => self.reduce(min(O)));
var max2 = /* @__PURE__ */ dual(2, (self, O) => self.reduce(max(O)));
var unfold2 = /* @__PURE__ */ __name((b, f) => {
  const out = [];
  let next4 = b;
  let o;
  while (isSome2(o = f(next4))) {
    const [a, b2] = o.value;
    out.push(a);
    next4 = b2;
  }
  return out;
}, "unfold");
var getOrder3 = array3;
var getEquivalence5 = array;
var forEach2 = /* @__PURE__ */ dual(2, (self, f) => fromIterable2(self).forEach((a, i) => f(a, i)));
var dedupeWith = /* @__PURE__ */ dual(2, (self, isEquivalent) => {
  const input = fromIterable2(self);
  if (isNonEmptyReadonlyArray(input)) {
    const out = [headNonEmpty(input)];
    const rest = tailNonEmpty(input);
    for (const r of rest) {
      if (out.every((a) => !isEquivalent(r, a))) {
        out.push(r);
      }
    }
    return out;
  }
  return [];
});
var dedupe = /* @__PURE__ */ __name((self) => dedupeWith(self, equivalence()), "dedupe");
var dedupeAdjacentWith2 = /* @__PURE__ */ dual(2, (self, isEquivalent) => {
  const out = [];
  let lastA = none2();
  for (const a of self) {
    if (isNone2(lastA) || !isEquivalent(a, lastA.value)) {
      out.push(a);
      lastA = some3(a);
    }
  }
  return out;
});
var dedupeAdjacent2 = /* @__PURE__ */ dedupeAdjacentWith2(/* @__PURE__ */ equivalence());
var join = /* @__PURE__ */ dual(2, (self, sep) => fromIterable2(self).join(sep));
var mapAccum = /* @__PURE__ */ dual(3, (self, s, f) => {
  let i = 0;
  let s1 = s;
  const out = [];
  for (const a of self) {
    const r = f(s1, a, i);
    s1 = r[0];
    out.push(r[1]);
    i++;
  }
  return [s1, out];
});
var cartesianWith2 = /* @__PURE__ */ dual(3, (self, that, f) => flatMap4(self, (a) => map6(that, (b) => f(a, b))));
var cartesian2 = /* @__PURE__ */ dual(2, (self, that) => cartesianWith2(self, that, (a, b) => [a, b]));
var Do3 = /* @__PURE__ */ of2({});
var bind4 = /* @__PURE__ */ bind(map6, flatMap4);
var bindTo4 = /* @__PURE__ */ bindTo(map6);
var let_4 = /* @__PURE__ */ let_(map6);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Number.js
var isNumber2 = isNumber;
var negate = /* @__PURE__ */ __name((n) => multiply(n, -1), "negate");
var sum = /* @__PURE__ */ dual(2, (self, that) => self + that);
var sumAll = /* @__PURE__ */ __name((collection) => reduce(collection, 0, sum), "sumAll");
var subtract = /* @__PURE__ */ dual(2, (minuend, subtrahend) => minuend - subtrahend);
var multiply = /* @__PURE__ */ dual(2, (multiplier, multiplicand) => multiplier * multiplicand);
var multiplyAll = /* @__PURE__ */ __name((collection) => {
  let out = 1;
  for (const n of collection) {
    if (n === 0) {
      return 0;
    }
    out *= n;
  }
  return out;
}, "multiplyAll");
var divide = /* @__PURE__ */ dual(2, (dividend, divisor) => divisor === 0 ? none : some2(dividend / divisor));
var unsafeDivide = /* @__PURE__ */ dual(2, (dividend, divisor) => dividend / divisor);
var increment = /* @__PURE__ */ __name((n) => sum(n, 1), "increment");
var decrement = /* @__PURE__ */ __name((n) => subtract(n, 1), "decrement");
var Equivalence = number;
var Order = number3;
var lessThan2 = /* @__PURE__ */ lessThan(Order);
var lessThanOrEqualTo2 = /* @__PURE__ */ lessThanOrEqualTo(Order);
var greaterThan2 = /* @__PURE__ */ greaterThan(Order);
var greaterThanOrEqualTo2 = /* @__PURE__ */ greaterThanOrEqualTo(Order);
var between2 = /* @__PURE__ */ between(Order);
var clamp3 = /* @__PURE__ */ clamp(Order);
var min3 = /* @__PURE__ */ min(Order);
var max3 = /* @__PURE__ */ max(Order);
var sign = /* @__PURE__ */ __name((n) => Order(n, 0), "sign");
var remainder = /* @__PURE__ */ dual(2, (dividend, divisor) => {
  const selfDecCount = (dividend.toString().split(".")[1] || "").length;
  const divisorDecCount = (divisor.toString().split(".")[1] || "").length;
  const decCount = selfDecCount > divisorDecCount ? selfDecCount : divisorDecCount;
  const selfInt = parseInt(dividend.toFixed(decCount).replace(".", ""));
  const divisorInt = parseInt(divisor.toFixed(decCount).replace(".", ""));
  return selfInt % divisorInt / Math.pow(10, decCount);
});
var nextPow2 = /* @__PURE__ */ __name((n) => {
  const nextPow = Math.ceil(Math.log(n) / Math.log(2));
  return Math.max(Math.pow(2, nextPow), 2);
}, "nextPow2");
var parse = /* @__PURE__ */ __name((s) => {
  if (s === "NaN") {
    return some2(NaN);
  }
  if (s === "Infinity") {
    return some2(Infinity);
  }
  if (s === "-Infinity") {
    return some2(-Infinity);
  }
  if (s.trim() === "") {
    return none;
  }
  const n = Number(s);
  return Number.isNaN(n) ? none : some2(n);
}, "parse");
var round = /* @__PURE__ */ dual(2, (self, precision) => {
  const factor = Math.pow(10, precision);
  return Math.round(self * factor) / factor;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/RegExp.js
var isRegExp2 = isRegExp;
var escape = /* @__PURE__ */ __name((string4) => string4.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&"), "escape");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Boolean.js
var isBoolean2 = isBoolean;
var match4 = /* @__PURE__ */ dual(2, (value2, options) => value2 ? options.onTrue() : options.onFalse());
var Equivalence2 = boolean;
var Order2 = boolean2;
var not2 = /* @__PURE__ */ __name((self) => !self, "not");
var and2 = /* @__PURE__ */ dual(2, (self, that) => self && that);
var nand2 = /* @__PURE__ */ dual(2, (self, that) => !(self && that));
var or2 = /* @__PURE__ */ dual(2, (self, that) => self || that);
var nor2 = /* @__PURE__ */ dual(2, (self, that) => !(self || that));
var xor2 = /* @__PURE__ */ dual(2, (self, that) => !self && that || self && !that);
var eqv2 = /* @__PURE__ */ dual(2, (self, that) => !xor2(self, that));
var implies2 = /* @__PURE__ */ dual(2, (self, that) => self ? that : true);
var every4 = /* @__PURE__ */ __name((collection) => {
  for (const b of collection) {
    if (!b) {
      return false;
    }
  }
  return true;
}, "every");
var some7 = /* @__PURE__ */ __name((collection) => {
  for (const b of collection) {
    if (b) {
      return true;
    }
  }
  return false;
}, "some");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Context.js
var Context_exports = {};
__export(Context_exports, {
  GenericTag: () => GenericTag,
  Reference: () => Reference2,
  ReferenceTypeId: () => ReferenceTypeId2,
  Tag: () => Tag2,
  TagTypeId: () => TagTypeId2,
  add: () => add2,
  empty: () => empty6,
  get: () => get4,
  getOption: () => getOption2,
  getOrElse: () => getOrElse4,
  isContext: () => isContext2,
  isReference: () => isReference2,
  isTag: () => isTag2,
  make: () => make6,
  merge: () => merge3,
  mergeAll: () => mergeAll2,
  omit: () => omit2,
  pick: () => pick2,
  unsafeGet: () => unsafeGet3,
  unsafeMake: () => unsafeMake
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/context.js
var TagTypeId = /* @__PURE__ */ Symbol.for("effect/Context/Tag");
var ReferenceTypeId = /* @__PURE__ */ Symbol.for("effect/Context/Reference");
var STMSymbolKey = "effect/STM";
var STMTypeId = /* @__PURE__ */ Symbol.for(STMSymbolKey);
var TagProto = {
  ...EffectPrototype,
  _op: "Tag",
  [STMTypeId]: effectVariance,
  [TagTypeId]: {
    _Service: /* @__PURE__ */ __name((_) => _, "_Service"),
    _Identifier: /* @__PURE__ */ __name((_) => _, "_Identifier")
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Tag",
      key: this.key,
      stack: this.stack
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  of(self) {
    return self;
  },
  context(self) {
    return make5(this, self);
  }
};
var ReferenceProto = {
  ...TagProto,
  [ReferenceTypeId]: ReferenceTypeId
};
var makeGenericTag = /* @__PURE__ */ __name((key) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const creationError = new Error();
  Error.stackTraceLimit = limit;
  const tag = Object.create(TagProto);
  Object.defineProperty(tag, "stack", {
    get() {
      return creationError.stack;
    }
  });
  tag.key = key;
  return tag;
}, "makeGenericTag");
var Tag = /* @__PURE__ */ __name((id3) => () => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const creationError = new Error();
  Error.stackTraceLimit = limit;
  function TagClass() {
  }
  __name(TagClass, "TagClass");
  Object.setPrototypeOf(TagClass, TagProto);
  TagClass.key = id3;
  Object.defineProperty(TagClass, "stack", {
    get() {
      return creationError.stack;
    }
  });
  return TagClass;
}, "Tag");
var Reference = /* @__PURE__ */ __name(() => (id3, options) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const creationError = new Error();
  Error.stackTraceLimit = limit;
  function ReferenceClass() {
  }
  __name(ReferenceClass, "ReferenceClass");
  Object.setPrototypeOf(ReferenceClass, ReferenceProto);
  ReferenceClass.key = id3;
  ReferenceClass.defaultValue = options.defaultValue;
  Object.defineProperty(ReferenceClass, "stack", {
    get() {
      return creationError.stack;
    }
  });
  return ReferenceClass;
}, "Reference");
var TypeId5 = /* @__PURE__ */ Symbol.for("effect/Context");
var ContextProto = {
  [TypeId5]: {
    _Services: /* @__PURE__ */ __name((_) => _, "_Services")
  },
  [symbol3](that) {
    if (isContext(that)) {
      if (this.unsafeMap.size === that.unsafeMap.size) {
        for (const k of this.unsafeMap.keys()) {
          if (!that.unsafeMap.has(k) || !equals(this.unsafeMap.get(k), that.unsafeMap.get(k))) {
            return false;
          }
        }
        return true;
      }
    }
    return false;
  },
  [symbol2]() {
    return cached(this, number2(this.unsafeMap.size));
  },
  pipe() {
    return pipeArguments(this, arguments);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Context",
      services: Array.from(this.unsafeMap).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var makeContext = /* @__PURE__ */ __name((unsafeMap) => {
  const context11 = Object.create(ContextProto);
  context11.unsafeMap = unsafeMap;
  return context11;
}, "makeContext");
var serviceNotFoundError = /* @__PURE__ */ __name((tag) => {
  const error2 = new Error(`Service not found${tag.key ? `: ${String(tag.key)}` : ""}`);
  if (tag.stack) {
    const lines = tag.stack.split("\n");
    if (lines.length > 2) {
      const afterAt = lines[2].match(/at (.*)/);
      if (afterAt) {
        error2.message = error2.message + ` (defined at ${afterAt[1]})`;
      }
    }
  }
  if (error2.stack) {
    const lines = error2.stack.split("\n");
    lines.splice(1, 3);
    error2.stack = lines.join("\n");
  }
  return error2;
}, "serviceNotFoundError");
var isContext = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId5), "isContext");
var isTag = /* @__PURE__ */ __name((u) => hasProperty(u, TagTypeId), "isTag");
var isReference = /* @__PURE__ */ __name((u) => hasProperty(u, ReferenceTypeId), "isReference");
var _empty = /* @__PURE__ */ makeContext(/* @__PURE__ */ new Map());
var empty5 = /* @__PURE__ */ __name(() => _empty, "empty");
var make5 = /* @__PURE__ */ __name((tag, service6) => makeContext(/* @__PURE__ */ new Map([[tag.key, service6]])), "make");
var add = /* @__PURE__ */ dual(3, (self, tag, service6) => {
  const map36 = new Map(self.unsafeMap);
  map36.set(tag.key, service6);
  return makeContext(map36);
});
var defaultValueCache = /* @__PURE__ */ globalValue("effect/Context/defaultValueCache", () => /* @__PURE__ */ new Map());
var getDefaultValue = /* @__PURE__ */ __name((tag) => {
  if (defaultValueCache.has(tag.key)) {
    return defaultValueCache.get(tag.key);
  }
  const value2 = tag.defaultValue();
  defaultValueCache.set(tag.key, value2);
  return value2;
}, "getDefaultValue");
var unsafeGetReference = /* @__PURE__ */ __name((self, tag) => {
  return self.unsafeMap.has(tag.key) ? self.unsafeMap.get(tag.key) : getDefaultValue(tag);
}, "unsafeGetReference");
var unsafeGet2 = /* @__PURE__ */ dual(2, (self, tag) => {
  if (!self.unsafeMap.has(tag.key)) {
    if (ReferenceTypeId in tag) return getDefaultValue(tag);
    throw serviceNotFoundError(tag);
  }
  return self.unsafeMap.get(tag.key);
});
var get3 = unsafeGet2;
var getOrElse3 = /* @__PURE__ */ dual(3, (self, tag, orElse15) => {
  if (!self.unsafeMap.has(tag.key)) {
    return isReference(tag) ? getDefaultValue(tag) : orElse15();
  }
  return self.unsafeMap.get(tag.key);
});
var getOption = /* @__PURE__ */ dual(2, (self, tag) => {
  if (!self.unsafeMap.has(tag.key)) {
    return isReference(tag) ? some2(getDefaultValue(tag)) : none;
  }
  return some2(self.unsafeMap.get(tag.key));
});
var merge2 = /* @__PURE__ */ dual(2, (self, that) => {
  const map36 = new Map(self.unsafeMap);
  for (const [tag, s] of that.unsafeMap) {
    map36.set(tag, s);
  }
  return makeContext(map36);
});
var mergeAll = /* @__PURE__ */ __name((...ctxs) => {
  const map36 = /* @__PURE__ */ new Map();
  for (let i = 0; i < ctxs.length; i++) {
    ctxs[i].unsafeMap.forEach((value2, key) => {
      map36.set(key, value2);
    });
  }
  return makeContext(map36);
}, "mergeAll");
var pick = /* @__PURE__ */ __name((...tags) => (self) => {
  const tagSet = new Set(tags.map((_) => _.key));
  const newEnv = /* @__PURE__ */ new Map();
  for (const [tag, s] of self.unsafeMap.entries()) {
    if (tagSet.has(tag)) {
      newEnv.set(tag, s);
    }
  }
  return makeContext(newEnv);
}, "pick");
var omit = /* @__PURE__ */ __name((...tags) => (self) => {
  const newEnv = new Map(self.unsafeMap);
  for (const tag of tags) {
    newEnv.delete(tag.key);
  }
  return makeContext(newEnv);
}, "omit");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Context.js
var TagTypeId2 = TagTypeId;
var ReferenceTypeId2 = ReferenceTypeId;
var GenericTag = makeGenericTag;
var TypeId6 = TypeId5;
var unsafeMake = makeContext;
var isContext2 = isContext;
var isTag2 = isTag;
var isReference2 = isReference;
var empty6 = empty5;
var make6 = make5;
var add2 = add;
var get4 = get3;
var getOrElse4 = getOrElse3;
var unsafeGet3 = unsafeGet2;
var getOption2 = getOption;
var merge3 = merge2;
var mergeAll2 = mergeAll;
var pick2 = pick;
var omit2 = omit;
var Tag2 = Tag;
var Reference2 = Reference;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Chunk.js
var TypeId7 = /* @__PURE__ */ Symbol.for("effect/Chunk");
function copy2(src, srcPos, dest, destPos, len) {
  for (let i = srcPos; i < Math.min(src.length, srcPos + len); i++) {
    dest[destPos + i - srcPos] = src[i];
  }
  return dest;
}
__name(copy2, "copy");
var emptyArray = [];
var getEquivalence6 = /* @__PURE__ */ __name((isEquivalent) => make((self, that) => self.length === that.length && toReadonlyArray(self).every((value2, i) => isEquivalent(value2, unsafeGet4(that, i)))), "getEquivalence");
var _equivalence4 = /* @__PURE__ */ getEquivalence6(equals);
var ChunkProto = {
  [TypeId7]: {
    _A: /* @__PURE__ */ __name((_) => _, "_A")
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Chunk",
      values: toReadonlyArray(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol3](that) {
    return isChunk(that) && _equivalence4(this, that);
  },
  [symbol2]() {
    return cached(this, array2(toReadonlyArray(this)));
  },
  [Symbol.iterator]() {
    switch (this.backing._tag) {
      case "IArray": {
        return this.backing.array[Symbol.iterator]();
      }
      case "IEmpty": {
        return emptyArray[Symbol.iterator]();
      }
      default: {
        return toReadonlyArray(this)[Symbol.iterator]();
      }
    }
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeChunk = /* @__PURE__ */ __name((backing) => {
  const chunk4 = Object.create(ChunkProto);
  chunk4.backing = backing;
  switch (backing._tag) {
    case "IEmpty": {
      chunk4.length = 0;
      chunk4.depth = 0;
      chunk4.left = chunk4;
      chunk4.right = chunk4;
      break;
    }
    case "IConcat": {
      chunk4.length = backing.left.length + backing.right.length;
      chunk4.depth = 1 + Math.max(backing.left.depth, backing.right.depth);
      chunk4.left = backing.left;
      chunk4.right = backing.right;
      break;
    }
    case "IArray": {
      chunk4.length = backing.array.length;
      chunk4.depth = 0;
      chunk4.left = _empty2;
      chunk4.right = _empty2;
      break;
    }
    case "ISingleton": {
      chunk4.length = 1;
      chunk4.depth = 0;
      chunk4.left = _empty2;
      chunk4.right = _empty2;
      break;
    }
    case "ISlice": {
      chunk4.length = backing.length;
      chunk4.depth = backing.chunk.depth + 1;
      chunk4.left = _empty2;
      chunk4.right = _empty2;
      break;
    }
  }
  return chunk4;
}, "makeChunk");
var isChunk = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId7), "isChunk");
var _empty2 = /* @__PURE__ */ makeChunk({
  _tag: "IEmpty"
});
var empty7 = /* @__PURE__ */ __name(() => _empty2, "empty");
var make7 = /* @__PURE__ */ __name((...as15) => unsafeFromNonEmptyArray(as15), "make");
var of3 = /* @__PURE__ */ __name((a) => makeChunk({
  _tag: "ISingleton",
  a
}), "of");
var fromIterable3 = /* @__PURE__ */ __name((self) => isChunk(self) ? self : unsafeFromArray(fromIterable2(self)), "fromIterable");
var copyToArray = /* @__PURE__ */ __name((self, array5, initial) => {
  switch (self.backing._tag) {
    case "IArray": {
      copy2(self.backing.array, 0, array5, initial, self.length);
      break;
    }
    case "IConcat": {
      copyToArray(self.left, array5, initial);
      copyToArray(self.right, array5, initial + self.left.length);
      break;
    }
    case "ISingleton": {
      array5[initial] = self.backing.a;
      break;
    }
    case "ISlice": {
      let i = 0;
      let j = initial;
      while (i < self.length) {
        array5[j] = unsafeGet4(self, i);
        i += 1;
        j += 1;
      }
      break;
    }
  }
}, "copyToArray");
var toArray_ = /* @__PURE__ */ __name((self) => toReadonlyArray(self).slice(), "toArray_");
var toArray2 = toArray_;
var toReadonlyArray_ = /* @__PURE__ */ __name((self) => {
  switch (self.backing._tag) {
    case "IEmpty": {
      return emptyArray;
    }
    case "IArray": {
      return self.backing.array;
    }
    default: {
      const arr = new Array(self.length);
      copyToArray(self, arr, 0);
      self.backing = {
        _tag: "IArray",
        array: arr
      };
      self.left = _empty2;
      self.right = _empty2;
      self.depth = 0;
      return arr;
    }
  }
}, "toReadonlyArray_");
var toReadonlyArray = toReadonlyArray_;
var reverseChunk = /* @__PURE__ */ __name((self) => {
  switch (self.backing._tag) {
    case "IEmpty":
    case "ISingleton":
      return self;
    case "IArray": {
      return makeChunk({
        _tag: "IArray",
        array: reverse2(self.backing.array)
      });
    }
    case "IConcat": {
      return makeChunk({
        _tag: "IConcat",
        left: reverse3(self.backing.right),
        right: reverse3(self.backing.left)
      });
    }
    case "ISlice":
      return unsafeFromArray(reverse2(toReadonlyArray(self)));
  }
}, "reverseChunk");
var reverse3 = reverseChunk;
var get5 = /* @__PURE__ */ dual(2, (self, index) => index < 0 || index >= self.length ? none2() : some3(unsafeGet4(self, index)));
var unsafeFromArray = /* @__PURE__ */ __name((self) => self.length === 0 ? empty7() : self.length === 1 ? of3(self[0]) : makeChunk({
  _tag: "IArray",
  array: self
}), "unsafeFromArray");
var unsafeFromNonEmptyArray = /* @__PURE__ */ __name((self) => unsafeFromArray(self), "unsafeFromNonEmptyArray");
var unsafeGet4 = /* @__PURE__ */ dual(2, (self, index) => {
  switch (self.backing._tag) {
    case "IEmpty": {
      throw new Error(`Index out of bounds`);
    }
    case "ISingleton": {
      if (index !== 0) {
        throw new Error(`Index out of bounds`);
      }
      return self.backing.a;
    }
    case "IArray": {
      if (index >= self.length || index < 0) {
        throw new Error(`Index out of bounds`);
      }
      return self.backing.array[index];
    }
    case "IConcat": {
      return index < self.left.length ? unsafeGet4(self.left, index) : unsafeGet4(self.right, index - self.left.length);
    }
    case "ISlice": {
      return unsafeGet4(self.backing.chunk, index + self.backing.offset);
    }
  }
});
var append3 = /* @__PURE__ */ dual(2, (self, a) => appendAll3(self, of3(a)));
var prepend3 = /* @__PURE__ */ dual(2, (self, elem) => appendAll3(of3(elem), self));
var take3 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return _empty2;
  } else if (n >= self.length) {
    return self;
  } else {
    switch (self.backing._tag) {
      case "ISlice": {
        return makeChunk({
          _tag: "ISlice",
          chunk: self.backing.chunk,
          length: n,
          offset: self.backing.offset
        });
      }
      case "IConcat": {
        if (n > self.left.length) {
          return makeChunk({
            _tag: "IConcat",
            left: self.left,
            right: take3(self.right, n - self.left.length)
          });
        }
        return take3(self.left, n);
      }
      default: {
        return makeChunk({
          _tag: "ISlice",
          chunk: self,
          offset: 0,
          length: n
        });
      }
    }
  }
});
var drop3 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return self;
  } else if (n >= self.length) {
    return _empty2;
  } else {
    switch (self.backing._tag) {
      case "ISlice": {
        return makeChunk({
          _tag: "ISlice",
          chunk: self.backing.chunk,
          offset: self.backing.offset + n,
          length: self.backing.length - n
        });
      }
      case "IConcat": {
        if (n > self.left.length) {
          return drop3(self.right, n - self.left.length);
        }
        return makeChunk({
          _tag: "IConcat",
          left: drop3(self.left, n),
          right: self.right
        });
      }
      default: {
        return makeChunk({
          _tag: "ISlice",
          chunk: self,
          offset: n,
          length: self.length - n
        });
      }
    }
  }
});
var dropRight2 = /* @__PURE__ */ dual(2, (self, n) => take3(self, Math.max(0, self.length - n)));
var dropWhile2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const arr = toReadonlyArray(self);
  const len = arr.length;
  let i = 0;
  while (i < len && predicate(arr[i])) {
    i++;
  }
  return drop3(self, i);
});
var prependAll3 = /* @__PURE__ */ dual(2, (self, that) => appendAll3(that, self));
var appendAll3 = /* @__PURE__ */ dual(2, (self, that) => {
  if (self.backing._tag === "IEmpty") {
    return that;
  }
  if (that.backing._tag === "IEmpty") {
    return self;
  }
  const diff12 = that.depth - self.depth;
  if (Math.abs(diff12) <= 1) {
    return makeChunk({
      _tag: "IConcat",
      left: self,
      right: that
    });
  } else if (diff12 < -1) {
    if (self.left.depth >= self.right.depth) {
      const nr = appendAll3(self.right, that);
      return makeChunk({
        _tag: "IConcat",
        left: self.left,
        right: nr
      });
    } else {
      const nrr = appendAll3(self.right.right, that);
      if (nrr.depth === self.depth - 3) {
        const nr = makeChunk({
          _tag: "IConcat",
          left: self.right.left,
          right: nrr
        });
        return makeChunk({
          _tag: "IConcat",
          left: self.left,
          right: nr
        });
      } else {
        const nl = makeChunk({
          _tag: "IConcat",
          left: self.left,
          right: self.right.left
        });
        return makeChunk({
          _tag: "IConcat",
          left: nl,
          right: nrr
        });
      }
    }
  } else {
    if (that.right.depth >= that.left.depth) {
      const nl = appendAll3(self, that.left);
      return makeChunk({
        _tag: "IConcat",
        left: nl,
        right: that.right
      });
    } else {
      const nll = appendAll3(self, that.left.left);
      if (nll.depth === that.depth - 3) {
        const nl = makeChunk({
          _tag: "IConcat",
          left: nll,
          right: that.left.right
        });
        return makeChunk({
          _tag: "IConcat",
          left: nl,
          right: that.right
        });
      } else {
        const nr = makeChunk({
          _tag: "IConcat",
          left: that.left.right,
          right: that.right
        });
        return makeChunk({
          _tag: "IConcat",
          left: nll,
          right: nr
        });
      }
    }
  }
});
var filterMap5 = /* @__PURE__ */ dual(2, (self, f) => unsafeFromArray(filterMap4(self, f)));
var filter5 = /* @__PURE__ */ dual(2, (self, predicate) => unsafeFromArray(filter4(self, predicate)));
var filterMapWhile3 = /* @__PURE__ */ dual(2, (self, f) => unsafeFromArray(filterMapWhile2(self, f)));
var compact = /* @__PURE__ */ __name((self) => filterMap5(self, identity), "compact");
var flatMap5 = /* @__PURE__ */ dual(2, (self, f) => {
  if (self.backing._tag === "ISingleton") {
    return f(self.backing.a, 0);
  }
  let out = _empty2;
  let i = 0;
  for (const k of self) {
    out = appendAll3(out, f(k, i++));
  }
  return out;
});
var forEach3 = /* @__PURE__ */ dual(2, (self, f) => toReadonlyArray(self).forEach(f));
var flatten4 = /* @__PURE__ */ flatMap5(identity);
var chunksOf3 = /* @__PURE__ */ dual(2, (self, n) => {
  const gr = [];
  let current2 = [];
  toReadonlyArray(self).forEach((a) => {
    current2.push(a);
    if (current2.length >= n) {
      gr.push(unsafeFromArray(current2));
      current2 = [];
    }
  });
  if (current2.length > 0) {
    gr.push(unsafeFromArray(current2));
  }
  return unsafeFromArray(gr);
});
var intersection3 = /* @__PURE__ */ dual(2, (self, that) => unsafeFromArray(intersection2(toReadonlyArray(self), toReadonlyArray(that))));
var isEmpty2 = /* @__PURE__ */ __name((self) => self.length === 0, "isEmpty");
var isNonEmpty = /* @__PURE__ */ __name((self) => self.length > 0, "isNonEmpty");
var head3 = /* @__PURE__ */ get5(0);
var unsafeHead2 = /* @__PURE__ */ __name((self) => unsafeGet4(self, 0), "unsafeHead");
var headNonEmpty2 = unsafeHead2;
var last2 = /* @__PURE__ */ __name((self) => get5(self, self.length - 1), "last");
var unsafeLast = /* @__PURE__ */ __name((self) => unsafeGet4(self, self.length - 1), "unsafeLast");
var lastNonEmpty2 = unsafeLast;
var map7 = /* @__PURE__ */ dual(2, (self, f) => self.backing._tag === "ISingleton" ? of3(f(self.backing.a, 0)) : unsafeFromArray(pipe(toReadonlyArray(self), map6((a, i) => f(a, i)))));
var mapAccum2 = /* @__PURE__ */ dual(3, (self, s, f) => {
  const [s1, as15] = mapAccum(self, s, f);
  return [s1, unsafeFromArray(as15)];
});
var partition3 = /* @__PURE__ */ dual(2, (self, predicate) => pipe(partition2(toReadonlyArray(self), predicate), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]));
var partitionMap4 = /* @__PURE__ */ dual(2, (self, f) => pipe(partitionMap3(toReadonlyArray(self), f), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]));
var separate3 = /* @__PURE__ */ __name((self) => pipe(separate2(toReadonlyArray(self)), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]), "separate");
var size3 = /* @__PURE__ */ __name((self) => self.length, "size");
var sort2 = /* @__PURE__ */ dual(2, (self, O) => unsafeFromArray(sort(toReadonlyArray(self), O)));
var sortWith2 = /* @__PURE__ */ dual(3, (self, f, order) => sort2(self, mapInput3(order, f)));
var splitAt2 = /* @__PURE__ */ dual(2, (self, n) => [take3(self, n), drop3(self, n)]);
var splitNonEmptyAt2 = /* @__PURE__ */ dual(2, (self, n) => {
  const _n = Math.max(1, Math.floor(n));
  return _n >= self.length ? [self, empty7()] : [take3(self, _n), drop3(self, _n)];
});
var split2 = /* @__PURE__ */ dual(2, (self, n) => chunksOf3(self, Math.ceil(self.length / Math.floor(n))));
var splitWhere2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  let i = 0;
  for (const a of toReadonlyArray(self)) {
    if (predicate(a)) {
      break;
    } else {
      i++;
    }
  }
  return splitAt2(self, i);
});
var tail2 = /* @__PURE__ */ __name((self) => self.length > 0 ? some3(drop3(self, 1)) : none2(), "tail");
var tailNonEmpty2 = /* @__PURE__ */ __name((self) => drop3(self, 1), "tailNonEmpty");
var takeRight2 = /* @__PURE__ */ dual(2, (self, n) => drop3(self, self.length - n));
var takeWhile3 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const out = [];
  for (const a of toReadonlyArray(self)) {
    if (predicate(a)) {
      out.push(a);
    } else {
      break;
    }
  }
  return unsafeFromArray(out);
});
var union3 = /* @__PURE__ */ dual(2, (self, that) => unsafeFromArray(union2(toReadonlyArray(self), toReadonlyArray(that))));
var dedupe2 = /* @__PURE__ */ __name((self) => unsafeFromArray(dedupe(toReadonlyArray(self))), "dedupe");
var dedupeAdjacent3 = /* @__PURE__ */ __name((self) => unsafeFromArray(dedupeAdjacent2(self)), "dedupeAdjacent");
var unzip2 = /* @__PURE__ */ __name((self) => {
  const [left3, right3] = unzip(self);
  return [unsafeFromArray(left3), unsafeFromArray(right3)];
}, "unzip");
var zipWith5 = /* @__PURE__ */ dual(3, (self, that, f) => unsafeFromArray(zipWith4(self, that, f)));
var zip3 = /* @__PURE__ */ dual(2, (self, that) => zipWith5(self, that, (a, b) => [a, b]));
var remove3 = /* @__PURE__ */ dual(2, (self, i) => {
  if (i < 0 || i >= self.length) return self;
  return unsafeFromArray(remove2(toReadonlyArray(self), i));
});
var removeOption2 = /* @__PURE__ */ dual(2, (self, i) => {
  if (i < 0 || i >= self.length) return none2();
  return some3(unsafeFromArray(remove2(toReadonlyArray(self), i)));
});
var modifyOption3 = /* @__PURE__ */ dual(3, (self, i, f) => {
  if (i < 0 || i >= self.length) return none2();
  return some3(unsafeFromArray(modify2(toReadonlyArray(self), i, f)));
});
var modify3 = /* @__PURE__ */ dual(3, (self, i, f) => getOrElse2(modifyOption3(self, i, f), () => self));
var replace3 = /* @__PURE__ */ dual(3, (self, i, b) => modify3(self, i, () => b));
var replaceOption3 = /* @__PURE__ */ dual(3, (self, i, b) => modifyOption3(self, i, () => b));
var makeBy3 = /* @__PURE__ */ dual(2, (n, f) => fromIterable3(makeBy2(n, f)));
var range3 = /* @__PURE__ */ __name((start3, end6) => start3 <= end6 ? makeBy3(end6 - start3 + 1, (i) => start3 + i) : of3(start3), "range");
var contains4 = contains3;
var containsWith4 = containsWith3;
var findFirst4 = findFirst3;
var findFirstIndex2 = findFirstIndex;
var findLast3 = findLast2;
var findLastIndex2 = findLastIndex;
var every5 = /* @__PURE__ */ dual(2, (self, refinement) => fromIterable2(self).every(refinement));
var some8 = /* @__PURE__ */ dual(2, (self, predicate) => fromIterable2(self).some(predicate));
var join2 = join;
var reduce4 = reduce3;
var reduceRight2 = reduceRight;
var differenceWith2 = /* @__PURE__ */ __name((isEquivalent) => {
  return dual(2, (self, that) => unsafeFromArray(differenceWith(isEquivalent)(that, self)));
}, "differenceWith");
var difference3 = /* @__PURE__ */ dual(2, (self, that) => unsafeFromArray(difference2(that, self)));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Duration.js
var TypeId8 = /* @__PURE__ */ Symbol.for("effect/Duration");
var bigint0 = /* @__PURE__ */ BigInt(0);
var bigint24 = /* @__PURE__ */ BigInt(24);
var bigint60 = /* @__PURE__ */ BigInt(60);
var bigint1e3 = /* @__PURE__ */ BigInt(1e3);
var bigint1e6 = /* @__PURE__ */ BigInt(1e6);
var bigint1e9 = /* @__PURE__ */ BigInt(1e9);
var DURATION_REGEX = /^(-?\d+(?:\.\d+)?)\s+(nanos?|micros?|millis?|seconds?|minutes?|hours?|days?|weeks?)$/;
var decode = /* @__PURE__ */ __name((input) => {
  if (isDuration(input)) {
    return input;
  } else if (isNumber(input)) {
    return millis(input);
  } else if (isBigInt(input)) {
    return nanos(input);
  } else if (Array.isArray(input) && input.length === 2 && input.every(isNumber)) {
    if (input[0] === -Infinity || input[1] === -Infinity || Number.isNaN(input[0]) || Number.isNaN(input[1])) {
      return zero;
    }
    if (input[0] === Infinity || input[1] === Infinity) {
      return infinity;
    }
    return nanos(BigInt(Math.round(input[0] * 1e9)) + BigInt(Math.round(input[1])));
  } else if (isString(input)) {
    const match33 = DURATION_REGEX.exec(input);
    if (match33) {
      const [_, valueStr, unit] = match33;
      const value2 = Number(valueStr);
      switch (unit) {
        case "nano":
        case "nanos":
          return nanos(BigInt(valueStr));
        case "micro":
        case "micros":
          return micros(BigInt(valueStr));
        case "milli":
        case "millis":
          return millis(value2);
        case "second":
        case "seconds":
          return seconds(value2);
        case "minute":
        case "minutes":
          return minutes(value2);
        case "hour":
        case "hours":
          return hours(value2);
        case "day":
        case "days":
          return days(value2);
        case "week":
        case "weeks":
          return weeks(value2);
      }
    }
  }
  throw new Error("Invalid DurationInput");
}, "decode");
var decodeUnknown = /* @__PURE__ */ liftThrowable(decode);
var zeroValue = {
  _tag: "Millis",
  millis: 0
};
var infinityValue = {
  _tag: "Infinity"
};
var DurationProto = {
  [TypeId8]: TypeId8,
  [symbol2]() {
    return cached(this, structure(this.value));
  },
  [symbol3](that) {
    return isDuration(that) && equals2(this, that);
  },
  toString() {
    return `Duration(${format2(this)})`;
  },
  toJSON() {
    switch (this.value._tag) {
      case "Millis":
        return {
          _id: "Duration",
          _tag: "Millis",
          millis: this.value.millis
        };
      case "Nanos":
        return {
          _id: "Duration",
          _tag: "Nanos",
          hrtime: toHrTime(this)
        };
      case "Infinity":
        return {
          _id: "Duration",
          _tag: "Infinity"
        };
    }
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make8 = /* @__PURE__ */ __name((input) => {
  const duration3 = Object.create(DurationProto);
  if (isNumber(input)) {
    if (isNaN(input) || input <= 0) {
      duration3.value = zeroValue;
    } else if (!Number.isFinite(input)) {
      duration3.value = infinityValue;
    } else if (!Number.isInteger(input)) {
      duration3.value = {
        _tag: "Nanos",
        nanos: BigInt(Math.round(input * 1e6))
      };
    } else {
      duration3.value = {
        _tag: "Millis",
        millis: input
      };
    }
  } else if (input <= bigint0) {
    duration3.value = zeroValue;
  } else {
    duration3.value = {
      _tag: "Nanos",
      nanos: input
    };
  }
  return duration3;
}, "make");
var isDuration = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId8), "isDuration");
var isFinite = /* @__PURE__ */ __name((self) => self.value._tag !== "Infinity", "isFinite");
var isZero = /* @__PURE__ */ __name((self) => {
  switch (self.value._tag) {
    case "Millis": {
      return self.value.millis === 0;
    }
    case "Nanos": {
      return self.value.nanos === bigint0;
    }
    case "Infinity": {
      return false;
    }
  }
}, "isZero");
var zero = /* @__PURE__ */ make8(0);
var infinity = /* @__PURE__ */ make8(Infinity);
var nanos = /* @__PURE__ */ __name((nanos2) => make8(nanos2), "nanos");
var micros = /* @__PURE__ */ __name((micros2) => make8(micros2 * bigint1e3), "micros");
var millis = /* @__PURE__ */ __name((millis2) => make8(millis2), "millis");
var seconds = /* @__PURE__ */ __name((seconds2) => make8(seconds2 * 1e3), "seconds");
var minutes = /* @__PURE__ */ __name((minutes2) => make8(minutes2 * 6e4), "minutes");
var hours = /* @__PURE__ */ __name((hours2) => make8(hours2 * 36e5), "hours");
var days = /* @__PURE__ */ __name((days2) => make8(days2 * 864e5), "days");
var weeks = /* @__PURE__ */ __name((weeks2) => make8(weeks2 * 6048e5), "weeks");
var toMillis = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 1e6, "onNanos")
}), "toMillis");
var toSeconds = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2 / 1e3, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 1e9, "onNanos")
}), "toSeconds");
var toMinutes = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2 / 6e4, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 6e10, "onNanos")
}), "toMinutes");
var toHours = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2 / 36e5, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 36e11, "onNanos")
}), "toHours");
var toDays = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2 / 864e5, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 864e11, "onNanos")
}), "toDays");
var toWeeks = /* @__PURE__ */ __name((self) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => millis2 / 6048e5, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => Number(nanos2) / 6048e11, "onNanos")
}), "toWeeks");
var toNanos = /* @__PURE__ */ __name((self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      return none2();
    case "Nanos":
      return some3(_self.value.nanos);
    case "Millis":
      return some3(BigInt(Math.round(_self.value.millis * 1e6)));
  }
}, "toNanos");
var unsafeToNanos = /* @__PURE__ */ __name((self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      throw new Error("Cannot convert infinite duration to nanos");
    case "Nanos":
      return _self.value.nanos;
    case "Millis":
      return BigInt(Math.round(_self.value.millis * 1e6));
  }
}, "unsafeToNanos");
var toHrTime = /* @__PURE__ */ __name((self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      return [Infinity, 0];
    case "Nanos":
      return [Number(_self.value.nanos / bigint1e9), Number(_self.value.nanos % bigint1e9)];
    case "Millis":
      return [Math.floor(_self.value.millis / 1e3), Math.round(_self.value.millis % 1e3 * 1e6)];
  }
}, "toHrTime");
var match5 = /* @__PURE__ */ dual(2, (self, options) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Nanos":
      return options.onNanos(_self.value.nanos);
    case "Infinity":
      return options.onMillis(Infinity);
    case "Millis":
      return options.onMillis(_self.value.millis);
  }
});
var matchWith = /* @__PURE__ */ dual(3, (self, that, options) => {
  const _self = decode(self);
  const _that = decode(that);
  if (_self.value._tag === "Infinity" || _that.value._tag === "Infinity") {
    return options.onMillis(toMillis(_self), toMillis(_that));
  } else if (_self.value._tag === "Nanos" || _that.value._tag === "Nanos") {
    const selfNanos = _self.value._tag === "Nanos" ? _self.value.nanos : BigInt(Math.round(_self.value.millis * 1e6));
    const thatNanos = _that.value._tag === "Nanos" ? _that.value.nanos : BigInt(Math.round(_that.value.millis * 1e6));
    return options.onNanos(selfNanos, thatNanos);
  }
  return options.onMillis(_self.value.millis, _that.value.millis);
});
var Order3 = /* @__PURE__ */ make2((self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 < that2 ? -1 : self2 > that2 ? 1 : 0, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 < that2 ? -1 : self2 > that2 ? 1 : 0, "onNanos")
}));
var between3 = /* @__PURE__ */ between(/* @__PURE__ */ mapInput3(Order3, decode));
var Equivalence3 = /* @__PURE__ */ __name((self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 === that2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 === that2, "onNanos")
}), "Equivalence");
var _min = /* @__PURE__ */ min(Order3);
var min4 = /* @__PURE__ */ dual(2, (self, that) => _min(decode(self), decode(that)));
var _max = /* @__PURE__ */ max(Order3);
var max4 = /* @__PURE__ */ dual(2, (self, that) => _max(decode(self), decode(that)));
var _clamp = /* @__PURE__ */ clamp(Order3);
var clamp4 = /* @__PURE__ */ dual(2, (self, options) => _clamp(decode(self), {
  minimum: decode(options.minimum),
  maximum: decode(options.maximum)
}));
var divide2 = /* @__PURE__ */ dual(2, (self, by) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => {
    if (by === 0 || isNaN(by) || !Number.isFinite(by)) {
      return none2();
    }
    return some3(make8(millis2 / by));
  }, "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => {
    if (isNaN(by) || by <= 0 || !Number.isFinite(by)) {
      return none2();
    }
    try {
      return some3(make8(nanos2 / BigInt(by)));
    } catch {
      return none2();
    }
  }, "onNanos")
}));
var unsafeDivide2 = /* @__PURE__ */ dual(2, (self, by) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => make8(millis2 / by), "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => {
    if (isNaN(by) || by < 0 || Object.is(by, -0)) {
      return zero;
    } else if (Object.is(by, 0) || !Number.isFinite(by)) {
      return infinity;
    }
    return make8(nanos2 / BigInt(by));
  }, "onNanos")
}));
var times = /* @__PURE__ */ dual(2, (self, times2) => match5(self, {
  onMillis: /* @__PURE__ */ __name((millis2) => make8(millis2 * times2), "onMillis"),
  onNanos: /* @__PURE__ */ __name((nanos2) => make8(nanos2 * BigInt(times2)), "onNanos")
}));
var subtract2 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => make8(self2 - that2), "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => make8(self2 - that2), "onNanos")
}));
var sum2 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => make8(self2 + that2), "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => make8(self2 + that2), "onNanos")
}));
var lessThan3 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 < that2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 < that2, "onNanos")
}));
var lessThanOrEqualTo3 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 <= that2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 <= that2, "onNanos")
}));
var greaterThan3 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 > that2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 > that2, "onNanos")
}));
var greaterThanOrEqualTo3 = /* @__PURE__ */ dual(2, (self, that) => matchWith(self, that, {
  onMillis: /* @__PURE__ */ __name((self2, that2) => self2 >= that2, "onMillis"),
  onNanos: /* @__PURE__ */ __name((self2, that2) => self2 >= that2, "onNanos")
}));
var equals2 = /* @__PURE__ */ dual(2, (self, that) => Equivalence3(decode(self), decode(that)));
var parts = /* @__PURE__ */ __name((self) => {
  const duration3 = decode(self);
  if (duration3.value._tag === "Infinity") {
    return {
      days: Infinity,
      hours: Infinity,
      minutes: Infinity,
      seconds: Infinity,
      millis: Infinity,
      nanos: Infinity
    };
  }
  const nanos2 = unsafeToNanos(duration3);
  const ms = nanos2 / bigint1e6;
  const sec = ms / bigint1e3;
  const min8 = sec / bigint60;
  const hr = min8 / bigint60;
  const days2 = hr / bigint24;
  return {
    days: Number(days2),
    hours: Number(hr % bigint24),
    minutes: Number(min8 % bigint60),
    seconds: Number(sec % bigint60),
    millis: Number(ms % bigint1e3),
    nanos: Number(nanos2 % bigint1e6)
  };
}, "parts");
var format2 = /* @__PURE__ */ __name((self) => {
  const duration3 = decode(self);
  if (duration3.value._tag === "Infinity") {
    return "Infinity";
  }
  if (isZero(duration3)) {
    return "0";
  }
  const fragments = parts(duration3);
  const pieces = [];
  if (fragments.days !== 0) {
    pieces.push(`${fragments.days}d`);
  }
  if (fragments.hours !== 0) {
    pieces.push(`${fragments.hours}h`);
  }
  if (fragments.minutes !== 0) {
    pieces.push(`${fragments.minutes}m`);
  }
  if (fragments.seconds !== 0) {
    pieces.push(`${fragments.seconds}s`);
  }
  if (fragments.millis !== 0) {
    pieces.push(`${fragments.millis}ms`);
  }
  if (fragments.nanos !== 0) {
    pieces.push(`${fragments.nanos}ns`);
  }
  return pieces.join(" ");
}, "format");
var unsafeFormatIso = /* @__PURE__ */ __name((self) => {
  const duration3 = decode(self);
  if (!isFinite(duration3)) {
    throw new RangeError("Cannot format infinite duration");
  }
  const fragments = [];
  const {
    days: days2,
    hours: hours2,
    millis: millis2,
    minutes: minutes2,
    nanos: nanos2,
    seconds: seconds2
  } = parts(duration3);
  let rest = days2;
  if (rest >= 365) {
    const years = Math.floor(rest / 365);
    rest %= 365;
    fragments.push(`${years}Y`);
  }
  if (rest >= 30) {
    const months = Math.floor(rest / 30);
    rest %= 30;
    fragments.push(`${months}M`);
  }
  if (rest >= 7) {
    const weeks2 = Math.floor(rest / 7);
    rest %= 7;
    fragments.push(`${weeks2}W`);
  }
  if (rest > 0) {
    fragments.push(`${rest}D`);
  }
  if (hours2 !== 0 || minutes2 !== 0 || seconds2 !== 0 || millis2 !== 0 || nanos2 !== 0) {
    fragments.push("T");
    if (hours2 !== 0) {
      fragments.push(`${hours2}H`);
    }
    if (minutes2 !== 0) {
      fragments.push(`${minutes2}M`);
    }
    if (seconds2 !== 0 || millis2 !== 0 || nanos2 !== 0) {
      const total = BigInt(seconds2) * bigint1e9 + BigInt(millis2) * bigint1e6 + BigInt(nanos2);
      const str = (Number(total) / 1e9).toFixed(9).replace(/\.?0+$/, "");
      fragments.push(`${str}S`);
    }
  }
  return `P${fragments.join("") || "T0S"}`;
}, "unsafeFormatIso");
var formatIso = /* @__PURE__ */ __name((self) => {
  const duration3 = decode(self);
  return isFinite(duration3) ? some3(unsafeFormatIso(duration3)) : none2();
}, "formatIso");
var fromIso = /* @__PURE__ */ __name((iso) => {
  const result = DURATION_ISO_REGEX.exec(iso);
  if (result == null) {
    return none2();
  }
  const [years, months, weeks2, days2, hours2, mins, secs] = result.slice(1, 8).map((_) => _ ? Number(_) : 0);
  const value2 = years * 365 * 24 * 60 * 60 + months * 30 * 24 * 60 * 60 + weeks2 * 7 * 24 * 60 * 60 + days2 * 24 * 60 * 60 + hours2 * 60 * 60 + mins * 60 + secs;
  return some3(seconds(value2));
}, "fromIso");
var DURATION_ISO_REGEX = /^P(?!$)(?:(\d+)Y)?(?:(\d+)M)?(?:(\d+)W)?(?:(\d+)D)?(?:T(?!$)(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?)?$/;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap/config.js
var SIZE = 5;
var BUCKET_SIZE = /* @__PURE__ */ Math.pow(2, SIZE);
var MASK = BUCKET_SIZE - 1;
var MAX_INDEX_NODE = BUCKET_SIZE / 2;
var MIN_ARRAY_NODE = BUCKET_SIZE / 4;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap/bitwise.js
function popcount(x) {
  x -= x >> 1 & 1431655765;
  x = (x & 858993459) + (x >> 2 & 858993459);
  x = x + (x >> 4) & 252645135;
  x += x >> 8;
  x += x >> 16;
  return x & 127;
}
__name(popcount, "popcount");
function hashFragment(shift2, h) {
  return h >>> shift2 & MASK;
}
__name(hashFragment, "hashFragment");
function toBitmap(x) {
  return 1 << x;
}
__name(toBitmap, "toBitmap");
function fromBitmap(bitmap, bit) {
  return popcount(bitmap & bit - 1);
}
__name(fromBitmap, "fromBitmap");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stack.js
var make9 = /* @__PURE__ */ __name((value2, previous2) => ({
  value: value2,
  previous: previous2
}), "make");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap/array.js
function arrayUpdate(mutate6, at5, v, arr) {
  let out = arr;
  if (!mutate6) {
    const len = arr.length;
    out = new Array(len);
    for (let i = 0; i < len; ++i) out[i] = arr[i];
  }
  out[at5] = v;
  return out;
}
__name(arrayUpdate, "arrayUpdate");
function arraySpliceOut(mutate6, at5, arr) {
  const newLen = arr.length - 1;
  let i = 0;
  let g = 0;
  let out = arr;
  if (mutate6) {
    i = g = at5;
  } else {
    out = new Array(newLen);
    while (i < at5) out[g++] = arr[i++];
  }
  ++i;
  while (i <= newLen) out[g++] = arr[i++];
  if (mutate6) {
    out.length = newLen;
  }
  return out;
}
__name(arraySpliceOut, "arraySpliceOut");
function arraySpliceIn(mutate6, at5, v, arr) {
  const len = arr.length;
  if (mutate6) {
    let i2 = len;
    while (i2 >= at5) arr[i2--] = arr[i2];
    arr[at5] = v;
    return arr;
  }
  let i = 0, g = 0;
  const out = new Array(len + 1);
  while (i < at5) out[g++] = arr[i++];
  out[at5] = v;
  while (i < len) out[++g] = arr[i++];
  return out;
}
__name(arraySpliceIn, "arraySpliceIn");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap/node.js
var EmptyNode = class _EmptyNode {
  static {
    __name(this, "EmptyNode");
  }
  _tag = "EmptyNode";
  modify(edit, _shift, f, hash2, key, size25) {
    const v = f(none2());
    if (isNone2(v)) return new _EmptyNode();
    ++size25.value;
    return new LeafNode(edit, hash2, key, v);
  }
};
function isEmptyNode(a) {
  return isTagged(a, "EmptyNode");
}
__name(isEmptyNode, "isEmptyNode");
function isLeafNode(node) {
  return isEmptyNode(node) || node._tag === "LeafNode" || node._tag === "CollisionNode";
}
__name(isLeafNode, "isLeafNode");
function canEditNode(node, edit) {
  return isEmptyNode(node) ? false : edit === node.edit;
}
__name(canEditNode, "canEditNode");
var LeafNode = class _LeafNode {
  static {
    __name(this, "LeafNode");
  }
  edit;
  hash;
  key;
  value;
  _tag = "LeafNode";
  constructor(edit, hash2, key, value2) {
    this.edit = edit;
    this.hash = hash2;
    this.key = key;
    this.value = value2;
  }
  modify(edit, shift2, f, hash2, key, size25) {
    if (equals(key, this.key)) {
      const v2 = f(this.value);
      if (v2 === this.value) return this;
      else if (isNone2(v2)) {
        --size25.value;
        return new EmptyNode();
      }
      if (canEditNode(this, edit)) {
        this.value = v2;
        return this;
      }
      return new _LeafNode(edit, hash2, key, v2);
    }
    const v = f(none2());
    if (isNone2(v)) return this;
    ++size25.value;
    return mergeLeaves(edit, shift2, this.hash, this, hash2, new _LeafNode(edit, hash2, key, v));
  }
};
var CollisionNode = class _CollisionNode {
  static {
    __name(this, "CollisionNode");
  }
  edit;
  hash;
  children;
  _tag = "CollisionNode";
  constructor(edit, hash2, children3) {
    this.edit = edit;
    this.hash = hash2;
    this.children = children3;
  }
  modify(edit, shift2, f, hash2, key, size25) {
    if (hash2 === this.hash) {
      const canEdit = canEditNode(this, edit);
      const list = this.updateCollisionList(canEdit, edit, this.hash, this.children, f, key, size25);
      if (list === this.children) return this;
      return list.length > 1 ? new _CollisionNode(edit, this.hash, list) : list[0];
    }
    const v = f(none2());
    if (isNone2(v)) return this;
    ++size25.value;
    return mergeLeaves(edit, shift2, this.hash, this, hash2, new LeafNode(edit, hash2, key, v));
  }
  updateCollisionList(mutate6, edit, hash2, list, f, key, size25) {
    const len = list.length;
    for (let i = 0; i < len; ++i) {
      const child = list[i];
      if ("key" in child && equals(key, child.key)) {
        const value2 = child.value;
        const newValue2 = f(value2);
        if (newValue2 === value2) return list;
        if (isNone2(newValue2)) {
          --size25.value;
          return arraySpliceOut(mutate6, i, list);
        }
        return arrayUpdate(mutate6, i, new LeafNode(edit, hash2, key, newValue2), list);
      }
    }
    const newValue = f(none2());
    if (isNone2(newValue)) return list;
    ++size25.value;
    return arrayUpdate(mutate6, len, new LeafNode(edit, hash2, key, newValue), list);
  }
};
var IndexedNode = class _IndexedNode {
  static {
    __name(this, "IndexedNode");
  }
  edit;
  mask;
  children;
  _tag = "IndexedNode";
  constructor(edit, mask, children3) {
    this.edit = edit;
    this.mask = mask;
    this.children = children3;
  }
  modify(edit, shift2, f, hash2, key, size25) {
    const mask = this.mask;
    const children3 = this.children;
    const frag = hashFragment(shift2, hash2);
    const bit = toBitmap(frag);
    const indx = fromBitmap(mask, bit);
    const exists6 = mask & bit;
    const canEdit = canEditNode(this, edit);
    if (!exists6) {
      const _newChild = new EmptyNode().modify(edit, shift2 + SIZE, f, hash2, key, size25);
      if (!_newChild) return this;
      return children3.length >= MAX_INDEX_NODE ? expand(edit, frag, _newChild, mask, children3) : new _IndexedNode(edit, mask | bit, arraySpliceIn(canEdit, indx, _newChild, children3));
    }
    const current2 = children3[indx];
    const child = current2.modify(edit, shift2 + SIZE, f, hash2, key, size25);
    if (current2 === child) return this;
    let bitmap = mask;
    let newChildren;
    if (isEmptyNode(child)) {
      bitmap &= ~bit;
      if (!bitmap) return new EmptyNode();
      if (children3.length <= 2 && isLeafNode(children3[indx ^ 1])) {
        return children3[indx ^ 1];
      }
      newChildren = arraySpliceOut(canEdit, indx, children3);
    } else {
      newChildren = arrayUpdate(canEdit, indx, child, children3);
    }
    if (canEdit) {
      this.mask = bitmap;
      this.children = newChildren;
      return this;
    }
    return new _IndexedNode(edit, bitmap, newChildren);
  }
};
var ArrayNode = class _ArrayNode {
  static {
    __name(this, "ArrayNode");
  }
  edit;
  size;
  children;
  _tag = "ArrayNode";
  constructor(edit, size25, children3) {
    this.edit = edit;
    this.size = size25;
    this.children = children3;
  }
  modify(edit, shift2, f, hash2, key, size25) {
    let count5 = this.size;
    const children3 = this.children;
    const frag = hashFragment(shift2, hash2);
    const child = children3[frag];
    const newChild = (child || new EmptyNode()).modify(edit, shift2 + SIZE, f, hash2, key, size25);
    if (child === newChild) return this;
    const canEdit = canEditNode(this, edit);
    let newChildren;
    if (isEmptyNode(child) && !isEmptyNode(newChild)) {
      ++count5;
      newChildren = arrayUpdate(canEdit, frag, newChild, children3);
    } else if (!isEmptyNode(child) && isEmptyNode(newChild)) {
      --count5;
      if (count5 <= MIN_ARRAY_NODE) {
        return pack(edit, count5, frag, children3);
      }
      newChildren = arrayUpdate(canEdit, frag, new EmptyNode(), children3);
    } else {
      newChildren = arrayUpdate(canEdit, frag, newChild, children3);
    }
    if (canEdit) {
      this.size = count5;
      this.children = newChildren;
      return this;
    }
    return new _ArrayNode(edit, count5, newChildren);
  }
};
function pack(edit, count5, removed, elements) {
  const children3 = new Array(count5 - 1);
  let g = 0;
  let bitmap = 0;
  for (let i = 0, len = elements.length; i < len; ++i) {
    if (i !== removed) {
      const elem = elements[i];
      if (elem && !isEmptyNode(elem)) {
        children3[g++] = elem;
        bitmap |= 1 << i;
      }
    }
  }
  return new IndexedNode(edit, bitmap, children3);
}
__name(pack, "pack");
function expand(edit, frag, child, bitmap, subNodes) {
  const arr = [];
  let bit = bitmap;
  let count5 = 0;
  for (let i = 0; bit; ++i) {
    if (bit & 1) arr[i] = subNodes[count5++];
    bit >>>= 1;
  }
  arr[frag] = child;
  return new ArrayNode(edit, count5 + 1, arr);
}
__name(expand, "expand");
function mergeLeavesInner(edit, shift2, h1, n1, h2, n2) {
  if (h1 === h2) return new CollisionNode(edit, h1, [n2, n1]);
  const subH1 = hashFragment(shift2, h1);
  const subH2 = hashFragment(shift2, h2);
  if (subH1 === subH2) {
    return (child) => new IndexedNode(edit, toBitmap(subH1) | toBitmap(subH2), [child]);
  } else {
    const children3 = subH1 < subH2 ? [n1, n2] : [n2, n1];
    return new IndexedNode(edit, toBitmap(subH1) | toBitmap(subH2), children3);
  }
}
__name(mergeLeavesInner, "mergeLeavesInner");
function mergeLeaves(edit, shift2, h1, n1, h2, n2) {
  let stack = void 0;
  let currentShift = shift2;
  while (true) {
    const res = mergeLeavesInner(edit, currentShift, h1, n1, h2, n2);
    if (typeof res === "function") {
      stack = make9(res, stack);
      currentShift = currentShift + SIZE;
    } else {
      let final = res;
      while (stack != null) {
        final = stack.value(final);
        stack = stack.previous;
      }
      return final;
    }
  }
}
__name(mergeLeaves, "mergeLeaves");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap.js
var HashMapSymbolKey = "effect/HashMap";
var HashMapTypeId = /* @__PURE__ */ Symbol.for(HashMapSymbolKey);
var HashMapProto = {
  [HashMapTypeId]: HashMapTypeId,
  [Symbol.iterator]() {
    return new HashMapIterator(this, (k, v) => [k, v]);
  },
  [symbol2]() {
    let hash2 = hash(HashMapSymbolKey);
    for (const item of this) {
      hash2 ^= pipe(hash(item[0]), combine2(hash(item[1])));
    }
    return cached(this, hash2);
  },
  [symbol3](that) {
    if (isHashMap(that)) {
      if (that._size !== this._size) {
        return false;
      }
      for (const item of this) {
        const elem = pipe(that, getHash(item[0], hash(item[0])));
        if (isNone2(elem)) {
          return false;
        } else {
          if (!equals(item[1], elem.value)) {
            return false;
          }
        }
      }
      return true;
    }
    return false;
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "HashMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl = /* @__PURE__ */ __name((editable, edit, root, size25) => {
  const map36 = Object.create(HashMapProto);
  map36._editable = editable;
  map36._edit = edit;
  map36._root = root;
  map36._size = size25;
  return map36;
}, "makeImpl");
var HashMapIterator = class _HashMapIterator {
  static {
    __name(this, "HashMapIterator");
  }
  map;
  f;
  v;
  constructor(map36, f) {
    this.map = map36;
    this.f = f;
    this.v = visitLazy(this.map._root, this.f, void 0);
  }
  next() {
    if (isNone2(this.v)) {
      return {
        done: true,
        value: void 0
      };
    }
    const v0 = this.v.value;
    this.v = applyCont(v0.cont);
    return {
      done: false,
      value: v0.value
    };
  }
  [Symbol.iterator]() {
    return new _HashMapIterator(this.map, this.f);
  }
};
var applyCont = /* @__PURE__ */ __name((cont) => cont ? visitLazyChildren(cont[0], cont[1], cont[2], cont[3], cont[4]) : none2(), "applyCont");
var visitLazy = /* @__PURE__ */ __name((node, f, cont = void 0) => {
  switch (node._tag) {
    case "LeafNode": {
      if (isSome2(node.value)) {
        return some3({
          value: f(node.key, node.value.value),
          cont
        });
      }
      return applyCont(cont);
    }
    case "CollisionNode":
    case "ArrayNode":
    case "IndexedNode": {
      const children3 = node.children;
      return visitLazyChildren(children3.length, children3, 0, f, cont);
    }
    default: {
      return applyCont(cont);
    }
  }
}, "visitLazy");
var visitLazyChildren = /* @__PURE__ */ __name((len, children3, i, f, cont) => {
  while (i < len) {
    const child = children3[i++];
    if (child && !isEmptyNode(child)) {
      return visitLazy(child, f, [len, children3, i, f, cont]);
    }
  }
  return applyCont(cont);
}, "visitLazyChildren");
var _empty3 = /* @__PURE__ */ makeImpl(false, 0, /* @__PURE__ */ new EmptyNode(), 0);
var empty8 = /* @__PURE__ */ __name(() => _empty3, "empty");
var make10 = /* @__PURE__ */ __name((...entries3) => fromIterable4(entries3), "make");
var fromIterable4 = /* @__PURE__ */ __name((entries3) => {
  const map36 = beginMutation(empty8());
  for (const entry of entries3) {
    set2(map36, entry[0], entry[1]);
  }
  return endMutation(map36);
}, "fromIterable");
var isHashMap = /* @__PURE__ */ __name((u) => hasProperty(u, HashMapTypeId), "isHashMap");
var isEmpty3 = /* @__PURE__ */ __name((self) => self && isEmptyNode(self._root), "isEmpty");
var get6 = /* @__PURE__ */ dual(2, (self, key) => getHash(self, key, hash(key)));
var getHash = /* @__PURE__ */ dual(3, (self, key, hash2) => {
  let node = self._root;
  let shift2 = 0;
  while (true) {
    switch (node._tag) {
      case "LeafNode": {
        return equals(key, node.key) ? node.value : none2();
      }
      case "CollisionNode": {
        if (hash2 === node.hash) {
          const children3 = node.children;
          for (let i = 0, len = children3.length; i < len; ++i) {
            const child = children3[i];
            if ("key" in child && equals(key, child.key)) {
              return child.value;
            }
          }
        }
        return none2();
      }
      case "IndexedNode": {
        const frag = hashFragment(shift2, hash2);
        const bit = toBitmap(frag);
        if (node.mask & bit) {
          node = node.children[fromBitmap(node.mask, bit)];
          shift2 += SIZE;
          break;
        }
        return none2();
      }
      case "ArrayNode": {
        node = node.children[hashFragment(shift2, hash2)];
        if (node) {
          shift2 += SIZE;
          break;
        }
        return none2();
      }
      default:
        return none2();
    }
  }
});
var unsafeGet5 = /* @__PURE__ */ dual(2, (self, key) => {
  const element = getHash(self, key, hash(key));
  if (isNone2(element)) {
    throw new Error("Expected map to contain key");
  }
  return element.value;
});
var has2 = /* @__PURE__ */ dual(2, (self, key) => isSome2(getHash(self, key, hash(key))));
var hasHash = /* @__PURE__ */ dual(3, (self, key, hash2) => isSome2(getHash(self, key, hash2)));
var hasBy = /* @__PURE__ */ dual(2, (self, predicate) => isSome2(findFirst5(self, predicate)));
var set2 = /* @__PURE__ */ dual(3, (self, key, value2) => modifyAt(self, key, () => some3(value2)));
var setTree = /* @__PURE__ */ dual(3, (self, newRoot, newSize) => {
  if (self._editable) {
    ;
    self._root = newRoot;
    self._size = newSize;
    return self;
  }
  return newRoot === self._root ? self : makeImpl(self._editable, self._edit, newRoot, newSize);
});
var keys2 = /* @__PURE__ */ __name((self) => new HashMapIterator(self, (key) => key), "keys");
var values2 = /* @__PURE__ */ __name((self) => new HashMapIterator(self, (_, value2) => value2), "values");
var entries = /* @__PURE__ */ __name((self) => new HashMapIterator(self, (key, value2) => [key, value2]), "entries");
var size4 = /* @__PURE__ */ __name((self) => self._size, "size");
var countBy3 = /* @__PURE__ */ dual(2, (self, f) => {
  let count5 = 0;
  for (const [k, a] of self) {
    if (f(a, k)) {
      count5++;
    }
  }
  return count5;
});
var beginMutation = /* @__PURE__ */ __name((self) => makeImpl(true, self._edit + 1, self._root, self._size), "beginMutation");
var endMutation = /* @__PURE__ */ __name((self) => {
  ;
  self._editable = false;
  return self;
}, "endMutation");
var mutate = /* @__PURE__ */ dual(2, (self, f) => {
  const transient = beginMutation(self);
  f(transient);
  return endMutation(transient);
});
var modifyAt = /* @__PURE__ */ dual(3, (self, key, f) => modifyHash(self, key, hash(key), f));
var modifyHash = /* @__PURE__ */ dual(4, (self, key, hash2, f) => {
  const size25 = {
    value: self._size
  };
  const newRoot = self._root.modify(self._editable ? self._edit : NaN, 0, f, hash2, key, size25);
  return pipe(self, setTree(newRoot, size25.value));
});
var modify4 = /* @__PURE__ */ dual(3, (self, key, f) => modifyAt(self, key, map2(f)));
var union4 = /* @__PURE__ */ dual(2, (self, that) => {
  const result = beginMutation(self);
  forEach4(that, (v, k) => set2(result, k, v));
  return endMutation(result);
});
var remove4 = /* @__PURE__ */ dual(2, (self, key) => modifyAt(self, key, none2));
var removeMany = /* @__PURE__ */ dual(2, (self, keys7) => mutate(self, (map36) => {
  for (const key of keys7) {
    remove4(key)(map36);
  }
}));
var map8 = /* @__PURE__ */ dual(2, (self, f) => reduce5(self, empty8(), (map36, value2, key) => set2(map36, key, f(value2, key))));
var flatMap6 = /* @__PURE__ */ dual(2, (self, f) => reduce5(self, empty8(), (zero2, value2, key) => mutate(zero2, (map36) => forEach4(f(value2, key), (value3, key2) => set2(map36, key2, value3)))));
var forEach4 = /* @__PURE__ */ dual(2, (self, f) => reduce5(self, void 0, (_, value2, key) => f(value2, key)));
var reduce5 = /* @__PURE__ */ dual(3, (self, zero2, f) => {
  const root = self._root;
  if (root._tag === "LeafNode") {
    return isSome2(root.value) ? f(zero2, root.value.value, root.key) : zero2;
  }
  if (root._tag === "EmptyNode") {
    return zero2;
  }
  const toVisit = [root.children];
  let children3;
  while (children3 = toVisit.pop()) {
    for (let i = 0, len = children3.length; i < len; ) {
      const child = children3[i++];
      if (child && !isEmptyNode(child)) {
        if (child._tag === "LeafNode") {
          if (isSome2(child.value)) {
            zero2 = f(zero2, child.value.value, child.key);
          }
        } else {
          toVisit.push(child.children);
        }
      }
    }
  }
  return zero2;
});
var filter6 = /* @__PURE__ */ dual(2, (self, f) => mutate(empty8(), (map36) => {
  for (const [k, a] of self) {
    if (f(a, k)) {
      set2(map36, k, a);
    }
  }
}));
var compact2 = /* @__PURE__ */ __name((self) => filterMap6(self, identity), "compact");
var filterMap6 = /* @__PURE__ */ dual(2, (self, f) => mutate(empty8(), (map36) => {
  for (const [k, a] of self) {
    const option5 = f(a, k);
    if (isSome2(option5)) {
      set2(map36, k, option5.value);
    }
  }
}));
var findFirst5 = /* @__PURE__ */ dual(2, (self, predicate) => {
  for (const ka of self) {
    if (predicate(ka[1], ka[0])) {
      return some3(ka);
    }
  }
  return none2();
});
var some9 = /* @__PURE__ */ dual(2, (self, predicate) => {
  for (const ka of self) {
    if (predicate(ka[1], ka[0])) {
      return true;
    }
  }
  return false;
});
var every6 = /* @__PURE__ */ dual(2, (self, predicate) => !some9(self, (a, k) => !predicate(a, k)));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashSet.js
var HashSetSymbolKey = "effect/HashSet";
var HashSetTypeId = /* @__PURE__ */ Symbol.for(HashSetSymbolKey);
var HashSetProto = {
  [HashSetTypeId]: HashSetTypeId,
  [Symbol.iterator]() {
    return keys2(this._keyMap);
  },
  [symbol2]() {
    return cached(this, combine2(hash(this._keyMap))(hash(HashSetSymbolKey)));
  },
  [symbol3](that) {
    if (isHashSet(that)) {
      return size4(this._keyMap) === size4(that._keyMap) && equals(this._keyMap, that._keyMap);
    }
    return false;
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "HashSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl2 = /* @__PURE__ */ __name((keyMap) => {
  const set14 = Object.create(HashSetProto);
  set14._keyMap = keyMap;
  return set14;
}, "makeImpl");
var isHashSet = /* @__PURE__ */ __name((u) => hasProperty(u, HashSetTypeId), "isHashSet");
var _empty4 = /* @__PURE__ */ makeImpl2(/* @__PURE__ */ empty8());
var empty9 = /* @__PURE__ */ __name(() => _empty4, "empty");
var fromIterable5 = /* @__PURE__ */ __name((elements) => {
  const set14 = beginMutation2(empty9());
  for (const value2 of elements) {
    add3(set14, value2);
  }
  return endMutation2(set14);
}, "fromIterable");
var make11 = /* @__PURE__ */ __name((...elements) => {
  const set14 = beginMutation2(empty9());
  for (const value2 of elements) {
    add3(set14, value2);
  }
  return endMutation2(set14);
}, "make");
var has3 = /* @__PURE__ */ dual(2, (self, value2) => has2(self._keyMap, value2));
var some10 = /* @__PURE__ */ dual(2, (self, f) => {
  let found = false;
  for (const value2 of self) {
    found = f(value2);
    if (found) {
      break;
    }
  }
  return found;
});
var every7 = /* @__PURE__ */ dual(2, (self, refinement) => !some10(self, (a) => !refinement(a)));
var isSubset = /* @__PURE__ */ dual(2, (self, that) => every7(self, (value2) => has3(that, value2)));
var values3 = /* @__PURE__ */ __name((self) => keys2(self._keyMap), "values");
var size5 = /* @__PURE__ */ __name((self) => size4(self._keyMap), "size");
var beginMutation2 = /* @__PURE__ */ __name((self) => makeImpl2(beginMutation(self._keyMap)), "beginMutation");
var endMutation2 = /* @__PURE__ */ __name((self) => {
  ;
  self._keyMap._editable = false;
  return self;
}, "endMutation");
var mutate2 = /* @__PURE__ */ dual(2, (self, f) => {
  const transient = beginMutation2(self);
  f(transient);
  return endMutation2(transient);
});
var add3 = /* @__PURE__ */ dual(2, (self, value2) => self._keyMap._editable ? (set2(value2, true)(self._keyMap), self) : makeImpl2(set2(value2, true)(self._keyMap)));
var remove5 = /* @__PURE__ */ dual(2, (self, value2) => self._keyMap._editable ? (remove4(value2)(self._keyMap), self) : makeImpl2(remove4(value2)(self._keyMap)));
var difference4 = /* @__PURE__ */ dual(2, (self, that) => mutate2(self, (set14) => {
  for (const value2 of that) {
    remove5(set14, value2);
  }
}));
var intersection4 = /* @__PURE__ */ dual(2, (self, that) => mutate2(empty9(), (set14) => {
  for (const value2 of that) {
    if (has3(value2)(self)) {
      add3(value2)(set14);
    }
  }
}));
var union5 = /* @__PURE__ */ dual(2, (self, that) => mutate2(empty9(), (set14) => {
  forEach5(self, (value2) => add3(set14, value2));
  for (const value2 of that) {
    add3(set14, value2);
  }
}));
var toggle = /* @__PURE__ */ dual(2, (self, value2) => has3(self, value2) ? remove5(self, value2) : add3(self, value2));
var map9 = /* @__PURE__ */ dual(2, (self, f) => mutate2(empty9(), (set14) => {
  forEach5(self, (a) => {
    const b = f(a);
    if (!has3(set14, b)) {
      add3(set14, b);
    }
  });
}));
var flatMap7 = /* @__PURE__ */ dual(2, (self, f) => mutate2(empty9(), (set14) => {
  forEach5(self, (a) => {
    for (const b of f(a)) {
      if (!has3(set14, b)) {
        add3(set14, b);
      }
    }
  });
}));
var forEach5 = /* @__PURE__ */ dual(2, (self, f) => forEach4(self._keyMap, (_, k) => f(k)));
var reduce6 = /* @__PURE__ */ dual(3, (self, zero2, f) => reduce5(self._keyMap, zero2, (z, _, a) => f(z, a)));
var filter7 = /* @__PURE__ */ dual(2, (self, f) => {
  return mutate2(empty9(), (set14) => {
    const iterator = values3(self);
    let next4;
    while (!(next4 = iterator.next()).done) {
      const value2 = next4.value;
      if (f(value2)) {
        add3(set14, value2);
      }
    }
  });
});
var partition4 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const iterator = values3(self);
  let next4;
  const right3 = beginMutation2(empty9());
  const left3 = beginMutation2(empty9());
  while (!(next4 = iterator.next()).done) {
    const value2 = next4.value;
    if (predicate(value2)) {
      add3(right3, value2);
    } else {
      add3(left3, value2);
    }
  }
  return [endMutation2(left3), endMutation2(right3)];
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/HashSet.js
var TypeId9 = HashSetTypeId;
var isHashSet2 = isHashSet;
var empty10 = empty9;
var fromIterable6 = fromIterable5;
var make12 = make11;
var has4 = has3;
var some11 = some10;
var every8 = every7;
var isSubset2 = isSubset;
var values4 = values3;
var toValues = /* @__PURE__ */ __name((self) => Array.from(values4(self)), "toValues");
var size6 = size5;
var beginMutation3 = beginMutation2;
var endMutation3 = endMutation2;
var mutate3 = mutate2;
var add4 = add3;
var remove6 = remove5;
var difference5 = difference4;
var intersection5 = intersection4;
var union6 = union5;
var toggle2 = toggle;
var map10 = map9;
var flatMap8 = flatMap7;
var forEach6 = forEach5;
var reduce7 = reduce6;
var filter8 = filter7;
var partition5 = partition4;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/MutableRef.js
var TypeId10 = /* @__PURE__ */ Symbol.for("effect/MutableRef");
var MutableRefProto = {
  [TypeId10]: TypeId10,
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableRef",
      current: toJSON(this.current)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make13 = /* @__PURE__ */ __name((value2) => {
  const ref = Object.create(MutableRefProto);
  ref.current = value2;
  return ref;
}, "make");
var compareAndSet = /* @__PURE__ */ dual(3, (self, oldValue, newValue) => {
  if (equals(oldValue, self.current)) {
    self.current = newValue;
    return true;
  }
  return false;
});
var decrement2 = /* @__PURE__ */ __name((self) => update(self, (n) => n - 1), "decrement");
var decrementAndGet = /* @__PURE__ */ __name((self) => updateAndGet(self, (n) => n - 1), "decrementAndGet");
var get7 = /* @__PURE__ */ __name((self) => self.current, "get");
var getAndDecrement = /* @__PURE__ */ __name((self) => getAndUpdate(self, (n) => n - 1), "getAndDecrement");
var getAndIncrement = /* @__PURE__ */ __name((self) => getAndUpdate(self, (n) => n + 1), "getAndIncrement");
var getAndSet = /* @__PURE__ */ dual(2, (self, value2) => {
  const ret = self.current;
  self.current = value2;
  return ret;
});
var getAndUpdate = /* @__PURE__ */ dual(2, (self, f) => getAndSet(self, f(get7(self))));
var increment2 = /* @__PURE__ */ __name((self) => update(self, (n) => n + 1), "increment");
var incrementAndGet = /* @__PURE__ */ __name((self) => updateAndGet(self, (n) => n + 1), "incrementAndGet");
var set3 = /* @__PURE__ */ dual(2, (self, value2) => {
  self.current = value2;
  return self;
});
var setAndGet = /* @__PURE__ */ dual(2, (self, value2) => {
  self.current = value2;
  return self.current;
});
var update = /* @__PURE__ */ dual(2, (self, f) => set3(self, f(get7(self))));
var updateAndGet = /* @__PURE__ */ dual(2, (self, f) => setAndGet(self, f(get7(self))));
var toggle3 = /* @__PURE__ */ __name((self) => update(self, (_) => !_), "toggle");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberId.js
var FiberIdSymbolKey = "effect/FiberId";
var FiberIdTypeId = /* @__PURE__ */ Symbol.for(FiberIdSymbolKey);
var OP_NONE = "None";
var OP_RUNTIME = "Runtime";
var OP_COMPOSITE = "Composite";
var emptyHash = /* @__PURE__ */ string2(`${FiberIdSymbolKey}-${OP_NONE}`);
var None = class {
  static {
    __name(this, "None");
  }
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_NONE;
  id = -1;
  startTimeMillis = -1;
  [symbol2]() {
    return emptyHash;
  }
  [symbol3](that) {
    return isFiberId(that) && that._tag === OP_NONE;
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var Runtime = class {
  static {
    __name(this, "Runtime");
  }
  id;
  startTimeMillis;
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_RUNTIME;
  constructor(id3, startTimeMillis) {
    this.id = id3;
    this.startTimeMillis = startTimeMillis;
  }
  [symbol2]() {
    return cached(this, string2(`${FiberIdSymbolKey}-${this._tag}-${this.id}-${this.startTimeMillis}`));
  }
  [symbol3](that) {
    return isFiberId(that) && that._tag === OP_RUNTIME && this.id === that.id && this.startTimeMillis === that.startTimeMillis;
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag,
      id: this.id,
      startTimeMillis: this.startTimeMillis
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var Composite = class {
  static {
    __name(this, "Composite");
  }
  left;
  right;
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_COMPOSITE;
  constructor(left3, right3) {
    this.left = left3;
    this.right = right3;
  }
  _hash;
  [symbol2]() {
    return pipe(string2(`${FiberIdSymbolKey}-${this._tag}`), combine2(hash(this.left)), combine2(hash(this.right)), cached(this));
  }
  [symbol3](that) {
    return isFiberId(that) && that._tag === OP_COMPOSITE && equals(this.left, that.left) && equals(this.right, that.right);
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag,
      left: toJSON(this.left),
      right: toJSON(this.right)
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var none3 = /* @__PURE__ */ new None();
var runtime = /* @__PURE__ */ __name((id3, startTimeMillis) => {
  return new Runtime(id3, startTimeMillis);
}, "runtime");
var composite = /* @__PURE__ */ __name((left3, right3) => {
  return new Composite(left3, right3);
}, "composite");
var isFiberId = /* @__PURE__ */ __name((self) => hasProperty(self, FiberIdTypeId), "isFiberId");
var isNone3 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_NONE || pipe(toSet(self), every8((id3) => isNone3(id3)));
}, "isNone");
var isRuntime = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_RUNTIME;
}, "isRuntime");
var isComposite = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_COMPOSITE;
}, "isComposite");
var combine4 = /* @__PURE__ */ dual(2, (self, that) => {
  if (self._tag === OP_NONE) {
    return that;
  }
  if (that._tag === OP_NONE) {
    return self;
  }
  return new Composite(self, that);
});
var combineAll3 = /* @__PURE__ */ __name((fiberIds) => {
  return pipe(fiberIds, reduce7(none3, (a, b) => combine4(b)(a)));
}, "combineAll");
var getOrElse5 = /* @__PURE__ */ dual(2, (self, that) => isNone3(self) ? that : self);
var ids = /* @__PURE__ */ __name((self) => {
  switch (self._tag) {
    case OP_NONE: {
      return empty10();
    }
    case OP_RUNTIME: {
      return make12(self.id);
    }
    case OP_COMPOSITE: {
      return pipe(ids(self.left), union6(ids(self.right)));
    }
  }
}, "ids");
var _fiberCounter = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Fiber/Id/_fiberCounter"), () => make13(0));
var make14 = /* @__PURE__ */ __name((id3, startTimeSeconds) => {
  return new Runtime(id3, startTimeSeconds);
}, "make");
var threadName = /* @__PURE__ */ __name((self) => {
  const identifiers = Array.from(ids(self)).map((n) => `#${n}`).join(",");
  return identifiers;
}, "threadName");
var toOption = /* @__PURE__ */ __name((self) => {
  const fiberIds = toSet(self);
  if (size6(fiberIds) === 0) {
    return none2();
  }
  let first4 = true;
  let acc;
  for (const fiberId4 of fiberIds) {
    if (first4) {
      acc = fiberId4;
      first4 = false;
    } else {
      acc = pipe(acc, combine4(fiberId4));
    }
  }
  return some3(acc);
}, "toOption");
var toSet = /* @__PURE__ */ __name((self) => {
  switch (self._tag) {
    case OP_NONE: {
      return empty10();
    }
    case OP_RUNTIME: {
      return make12(self);
    }
    case OP_COMPOSITE: {
      return pipe(toSet(self.left), union6(toSet(self.right)));
    }
  }
}, "toSet");
var unsafeMake2 = /* @__PURE__ */ __name(() => {
  const id3 = get7(_fiberCounter);
  pipe(_fiberCounter, set3(id3 + 1));
  return new Runtime(id3, Date.now());
}, "unsafeMake");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/FiberId.js
var FiberIdTypeId2 = FiberIdTypeId;
var none4 = none3;
var runtime2 = runtime;
var composite2 = composite;
var isFiberId2 = isFiberId;
var isNone4 = isNone3;
var isRuntime2 = isRuntime;
var isComposite2 = isComposite;
var combine5 = combine4;
var combineAll4 = combineAll3;
var getOrElse6 = getOrElse5;
var ids2 = ids;
var make15 = make14;
var threadName2 = threadName;
var toOption2 = toOption;
var toSet2 = toSet;
var unsafeMake3 = unsafeMake2;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/HashMap.js
var HashMap_exports = {};
__export(HashMap_exports, {
  beginMutation: () => beginMutation4,
  compact: () => compact3,
  countBy: () => countBy4,
  empty: () => empty11,
  endMutation: () => endMutation4,
  entries: () => entries2,
  every: () => every9,
  filter: () => filter9,
  filterMap: () => filterMap7,
  findFirst: () => findFirst6,
  flatMap: () => flatMap9,
  forEach: () => forEach7,
  fromIterable: () => fromIterable7,
  get: () => get8,
  getHash: () => getHash2,
  has: () => has5,
  hasBy: () => hasBy2,
  hasHash: () => hasHash2,
  isEmpty: () => isEmpty4,
  isHashMap: () => isHashMap2,
  keySet: () => keySet2,
  keys: () => keys3,
  make: () => make16,
  map: () => map11,
  modify: () => modify5,
  modifyAt: () => modifyAt2,
  modifyHash: () => modifyHash2,
  mutate: () => mutate4,
  reduce: () => reduce8,
  remove: () => remove7,
  removeMany: () => removeMany2,
  set: () => set4,
  size: () => size7,
  some: () => some12,
  toEntries: () => toEntries2,
  toValues: () => toValues2,
  union: () => union7,
  unsafeGet: () => unsafeGet6,
  values: () => values5
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/hashMap/keySet.js
function keySet(self) {
  return makeImpl2(self);
}
__name(keySet, "keySet");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/HashMap.js
var TypeId11 = HashMapTypeId;
var isHashMap2 = isHashMap;
var empty11 = empty8;
var make16 = make10;
var fromIterable7 = fromIterable4;
var isEmpty4 = isEmpty3;
var get8 = get6;
var getHash2 = getHash;
var unsafeGet6 = unsafeGet5;
var has5 = has2;
var hasHash2 = hasHash;
var hasBy2 = hasBy;
var set4 = set2;
var keys3 = keys2;
var keySet2 = keySet;
var values5 = values2;
var toValues2 = /* @__PURE__ */ __name((self) => Array.from(values5(self)), "toValues");
var entries2 = entries;
var toEntries2 = /* @__PURE__ */ __name((self) => Array.from(entries2(self)), "toEntries");
var size7 = size4;
var countBy4 = countBy3;
var beginMutation4 = beginMutation;
var endMutation4 = endMutation;
var mutate4 = mutate;
var modifyAt2 = modifyAt;
var modifyHash2 = modifyHash;
var modify5 = modify4;
var union7 = union4;
var remove7 = remove4;
var removeMany2 = removeMany;
var map11 = map8;
var flatMap9 = flatMap6;
var forEach7 = forEach4;
var reduce8 = reduce5;
var filter9 = filter6;
var compact3 = compact2;
var filterMap7 = filterMap6;
var findFirst6 = findFirst5;
var some12 = some9;
var every9 = every6;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/List.js
var TypeId12 = /* @__PURE__ */ Symbol.for("effect/List");
var toArray3 = /* @__PURE__ */ __name((self) => fromIterable2(self), "toArray");
var getEquivalence7 = /* @__PURE__ */ __name((isEquivalent) => mapInput(getEquivalence5(isEquivalent), toArray3), "getEquivalence");
var _equivalence5 = /* @__PURE__ */ getEquivalence7(equals);
var ConsProto = {
  [TypeId12]: TypeId12,
  _tag: "Cons",
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "List",
      _tag: "Cons",
      values: toArray3(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol3](that) {
    return isList(that) && this._tag === that._tag && _equivalence5(this, that);
  },
  [symbol2]() {
    return cached(this, array2(toArray3(this)));
  },
  [Symbol.iterator]() {
    let done12 = false;
    let self = this;
    return {
      next() {
        if (done12) {
          return this.return();
        }
        if (self._tag === "Nil") {
          done12 = true;
          return this.return();
        }
        const value2 = self.head;
        self = self.tail;
        return {
          done: done12,
          value: value2
        };
      },
      return(value2) {
        if (!done12) {
          done12 = true;
        }
        return {
          done: true,
          value: value2
        };
      }
    };
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeCons = /* @__PURE__ */ __name((head10, tail5) => {
  const cons2 = Object.create(ConsProto);
  cons2.head = head10;
  cons2.tail = tail5;
  return cons2;
}, "makeCons");
var NilHash = /* @__PURE__ */ string2("Nil");
var NilProto = {
  [TypeId12]: TypeId12,
  _tag: "Nil",
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "List",
      _tag: "Nil"
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol2]() {
    return NilHash;
  },
  [symbol3](that) {
    return isList(that) && this._tag === that._tag;
  },
  [Symbol.iterator]() {
    return {
      next() {
        return {
          done: true,
          value: void 0
        };
      }
    };
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var _Nil = /* @__PURE__ */ Object.create(NilProto);
var isList = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId12), "isList");
var isNil = /* @__PURE__ */ __name((self) => self._tag === "Nil", "isNil");
var isCons = /* @__PURE__ */ __name((self) => self._tag === "Cons", "isCons");
var size8 = /* @__PURE__ */ __name((self) => {
  let these = self;
  let len = 0;
  while (!isNil(these)) {
    len += 1;
    these = these.tail;
  }
  return len;
}, "size");
var nil = /* @__PURE__ */ __name(() => _Nil, "nil");
var cons = /* @__PURE__ */ __name((head10, tail5) => makeCons(head10, tail5), "cons");
var empty12 = nil;
var of4 = /* @__PURE__ */ __name((value2) => makeCons(value2, _Nil), "of");
var fromIterable8 = /* @__PURE__ */ __name((prefix) => {
  const iterator = prefix[Symbol.iterator]();
  let next4;
  if ((next4 = iterator.next()) && !next4.done) {
    const result = makeCons(next4.value, _Nil);
    let curr = result;
    while ((next4 = iterator.next()) && !next4.done) {
      const temp = makeCons(next4.value, _Nil);
      curr.tail = temp;
      curr = temp;
    }
    return result;
  } else {
    return _Nil;
  }
}, "fromIterable");
var make17 = /* @__PURE__ */ __name((...elements) => fromIterable8(elements), "make");
var append4 = /* @__PURE__ */ dual(2, (self, element) => appendAll4(self, of4(element)));
var appendAll4 = /* @__PURE__ */ dual(2, (self, that) => prependAll4(that, self));
var prepend4 = /* @__PURE__ */ dual(2, (self, element) => cons(element, self));
var prependAll4 = /* @__PURE__ */ dual(2, (self, prefix) => {
  if (isNil(self)) {
    return prefix;
  } else if (isNil(prefix)) {
    return self;
  } else {
    const result = makeCons(prefix.head, self);
    let curr = result;
    let that = prefix.tail;
    while (!isNil(that)) {
      const temp = makeCons(that.head, self);
      curr.tail = temp;
      curr = temp;
      that = that.tail;
    }
    return result;
  }
});
var prependAllReversed = /* @__PURE__ */ dual(2, (self, prefix) => {
  let out = self;
  let pres = prefix;
  while (isCons(pres)) {
    out = makeCons(pres.head, out);
    pres = pres.tail;
  }
  return out;
});
var drop4 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return self;
  }
  if (n >= size8(self)) {
    return _Nil;
  }
  let these = self;
  let i = 0;
  while (!isNil(these) && i < n) {
    these = these.tail;
    i += 1;
  }
  return these;
});
var every10 = /* @__PURE__ */ dual(2, (self, refinement) => {
  for (const a of self) {
    if (!refinement(a)) {
      return false;
    }
  }
  return true;
});
var some13 = /* @__PURE__ */ dual(2, (self, predicate) => {
  let these = self;
  while (!isNil(these)) {
    if (predicate(these.head)) {
      return true;
    }
    these = these.tail;
  }
  return false;
});
var filter10 = /* @__PURE__ */ dual(2, (self, predicate) => noneIn(self, predicate, false));
var noneIn = /* @__PURE__ */ __name((self, predicate, isFlipped) => {
  while (true) {
    if (isNil(self)) {
      return _Nil;
    } else {
      if (predicate(self.head) !== isFlipped) {
        return allIn(self, self.tail, predicate, isFlipped);
      } else {
        self = self.tail;
      }
    }
  }
}, "noneIn");
var allIn = /* @__PURE__ */ __name((start3, remaining, predicate, isFlipped) => {
  while (true) {
    if (isNil(remaining)) {
      return start3;
    } else {
      if (predicate(remaining.head) !== isFlipped) {
        remaining = remaining.tail;
      } else {
        return partialFill(start3, remaining, predicate, isFlipped);
      }
    }
  }
}, "allIn");
var partialFill = /* @__PURE__ */ __name((origStart, firstMiss, predicate, isFlipped) => {
  const newHead = makeCons(unsafeHead3(origStart), _Nil);
  let toProcess = unsafeTail(origStart);
  let currentLast = newHead;
  while (!(toProcess === firstMiss)) {
    const newElem = makeCons(unsafeHead3(toProcess), _Nil);
    currentLast.tail = newElem;
    currentLast = unsafeCoerce(newElem);
    toProcess = unsafeCoerce(toProcess.tail);
  }
  let next4 = firstMiss.tail;
  let nextToCopy = unsafeCoerce(next4);
  while (!isNil(next4)) {
    const head10 = unsafeHead3(next4);
    if (predicate(head10) !== isFlipped) {
      next4 = next4.tail;
    } else {
      while (!(nextToCopy === next4)) {
        const newElem = makeCons(unsafeHead3(nextToCopy), _Nil);
        currentLast.tail = newElem;
        currentLast = newElem;
        nextToCopy = unsafeCoerce(nextToCopy.tail);
      }
      nextToCopy = unsafeCoerce(next4.tail);
      next4 = next4.tail;
    }
  }
  if (!isNil(nextToCopy)) {
    currentLast.tail = nextToCopy;
  }
  return newHead;
}, "partialFill");
var filterMap8 = /* @__PURE__ */ dual(2, (self, f) => {
  const bs = [];
  for (const a of self) {
    const oa = f(a);
    if (isSome2(oa)) {
      bs.push(oa.value);
    }
  }
  return fromIterable8(bs);
});
var compact4 = /* @__PURE__ */ __name((self) => filterMap8(self, identity), "compact");
var findFirst7 = /* @__PURE__ */ dual(2, (self, predicate) => {
  let these = self;
  while (!isNil(these)) {
    if (predicate(these.head)) {
      return some3(these.head);
    }
    these = these.tail;
  }
  return none2();
});
var flatMap10 = /* @__PURE__ */ dual(2, (self, f) => {
  let rest = self;
  let head10 = void 0;
  let tail5 = void 0;
  while (!isNil(rest)) {
    let bs = f(rest.head);
    while (!isNil(bs)) {
      const next4 = makeCons(bs.head, _Nil);
      if (tail5 === void 0) {
        head10 = next4;
      } else {
        tail5.tail = next4;
      }
      tail5 = next4;
      bs = bs.tail;
    }
    rest = rest.tail;
  }
  if (head10 === void 0) {
    return _Nil;
  }
  return head10;
});
var forEach8 = /* @__PURE__ */ dual(2, (self, f) => {
  let these = self;
  while (!isNil(these)) {
    f(these.head);
    these = these.tail;
  }
});
var head4 = /* @__PURE__ */ __name((self) => isNil(self) ? none2() : some3(self.head), "head");
var last3 = /* @__PURE__ */ __name((self) => isNil(self) ? none2() : some3(unsafeLast2(self)), "last");
var map12 = /* @__PURE__ */ dual(2, (self, f) => {
  if (isNil(self)) {
    return self;
  } else {
    let i = 0;
    const head10 = makeCons(f(self.head, i++), _Nil);
    let nextHead = head10;
    let rest = self.tail;
    while (!isNil(rest)) {
      const next4 = makeCons(f(rest.head, i++), _Nil);
      nextHead.tail = next4;
      nextHead = next4;
      rest = rest.tail;
    }
    return head10;
  }
});
var partition6 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const left3 = [];
  const right3 = [];
  for (const a of self) {
    if (predicate(a)) {
      right3.push(a);
    } else {
      left3.push(a);
    }
  }
  return [fromIterable8(left3), fromIterable8(right3)];
});
var partitionMap5 = /* @__PURE__ */ dual(2, (self, f) => {
  const left3 = [];
  const right3 = [];
  for (const a of self) {
    const e = f(a);
    if (isLeft2(e)) {
      left3.push(e.left);
    } else {
      right3.push(e.right);
    }
  }
  return [fromIterable8(left3), fromIterable8(right3)];
});
var reduce9 = /* @__PURE__ */ dual(3, (self, zero2, f) => {
  let acc = zero2;
  let these = self;
  while (!isNil(these)) {
    acc = f(acc, these.head);
    these = these.tail;
  }
  return acc;
});
var reduceRight3 = /* @__PURE__ */ dual(3, (self, zero2, f) => {
  let acc = zero2;
  let these = reverse4(self);
  while (!isNil(these)) {
    acc = f(acc, these.head);
    these = these.tail;
  }
  return acc;
});
var reverse4 = /* @__PURE__ */ __name((self) => {
  let result = empty12();
  let these = self;
  while (!isNil(these)) {
    result = prepend4(result, these.head);
    these = these.tail;
  }
  return result;
}, "reverse");
var splitAt3 = /* @__PURE__ */ dual(2, (self, n) => [take4(self, n), drop4(self, n)]);
var tail3 = /* @__PURE__ */ __name((self) => isNil(self) ? none2() : some3(self.tail), "tail");
var take4 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return _Nil;
  }
  if (n >= size8(self)) {
    return self;
  }
  let these = make17(unsafeHead3(self));
  let current2 = unsafeTail(self);
  for (let i = 1; i < n; i++) {
    these = makeCons(unsafeHead3(current2), these);
    current2 = unsafeTail(current2);
  }
  return reverse4(these);
});
var toChunk = /* @__PURE__ */ __name((self) => fromIterable3(self), "toChunk");
var getExpectedListToBeNonEmptyErrorMessage = "Expected List to be non-empty";
var unsafeHead3 = /* @__PURE__ */ __name((self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  return self.head;
}, "unsafeHead");
var unsafeLast2 = /* @__PURE__ */ __name((self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  let these = self;
  let scout = self.tail;
  while (!isNil(scout)) {
    these = scout;
    scout = scout.tail;
  }
  return these.head;
}, "unsafeLast");
var unsafeTail = /* @__PURE__ */ __name((self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  return self.tail;
}, "unsafeTail");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/data.js
var ArrayProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(Array.prototype), {
  [symbol2]() {
    return cached(this, array2(this));
  },
  [symbol3](that) {
    if (Array.isArray(that) && this.length === that.length) {
      return this.every((v, i) => equals(v, that[i]));
    } else {
      return false;
    }
  }
});
var Structural = /* @__PURE__ */ (function() {
  function Structural3(args2) {
    if (args2) {
      Object.assign(this, args2);
    }
  }
  __name(Structural3, "Structural");
  Structural3.prototype = StructuralPrototype;
  return Structural3;
})();
var struct4 = /* @__PURE__ */ __name((as15) => Object.assign(Object.create(StructuralPrototype), as15), "struct");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/chunkPatch.js
var ChunkPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferChunkPatch");
function variance(a) {
  return a;
}
__name(variance, "variance");
var PatchProto = {
  ...Structural.prototype,
  [ChunkPatchTypeId]: {
    _Value: variance,
    _Patch: variance
  }
};
var EmptyProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto), {
  _tag: "Empty"
});
var _empty5 = /* @__PURE__ */ Object.create(EmptyProto);
var empty13 = /* @__PURE__ */ __name(() => _empty5, "empty");
var AndThenProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto), {
  _tag: "AndThen"
});
var makeAndThen = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var AppendProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto), {
  _tag: "Append"
});
var makeAppend = /* @__PURE__ */ __name((values10) => {
  const o = Object.create(AppendProto);
  o.values = values10;
  return o;
}, "makeAppend");
var SliceProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto), {
  _tag: "Slice"
});
var makeSlice = /* @__PURE__ */ __name((from, until) => {
  const o = Object.create(SliceProto);
  o.from = from;
  o.until = until;
  return o;
}, "makeSlice");
var UpdateProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto), {
  _tag: "Update"
});
var makeUpdate = /* @__PURE__ */ __name((index, patch13) => {
  const o = Object.create(UpdateProto);
  o.index = index;
  o.patch = patch13;
  return o;
}, "makeUpdate");
var diff = /* @__PURE__ */ __name((options) => {
  let i = 0;
  let patch13 = empty13();
  while (i < options.oldValue.length && i < options.newValue.length) {
    const oldElement = unsafeGet4(i)(options.oldValue);
    const newElement = unsafeGet4(i)(options.newValue);
    const valuePatch = options.differ.diff(oldElement, newElement);
    if (!equals(valuePatch, options.differ.empty)) {
      patch13 = pipe(patch13, combine6(makeUpdate(i, valuePatch)));
    }
    i = i + 1;
  }
  if (i < options.oldValue.length) {
    patch13 = pipe(patch13, combine6(makeSlice(0, i)));
  }
  if (i < options.newValue.length) {
    patch13 = pipe(patch13, combine6(makeAppend(drop3(i)(options.newValue))));
  }
  return patch13;
}, "diff");
var combine6 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen(self, that));
var patch = /* @__PURE__ */ dual(3, (self, oldValue, differ3) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let chunk4 = oldValue;
  let patches = of3(self);
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    const tail5 = tailNonEmpty2(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AndThen": {
        patches = prepend3(head10.first)(prepend3(head10.second)(tail5));
        break;
      }
      case "Append": {
        chunk4 = appendAll3(head10.values)(chunk4);
        patches = tail5;
        break;
      }
      case "Slice": {
        const array5 = toReadonlyArray(chunk4);
        chunk4 = unsafeFromArray(array5.slice(head10.from, head10.until));
        patches = tail5;
        break;
      }
      case "Update": {
        const array5 = toReadonlyArray(chunk4);
        array5[head10.index] = differ3.patch(head10.patch, array5[head10.index]);
        chunk4 = unsafeFromArray(array5);
        patches = tail5;
        break;
      }
    }
  }
  return chunk4;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/contextPatch.js
var ContextPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferContextPatch");
function variance2(a) {
  return a;
}
__name(variance2, "variance");
var PatchProto2 = {
  ...Structural.prototype,
  [ContextPatchTypeId]: {
    _Value: variance2,
    _Patch: variance2
  }
};
var EmptyProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto2), {
  _tag: "Empty"
});
var _empty6 = /* @__PURE__ */ Object.create(EmptyProto2);
var empty14 = /* @__PURE__ */ __name(() => _empty6, "empty");
var AndThenProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto2), {
  _tag: "AndThen"
});
var makeAndThen2 = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto2);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var AddServiceProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto2), {
  _tag: "AddService"
});
var makeAddService = /* @__PURE__ */ __name((key, service6) => {
  const o = Object.create(AddServiceProto);
  o.key = key;
  o.service = service6;
  return o;
}, "makeAddService");
var RemoveServiceProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto2), {
  _tag: "RemoveService"
});
var makeRemoveService = /* @__PURE__ */ __name((key) => {
  const o = Object.create(RemoveServiceProto);
  o.key = key;
  return o;
}, "makeRemoveService");
var UpdateServiceProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto2), {
  _tag: "UpdateService"
});
var makeUpdateService = /* @__PURE__ */ __name((key, update11) => {
  const o = Object.create(UpdateServiceProto);
  o.key = key;
  o.update = update11;
  return o;
}, "makeUpdateService");
var diff2 = /* @__PURE__ */ __name((oldValue, newValue) => {
  const missingServices = new Map(oldValue.unsafeMap);
  let patch13 = empty14();
  for (const [tag, newService] of newValue.unsafeMap.entries()) {
    if (missingServices.has(tag)) {
      const old = missingServices.get(tag);
      missingServices.delete(tag);
      if (!equals(old, newService)) {
        patch13 = combine7(makeUpdateService(tag, () => newService))(patch13);
      }
    } else {
      missingServices.delete(tag);
      patch13 = combine7(makeAddService(tag, newService))(patch13);
    }
  }
  for (const [tag] of missingServices.entries()) {
    patch13 = combine7(makeRemoveService(tag))(patch13);
  }
  return patch13;
}, "diff");
var combine7 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen2(self, that));
var patch2 = /* @__PURE__ */ dual(2, (self, context11) => {
  if (self._tag === "Empty") {
    return context11;
  }
  let wasServiceUpdated = false;
  let patches = of3(self);
  const updatedContext = new Map(context11.unsafeMap);
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    const tail5 = tailNonEmpty2(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AddService": {
        updatedContext.set(head10.key, head10.service);
        patches = tail5;
        break;
      }
      case "AndThen": {
        patches = prepend3(prepend3(tail5, head10.second), head10.first);
        break;
      }
      case "RemoveService": {
        updatedContext.delete(head10.key);
        patches = tail5;
        break;
      }
      case "UpdateService": {
        updatedContext.set(head10.key, head10.update(updatedContext.get(head10.key)));
        wasServiceUpdated = true;
        patches = tail5;
        break;
      }
    }
  }
  if (!wasServiceUpdated) {
    return makeContext(updatedContext);
  }
  const map36 = /* @__PURE__ */ new Map();
  for (const [tag] of context11.unsafeMap) {
    if (updatedContext.has(tag)) {
      map36.set(tag, updatedContext.get(tag));
      updatedContext.delete(tag);
    }
  }
  for (const [tag, s] of updatedContext) {
    map36.set(tag, s);
  }
  return makeContext(map36);
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/hashMapPatch.js
var HashMapPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferHashMapPatch");
function variance3(a) {
  return a;
}
__name(variance3, "variance");
var PatchProto3 = {
  ...Structural.prototype,
  [HashMapPatchTypeId]: {
    _Value: variance3,
    _Key: variance3,
    _Patch: variance3
  }
};
var EmptyProto3 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto3), {
  _tag: "Empty"
});
var _empty7 = /* @__PURE__ */ Object.create(EmptyProto3);
var empty15 = /* @__PURE__ */ __name(() => _empty7, "empty");
var AndThenProto3 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto3), {
  _tag: "AndThen"
});
var makeAndThen3 = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto3);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var AddProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto3), {
  _tag: "Add"
});
var makeAdd = /* @__PURE__ */ __name((key, value2) => {
  const o = Object.create(AddProto);
  o.key = key;
  o.value = value2;
  return o;
}, "makeAdd");
var RemoveProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto3), {
  _tag: "Remove"
});
var makeRemove = /* @__PURE__ */ __name((key) => {
  const o = Object.create(RemoveProto);
  o.key = key;
  return o;
}, "makeRemove");
var UpdateProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto3), {
  _tag: "Update"
});
var makeUpdate2 = /* @__PURE__ */ __name((key, patch13) => {
  const o = Object.create(UpdateProto2);
  o.key = key;
  o.patch = patch13;
  return o;
}, "makeUpdate");
var diff3 = /* @__PURE__ */ __name((options) => {
  const [removed, patch13] = reduce8([options.oldValue, empty15()], ([map36, patch14], newValue, key) => {
    const option5 = get8(key)(map36);
    switch (option5._tag) {
      case "Some": {
        const valuePatch = options.differ.diff(option5.value, newValue);
        if (equals(valuePatch, options.differ.empty)) {
          return [remove7(key)(map36), patch14];
        }
        return [remove7(key)(map36), combine8(makeUpdate2(key, valuePatch))(patch14)];
      }
      case "None": {
        return [map36, combine8(makeAdd(key, newValue))(patch14)];
      }
    }
  })(options.newValue);
  return reduce8(patch13, (patch14, _, key) => combine8(makeRemove(key))(patch14))(removed);
}, "diff");
var combine8 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen3(self, that));
var patch3 = /* @__PURE__ */ dual(3, (self, oldValue, differ3) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let map36 = oldValue;
  let patches = of3(self);
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    const tail5 = tailNonEmpty2(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AndThen": {
        patches = prepend3(head10.first)(prepend3(head10.second)(tail5));
        break;
      }
      case "Add": {
        map36 = set4(head10.key, head10.value)(map36);
        patches = tail5;
        break;
      }
      case "Remove": {
        map36 = remove7(head10.key)(map36);
        patches = tail5;
        break;
      }
      case "Update": {
        const option5 = get8(head10.key)(map36);
        if (option5._tag === "Some") {
          map36 = set4(head10.key, differ3.patch(head10.patch, option5.value))(map36);
        }
        patches = tail5;
        break;
      }
    }
  }
  return map36;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/hashSetPatch.js
var HashSetPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferHashSetPatch");
function variance4(a) {
  return a;
}
__name(variance4, "variance");
var PatchProto4 = {
  ...Structural.prototype,
  [HashSetPatchTypeId]: {
    _Value: variance4,
    _Key: variance4,
    _Patch: variance4
  }
};
var EmptyProto4 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto4), {
  _tag: "Empty"
});
var _empty8 = /* @__PURE__ */ Object.create(EmptyProto4);
var empty16 = /* @__PURE__ */ __name(() => _empty8, "empty");
var AndThenProto4 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto4), {
  _tag: "AndThen"
});
var makeAndThen4 = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto4);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var AddProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto4), {
  _tag: "Add"
});
var makeAdd2 = /* @__PURE__ */ __name((value2) => {
  const o = Object.create(AddProto2);
  o.value = value2;
  return o;
}, "makeAdd");
var RemoveProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto4), {
  _tag: "Remove"
});
var makeRemove2 = /* @__PURE__ */ __name((value2) => {
  const o = Object.create(RemoveProto2);
  o.value = value2;
  return o;
}, "makeRemove");
var diff4 = /* @__PURE__ */ __name((oldValue, newValue) => {
  const [removed, patch13] = reduce7([oldValue, empty16()], ([set14, patch14], value2) => {
    if (has4(value2)(set14)) {
      return [remove6(value2)(set14), patch14];
    }
    return [set14, combine9(makeAdd2(value2))(patch14)];
  })(newValue);
  return reduce7(patch13, (patch14, value2) => combine9(makeRemove2(value2))(patch14))(removed);
}, "diff");
var combine9 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen4(self, that));
var patch4 = /* @__PURE__ */ dual(2, (self, oldValue) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let set14 = oldValue;
  let patches = of3(self);
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    const tail5 = tailNonEmpty2(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AndThen": {
        patches = prepend3(head10.first)(prepend3(head10.second)(tail5));
        break;
      }
      case "Add": {
        set14 = add4(head10.value)(set14);
        patches = tail5;
        break;
      }
      case "Remove": {
        set14 = remove6(head10.value)(set14);
        patches = tail5;
      }
    }
  }
  return set14;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/orPatch.js
var OrPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferOrPatch");
function variance5(a) {
  return a;
}
__name(variance5, "variance");
var PatchProto5 = {
  ...Structural.prototype,
  [OrPatchTypeId]: {
    _Value: variance5,
    _Key: variance5,
    _Patch: variance5
  }
};
var EmptyProto5 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "Empty"
});
var _empty9 = /* @__PURE__ */ Object.create(EmptyProto5);
var empty17 = /* @__PURE__ */ __name(() => _empty9, "empty");
var AndThenProto5 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "AndThen"
});
var makeAndThen5 = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto5);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var SetLeftProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "SetLeft"
});
var makeSetLeft = /* @__PURE__ */ __name((value2) => {
  const o = Object.create(SetLeftProto);
  o.value = value2;
  return o;
}, "makeSetLeft");
var SetRightProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "SetRight"
});
var makeSetRight = /* @__PURE__ */ __name((value2) => {
  const o = Object.create(SetRightProto);
  o.value = value2;
  return o;
}, "makeSetRight");
var UpdateLeftProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "UpdateLeft"
});
var makeUpdateLeft = /* @__PURE__ */ __name((patch13) => {
  const o = Object.create(UpdateLeftProto);
  o.patch = patch13;
  return o;
}, "makeUpdateLeft");
var UpdateRightProto = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto5), {
  _tag: "UpdateRight"
});
var makeUpdateRight = /* @__PURE__ */ __name((patch13) => {
  const o = Object.create(UpdateRightProto);
  o.patch = patch13;
  return o;
}, "makeUpdateRight");
var diff5 = /* @__PURE__ */ __name((options) => {
  switch (options.oldValue._tag) {
    case "Left": {
      switch (options.newValue._tag) {
        case "Left": {
          const valuePatch = options.left.diff(options.oldValue.left, options.newValue.left);
          if (equals(valuePatch, options.left.empty)) {
            return empty17();
          }
          return makeUpdateLeft(valuePatch);
        }
        case "Right": {
          return makeSetRight(options.newValue.right);
        }
      }
    }
    case "Right": {
      switch (options.newValue._tag) {
        case "Left": {
          return makeSetLeft(options.newValue.left);
        }
        case "Right": {
          const valuePatch = options.right.diff(options.oldValue.right, options.newValue.right);
          if (equals(valuePatch, options.right.empty)) {
            return empty17();
          }
          return makeUpdateRight(valuePatch);
        }
      }
    }
  }
}, "diff");
var combine10 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen5(self, that));
var patch5 = /* @__PURE__ */ dual(2, (self, {
  left: left3,
  oldValue,
  right: right3
}) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let patches = of3(self);
  let result = oldValue;
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    const tail5 = tailNonEmpty2(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AndThen": {
        patches = prepend3(head10.first)(prepend3(head10.second)(tail5));
        break;
      }
      case "UpdateLeft": {
        if (result._tag === "Left") {
          result = left2(left3.patch(head10.patch, result.left));
        }
        patches = tail5;
        break;
      }
      case "UpdateRight": {
        if (result._tag === "Right") {
          result = right2(right3.patch(head10.patch, result.right));
        }
        patches = tail5;
        break;
      }
      case "SetLeft": {
        result = left2(head10.value);
        patches = tail5;
        break;
      }
      case "SetRight": {
        result = right2(head10.value);
        patches = tail5;
        break;
      }
    }
  }
  return result;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ/readonlyArrayPatch.js
var ReadonlyArrayPatchTypeId = /* @__PURE__ */ Symbol.for("effect/DifferReadonlyArrayPatch");
function variance6(a) {
  return a;
}
__name(variance6, "variance");
var PatchProto6 = {
  ...Structural.prototype,
  [ReadonlyArrayPatchTypeId]: {
    _Value: variance6,
    _Patch: variance6
  }
};
var EmptyProto6 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto6), {
  _tag: "Empty"
});
var _empty10 = /* @__PURE__ */ Object.create(EmptyProto6);
var empty18 = /* @__PURE__ */ __name(() => _empty10, "empty");
var AndThenProto6 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto6), {
  _tag: "AndThen"
});
var makeAndThen6 = /* @__PURE__ */ __name((first4, second) => {
  const o = Object.create(AndThenProto6);
  o.first = first4;
  o.second = second;
  return o;
}, "makeAndThen");
var AppendProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto6), {
  _tag: "Append"
});
var makeAppend2 = /* @__PURE__ */ __name((values10) => {
  const o = Object.create(AppendProto2);
  o.values = values10;
  return o;
}, "makeAppend");
var SliceProto2 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto6), {
  _tag: "Slice"
});
var makeSlice2 = /* @__PURE__ */ __name((from, until) => {
  const o = Object.create(SliceProto2);
  o.from = from;
  o.until = until;
  return o;
}, "makeSlice");
var UpdateProto3 = /* @__PURE__ */ Object.assign(/* @__PURE__ */ Object.create(PatchProto6), {
  _tag: "Update"
});
var makeUpdate3 = /* @__PURE__ */ __name((index, patch13) => {
  const o = Object.create(UpdateProto3);
  o.index = index;
  o.patch = patch13;
  return o;
}, "makeUpdate");
var diff6 = /* @__PURE__ */ __name((options) => {
  let i = 0;
  let patch13 = empty18();
  while (i < options.oldValue.length && i < options.newValue.length) {
    const oldElement = options.oldValue[i];
    const newElement = options.newValue[i];
    const valuePatch = options.differ.diff(oldElement, newElement);
    if (!equals(valuePatch, options.differ.empty)) {
      patch13 = combine11(patch13, makeUpdate3(i, valuePatch));
    }
    i = i + 1;
  }
  if (i < options.oldValue.length) {
    patch13 = combine11(patch13, makeSlice2(0, i));
  }
  if (i < options.newValue.length) {
    patch13 = combine11(patch13, makeAppend2(drop2(i)(options.newValue)));
  }
  return patch13;
}, "diff");
var combine11 = /* @__PURE__ */ dual(2, (self, that) => makeAndThen6(self, that));
var patch6 = /* @__PURE__ */ dual(3, (self, oldValue, differ3) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let readonlyArray3 = oldValue.slice();
  let patches = of2(self);
  while (isNonEmptyArray2(patches)) {
    const head10 = headNonEmpty(patches);
    const tail5 = tailNonEmpty(patches);
    switch (head10._tag) {
      case "Empty": {
        patches = tail5;
        break;
      }
      case "AndThen": {
        tail5.unshift(head10.first, head10.second);
        patches = tail5;
        break;
      }
      case "Append": {
        for (const value2 of head10.values) {
          readonlyArray3.push(value2);
        }
        patches = tail5;
        break;
      }
      case "Slice": {
        readonlyArray3 = readonlyArray3.slice(head10.from, head10.until);
        patches = tail5;
        break;
      }
      case "Update": {
        readonlyArray3[head10.index] = differ3.patch(head10.patch, readonlyArray3[head10.index]);
        patches = tail5;
        break;
      }
    }
  }
  return readonlyArray3;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/differ.js
var DifferTypeId = /* @__PURE__ */ Symbol.for("effect/Differ");
var DifferProto = {
  [DifferTypeId]: {
    _P: identity,
    _V: identity
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make18 = /* @__PURE__ */ __name((params) => {
  const differ3 = Object.create(DifferProto);
  differ3.empty = params.empty;
  differ3.diff = params.diff;
  differ3.combine = params.combine;
  differ3.patch = params.patch;
  return differ3;
}, "make");
var environment = /* @__PURE__ */ __name(() => make18({
  empty: empty14(),
  combine: /* @__PURE__ */ __name((first4, second) => combine7(second)(first4), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff2(oldValue, newValue), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch2(oldValue)(patch13), "patch")
}), "environment");
var chunk = /* @__PURE__ */ __name((differ3) => make18({
  empty: empty13(),
  combine: /* @__PURE__ */ __name((first4, second) => combine6(second)(first4), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff({
    oldValue,
    newValue,
    differ: differ3
  }), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch(oldValue, differ3)(patch13), "patch")
}), "chunk");
var hashMap = /* @__PURE__ */ __name((differ3) => make18({
  empty: empty15(),
  combine: /* @__PURE__ */ __name((first4, second) => combine8(second)(first4), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff3({
    oldValue,
    newValue,
    differ: differ3
  }), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch3(oldValue, differ3)(patch13), "patch")
}), "hashMap");
var hashSet = /* @__PURE__ */ __name(() => make18({
  empty: empty16(),
  combine: /* @__PURE__ */ __name((first4, second) => combine9(second)(first4), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff4(oldValue, newValue), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch4(oldValue)(patch13), "patch")
}), "hashSet");
var orElseEither2 = /* @__PURE__ */ dual(2, (self, that) => make18({
  empty: empty17(),
  combine: /* @__PURE__ */ __name((first4, second) => combine10(first4, second), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff5({
    oldValue,
    newValue,
    left: self,
    right: that
  }), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch5(patch13, {
    oldValue,
    left: self,
    right: that
  }), "patch")
}));
var readonlyArray = /* @__PURE__ */ __name((differ3) => make18({
  empty: empty18(),
  combine: /* @__PURE__ */ __name((first4, second) => combine11(first4, second), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff6({
    oldValue,
    newValue,
    differ: differ3
  }), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => patch6(patch13, oldValue, differ3), "patch")
}), "readonlyArray");
var transform = /* @__PURE__ */ dual(2, (self, {
  toNew,
  toOld
}) => make18({
  empty: self.empty,
  combine: /* @__PURE__ */ __name((first4, second) => self.combine(first4, second), "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => self.diff(toOld(oldValue), toOld(newValue)), "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => toNew(self.patch(patch13, toOld(oldValue))), "patch")
}));
var update2 = /* @__PURE__ */ __name(() => updateWith((_, a) => a), "update");
var updateWith = /* @__PURE__ */ __name((f) => make18({
  empty: identity,
  combine: /* @__PURE__ */ __name((first4, second) => {
    if (first4 === identity) {
      return second;
    }
    if (second === identity) {
      return first4;
    }
    return (a) => second(first4(a));
  }, "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => {
    if (equals(oldValue, newValue)) {
      return identity;
    }
    return constant(newValue);
  }, "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => f(oldValue, patch13(oldValue)), "patch")
}), "updateWith");
var zip4 = /* @__PURE__ */ dual(2, (self, that) => make18({
  empty: [self.empty, that.empty],
  combine: /* @__PURE__ */ __name((first4, second) => [self.combine(first4[0], second[0]), that.combine(first4[1], second[1])], "combine"),
  diff: /* @__PURE__ */ __name((oldValue, newValue) => [self.diff(oldValue[0], newValue[0]), that.diff(oldValue[1], newValue[1])], "diff"),
  patch: /* @__PURE__ */ __name((patch13, oldValue) => [self.patch(patch13[0], oldValue[0]), that.patch(patch13[1], oldValue[1])], "patch")
}));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/runtimeFlagsPatch.js
var BIT_MASK = 255;
var BIT_SHIFT = 8;
var active = /* @__PURE__ */ __name((patch13) => patch13 & BIT_MASK, "active");
var enabled = /* @__PURE__ */ __name((patch13) => patch13 >> BIT_SHIFT & BIT_MASK, "enabled");
var make19 = /* @__PURE__ */ __name((active2, enabled2) => (active2 & BIT_MASK) + ((enabled2 & active2 & BIT_MASK) << BIT_SHIFT), "make");
var empty19 = /* @__PURE__ */ make19(0, 0);
var enable = /* @__PURE__ */ __name((flag) => make19(flag, flag), "enable");
var disable = /* @__PURE__ */ __name((flag) => make19(flag, 0), "disable");
var isEmpty5 = /* @__PURE__ */ __name((patch13) => patch13 === 0, "isEmpty");
var isActive = /* @__PURE__ */ dual(2, (self, flag) => (active(self) & flag) !== 0);
var isEnabled = /* @__PURE__ */ dual(2, (self, flag) => (enabled(self) & flag) !== 0);
var isDisabled = /* @__PURE__ */ dual(2, (self, flag) => (active(self) & flag) !== 0 && (enabled(self) & flag) === 0);
var exclude = /* @__PURE__ */ dual(2, (self, flag) => make19(active(self) & ~flag, enabled(self)));
var both = /* @__PURE__ */ dual(2, (self, that) => make19(active(self) | active(that), enabled(self) & enabled(that)));
var either = /* @__PURE__ */ dual(2, (self, that) => make19(active(self) | active(that), enabled(self) | enabled(that)));
var andThen3 = /* @__PURE__ */ dual(2, (self, that) => self | that);
var inverse = /* @__PURE__ */ __name((patch13) => make19(enabled(patch13), invert(active(patch13))), "inverse");
var invert = /* @__PURE__ */ __name((n) => ~n >>> 0 & BIT_MASK, "invert");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/runtimeFlags.js
var None2 = 0;
var Interruption = 1 << 0;
var OpSupervision = 1 << 1;
var RuntimeMetrics = 1 << 2;
var WindDown = 1 << 4;
var CooperativeYielding = 1 << 5;
var allFlags = [None2, Interruption, OpSupervision, RuntimeMetrics, WindDown, CooperativeYielding];
var print = /* @__PURE__ */ __name((flag) => {
  switch (flag) {
    case CooperativeYielding: {
      return "CooperativeYielding";
    }
    case WindDown: {
      return "WindDown";
    }
    case RuntimeMetrics: {
      return "RuntimeMetrics";
    }
    case OpSupervision: {
      return "OpSupervision";
    }
    case Interruption: {
      return "Interruption";
    }
    case None2: {
      return "None";
    }
  }
}, "print");
var cooperativeYielding = /* @__PURE__ */ __name((self) => isEnabled2(self, CooperativeYielding), "cooperativeYielding");
var disable2 = /* @__PURE__ */ dual(2, (self, flag) => self & ~flag);
var disableAll = /* @__PURE__ */ dual(2, (self, flags) => self & ~flags);
var enable2 = /* @__PURE__ */ dual(2, (self, flag) => self | flag);
var enableAll = /* @__PURE__ */ dual(2, (self, flags) => self | flags);
var interruptible = /* @__PURE__ */ __name((self) => interruption(self) && !windDown(self), "interruptible");
var interruption = /* @__PURE__ */ __name((self) => isEnabled2(self, Interruption), "interruption");
var isDisabled2 = /* @__PURE__ */ dual(2, (self, flag) => !isEnabled2(self, flag));
var isEnabled2 = /* @__PURE__ */ dual(2, (self, flag) => (self & flag) !== 0);
var make20 = /* @__PURE__ */ __name((...flags) => flags.reduce((a, b) => a | b, 0), "make");
var none5 = /* @__PURE__ */ make20(None2);
var opSupervision = /* @__PURE__ */ __name((self) => isEnabled2(self, OpSupervision), "opSupervision");
var render = /* @__PURE__ */ __name((self) => {
  const active2 = [];
  allFlags.forEach((flag) => {
    if (isEnabled2(self, flag)) {
      active2.push(`${print(flag)}`);
    }
  });
  return `RuntimeFlags(${active2.join(", ")})`;
}, "render");
var runtimeMetrics = /* @__PURE__ */ __name((self) => isEnabled2(self, RuntimeMetrics), "runtimeMetrics");
var toSet3 = /* @__PURE__ */ __name((self) => new Set(allFlags.filter((flag) => isEnabled2(self, flag))), "toSet");
var windDown = /* @__PURE__ */ __name((self) => isEnabled2(self, WindDown), "windDown");
var enabledSet = /* @__PURE__ */ __name((self) => toSet3(active(self) & enabled(self)), "enabledSet");
var disabledSet = /* @__PURE__ */ __name((self) => toSet3(active(self) & ~enabled(self)), "disabledSet");
var diff7 = /* @__PURE__ */ dual(2, (self, that) => make19(self ^ that, that));
var patch7 = /* @__PURE__ */ dual(2, (self, patch13) => self & (invert(active(patch13)) | enabled(patch13)) | active(patch13) & enabled(patch13));
var renderPatch = /* @__PURE__ */ __name((self) => {
  const enabled2 = Array.from(enabledSet(self)).map((flag) => print(flag)).join(", ");
  const disabled = Array.from(disabledSet(self)).map((flag) => print(flag)).join(", ");
  return `RuntimeFlagsPatch(enabled = (${enabled2}), disabled = (${disabled}))`;
}, "renderPatch");
var differ = /* @__PURE__ */ make18({
  empty: empty19,
  diff: /* @__PURE__ */ __name((oldValue, newValue) => diff7(oldValue, newValue), "diff"),
  combine: /* @__PURE__ */ __name((first4, second) => andThen3(second)(first4), "combine"),
  patch: /* @__PURE__ */ __name((_patch, oldValue) => patch7(oldValue, _patch), "patch")
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/RuntimeFlagsPatch.js
var empty20 = empty19;
var make21 = make19;
var enable3 = enable;
var disable3 = disable;
var isEmpty6 = isEmpty5;
var isActive2 = isActive;
var isEnabled3 = isEnabled;
var isDisabled3 = isDisabled;
var includes = isActive;
var andThen4 = andThen3;
var both2 = both;
var either2 = either;
var exclude2 = exclude;
var inverse2 = inverse;
var enabledSet2 = enabledSet;
var disabledSet2 = disabledSet;
var render2 = renderPatch;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/blockedRequests.js
var empty21 = {
  _tag: "Empty"
};
var par = /* @__PURE__ */ __name((self, that) => ({
  _tag: "Par",
  left: self,
  right: that
}), "par");
var seq = /* @__PURE__ */ __name((self, that) => ({
  _tag: "Seq",
  left: self,
  right: that
}), "seq");
var single = /* @__PURE__ */ __name((dataSource, blockedRequest) => ({
  _tag: "Single",
  dataSource,
  blockedRequest
}), "single");
var MapRequestResolversReducer = /* @__PURE__ */ __name((f) => ({
  emptyCase: /* @__PURE__ */ __name(() => empty21, "emptyCase"),
  parCase: /* @__PURE__ */ __name((left3, right3) => par(left3, right3), "parCase"),
  seqCase: /* @__PURE__ */ __name((left3, right3) => seq(left3, right3), "seqCase"),
  singleCase: /* @__PURE__ */ __name((dataSource, blockedRequest) => single(f(dataSource), blockedRequest), "singleCase")
}), "MapRequestResolversReducer");
var mapRequestResolvers = /* @__PURE__ */ __name((self, f) => reduce10(self, MapRequestResolversReducer(f)), "mapRequestResolvers");
var reduce10 = /* @__PURE__ */ __name((self, reducer) => {
  let input = of4(self);
  let output = empty12();
  while (isCons(input)) {
    const current2 = input.head;
    switch (current2._tag) {
      case "Empty": {
        output = cons(right2(reducer.emptyCase()), output);
        input = input.tail;
        break;
      }
      case "Par": {
        output = cons(left2({
          _tag: "ParCase"
        }), output);
        input = cons(current2.left, cons(current2.right, input.tail));
        break;
      }
      case "Seq": {
        output = cons(left2({
          _tag: "SeqCase"
        }), output);
        input = cons(current2.left, cons(current2.right, input.tail));
        break;
      }
      case "Single": {
        const result2 = reducer.singleCase(current2.dataSource, current2.blockedRequest);
        output = cons(right2(result2), output);
        input = input.tail;
        break;
      }
    }
  }
  const result = reduce9(output, empty12(), (acc, current2) => {
    switch (current2._tag) {
      case "Left": {
        const left3 = unsafeHead3(acc);
        const right3 = unsafeHead3(unsafeTail(acc));
        const tail5 = unsafeTail(unsafeTail(acc));
        switch (current2.left._tag) {
          case "ParCase": {
            return cons(reducer.parCase(left3, right3), tail5);
          }
          case "SeqCase": {
            return cons(reducer.seqCase(left3, right3), tail5);
          }
        }
      }
      case "Right": {
        return cons(current2.right, acc);
      }
    }
  });
  if (isNil(result)) {
    throw new Error("BUG: BlockedRequests.reduce - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return result.head;
}, "reduce");
var flatten5 = /* @__PURE__ */ __name((self) => {
  let current2 = of4(self);
  let updated = empty12();
  while (1) {
    const [parallel5, sequential5] = reduce9(current2, [parallelCollectionEmpty(), empty12()], ([parallel6, sequential6], blockedRequest) => {
      const [par2, seq2] = step(blockedRequest);
      return [parallelCollectionCombine(parallel6, par2), appendAll4(sequential6, seq2)];
    });
    updated = merge4(updated, parallel5);
    if (isNil(sequential5)) {
      return reverse4(updated);
    }
    current2 = sequential5;
  }
  throw new Error("BUG: BlockedRequests.flatten - please report an issue at https://github.com/Effect-TS/effect/issues");
}, "flatten");
var step = /* @__PURE__ */ __name((requests) => {
  let current2 = requests;
  let parallel5 = parallelCollectionEmpty();
  let stack = empty12();
  let sequential5 = empty12();
  while (1) {
    switch (current2._tag) {
      case "Empty": {
        if (isNil(stack)) {
          return [parallel5, sequential5];
        }
        current2 = stack.head;
        stack = stack.tail;
        break;
      }
      case "Par": {
        stack = cons(current2.right, stack);
        current2 = current2.left;
        break;
      }
      case "Seq": {
        const left3 = current2.left;
        const right3 = current2.right;
        switch (left3._tag) {
          case "Empty": {
            current2 = right3;
            break;
          }
          case "Par": {
            const l = left3.left;
            const r = left3.right;
            current2 = par(seq(l, right3), seq(r, right3));
            break;
          }
          case "Seq": {
            const l = left3.left;
            const r = left3.right;
            current2 = seq(l, seq(r, right3));
            break;
          }
          case "Single": {
            current2 = left3;
            sequential5 = cons(right3, sequential5);
            break;
          }
        }
        break;
      }
      case "Single": {
        parallel5 = parallelCollectionAdd(parallel5, current2);
        if (isNil(stack)) {
          return [parallel5, sequential5];
        }
        current2 = stack.head;
        stack = stack.tail;
        break;
      }
    }
  }
  throw new Error("BUG: BlockedRequests.step - please report an issue at https://github.com/Effect-TS/effect/issues");
}, "step");
var merge4 = /* @__PURE__ */ __name((sequential5, parallel5) => {
  if (isNil(sequential5)) {
    return of4(parallelCollectionToSequentialCollection(parallel5));
  }
  if (parallelCollectionIsEmpty(parallel5)) {
    return sequential5;
  }
  const seqHeadKeys = sequentialCollectionKeys(sequential5.head);
  const parKeys = parallelCollectionKeys(parallel5);
  if (seqHeadKeys.length === 1 && parKeys.length === 1 && equals(seqHeadKeys[0], parKeys[0])) {
    return cons(sequentialCollectionCombine(sequential5.head, parallelCollectionToSequentialCollection(parallel5)), sequential5.tail);
  }
  return cons(parallelCollectionToSequentialCollection(parallel5), sequential5);
}, "merge");
var EntryTypeId = /* @__PURE__ */ Symbol.for("effect/RequestBlock/Entry");
var EntryImpl = class {
  static {
    __name(this, "EntryImpl");
  }
  request;
  result;
  listeners;
  ownerId;
  state;
  [EntryTypeId] = blockedRequestVariance;
  constructor(request2, result, listeners, ownerId, state) {
    this.request = request2;
    this.result = result;
    this.listeners = listeners;
    this.ownerId = ownerId;
    this.state = state;
  }
};
var blockedRequestVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var isEntry = /* @__PURE__ */ __name((u) => hasProperty(u, EntryTypeId), "isEntry");
var makeEntry = /* @__PURE__ */ __name((options) => new EntryImpl(options.request, options.result, options.listeners, options.ownerId, options.state), "makeEntry");
var RequestBlockParallelTypeId = /* @__PURE__ */ Symbol.for("effect/RequestBlock/RequestBlockParallel");
var parallelVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var ParallelImpl = class {
  static {
    __name(this, "ParallelImpl");
  }
  map;
  [RequestBlockParallelTypeId] = parallelVariance;
  constructor(map36) {
    this.map = map36;
  }
};
var parallelCollectionEmpty = /* @__PURE__ */ __name(() => new ParallelImpl(empty11()), "parallelCollectionEmpty");
var parallelCollectionMake = /* @__PURE__ */ __name((dataSource, blockedRequest) => new ParallelImpl(make16([dataSource, of3(blockedRequest)])), "parallelCollectionMake");
var parallelCollectionAdd = /* @__PURE__ */ __name((self, blockedRequest) => new ParallelImpl(modifyAt2(self.map, blockedRequest.dataSource, (_) => orElseSome(map2(_, append3(blockedRequest.blockedRequest)), () => of3(blockedRequest.blockedRequest)))), "parallelCollectionAdd");
var parallelCollectionCombine = /* @__PURE__ */ __name((self, that) => new ParallelImpl(reduce8(self.map, that.map, (map36, value2, key) => set4(map36, key, match2(get8(map36, key), {
  onNone: /* @__PURE__ */ __name(() => value2, "onNone"),
  onSome: /* @__PURE__ */ __name((other) => appendAll3(value2, other), "onSome")
})))), "parallelCollectionCombine");
var parallelCollectionIsEmpty = /* @__PURE__ */ __name((self) => isEmpty4(self.map), "parallelCollectionIsEmpty");
var parallelCollectionKeys = /* @__PURE__ */ __name((self) => Array.from(keys3(self.map)), "parallelCollectionKeys");
var parallelCollectionToSequentialCollection = /* @__PURE__ */ __name((self) => sequentialCollectionMake(map11(self.map, (x) => of3(x))), "parallelCollectionToSequentialCollection");
var SequentialCollectionTypeId = /* @__PURE__ */ Symbol.for("effect/RequestBlock/RequestBlockSequential");
var sequentialVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var SequentialImpl = class {
  static {
    __name(this, "SequentialImpl");
  }
  map;
  [SequentialCollectionTypeId] = sequentialVariance;
  constructor(map36) {
    this.map = map36;
  }
};
var sequentialCollectionMake = /* @__PURE__ */ __name((map36) => new SequentialImpl(map36), "sequentialCollectionMake");
var sequentialCollectionCombine = /* @__PURE__ */ __name((self, that) => new SequentialImpl(reduce8(that.map, self.map, (map36, value2, key) => set4(map36, key, match2(get8(map36, key), {
  onNone: /* @__PURE__ */ __name(() => empty7(), "onNone"),
  onSome: /* @__PURE__ */ __name((a) => appendAll3(a, value2), "onSome")
})))), "sequentialCollectionCombine");
var sequentialCollectionIsEmpty = /* @__PURE__ */ __name((self) => isEmpty4(self.map), "sequentialCollectionIsEmpty");
var sequentialCollectionKeys = /* @__PURE__ */ __name((self) => Array.from(keys3(self.map)), "sequentialCollectionKeys");
var sequentialCollectionToChunk = /* @__PURE__ */ __name((self) => Array.from(self.map), "sequentialCollectionToChunk");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/cause.js
var OP_DIE = "Die";
var OP_EMPTY = "Empty";
var OP_FAIL = "Fail";
var OP_INTERRUPT = "Interrupt";
var OP_PARALLEL = "Parallel";
var OP_SEQUENTIAL = "Sequential";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/cause.js
var CauseSymbolKey = "effect/Cause";
var CauseTypeId = /* @__PURE__ */ Symbol.for(CauseSymbolKey);
var variance7 = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E")
};
var proto = {
  [CauseTypeId]: variance7,
  [symbol2]() {
    return pipe(hash(CauseSymbolKey), combine2(hash(flattenCause(this))), cached(this));
  },
  [symbol3](that) {
    return isCause(that) && causeEquals(this, that);
  },
  pipe() {
    return pipeArguments(this, arguments);
  },
  toJSON() {
    switch (this._tag) {
      case "Empty":
        return {
          _id: "Cause",
          _tag: this._tag
        };
      case "Die":
        return {
          _id: "Cause",
          _tag: this._tag,
          defect: toJSON(this.defect)
        };
      case "Interrupt":
        return {
          _id: "Cause",
          _tag: this._tag,
          fiberId: this.fiberId.toJSON()
        };
      case "Fail":
        return {
          _id: "Cause",
          _tag: this._tag,
          failure: toJSON(this.error)
        };
      case "Sequential":
      case "Parallel":
        return {
          _id: "Cause",
          _tag: this._tag,
          left: toJSON(this.left),
          right: toJSON(this.right)
        };
    }
  },
  toString() {
    return pretty(this);
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var empty22 = /* @__PURE__ */ (() => {
  const o = /* @__PURE__ */ Object.create(proto);
  o._tag = OP_EMPTY;
  return o;
})();
var fail = /* @__PURE__ */ __name((error2) => {
  const o = Object.create(proto);
  o._tag = OP_FAIL;
  o.error = error2;
  return o;
}, "fail");
var die = /* @__PURE__ */ __name((defect) => {
  const o = Object.create(proto);
  o._tag = OP_DIE;
  o.defect = defect;
  return o;
}, "die");
var interrupt = /* @__PURE__ */ __name((fiberId4) => {
  const o = Object.create(proto);
  o._tag = OP_INTERRUPT;
  o.fiberId = fiberId4;
  return o;
}, "interrupt");
var parallel = /* @__PURE__ */ __name((left3, right3) => {
  const o = Object.create(proto);
  o._tag = OP_PARALLEL;
  o.left = left3;
  o.right = right3;
  return o;
}, "parallel");
var sequential = /* @__PURE__ */ __name((left3, right3) => {
  const o = Object.create(proto);
  o._tag = OP_SEQUENTIAL;
  o.left = left3;
  o.right = right3;
  return o;
}, "sequential");
var isCause = /* @__PURE__ */ __name((u) => hasProperty(u, CauseTypeId), "isCause");
var isEmptyType = /* @__PURE__ */ __name((self) => self._tag === OP_EMPTY, "isEmptyType");
var isFailType = /* @__PURE__ */ __name((self) => self._tag === OP_FAIL, "isFailType");
var isDieType = /* @__PURE__ */ __name((self) => self._tag === OP_DIE, "isDieType");
var isInterruptType = /* @__PURE__ */ __name((self) => self._tag === OP_INTERRUPT, "isInterruptType");
var isSequentialType = /* @__PURE__ */ __name((self) => self._tag === OP_SEQUENTIAL, "isSequentialType");
var isParallelType = /* @__PURE__ */ __name((self) => self._tag === OP_PARALLEL, "isParallelType");
var size9 = /* @__PURE__ */ __name((self) => reduceWithContext(self, void 0, SizeCauseReducer), "size");
var isEmpty7 = /* @__PURE__ */ __name((self) => {
  if (self._tag === OP_EMPTY) {
    return true;
  }
  return reduce11(self, true, (acc, cause3) => {
    switch (cause3._tag) {
      case OP_EMPTY: {
        return some3(acc);
      }
      case OP_DIE:
      case OP_FAIL:
      case OP_INTERRUPT: {
        return some3(false);
      }
      default: {
        return none2();
      }
    }
  });
}, "isEmpty");
var isFailure = /* @__PURE__ */ __name((self) => isSome2(failureOption(self)), "isFailure");
var isDie = /* @__PURE__ */ __name((self) => isSome2(dieOption(self)), "isDie");
var isInterrupted = /* @__PURE__ */ __name((self) => isSome2(interruptOption(self)), "isInterrupted");
var isInterruptedOnly = /* @__PURE__ */ __name((self) => reduceWithContext(void 0, IsInterruptedOnlyCauseReducer)(self), "isInterruptedOnly");
var failures = /* @__PURE__ */ __name((self) => reverse3(reduce11(self, empty7(), (list, cause3) => cause3._tag === OP_FAIL ? some3(pipe(list, prepend3(cause3.error))) : none2())), "failures");
var defects = /* @__PURE__ */ __name((self) => reverse3(reduce11(self, empty7(), (list, cause3) => cause3._tag === OP_DIE ? some3(pipe(list, prepend3(cause3.defect))) : none2())), "defects");
var interruptors = /* @__PURE__ */ __name((self) => reduce11(self, empty10(), (set14, cause3) => cause3._tag === OP_INTERRUPT ? some3(pipe(set14, add4(cause3.fiberId))) : none2()), "interruptors");
var failureOption = /* @__PURE__ */ __name((self) => find(self, (cause3) => cause3._tag === OP_FAIL ? some3(cause3.error) : none2()), "failureOption");
var failureOrCause = /* @__PURE__ */ __name((self) => {
  const option5 = failureOption(self);
  switch (option5._tag) {
    case "None": {
      return right2(self);
    }
    case "Some": {
      return left2(option5.value);
    }
  }
}, "failureOrCause");
var dieOption = /* @__PURE__ */ __name((self) => find(self, (cause3) => cause3._tag === OP_DIE ? some3(cause3.defect) : none2()), "dieOption");
var flipCauseOption = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: some3(empty22),
  onFail: map2(fail),
  onDie: /* @__PURE__ */ __name((defect) => some3(die(defect)), "onDie"),
  onInterrupt: /* @__PURE__ */ __name((fiberId4) => some3(interrupt(fiberId4)), "onInterrupt"),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
}), "flipCauseOption");
var interruptOption = /* @__PURE__ */ __name((self) => find(self, (cause3) => cause3._tag === OP_INTERRUPT ? some3(cause3.fiberId) : none2()), "interruptOption");
var keepDefects = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: none2(),
  onFail: /* @__PURE__ */ __name(() => none2(), "onFail"),
  onDie: /* @__PURE__ */ __name((defect) => some3(die(defect)), "onDie"),
  onInterrupt: /* @__PURE__ */ __name(() => none2(), "onInterrupt"),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
}), "keepDefects");
var keepDefectsAndElectFailures = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: none2(),
  onFail: /* @__PURE__ */ __name((failure) => some3(die(failure)), "onFail"),
  onDie: /* @__PURE__ */ __name((defect) => some3(die(defect)), "onDie"),
  onInterrupt: /* @__PURE__ */ __name(() => none2(), "onInterrupt"),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
}), "keepDefectsAndElectFailures");
var linearize = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: empty10(),
  onFail: /* @__PURE__ */ __name((error2) => make12(fail(error2)), "onFail"),
  onDie: /* @__PURE__ */ __name((defect) => make12(die(defect)), "onDie"),
  onInterrupt: /* @__PURE__ */ __name((fiberId4) => make12(interrupt(fiberId4)), "onInterrupt"),
  onSequential: /* @__PURE__ */ __name((leftSet, rightSet) => flatMap8(leftSet, (leftCause) => map10(rightSet, (rightCause) => sequential(leftCause, rightCause))), "onSequential"),
  onParallel: /* @__PURE__ */ __name((leftSet, rightSet) => flatMap8(leftSet, (leftCause) => map10(rightSet, (rightCause) => parallel(leftCause, rightCause))), "onParallel")
}), "linearize");
var stripFailures = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: empty22,
  onFail: /* @__PURE__ */ __name(() => empty22, "onFail"),
  onDie: die,
  onInterrupt: interrupt,
  onSequential: sequential,
  onParallel: parallel
}), "stripFailures");
var electFailures = /* @__PURE__ */ __name((self) => match6(self, {
  onEmpty: empty22,
  onFail: die,
  onDie: die,
  onInterrupt: interrupt,
  onSequential: sequential,
  onParallel: parallel
}), "electFailures");
var stripSomeDefects = /* @__PURE__ */ dual(2, (self, pf) => match6(self, {
  onEmpty: some3(empty22),
  onFail: /* @__PURE__ */ __name((error2) => some3(fail(error2)), "onFail"),
  onDie: /* @__PURE__ */ __name((defect) => {
    const option5 = pf(defect);
    return isSome2(option5) ? none2() : some3(die(defect));
  }, "onDie"),
  onInterrupt: /* @__PURE__ */ __name((fiberId4) => some3(interrupt(fiberId4)), "onInterrupt"),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
}));
var as2 = /* @__PURE__ */ dual(2, (self, error2) => map13(self, () => error2));
var map13 = /* @__PURE__ */ dual(2, (self, f) => flatMap11(self, (e) => fail(f(e))));
var flatMap11 = /* @__PURE__ */ dual(2, (self, f) => match6(self, {
  onEmpty: empty22,
  onFail: /* @__PURE__ */ __name((error2) => f(error2), "onFail"),
  onDie: /* @__PURE__ */ __name((defect) => die(defect), "onDie"),
  onInterrupt: /* @__PURE__ */ __name((fiberId4) => interrupt(fiberId4), "onInterrupt"),
  onSequential: /* @__PURE__ */ __name((left3, right3) => sequential(left3, right3), "onSequential"),
  onParallel: /* @__PURE__ */ __name((left3, right3) => parallel(left3, right3), "onParallel")
}));
var flatten6 = /* @__PURE__ */ __name((self) => flatMap11(self, identity), "flatten");
var andThen5 = /* @__PURE__ */ dual(2, (self, f) => isFunction2(f) ? flatMap11(self, f) : flatMap11(self, () => f));
var contains5 = /* @__PURE__ */ dual(2, (self, that) => {
  if (that._tag === OP_EMPTY || self === that) {
    return true;
  }
  return reduce11(self, false, (accumulator, cause3) => {
    return some3(accumulator || causeEquals(cause3, that));
  });
});
var causeEquals = /* @__PURE__ */ __name((left3, right3) => {
  let leftStack = of3(left3);
  let rightStack = of3(right3);
  while (isNonEmpty(leftStack) && isNonEmpty(rightStack)) {
    const [leftParallel, leftSequential] = pipe(headNonEmpty2(leftStack), reduce11([empty10(), empty7()], ([parallel5, sequential5], cause3) => {
      const [par2, seq2] = evaluateCause(cause3);
      return some3([pipe(parallel5, union6(par2)), pipe(sequential5, appendAll3(seq2))]);
    }));
    const [rightParallel, rightSequential] = pipe(headNonEmpty2(rightStack), reduce11([empty10(), empty7()], ([parallel5, sequential5], cause3) => {
      const [par2, seq2] = evaluateCause(cause3);
      return some3([pipe(parallel5, union6(par2)), pipe(sequential5, appendAll3(seq2))]);
    }));
    if (!equals(leftParallel, rightParallel)) {
      return false;
    }
    leftStack = leftSequential;
    rightStack = rightSequential;
  }
  return true;
}, "causeEquals");
var flattenCause = /* @__PURE__ */ __name((cause3) => {
  return flattenCauseLoop(of3(cause3), empty7());
}, "flattenCause");
var flattenCauseLoop = /* @__PURE__ */ __name((causes, flattened) => {
  while (1) {
    const [parallel5, sequential5] = pipe(causes, reduce3([empty10(), empty7()], ([parallel6, sequential6], cause3) => {
      const [par2, seq2] = evaluateCause(cause3);
      return [pipe(parallel6, union6(par2)), pipe(sequential6, appendAll3(seq2))];
    }));
    const updated = size6(parallel5) > 0 ? pipe(flattened, prepend3(parallel5)) : flattened;
    if (isEmpty2(sequential5)) {
      return reverse3(updated);
    }
    causes = sequential5;
    flattened = updated;
  }
  throw new Error(getBugErrorMessage("Cause.flattenCauseLoop"));
}, "flattenCauseLoop");
var find = /* @__PURE__ */ dual(2, (self, pf) => {
  const stack = [self];
  while (stack.length > 0) {
    const item = stack.pop();
    const option5 = pf(item);
    switch (option5._tag) {
      case "None": {
        switch (item._tag) {
          case OP_SEQUENTIAL:
          case OP_PARALLEL: {
            stack.push(item.right);
            stack.push(item.left);
            break;
          }
        }
        break;
      }
      case "Some": {
        return option5;
      }
    }
  }
  return none2();
});
var filter11 = /* @__PURE__ */ dual(2, (self, predicate) => reduceWithContext(self, void 0, FilterCauseReducer(predicate)));
var evaluateCause = /* @__PURE__ */ __name((self) => {
  let cause3 = self;
  const stack = [];
  let _parallel = empty10();
  let _sequential = empty7();
  while (cause3 !== void 0) {
    switch (cause3._tag) {
      case OP_EMPTY: {
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause3 = stack.pop();
        break;
      }
      case OP_FAIL: {
        _parallel = add4(_parallel, make7(cause3._tag, cause3.error));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause3 = stack.pop();
        break;
      }
      case OP_DIE: {
        _parallel = add4(_parallel, make7(cause3._tag, cause3.defect));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause3 = stack.pop();
        break;
      }
      case OP_INTERRUPT: {
        _parallel = add4(_parallel, make7(cause3._tag, cause3.fiberId));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause3 = stack.pop();
        break;
      }
      case OP_SEQUENTIAL: {
        switch (cause3.left._tag) {
          case OP_EMPTY: {
            cause3 = cause3.right;
            break;
          }
          case OP_SEQUENTIAL: {
            cause3 = sequential(cause3.left.left, sequential(cause3.left.right, cause3.right));
            break;
          }
          case OP_PARALLEL: {
            cause3 = parallel(sequential(cause3.left.left, cause3.right), sequential(cause3.left.right, cause3.right));
            break;
          }
          default: {
            _sequential = prepend3(_sequential, cause3.right);
            cause3 = cause3.left;
            break;
          }
        }
        break;
      }
      case OP_PARALLEL: {
        stack.push(cause3.right);
        cause3 = cause3.left;
        break;
      }
    }
  }
  throw new Error(getBugErrorMessage("Cause.evaluateCauseLoop"));
}, "evaluateCause");
var SizeCauseReducer = {
  emptyCase: /* @__PURE__ */ __name(() => 0, "emptyCase"),
  failCase: /* @__PURE__ */ __name(() => 1, "failCase"),
  dieCase: /* @__PURE__ */ __name(() => 1, "dieCase"),
  interruptCase: /* @__PURE__ */ __name(() => 1, "interruptCase"),
  sequentialCase: /* @__PURE__ */ __name((_, left3, right3) => left3 + right3, "sequentialCase"),
  parallelCase: /* @__PURE__ */ __name((_, left3, right3) => left3 + right3, "parallelCase")
};
var IsInterruptedOnlyCauseReducer = {
  emptyCase: constTrue,
  failCase: constFalse,
  dieCase: constFalse,
  interruptCase: constTrue,
  sequentialCase: /* @__PURE__ */ __name((_, left3, right3) => left3 && right3, "sequentialCase"),
  parallelCase: /* @__PURE__ */ __name((_, left3, right3) => left3 && right3, "parallelCase")
};
var FilterCauseReducer = /* @__PURE__ */ __name((predicate) => ({
  emptyCase: /* @__PURE__ */ __name(() => empty22, "emptyCase"),
  failCase: /* @__PURE__ */ __name((_, error2) => fail(error2), "failCase"),
  dieCase: /* @__PURE__ */ __name((_, defect) => die(defect), "dieCase"),
  interruptCase: /* @__PURE__ */ __name((_, fiberId4) => interrupt(fiberId4), "interruptCase"),
  sequentialCase: /* @__PURE__ */ __name((_, left3, right3) => {
    if (predicate(left3)) {
      if (predicate(right3)) {
        return sequential(left3, right3);
      }
      return left3;
    }
    if (predicate(right3)) {
      return right3;
    }
    return empty22;
  }, "sequentialCase"),
  parallelCase: /* @__PURE__ */ __name((_, left3, right3) => {
    if (predicate(left3)) {
      if (predicate(right3)) {
        return parallel(left3, right3);
      }
      return left3;
    }
    if (predicate(right3)) {
      return right3;
    }
    return empty22;
  }, "parallelCase")
}), "FilterCauseReducer");
var OP_SEQUENTIAL_CASE = "SequentialCase";
var OP_PARALLEL_CASE = "ParallelCase";
var match6 = /* @__PURE__ */ dual(2, (self, {
  onDie,
  onEmpty,
  onFail,
  onInterrupt: onInterrupt4,
  onParallel,
  onSequential
}) => {
  return reduceWithContext(self, void 0, {
    emptyCase: /* @__PURE__ */ __name(() => onEmpty, "emptyCase"),
    failCase: /* @__PURE__ */ __name((_, error2) => onFail(error2), "failCase"),
    dieCase: /* @__PURE__ */ __name((_, defect) => onDie(defect), "dieCase"),
    interruptCase: /* @__PURE__ */ __name((_, fiberId4) => onInterrupt4(fiberId4), "interruptCase"),
    sequentialCase: /* @__PURE__ */ __name((_, left3, right3) => onSequential(left3, right3), "sequentialCase"),
    parallelCase: /* @__PURE__ */ __name((_, left3, right3) => onParallel(left3, right3), "parallelCase")
  });
});
var reduce11 = /* @__PURE__ */ dual(3, (self, zero2, pf) => {
  let accumulator = zero2;
  let cause3 = self;
  const causes = [];
  while (cause3 !== void 0) {
    const option5 = pf(accumulator, cause3);
    accumulator = isSome2(option5) ? option5.value : accumulator;
    switch (cause3._tag) {
      case OP_SEQUENTIAL: {
        causes.push(cause3.right);
        cause3 = cause3.left;
        break;
      }
      case OP_PARALLEL: {
        causes.push(cause3.right);
        cause3 = cause3.left;
        break;
      }
      default: {
        cause3 = void 0;
        break;
      }
    }
    if (cause3 === void 0 && causes.length > 0) {
      cause3 = causes.pop();
    }
  }
  return accumulator;
});
var reduceWithContext = /* @__PURE__ */ dual(3, (self, context11, reducer) => {
  const input = [self];
  const output = [];
  while (input.length > 0) {
    const cause3 = input.pop();
    switch (cause3._tag) {
      case OP_EMPTY: {
        output.push(right2(reducer.emptyCase(context11)));
        break;
      }
      case OP_FAIL: {
        output.push(right2(reducer.failCase(context11, cause3.error)));
        break;
      }
      case OP_DIE: {
        output.push(right2(reducer.dieCase(context11, cause3.defect)));
        break;
      }
      case OP_INTERRUPT: {
        output.push(right2(reducer.interruptCase(context11, cause3.fiberId)));
        break;
      }
      case OP_SEQUENTIAL: {
        input.push(cause3.right);
        input.push(cause3.left);
        output.push(left2({
          _tag: OP_SEQUENTIAL_CASE
        }));
        break;
      }
      case OP_PARALLEL: {
        input.push(cause3.right);
        input.push(cause3.left);
        output.push(left2({
          _tag: OP_PARALLEL_CASE
        }));
        break;
      }
    }
  }
  const accumulator = [];
  while (output.length > 0) {
    const either11 = output.pop();
    switch (either11._tag) {
      case "Left": {
        switch (either11.left._tag) {
          case OP_SEQUENTIAL_CASE: {
            const left3 = accumulator.pop();
            const right3 = accumulator.pop();
            const value2 = reducer.sequentialCase(context11, left3, right3);
            accumulator.push(value2);
            break;
          }
          case OP_PARALLEL_CASE: {
            const left3 = accumulator.pop();
            const right3 = accumulator.pop();
            const value2 = reducer.parallelCase(context11, left3, right3);
            accumulator.push(value2);
            break;
          }
        }
        break;
      }
      case "Right": {
        accumulator.push(either11.right);
        break;
      }
    }
  }
  if (accumulator.length === 0) {
    throw new Error("BUG: Cause.reduceWithContext - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return accumulator.pop();
});
var pretty = /* @__PURE__ */ __name((cause3, options) => {
  if (isInterruptedOnly(cause3)) {
    return "All fibers interrupted without errors.";
  }
  return prettyErrors(cause3).map(function(e) {
    if (options?.renderErrorCause !== true || e.cause === void 0) {
      return e.stack;
    }
    return `${e.stack} {
${renderErrorCause(e.cause, "  ")}
}`;
  }).join("\n");
}, "pretty");
var renderErrorCause = /* @__PURE__ */ __name((cause3, prefix) => {
  const lines = cause3.stack.split("\n");
  let stack = `${prefix}[cause]: ${lines[0]}`;
  for (let i = 1, len = lines.length; i < len; i++) {
    stack += `
${prefix}${lines[i]}`;
  }
  if (cause3.cause) {
    stack += ` {
${renderErrorCause(cause3.cause, `${prefix}  `)}
${prefix}}`;
  }
  return stack;
}, "renderErrorCause");
var PrettyError = class _PrettyError extends globalThis.Error {
  static {
    __name(this, "PrettyError");
  }
  span = void 0;
  constructor(originalError2) {
    const originalErrorIsObject = typeof originalError2 === "object" && originalError2 !== null;
    const prevLimit = Error.stackTraceLimit;
    Error.stackTraceLimit = 1;
    super(prettyErrorMessage(originalError2), originalErrorIsObject && "cause" in originalError2 && typeof originalError2.cause !== "undefined" ? {
      cause: new _PrettyError(originalError2.cause)
    } : void 0);
    if (this.message === "") {
      this.message = "An error has occurred";
    }
    Error.stackTraceLimit = prevLimit;
    this.name = originalError2 instanceof Error ? originalError2.name : "Error";
    if (originalErrorIsObject) {
      if (spanSymbol in originalError2) {
        this.span = originalError2[spanSymbol];
      }
      Object.keys(originalError2).forEach((key) => {
        if (!(key in this)) {
          this[key] = originalError2[key];
        }
      });
    }
    this.stack = prettyErrorStack(`${this.name}: ${this.message}`, originalError2 instanceof Error && originalError2.stack ? originalError2.stack : "", this.span);
  }
};
var prettyErrorMessage = /* @__PURE__ */ __name((u) => {
  if (typeof u === "string") {
    return u;
  }
  if (typeof u === "object" && u !== null && u instanceof Error) {
    return u.message;
  }
  try {
    if (hasProperty(u, "toString") && isFunction2(u["toString"]) && u["toString"] !== Object.prototype.toString && u["toString"] !== globalThis.Array.prototype.toString) {
      return u["toString"]();
    }
  } catch {
  }
  return stringifyCircular(u);
}, "prettyErrorMessage");
var locationRegex = /\((.*)\)/g;
var spanToTrace = /* @__PURE__ */ globalValue("effect/Tracer/spanToTrace", () => /* @__PURE__ */ new WeakMap());
var prettyErrorStack = /* @__PURE__ */ __name((message, stack, span4) => {
  const out = [message];
  const lines = stack.startsWith(message) ? stack.slice(message.length).split("\n") : stack.split("\n");
  for (let i = 1; i < lines.length; i++) {
    if (lines[i].includes(" at new BaseEffectError") || lines[i].includes(" at new YieldableError")) {
      i++;
      continue;
    }
    if (lines[i].includes("Generator.next")) {
      break;
    }
    if (lines[i].includes("effect_internal_function")) {
      break;
    }
    out.push(lines[i].replace(/at .*effect_instruction_i.*\((.*)\)/, "at $1").replace(/EffectPrimitive\.\w+/, "<anonymous>"));
  }
  if (span4) {
    let current2 = span4;
    let i = 0;
    while (current2 && current2._tag === "Span" && i < 10) {
      const stackFn = spanToTrace.get(current2);
      if (typeof stackFn === "function") {
        const stack2 = stackFn();
        if (typeof stack2 === "string") {
          const locationMatchAll = stack2.matchAll(locationRegex);
          let match33 = false;
          for (const [, location] of locationMatchAll) {
            match33 = true;
            out.push(`    at ${current2.name} (${location})`);
          }
          if (!match33) {
            out.push(`    at ${current2.name} (${stack2.replace(/^at /, "")})`);
          }
        } else {
          out.push(`    at ${current2.name}`);
        }
      } else {
        out.push(`    at ${current2.name}`);
      }
      current2 = getOrUndefined2(current2.parent);
      i++;
    }
  }
  return out.join("\n");
}, "prettyErrorStack");
var spanSymbol = /* @__PURE__ */ Symbol.for("effect/SpanAnnotation");
var prettyErrors = /* @__PURE__ */ __name((cause3) => reduceWithContext(cause3, void 0, {
  emptyCase: /* @__PURE__ */ __name(() => [], "emptyCase"),
  dieCase: /* @__PURE__ */ __name((_, unknownError) => {
    return [new PrettyError(unknownError)];
  }, "dieCase"),
  failCase: /* @__PURE__ */ __name((_, error2) => {
    return [new PrettyError(error2)];
  }, "failCase"),
  interruptCase: /* @__PURE__ */ __name(() => [], "interruptCase"),
  parallelCase: /* @__PURE__ */ __name((_, l, r) => [...l, ...r], "parallelCase"),
  sequentialCase: /* @__PURE__ */ __name((_, l, r) => [...l, ...r], "sequentialCase")
}), "prettyErrors");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/deferred.js
var OP_STATE_PENDING = "Pending";
var OP_STATE_DONE = "Done";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/deferred.js
var DeferredSymbolKey = "effect/Deferred";
var DeferredTypeId = /* @__PURE__ */ Symbol.for(DeferredSymbolKey);
var deferredVariance = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var pending = /* @__PURE__ */ __name((joiners) => {
  return {
    _tag: OP_STATE_PENDING,
    joiners
  };
}, "pending");
var done = /* @__PURE__ */ __name((effect4) => {
  return {
    _tag: OP_STATE_DONE,
    effect: effect4
  };
}, "done");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/singleShotGen.js
var SingleShotGen2 = class _SingleShotGen {
  static {
    __name(this, "SingleShotGen");
  }
  self;
  called = false;
  constructor(self) {
    this.self = self;
  }
  next(a) {
    return this.called ? {
      value: a,
      done: true
    } : (this.called = true, {
      value: this.self,
      done: false
    });
  }
  return(a) {
    return {
      value: a,
      done: true
    };
  }
  throw(e) {
    throw e;
  }
  [Symbol.iterator]() {
    return new _SingleShotGen(this.self);
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/core.js
var blocked = /* @__PURE__ */ __name((blockedRequests, _continue3) => {
  const effect4 = new EffectPrimitive("Blocked");
  effect4.effect_instruction_i0 = blockedRequests;
  effect4.effect_instruction_i1 = _continue3;
  return effect4;
}, "blocked");
var runRequestBlock = /* @__PURE__ */ __name((blockedRequests) => {
  const effect4 = new EffectPrimitive("RunBlocked");
  effect4.effect_instruction_i0 = blockedRequests;
  return effect4;
}, "runRequestBlock");
var EffectTypeId2 = /* @__PURE__ */ Symbol.for("effect/Effect");
var RevertFlags = class {
  static {
    __name(this, "RevertFlags");
  }
  patch;
  op;
  _op = OP_REVERT_FLAGS;
  constructor(patch13, op) {
    this.patch = patch13;
    this.op = op;
  }
};
var EffectPrimitive = class {
  static {
    __name(this, "EffectPrimitive");
  }
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId2] = effectVariance;
  constructor(_op) {
    this._op = _op;
  }
  [symbol3](that) {
    return this === that;
  }
  [symbol2]() {
    return cached(this, random(this));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Effect",
      _op: this._op,
      effect_instruction_i0: toJSON(this.effect_instruction_i0),
      effect_instruction_i1: toJSON(this.effect_instruction_i1),
      effect_instruction_i2: toJSON(this.effect_instruction_i2)
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen2(new YieldWrap(this));
  }
};
var EffectPrimitiveFailure = class {
  static {
    __name(this, "EffectPrimitiveFailure");
  }
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId2] = effectVariance;
  constructor(_op) {
    this._op = _op;
    this._tag = _op;
  }
  [symbol3](that) {
    return exitIsExit(that) && that._op === "Failure" && // @ts-expect-error
    equals(this.effect_instruction_i0, that.effect_instruction_i0);
  }
  [symbol2]() {
    return pipe(
      // @ts-expect-error
      string2(this._tag),
      // @ts-expect-error
      combine2(hash(this.effect_instruction_i0)),
      cached(this)
    );
  }
  get cause() {
    return this.effect_instruction_i0;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Exit",
      _tag: this._op,
      cause: this.cause.toJSON()
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen2(new YieldWrap(this));
  }
};
var EffectPrimitiveSuccess = class {
  static {
    __name(this, "EffectPrimitiveSuccess");
  }
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId2] = effectVariance;
  constructor(_op) {
    this._op = _op;
    this._tag = _op;
  }
  [symbol3](that) {
    return exitIsExit(that) && that._op === "Success" && // @ts-expect-error
    equals(this.effect_instruction_i0, that.effect_instruction_i0);
  }
  [symbol2]() {
    return pipe(
      // @ts-expect-error
      string2(this._tag),
      // @ts-expect-error
      combine2(hash(this.effect_instruction_i0)),
      cached(this)
    );
  }
  get value() {
    return this.effect_instruction_i0;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Exit",
      _tag: this._op,
      value: toJSON(this.value)
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen2(new YieldWrap(this));
  }
};
var isEffect = /* @__PURE__ */ __name((u) => hasProperty(u, EffectTypeId2), "isEffect");
var withFiberRuntime = /* @__PURE__ */ __name((withRuntime) => {
  const effect4 = new EffectPrimitive(OP_WITH_RUNTIME);
  effect4.effect_instruction_i0 = withRuntime;
  return effect4;
}, "withFiberRuntime");
var acquireUseRelease = /* @__PURE__ */ dual(3, (acquire, use2, release) => uninterruptibleMask((restore) => flatMap12(acquire, (a) => flatMap12(exit(suspend(() => restore(use2(a)))), (exit4) => {
  return suspend(() => release(a, exit4)).pipe(matchCauseEffect({
    onFailure: /* @__PURE__ */ __name((cause3) => {
      switch (exit4._tag) {
        case OP_FAILURE:
          return failCause(sequential(exit4.effect_instruction_i0, cause3));
        case OP_SUCCESS:
          return failCause(cause3);
      }
    }, "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => exit4, "onSuccess")
  }));
}))));
var as3 = /* @__PURE__ */ dual(2, (self, value2) => flatMap12(self, () => succeed(value2)));
var asVoid2 = /* @__PURE__ */ __name((self) => as3(self, void 0), "asVoid");
var custom = /* @__PURE__ */ __name(function() {
  const wrapper = new EffectPrimitive(OP_COMMIT);
  switch (arguments.length) {
    case 2: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.commit = arguments[1];
      break;
    }
    case 3: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.effect_instruction_i1 = arguments[1];
      wrapper.commit = arguments[2];
      break;
    }
    case 4: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.effect_instruction_i1 = arguments[1];
      wrapper.effect_instruction_i2 = arguments[2];
      wrapper.commit = arguments[3];
      break;
    }
    default: {
      throw new Error(getBugErrorMessage("you're not supposed to end up here"));
    }
  }
  return wrapper;
}, "custom");
var unsafeAsync = /* @__PURE__ */ __name((register, blockingOn = none4) => {
  const effect4 = new EffectPrimitive(OP_ASYNC);
  let cancelerRef = void 0;
  effect4.effect_instruction_i0 = (resume2) => {
    cancelerRef = register(resume2);
  };
  effect4.effect_instruction_i1 = blockingOn;
  return onInterrupt(effect4, (_) => isEffect(cancelerRef) ? cancelerRef : void_3);
}, "unsafeAsync");
var asyncInterrupt = /* @__PURE__ */ __name((register, blockingOn = none4) => suspend(() => unsafeAsync(register, blockingOn)), "asyncInterrupt");
var async_ = /* @__PURE__ */ __name((resume2, blockingOn = none4) => {
  return custom(resume2, function() {
    let backingResume = void 0;
    let pendingEffect = void 0;
    function proxyResume(effect5) {
      if (backingResume) {
        backingResume(effect5);
      } else if (pendingEffect === void 0) {
        pendingEffect = effect5;
      }
    }
    __name(proxyResume, "proxyResume");
    const effect4 = new EffectPrimitive(OP_ASYNC);
    effect4.effect_instruction_i0 = (resume3) => {
      backingResume = resume3;
      if (pendingEffect) {
        resume3(pendingEffect);
      }
    };
    effect4.effect_instruction_i1 = blockingOn;
    let cancelerRef = void 0;
    let controllerRef = void 0;
    if (this.effect_instruction_i0.length !== 1) {
      controllerRef = new AbortController();
      cancelerRef = internalCall(() => this.effect_instruction_i0(proxyResume, controllerRef.signal));
    } else {
      cancelerRef = internalCall(() => this.effect_instruction_i0(proxyResume));
    }
    return cancelerRef || controllerRef ? onInterrupt(effect4, (_) => {
      if (controllerRef) {
        controllerRef.abort();
      }
      return cancelerRef ?? void_3;
    }) : effect4;
  });
}, "async_");
var catchAllCause = /* @__PURE__ */ dual(2, (self, f) => {
  const effect4 = new EffectPrimitive(OP_ON_FAILURE);
  effect4.effect_instruction_i0 = self;
  effect4.effect_instruction_i1 = f;
  return effect4;
});
var catchAll = /* @__PURE__ */ dual(2, (self, f) => matchEffect(self, {
  onFailure: f,
  onSuccess: succeed
}));
var catchIf = /* @__PURE__ */ dual(3, (self, predicate, f) => catchAllCause(self, (cause3) => {
  const either11 = failureOrCause(cause3);
  switch (either11._tag) {
    case "Left":
      return predicate(either11.left) ? f(either11.left) : failCause(cause3);
    case "Right":
      return failCause(either11.right);
  }
}));
var catchSome = /* @__PURE__ */ dual(2, (self, pf) => catchAllCause(self, (cause3) => {
  const either11 = failureOrCause(cause3);
  switch (either11._tag) {
    case "Left":
      return pipe(pf(either11.left), getOrElse2(() => failCause(cause3)));
    case "Right":
      return failCause(either11.right);
  }
}));
var checkInterruptible = /* @__PURE__ */ __name((f) => withFiberRuntime((_, status3) => f(interruption(status3.runtimeFlags))), "checkInterruptible");
var originalSymbol = /* @__PURE__ */ Symbol.for("effect/OriginalAnnotation");
var originalInstance = /* @__PURE__ */ __name((obj) => {
  if (hasProperty(obj, originalSymbol)) {
    return obj[originalSymbol];
  }
  return obj;
}, "originalInstance");
var capture = /* @__PURE__ */ __name((obj, span4) => {
  if (isSome2(span4)) {
    return new Proxy(obj, {
      has(target, p) {
        return p === spanSymbol || p === originalSymbol || p in target;
      },
      get(target, p) {
        if (p === spanSymbol) {
          return span4.value;
        }
        if (p === originalSymbol) {
          return obj;
        }
        return target[p];
      }
    });
  }
  return obj;
}, "capture");
var die2 = /* @__PURE__ */ __name((defect) => isObject(defect) && !(spanSymbol in defect) ? withFiberRuntime((fiber) => failCause(die(capture(defect, currentSpanFromFiber(fiber))))) : failCause(die(defect)), "die");
var dieMessage = /* @__PURE__ */ __name((message) => failCauseSync(() => die(new RuntimeException(message))), "dieMessage");
var dieSync = /* @__PURE__ */ __name((evaluate3) => flatMap12(sync(evaluate3), die2), "dieSync");
var either3 = /* @__PURE__ */ __name((self) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((e) => succeed(left2(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed(right2(a)), "onSuccess")
}), "either");
var exit = /* @__PURE__ */ __name((self) => matchCause(self, {
  onFailure: exitFailCause,
  onSuccess: exitSucceed
}), "exit");
var fail2 = /* @__PURE__ */ __name((error2) => isObject(error2) && !(spanSymbol in error2) ? withFiberRuntime((fiber) => failCause(fail(capture(error2, currentSpanFromFiber(fiber))))) : failCause(fail(error2)), "fail");
var failSync = /* @__PURE__ */ __name((evaluate3) => flatMap12(sync(evaluate3), fail2), "failSync");
var failCause = /* @__PURE__ */ __name((cause3) => {
  const effect4 = new EffectPrimitiveFailure(OP_FAILURE);
  effect4.effect_instruction_i0 = cause3;
  return effect4;
}, "failCause");
var failCauseSync = /* @__PURE__ */ __name((evaluate3) => flatMap12(sync(evaluate3), failCause), "failCauseSync");
var fiberId = /* @__PURE__ */ withFiberRuntime((state) => succeed(state.id()));
var fiberIdWith = /* @__PURE__ */ __name((f) => withFiberRuntime((state) => f(state.id())), "fiberIdWith");
var flatMap12 = /* @__PURE__ */ dual(2, (self, f) => {
  const effect4 = new EffectPrimitive(OP_ON_SUCCESS);
  effect4.effect_instruction_i0 = self;
  effect4.effect_instruction_i1 = f;
  return effect4;
});
var andThen6 = /* @__PURE__ */ dual(2, (self, f) => flatMap12(self, (a) => {
  const b = typeof f === "function" ? f(a) : f;
  if (isEffect(b)) {
    return b;
  } else if (isPromiseLike(b)) {
    return unsafeAsync((resume2) => {
      b.then((a2) => resume2(succeed(a2)), (e) => resume2(fail2(new UnknownException(e, "An unknown error occurred in Effect.andThen"))));
    });
  }
  return succeed(b);
}));
var step2 = /* @__PURE__ */ __name((self) => {
  const effect4 = new EffectPrimitive("OnStep");
  effect4.effect_instruction_i0 = self;
  return effect4;
}, "step");
var flatten7 = /* @__PURE__ */ __name((self) => flatMap12(self, identity), "flatten");
var flip3 = /* @__PURE__ */ __name((self) => matchEffect(self, {
  onFailure: succeed,
  onSuccess: fail2
}), "flip");
var matchCause = /* @__PURE__ */ dual(2, (self, options) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => succeed(options.onFailure(cause3)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed(options.onSuccess(a)), "onSuccess")
}));
var matchCauseEffect = /* @__PURE__ */ dual(2, (self, options) => {
  const effect4 = new EffectPrimitive(OP_ON_SUCCESS_AND_FAILURE);
  effect4.effect_instruction_i0 = self;
  effect4.effect_instruction_i1 = options.onFailure;
  effect4.effect_instruction_i2 = options.onSuccess;
  return effect4;
});
var matchEffect = /* @__PURE__ */ dual(2, (self, options) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const defects3 = defects(cause3);
    if (defects3.length > 0) {
      return failCause(electFailures(cause3));
    }
    const failures3 = failures(cause3);
    if (failures3.length > 0) {
      return options.onFailure(unsafeHead2(failures3));
    }
    return failCause(cause3);
  }, "onFailure"),
  onSuccess: options.onSuccess
}));
var forEachSequential = /* @__PURE__ */ dual(2, (self, f) => suspend(() => {
  const arr = fromIterable2(self);
  const ret = allocate(arr.length);
  let i = 0;
  return as3(whileLoop({
    while: /* @__PURE__ */ __name(() => i < arr.length, "while"),
    body: /* @__PURE__ */ __name(() => f(arr[i], i), "body"),
    step: /* @__PURE__ */ __name((b) => {
      ret[i++] = b;
    }, "step")
  }), ret);
}));
var forEachSequentialDiscard = /* @__PURE__ */ dual(2, (self, f) => suspend(() => {
  const arr = fromIterable2(self);
  let i = 0;
  return whileLoop({
    while: /* @__PURE__ */ __name(() => i < arr.length, "while"),
    body: /* @__PURE__ */ __name(() => f(arr[i], i), "body"),
    step: /* @__PURE__ */ __name(() => {
      i++;
    }, "step")
  });
}));
var if_ = /* @__PURE__ */ dual((args2) => typeof args2[0] === "boolean" || isEffect(args2[0]), (self, options) => isEffect(self) ? flatMap12(self, (b) => b ? options.onTrue() : options.onFalse()) : self ? options.onTrue() : options.onFalse());
var interrupt2 = /* @__PURE__ */ flatMap12(fiberId, (fiberId4) => interruptWith(fiberId4));
var interruptWith = /* @__PURE__ */ __name((fiberId4) => failCause(interrupt(fiberId4)), "interruptWith");
var interruptible2 = /* @__PURE__ */ __name((self) => {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = enable3(Interruption);
  effect4.effect_instruction_i1 = () => self;
  return effect4;
}, "interruptible");
var interruptibleMask = /* @__PURE__ */ __name((f) => custom(f, function() {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = enable3(Interruption);
  effect4.effect_instruction_i1 = (oldFlags) => interruption(oldFlags) ? internalCall(() => this.effect_instruction_i0(interruptible2)) : internalCall(() => this.effect_instruction_i0(uninterruptible));
  return effect4;
}), "interruptibleMask");
var intoDeferred = /* @__PURE__ */ dual(2, (self, deferred) => uninterruptibleMask((restore) => flatMap12(exit(restore(self)), (exit4) => deferredDone(deferred, exit4))));
var map14 = /* @__PURE__ */ dual(2, (self, f) => flatMap12(self, (a) => sync(() => f(a))));
var mapBoth3 = /* @__PURE__ */ dual(2, (self, options) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((e) => failSync(() => options.onFailure(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => sync(() => options.onSuccess(a)), "onSuccess")
}));
var mapError = /* @__PURE__ */ dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const either11 = failureOrCause(cause3);
    switch (either11._tag) {
      case "Left": {
        return failSync(() => f(either11.left));
      }
      case "Right": {
        return failCause(either11.right);
      }
    }
  }, "onFailure"),
  onSuccess: succeed
}));
var onError = /* @__PURE__ */ dual(2, (self, cleanup) => onExit(self, (exit4) => exitIsSuccess(exit4) ? void_3 : cleanup(exit4.effect_instruction_i0)));
var onExit = /* @__PURE__ */ dual(2, (self, cleanup) => uninterruptibleMask((restore) => matchCauseEffect(restore(self), {
  onFailure: /* @__PURE__ */ __name((cause1) => {
    const result = exitFailCause(cause1);
    return matchCauseEffect(cleanup(result), {
      onFailure: /* @__PURE__ */ __name((cause22) => exitFailCause(sequential(cause1, cause22)), "onFailure"),
      onSuccess: /* @__PURE__ */ __name(() => result, "onSuccess")
    });
  }, "onFailure"),
  onSuccess: /* @__PURE__ */ __name((success) => {
    const result = exitSucceed(success);
    return zipRight2(cleanup(result), result);
  }, "onSuccess")
})));
var onInterrupt = /* @__PURE__ */ dual(2, (self, cleanup) => onExit(self, exitMatch({
  onFailure: /* @__PURE__ */ __name((cause3) => isInterruptedOnly(cause3) ? asVoid2(cleanup(interruptors(cause3))) : void_3, "onFailure"),
  onSuccess: /* @__PURE__ */ __name(() => void_3, "onSuccess")
})));
var orElse3 = /* @__PURE__ */ dual(2, (self, that) => attemptOrElse(self, that, succeed));
var orDie = /* @__PURE__ */ __name((self) => orDieWith(self, identity), "orDie");
var orDieWith = /* @__PURE__ */ dual(2, (self, f) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((e) => die2(f(e)), "onFailure"),
  onSuccess: succeed
}));
var partitionMap6 = partitionMap3;
var runtimeFlags = /* @__PURE__ */ withFiberRuntime((_, status3) => succeed(status3.runtimeFlags));
var succeed = /* @__PURE__ */ __name((value2) => {
  const effect4 = new EffectPrimitiveSuccess(OP_SUCCESS);
  effect4.effect_instruction_i0 = value2;
  return effect4;
}, "succeed");
var suspend = /* @__PURE__ */ __name((evaluate3) => {
  const effect4 = new EffectPrimitive(OP_COMMIT);
  effect4.commit = evaluate3;
  return effect4;
}, "suspend");
var sync = /* @__PURE__ */ __name((thunk) => {
  const effect4 = new EffectPrimitive(OP_SYNC);
  effect4.effect_instruction_i0 = thunk;
  return effect4;
}, "sync");
var tap2 = /* @__PURE__ */ dual((args2) => args2.length === 3 || args2.length === 2 && !(isObject(args2[1]) && "onlyEffect" in args2[1]), (self, f) => flatMap12(self, (a) => {
  const b = typeof f === "function" ? f(a) : f;
  if (isEffect(b)) {
    return as3(b, a);
  } else if (isPromiseLike(b)) {
    return unsafeAsync((resume2) => {
      b.then((_) => resume2(succeed(a)), (e) => resume2(fail2(new UnknownException(e, "An unknown error occurred in Effect.tap"))));
    });
  }
  return succeed(a);
}));
var transplant = /* @__PURE__ */ __name((f) => withFiberRuntime((state) => {
  const scopeOverride = state.getFiberRef(currentForkScopeOverride);
  const scope6 = pipe(scopeOverride, getOrElse2(() => state.scope()));
  return f(fiberRefLocally(currentForkScopeOverride, some3(scope6)));
}), "transplant");
var attemptOrElse = /* @__PURE__ */ dual(3, (self, that, onSuccess) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const defects3 = defects(cause3);
    if (defects3.length > 0) {
      return failCause(getOrThrow2(keepDefectsAndElectFailures(cause3)));
    }
    return that();
  }, "onFailure"),
  onSuccess
}));
var uninterruptible = /* @__PURE__ */ __name((self) => {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = disable3(Interruption);
  effect4.effect_instruction_i1 = () => self;
  return effect4;
}, "uninterruptible");
var uninterruptibleMask = /* @__PURE__ */ __name((f) => custom(f, function() {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = disable3(Interruption);
  effect4.effect_instruction_i1 = (oldFlags) => interruption(oldFlags) ? internalCall(() => this.effect_instruction_i0(interruptible2)) : internalCall(() => this.effect_instruction_i0(uninterruptible));
  return effect4;
}), "uninterruptibleMask");
var void_3 = /* @__PURE__ */ succeed(void 0);
var updateRuntimeFlags = /* @__PURE__ */ __name((patch13) => {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = patch13;
  effect4.effect_instruction_i1 = void 0;
  return effect4;
}, "updateRuntimeFlags");
var whenEffect = /* @__PURE__ */ dual(2, (self, condition) => flatMap12(condition, (b) => {
  if (b) {
    return pipe(self, map14(some3));
  }
  return succeed(none2());
}));
var whileLoop = /* @__PURE__ */ __name((options) => {
  const effect4 = new EffectPrimitive(OP_WHILE);
  effect4.effect_instruction_i0 = options.while;
  effect4.effect_instruction_i1 = options.body;
  effect4.effect_instruction_i2 = options.step;
  return effect4;
}, "whileLoop");
var fromIterator = /* @__PURE__ */ __name((iterator) => suspend(() => {
  const effect4 = new EffectPrimitive(OP_ITERATOR);
  effect4.effect_instruction_i0 = iterator();
  return effect4;
}), "fromIterator");
var gen3 = /* @__PURE__ */ __name(function() {
  const f = arguments.length === 1 ? arguments[0] : arguments[1].bind(arguments[0]);
  return fromIterator(() => f(pipe));
}, "gen");
var fnUntraced = /* @__PURE__ */ __name((body, ...pipeables) => Object.defineProperty(pipeables.length === 0 ? function(...args2) {
  return fromIterator(() => body.apply(this, args2));
} : function(...args2) {
  let effect4 = fromIterator(() => body.apply(this, args2));
  for (const x of pipeables) {
    effect4 = x(effect4, ...args2);
  }
  return effect4;
}, "length", {
  value: body.length,
  configurable: true
}), "fnUntraced");
var withConcurrency = /* @__PURE__ */ dual(2, (self, concurrency) => fiberRefLocally(self, currentConcurrency, concurrency));
var withRequestBatching = /* @__PURE__ */ dual(2, (self, requestBatching) => fiberRefLocally(self, currentRequestBatching, requestBatching));
var withRuntimeFlags = /* @__PURE__ */ dual(2, (self, update11) => {
  const effect4 = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect4.effect_instruction_i0 = update11;
  effect4.effect_instruction_i1 = () => self;
  return effect4;
});
var withTracerEnabled = /* @__PURE__ */ dual(2, (effect4, enabled2) => fiberRefLocally(effect4, currentTracerEnabled, enabled2));
var withTracerTiming = /* @__PURE__ */ dual(2, (effect4, enabled2) => fiberRefLocally(effect4, currentTracerTimingEnabled, enabled2));
var yieldNow = /* @__PURE__ */ __name((options) => {
  const effect4 = new EffectPrimitive(OP_YIELD);
  return typeof options?.priority !== "undefined" ? withSchedulingPriority(effect4, options.priority) : effect4;
}, "yieldNow");
var zip5 = /* @__PURE__ */ dual(2, (self, that) => flatMap12(self, (a) => map14(that, (b) => [a, b])));
var zipFlatten = /* @__PURE__ */ dual(2, (self, that) => flatMap12(self, (a) => map14(that, (b) => [...a, b])));
var zipLeft2 = /* @__PURE__ */ dual(2, (self, that) => flatMap12(self, (a) => as3(that, a)));
var zipRight2 = /* @__PURE__ */ dual(2, (self, that) => flatMap12(self, () => that));
var zipWith6 = /* @__PURE__ */ dual(3, (self, that, f) => flatMap12(self, (a) => map14(that, (b) => f(a, b))));
var never = /* @__PURE__ */ asyncInterrupt(() => {
  const interval = setInterval(() => {
  }, 2 ** 31 - 1);
  return sync(() => clearInterval(interval));
});
var interruptFiber = /* @__PURE__ */ __name((self) => flatMap12(fiberId, (fiberId4) => pipe(self, interruptAsFiber(fiberId4))), "interruptFiber");
var interruptAsFiber = /* @__PURE__ */ dual(2, (self, fiberId4) => flatMap12(self.interruptAsFork(fiberId4), () => self.await));
var logLevelAll = {
  _tag: "All",
  syslog: 0,
  label: "ALL",
  ordinal: Number.MIN_SAFE_INTEGER,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelFatal = {
  _tag: "Fatal",
  syslog: 2,
  label: "FATAL",
  ordinal: 5e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelError = {
  _tag: "Error",
  syslog: 3,
  label: "ERROR",
  ordinal: 4e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelWarning = {
  _tag: "Warning",
  syslog: 4,
  label: "WARN",
  ordinal: 3e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelInfo = {
  _tag: "Info",
  syslog: 6,
  label: "INFO",
  ordinal: 2e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelDebug = {
  _tag: "Debug",
  syslog: 7,
  label: "DEBUG",
  ordinal: 1e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelTrace = {
  _tag: "Trace",
  syslog: 7,
  label: "TRACE",
  ordinal: 0,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelNone = {
  _tag: "None",
  syslog: 7,
  label: "OFF",
  ordinal: Number.MAX_SAFE_INTEGER,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var allLogLevels = [logLevelAll, logLevelTrace, logLevelDebug, logLevelInfo, logLevelWarning, logLevelError, logLevelFatal, logLevelNone];
var FiberRefSymbolKey = "effect/FiberRef";
var FiberRefTypeId = /* @__PURE__ */ Symbol.for(FiberRefSymbolKey);
var fiberRefVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var fiberRefGet = /* @__PURE__ */ __name((self) => withFiberRuntime((fiber) => exitSucceed(fiber.getFiberRef(self))), "fiberRefGet");
var fiberRefGetAndSet = /* @__PURE__ */ dual(2, (self, value2) => fiberRefModify(self, (v) => [v, value2]));
var fiberRefGetAndUpdate = /* @__PURE__ */ dual(2, (self, f) => fiberRefModify(self, (v) => [v, f(v)]));
var fiberRefGetAndUpdateSome = /* @__PURE__ */ dual(2, (self, pf) => fiberRefModify(self, (v) => [v, getOrElse2(pf(v), () => v)]));
var fiberRefGetWith = /* @__PURE__ */ dual(2, (self, f) => flatMap12(fiberRefGet(self), f));
var fiberRefSet = /* @__PURE__ */ dual(2, (self, value2) => fiberRefModify(self, () => [void 0, value2]));
var fiberRefDelete = /* @__PURE__ */ __name((self) => withFiberRuntime((state) => {
  state.unsafeDeleteFiberRef(self);
  return void_3;
}), "fiberRefDelete");
var fiberRefReset = /* @__PURE__ */ __name((self) => fiberRefSet(self, self.initial), "fiberRefReset");
var fiberRefModify = /* @__PURE__ */ dual(2, (self, f) => withFiberRuntime((state) => {
  const [b, a] = f(state.getFiberRef(self));
  state.setFiberRef(self, a);
  return succeed(b);
}));
var fiberRefModifySome = /* @__PURE__ */ __name((self, def, f) => fiberRefModify(self, (v) => getOrElse2(f(v), () => [def, v])), "fiberRefModifySome");
var fiberRefUpdate = /* @__PURE__ */ dual(2, (self, f) => fiberRefModify(self, (v) => [void 0, f(v)]));
var fiberRefUpdateSome = /* @__PURE__ */ dual(2, (self, pf) => fiberRefModify(self, (v) => [void 0, getOrElse2(pf(v), () => v)]));
var fiberRefUpdateAndGet = /* @__PURE__ */ dual(2, (self, f) => fiberRefModify(self, (v) => {
  const result = f(v);
  return [result, result];
}));
var fiberRefUpdateSomeAndGet = /* @__PURE__ */ dual(2, (self, pf) => fiberRefModify(self, (v) => {
  const result = getOrElse2(pf(v), () => v);
  return [result, result];
}));
var RequestResolverSymbolKey = "effect/RequestResolver";
var RequestResolverTypeId = /* @__PURE__ */ Symbol.for(RequestResolverSymbolKey);
var requestResolverVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var RequestResolverImpl = class _RequestResolverImpl {
  static {
    __name(this, "RequestResolverImpl");
  }
  runAll;
  target;
  [RequestResolverTypeId] = requestResolverVariance;
  constructor(runAll, target) {
    this.runAll = runAll;
    this.target = target;
  }
  [symbol2]() {
    return cached(this, this.target ? hash(this.target) : random(this));
  }
  [symbol3](that) {
    return this.target ? isRequestResolver(that) && equals(this.target, that.target) : this === that;
  }
  identified(...ids3) {
    return new _RequestResolverImpl(this.runAll, fromIterable3(ids3));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isRequestResolver = /* @__PURE__ */ __name((u) => hasProperty(u, RequestResolverTypeId), "isRequestResolver");
var resolverLocally = /* @__PURE__ */ dual(3, (use2, self, value2) => new RequestResolverImpl((requests) => fiberRefLocally(use2.runAll(requests), self, value2), make7("Locally", use2, self, value2)));
var requestBlockLocally = /* @__PURE__ */ __name((self, ref, value2) => reduce10(self, LocallyReducer(ref, value2)), "requestBlockLocally");
var LocallyReducer = /* @__PURE__ */ __name((ref, value2) => ({
  emptyCase: /* @__PURE__ */ __name(() => empty21, "emptyCase"),
  parCase: /* @__PURE__ */ __name((left3, right3) => par(left3, right3), "parCase"),
  seqCase: /* @__PURE__ */ __name((left3, right3) => seq(left3, right3), "seqCase"),
  singleCase: /* @__PURE__ */ __name((dataSource, blockedRequest) => single(resolverLocally(dataSource, ref, value2), blockedRequest), "singleCase")
}), "LocallyReducer");
var fiberRefLocally = /* @__PURE__ */ dual(3, (use2, self, value2) => acquireUseRelease(zipLeft2(fiberRefGet(self), fiberRefSet(self, value2)), () => use2, (oldValue) => fiberRefSet(self, oldValue)));
var fiberRefLocallyWith = /* @__PURE__ */ dual(3, (use2, self, f) => fiberRefGetWith(self, (a) => fiberRefLocally(use2, self, f(a))));
var fiberRefUnsafeMake = /* @__PURE__ */ __name((initial, options) => fiberRefUnsafeMakePatch(initial, {
  differ: update2(),
  fork: options?.fork ?? identity,
  join: options?.join
}), "fiberRefUnsafeMake");
var fiberRefUnsafeMakeHashSet = /* @__PURE__ */ __name((initial) => {
  const differ3 = hashSet();
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ3,
    fork: differ3.empty
  });
}, "fiberRefUnsafeMakeHashSet");
var fiberRefUnsafeMakeReadonlyArray = /* @__PURE__ */ __name((initial) => {
  const differ3 = readonlyArray(update2());
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ3,
    fork: differ3.empty
  });
}, "fiberRefUnsafeMakeReadonlyArray");
var fiberRefUnsafeMakeContext = /* @__PURE__ */ __name((initial) => {
  const differ3 = environment();
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ3,
    fork: differ3.empty
  });
}, "fiberRefUnsafeMakeContext");
var fiberRefUnsafeMakePatch = /* @__PURE__ */ __name((initial, options) => {
  const _fiberRef = {
    ...CommitPrototype,
    [FiberRefTypeId]: fiberRefVariance,
    initial,
    commit() {
      return fiberRefGet(this);
    },
    diff: /* @__PURE__ */ __name((oldValue, newValue) => options.differ.diff(oldValue, newValue), "diff"),
    combine: /* @__PURE__ */ __name((first4, second) => options.differ.combine(first4, second), "combine"),
    patch: /* @__PURE__ */ __name((patch13) => (oldValue) => options.differ.patch(patch13, oldValue), "patch"),
    fork: options.fork,
    join: options.join ?? ((_, n) => n)
  };
  return _fiberRef;
}, "fiberRefUnsafeMakePatch");
var fiberRefUnsafeMakeRuntimeFlags = /* @__PURE__ */ __name((initial) => fiberRefUnsafeMakePatch(initial, {
  differ,
  fork: differ.empty
}), "fiberRefUnsafeMakeRuntimeFlags");
var currentContext = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentContext"), () => fiberRefUnsafeMakeContext(empty6()));
var currentSchedulingPriority = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentSchedulingPriority"), () => fiberRefUnsafeMake(0));
var currentMaxOpsBeforeYield = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentMaxOpsBeforeYield"), () => fiberRefUnsafeMake(2048));
var currentLogAnnotations = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentLogAnnotation"), () => fiberRefUnsafeMake(empty11()));
var currentLogLevel = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentLogLevel"), () => fiberRefUnsafeMake(logLevelInfo));
var currentLogSpan = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentLogSpan"), () => fiberRefUnsafeMake(empty12()));
var withSchedulingPriority = /* @__PURE__ */ dual(2, (self, scheduler) => fiberRefLocally(self, currentSchedulingPriority, scheduler));
var withMaxOpsBeforeYield = /* @__PURE__ */ dual(2, (self, scheduler) => fiberRefLocally(self, currentMaxOpsBeforeYield, scheduler));
var currentConcurrency = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentConcurrency"), () => fiberRefUnsafeMake("unbounded"));
var currentRequestBatching = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentRequestBatching"), () => fiberRefUnsafeMake(true));
var currentUnhandledErrorLogLevel = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentUnhandledErrorLogLevel"), () => fiberRefUnsafeMake(some3(logLevelDebug)));
var currentVersionMismatchErrorLogLevel = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/versionMismatchErrorLogLevel"), () => fiberRefUnsafeMake(some3(logLevelWarning)));
var withUnhandledErrorLogLevel = /* @__PURE__ */ dual(2, (self, level) => fiberRefLocally(self, currentUnhandledErrorLogLevel, level));
var currentMetricLabels = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentMetricLabels"), () => fiberRefUnsafeMakeReadonlyArray(empty4()));
var metricLabels = /* @__PURE__ */ fiberRefGet(currentMetricLabels);
var currentForkScopeOverride = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentForkScopeOverride"), () => fiberRefUnsafeMake(none2(), {
  fork: /* @__PURE__ */ __name(() => none2(), "fork"),
  join: /* @__PURE__ */ __name((parent, _) => parent, "join")
}));
var currentInterruptedCause = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentInterruptedCause"), () => fiberRefUnsafeMake(empty22, {
  fork: /* @__PURE__ */ __name(() => empty22, "fork"),
  join: /* @__PURE__ */ __name((parent, _) => parent, "join")
}));
var currentTracerEnabled = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentTracerEnabled"), () => fiberRefUnsafeMake(true));
var currentTracerTimingEnabled = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentTracerTiming"), () => fiberRefUnsafeMake(true));
var currentTracerSpanAnnotations = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentTracerSpanAnnotations"), () => fiberRefUnsafeMake(empty11()));
var currentTracerSpanLinks = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentTracerSpanLinks"), () => fiberRefUnsafeMake(empty7()));
var ScopeTypeId = /* @__PURE__ */ Symbol.for("effect/Scope");
var CloseableScopeTypeId = /* @__PURE__ */ Symbol.for("effect/CloseableScope");
var scopeAddFinalizer = /* @__PURE__ */ __name((self, finalizer3) => self.addFinalizer(() => asVoid2(finalizer3)), "scopeAddFinalizer");
var scopeAddFinalizerExit = /* @__PURE__ */ __name((self, finalizer3) => self.addFinalizer(finalizer3), "scopeAddFinalizerExit");
var scopeClose = /* @__PURE__ */ __name((self, exit4) => self.close(exit4), "scopeClose");
var scopeFork = /* @__PURE__ */ __name((self, strategy) => self.fork(strategy), "scopeFork");
var causeSquash = /* @__PURE__ */ __name((self) => {
  return causeSquashWith(identity)(self);
}, "causeSquash");
var causeSquashWith = /* @__PURE__ */ dual(2, (self, f) => {
  const option5 = pipe(self, failureOption, map2(f));
  switch (option5._tag) {
    case "None": {
      return pipe(defects(self), head3, match2({
        onNone: /* @__PURE__ */ __name(() => {
          const interrupts = fromIterable2(interruptors(self)).flatMap((fiberId4) => fromIterable2(ids2(fiberId4)).map((id3) => `#${id3}`));
          return new InterruptedException(interrupts ? `Interrupted by fibers: ${interrupts.join(", ")}` : void 0);
        }, "onNone"),
        onSome: identity
      }));
    }
    case "Some": {
      return option5.value;
    }
  }
});
var YieldableError = /* @__PURE__ */ (function() {
  class YieldableError4 extends globalThis.Error {
    static {
      __name(this, "YieldableError");
    }
    commit() {
      return fail2(this);
    }
    toJSON() {
      const obj = {
        ...this
      };
      if (this.message) obj.message = this.message;
      if (this.cause) obj.cause = this.cause;
      return obj;
    }
    [NodeInspectSymbol]() {
      if (this.toString !== globalThis.Error.prototype.toString) {
        return this.stack ? `${this.toString()}
${this.stack.split("\n").slice(1).join("\n")}` : this.toString();
      } else if ("Bun" in globalThis) {
        return pretty(fail(this), {
          renderErrorCause: true
        });
      }
      return this;
    }
  }
  Object.assign(YieldableError4.prototype, StructuralCommitPrototype);
  return YieldableError4;
})();
var makeException = /* @__PURE__ */ __name((proto12, tag) => {
  class Base4 extends YieldableError {
    static {
      __name(this, "Base");
    }
    _tag = tag;
  }
  Object.assign(Base4.prototype, proto12);
  Base4.prototype.name = tag;
  return Base4;
}, "makeException");
var RuntimeExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/RuntimeException");
var RuntimeException = /* @__PURE__ */ makeException({
  [RuntimeExceptionTypeId]: RuntimeExceptionTypeId
}, "RuntimeException");
var isRuntimeException = /* @__PURE__ */ __name((u) => hasProperty(u, RuntimeExceptionTypeId), "isRuntimeException");
var InterruptedExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/InterruptedException");
var InterruptedException = /* @__PURE__ */ makeException({
  [InterruptedExceptionTypeId]: InterruptedExceptionTypeId
}, "InterruptedException");
var isInterruptedException = /* @__PURE__ */ __name((u) => hasProperty(u, InterruptedExceptionTypeId), "isInterruptedException");
var IllegalArgumentExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/IllegalArgument");
var IllegalArgumentException = /* @__PURE__ */ makeException({
  [IllegalArgumentExceptionTypeId]: IllegalArgumentExceptionTypeId
}, "IllegalArgumentException");
var isIllegalArgumentException = /* @__PURE__ */ __name((u) => hasProperty(u, IllegalArgumentExceptionTypeId), "isIllegalArgumentException");
var NoSuchElementExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/NoSuchElement");
var NoSuchElementException = /* @__PURE__ */ makeException({
  [NoSuchElementExceptionTypeId]: NoSuchElementExceptionTypeId
}, "NoSuchElementException");
var isNoSuchElementException = /* @__PURE__ */ __name((u) => hasProperty(u, NoSuchElementExceptionTypeId), "isNoSuchElementException");
var InvalidPubSubCapacityExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/InvalidPubSubCapacityException");
var InvalidPubSubCapacityException = /* @__PURE__ */ makeException({
  [InvalidPubSubCapacityExceptionTypeId]: InvalidPubSubCapacityExceptionTypeId
}, "InvalidPubSubCapacityException");
var ExceededCapacityExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/ExceededCapacityException");
var ExceededCapacityException = /* @__PURE__ */ makeException({
  [ExceededCapacityExceptionTypeId]: ExceededCapacityExceptionTypeId
}, "ExceededCapacityException");
var isExceededCapacityException = /* @__PURE__ */ __name((u) => hasProperty(u, ExceededCapacityExceptionTypeId), "isExceededCapacityException");
var isInvalidCapacityError = /* @__PURE__ */ __name((u) => hasProperty(u, InvalidPubSubCapacityExceptionTypeId), "isInvalidCapacityError");
var TimeoutExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/Timeout");
var TimeoutException = /* @__PURE__ */ makeException({
  [TimeoutExceptionTypeId]: TimeoutExceptionTypeId
}, "TimeoutException");
var timeoutExceptionFromDuration = /* @__PURE__ */ __name((duration3) => new TimeoutException(`Operation timed out after '${format2(duration3)}'`), "timeoutExceptionFromDuration");
var isTimeoutException = /* @__PURE__ */ __name((u) => hasProperty(u, TimeoutExceptionTypeId), "isTimeoutException");
var UnknownExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Cause/errors/UnknownException");
var UnknownException = /* @__PURE__ */ (function() {
  class UnknownException3 extends YieldableError {
    static {
      __name(this, "UnknownException");
    }
    _tag = "UnknownException";
    error;
    constructor(cause3, message) {
      super(message ?? "An unknown error occurred", {
        cause: cause3
      });
      this.error = cause3;
    }
  }
  Object.assign(UnknownException3.prototype, {
    [UnknownExceptionTypeId]: UnknownExceptionTypeId,
    name: "UnknownException"
  });
  return UnknownException3;
})();
var isUnknownException = /* @__PURE__ */ __name((u) => hasProperty(u, UnknownExceptionTypeId), "isUnknownException");
var exitIsExit = /* @__PURE__ */ __name((u) => isEffect(u) && "_tag" in u && (u._tag === "Success" || u._tag === "Failure"), "exitIsExit");
var exitIsFailure = /* @__PURE__ */ __name((self) => self._tag === "Failure", "exitIsFailure");
var exitIsSuccess = /* @__PURE__ */ __name((self) => self._tag === "Success", "exitIsSuccess");
var exitIsInterrupted = /* @__PURE__ */ __name((self) => {
  switch (self._tag) {
    case OP_FAILURE:
      return isInterrupted(self.effect_instruction_i0);
    case OP_SUCCESS:
      return false;
  }
}, "exitIsInterrupted");
var exitAs = /* @__PURE__ */ dual(2, (self, value2) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return exitFailCause(self.effect_instruction_i0);
    }
    case OP_SUCCESS: {
      return exitSucceed(value2);
    }
  }
});
var exitAsVoid = /* @__PURE__ */ __name((self) => exitAs(self, void 0), "exitAsVoid");
var exitCauseOption = /* @__PURE__ */ __name((self) => {
  switch (self._tag) {
    case OP_FAILURE:
      return some3(self.effect_instruction_i0);
    case OP_SUCCESS:
      return none2();
  }
}, "exitCauseOption");
var exitCollectAll = /* @__PURE__ */ __name((exits, options) => exitCollectAllInternal(exits, options?.parallel ? parallel : sequential), "exitCollectAll");
var exitDie = /* @__PURE__ */ __name((defect) => exitFailCause(die(defect)), "exitDie");
var exitExists = /* @__PURE__ */ dual(2, (self, refinement) => {
  switch (self._tag) {
    case OP_FAILURE:
      return false;
    case OP_SUCCESS:
      return refinement(self.effect_instruction_i0);
  }
});
var exitFail = /* @__PURE__ */ __name((error2) => exitFailCause(fail(error2)), "exitFail");
var exitFailCause = /* @__PURE__ */ __name((cause3) => {
  const effect4 = new EffectPrimitiveFailure(OP_FAILURE);
  effect4.effect_instruction_i0 = cause3;
  return effect4;
}, "exitFailCause");
var exitFlatMap = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return exitFailCause(self.effect_instruction_i0);
    }
    case OP_SUCCESS: {
      return f(self.effect_instruction_i0);
    }
  }
});
var exitFlatMapEffect = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return succeed(exitFailCause(self.effect_instruction_i0));
    }
    case OP_SUCCESS: {
      return f(self.effect_instruction_i0);
    }
  }
});
var exitFlatten = /* @__PURE__ */ __name((self) => pipe(self, exitFlatMap(identity)), "exitFlatten");
var exitForEachEffect = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return succeed(exitFailCause(self.effect_instruction_i0));
    }
    case OP_SUCCESS: {
      return exit(f(self.effect_instruction_i0));
    }
  }
});
var exitFromEither = /* @__PURE__ */ __name((either11) => {
  switch (either11._tag) {
    case "Left":
      return exitFail(either11.left);
    case "Right":
      return exitSucceed(either11.right);
  }
}, "exitFromEither");
var exitFromOption = /* @__PURE__ */ __name((option5) => {
  switch (option5._tag) {
    case "None":
      return exitFail(void 0);
    case "Some":
      return exitSucceed(option5.value);
  }
}, "exitFromOption");
var exitGetOrElse = /* @__PURE__ */ dual(2, (self, orElse15) => {
  switch (self._tag) {
    case OP_FAILURE:
      return orElse15(self.effect_instruction_i0);
    case OP_SUCCESS:
      return self.effect_instruction_i0;
  }
});
var exitInterrupt = /* @__PURE__ */ __name((fiberId4) => exitFailCause(interrupt(fiberId4)), "exitInterrupt");
var exitMap = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(self.effect_instruction_i0);
    case OP_SUCCESS:
      return exitSucceed(f(self.effect_instruction_i0));
  }
});
var exitMapBoth = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(pipe(self.effect_instruction_i0, map13(onFailure)));
    case OP_SUCCESS:
      return exitSucceed(onSuccess(self.effect_instruction_i0));
  }
});
var exitMapError = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(pipe(self.effect_instruction_i0, map13(f)));
    case OP_SUCCESS:
      return exitSucceed(self.effect_instruction_i0);
  }
});
var exitMapErrorCause = /* @__PURE__ */ dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(f(self.effect_instruction_i0));
    case OP_SUCCESS:
      return exitSucceed(self.effect_instruction_i0);
  }
});
var exitMatch = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return onFailure(self.effect_instruction_i0);
    case OP_SUCCESS:
      return onSuccess(self.effect_instruction_i0);
  }
});
var exitMatchEffect = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return onFailure(self.effect_instruction_i0);
    case OP_SUCCESS:
      return onSuccess(self.effect_instruction_i0);
  }
});
var exitSucceed = /* @__PURE__ */ __name((value2) => {
  const effect4 = new EffectPrimitiveSuccess(OP_SUCCESS);
  effect4.effect_instruction_i0 = value2;
  return effect4;
}, "exitSucceed");
var exitVoid = /* @__PURE__ */ exitSucceed(void 0);
var exitZip = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((a, a2) => [a, a2], "onSuccess"),
  onFailure: sequential
}));
var exitZipLeft = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((a, _) => a, "onSuccess"),
  onFailure: sequential
}));
var exitZipRight = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((_, a2) => a2, "onSuccess"),
  onFailure: sequential
}));
var exitZipPar = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((a, a2) => [a, a2], "onSuccess"),
  onFailure: parallel
}));
var exitZipParLeft = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((a, _) => a, "onSuccess"),
  onFailure: parallel
}));
var exitZipParRight = /* @__PURE__ */ dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: /* @__PURE__ */ __name((_, a2) => a2, "onSuccess"),
  onFailure: parallel
}));
var exitZipWith = /* @__PURE__ */ dual(3, (self, that, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE: {
      switch (that._tag) {
        case OP_SUCCESS:
          return exitFailCause(self.effect_instruction_i0);
        case OP_FAILURE: {
          return exitFailCause(onFailure(self.effect_instruction_i0, that.effect_instruction_i0));
        }
      }
    }
    case OP_SUCCESS: {
      switch (that._tag) {
        case OP_SUCCESS:
          return exitSucceed(onSuccess(self.effect_instruction_i0, that.effect_instruction_i0));
        case OP_FAILURE:
          return exitFailCause(that.effect_instruction_i0);
      }
    }
  }
});
var exitCollectAllInternal = /* @__PURE__ */ __name((exits, combineCauses) => {
  const list = fromIterable3(exits);
  if (!isNonEmpty(list)) {
    return none2();
  }
  return pipe(tailNonEmpty2(list), reduce3(pipe(headNonEmpty2(list), exitMap(of3)), (accumulator, current2) => pipe(accumulator, exitZipWith(current2, {
    onSuccess: /* @__PURE__ */ __name((list2, value2) => pipe(list2, prepend3(value2)), "onSuccess"),
    onFailure: combineCauses
  }))), exitMap(reverse3), exitMap((chunk4) => toReadonlyArray(chunk4)), some3);
}, "exitCollectAllInternal");
var deferredUnsafeMake = /* @__PURE__ */ __name((fiberId4) => {
  const _deferred = {
    ...CommitPrototype,
    [DeferredTypeId]: deferredVariance,
    state: make13(pending([])),
    commit() {
      return deferredAwait(this);
    },
    blockingOn: fiberId4
  };
  return _deferred;
}, "deferredUnsafeMake");
var deferredMake = /* @__PURE__ */ __name(() => flatMap12(fiberId, (id3) => deferredMakeAs(id3)), "deferredMake");
var deferredMakeAs = /* @__PURE__ */ __name((fiberId4) => sync(() => deferredUnsafeMake(fiberId4)), "deferredMakeAs");
var deferredAwait = /* @__PURE__ */ __name((self) => asyncInterrupt((resume2) => {
  const state = get7(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return resume2(state.effect);
    }
    case OP_STATE_PENDING: {
      state.joiners.push(resume2);
      return deferredInterruptJoiner(self, resume2);
    }
  }
}, self.blockingOn), "deferredAwait");
var deferredComplete = /* @__PURE__ */ dual(2, (self, effect4) => intoDeferred(effect4, self));
var deferredCompleteWith = /* @__PURE__ */ dual(2, (self, effect4) => sync(() => {
  const state = get7(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return false;
    }
    case OP_STATE_PENDING: {
      set3(self.state, done(effect4));
      for (let i = 0, len = state.joiners.length; i < len; i++) {
        state.joiners[i](effect4);
      }
      return true;
    }
  }
}));
var deferredDone = /* @__PURE__ */ dual(2, (self, exit4) => deferredCompleteWith(self, exit4));
var deferredFail = /* @__PURE__ */ dual(2, (self, error2) => deferredCompleteWith(self, fail2(error2)));
var deferredFailSync = /* @__PURE__ */ dual(2, (self, evaluate3) => deferredCompleteWith(self, failSync(evaluate3)));
var deferredFailCause = /* @__PURE__ */ dual(2, (self, cause3) => deferredCompleteWith(self, failCause(cause3)));
var deferredFailCauseSync = /* @__PURE__ */ dual(2, (self, evaluate3) => deferredCompleteWith(self, failCauseSync(evaluate3)));
var deferredDie = /* @__PURE__ */ dual(2, (self, defect) => deferredCompleteWith(self, die2(defect)));
var deferredDieSync = /* @__PURE__ */ dual(2, (self, evaluate3) => deferredCompleteWith(self, dieSync(evaluate3)));
var deferredInterrupt = /* @__PURE__ */ __name((self) => flatMap12(fiberId, (fiberId4) => deferredCompleteWith(self, interruptWith(fiberId4))), "deferredInterrupt");
var deferredInterruptWith = /* @__PURE__ */ dual(2, (self, fiberId4) => deferredCompleteWith(self, interruptWith(fiberId4)));
var deferredIsDone = /* @__PURE__ */ __name((self) => sync(() => get7(self.state)._tag === OP_STATE_DONE), "deferredIsDone");
var deferredPoll = /* @__PURE__ */ __name((self) => sync(() => {
  const state = get7(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return some3(state.effect);
    }
    case OP_STATE_PENDING: {
      return none2();
    }
  }
}), "deferredPoll");
var deferredSucceed = /* @__PURE__ */ dual(2, (self, value2) => deferredCompleteWith(self, succeed(value2)));
var deferredSync = /* @__PURE__ */ dual(2, (self, evaluate3) => deferredCompleteWith(self, sync(evaluate3)));
var deferredUnsafeDone = /* @__PURE__ */ __name((self, effect4) => {
  const state = get7(self.state);
  if (state._tag === OP_STATE_PENDING) {
    set3(self.state, done(effect4));
    for (let i = 0, len = state.joiners.length; i < len; i++) {
      state.joiners[i](effect4);
    }
  }
}, "deferredUnsafeDone");
var deferredInterruptJoiner = /* @__PURE__ */ __name((self, joiner) => sync(() => {
  const state = get7(self.state);
  if (state._tag === OP_STATE_PENDING) {
    const index = state.joiners.indexOf(joiner);
    if (index >= 0) {
      state.joiners.splice(index, 1);
    }
  }
}), "deferredInterruptJoiner");
var constContext = /* @__PURE__ */ withFiberRuntime((fiber) => exitSucceed(fiber.currentContext));
var context = /* @__PURE__ */ __name(() => constContext, "context");
var contextWith = /* @__PURE__ */ __name((f) => map14(context(), f), "contextWith");
var contextWithEffect = /* @__PURE__ */ __name((f) => flatMap12(context(), f), "contextWithEffect");
var provideContext = /* @__PURE__ */ dual(2, (self, context11) => fiberRefLocally(currentContext, context11)(self));
var provideSomeContext = /* @__PURE__ */ dual(2, (self, context11) => fiberRefLocallyWith(currentContext, (parent) => merge3(parent, context11))(self));
var mapInputContext = /* @__PURE__ */ dual(2, (self, f) => contextWithEffect((context11) => provideContext(self, f(context11))));
var filterEffectOrElse = /* @__PURE__ */ dual(2, (self, options) => flatMap12(self, (a) => flatMap12(options.predicate(a), (pass) => pass ? succeed(a) : options.orElse(a))));
var filterEffectOrFail = /* @__PURE__ */ dual(2, (self, options) => filterEffectOrElse(self, {
  predicate: options.predicate,
  orElse: /* @__PURE__ */ __name((a) => fail2(options.orFailWith(a)), "orElse")
}));
var currentSpanFromFiber = /* @__PURE__ */ __name((fiber) => {
  const span4 = fiber.currentSpan;
  return span4 !== void 0 && span4._tag === "Span" ? some3(span4) : none2();
}, "currentSpanFromFiber");
var NoopSpanProto = {
  _tag: "Span",
  spanId: "noop",
  traceId: "noop",
  sampled: false,
  status: {
    _tag: "Ended",
    startTime: /* @__PURE__ */ BigInt(0),
    endTime: /* @__PURE__ */ BigInt(0),
    exit: exitVoid
  },
  attributes: /* @__PURE__ */ new Map(),
  links: [],
  kind: "internal",
  attribute() {
  },
  event() {
  },
  end() {
  },
  addLinks() {
  }
};
var noopSpan = /* @__PURE__ */ __name((options) => Object.assign(Object.create(NoopSpanProto), options), "noopSpan");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Deferred.js
var DeferredTypeId2 = DeferredTypeId;
var make22 = deferredMake;
var makeAs = deferredMakeAs;
var _await = deferredAwait;
var complete = deferredComplete;
var completeWith = deferredCompleteWith;
var done2 = deferredDone;
var fail3 = deferredFail;
var failSync2 = deferredFailSync;
var failCause2 = deferredFailCause;
var failCauseSync2 = deferredFailCauseSync;
var die3 = deferredDie;
var dieSync2 = deferredDieSync;
var interrupt3 = deferredInterrupt;
var interruptWith2 = deferredInterruptWith;
var isDone = deferredIsDone;
var poll = deferredPoll;
var succeed2 = deferredSucceed;
var sync2 = deferredSync;
var unsafeMake4 = deferredUnsafeMake;
var unsafeDone = deferredUnsafeDone;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Exit.js
var isExit = exitIsExit;
var isFailure2 = exitIsFailure;
var isSuccess = exitIsSuccess;
var isInterrupted2 = exitIsInterrupted;
var as4 = exitAs;
var asVoid3 = exitAsVoid;
var causeOption = exitCauseOption;
var all6 = exitCollectAll;
var die4 = exitDie;
var exists2 = exitExists;
var fail4 = exitFail;
var failCause3 = exitFailCause;
var flatMap13 = exitFlatMap;
var flatMapEffect = exitFlatMapEffect;
var flatten8 = exitFlatten;
var forEachEffect = exitForEachEffect;
var fromEither = exitFromEither;
var fromOption4 = exitFromOption;
var getOrElse7 = exitGetOrElse;
var interrupt4 = exitInterrupt;
var map15 = exitMap;
var mapBoth4 = exitMapBoth;
var mapError2 = exitMapError;
var mapErrorCause = exitMapErrorCause;
var match7 = exitMatch;
var matchEffect2 = exitMatchEffect;
var succeed3 = exitSucceed;
var void_4 = exitVoid;
var zip6 = exitZip;
var zipLeft3 = exitZipLeft;
var zipRight3 = exitZipRight;
var zipPar = exitZipPar;
var zipParLeft = exitZipParLeft;
var zipParRight = exitZipParRight;
var zipWith7 = exitZipWith;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/MutableHashMap.js
var TypeId13 = /* @__PURE__ */ Symbol.for("effect/MutableHashMap");
var MutableHashMapProto = {
  [TypeId13]: TypeId13,
  [Symbol.iterator]() {
    return new MutableHashMapIterator(this);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableHashMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var MutableHashMapIterator = class _MutableHashMapIterator {
  static {
    __name(this, "MutableHashMapIterator");
  }
  self;
  referentialIterator;
  bucketIterator;
  constructor(self) {
    this.self = self;
    this.referentialIterator = self.referential[Symbol.iterator]();
  }
  next() {
    if (this.bucketIterator !== void 0) {
      return this.bucketIterator.next();
    }
    const result = this.referentialIterator.next();
    if (result.done) {
      this.bucketIterator = new BucketIterator(this.self.buckets.values());
      return this.next();
    }
    return result;
  }
  [Symbol.iterator]() {
    return new _MutableHashMapIterator(this.self);
  }
};
var BucketIterator = class {
  static {
    __name(this, "BucketIterator");
  }
  backing;
  constructor(backing) {
    this.backing = backing;
  }
  currentBucket;
  next() {
    if (this.currentBucket === void 0) {
      const result2 = this.backing.next();
      if (result2.done) {
        return result2;
      }
      this.currentBucket = result2.value[Symbol.iterator]();
    }
    const result = this.currentBucket.next();
    if (result.done) {
      this.currentBucket = void 0;
      return this.next();
    }
    return result;
  }
};
var empty23 = /* @__PURE__ */ __name(() => {
  const self = Object.create(MutableHashMapProto);
  self.referential = /* @__PURE__ */ new Map();
  self.buckets = /* @__PURE__ */ new Map();
  self.bucketsSize = 0;
  return self;
}, "empty");
var make23 = /* @__PURE__ */ __name((...entries3) => fromIterable9(entries3), "make");
var fromIterable9 = /* @__PURE__ */ __name((entries3) => {
  const self = empty23();
  for (const [key, value2] of entries3) {
    set5(self, key, value2);
  }
  return self;
}, "fromIterable");
var get9 = /* @__PURE__ */ dual(2, (self, key) => {
  if (isEqual(key) === false) {
    return self.referential.has(key) ? some3(self.referential.get(key)) : none2();
  }
  const hash2 = key[symbol2]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return none2();
  }
  return getFromBucket(self, bucket, key);
});
var keys4 = /* @__PURE__ */ __name((self) => {
  const keys7 = Array.from(self.referential.keys());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      keys7.push(bucket[i][0]);
    }
  }
  return keys7;
}, "keys");
var values6 = /* @__PURE__ */ __name((self) => {
  const values10 = Array.from(self.referential.values());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      values10.push(bucket[i][1]);
    }
  }
  return values10;
}, "values");
var getFromBucket = /* @__PURE__ */ __name((self, bucket, key, remove11 = false) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol3](bucket[i][0])) {
      const value2 = bucket[i][1];
      if (remove11) {
        bucket.splice(i, 1);
        self.bucketsSize--;
      }
      return some3(value2);
    }
  }
  return none2();
}, "getFromBucket");
var has6 = /* @__PURE__ */ dual(2, (self, key) => isSome2(get9(self, key)));
var set5 = /* @__PURE__ */ dual(3, (self, key, value2) => {
  if (isEqual(key) === false) {
    self.referential.set(key, value2);
    return self;
  }
  const hash2 = key[symbol2]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    self.buckets.set(hash2, [[key, value2]]);
    self.bucketsSize++;
    return self;
  }
  removeFromBucket(self, bucket, key);
  bucket.push([key, value2]);
  self.bucketsSize++;
  return self;
});
var removeFromBucket = /* @__PURE__ */ __name((self, bucket, key) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol3](bucket[i][0])) {
      bucket.splice(i, 1);
      self.bucketsSize--;
      return;
    }
  }
}, "removeFromBucket");
var modify6 = /* @__PURE__ */ dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    if (self.referential.has(key)) {
      self.referential.set(key, f(self.referential.get(key)));
    }
    return self;
  }
  const hash2 = key[symbol2]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  const value2 = getFromBucket(self, bucket, key, true);
  if (isNone2(value2)) {
    return self;
  }
  bucket.push([key, f(value2.value)]);
  self.bucketsSize++;
  return self;
});
var modifyAt3 = /* @__PURE__ */ dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    const result2 = f(get9(self, key));
    if (isSome2(result2)) {
      set5(self, key, result2.value);
    } else {
      remove8(self, key);
    }
    return self;
  }
  const hash2 = key[symbol2]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    const result2 = f(none2());
    return isSome2(result2) ? set5(self, key, result2.value) : self;
  }
  const result = f(getFromBucket(self, bucket, key, true));
  if (isNone2(result)) {
    if (bucket.length === 0) {
      self.buckets.delete(hash2);
    }
    return self;
  }
  bucket.push([key, result.value]);
  self.bucketsSize++;
  return self;
});
var remove8 = /* @__PURE__ */ dual(2, (self, key) => {
  if (isEqual(key) === false) {
    self.referential.delete(key);
    return self;
  }
  const hash2 = key[symbol2]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  removeFromBucket(self, bucket, key);
  if (bucket.length === 0) {
    self.buckets.delete(hash2);
  }
  return self;
});
var clear = /* @__PURE__ */ __name((self) => {
  self.referential.clear();
  self.buckets.clear();
  self.bucketsSize = 0;
  return self;
}, "clear");
var size10 = /* @__PURE__ */ __name((self) => {
  return self.referential.size + self.bucketsSize;
}, "size");
var isEmpty8 = /* @__PURE__ */ __name((self) => size10(self) === 0, "isEmpty");
var forEach9 = /* @__PURE__ */ dual(2, (self, f) => {
  self.referential.forEach(f);
  for (const bucket of self.buckets.values()) {
    for (const [key, value2] of bucket) {
      f(value2, key);
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/MutableList.js
var TypeId14 = /* @__PURE__ */ Symbol.for("effect/MutableList");
var MutableListProto = {
  [TypeId14]: TypeId14,
  [Symbol.iterator]() {
    let done12 = false;
    let head10 = this.head;
    return {
      next() {
        if (done12) {
          return this.return();
        }
        if (head10 == null) {
          done12 = true;
          return this.return();
        }
        const value2 = head10.value;
        head10 = head10.next;
        return {
          done: done12,
          value: value2
        };
      },
      return(value2) {
        if (!done12) {
          done12 = true;
        }
        return {
          done: true,
          value: value2
        };
      }
    };
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableList",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeNode = /* @__PURE__ */ __name((value2) => ({
  value: value2,
  removed: false,
  prev: void 0,
  next: void 0
}), "makeNode");
var empty24 = /* @__PURE__ */ __name(() => {
  const list = Object.create(MutableListProto);
  list.head = void 0;
  list.tail = void 0;
  list._length = 0;
  return list;
}, "empty");
var fromIterable10 = /* @__PURE__ */ __name((iterable) => {
  const list = empty24();
  for (const element of iterable) {
    append5(list, element);
  }
  return list;
}, "fromIterable");
var make24 = /* @__PURE__ */ __name((...elements) => fromIterable10(elements), "make");
var isEmpty9 = /* @__PURE__ */ __name((self) => length2(self) === 0, "isEmpty");
var length2 = /* @__PURE__ */ __name((self) => self._length, "length");
var tail4 = /* @__PURE__ */ __name((self) => self.tail === void 0 ? void 0 : self.tail.value, "tail");
var head5 = /* @__PURE__ */ __name((self) => self.head === void 0 ? void 0 : self.head.value, "head");
var forEach10 = /* @__PURE__ */ dual(2, (self, f) => {
  let current2 = self.head;
  while (current2 !== void 0) {
    f(current2.value);
    current2 = current2.next;
  }
});
var reset = /* @__PURE__ */ __name((self) => {
  ;
  self._length = 0;
  self.head = void 0;
  self.tail = void 0;
  return self;
}, "reset");
var append5 = /* @__PURE__ */ dual(2, (self, value2) => {
  const node = makeNode(value2);
  if (self.head === void 0) {
    self.head = node;
  }
  if (self.tail === void 0) {
    self.tail = node;
  } else {
    self.tail.next = node;
    node.prev = self.tail;
    self.tail = node;
  }
  ;
  self._length += 1;
  return self;
});
var shift = /* @__PURE__ */ __name((self) => {
  const head10 = self.head;
  if (head10 !== void 0) {
    remove9(self, head10);
    return head10.value;
  }
  return void 0;
}, "shift");
var pop2 = /* @__PURE__ */ __name((self) => {
  const tail5 = self.tail;
  if (tail5 !== void 0) {
    remove9(self, tail5);
    return tail5.value;
  }
  return void 0;
}, "pop");
var prepend5 = /* @__PURE__ */ dual(2, (self, value2) => {
  const node = makeNode(value2);
  node.next = self.head;
  if (self.head !== void 0) {
    self.head.prev = node;
  }
  self.head = node;
  if (self.tail === void 0) {
    self.tail = node;
  }
  ;
  self._length += 1;
  return self;
});
var remove9 = /* @__PURE__ */ __name((self, node) => {
  if (node.removed) {
    return;
  }
  node.removed = true;
  if (node.prev !== void 0 && node.next !== void 0) {
    node.prev.next = node.next;
    node.next.prev = node.prev;
  } else if (node.prev !== void 0) {
    self.tail = node.prev;
    node.prev.next = void 0;
  } else if (node.next !== void 0) {
    self.head = node.next;
    node.next.prev = void 0;
  } else {
    self.tail = void 0;
    self.head = void 0;
  }
  if (self._length > 0) {
    ;
    self._length -= 1;
  }
}, "remove");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/MutableQueue.js
var TypeId15 = /* @__PURE__ */ Symbol.for("effect/MutableQueue");
var EmptyMutableQueue = /* @__PURE__ */ Symbol.for("effect/mutable/MutableQueue/Empty");
var MutableQueueProto = {
  [TypeId15]: TypeId15,
  [Symbol.iterator]() {
    return Array.from(this.queue)[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableQueue",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make25 = /* @__PURE__ */ __name((capacity10) => {
  const queue = Object.create(MutableQueueProto);
  queue.queue = empty24();
  queue.capacity = capacity10;
  return queue;
}, "make");
var bounded = /* @__PURE__ */ __name((capacity10) => make25(capacity10), "bounded");
var unbounded = /* @__PURE__ */ __name(() => make25(void 0), "unbounded");
var length3 = /* @__PURE__ */ __name((self) => length2(self.queue), "length");
var isEmpty10 = /* @__PURE__ */ __name((self) => isEmpty9(self.queue), "isEmpty");
var isFull = /* @__PURE__ */ __name((self) => self.capacity === void 0 ? false : length2(self.queue) === self.capacity, "isFull");
var capacity = /* @__PURE__ */ __name((self) => self.capacity === void 0 ? Infinity : self.capacity, "capacity");
var offer = /* @__PURE__ */ dual(2, (self, value2) => {
  const queueLength = length2(self.queue);
  if (self.capacity !== void 0 && queueLength === self.capacity) {
    return false;
  }
  append5(value2)(self.queue);
  return true;
});
var offerAll = /* @__PURE__ */ dual(2, (self, values10) => {
  const iterator = values10[Symbol.iterator]();
  let next4;
  let remainder2 = empty7();
  let offering = true;
  while (offering && (next4 = iterator.next()) && !next4.done) {
    offering = offer(next4.value)(self);
  }
  while (next4 != null && !next4.done) {
    remainder2 = prepend3(next4.value)(remainder2);
    next4 = iterator.next();
  }
  return reverse3(remainder2);
});
var poll2 = /* @__PURE__ */ dual(2, (self, def) => {
  if (isEmpty9(self.queue)) {
    return def;
  }
  return shift(self.queue);
});
var pollUpTo = /* @__PURE__ */ dual(2, (self, n) => {
  let result = empty7();
  let count5 = 0;
  while (count5 < n) {
    const element = poll2(EmptyMutableQueue)(self);
    if (element === EmptyMutableQueue) {
      break;
    }
    result = prepend3(element)(result);
    count5 += 1;
  }
  return reverse3(result);
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/clock.js
var ClockSymbolKey = "effect/Clock";
var ClockTypeId = /* @__PURE__ */ Symbol.for(ClockSymbolKey);
var clockTag = /* @__PURE__ */ GenericTag("effect/Clock");
var MAX_TIMER_MILLIS = 2 ** 31 - 1;
var globalClockScheduler = {
  unsafeSchedule(task, duration3) {
    const millis2 = toMillis(duration3);
    if (millis2 > MAX_TIMER_MILLIS) {
      return constFalse;
    }
    let completed = false;
    const handle = setTimeout(() => {
      completed = true;
      task();
    }, millis2);
    return () => {
      clearTimeout(handle);
      return !completed;
    };
  }
};
var performanceNowNanos = /* @__PURE__ */ (function() {
  const bigint1e62 = /* @__PURE__ */ BigInt(1e6);
  if (typeof performance === "undefined" || typeof performance.now !== "function") {
    return () => BigInt(Date.now()) * bigint1e62;
  }
  let origin;
  return () => {
    if (origin === void 0) {
      origin = BigInt(Date.now()) * bigint1e62 - BigInt(Math.round(performance.now() * 1e6));
    }
    return origin + BigInt(Math.round(performance.now() * 1e6));
  };
})();
var processOrPerformanceNow = /* @__PURE__ */ (function() {
  const processHrtime = typeof process === "object" && "hrtime" in process && typeof process.hrtime.bigint === "function" ? process.hrtime : void 0;
  if (!processHrtime) {
    return performanceNowNanos;
  }
  const origin = /* @__PURE__ */ performanceNowNanos() - /* @__PURE__ */ processHrtime.bigint();
  return () => origin + processHrtime.bigint();
})();
var ClockImpl = class {
  static {
    __name(this, "ClockImpl");
  }
  [ClockTypeId] = ClockTypeId;
  unsafeCurrentTimeMillis() {
    return Date.now();
  }
  unsafeCurrentTimeNanos() {
    return processOrPerformanceNow();
  }
  currentTimeMillis = /* @__PURE__ */ sync(() => this.unsafeCurrentTimeMillis());
  currentTimeNanos = /* @__PURE__ */ sync(() => this.unsafeCurrentTimeNanos());
  scheduler() {
    return succeed(globalClockScheduler);
  }
  sleep(duration3) {
    return async_((resume2) => {
      const canceler = globalClockScheduler.unsafeSchedule(() => resume2(void_3), duration3);
      return asVoid2(sync(canceler));
    });
  }
};
var make26 = /* @__PURE__ */ __name(() => new ClockImpl(), "make");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/configError.js
var OP_AND = "And";
var OP_OR = "Or";
var OP_INVALID_DATA = "InvalidData";
var OP_MISSING_DATA = "MissingData";
var OP_SOURCE_UNAVAILABLE = "SourceUnavailable";
var OP_UNSUPPORTED = "Unsupported";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/configError.js
var ConfigErrorSymbolKey = "effect/ConfigError";
var ConfigErrorTypeId = /* @__PURE__ */ Symbol.for(ConfigErrorSymbolKey);
var proto2 = {
  _tag: "ConfigError",
  [ConfigErrorTypeId]: ConfigErrorTypeId
};
var And = /* @__PURE__ */ __name((self, that) => {
  const error2 = Object.create(proto2);
  error2._op = OP_AND;
  error2.left = self;
  error2.right = that;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      return `${this.left} and ${this.right}`;
    }
  });
  Object.defineProperty(error2, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error2;
}, "And");
var Or = /* @__PURE__ */ __name((self, that) => {
  const error2 = Object.create(proto2);
  error2._op = OP_OR;
  error2.left = self;
  error2.right = that;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      return `${this.left} or ${this.right}`;
    }
  });
  Object.defineProperty(error2, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error2;
}, "Or");
var InvalidData = /* @__PURE__ */ __name((path, message, options = {
  pathDelim: "."
}) => {
  const error2 = Object.create(proto2);
  error2._op = OP_INVALID_DATA;
  error2.path = path;
  error2.message = message;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Invalid data at ${path2}: "${this.message}")`;
    }
  });
  return error2;
}, "InvalidData");
var MissingData = /* @__PURE__ */ __name((path, message, options = {
  pathDelim: "."
}) => {
  const error2 = Object.create(proto2);
  error2._op = OP_MISSING_DATA;
  error2.path = path;
  error2.message = message;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Missing data at ${path2}: "${this.message}")`;
    }
  });
  return error2;
}, "MissingData");
var SourceUnavailable = /* @__PURE__ */ __name((path, message, cause3, options = {
  pathDelim: "."
}) => {
  const error2 = Object.create(proto2);
  error2._op = OP_SOURCE_UNAVAILABLE;
  error2.path = path;
  error2.message = message;
  error2.cause = cause3;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Source unavailable at ${path2}: "${this.message}")`;
    }
  });
  return error2;
}, "SourceUnavailable");
var Unsupported = /* @__PURE__ */ __name((path, message, options = {
  pathDelim: "."
}) => {
  const error2 = Object.create(proto2);
  error2._op = OP_UNSUPPORTED;
  error2.path = path;
  error2.message = message;
  Object.defineProperty(error2, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Unsupported operation at ${path2}: "${this.message}")`;
    }
  });
  return error2;
}, "Unsupported");
var isConfigError = /* @__PURE__ */ __name((u) => hasProperty(u, ConfigErrorTypeId), "isConfigError");
var isAnd = /* @__PURE__ */ __name((self) => self._op === OP_AND, "isAnd");
var isOr = /* @__PURE__ */ __name((self) => self._op === OP_OR, "isOr");
var isInvalidData = /* @__PURE__ */ __name((self) => self._op === OP_INVALID_DATA, "isInvalidData");
var isMissingData = /* @__PURE__ */ __name((self) => self._op === OP_MISSING_DATA, "isMissingData");
var isSourceUnavailable = /* @__PURE__ */ __name((self) => self._op === OP_SOURCE_UNAVAILABLE, "isSourceUnavailable");
var isUnsupported = /* @__PURE__ */ __name((self) => self._op === OP_UNSUPPORTED, "isUnsupported");
var prefixed = /* @__PURE__ */ dual(2, (self, prefix) => {
  switch (self._op) {
    case OP_AND: {
      return And(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_OR: {
      return Or(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_INVALID_DATA: {
      return InvalidData([...prefix, ...self.path], self.message);
    }
    case OP_MISSING_DATA: {
      return MissingData([...prefix, ...self.path], self.message);
    }
    case OP_SOURCE_UNAVAILABLE: {
      return SourceUnavailable([...prefix, ...self.path], self.message, self.cause);
    }
    case OP_UNSUPPORTED: {
      return Unsupported([...prefix, ...self.path], self.message);
    }
  }
});
var IsMissingDataOnlyReducer = {
  andCase: /* @__PURE__ */ __name((_, left3, right3) => left3 && right3, "andCase"),
  orCase: /* @__PURE__ */ __name((_, left3, right3) => left3 && right3, "orCase"),
  invalidDataCase: constFalse,
  missingDataCase: constTrue,
  sourceUnavailableCase: constFalse,
  unsupportedCase: constFalse
};
var reduceWithContext2 = /* @__PURE__ */ dual(3, (self, context11, reducer) => {
  const input = [self];
  const output = [];
  while (input.length > 0) {
    const error2 = input.pop();
    switch (error2._op) {
      case OP_AND: {
        input.push(error2.right);
        input.push(error2.left);
        output.push(left2({
          _op: "AndCase"
        }));
        break;
      }
      case OP_OR: {
        input.push(error2.right);
        input.push(error2.left);
        output.push(left2({
          _op: "OrCase"
        }));
        break;
      }
      case OP_INVALID_DATA: {
        output.push(right2(reducer.invalidDataCase(context11, error2.path, error2.message)));
        break;
      }
      case OP_MISSING_DATA: {
        output.push(right2(reducer.missingDataCase(context11, error2.path, error2.message)));
        break;
      }
      case OP_SOURCE_UNAVAILABLE: {
        output.push(right2(reducer.sourceUnavailableCase(context11, error2.path, error2.message, error2.cause)));
        break;
      }
      case OP_UNSUPPORTED: {
        output.push(right2(reducer.unsupportedCase(context11, error2.path, error2.message)));
        break;
      }
    }
  }
  const accumulator = [];
  while (output.length > 0) {
    const either11 = output.pop();
    switch (either11._op) {
      case "Left": {
        switch (either11.left._op) {
          case "AndCase": {
            const left3 = accumulator.pop();
            const right3 = accumulator.pop();
            const value2 = reducer.andCase(context11, left3, right3);
            accumulator.push(value2);
            break;
          }
          case "OrCase": {
            const left3 = accumulator.pop();
            const right3 = accumulator.pop();
            const value2 = reducer.orCase(context11, left3, right3);
            accumulator.push(value2);
            break;
          }
        }
        break;
      }
      case "Right": {
        accumulator.push(either11.right);
        break;
      }
    }
  }
  if (accumulator.length === 0) {
    throw new Error("BUG: ConfigError.reduceWithContext - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return accumulator.pop();
});
var isMissingDataOnly = /* @__PURE__ */ __name((self) => reduceWithContext2(self, void 0, IsMissingDataOnlyReducer), "isMissingDataOnly");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/configProvider/pathPatch.js
var empty25 = {
  _tag: "Empty"
};
var andThen7 = /* @__PURE__ */ dual(2, (self, that) => ({
  _tag: "AndThen",
  first: self,
  second: that
}));
var mapName = /* @__PURE__ */ dual(2, (self, f) => andThen7(self, {
  _tag: "MapName",
  f
}));
var nested = /* @__PURE__ */ dual(2, (self, name) => andThen7(self, {
  _tag: "Nested",
  name
}));
var unnested = /* @__PURE__ */ dual(2, (self, name) => andThen7(self, {
  _tag: "Unnested",
  name
}));
var patch8 = /* @__PURE__ */ dual(2, (path, patch13) => {
  let input = of4(patch13);
  let output = path;
  while (isCons(input)) {
    const patch14 = input.head;
    switch (patch14._tag) {
      case "Empty": {
        input = input.tail;
        break;
      }
      case "AndThen": {
        input = cons(patch14.first, cons(patch14.second, input.tail));
        break;
      }
      case "MapName": {
        output = map6(output, patch14.f);
        input = input.tail;
        break;
      }
      case "Nested": {
        output = prepend2(output, patch14.name);
        input = input.tail;
        break;
      }
      case "Unnested": {
        const containsName = pipe(head2(output), contains(patch14.name));
        if (containsName) {
          output = tailNonEmpty(output);
          input = input.tail;
        } else {
          return left2(MissingData(output, `Expected ${patch14.name} to be in path in ConfigProvider#unnested`));
        }
        break;
      }
    }
  }
  return right2(output);
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/config.js
var OP_CONSTANT = "Constant";
var OP_FAIL2 = "Fail";
var OP_FALLBACK = "Fallback";
var OP_DESCRIBED = "Described";
var OP_LAZY = "Lazy";
var OP_MAP_OR_FAIL = "MapOrFail";
var OP_NESTED = "Nested";
var OP_PRIMITIVE = "Primitive";
var OP_SEQUENCE = "Sequence";
var OP_HASHMAP = "HashMap";
var OP_ZIP_WITH = "ZipWith";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/string-utils.js
var lowerCase = /* @__PURE__ */ __name((str) => str.toLowerCase(), "lowerCase");
var upperCase = /* @__PURE__ */ __name((str) => str.toUpperCase(), "upperCase");
var replace4 = /* @__PURE__ */ __name((input, re, value2) => re instanceof RegExp ? input.replace(re, value2) : re.reduce((input2, re2) => input2.replace(re2, value2), input), "replace");
var DEFAULT_SPLIT_REGEXP = [/([a-z0-9])([A-Z])/g, /([A-Z])([A-Z][a-z])/g];
var DEFAULT_STRIP_REGEXP = /[^A-Z0-9]+/gi;
var noCase = /* @__PURE__ */ __name((input, options = {}) => {
  const {
    delimiter = " ",
    splitRegexp = DEFAULT_SPLIT_REGEXP,
    stripRegexp = DEFAULT_STRIP_REGEXP,
    transform: transform3 = lowerCase
  } = options;
  const result = replace4(replace4(input, splitRegexp, "$1\0$2"), stripRegexp, "\0");
  let start3 = 0;
  let end6 = result.length;
  while (result.charAt(start3) === "\0") {
    start3++;
  }
  while (result.charAt(end6 - 1) === "\0") {
    end6--;
  }
  return result.slice(start3, end6).split("\0").map(transform3).join(delimiter);
}, "noCase");
var pascalCaseTransform = /* @__PURE__ */ __name((input, index) => {
  const firstChar = input.charAt(0);
  const lowerChars = input.substring(1).toLowerCase();
  if (index > 0 && firstChar >= "0" && firstChar <= "9") {
    return `_${firstChar}${lowerChars}`;
  }
  return `${firstChar.toUpperCase()}${lowerChars}`;
}, "pascalCaseTransform");
var pascalCase = /* @__PURE__ */ __name((input, options) => noCase(input, {
  delimiter: "",
  transform: pascalCaseTransform,
  ...options
}), "pascalCase");
var camelCaseTransform = /* @__PURE__ */ __name((input, index) => index === 0 ? input.toLowerCase() : pascalCaseTransform(input, index), "camelCaseTransform");
var camelCase = /* @__PURE__ */ __name((input, options) => pascalCase(input, {
  transform: camelCaseTransform,
  ...options
}), "camelCase");
var constantCase = /* @__PURE__ */ __name((input, options) => noCase(input, {
  delimiter: "_",
  transform: upperCase,
  ...options
}), "constantCase");
var kebabCase = /* @__PURE__ */ __name((input, options) => noCase(input, {
  delimiter: "-",
  ...options
}), "kebabCase");
var snakeCase = /* @__PURE__ */ __name((input, options) => noCase(input, {
  delimiter: "_",
  ...options
}), "snakeCase");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/configProvider.js
var concat = /* @__PURE__ */ __name((l, r) => [...l, ...r], "concat");
var ConfigProviderSymbolKey = "effect/ConfigProvider";
var ConfigProviderTypeId = /* @__PURE__ */ Symbol.for(ConfigProviderSymbolKey);
var configProviderTag = /* @__PURE__ */ GenericTag("effect/ConfigProvider");
var FlatConfigProviderSymbolKey = "effect/ConfigProviderFlat";
var FlatConfigProviderTypeId = /* @__PURE__ */ Symbol.for(FlatConfigProviderSymbolKey);
var make27 = /* @__PURE__ */ __name((options) => ({
  [ConfigProviderTypeId]: ConfigProviderTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
}), "make");
var makeFlat = /* @__PURE__ */ __name((options) => ({
  [FlatConfigProviderTypeId]: FlatConfigProviderTypeId,
  patch: options.patch,
  load: /* @__PURE__ */ __name((path, config2, split6 = true) => options.load(path, config2, split6), "load"),
  enumerateChildren: options.enumerateChildren
}), "makeFlat");
var fromFlat = /* @__PURE__ */ __name((flat) => make27({
  load: /* @__PURE__ */ __name((config2) => flatMap12(fromFlatLoop(flat, empty4(), config2, false), (chunk4) => match2(head2(chunk4), {
    onNone: /* @__PURE__ */ __name(() => fail2(MissingData(empty4(), `Expected a single value having structure: ${config2}`)), "onNone"),
    onSome: succeed
  })), "load"),
  flattened: flat
}), "fromFlat");
var fromEnv = /* @__PURE__ */ __name((options) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({}, {
    pathDelim: "_",
    seqDelim: ","
  }, options);
  const makePathString = /* @__PURE__ */ __name((path) => pipe(path, join(pathDelim)), "makePathString");
  const unmakePathString = /* @__PURE__ */ __name((pathString) => pathString.split(pathDelim), "unmakePathString");
  const getEnv = /* @__PURE__ */ __name(() => typeof process !== "undefined" && "env" in process && typeof process.env === "object" ? process.env : {}, "getEnv");
  const load = /* @__PURE__ */ __name((path, primitive, split6 = true) => {
    const pathString = makePathString(path);
    const current2 = getEnv();
    const valueOpt = pathString in current2 ? some3(current2[pathString]) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the process context`)), flatMap12((value2) => parsePrimitive(value2, path, primitive, seqDelim, split6)));
  }, "load");
  const enumerateChildren = /* @__PURE__ */ __name((path) => sync(() => {
    const current2 = getEnv();
    const keys7 = Object.keys(current2);
    const keyPaths = keys7.map((value2) => unmakePathString(value2.toUpperCase()));
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable6(filteredKeyPaths);
  }), "enumerateChildren");
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty25
  }));
}, "fromEnv");
var fromMap = /* @__PURE__ */ __name((map36, config2) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({
    seqDelim: ",",
    pathDelim: "."
  }, config2);
  const makePathString = /* @__PURE__ */ __name((path) => pipe(path, join(pathDelim)), "makePathString");
  const unmakePathString = /* @__PURE__ */ __name((pathString) => pathString.split(pathDelim), "unmakePathString");
  const mapWithIndexSplit = splitIndexInKeys(map36, (str) => unmakePathString(str), makePathString);
  const load = /* @__PURE__ */ __name((path, primitive, split6 = true) => {
    const pathString = makePathString(path);
    const valueOpt = mapWithIndexSplit.has(pathString) ? some3(mapWithIndexSplit.get(pathString)) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the provided map`)), flatMap12((value2) => parsePrimitive(value2, path, primitive, seqDelim, split6)));
  }, "load");
  const enumerateChildren = /* @__PURE__ */ __name((path) => sync(() => {
    const keyPaths = fromIterable2(mapWithIndexSplit.keys()).map(unmakePathString);
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable6(filteredKeyPaths);
  }), "enumerateChildren");
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty25
  }));
}, "fromMap");
var extend2 = /* @__PURE__ */ __name((leftDef, rightDef, left3, right3) => {
  const leftPad = unfold2(left3.length, (index) => index >= right3.length ? none2() : some3([leftDef(index), index + 1]));
  const rightPad = unfold2(right3.length, (index) => index >= left3.length ? none2() : some3([rightDef(index), index + 1]));
  const leftExtension = concat(left3, leftPad);
  const rightExtension = concat(right3, rightPad);
  return [leftExtension, rightExtension];
}, "extend");
var appendConfigPath = /* @__PURE__ */ __name((path, config2) => {
  let op = config2;
  if (op._tag === "Nested") {
    const out = path.slice();
    while (op._tag === "Nested") {
      out.push(op.name);
      op = op.config;
    }
    return out;
  }
  return path;
}, "appendConfigPath");
var fromFlatLoop = /* @__PURE__ */ __name((flat, prefix, config2, split6) => {
  const op = config2;
  switch (op._tag) {
    case OP_CONSTANT: {
      return succeed(of2(op.value));
    }
    case OP_DESCRIBED: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config, split6));
    }
    case OP_FAIL2: {
      return fail2(MissingData(prefix, op.message));
    }
    case OP_FALLBACK: {
      return pipe(suspend(() => fromFlatLoop(flat, prefix, op.first, split6)), catchAll((error1) => {
        if (op.condition(error1)) {
          return pipe(fromFlatLoop(flat, prefix, op.second, split6), catchAll((error2) => fail2(Or(error1, error2))));
        }
        return fail2(error1);
      }));
    }
    case OP_LAZY: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config(), split6));
    }
    case OP_MAP_OR_FAIL: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.original, split6), flatMap12(forEachSequential((a) => pipe(op.mapOrFail(a), mapError(prefixed(appendConfigPath(prefix, op.original))))))));
    }
    case OP_NESTED: {
      return suspend(() => fromFlatLoop(flat, concat(prefix, of2(op.name)), op.config, split6));
    }
    case OP_PRIMITIVE: {
      return pipe(patch8(prefix, flat.patch), flatMap12((prefix2) => pipe(flat.load(prefix2, op, split6), flatMap12((values10) => {
        if (values10.length === 0) {
          const name = pipe(last(prefix2), getOrElse2(() => "<n/a>"));
          return fail2(MissingData([], `Expected ${op.description} with name ${name}`));
        }
        return succeed(values10);
      }))));
    }
    case OP_SEQUENCE: {
      return pipe(patch8(prefix, flat.patch), flatMap12((patchedPrefix) => pipe(flat.enumerateChildren(patchedPrefix), flatMap12(indicesFrom), flatMap12((indices) => {
        if (indices.length === 0) {
          return suspend(() => map14(fromFlatLoop(flat, prefix, op.config, true), of2));
        }
        return pipe(forEachSequential(indices, (index) => fromFlatLoop(flat, append2(prefix, `[${index}]`), op.config, true)), map14((chunkChunk) => {
          const flattened = flatten3(chunkChunk);
          if (flattened.length === 0) {
            return of2(empty4());
          }
          return of2(flattened);
        }));
      }))));
    }
    case OP_HASHMAP: {
      return suspend(() => pipe(patch8(prefix, flat.patch), flatMap12((prefix2) => pipe(flat.enumerateChildren(prefix2), flatMap12((keys7) => {
        return pipe(keys7, forEachSequential((key) => fromFlatLoop(flat, concat(prefix2, of2(key)), op.valueConfig, split6)), map14((matrix) => {
          if (matrix.length === 0) {
            return of2(empty11());
          }
          return pipe(transpose(matrix), map6((values10) => fromIterable7(zip2(fromIterable2(keys7), values10))));
        }));
      })))));
    }
    case OP_ZIP_WITH: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.left, split6), either3, flatMap12((left3) => pipe(fromFlatLoop(flat, prefix, op.right, split6), either3, flatMap12((right3) => {
        if (isLeft2(left3) && isLeft2(right3)) {
          return fail2(And(left3.left, right3.left));
        }
        if (isLeft2(left3) && isRight2(right3)) {
          return fail2(left3.left);
        }
        if (isRight2(left3) && isLeft2(right3)) {
          return fail2(right3.left);
        }
        if (isRight2(left3) && isRight2(right3)) {
          const path = pipe(prefix, join("."));
          const fail22 = fromFlatLoopFail(prefix, path);
          const [lefts, rights] = extend2(fail22, fail22, pipe(left3.right, map6(right2)), pipe(right3.right, map6(right2)));
          return pipe(lefts, zip2(rights), forEachSequential(([left4, right4]) => pipe(zip5(left4, right4), map14(([left5, right5]) => op.zip(left5, right5)))));
        }
        throw new Error("BUG: ConfigProvider.fromFlatLoop - please report an issue at https://github.com/Effect-TS/effect/issues");
      })))));
    }
  }
}, "fromFlatLoop");
var fromFlatLoopFail = /* @__PURE__ */ __name((prefix, path) => (index) => left2(MissingData(prefix, `The element at index ${index} in a sequence at path "${path}" was missing`)), "fromFlatLoopFail");
var mapInputPath = /* @__PURE__ */ dual(2, (self, f) => fromFlat(mapInputPathFlat(self.flattened, f)));
var mapInputPathFlat = /* @__PURE__ */ __name((self, f) => makeFlat({
  load: /* @__PURE__ */ __name((path, config2, split6 = true) => self.load(path, config2, split6), "load"),
  enumerateChildren: /* @__PURE__ */ __name((path) => self.enumerateChildren(path), "enumerateChildren"),
  patch: mapName(self.patch, f)
}), "mapInputPathFlat");
var nested2 = /* @__PURE__ */ dual(2, (self, name) => fromFlat(makeFlat({
  load: /* @__PURE__ */ __name((path, config2) => self.flattened.load(path, config2, true), "load"),
  enumerateChildren: /* @__PURE__ */ __name((path) => self.flattened.enumerateChildren(path), "enumerateChildren"),
  patch: nested(self.flattened.patch, name)
})));
var unnested2 = /* @__PURE__ */ dual(2, (self, name) => fromFlat(makeFlat({
  load: /* @__PURE__ */ __name((path, config2) => self.flattened.load(path, config2, true), "load"),
  enumerateChildren: /* @__PURE__ */ __name((path) => self.flattened.enumerateChildren(path), "enumerateChildren"),
  patch: unnested(self.flattened.patch, name)
})));
var orElse4 = /* @__PURE__ */ dual(2, (self, that) => fromFlat(orElseFlat(self.flattened, () => that().flattened)));
var orElseFlat = /* @__PURE__ */ __name((self, that) => makeFlat({
  load: /* @__PURE__ */ __name((path, config2, split6) => pipe(patch8(path, self.patch), flatMap12((patch13) => self.load(patch13, config2, split6)), catchAll((error1) => pipe(sync(that), flatMap12((that2) => pipe(patch8(path, that2.patch), flatMap12((patch13) => that2.load(patch13, config2, split6)), catchAll((error2) => fail2(Or(error1, error2)))))))), "load"),
  enumerateChildren: /* @__PURE__ */ __name((path) => pipe(patch8(path, self.patch), flatMap12((patch13) => self.enumerateChildren(patch13)), either3, flatMap12((left3) => pipe(sync(that), flatMap12((that2) => pipe(patch8(path, that2.patch), flatMap12((patch13) => that2.enumerateChildren(patch13)), either3, flatMap12((right3) => {
    if (isLeft2(left3) && isLeft2(right3)) {
      return fail2(And(left3.left, right3.left));
    }
    if (isLeft2(left3) && isRight2(right3)) {
      return succeed(right3.right);
    }
    if (isRight2(left3) && isLeft2(right3)) {
      return succeed(left3.right);
    }
    if (isRight2(left3) && isRight2(right3)) {
      return succeed(pipe(left3.right, union6(right3.right)));
    }
    throw new Error("BUG: ConfigProvider.orElseFlat - please report an issue at https://github.com/Effect-TS/effect/issues");
  })))))), "enumerateChildren"),
  patch: empty25
}), "orElseFlat");
var constantCase2 = /* @__PURE__ */ __name((self) => mapInputPath(self, constantCase), "constantCase");
var kebabCase2 = /* @__PURE__ */ __name((self) => mapInputPath(self, kebabCase), "kebabCase");
var lowerCase2 = /* @__PURE__ */ __name((self) => mapInputPath(self, lowerCase), "lowerCase");
var snakeCase2 = /* @__PURE__ */ __name((self) => mapInputPath(self, snakeCase), "snakeCase");
var upperCase2 = /* @__PURE__ */ __name((self) => mapInputPath(self, upperCase), "upperCase");
var within = /* @__PURE__ */ dual(3, (self, path, f) => {
  const unnest = reduce3(path, self, (provider, name) => unnested2(provider, name));
  const nest = reduceRight(path, f(unnest), (provider, name) => nested2(provider, name));
  return orElse4(nest, () => self);
});
var splitPathString = /* @__PURE__ */ __name((text, delim) => {
  const split6 = text.split(new RegExp(`\\s*${escape(delim)}\\s*`));
  return split6;
}, "splitPathString");
var parsePrimitive = /* @__PURE__ */ __name((text, path, primitive, delimiter, split6) => {
  if (!split6) {
    return pipe(primitive.parse(text), mapBoth3({
      onFailure: prefixed(path),
      onSuccess: of2
    }));
  }
  return pipe(splitPathString(text, delimiter), forEachSequential((char) => primitive.parse(char.trim())), mapError(prefixed(path)));
}, "parsePrimitive");
var transpose = /* @__PURE__ */ __name((array5) => {
  return Object.keys(array5[0]).map((column) => array5.map((row) => row[column]));
}, "transpose");
var indicesFrom = /* @__PURE__ */ __name((quotedIndices) => pipe(forEachSequential(quotedIndices, parseQuotedIndex), mapBoth3({
  onFailure: /* @__PURE__ */ __name(() => empty4(), "onFailure"),
  onSuccess: sort(Order)
}), either3, map14(merge)), "indicesFrom");
var STR_INDEX_REGEX = /(^.+)(\[(\d+)\])$/;
var QUOTED_INDEX_REGEX = /^(\[(\d+)\])$/;
var parseQuotedIndex = /* @__PURE__ */ __name((str) => {
  const match33 = str.match(QUOTED_INDEX_REGEX);
  if (match33 !== null) {
    const matchedIndex = match33[2];
    return pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some3(matchedIndex) : none2(), flatMap2(parseInteger));
  }
  return none2();
}, "parseQuotedIndex");
var splitIndexInKeys = /* @__PURE__ */ __name((map36, unmakePathString, makePathString) => {
  const newMap = /* @__PURE__ */ new Map();
  for (const [pathString, value2] of map36) {
    const keyWithIndex = pipe(unmakePathString(pathString), flatMap4((key) => match2(splitIndexFrom(key), {
      onNone: /* @__PURE__ */ __name(() => of2(key), "onNone"),
      onSome: /* @__PURE__ */ __name(([key2, index]) => make4(key2, `[${index}]`), "onSome")
    })));
    newMap.set(makePathString(keyWithIndex), value2);
  }
  return newMap;
}, "splitIndexInKeys");
var splitIndexFrom = /* @__PURE__ */ __name((key) => {
  const match33 = key.match(STR_INDEX_REGEX);
  if (match33 !== null) {
    const matchedString = match33[1];
    const matchedIndex = match33[3];
    const optionalString = matchedString !== void 0 && matchedString.length > 0 ? some3(matchedString) : none2();
    const optionalIndex = pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some3(matchedIndex) : none2(), flatMap2(parseInteger));
    return all5([optionalString, optionalIndex]);
  }
  return none2();
}, "splitIndexFrom");
var parseInteger = /* @__PURE__ */ __name((str) => {
  const parsedIndex = Number.parseInt(str);
  return Number.isNaN(parsedIndex) ? none2() : some3(parsedIndex);
}, "parseInteger");
var keyName = /* @__PURE__ */ __name((name) => ({
  _tag: "KeyName",
  name
}), "keyName");
var keyIndex = /* @__PURE__ */ __name((index) => ({
  _tag: "KeyIndex",
  index
}), "keyIndex");
var fromJson = /* @__PURE__ */ __name((json) => {
  const hiddenDelimiter = "\uFEFF";
  const indexedEntries = map6(getIndexedEntries(json), ([key, value2]) => [configPathToString(key).join(hiddenDelimiter), value2]);
  return fromMap(new Map(indexedEntries), {
    pathDelim: hiddenDelimiter,
    seqDelim: hiddenDelimiter
  });
}, "fromJson");
var configPathToString = /* @__PURE__ */ __name((path) => {
  const output = [];
  let i = 0;
  while (i < path.length) {
    const component = path[i];
    if (component._tag === "KeyName") {
      if (i + 1 < path.length) {
        const nextComponent = path[i + 1];
        if (nextComponent._tag === "KeyIndex") {
          output.push(`${component.name}[${nextComponent.index}]`);
          i += 2;
        } else {
          output.push(component.name);
          i += 1;
        }
      } else {
        output.push(component.name);
        i += 1;
      }
    }
  }
  return output;
}, "configPathToString");
var getIndexedEntries = /* @__PURE__ */ __name((config2) => {
  const loopAny = /* @__PURE__ */ __name((path, value2) => {
    if (typeof value2 === "string") {
      return make4([path, value2]);
    }
    if (typeof value2 === "number" || typeof value2 === "boolean") {
      return make4([path, String(value2)]);
    }
    if (isArray(value2)) {
      return loopArray(path, value2);
    }
    if (typeof value2 === "object" && value2 !== null) {
      return loopObject(path, value2);
    }
    return empty4();
  }, "loopAny");
  const loopArray = /* @__PURE__ */ __name((path, values10) => match3(values10, {
    onEmpty: /* @__PURE__ */ __name(() => make4([path, "<nil>"]), "onEmpty"),
    onNonEmpty: flatMap4((value2, index) => loopAny(append2(path, keyIndex(index)), value2))
  }), "loopArray");
  const loopObject = /* @__PURE__ */ __name((path, value2) => Object.entries(value2).filter(([, value3]) => isNotNullable(value3)).flatMap(([key, value3]) => {
    const newPath = append2(path, keyName(key));
    const result = loopAny(newPath, value3);
    if (isEmptyReadonlyArray(result)) {
      return make4([newPath, ""]);
    }
    return result;
  }), "loopObject");
  return loopObject(empty4(), config2);
}, "getIndexedEntries");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/defaultServices/console.js
var TypeId16 = /* @__PURE__ */ Symbol.for("effect/Console");
var consoleTag = /* @__PURE__ */ GenericTag("effect/Console");
var defaultConsole = {
  [TypeId16]: TypeId16,
  assert(condition, ...args2) {
    return sync(() => {
      console.assert(condition, ...args2);
    });
  },
  clear: /* @__PURE__ */ sync(() => {
    console.clear();
  }),
  count(label) {
    return sync(() => {
      console.count(label);
    });
  },
  countReset(label) {
    return sync(() => {
      console.countReset(label);
    });
  },
  debug(...args2) {
    return sync(() => {
      console.debug(...args2);
    });
  },
  dir(item, options) {
    return sync(() => {
      console.dir(item, options);
    });
  },
  dirxml(...args2) {
    return sync(() => {
      console.dirxml(...args2);
    });
  },
  error(...args2) {
    return sync(() => {
      console.error(...args2);
    });
  },
  group(options) {
    return options?.collapsed ? sync(() => console.groupCollapsed(options?.label)) : sync(() => console.group(options?.label));
  },
  groupEnd: /* @__PURE__ */ sync(() => {
    console.groupEnd();
  }),
  info(...args2) {
    return sync(() => {
      console.info(...args2);
    });
  },
  log(...args2) {
    return sync(() => {
      console.log(...args2);
    });
  },
  table(tabularData, properties) {
    return sync(() => {
      console.table(tabularData, properties);
    });
  },
  time(label) {
    return sync(() => console.time(label));
  },
  timeEnd(label) {
    return sync(() => console.timeEnd(label));
  },
  timeLog(label, ...args2) {
    return sync(() => {
      console.timeLog(label, ...args2);
    });
  },
  trace(...args2) {
    return sync(() => {
      console.trace(...args2);
    });
  },
  warn(...args2) {
    return sync(() => {
      console.warn(...args2);
    });
  },
  unsafe: console
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/random.js
var RandomSymbolKey = "effect/Random";
var RandomTypeId = /* @__PURE__ */ Symbol.for(RandomSymbolKey);
var randomTag = /* @__PURE__ */ GenericTag("effect/Random");
var RandomImpl = class {
  static {
    __name(this, "RandomImpl");
  }
  seed;
  [RandomTypeId] = RandomTypeId;
  PRNG;
  constructor(seed) {
    this.seed = seed;
    this.PRNG = new PCGRandom(seed);
  }
  get next() {
    return sync(() => this.PRNG.number());
  }
  get nextBoolean() {
    return map14(this.next, (n) => n > 0.5);
  }
  get nextInt() {
    return sync(() => this.PRNG.integer(Number.MAX_SAFE_INTEGER));
  }
  nextRange(min8, max10) {
    return map14(this.next, (n) => (max10 - min8) * n + min8);
  }
  nextIntBetween(min8, max10) {
    return sync(() => this.PRNG.integer(max10 - min8) + min8);
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var shuffleWith = /* @__PURE__ */ __name((elements, nextIntBounded) => {
  return suspend(() => pipe(sync(() => Array.from(elements)), flatMap12((buffer4) => {
    const numbers = [];
    for (let i = buffer4.length; i >= 2; i = i - 1) {
      numbers.push(i);
    }
    return pipe(numbers, forEachSequentialDiscard((n) => pipe(nextIntBounded(n), map14((k) => swap2(buffer4, n - 1, k)))), as3(fromIterable3(buffer4)));
  })));
}, "shuffleWith");
var swap2 = /* @__PURE__ */ __name((buffer4, index1, index2) => {
  const tmp = buffer4[index1];
  buffer4[index1] = buffer4[index2];
  buffer4[index2] = tmp;
  return buffer4;
}, "swap");
var make28 = /* @__PURE__ */ __name((seed) => new RandomImpl(hash(seed)), "make");
var FixedRandomImpl = class {
  static {
    __name(this, "FixedRandomImpl");
  }
  values;
  [RandomTypeId] = RandomTypeId;
  index = 0;
  constructor(values10) {
    this.values = values10;
    if (values10.length === 0) {
      throw new Error("Requires at least one value");
    }
  }
  getNextValue() {
    const value2 = this.values[this.index];
    this.index = (this.index + 1) % this.values.length;
    return value2;
  }
  get next() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number") {
        return Math.max(0, Math.min(1, value2));
      }
      return hash(value2) / 2147483647;
    });
  }
  get nextBoolean() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "boolean") {
        return value2;
      }
      return hash(value2) % 2 === 0;
    });
  }
  get nextInt() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.round(value2);
      }
      return Math.abs(hash(value2));
    });
  }
  nextRange(min8, max10) {
    return map14(this.next, (n) => (max10 - min8) * n + min8);
  }
  nextIntBetween(min8, max10) {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.max(min8, Math.min(max10 - 1, Math.round(value2)));
      }
      const hash2 = Math.abs(hash(value2));
      return min8 + hash2 % (max10 - min8);
    });
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var fixed = /* @__PURE__ */ __name((values10) => new FixedRandomImpl(values10), "fixed");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/tracer.js
var TracerTypeId = /* @__PURE__ */ Symbol.for("effect/Tracer");
var make29 = /* @__PURE__ */ __name((options) => ({
  [TracerTypeId]: TracerTypeId,
  ...options
}), "make");
var tracerTag = /* @__PURE__ */ GenericTag("effect/Tracer");
var spanTag = /* @__PURE__ */ GenericTag("effect/ParentSpan");
var randomHexString = /* @__PURE__ */ (function() {
  const characters = "abcdef0123456789";
  const charactersLength = characters.length;
  return function(length5) {
    let result = "";
    for (let i = 0; i < length5; i++) {
      result += characters.charAt(Math.floor(Math.random() * charactersLength));
    }
    return result;
  };
})();
var NativeSpan = class {
  static {
    __name(this, "NativeSpan");
  }
  name;
  parent;
  context;
  startTime;
  kind;
  _tag = "Span";
  spanId;
  traceId = "native";
  sampled = true;
  status;
  attributes;
  events = [];
  links;
  constructor(name, parent, context11, links, startTime, kind) {
    this.name = name;
    this.parent = parent;
    this.context = context11;
    this.startTime = startTime;
    this.kind = kind;
    this.status = {
      _tag: "Started",
      startTime
    };
    this.attributes = /* @__PURE__ */ new Map();
    this.traceId = parent._tag === "Some" ? parent.value.traceId : randomHexString(32);
    this.spanId = randomHexString(16);
    this.links = Array.from(links);
  }
  end(endTime, exit4) {
    this.status = {
      _tag: "Ended",
      endTime,
      exit: exit4,
      startTime: this.status.startTime
    };
  }
  attribute(key, value2) {
    this.attributes.set(key, value2);
  }
  event(name, startTime, attributes) {
    this.events.push([name, startTime, attributes ?? {}]);
  }
  addLinks(links) {
    this.links.push(...links);
  }
};
var nativeTracer = /* @__PURE__ */ make29({
  span: /* @__PURE__ */ __name((name, parent, context11, links, startTime, kind) => new NativeSpan(name, parent, context11, links, startTime, kind), "span"),
  context: /* @__PURE__ */ __name((f) => f(), "context")
});
var externalSpan = /* @__PURE__ */ __name((options) => ({
  _tag: "ExternalSpan",
  spanId: options.spanId,
  traceId: options.traceId,
  sampled: options.sampled ?? true,
  context: options.context ?? empty6()
}), "externalSpan");
var addSpanStackTrace = /* @__PURE__ */ __name((options) => {
  if (options?.captureStackTrace === false) {
    return options;
  } else if (options?.captureStackTrace !== void 0 && typeof options.captureStackTrace !== "boolean") {
    return options;
  }
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 3;
  const traceError = new Error();
  Error.stackTraceLimit = limit;
  let cache = false;
  return {
    ...options,
    captureStackTrace: /* @__PURE__ */ __name(() => {
      if (cache !== false) {
        return cache;
      }
      if (traceError.stack !== void 0) {
        const stack = traceError.stack.split("\n");
        if (stack[3] !== void 0) {
          cache = stack[3].trim();
          return cache;
        }
      }
    }, "captureStackTrace")
  };
}, "addSpanStackTrace");
var DisablePropagation = /* @__PURE__ */ Reference2()("effect/Tracer/DisablePropagation", {
  defaultValue: constFalse
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/defaultServices.js
var liveServices = /* @__PURE__ */ pipe(/* @__PURE__ */ empty6(), /* @__PURE__ */ add2(clockTag, /* @__PURE__ */ make26()), /* @__PURE__ */ add2(consoleTag, defaultConsole), /* @__PURE__ */ add2(randomTag, /* @__PURE__ */ make28(/* @__PURE__ */ Math.random())), /* @__PURE__ */ add2(configProviderTag, /* @__PURE__ */ fromEnv()), /* @__PURE__ */ add2(tracerTag, nativeTracer));
var currentServices = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/DefaultServices/currentServices"), () => fiberRefUnsafeMakeContext(liveServices));
var sleep = /* @__PURE__ */ __name((duration3) => {
  const decodedDuration = decode(duration3);
  return clockWith((clock3) => clock3.sleep(decodedDuration));
}, "sleep");
var defaultServicesWith = /* @__PURE__ */ __name((f) => withFiberRuntime((fiber) => f(fiber.currentDefaultServices)), "defaultServicesWith");
var clockWith = /* @__PURE__ */ __name((f) => defaultServicesWith((services) => f(services.unsafeMap.get(clockTag.key))), "clockWith");
var currentTimeMillis = /* @__PURE__ */ clockWith((clock3) => clock3.currentTimeMillis);
var currentTimeNanos = /* @__PURE__ */ clockWith((clock3) => clock3.currentTimeNanos);
var withClock = /* @__PURE__ */ dual(2, (effect4, c) => fiberRefLocallyWith(currentServices, add2(clockTag, c))(effect4));
var withConfigProvider = /* @__PURE__ */ dual(2, (self, provider) => fiberRefLocallyWith(currentServices, add2(configProviderTag, provider))(self));
var configProviderWith = /* @__PURE__ */ __name((f) => defaultServicesWith((services) => f(services.unsafeMap.get(configProviderTag.key))), "configProviderWith");
var config = /* @__PURE__ */ __name((config2) => configProviderWith((_) => _.load(config2)), "config");
var configOrDie = /* @__PURE__ */ __name((config2) => orDie(configProviderWith((_) => _.load(config2))), "configOrDie");
var randomWith = /* @__PURE__ */ __name((f) => defaultServicesWith((services) => f(services.unsafeMap.get(randomTag.key))), "randomWith");
var withRandom = /* @__PURE__ */ dual(2, (effect4, value2) => fiberRefLocallyWith(currentServices, add2(randomTag, value2))(effect4));
var next = /* @__PURE__ */ randomWith((random4) => random4.next);
var nextInt = /* @__PURE__ */ randomWith((random4) => random4.nextInt);
var nextBoolean = /* @__PURE__ */ randomWith((random4) => random4.nextBoolean);
var nextRange = /* @__PURE__ */ __name((min8, max10) => randomWith((random4) => random4.nextRange(min8, max10)), "nextRange");
var nextIntBetween = /* @__PURE__ */ __name((min8, max10) => randomWith((random4) => random4.nextIntBetween(min8, max10)), "nextIntBetween");
var shuffle = /* @__PURE__ */ __name((elements) => randomWith((random4) => random4.shuffle(elements)), "shuffle");
var choice = /* @__PURE__ */ __name((elements) => {
  const array5 = fromIterable2(elements);
  return map14(array5.length === 0 ? fail2(new NoSuchElementException("Cannot select a random element from an empty array")) : randomWith((random4) => random4.nextIntBetween(0, array5.length)), (i) => array5[i]);
}, "choice");
var tracerWith = /* @__PURE__ */ __name((f) => defaultServicesWith((services) => f(services.unsafeMap.get(tracerTag.key))), "tracerWith");
var withTracer = /* @__PURE__ */ dual(2, (effect4, value2) => fiberRefLocallyWith(currentServices, add2(tracerTag, value2))(effect4));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Clock.js
var ClockTypeId2 = ClockTypeId;
var make30 = make26;
var sleep2 = sleep;
var currentTimeMillis2 = currentTimeMillis;
var currentTimeNanos2 = currentTimeNanos;
var clockWith2 = clockWith;
var Clock = clockTag;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberRefs.js
function unsafeMake5(fiberRefLocals) {
  return new FiberRefsImpl(fiberRefLocals);
}
__name(unsafeMake5, "unsafeMake");
function empty26() {
  return unsafeMake5(/* @__PURE__ */ new Map());
}
__name(empty26, "empty");
var FiberRefsSym = /* @__PURE__ */ Symbol.for("effect/FiberRefs");
var FiberRefsImpl = class {
  static {
    __name(this, "FiberRefsImpl");
  }
  locals;
  [FiberRefsSym] = FiberRefsSym;
  constructor(locals) {
    this.locals = locals;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var findAncestor = /* @__PURE__ */ __name((_ref, _parentStack, _childStack, _childModified = false) => {
  const ref = _ref;
  let parentStack = _parentStack;
  let childStack = _childStack;
  let childModified = _childModified;
  let ret = void 0;
  while (ret === void 0) {
    if (isNonEmptyReadonlyArray(parentStack) && isNonEmptyReadonlyArray(childStack)) {
      const parentFiberId = headNonEmpty(parentStack)[0];
      const parentAncestors = tailNonEmpty(parentStack);
      const childFiberId = headNonEmpty(childStack)[0];
      const childRefValue = headNonEmpty(childStack)[1];
      const childAncestors = tailNonEmpty(childStack);
      if (parentFiberId.startTimeMillis < childFiberId.startTimeMillis) {
        childStack = childAncestors;
        childModified = true;
      } else if (parentFiberId.startTimeMillis > childFiberId.startTimeMillis) {
        parentStack = parentAncestors;
      } else {
        if (parentFiberId.id < childFiberId.id) {
          childStack = childAncestors;
          childModified = true;
        } else if (parentFiberId.id > childFiberId.id) {
          parentStack = parentAncestors;
        } else {
          ret = [childRefValue, childModified];
        }
      }
    } else {
      ret = [ref.initial, true];
    }
  }
  return ret;
}, "findAncestor");
var joinAs = /* @__PURE__ */ dual(3, (self, fiberId4, that) => {
  const parentFiberRefs = new Map(self.locals);
  that.locals.forEach((childStack, fiberRef) => {
    const childValue = childStack[0][1];
    if (!childStack[0][0][symbol3](fiberId4)) {
      if (!parentFiberRefs.has(fiberRef)) {
        if (equals(childValue, fiberRef.initial)) {
          return;
        }
        parentFiberRefs.set(fiberRef, [[fiberId4, fiberRef.join(fiberRef.initial, childValue)]]);
        return;
      }
      const parentStack = parentFiberRefs.get(fiberRef);
      const [ancestor, wasModified] = findAncestor(fiberRef, parentStack, childStack);
      if (wasModified) {
        const patch13 = fiberRef.diff(ancestor, childValue);
        const oldValue = parentStack[0][1];
        const newValue = fiberRef.join(oldValue, fiberRef.patch(patch13)(oldValue));
        if (!equals(oldValue, newValue)) {
          let newStack;
          const parentFiberId = parentStack[0][0];
          if (parentFiberId[symbol3](fiberId4)) {
            newStack = [[parentFiberId, newValue], ...parentStack.slice(1)];
          } else {
            newStack = [[fiberId4, newValue], ...parentStack];
          }
          parentFiberRefs.set(fiberRef, newStack);
        }
      }
    }
  });
  return new FiberRefsImpl(parentFiberRefs);
});
var forkAs = /* @__PURE__ */ dual(2, (self, childId) => {
  const map36 = /* @__PURE__ */ new Map();
  unsafeForkAs(self, map36, childId);
  return new FiberRefsImpl(map36);
});
var unsafeForkAs = /* @__PURE__ */ __name((self, map36, fiberId4) => {
  self.locals.forEach((stack, fiberRef) => {
    const oldValue = stack[0][1];
    const newValue = fiberRef.patch(fiberRef.fork)(oldValue);
    if (equals(oldValue, newValue)) {
      map36.set(fiberRef, stack);
    } else {
      map36.set(fiberRef, [[fiberId4, newValue], ...stack]);
    }
  });
}, "unsafeForkAs");
var fiberRefs = /* @__PURE__ */ __name((self) => fromIterable6(self.locals.keys()), "fiberRefs");
var setAll = /* @__PURE__ */ __name((self) => forEachSequentialDiscard(fiberRefs(self), (fiberRef) => fiberRefSet(fiberRef, getOrDefault(self, fiberRef))), "setAll");
var delete_ = /* @__PURE__ */ dual(2, (self, fiberRef) => {
  const locals = new Map(self.locals);
  locals.delete(fiberRef);
  return new FiberRefsImpl(locals);
});
var get10 = /* @__PURE__ */ dual(2, (self, fiberRef) => {
  if (!self.locals.has(fiberRef)) {
    return none2();
  }
  return some3(headNonEmpty(self.locals.get(fiberRef))[1]);
});
var getOrDefault = /* @__PURE__ */ dual(2, (self, fiberRef) => pipe(get10(self, fiberRef), getOrElse2(() => fiberRef.initial)));
var updateAs = /* @__PURE__ */ dual(2, (self, {
  fiberId: fiberId4,
  fiberRef,
  value: value2
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(/* @__PURE__ */ new Map([[fiberRef, [[fiberId4, value2]]]]));
  }
  const locals = new Map(self.locals);
  unsafeUpdateAs(locals, fiberId4, fiberRef, value2);
  return new FiberRefsImpl(locals);
});
var unsafeUpdateAs = /* @__PURE__ */ __name((locals, fiberId4, fiberRef, value2) => {
  const oldStack = locals.get(fiberRef) ?? [];
  let newStack;
  if (isNonEmptyReadonlyArray(oldStack)) {
    const [currentId, currentValue] = headNonEmpty(oldStack);
    if (currentId[symbol3](fiberId4)) {
      if (equals(currentValue, value2)) {
        return;
      } else {
        newStack = [[fiberId4, value2], ...oldStack.slice(1)];
      }
    } else {
      newStack = [[fiberId4, value2], ...oldStack];
    }
  } else {
    newStack = [[fiberId4, value2]];
  }
  locals.set(fiberRef, newStack);
}, "unsafeUpdateAs");
var updateManyAs = /* @__PURE__ */ dual(2, (self, {
  entries: entries3,
  forkAs: forkAs3
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(new Map(entries3));
  }
  const locals = new Map(self.locals);
  if (forkAs3 !== void 0) {
    unsafeForkAs(self, locals, forkAs3);
  }
  entries3.forEach(([fiberRef, values10]) => {
    if (values10.length === 1) {
      unsafeUpdateAs(locals, values10[0][0], fiberRef, values10[0][1]);
    } else {
      values10.forEach(([fiberId4, value2]) => {
        unsafeUpdateAs(locals, fiberId4, fiberRef, value2);
      });
    }
  });
  return new FiberRefsImpl(locals);
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/FiberRefs.js
var FiberRefsSym2 = FiberRefsSym;
var delete_2 = delete_;
var fiberRefs2 = fiberRefs;
var forkAs2 = forkAs;
var get11 = get10;
var getOrDefault2 = getOrDefault;
var joinAs2 = joinAs;
var setAll2 = setAll;
var updateAs2 = updateAs;
var updateManyAs2 = updateManyAs;
var unsafeMake6 = unsafeMake5;
var empty27 = empty26;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/LogLevel.js
var All = logLevelAll;
var Fatal = logLevelFatal;
var Error2 = logLevelError;
var Warning = logLevelWarning;
var Info = logLevelInfo;
var Debug = logLevelDebug;
var Trace = logLevelTrace;
var None3 = logLevelNone;
var allLevels = allLogLevels;
var locally = /* @__PURE__ */ dual(2, (use2, self) => fiberRefLocally(use2, currentLogLevel, self));
var Order4 = /* @__PURE__ */ pipe(Order, /* @__PURE__ */ mapInput3((level) => level.ordinal));
var lessThan4 = /* @__PURE__ */ lessThan(Order4);
var lessThanEqual = /* @__PURE__ */ lessThanOrEqualTo(Order4);
var greaterThan4 = /* @__PURE__ */ greaterThan(Order4);
var greaterThanEqual = /* @__PURE__ */ greaterThanOrEqualTo(Order4);
var fromLiteral = /* @__PURE__ */ __name((literal) => {
  switch (literal) {
    case "All":
      return All;
    case "Debug":
      return Debug;
    case "Error":
      return Error2;
    case "Fatal":
      return Fatal;
    case "Info":
      return Info;
    case "Trace":
      return Trace;
    case "None":
      return None3;
    case "Warning":
      return Warning;
  }
}, "fromLiteral");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/logSpan.js
var make31 = /* @__PURE__ */ __name((label, startTime) => ({
  label,
  startTime
}), "make");
var formatLabel = /* @__PURE__ */ __name((key) => key.replace(/[\s="]/g, "_"), "formatLabel");
var render3 = /* @__PURE__ */ __name((now2) => (self) => {
  const label = formatLabel(self.label);
  return `${label}=${now2 - self.startTime}ms`;
}, "render");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/LogSpan.js
var make32 = make31;
var render4 = render3;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Effectable.js
var EffectTypeId3 = EffectTypeId;
var StreamTypeId2 = StreamTypeId;
var SinkTypeId2 = SinkTypeId;
var ChannelTypeId2 = ChannelTypeId;
var EffectPrototype2 = EffectPrototype;
var CommitPrototype2 = CommitPrototype;
var StructuralCommitPrototype2 = StructuralCommitPrototype;
var Base3 = Base2;
var StructuralBase2 = StructuralBase;
var Class3 = class extends Base3 {
  static {
    __name(this, "Class");
  }
};
var StructuralClass = class extends StructuralBase2 {
  static {
    __name(this, "StructuralClass");
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Readable.js
var TypeId17 = /* @__PURE__ */ Symbol.for("effect/Readable");
var isReadable = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId17), "isReadable");
var Proto = {
  [TypeId17]: TypeId17,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make33 = /* @__PURE__ */ __name((get21) => {
  const self = Object.create(Proto);
  self.get = get21;
  return self;
}, "make");
var map16 = /* @__PURE__ */ dual(2, (self, f) => make33(map14(self.get, f)));
var mapEffect = /* @__PURE__ */ dual(2, (self, f) => make33(flatMap12(self.get, f)));
var unwrap = /* @__PURE__ */ __name((effect4) => make33(flatMap12(effect4, (s) => s.get)), "unwrap");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/ref.js
var RefTypeId = /* @__PURE__ */ Symbol.for("effect/Ref");
var refVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var RefImpl = class extends Class3 {
  static {
    __name(this, "RefImpl");
  }
  ref;
  commit() {
    return this.get;
  }
  [RefTypeId] = refVariance;
  [TypeId17] = TypeId17;
  constructor(ref) {
    super();
    this.ref = ref;
    this.get = sync(() => get7(this.ref));
  }
  get;
  modify(f) {
    return sync(() => {
      const current2 = get7(this.ref);
      const [b, a] = f(current2);
      if (current2 !== a) {
        set3(a)(this.ref);
      }
      return b;
    });
  }
};
var unsafeMake7 = /* @__PURE__ */ __name((value2) => new RefImpl(make13(value2)), "unsafeMake");
var make34 = /* @__PURE__ */ __name((value2) => sync(() => unsafeMake7(value2)), "make");
var get12 = /* @__PURE__ */ __name((self) => self.get, "get");
var set6 = /* @__PURE__ */ dual(2, (self, value2) => self.modify(() => [void 0, value2]));
var getAndSet2 = /* @__PURE__ */ dual(2, (self, value2) => self.modify((a) => [a, value2]));
var getAndUpdate2 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome = /* @__PURE__ */ dual(2, (self, pf) => self.modify((value2) => {
  const option5 = pf(value2);
  switch (option5._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [value2, option5.value];
    }
  }
}));
var setAndGet2 = /* @__PURE__ */ dual(2, (self, value2) => self.modify(() => [value2, value2]));
var modify7 = /* @__PURE__ */ dual(2, (self, f) => self.modify(f));
var modifySome = /* @__PURE__ */ dual(3, (self, fallback, pf) => self.modify((value2) => {
  const option5 = pf(value2);
  switch (option5._tag) {
    case "None": {
      return [fallback, value2];
    }
    case "Some": {
      return option5.value;
    }
  }
}));
var update3 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet2 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => {
  const result = f(a);
  return [result, result];
}));
var updateSome = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [void 0, match2(f(a), {
  onNone: /* @__PURE__ */ __name(() => a, "onNone"),
  onSome: /* @__PURE__ */ __name((b) => b, "onSome")
})]));
var updateSomeAndGet = /* @__PURE__ */ dual(2, (self, pf) => self.modify((value2) => {
  const option5 = pf(value2);
  switch (option5._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [option5.value, option5.value];
    }
  }
}));
var unsafeGet7 = /* @__PURE__ */ __name((self) => get7(self.ref), "unsafeGet");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Ref.js
var RefTypeId2 = RefTypeId;
var make35 = make34;
var get13 = get12;
var getAndSet3 = getAndSet2;
var getAndUpdate3 = getAndUpdate2;
var getAndUpdateSome2 = getAndUpdateSome;
var modify8 = modify7;
var modifySome2 = modifySome;
var set7 = set6;
var setAndGet3 = setAndGet2;
var update4 = update3;
var updateAndGet3 = updateAndGet2;
var updateSome2 = updateSome;
var updateSomeAndGet2 = updateSomeAndGet;
var unsafeMake8 = unsafeMake7;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Tracer.js
var TracerTypeId2 = TracerTypeId;
var ParentSpan = spanTag;
var Tracer = tracerTag;
var make36 = make29;
var externalSpan2 = externalSpan;
var tracerWith2 = tracerWith;
var DisablePropagation2 = DisablePropagation;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberRefs/patch.js
var OP_EMPTY2 = "Empty";
var OP_ADD = "Add";
var OP_REMOVE = "Remove";
var OP_UPDATE = "Update";
var OP_AND_THEN = "AndThen";
var empty28 = {
  _tag: OP_EMPTY2
};
var diff8 = /* @__PURE__ */ __name((oldValue, newValue) => {
  const missingLocals = new Map(oldValue.locals);
  let patch13 = empty28;
  for (const [fiberRef, pairs] of newValue.locals.entries()) {
    const newValue2 = headNonEmpty(pairs)[1];
    const old = missingLocals.get(fiberRef);
    if (old !== void 0) {
      const oldValue2 = headNonEmpty(old)[1];
      if (!equals(oldValue2, newValue2)) {
        patch13 = combine12({
          _tag: OP_UPDATE,
          fiberRef,
          patch: fiberRef.diff(oldValue2, newValue2)
        })(patch13);
      }
    } else {
      patch13 = combine12({
        _tag: OP_ADD,
        fiberRef,
        value: newValue2
      })(patch13);
    }
    missingLocals.delete(fiberRef);
  }
  for (const [fiberRef] of missingLocals.entries()) {
    patch13 = combine12({
      _tag: OP_REMOVE,
      fiberRef
    })(patch13);
  }
  return patch13;
}, "diff");
var combine12 = /* @__PURE__ */ dual(2, (self, that) => ({
  _tag: OP_AND_THEN,
  first: self,
  second: that
}));
var patch9 = /* @__PURE__ */ dual(3, (self, fiberId4, oldValue) => {
  let fiberRefs4 = oldValue;
  let patches = of2(self);
  while (isNonEmptyReadonlyArray(patches)) {
    const head10 = headNonEmpty(patches);
    const tail5 = tailNonEmpty(patches);
    switch (head10._tag) {
      case OP_EMPTY2: {
        patches = tail5;
        break;
      }
      case OP_ADD: {
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId4,
          fiberRef: head10.fiberRef,
          value: head10.value
        });
        patches = tail5;
        break;
      }
      case OP_REMOVE: {
        fiberRefs4 = delete_(fiberRefs4, head10.fiberRef);
        patches = tail5;
        break;
      }
      case OP_UPDATE: {
        const value2 = getOrDefault(fiberRefs4, head10.fiberRef);
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId4,
          fiberRef: head10.fiberRef,
          value: head10.fiberRef.patch(head10.patch)(value2)
        });
        patches = tail5;
        break;
      }
      case OP_AND_THEN: {
        patches = prepend2(head10.first)(prepend2(head10.second)(tail5));
        break;
      }
    }
  }
  return fiberRefs4;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/label.js
var MetricLabelSymbolKey = "effect/MetricLabel";
var MetricLabelTypeId = /* @__PURE__ */ Symbol.for(MetricLabelSymbolKey);
var MetricLabelImpl = class {
  static {
    __name(this, "MetricLabelImpl");
  }
  key;
  value;
  [MetricLabelTypeId] = MetricLabelTypeId;
  _hash;
  constructor(key, value2) {
    this.key = key;
    this.value = value2;
    this._hash = string2(MetricLabelSymbolKey + this.key + this.value);
  }
  [symbol2]() {
    return this._hash;
  }
  [symbol3](that) {
    return isMetricLabel(that) && this.key === that.key && this.value === that.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make37 = /* @__PURE__ */ __name((key, value2) => {
  return new MetricLabelImpl(key, value2);
}, "make");
var isMetricLabel = /* @__PURE__ */ __name((u) => hasProperty(u, MetricLabelTypeId), "isMetricLabel");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/core-effect.js
var annotateLogs = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentLogAnnotations, typeof args2[1] === "string" ? set4(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set4(acc, key, value2), annotations));
});
var asSome = /* @__PURE__ */ __name((self) => map14(self, some3), "asSome");
var asSomeError = /* @__PURE__ */ __name((self) => mapError(self, some3), "asSomeError");
var try_2 = /* @__PURE__ */ __name((arg) => {
  let evaluate3;
  let onFailure = void 0;
  if (typeof arg === "function") {
    evaluate3 = arg;
  } else {
    evaluate3 = arg.try;
    onFailure = arg.catch;
  }
  return suspend(() => {
    try {
      return succeed(internalCall(evaluate3));
    } catch (error2) {
      return fail2(onFailure ? internalCall(() => onFailure(error2)) : new UnknownException(error2, "An unknown error occurred in Effect.try"));
    }
  });
}, "try_");
var _catch = /* @__PURE__ */ dual(3, (self, tag, options) => catchAll(self, (e) => {
  if (hasProperty(e, tag) && e[tag] === options.failure) {
    return options.onFailure(e);
  }
  return fail2(e);
}));
var catchAllDefect = /* @__PURE__ */ dual(2, (self, f) => catchAllCause(self, (cause3) => {
  const option5 = find(cause3, (_) => isDieType(_) ? some3(_) : none2());
  switch (option5._tag) {
    case "None": {
      return failCause(cause3);
    }
    case "Some": {
      return f(option5.value.defect);
    }
  }
}));
var catchSomeCause = /* @__PURE__ */ dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const option5 = f(cause3);
    switch (option5._tag) {
      case "None": {
        return failCause(cause3);
      }
      case "Some": {
        return option5.value;
      }
    }
  }, "onFailure"),
  onSuccess: succeed
}));
var catchSomeDefect = /* @__PURE__ */ dual(2, (self, pf) => catchAllCause(self, (cause3) => {
  const option5 = find(cause3, (_) => isDieType(_) ? some3(_) : none2());
  switch (option5._tag) {
    case "None": {
      return failCause(cause3);
    }
    case "Some": {
      const optionEffect = pf(option5.value.defect);
      return optionEffect._tag === "Some" ? optionEffect.value : failCause(cause3);
    }
  }
}));
var catchTag = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, ...args2) => {
  const f = args2[args2.length - 1];
  let predicate;
  if (args2.length === 2) {
    predicate = isTagged(args2[0]);
  } else {
    predicate = /* @__PURE__ */ __name((e) => {
      const tag = hasProperty(e, "_tag") ? e["_tag"] : void 0;
      if (!tag) return false;
      for (let i = 0; i < args2.length - 1; i++) {
        if (args2[i] === tag) return true;
      }
      return false;
    }, "predicate");
  }
  return catchIf(self, predicate, f);
});
var catchTags = /* @__PURE__ */ dual(2, (self, cases) => {
  let keys7;
  return catchIf(self, (e) => {
    keys7 ??= Object.keys(cases);
    return hasProperty(e, "_tag") && isString(e["_tag"]) && keys7.includes(e["_tag"]);
  }, (e) => cases[e["_tag"]](e));
});
var cause = /* @__PURE__ */ __name((self) => matchCause(self, {
  onFailure: identity,
  onSuccess: /* @__PURE__ */ __name(() => empty22, "onSuccess")
}), "cause");
var clockWith3 = clockWith2;
var clock = /* @__PURE__ */ clockWith3(succeed);
var delay = /* @__PURE__ */ dual(2, (self, duration3) => zipRight2(sleep2(duration3), self));
var descriptorWith = /* @__PURE__ */ __name((f) => withFiberRuntime((state, status3) => f({
  id: state.id(),
  status: status3,
  interruptors: interruptors(state.getFiberRef(currentInterruptedCause))
})), "descriptorWith");
var allowInterrupt = /* @__PURE__ */ descriptorWith((descriptor3) => size6(descriptor3.interruptors) > 0 ? interrupt2 : void_3);
var descriptor = /* @__PURE__ */ descriptorWith(succeed);
var diffFiberRefs = /* @__PURE__ */ __name((self) => summarized(self, fiberRefs3, diff8), "diffFiberRefs");
var diffFiberRefsAndRuntimeFlags = /* @__PURE__ */ __name((self) => summarized(self, zip5(fiberRefs3, runtimeFlags), ([refs, flags], [refsNew, flagsNew]) => [diff8(refs, refsNew), diff7(flags, flagsNew)]), "diffFiberRefsAndRuntimeFlags");
var Do4 = /* @__PURE__ */ succeed({});
var bind5 = /* @__PURE__ */ bind(map14, flatMap12);
var bindTo5 = /* @__PURE__ */ bindTo(map14);
var let_5 = /* @__PURE__ */ let_(map14);
var dropUntil = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next4;
  let dropping9 = succeed(false);
  let i = 0;
  while ((next4 = iterator.next()) && !next4.done) {
    const a = next4.value;
    const index = i++;
    dropping9 = flatMap12(dropping9, (bool) => {
      if (bool) {
        builder.push(a);
        return succeed(true);
      }
      return predicate(a, index);
    });
  }
  return map14(dropping9, () => builder);
}));
var dropWhile3 = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next4;
  let dropping9 = succeed(true);
  let i = 0;
  while ((next4 = iterator.next()) && !next4.done) {
    const a = next4.value;
    const index = i++;
    dropping9 = flatMap12(dropping9, (d) => map14(d ? predicate(a, index) : succeed(false), (b) => {
      if (!b) {
        builder.push(a);
      }
      return b;
    }));
  }
  return map14(dropping9, () => builder);
}));
var contextWith2 = /* @__PURE__ */ __name((f) => map14(context(), f), "contextWith");
var eventually = /* @__PURE__ */ __name((self) => orElse3(self, () => flatMap12(yieldNow(), () => eventually(self))), "eventually");
var filterMap9 = /* @__PURE__ */ dual(2, (elements, pf) => map14(forEachSequential(elements, identity), filterMap4(pf)));
var filterOrDie = /* @__PURE__ */ dual(3, (self, predicate, orDieWith7) => filterOrElse(self, predicate, (a) => dieSync(() => orDieWith7(a))));
var filterOrDieMessage = /* @__PURE__ */ dual(3, (self, predicate, message) => filterOrElse(self, predicate, () => dieMessage(message)));
var filterOrElse = /* @__PURE__ */ dual(3, (self, predicate, orElse15) => flatMap12(self, (a) => predicate(a) ? succeed(a) : orElse15(a)));
var liftPredicate4 = /* @__PURE__ */ dual(3, (self, predicate, orFailWith) => suspend(() => predicate(self) ? succeed(self) : fail2(orFailWith(self))));
var filterOrFail = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, predicate, orFailWith) => filterOrElse(self, predicate, (a) => orFailWith === void 0 ? fail2(new NoSuchElementException()) : failSync(() => orFailWith(a))));
var findFirst8 = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const next4 = iterator.next();
  if (!next4.done) {
    return findLoop(iterator, 0, predicate, next4.value);
  }
  return succeed(none2());
}));
var findLoop = /* @__PURE__ */ __name((iterator, index, f, value2) => flatMap12(f(value2, index), (result) => {
  if (result) {
    return succeed(some3(value2));
  }
  const next4 = iterator.next();
  if (!next4.done) {
    return findLoop(iterator, index + 1, f, next4.value);
  }
  return succeed(none2());
}), "findLoop");
var firstSuccessOf = /* @__PURE__ */ __name((effects) => suspend(() => {
  const list = fromIterable3(effects);
  if (!isNonEmpty(list)) {
    return dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`));
  }
  return pipe(tailNonEmpty2(list), reduce3(headNonEmpty2(list), (left3, right3) => orElse3(left3, () => right3)));
}), "firstSuccessOf");
var flipWith = /* @__PURE__ */ dual(2, (self, f) => flip3(f(flip3(self))));
var match8 = /* @__PURE__ */ dual(2, (self, options) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((e) => succeed(options.onFailure(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed(options.onSuccess(a)), "onSuccess")
}));
var every11 = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => forAllLoop(elements[Symbol.iterator](), 0, predicate)));
var forAllLoop = /* @__PURE__ */ __name((iterator, index, f) => {
  const next4 = iterator.next();
  return next4.done ? succeed(true) : flatMap12(f(next4.value, index), (b) => b ? forAllLoop(iterator, index + 1, f) : succeed(b));
}, "forAllLoop");
var forever = /* @__PURE__ */ __name((self) => {
  const loop4 = flatMap12(flatMap12(self, () => yieldNow()), () => loop4);
  return loop4;
}, "forever");
var fiberRefs3 = /* @__PURE__ */ withFiberRuntime((state) => succeed(state.getFiberRefs()));
var head6 = /* @__PURE__ */ __name((self) => flatMap12(self, (as15) => {
  const iterator = as15[Symbol.iterator]();
  const next4 = iterator.next();
  if (next4.done) {
    return fail2(new NoSuchElementException());
  }
  return succeed(next4.value);
}), "head");
var ignore = /* @__PURE__ */ __name((self) => match8(self, {
  onFailure: constVoid,
  onSuccess: constVoid
}), "ignore");
var ignoreLogged = /* @__PURE__ */ __name((self) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => logDebug(cause3, "An error was silently ignored because it is not anticipated to be useful"), "onFailure"),
  onSuccess: /* @__PURE__ */ __name(() => void_3, "onSuccess")
}), "ignoreLogged");
var inheritFiberRefs = /* @__PURE__ */ __name((childFiberRefs) => updateFiberRefs((parentFiberId, parentFiberRefs) => joinAs2(parentFiberRefs, parentFiberId, childFiberRefs)), "inheritFiberRefs");
var isFailure3 = /* @__PURE__ */ __name((self) => match8(self, {
  onFailure: constTrue,
  onSuccess: constFalse
}), "isFailure");
var isSuccess2 = /* @__PURE__ */ __name((self) => match8(self, {
  onFailure: constFalse,
  onSuccess: constTrue
}), "isSuccess");
var iterate = /* @__PURE__ */ __name((initial, options) => suspend(() => {
  if (options.while(initial)) {
    return flatMap12(options.body(initial), (z2) => iterate(z2, options));
  }
  return succeed(initial);
}), "iterate");
var logWithLevel = /* @__PURE__ */ __name((level) => (...message) => {
  const levelOption = fromNullable2(level);
  let cause3 = void 0;
  for (let i = 0, len = message.length; i < len; i++) {
    const msg = message[i];
    if (isCause(msg)) {
      if (cause3 !== void 0) {
        cause3 = sequential(cause3, msg);
      } else {
        cause3 = msg;
      }
      message = [...message.slice(0, i), ...message.slice(i + 1)];
      i--;
    }
  }
  if (cause3 === void 0) {
    cause3 = empty22;
  }
  return withFiberRuntime((fiberState) => {
    fiberState.log(message, cause3, levelOption);
    return void_3;
  });
}, "logWithLevel");
var log = /* @__PURE__ */ logWithLevel();
var logTrace = /* @__PURE__ */ logWithLevel(Trace);
var logDebug = /* @__PURE__ */ logWithLevel(Debug);
var logInfo = /* @__PURE__ */ logWithLevel(Info);
var logWarning = /* @__PURE__ */ logWithLevel(Warning);
var logError = /* @__PURE__ */ logWithLevel(Error2);
var logFatal = /* @__PURE__ */ logWithLevel(Fatal);
var withLogSpan = /* @__PURE__ */ dual(2, (effect4, label) => flatMap12(currentTimeMillis2, (now2) => fiberRefLocallyWith(effect4, currentLogSpan, prepend4(make32(label, now2)))));
var logAnnotations = /* @__PURE__ */ fiberRefGet(currentLogAnnotations);
var loop = /* @__PURE__ */ __name((initial, options) => options.discard ? loopDiscard(initial, options.while, options.step, options.body) : map14(loopInternal(initial, options.while, options.step, options.body), fromIterable2), "loop");
var loopInternal = /* @__PURE__ */ __name((initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap12(body(initial), (a) => map14(loopInternal(inc(initial), cont, inc, body), prepend4(a))) : sync(() => empty12())), "loopInternal");
var loopDiscard = /* @__PURE__ */ __name((initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap12(body(initial), () => loopDiscard(inc(initial), cont, inc, body)) : void_3), "loopDiscard");
var mapAccum3 = /* @__PURE__ */ dual(3, (elements, initial, f) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let result = succeed(initial);
  let next4;
  let i = 0;
  while (!(next4 = iterator.next()).done) {
    const index = i++;
    const value2 = next4.value;
    result = flatMap12(result, (state) => map14(f(state, value2, index), ([z, b]) => {
      builder.push(b);
      return z;
    }));
  }
  return map14(result, (z) => [z, builder]);
}));
var mapErrorCause2 = /* @__PURE__ */ dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((c) => failCauseSync(() => f(c)), "onFailure"),
  onSuccess: succeed
}));
var memoize = /* @__PURE__ */ __name((self) => pipe(deferredMake(), flatMap12((deferred) => pipe(diffFiberRefsAndRuntimeFlags(self), intoDeferred(deferred), once2, map14((complete5) => zipRight2(complete5, pipe(deferredAwait(deferred), flatMap12(([patch13, a]) => as3(zip5(patchFiberRefs(patch13[0]), updateRuntimeFlags(patch13[1])), a)))))))), "memoize");
var merge5 = /* @__PURE__ */ __name((self) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((e) => succeed(e), "onFailure"),
  onSuccess: succeed
}), "merge");
var negate2 = /* @__PURE__ */ __name((self) => map14(self, (b) => !b), "negate");
var none6 = /* @__PURE__ */ __name((self) => flatMap12(self, (option5) => {
  switch (option5._tag) {
    case "None":
      return void_3;
    case "Some":
      return fail2(new NoSuchElementException());
  }
}), "none");
var once2 = /* @__PURE__ */ __name((self) => map14(make35(true), (ref) => asVoid2(whenEffect(self, getAndSet3(ref, false)))), "once");
var option = /* @__PURE__ */ __name((self) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name(() => succeed(none2()), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed(some3(a)), "onSuccess")
}), "option");
var orElseFail = /* @__PURE__ */ dual(2, (self, evaluate3) => orElse3(self, () => failSync(evaluate3)));
var orElseSucceed = /* @__PURE__ */ dual(2, (self, evaluate3) => orElse3(self, () => sync(evaluate3)));
var parallelErrors = /* @__PURE__ */ __name((self) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const errors = fromIterable2(failures(cause3));
    return errors.length === 0 ? failCause(cause3) : fail2(errors);
  }, "onFailure"),
  onSuccess: succeed
}), "parallelErrors");
var patchFiberRefs = /* @__PURE__ */ __name((patch13) => updateFiberRefs((fiberId4, fiberRefs4) => pipe(patch13, patch9(fiberId4, fiberRefs4))), "patchFiberRefs");
var promise = /* @__PURE__ */ __name((evaluate3) => evaluate3.length >= 1 ? async_((resolve, signal) => {
  try {
    evaluate3(signal).then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
}) : async_((resolve) => {
  try {
    ;
    evaluate3().then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
}), "promise");
var provideService = /* @__PURE__ */ dual(3, (self, tag, service6) => contextWithEffect((env) => provideContext(self, add2(env, tag, service6))));
var provideServiceEffect = /* @__PURE__ */ dual(3, (self, tag, effect4) => contextWithEffect((env) => flatMap12(effect4, (service6) => provideContext(self, pipe(env, add2(tag, service6))))));
var random2 = /* @__PURE__ */ randomWith(succeed);
var reduce12 = /* @__PURE__ */ dual(3, (elements, zero2, f) => fromIterable2(elements).reduce((acc, el, i) => flatMap12(acc, (a) => f(a, el, i)), succeed(zero2)));
var reduceRight4 = /* @__PURE__ */ dual(3, (elements, zero2, f) => fromIterable2(elements).reduceRight((acc, el, i) => flatMap12(acc, (a) => f(el, a, i)), succeed(zero2)));
var reduceWhile = /* @__PURE__ */ dual(3, (elements, zero2, options) => flatMap12(sync(() => elements[Symbol.iterator]()), (iterator) => reduceWhileLoop(iterator, 0, zero2, options.while, options.body)));
var reduceWhileLoop = /* @__PURE__ */ __name((iterator, index, state, predicate, f) => {
  const next4 = iterator.next();
  if (!next4.done && predicate(state)) {
    return flatMap12(f(state, next4.value, index), (nextState) => reduceWhileLoop(iterator, index + 1, nextState, predicate, f));
  }
  return succeed(state);
}, "reduceWhileLoop");
var repeatN = /* @__PURE__ */ dual(2, (self, n) => suspend(() => repeatNLoop(self, n)));
var repeatNLoop = /* @__PURE__ */ __name((self, n) => flatMap12(self, (a) => n <= 0 ? succeed(a) : zipRight2(yieldNow(), repeatNLoop(self, n - 1))), "repeatNLoop");
var sandbox = /* @__PURE__ */ __name((self) => matchCauseEffect(self, {
  onFailure: fail2,
  onSuccess: succeed
}), "sandbox");
var setFiberRefs = /* @__PURE__ */ __name((fiberRefs4) => suspend(() => setAll2(fiberRefs4)), "setFiberRefs");
var sleep3 = sleep2;
var succeedNone = /* @__PURE__ */ succeed(/* @__PURE__ */ none2());
var succeedSome = /* @__PURE__ */ __name((value2) => succeed(some3(value2)), "succeedSome");
var summarized = /* @__PURE__ */ dual(3, (self, summary6, f) => flatMap12(summary6, (start3) => flatMap12(self, (value2) => map14(summary6, (end6) => [f(start3, end6), value2]))));
var tagMetrics = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), function() {
  return labelMetrics(arguments[0], typeof arguments[1] === "string" ? [make37(arguments[1], arguments[2])] : Object.entries(arguments[1]).map(([k, v]) => make37(k, v)));
});
var labelMetrics = /* @__PURE__ */ dual(2, (self, labels) => fiberRefLocallyWith(self, currentMetricLabels, (old) => union2(old, labels)));
var takeUntil = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next4;
  let effect4 = succeed(false);
  let i = 0;
  while ((next4 = iterator.next()) && !next4.done) {
    const a = next4.value;
    const index = i++;
    effect4 = flatMap12(effect4, (bool) => {
      if (bool) {
        return succeed(true);
      }
      builder.push(a);
      return predicate(a, index);
    });
  }
  return map14(effect4, () => builder);
}));
var takeWhile4 = /* @__PURE__ */ dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next4;
  let taking = succeed(true);
  let i = 0;
  while ((next4 = iterator.next()) && !next4.done) {
    const a = next4.value;
    const index = i++;
    taking = flatMap12(taking, (taking2) => pipe(taking2 ? predicate(a, index) : succeed(false), map14((bool) => {
      if (bool) {
        builder.push(a);
      }
      return bool;
    })));
  }
  return map14(taking, () => builder);
}));
var tapBoth = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const either11 = failureOrCause(cause3);
    switch (either11._tag) {
      case "Left": {
        return zipRight2(onFailure(either11.left), failCause(cause3));
      }
      case "Right": {
        return failCause(cause3);
      }
    }
  }, "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => as3(onSuccess(a), a), "onSuccess")
}));
var tapDefect = /* @__PURE__ */ dual(2, (self, f) => catchAllCause(self, (cause3) => match2(keepDefects(cause3), {
  onNone: /* @__PURE__ */ __name(() => failCause(cause3), "onNone"),
  onSome: /* @__PURE__ */ __name((a) => zipRight2(f(a), failCause(cause3)), "onSome")
})));
var tapError = /* @__PURE__ */ dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const either11 = failureOrCause(cause3);
    switch (either11._tag) {
      case "Left":
        return zipRight2(f(either11.left), failCause(cause3));
      case "Right":
        return failCause(cause3);
    }
  }, "onFailure"),
  onSuccess: succeed
}));
var tapErrorTag = /* @__PURE__ */ dual(3, (self, k, f) => tapError(self, (e) => {
  if (isTagged(e, k)) {
    return f(e);
  }
  return void_3;
}));
var tapErrorCause = /* @__PURE__ */ dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => zipRight2(f(cause3), failCause(cause3)), "onFailure"),
  onSuccess: succeed
}));
var timed = /* @__PURE__ */ __name((self) => timedWith(self, currentTimeNanos2), "timed");
var timedWith = /* @__PURE__ */ dual(2, (self, nanos2) => summarized(self, nanos2, (start3, end6) => nanos(end6 - start3)));
var tracerWith3 = tracerWith2;
var tracer = /* @__PURE__ */ tracerWith3(succeed);
var tryPromise = /* @__PURE__ */ __name((arg) => {
  let evaluate3;
  let catcher = void 0;
  if (typeof arg === "function") {
    evaluate3 = arg;
  } else {
    evaluate3 = arg.try;
    catcher = arg.catch;
  }
  const fail22 = /* @__PURE__ */ __name((e) => catcher ? failSync(() => catcher(e)) : fail2(new UnknownException(e, "An unknown error occurred in Effect.tryPromise")), "fail");
  if (evaluate3.length >= 1) {
    return async_((resolve, signal) => {
      try {
        evaluate3(signal).then((a) => resolve(succeed(a)), (e) => resolve(fail22(e)));
      } catch (e) {
        resolve(fail22(e));
      }
    });
  }
  return async_((resolve) => {
    try {
      evaluate3().then((a) => resolve(succeed(a)), (e) => resolve(fail22(e)));
    } catch (e) {
      resolve(fail22(e));
    }
  });
}, "tryPromise");
var tryMap = /* @__PURE__ */ dual(2, (self, options) => flatMap12(self, (a) => try_2({
  try: /* @__PURE__ */ __name(() => options.try(a), "try"),
  catch: options.catch
})));
var tryMapPromise = /* @__PURE__ */ dual(2, (self, options) => flatMap12(self, (a) => tryPromise({
  try: options.try.length >= 1 ? (signal) => options.try(a, signal) : () => options.try(a),
  catch: options.catch
})));
var unless = /* @__PURE__ */ dual(2, (self, condition) => suspend(() => condition() ? succeedNone : asSome(self)));
var unlessEffect = /* @__PURE__ */ dual(2, (self, condition) => flatMap12(condition, (b) => b ? succeedNone : asSome(self)));
var unsandbox = /* @__PURE__ */ __name((self) => mapErrorCause2(self, flatten6), "unsandbox");
var updateFiberRefs = /* @__PURE__ */ __name((f) => withFiberRuntime((state) => {
  state.setFiberRefs(f(state.id(), state.getFiberRefs()));
  return void_3;
}), "updateFiberRefs");
var updateService = /* @__PURE__ */ dual(3, (self, tag, f) => mapInputContext(self, (context11) => add2(context11, tag, f(unsafeGet3(context11, tag)))));
var when = /* @__PURE__ */ dual(2, (self, condition) => suspend(() => condition() ? map14(self, some3) : succeed(none2())));
var whenFiberRef = /* @__PURE__ */ dual(3, (self, fiberRef, predicate) => flatMap12(fiberRefGet(fiberRef), (s) => predicate(s) ? map14(self, (a) => [s, some3(a)]) : succeed([s, none2()])));
var whenRef = /* @__PURE__ */ dual(3, (self, ref, predicate) => flatMap12(get13(ref), (s) => predicate(s) ? map14(self, (a) => [s, some3(a)]) : succeed([s, none2()])));
var withMetric = /* @__PURE__ */ dual(2, (self, metric) => metric(self));
var serviceFunctionEffect = /* @__PURE__ */ __name((getService, f) => (...args2) => flatMap12(getService, (a) => f(a)(...args2)), "serviceFunctionEffect");
var serviceFunction = /* @__PURE__ */ __name((getService, f) => (...args2) => map14(getService, (a) => f(a)(...args2)), "serviceFunction");
var serviceFunctions = /* @__PURE__ */ __name((getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return (...args2) => flatMap12(getService, (s) => s[prop](...args2));
  }
}), "serviceFunctions");
var serviceConstants = /* @__PURE__ */ __name((getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return flatMap12(getService, (s) => isEffect(s[prop]) ? s[prop] : succeed(s[prop]));
  }
}), "serviceConstants");
var serviceMembers = /* @__PURE__ */ __name((getService) => ({
  functions: serviceFunctions(getService),
  constants: serviceConstants(getService)
}), "serviceMembers");
var serviceOption = /* @__PURE__ */ __name((tag) => map14(context(), getOption2(tag)), "serviceOption");
var serviceOptional = /* @__PURE__ */ __name((tag) => flatMap12(context(), getOption2(tag)), "serviceOptional");
var annotateCurrentSpan = /* @__PURE__ */ __name(function() {
  const args2 = arguments;
  return ignore(flatMap12(currentSpan, (span4) => sync(() => {
    if (typeof args2[0] === "string") {
      span4.attribute(args2[0], args2[1]);
    } else {
      for (const key in args2[0]) {
        span4.attribute(key, args2[0][key]);
      }
    }
  })));
}, "annotateCurrentSpan");
var linkSpanCurrent = /* @__PURE__ */ __name(function() {
  const args2 = arguments;
  const links = Array.isArray(args2[0]) ? args2[0] : [{
    _tag: "SpanLink",
    span: args2[0],
    attributes: args2[1] ?? {}
  }];
  return ignore(flatMap12(currentSpan, (span4) => sync(() => span4.addLinks(links))));
}, "linkSpanCurrent");
var annotateSpans = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentTracerSpanAnnotations, typeof args2[1] === "string" ? set4(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set4(acc, key, value2), annotations));
});
var currentParentSpan = /* @__PURE__ */ serviceOptional(spanTag);
var currentSpan = /* @__PURE__ */ flatMap12(/* @__PURE__ */ context(), (context11) => {
  const span4 = context11.unsafeMap.get(spanTag.key);
  return span4 !== void 0 && span4._tag === "Span" ? succeed(span4) : fail2(new NoSuchElementException());
});
var linkSpans = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, span4, attributes) => fiberRefLocallyWith(self, currentTracerSpanLinks, append3({
  _tag: "SpanLink",
  span: span4,
  attributes: attributes ?? {}
})));
var bigint02 = /* @__PURE__ */ BigInt(0);
var filterDisablePropagation = /* @__PURE__ */ flatMap2((span4) => get4(span4.context, DisablePropagation) ? span4._tag === "Span" ? filterDisablePropagation(span4.parent) : none2() : some3(span4));
var unsafeMakeSpan = /* @__PURE__ */ __name((fiber, name, options) => {
  const disablePropagation = !fiber.getFiberRef(currentTracerEnabled) || options.context && get4(options.context, DisablePropagation);
  const context11 = fiber.getFiberRef(currentContext);
  const parent = options.parent ? some3(options.parent) : options.root ? none2() : filterDisablePropagation(getOption2(context11, spanTag));
  let span4;
  if (disablePropagation) {
    span4 = noopSpan({
      name,
      parent,
      context: add2(options.context ?? empty6(), DisablePropagation, true)
    });
  } else {
    const services = fiber.getFiberRef(currentServices);
    const tracer3 = get4(services, tracerTag);
    const clock3 = get4(services, Clock);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const fiberRefs4 = fiber.getFiberRefs();
    const annotationsFromEnv = get11(fiberRefs4, currentTracerSpanAnnotations);
    const linksFromEnv = get11(fiberRefs4, currentTracerSpanLinks);
    const links = linksFromEnv._tag === "Some" ? options.links !== void 0 ? [...toReadonlyArray(linksFromEnv.value), ...options.links ?? []] : toReadonlyArray(linksFromEnv.value) : options.links ?? empty4();
    span4 = tracer3.span(name, parent, options.context ?? empty6(), links, timingEnabled ? clock3.unsafeCurrentTimeNanos() : bigint02, options.kind ?? "internal", options);
    if (annotationsFromEnv._tag === "Some") {
      forEach7(annotationsFromEnv.value, (value2, key) => span4.attribute(key, value2));
    }
    if (options.attributes !== void 0) {
      Object.entries(options.attributes).forEach(([k, v]) => span4.attribute(k, v));
    }
  }
  if (typeof options.captureStackTrace === "function") {
    spanToTrace.set(span4, options.captureStackTrace);
  }
  return span4;
}, "unsafeMakeSpan");
var makeSpan = /* @__PURE__ */ __name((name, options) => {
  options = addSpanStackTrace(options);
  return withFiberRuntime((fiber) => succeed(unsafeMakeSpan(fiber, name, options)));
}, "makeSpan");
var spanAnnotations = /* @__PURE__ */ fiberRefGet(currentTracerSpanAnnotations);
var spanLinks = /* @__PURE__ */ fiberRefGet(currentTracerSpanLinks);
var endSpan = /* @__PURE__ */ __name((span4, exit4, clock3, timingEnabled) => sync(() => {
  if (span4.status._tag === "Ended") {
    return;
  }
  if (exitIsFailure(exit4) && spanToTrace.has(span4)) {
    span4.attribute("code.stacktrace", spanToTrace.get(span4)());
  }
  span4.end(timingEnabled ? clock3.unsafeCurrentTimeNanos() : bigint02, exit4);
}), "endSpan");
var useSpan = /* @__PURE__ */ __name((name, ...args2) => {
  const options = addSpanStackTrace(args2.length === 1 ? void 0 : args2[0]);
  const evaluate3 = args2[args2.length - 1];
  return withFiberRuntime((fiber) => {
    const span4 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock3 = get4(fiber.getFiberRef(currentServices), clockTag);
    return onExit(evaluate3(span4), (exit4) => endSpan(span4, exit4, clock3, timingEnabled));
  });
}, "useSpan");
var withParentSpan = /* @__PURE__ */ dual(2, (self, span4) => provideService(self, spanTag, span4));
var withSpan = /* @__PURE__ */ __name(function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return useSpan(name, options, (span4) => withParentSpan(self, span4));
  }
  return (self) => useSpan(name, options, (span4) => withParentSpan(self, span4));
}, "withSpan");
var functionWithSpan = /* @__PURE__ */ __name((options) => function() {
  let captureStackTrace = options.captureStackTrace ?? false;
  if (options.captureStackTrace !== false) {
    const limit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const error2 = new Error();
    Error.stackTraceLimit = limit;
    let cache = false;
    captureStackTrace = /* @__PURE__ */ __name(() => {
      if (cache !== false) {
        return cache;
      }
      if (error2.stack) {
        const stack = error2.stack.trim().split("\n");
        cache = stack.slice(2).join("\n").trim();
        return cache;
      }
    }, "captureStackTrace");
  }
  return suspend(() => {
    const opts = typeof options.options === "function" ? options.options.apply(null, arguments) : options.options;
    return withSpan(suspend(() => internalCall(() => options.body.apply(this, arguments))), opts.name, {
      ...opts,
      captureStackTrace
    });
  });
}, "functionWithSpan");
var fromNullable4 = /* @__PURE__ */ __name((value2) => value2 == null ? fail2(new NoSuchElementException()) : succeed(value2), "fromNullable");
var optionFromOptional = /* @__PURE__ */ __name((self) => catchAll(map14(self, some3), (error2) => isNoSuchElementException(error2) ? succeedNone : fail2(error2)), "optionFromOptional");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/executionStrategy.js
var OP_SEQUENTIAL2 = "Sequential";
var OP_PARALLEL2 = "Parallel";
var OP_PARALLEL_N = "ParallelN";
var sequential2 = {
  _tag: OP_SEQUENTIAL2
};
var parallel2 = {
  _tag: OP_PARALLEL2
};
var parallelN = /* @__PURE__ */ __name((parallelism) => ({
  _tag: OP_PARALLEL_N,
  parallelism
}), "parallelN");
var isSequential = /* @__PURE__ */ __name((self) => self._tag === OP_SEQUENTIAL2, "isSequential");
var isParallel = /* @__PURE__ */ __name((self) => self._tag === OP_PARALLEL2, "isParallel");
var isParallelN = /* @__PURE__ */ __name((self) => self._tag === OP_PARALLEL_N, "isParallelN");
var match9 = /* @__PURE__ */ dual(2, (self, options) => {
  switch (self._tag) {
    case OP_SEQUENTIAL2: {
      return options.onSequential();
    }
    case OP_PARALLEL2: {
      return options.onParallel();
    }
    case OP_PARALLEL_N: {
      return options.onParallelN(self.parallelism);
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/ExecutionStrategy.js
var sequential3 = sequential2;
var parallel3 = parallel2;
var parallelN2 = parallelN;
var isSequential2 = isSequential;
var isParallel2 = isParallel;
var isParallelN2 = isParallelN;
var match10 = match9;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/FiberRefsPatch.js
var empty29 = empty28;
var diff9 = diff8;
var combine13 = combine12;
var patch10 = patch9;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberStatus.js
var FiberStatusSymbolKey = "effect/FiberStatus";
var FiberStatusTypeId = /* @__PURE__ */ Symbol.for(FiberStatusSymbolKey);
var OP_DONE = "Done";
var OP_RUNNING = "Running";
var OP_SUSPENDED = "Suspended";
var DoneHash = /* @__PURE__ */ string2(`${FiberStatusSymbolKey}-${OP_DONE}`);
var Done = class {
  static {
    __name(this, "Done");
  }
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_DONE;
  [symbol2]() {
    return DoneHash;
  }
  [symbol3](that) {
    return isFiberStatus(that) && that._tag === OP_DONE;
  }
};
var Running = class {
  static {
    __name(this, "Running");
  }
  runtimeFlags;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_RUNNING;
  constructor(runtimeFlags2) {
    this.runtimeFlags = runtimeFlags2;
  }
  [symbol2]() {
    return pipe(hash(FiberStatusSymbolKey), combine2(hash(this._tag)), combine2(hash(this.runtimeFlags)), cached(this));
  }
  [symbol3](that) {
    return isFiberStatus(that) && that._tag === OP_RUNNING && this.runtimeFlags === that.runtimeFlags;
  }
};
var Suspended = class {
  static {
    __name(this, "Suspended");
  }
  runtimeFlags;
  blockingOn;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_SUSPENDED;
  constructor(runtimeFlags2, blockingOn) {
    this.runtimeFlags = runtimeFlags2;
    this.blockingOn = blockingOn;
  }
  [symbol2]() {
    return pipe(hash(FiberStatusSymbolKey), combine2(hash(this._tag)), combine2(hash(this.runtimeFlags)), combine2(hash(this.blockingOn)), cached(this));
  }
  [symbol3](that) {
    return isFiberStatus(that) && that._tag === OP_SUSPENDED && this.runtimeFlags === that.runtimeFlags && equals(this.blockingOn, that.blockingOn);
  }
};
var done3 = /* @__PURE__ */ new Done();
var running = /* @__PURE__ */ __name((runtimeFlags2) => new Running(runtimeFlags2), "running");
var suspended = /* @__PURE__ */ __name((runtimeFlags2, blockingOn) => new Suspended(runtimeFlags2, blockingOn), "suspended");
var isFiberStatus = /* @__PURE__ */ __name((u) => hasProperty(u, FiberStatusTypeId), "isFiberStatus");
var isDone2 = /* @__PURE__ */ __name((self) => self._tag === OP_DONE, "isDone");
var isRunning = /* @__PURE__ */ __name((self) => self._tag === OP_RUNNING, "isRunning");
var isSuspended = /* @__PURE__ */ __name((self) => self._tag === OP_SUSPENDED, "isSuspended");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/FiberStatus.js
var FiberStatusTypeId2 = FiberStatusTypeId;
var done4 = done3;
var running2 = running;
var suspended2 = suspended;
var isFiberStatus2 = isFiberStatus;
var isDone3 = isDone2;
var isRunning2 = isRunning;
var isSuspended2 = isSuspended;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Micro.js
var TypeId18 = /* @__PURE__ */ Symbol.for("effect/Micro");
var MicroExitTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroExit");
var isMicro = /* @__PURE__ */ __name((u) => typeof u === "object" && u !== null && TypeId18 in u, "isMicro");
var MicroCauseTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroCause");
var isMicroCause = /* @__PURE__ */ __name((self) => hasProperty(self, MicroCauseTypeId), "isMicroCause");
var microCauseVariance = {
  _E: identity
};
var MicroCauseImpl = class extends globalThis.Error {
  static {
    __name(this, "MicroCauseImpl");
  }
  _tag;
  traces;
  [MicroCauseTypeId];
  constructor(_tag, originalError2, traces) {
    const causeName = `MicroCause.${_tag}`;
    let name;
    let message;
    let stack;
    if (originalError2 instanceof globalThis.Error) {
      name = `(${causeName}) ${originalError2.name}`;
      message = originalError2.message;
      const messageLines = message.split("\n").length;
      stack = originalError2.stack ? `(${causeName}) ${originalError2.stack.split("\n").slice(0, messageLines + 3).join("\n")}` : `${name}: ${message}`;
    } else {
      name = causeName;
      message = toStringUnknown(originalError2, 0);
      stack = `${name}: ${message}`;
    }
    if (traces.length > 0) {
      stack += `
    ${traces.join("\n    ")}`;
    }
    super(message);
    this._tag = _tag;
    this.traces = traces;
    this[MicroCauseTypeId] = microCauseVariance;
    this.name = name;
    this.stack = stack;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toString() {
    return this.stack;
  }
  [NodeInspectSymbol]() {
    return this.stack;
  }
};
var Fail = class extends MicroCauseImpl {
  static {
    __name(this, "Fail");
  }
  error;
  constructor(error2, traces = []) {
    super("Fail", error2, traces);
    this.error = error2;
  }
};
var causeFail = /* @__PURE__ */ __name((error2, traces = []) => new Fail(error2, traces), "causeFail");
var Die = class extends MicroCauseImpl {
  static {
    __name(this, "Die");
  }
  defect;
  constructor(defect, traces = []) {
    super("Die", defect, traces);
    this.defect = defect;
  }
};
var causeDie = /* @__PURE__ */ __name((defect, traces = []) => new Die(defect, traces), "causeDie");
var Interrupt = class extends MicroCauseImpl {
  static {
    __name(this, "Interrupt");
  }
  constructor(traces = []) {
    super("Interrupt", "interrupted", traces);
  }
};
var causeInterrupt = /* @__PURE__ */ __name((traces = []) => new Interrupt(traces), "causeInterrupt");
var causeIsFail = /* @__PURE__ */ __name((self) => self._tag === "Fail", "causeIsFail");
var causeIsDie = /* @__PURE__ */ __name((self) => self._tag === "Die", "causeIsDie");
var causeIsInterrupt = /* @__PURE__ */ __name((self) => self._tag === "Interrupt", "causeIsInterrupt");
var causeSquash2 = /* @__PURE__ */ __name((self) => self._tag === "Fail" ? self.error : self._tag === "Die" ? self.defect : self, "causeSquash");
var causeWithTrace = /* @__PURE__ */ dual(2, (self, trace2) => {
  const traces = [...self.traces, trace2];
  switch (self._tag) {
    case "Die":
      return causeDie(self.defect, traces);
    case "Interrupt":
      return causeInterrupt(traces);
    case "Fail":
      return causeFail(self.error, traces);
  }
});
var MicroFiberTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroFiber");
var fiberVariance = {
  _A: identity,
  _E: identity
};
var MicroFiberImpl = class {
  static {
    __name(this, "MicroFiberImpl");
  }
  context;
  interruptible;
  [MicroFiberTypeId];
  _stack = [];
  _observers = [];
  _exit;
  _children;
  currentOpCount = 0;
  constructor(context11, interruptible5 = true) {
    this.context = context11;
    this.interruptible = interruptible5;
    this[MicroFiberTypeId] = fiberVariance;
  }
  getRef(ref) {
    return unsafeGetReference(this.context, ref);
  }
  addObserver(cb) {
    if (this._exit) {
      cb(this._exit);
      return constVoid;
    }
    this._observers.push(cb);
    return () => {
      const index = this._observers.indexOf(cb);
      if (index >= 0) {
        this._observers.splice(index, 1);
      }
    };
  }
  _interrupted = false;
  unsafeInterrupt() {
    if (this._exit) {
      return;
    }
    this._interrupted = true;
    if (this.interruptible) {
      this.evaluate(exitInterrupt2);
    }
  }
  unsafePoll() {
    return this._exit;
  }
  evaluate(effect4) {
    if (this._exit) {
      return;
    } else if (this._yielded !== void 0) {
      const yielded = this._yielded;
      this._yielded = void 0;
      yielded();
    }
    const exit4 = this.runLoop(effect4);
    if (exit4 === Yield) {
      return;
    }
    const interruptChildren = fiberMiddleware.interruptChildren && fiberMiddleware.interruptChildren(this);
    if (interruptChildren !== void 0) {
      return this.evaluate(flatMap14(interruptChildren, () => exit4));
    }
    this._exit = exit4;
    for (let i = 0; i < this._observers.length; i++) {
      this._observers[i](exit4);
    }
    this._observers.length = 0;
  }
  runLoop(effect4) {
    let yielding = false;
    let current2 = effect4;
    this.currentOpCount = 0;
    try {
      while (true) {
        this.currentOpCount++;
        if (!yielding && this.getRef(CurrentScheduler).shouldYield(this)) {
          yielding = true;
          const prev = current2;
          current2 = flatMap14(yieldNow2, () => prev);
        }
        current2 = current2[evaluate](this);
        if (current2 === Yield) {
          const yielded = this._yielded;
          if (MicroExitTypeId in yielded) {
            this._yielded = void 0;
            return yielded;
          }
          return Yield;
        }
      }
    } catch (error2) {
      if (!hasProperty(current2, evaluate)) {
        return exitDie2(`MicroFiber.runLoop: Not a valid effect: ${String(current2)}`);
      }
      return exitDie2(error2);
    }
  }
  getCont(symbol4) {
    while (true) {
      const op = this._stack.pop();
      if (!op) return void 0;
      const cont = op[ensureCont] && op[ensureCont](this);
      if (cont) return {
        [symbol4]: cont
      };
      if (op[symbol4]) return op;
    }
  }
  // cancel the yielded operation, or for the yielded exit value
  _yielded = void 0;
  yieldWith(value2) {
    this._yielded = value2;
    return Yield;
  }
  children() {
    return this._children ??= /* @__PURE__ */ new Set();
  }
};
var fiberMiddleware = /* @__PURE__ */ globalValue("effect/Micro/fiberMiddleware", () => ({
  interruptChildren: void 0
}));
var fiberInterruptChildren = /* @__PURE__ */ __name((fiber) => {
  if (fiber._children === void 0 || fiber._children.size === 0) {
    return void 0;
  }
  return fiberInterruptAll(fiber._children);
}, "fiberInterruptChildren");
var fiberAwait = /* @__PURE__ */ __name((self) => async((resume2) => sync3(self.addObserver((exit4) => resume2(succeed4(exit4))))), "fiberAwait");
var fiberJoin = /* @__PURE__ */ __name((self) => flatten9(fiberAwait(self)), "fiberJoin");
var fiberInterrupt = /* @__PURE__ */ __name((self) => suspend2(() => {
  self.unsafeInterrupt();
  return asVoid4(fiberAwait(self));
}), "fiberInterrupt");
var fiberInterruptAll = /* @__PURE__ */ __name((fibers) => suspend2(() => {
  for (const fiber of fibers) fiber.unsafeInterrupt();
  const iter = fibers[Symbol.iterator]();
  const wait = suspend2(() => {
    let result = iter.next();
    while (!result.done) {
      if (result.value.unsafePoll()) {
        result = iter.next();
        continue;
      }
      const fiber = result.value;
      return async((resume2) => {
        fiber.addObserver((_) => {
          resume2(wait);
        });
      });
    }
    return exitVoid2;
  });
  return wait;
}), "fiberInterruptAll");
var identifier = /* @__PURE__ */ Symbol.for("effect/Micro/identifier");
var args = /* @__PURE__ */ Symbol.for("effect/Micro/args");
var evaluate = /* @__PURE__ */ Symbol.for("effect/Micro/evaluate");
var successCont = /* @__PURE__ */ Symbol.for("effect/Micro/successCont");
var failureCont = /* @__PURE__ */ Symbol.for("effect/Micro/failureCont");
var ensureCont = /* @__PURE__ */ Symbol.for("effect/Micro/ensureCont");
var Yield = /* @__PURE__ */ Symbol.for("effect/Micro/Yield");
var microVariance = {
  _A: identity,
  _E: identity,
  _R: identity
};
var MicroProto = {
  ...EffectPrototype2,
  _op: "Micro",
  [TypeId18]: microVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  },
  toJSON() {
    return {
      _id: "Micro",
      op: this[identifier],
      ...args in this ? {
        args: this[args]
      } : void 0
    };
  },
  toString() {
    return format(this);
  },
  [NodeInspectSymbol]() {
    return format(this);
  }
};
function defaultEvaluate(_fiber) {
  return exitDie2(`Micro.evaluate: Not implemented`);
}
__name(defaultEvaluate, "defaultEvaluate");
var makePrimitiveProto = /* @__PURE__ */ __name((options) => ({
  ...MicroProto,
  [identifier]: options.op,
  [evaluate]: options.eval ?? defaultEvaluate,
  [successCont]: options.contA,
  [failureCont]: options.contE,
  [ensureCont]: options.ensure
}), "makePrimitiveProto");
var makePrimitive = /* @__PURE__ */ __name((options) => {
  const Proto4 = makePrimitiveProto(options);
  return function() {
    const self = Object.create(Proto4);
    self[args] = options.single === false ? arguments : arguments[0];
    return self;
  };
}, "makePrimitive");
var makeExit = /* @__PURE__ */ __name((options) => {
  const Proto4 = {
    ...makePrimitiveProto(options),
    [MicroExitTypeId]: MicroExitTypeId,
    _tag: options.op,
    get [options.prop]() {
      return this[args];
    },
    toJSON() {
      return {
        _id: "MicroExit",
        _tag: options.op,
        [options.prop]: this[args]
      };
    },
    [symbol3](that) {
      return isMicroExit(that) && that._tag === options.op && equals(this[args], that[args]);
    },
    [symbol2]() {
      return cached(this, combine2(string2(options.op))(hash(this[args])));
    }
  };
  return function(value2) {
    const self = Object.create(Proto4);
    self[args] = value2;
    self[successCont] = void 0;
    self[failureCont] = void 0;
    self[ensureCont] = void 0;
    return self;
  };
}, "makeExit");
var succeed4 = /* @__PURE__ */ makeExit({
  op: "Success",
  prop: "value",
  eval(fiber) {
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var failCause4 = /* @__PURE__ */ makeExit({
  op: "Failure",
  prop: "cause",
  eval(fiber) {
    let cont = fiber.getCont(failureCont);
    while (causeIsInterrupt(this[args]) && cont && fiber.interruptible) {
      cont = fiber.getCont(failureCont);
    }
    return cont ? cont[failureCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var fail5 = /* @__PURE__ */ __name((error2) => failCause4(causeFail(error2)), "fail");
var sync3 = /* @__PURE__ */ makePrimitive({
  op: "Sync",
  eval(fiber) {
    const value2 = this[args]();
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](value2, fiber) : fiber.yieldWith(exitSucceed2(value2));
  }
});
var suspend2 = /* @__PURE__ */ makePrimitive({
  op: "Suspend",
  eval(_fiber) {
    return this[args]();
  }
});
var yieldNowWith = /* @__PURE__ */ makePrimitive({
  op: "Yield",
  eval(fiber) {
    let resumed = false;
    fiber.getRef(CurrentScheduler).scheduleTask(() => {
      if (resumed) return;
      fiber.evaluate(exitVoid2);
    }, this[args] ?? 0);
    return fiber.yieldWith(() => {
      resumed = true;
    });
  }
});
var yieldNow2 = /* @__PURE__ */ yieldNowWith(0);
var succeedSome2 = /* @__PURE__ */ __name((a) => succeed4(some3(a)), "succeedSome");
var succeedNone2 = /* @__PURE__ */ succeed4(/* @__PURE__ */ none2());
var failCauseSync3 = /* @__PURE__ */ __name((evaluate3) => suspend2(() => failCause4(evaluate3())), "failCauseSync");
var die5 = /* @__PURE__ */ __name((defect) => exitDie2(defect), "die");
var failSync3 = /* @__PURE__ */ __name((error2) => suspend2(() => fail5(error2())), "failSync");
var fromOption5 = /* @__PURE__ */ __name((option5) => option5._tag === "Some" ? succeed4(option5.value) : fail5(new NoSuchElementException2({})), "fromOption");
var fromEither2 = /* @__PURE__ */ __name((either11) => either11._tag === "Right" ? succeed4(either11.right) : fail5(either11.left), "fromEither");
var void_5 = /* @__PURE__ */ succeed4(void 0);
var try_3 = /* @__PURE__ */ __name((options) => suspend2(() => {
  try {
    return succeed4(options.try());
  } catch (err) {
    return fail5(options.catch(err));
  }
}), "try_");
var promise2 = /* @__PURE__ */ __name((evaluate3) => asyncOptions(function(resume2, signal) {
  evaluate3(signal).then((a) => resume2(succeed4(a)), (e) => resume2(die5(e)));
}, evaluate3.length !== 0), "promise");
var tryPromise2 = /* @__PURE__ */ __name((options) => asyncOptions(function(resume2, signal) {
  try {
    options.try(signal).then((a) => resume2(succeed4(a)), (e) => resume2(fail5(options.catch(e))));
  } catch (err) {
    resume2(fail5(options.catch(err)));
  }
}, options.try.length !== 0), "tryPromise");
var withMicroFiber = /* @__PURE__ */ makePrimitive({
  op: "WithMicroFiber",
  eval(fiber) {
    return this[args](fiber);
  }
});
var yieldFlush = /* @__PURE__ */ withMicroFiber((fiber) => {
  fiber.getRef(CurrentScheduler).flush();
  return exitVoid2;
});
var asyncOptions = /* @__PURE__ */ makePrimitive({
  op: "Async",
  single: false,
  eval(fiber) {
    const register = this[args][0];
    let resumed = false;
    let yielded = false;
    const controller = this[args][1] ? new AbortController() : void 0;
    const onCancel = register((effect4) => {
      if (resumed) return;
      resumed = true;
      if (yielded) {
        fiber.evaluate(effect4);
      } else {
        yielded = effect4;
      }
    }, controller?.signal);
    if (yielded !== false) return yielded;
    yielded = true;
    fiber._yielded = () => {
      resumed = true;
    };
    if (controller === void 0 && onCancel === void 0) {
      return Yield;
    }
    fiber._stack.push(asyncFinalizer(() => {
      resumed = true;
      controller?.abort();
      return onCancel ?? exitVoid2;
    }));
    return Yield;
  }
});
var asyncFinalizer = /* @__PURE__ */ makePrimitive({
  op: "AsyncFinalizer",
  ensure(fiber) {
    if (fiber.interruptible) {
      fiber.interruptible = false;
      fiber._stack.push(setInterruptible(true));
    }
  },
  contE(cause3, _fiber) {
    return causeIsInterrupt(cause3) ? flatMap14(this[args](), () => failCause4(cause3)) : failCause4(cause3);
  }
});
var async = /* @__PURE__ */ __name((register) => asyncOptions(register, register.length >= 2), "async");
var never2 = /* @__PURE__ */ async(function() {
  const interval = setInterval(constVoid, 2147483646);
  return sync3(() => clearInterval(interval));
});
var gen4 = /* @__PURE__ */ __name((...args2) => suspend2(() => fromIterator2(args2.length === 1 ? args2[0]() : args2[1].call(args2[0]))), "gen");
var fromIterator2 = /* @__PURE__ */ makePrimitive({
  op: "Iterator",
  contA(value2, fiber) {
    const state = this[args].next(value2);
    if (state.done) return succeed4(state.value);
    fiber._stack.push(this);
    return yieldWrapGet(state.value);
  },
  eval(fiber) {
    return this[successCont](void 0, fiber);
  }
});
var as5 = /* @__PURE__ */ dual(2, (self, value2) => map17(self, (_) => value2));
var asSome2 = /* @__PURE__ */ __name((self) => map17(self, some3), "asSome");
var flip4 = /* @__PURE__ */ __name((self) => matchEffect3(self, {
  onFailure: succeed4,
  onSuccess: fail5
}), "flip");
var andThen8 = /* @__PURE__ */ dual(2, (self, f) => flatMap14(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? value2 : succeed4(value2);
}));
var tap3 = /* @__PURE__ */ dual(2, (self, f) => flatMap14(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? as5(value2, a) : succeed4(a);
}));
var asVoid4 = /* @__PURE__ */ __name((self) => flatMap14(self, (_) => exitVoid2), "asVoid");
var exit2 = /* @__PURE__ */ __name((self) => matchCause2(self, {
  onFailure: exitFailCause2,
  onSuccess: exitSucceed2
}), "exit");
var sandbox2 = /* @__PURE__ */ __name((self) => catchAllCause2(self, fail5), "sandbox");
var raceAll = /* @__PURE__ */ __name((all12) => withMicroFiber((parent) => async((resume2) => {
  const effects = fromIterable2(all12);
  const len = effects.length;
  let doneCount = 0;
  let done12 = false;
  const fibers = /* @__PURE__ */ new Set();
  const causes = [];
  const onExit4 = /* @__PURE__ */ __name((exit4) => {
    doneCount++;
    if (exit4._tag === "Failure") {
      causes.push(exit4.cause);
      if (doneCount >= len) {
        resume2(failCause4(causes[0]));
      }
      return;
    }
    done12 = true;
    resume2(fibers.size === 0 ? exit4 : flatMap14(uninterruptible2(fiberInterruptAll(fibers)), () => exit4));
  }, "onExit");
  for (let i = 0; i < len; i++) {
    if (done12) break;
    const fiber = unsafeFork(parent, interruptible3(effects[i]), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit4) => {
      fibers.delete(fiber);
      onExit4(exit4);
    });
  }
  return fiberInterruptAll(fibers);
})), "raceAll");
var raceAllFirst = /* @__PURE__ */ __name((all12) => withMicroFiber((parent) => async((resume2) => {
  let done12 = false;
  const fibers = /* @__PURE__ */ new Set();
  const onExit4 = /* @__PURE__ */ __name((exit4) => {
    done12 = true;
    resume2(fibers.size === 0 ? exit4 : flatMap14(fiberInterruptAll(fibers), () => exit4));
  }, "onExit");
  for (const effect4 of all12) {
    if (done12) break;
    const fiber = unsafeFork(parent, interruptible3(effect4), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit4) => {
      fibers.delete(fiber);
      onExit4(exit4);
    });
  }
  return fiberInterruptAll(fibers);
})), "raceAllFirst");
var race = /* @__PURE__ */ dual(2, (self, that) => raceAll([self, that]));
var raceFirst = /* @__PURE__ */ dual(2, (self, that) => raceAllFirst([self, that]));
var flatMap14 = /* @__PURE__ */ dual(2, (self, f) => {
  const onSuccess = Object.create(OnSuccessProto);
  onSuccess[args] = self;
  onSuccess[successCont] = f;
  return onSuccess;
});
var OnSuccessProto = /* @__PURE__ */ makePrimitiveProto({
  op: "OnSuccess",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var flatten9 = /* @__PURE__ */ __name((self) => flatMap14(self, identity), "flatten");
var map17 = /* @__PURE__ */ dual(2, (self, f) => flatMap14(self, (a) => succeed4(f(a))));
var isMicroExit = /* @__PURE__ */ __name((u) => hasProperty(u, MicroExitTypeId), "isMicroExit");
var exitSucceed2 = succeed4;
var exitFailCause2 = failCause4;
var exitInterrupt2 = /* @__PURE__ */ exitFailCause2(/* @__PURE__ */ causeInterrupt());
var exitFail2 = /* @__PURE__ */ __name((e) => exitFailCause2(causeFail(e)), "exitFail");
var exitDie2 = /* @__PURE__ */ __name((defect) => exitFailCause2(causeDie(defect)), "exitDie");
var exitIsSuccess2 = /* @__PURE__ */ __name((self) => self._tag === "Success", "exitIsSuccess");
var exitIsFailure2 = /* @__PURE__ */ __name((self) => self._tag === "Failure", "exitIsFailure");
var exitIsInterrupt = /* @__PURE__ */ __name((self) => exitIsFailure2(self) && self.cause._tag === "Interrupt", "exitIsInterrupt");
var exitIsFail = /* @__PURE__ */ __name((self) => exitIsFailure2(self) && self.cause._tag === "Fail", "exitIsFail");
var exitIsDie = /* @__PURE__ */ __name((self) => exitIsFailure2(self) && self.cause._tag === "Die", "exitIsDie");
var exitVoid2 = /* @__PURE__ */ exitSucceed2(void 0);
var exitVoidAll = /* @__PURE__ */ __name((exits) => {
  for (const exit4 of exits) {
    if (exit4._tag === "Failure") {
      return exit4;
    }
  }
  return exitVoid2;
}, "exitVoidAll");
var setImmediate = "setImmediate" in globalThis ? globalThis.setImmediate : (f) => setTimeout(f, 0);
var MicroSchedulerDefault = class {
  static {
    __name(this, "MicroSchedulerDefault");
  }
  tasks = [];
  running = false;
  /**
   * @since 3.5.9
   */
  scheduleTask(task, _priority) {
    this.tasks.push(task);
    if (!this.running) {
      this.running = true;
      setImmediate(this.afterScheduled);
    }
  }
  /**
   * @since 3.5.9
   */
  afterScheduled = /* @__PURE__ */ __name(() => {
    this.running = false;
    this.runTasks();
  }, "afterScheduled");
  /**
   * @since 3.5.9
   */
  runTasks() {
    const tasks = this.tasks;
    this.tasks = [];
    for (let i = 0, len = tasks.length; i < len; i++) {
      tasks[i]();
    }
  }
  /**
   * @since 3.5.9
   */
  shouldYield(fiber) {
    return fiber.currentOpCount >= fiber.getRef(MaxOpsBeforeYield);
  }
  /**
   * @since 3.5.9
   */
  flush() {
    while (this.tasks.length > 0) {
      this.runTasks();
    }
  }
};
var service = /* @__PURE__ */ __name((tag) => withMicroFiber((fiber) => succeed4(unsafeGet3(fiber.context, tag))), "service");
var serviceOption2 = /* @__PURE__ */ __name((tag) => withMicroFiber((fiber) => succeed4(getOption2(fiber.context, tag))), "serviceOption");
var updateContext = /* @__PURE__ */ dual(2, (self, f) => withMicroFiber((fiber) => {
  const prev = fiber.context;
  fiber.context = f(prev);
  return onExit2(self, () => {
    fiber.context = prev;
    return void_5;
  });
}));
var updateService2 = /* @__PURE__ */ dual(3, (self, tag, f) => withMicroFiber((fiber) => {
  const prev = unsafeGet3(fiber.context, tag);
  fiber.context = add2(fiber.context, tag, f(prev));
  return onExit2(self, () => {
    fiber.context = add2(fiber.context, tag, prev);
    return void_5;
  });
}));
var context2 = /* @__PURE__ */ __name(() => getContext, "context");
var getContext = /* @__PURE__ */ withMicroFiber((fiber) => succeed4(fiber.context));
var provideContext2 = /* @__PURE__ */ dual(2, (self, provided) => updateContext(self, merge3(provided)));
var provideService2 = /* @__PURE__ */ dual(3, (self, tag, service6) => updateContext(self, add2(tag, service6)));
var provideServiceEffect2 = /* @__PURE__ */ dual(3, (self, tag, acquire) => flatMap14(acquire, (service6) => provideService2(self, tag, service6)));
var MaxOpsBeforeYield = class extends (/* @__PURE__ */ Reference2()("effect/Micro/currentMaxOpsBeforeYield", {
  defaultValue: /* @__PURE__ */ __name(() => 2048, "defaultValue")
})) {
  static {
    __name(this, "MaxOpsBeforeYield");
  }
};
var CurrentConcurrency = class extends (/* @__PURE__ */ Reference2()("effect/Micro/currentConcurrency", {
  defaultValue: /* @__PURE__ */ __name(() => "unbounded", "defaultValue")
})) {
  static {
    __name(this, "CurrentConcurrency");
  }
};
var CurrentScheduler = class extends (/* @__PURE__ */ Reference2()("effect/Micro/currentScheduler", {
  defaultValue: /* @__PURE__ */ __name(() => new MicroSchedulerDefault(), "defaultValue")
})) {
  static {
    __name(this, "CurrentScheduler");
  }
};
var withConcurrency2 = /* @__PURE__ */ dual(2, (self, concurrency) => provideService2(self, CurrentConcurrency, concurrency));
var zip7 = /* @__PURE__ */ dual((args2) => isMicro(args2[1]), (self, that, options) => zipWith8(self, that, (a, a2) => [a, a2], options));
var zipWith8 = /* @__PURE__ */ dual((args2) => isMicro(args2[1]), (self, that, f, options) => options?.concurrent ? map17(all7([self, that], {
  concurrency: 2
}), ([a, a2]) => f(a, a2)) : flatMap14(self, (a) => map17(that, (a2) => f(a, a2))));
var filterOrFailCause = /* @__PURE__ */ dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap14(self, (a) => refinement(a) ? succeed4(a) : failCause4(orFailWith(a))));
var filterOrFail2 = /* @__PURE__ */ dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap14(self, (a) => refinement(a) ? succeed4(a) : fail5(orFailWith(a))));
var when2 = /* @__PURE__ */ dual(2, (self, condition) => flatMap14(isMicro(condition) ? condition : sync3(condition), (pass) => pass ? asSome2(self) : succeedNone2));
var repeatExit = /* @__PURE__ */ dual(2, (self, options) => suspend2(() => {
  const startedAt = options.schedule ? Date.now() : 0;
  let attempt2 = 0;
  const loop4 = flatMap14(exit2(self), (exit4) => {
    if (options.while !== void 0 && !options.while(exit4)) {
      return exit4;
    } else if (options.times !== void 0 && attempt2 >= options.times) {
      return exit4;
    }
    attempt2++;
    let delayEffect = yieldNow2;
    if (options.schedule !== void 0) {
      const elapsed3 = Date.now() - startedAt;
      const duration3 = options.schedule(attempt2, elapsed3);
      if (isNone2(duration3)) {
        return exit4;
      }
      delayEffect = sleep4(duration3.value);
    }
    return flatMap14(delayEffect, () => loop4);
  });
  return loop4;
}));
var repeat = /* @__PURE__ */ dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: /* @__PURE__ */ __name((exit4) => exit4._tag === "Success" && (options?.while === void 0 || options.while(exit4.value)), "while")
}));
var replicate3 = /* @__PURE__ */ dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect = /* @__PURE__ */ dual((args2) => isMicro(args2[0]), (self, n, options) => all7(replicate3(self, n), options));
var forever2 = /* @__PURE__ */ __name((self) => repeat(self), "forever");
var scheduleRecurs = /* @__PURE__ */ __name((n) => (attempt2) => attempt2 <= n ? some3(0) : none2(), "scheduleRecurs");
var scheduleSpaced = /* @__PURE__ */ __name((millis2) => () => some3(millis2), "scheduleSpaced");
var scheduleExponential = /* @__PURE__ */ __name((baseMillis, factor = 2) => (attempt2) => some3(Math.pow(factor, attempt2) * baseMillis), "scheduleExponential");
var scheduleAddDelay = /* @__PURE__ */ dual(2, (self, f) => (attempt2, elapsed3) => map2(self(attempt2, elapsed3), (duration3) => duration3 + f()));
var scheduleWithMaxDelay = /* @__PURE__ */ dual(2, (self, max10) => (attempt2, elapsed3) => map2(self(attempt2, elapsed3), (duration3) => Math.min(duration3, max10)));
var scheduleWithMaxElapsed = /* @__PURE__ */ dual(2, (self, max10) => (attempt2, elapsed3) => elapsed3 < max10 ? self(attempt2, elapsed3) : none2());
var scheduleUnion = /* @__PURE__ */ dual(2, (self, that) => (attempt2, elapsed3) => zipWith2(self(attempt2, elapsed3), that(attempt2, elapsed3), (d1, d2) => Math.min(d1, d2)));
var scheduleIntersect = /* @__PURE__ */ dual(2, (self, that) => (attempt2, elapsed3) => zipWith2(self(attempt2, elapsed3), that(attempt2, elapsed3), (d1, d2) => Math.max(d1, d2)));
var catchAllCause2 = /* @__PURE__ */ dual(2, (self, f) => {
  const onFailure = Object.create(OnFailureProto);
  onFailure[args] = self;
  onFailure[failureCont] = f;
  return onFailure;
});
var OnFailureProto = /* @__PURE__ */ makePrimitiveProto({
  op: "OnFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var catchCauseIf = /* @__PURE__ */ dual(3, (self, predicate, f) => catchAllCause2(self, (cause3) => predicate(cause3) ? f(cause3) : failCause4(cause3)));
var catchAll2 = /* @__PURE__ */ dual(2, (self, f) => catchCauseIf(self, causeIsFail, (cause3) => f(cause3.error)));
var catchAllDefect2 = /* @__PURE__ */ dual(2, (self, f) => catchCauseIf(self, causeIsDie, (die16) => f(die16.defect)));
var tapErrorCause2 = /* @__PURE__ */ dual(2, (self, f) => tapErrorCauseIf(self, constTrue, f));
var tapErrorCauseIf = /* @__PURE__ */ dual(3, (self, refinement, f) => catchCauseIf(self, refinement, (cause3) => andThen8(f(cause3), failCause4(cause3))));
var tapError2 = /* @__PURE__ */ dual(2, (self, f) => tapErrorCauseIf(self, causeIsFail, (fail22) => f(fail22.error)));
var tapDefect2 = /* @__PURE__ */ dual(2, (self, f) => tapErrorCauseIf(self, causeIsDie, (die16) => f(die16.defect)));
var catchIf2 = /* @__PURE__ */ dual(3, (self, predicate, f) => catchCauseIf(self, (f2) => causeIsFail(f2) && predicate(f2.error), (fail22) => f(fail22.error)));
var catchTag2 = /* @__PURE__ */ dual(3, (self, k, f) => catchIf2(self, isTagged(k), f));
var mapErrorCause3 = /* @__PURE__ */ dual(2, (self, f) => catchAllCause2(self, (cause3) => failCause4(f(cause3))));
var mapError3 = /* @__PURE__ */ dual(2, (self, f) => catchAll2(self, (error2) => fail5(f(error2))));
var orDie2 = /* @__PURE__ */ __name((self) => catchAll2(self, die5), "orDie");
var orElseSucceed2 = /* @__PURE__ */ dual(2, (self, f) => catchAll2(self, (_) => sync3(f)));
var ignore2 = /* @__PURE__ */ __name((self) => matchEffect3(self, {
  onFailure: /* @__PURE__ */ __name((_) => void_5, "onFailure"),
  onSuccess: /* @__PURE__ */ __name((_) => void_5, "onSuccess")
}), "ignore");
var ignoreLogged2 = /* @__PURE__ */ __name((self) => matchEffect3(self, {
  // eslint-disable-next-line no-console
  onFailure: /* @__PURE__ */ __name((error2) => sync3(() => console.error(error2)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((_) => void_5, "onSuccess")
}), "ignoreLogged");
var option2 = /* @__PURE__ */ __name((self) => match11(self, {
  onFailure: none2,
  onSuccess: some3
}), "option");
var either4 = /* @__PURE__ */ __name((self) => match11(self, {
  onFailure: left2,
  onSuccess: right2
}), "either");
var retry = /* @__PURE__ */ dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: /* @__PURE__ */ __name((exit4) => exit4._tag === "Failure" && exit4.cause._tag === "Fail" && (options?.while === void 0 || options.while(exit4.cause.error)), "while")
}));
var withTrace = /* @__PURE__ */ __name(function() {
  const prevLimit = globalThis.Error.stackTraceLimit;
  globalThis.Error.stackTraceLimit = 2;
  const error2 = new globalThis.Error();
  globalThis.Error.stackTraceLimit = prevLimit;
  function generate(name, cause3) {
    const stack = error2.stack;
    if (!stack) {
      return cause3;
    }
    const line = stack.split("\n")[2]?.trim().replace(/^at /, "");
    if (!line) {
      return cause3;
    }
    const lineMatch = line.match(/\((.*)\)$/);
    return causeWithTrace(cause3, `at ${name} (${lineMatch ? lineMatch[1] : line})`);
  }
  __name(generate, "generate");
  const f = /* @__PURE__ */ __name((name) => (self) => onError2(self, (cause3) => failCause4(generate(name, cause3))), "f");
  if (arguments.length === 2) {
    return f(arguments[1])(arguments[0]);
  }
  return f(arguments[0]);
}, "withTrace");
var matchCauseEffect2 = /* @__PURE__ */ dual(2, (self, options) => {
  const primitive = Object.create(OnSuccessAndFailureProto);
  primitive[args] = self;
  primitive[successCont] = options.onSuccess;
  primitive[failureCont] = options.onFailure;
  return primitive;
});
var OnSuccessAndFailureProto = /* @__PURE__ */ makePrimitiveProto({
  op: "OnSuccessAndFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var matchCause2 = /* @__PURE__ */ dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => sync3(() => options.onFailure(cause3)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((value2) => sync3(() => options.onSuccess(value2)), "onSuccess")
}));
var matchEffect3 = /* @__PURE__ */ dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => cause3._tag === "Fail" ? options.onFailure(cause3.error) : failCause4(cause3), "onFailure"),
  onSuccess: options.onSuccess
}));
var match11 = /* @__PURE__ */ dual(2, (self, options) => matchEffect3(self, {
  onFailure: /* @__PURE__ */ __name((error2) => sync3(() => options.onFailure(error2)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((value2) => sync3(() => options.onSuccess(value2)), "onSuccess")
}));
var sleep4 = /* @__PURE__ */ __name((millis2) => async((resume2) => {
  const timeout6 = setTimeout(() => {
    resume2(void_5);
  }, millis2);
  return sync3(() => {
    clearTimeout(timeout6);
  });
}), "sleep");
var delay2 = /* @__PURE__ */ dual(2, (self, millis2) => andThen8(sleep4(millis2), self));
var timeoutOrElse = /* @__PURE__ */ dual(2, (self, options) => raceFirst(self, andThen8(interruptible3(sleep4(options.duration)), options.onTimeout)));
var timeout = /* @__PURE__ */ dual(2, (self, millis2) => timeoutOrElse(self, {
  duration: millis2,
  onTimeout: /* @__PURE__ */ __name(() => fail5(new TimeoutException2()), "onTimeout")
}));
var timeoutOption = /* @__PURE__ */ dual(2, (self, millis2) => raceFirst(asSome2(self), as5(interruptible3(sleep4(millis2)), none2())));
var MicroScopeTypeId = /* @__PURE__ */ Symbol.for("effect/Micro/MicroScope");
var MicroScope = /* @__PURE__ */ GenericTag("effect/Micro/MicroScope");
var MicroScopeImpl = class _MicroScopeImpl {
  static {
    __name(this, "MicroScopeImpl");
  }
  [MicroScopeTypeId];
  state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Set()
  };
  constructor() {
    this[MicroScopeTypeId] = MicroScopeTypeId;
  }
  unsafeAddFinalizer(finalizer3) {
    if (this.state._tag === "Open") {
      this.state.finalizers.add(finalizer3);
    }
  }
  addFinalizer(finalizer3) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        this.state.finalizers.add(finalizer3);
        return void_5;
      }
      return finalizer3(this.state.exit);
    });
  }
  unsafeRemoveFinalizer(finalizer3) {
    if (this.state._tag === "Open") {
      this.state.finalizers.delete(finalizer3);
    }
  }
  close(microExit) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        const finalizers = Array.from(this.state.finalizers).reverse();
        this.state = {
          _tag: "Closed",
          exit: microExit
        };
        return flatMap14(forEach11(finalizers, (finalizer3) => exit2(finalizer3(microExit))), exitVoidAll);
      }
      return void_5;
    });
  }
  get fork() {
    return sync3(() => {
      const newScope = new _MicroScopeImpl();
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      function fin(exit4) {
        return newScope.close(exit4);
      }
      __name(fin, "fin");
      this.state.finalizers.add(fin);
      newScope.unsafeAddFinalizer((_) => sync3(() => this.unsafeRemoveFinalizer(fin)));
      return newScope;
    });
  }
};
var scopeMake = /* @__PURE__ */ sync3(() => new MicroScopeImpl());
var scopeUnsafeMake = /* @__PURE__ */ __name(() => new MicroScopeImpl(), "scopeUnsafeMake");
var scope = /* @__PURE__ */ service(MicroScope);
var provideScope = /* @__PURE__ */ dual(2, (self, scope6) => provideService2(self, MicroScope, scope6));
var scoped = /* @__PURE__ */ __name((self) => suspend2(() => {
  const scope6 = new MicroScopeImpl();
  return onExit2(provideService2(self, MicroScope, scope6), (exit4) => scope6.close(exit4));
}), "scoped");
var acquireRelease = /* @__PURE__ */ __name((acquire, release) => uninterruptible2(flatMap14(scope, (scope6) => tap3(acquire, (a) => scope6.addFinalizer((exit4) => release(a, exit4))))), "acquireRelease");
var addFinalizer = /* @__PURE__ */ __name((finalizer3) => flatMap14(scope, (scope6) => scope6.addFinalizer(finalizer3)), "addFinalizer");
var onExit2 = /* @__PURE__ */ dual(2, (self, f) => uninterruptibleMask2((restore) => matchCauseEffect2(restore(self), {
  onFailure: /* @__PURE__ */ __name((cause3) => flatMap14(f(exitFailCause2(cause3)), () => failCause4(cause3)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => flatMap14(f(exitSucceed2(a)), () => succeed4(a)), "onSuccess")
})));
var ensuring = /* @__PURE__ */ dual(2, (self, finalizer3) => onExit2(self, (_) => finalizer3));
var onExitIf = /* @__PURE__ */ dual(3, (self, refinement, f) => onExit2(self, (exit4) => refinement(exit4) ? f(exit4) : exitVoid2));
var onError2 = /* @__PURE__ */ dual(2, (self, f) => onExitIf(self, exitIsFailure2, (exit4) => f(exit4.cause)));
var onInterrupt2 = /* @__PURE__ */ dual(2, (self, finalizer3) => onExitIf(self, exitIsInterrupt, (_) => finalizer3));
var acquireUseRelease2 = /* @__PURE__ */ __name((acquire, use2, release) => uninterruptibleMask2((restore) => flatMap14(acquire, (a) => flatMap14(exit2(restore(use2(a))), (exit4) => andThen8(release(a, exit4), exit4)))), "acquireUseRelease");
var interrupt5 = /* @__PURE__ */ failCause4(/* @__PURE__ */ causeInterrupt());
var uninterruptible2 = /* @__PURE__ */ __name((self) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return self;
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return self;
}), "uninterruptible");
var setInterruptible = /* @__PURE__ */ makePrimitive({
  op: "SetInterruptible",
  ensure(fiber) {
    fiber.interruptible = this[args];
    if (fiber._interrupted && fiber.interruptible) {
      return () => exitInterrupt2;
    }
  }
});
var interruptible3 = /* @__PURE__ */ __name((self) => withMicroFiber((fiber) => {
  if (fiber.interruptible) return self;
  fiber.interruptible = true;
  fiber._stack.push(setInterruptible(false));
  if (fiber._interrupted) return exitInterrupt2;
  return self;
}), "interruptible");
var uninterruptibleMask2 = /* @__PURE__ */ __name((f) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return f(identity);
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return f(interruptible3);
}), "uninterruptibleMask");
var all7 = /* @__PURE__ */ __name((arg, options) => {
  if (Array.isArray(arg) || isIterable(arg)) {
    return forEach11(arg, identity, options);
  } else if (options?.discard) {
    return forEach11(Object.values(arg), identity, options);
  }
  return suspend2(() => {
    const out = {};
    return as5(forEach11(Object.entries(arg), ([key, effect4]) => map17(effect4, (value2) => {
      out[key] = value2;
    }), {
      discard: true,
      concurrency: options?.concurrency
    }), out);
  });
}, "all");
var whileLoop2 = /* @__PURE__ */ makePrimitive({
  op: "While",
  contA(value2, fiber) {
    this[args].step(value2);
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  },
  eval(fiber) {
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  }
});
var forEach11 = /* @__PURE__ */ __name((iterable, f, options) => withMicroFiber((parent) => {
  const concurrencyOption = options?.concurrency === "inherit" ? parent.getRef(CurrentConcurrency) : options?.concurrency ?? 1;
  const concurrency = concurrencyOption === "unbounded" ? Number.POSITIVE_INFINITY : Math.max(1, concurrencyOption);
  const items = fromIterable2(iterable);
  let length5 = items.length;
  if (length5 === 0) {
    return options?.discard ? void_5 : succeed4([]);
  }
  const out = options?.discard ? void 0 : new Array(length5);
  let index = 0;
  if (concurrency === 1) {
    return as5(whileLoop2({
      while: /* @__PURE__ */ __name(() => index < items.length, "while"),
      body: /* @__PURE__ */ __name(() => f(items[index], index), "body"),
      step: out ? (b) => out[index++] = b : (_) => index++
    }), out);
  }
  return async((resume2) => {
    const fibers = /* @__PURE__ */ new Set();
    let result = void 0;
    let inProgress = 0;
    let doneCount = 0;
    let pumping = false;
    let interrupted4 = false;
    function pump() {
      pumping = true;
      while (inProgress < concurrency && index < length5) {
        const currentIndex = index;
        const item = items[currentIndex];
        index++;
        inProgress++;
        try {
          const child = unsafeFork(parent, f(item, currentIndex), true, true);
          fibers.add(child);
          child.addObserver((exit4) => {
            fibers.delete(child);
            if (interrupted4) {
              return;
            } else if (exit4._tag === "Failure") {
              if (result === void 0) {
                result = exit4;
                length5 = index;
                fibers.forEach((fiber) => fiber.unsafeInterrupt());
              }
            } else if (out !== void 0) {
              out[currentIndex] = exit4.value;
            }
            doneCount++;
            inProgress--;
            if (doneCount === length5) {
              resume2(result ?? succeed4(out));
            } else if (!pumping && inProgress < concurrency) {
              pump();
            }
          });
        } catch (err) {
          result = exitDie2(err);
          length5 = index;
          fibers.forEach((fiber) => fiber.unsafeInterrupt());
        }
      }
      pumping = false;
    }
    __name(pump, "pump");
    pump();
    return suspend2(() => {
      interrupted4 = true;
      index = length5;
      return fiberInterruptAll(fibers);
    });
  });
}), "forEach");
var filter12 = /* @__PURE__ */ __name((iterable, f, options) => filterMap10(iterable, (a) => map17(f(a), (pass) => {
  pass = options?.negate ? !pass : pass;
  return pass ? some3(a) : none2();
}), options), "filter");
var filterMap10 = /* @__PURE__ */ __name((iterable, f, options) => suspend2(() => {
  const out = [];
  return as5(forEach11(iterable, (a) => map17(f(a), (o) => {
    if (o._tag === "Some") {
      out.push(o.value);
    }
  }), {
    discard: true,
    concurrency: options?.concurrency
  }), out);
}), "filterMap");
var Do5 = /* @__PURE__ */ succeed4({});
var bindTo6 = /* @__PURE__ */ bindTo(map17);
var bind6 = /* @__PURE__ */ bind(map17, flatMap14);
var let_6 = /* @__PURE__ */ let_(map17);
var fork = /* @__PURE__ */ __name((self) => withMicroFiber((fiber) => {
  fiberMiddleware.interruptChildren ??= fiberInterruptChildren;
  return succeed4(unsafeFork(fiber, self));
}), "fork");
var unsafeFork = /* @__PURE__ */ __name((parent, effect4, immediate = false, daemon = false) => {
  const child = new MicroFiberImpl(parent.context, parent.interruptible);
  if (!daemon) {
    parent.children().add(child);
    child.addObserver(() => parent.children().delete(child));
  }
  if (immediate) {
    child.evaluate(effect4);
  } else {
    parent.getRef(CurrentScheduler).scheduleTask(() => child.evaluate(effect4), 0);
  }
  return child;
}, "unsafeFork");
var forkDaemon = /* @__PURE__ */ __name((self) => withMicroFiber((fiber) => succeed4(unsafeFork(fiber, self, false, true))), "forkDaemon");
var forkIn = /* @__PURE__ */ dual(2, (self, scope6) => uninterruptibleMask2((restore) => flatMap14(scope6.fork, (scope7) => tap3(restore(forkDaemon(onExit2(self, (exit4) => scope7.close(exit4)))), (fiber) => scope7.addFinalizer((_) => fiberInterrupt(fiber))))));
var forkScoped = /* @__PURE__ */ __name((self) => flatMap14(scope, (scope6) => forkIn(self, scope6)), "forkScoped");
var runFork = /* @__PURE__ */ __name((effect4, options) => {
  const fiber = new MicroFiberImpl(CurrentScheduler.context(options?.scheduler ?? new MicroSchedulerDefault()));
  fiber.evaluate(effect4);
  if (options?.signal) {
    if (options.signal.aborted) {
      fiber.unsafeInterrupt();
    } else {
      const abort = /* @__PURE__ */ __name(() => fiber.unsafeInterrupt(), "abort");
      options.signal.addEventListener("abort", abort, {
        once: true
      });
      fiber.addObserver(() => options.signal.removeEventListener("abort", abort));
    }
  }
  return fiber;
}, "runFork");
var runPromiseExit = /* @__PURE__ */ __name((effect4, options) => new Promise((resolve, _reject) => {
  const handle = runFork(effect4, options);
  handle.addObserver(resolve);
}), "runPromiseExit");
var runPromise = /* @__PURE__ */ __name((effect4, options) => runPromiseExit(effect4, options).then((exit4) => {
  if (exit4._tag === "Failure") {
    throw exit4.cause;
  }
  return exit4.value;
}), "runPromise");
var runSyncExit = /* @__PURE__ */ __name((effect4) => {
  const scheduler = new MicroSchedulerDefault();
  const fiber = runFork(effect4, {
    scheduler
  });
  scheduler.flush();
  return fiber._exit ?? exitDie2(fiber);
}, "runSyncExit");
var runSync = /* @__PURE__ */ __name((effect4) => {
  const exit4 = runSyncExit(effect4);
  if (exit4._tag === "Failure") throw exit4.cause;
  return exit4.value;
}, "runSync");
var YieldableError2 = /* @__PURE__ */ (function() {
  class YieldableError4 extends globalThis.Error {
    static {
      __name(this, "YieldableError");
    }
  }
  Object.assign(YieldableError4.prototype, MicroProto, StructuralPrototype, {
    [identifier]: "Failure",
    [evaluate]() {
      return fail5(this);
    },
    toString() {
      return this.message ? `${this.name}: ${this.message}` : this.name;
    },
    toJSON() {
      return {
        ...this
      };
    },
    [NodeInspectSymbol]() {
      const stack = this.stack;
      if (stack) {
        return `${this.toString()}
${stack.split("\n").slice(1).join("\n")}`;
      }
      return this.toString();
    }
  });
  return YieldableError4;
})();
var Error3 = /* @__PURE__ */ (function() {
  return class extends YieldableError2 {
    constructor(args2) {
      super();
      if (args2) {
        Object.assign(this, args2);
      }
    }
  };
})();
var TaggedError = /* @__PURE__ */ __name((tag) => {
  class Base4 extends Error3 {
    static {
      __name(this, "Base");
    }
    _tag = tag;
  }
  ;
  Base4.prototype.name = tag;
  return Base4;
}, "TaggedError");
var NoSuchElementException2 = class extends (/* @__PURE__ */ TaggedError("NoSuchElementException")) {
  static {
    __name(this, "NoSuchElementException");
  }
};
var TimeoutException2 = class extends (/* @__PURE__ */ TaggedError("TimeoutException")) {
  static {
    __name(this, "TimeoutException");
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Scheduler.js
var PriorityBuckets = class {
  static {
    __name(this, "PriorityBuckets");
  }
  /**
   * @since 2.0.0
   */
  buckets = [];
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    const length5 = this.buckets.length;
    let bucket = void 0;
    let index = 0;
    for (; index < length5; index++) {
      if (this.buckets[index][0] <= priority) {
        bucket = this.buckets[index];
      } else {
        break;
      }
    }
    if (bucket && bucket[0] === priority) {
      bucket[1].push(task);
    } else if (index === length5) {
      this.buckets.push([priority, [task]]);
    } else {
      this.buckets.splice(index, 0, [priority, [task]]);
    }
  }
};
var MixedScheduler = class {
  static {
    __name(this, "MixedScheduler");
  }
  maxNextTickBeforeTimer;
  /**
   * @since 2.0.0
   */
  running = false;
  /**
   * @since 2.0.0
   */
  tasks = /* @__PURE__ */ new PriorityBuckets();
  constructor(maxNextTickBeforeTimer) {
    this.maxNextTickBeforeTimer = maxNextTickBeforeTimer;
  }
  /**
   * @since 2.0.0
   */
  starveInternal(depth) {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (this.tasks.buckets.length === 0) {
      this.running = false;
    } else {
      this.starve(depth);
    }
  }
  /**
   * @since 2.0.0
   */
  starve(depth = 0) {
    if (depth >= this.maxNextTickBeforeTimer) {
      setTimeout(() => this.starveInternal(0), 0);
    } else {
      Promise.resolve(void 0).then(() => this.starveInternal(depth + 1));
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    this.tasks.scheduleTask(task, priority);
    if (!this.running) {
      this.running = true;
      this.starve();
    }
  }
};
var defaultScheduler = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Scheduler/defaultScheduler"), () => new MixedScheduler(2048));
var SyncScheduler = class {
  static {
    __name(this, "SyncScheduler");
  }
  /**
   * @since 2.0.0
   */
  tasks = /* @__PURE__ */ new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  flush() {
    while (this.tasks.buckets.length > 0) {
      const tasks = this.tasks.buckets;
      this.tasks.buckets = [];
      for (const [_, toRun] of tasks) {
        for (let i = 0; i < toRun.length; i++) {
          toRun[i]();
        }
      }
    }
    this.deferred = true;
  }
};
var ControlledScheduler = class {
  static {
    __name(this, "ControlledScheduler");
  }
  /**
   * @since 2.0.0
   */
  tasks = /* @__PURE__ */ new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  step() {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
  }
};
var makeMatrix = /* @__PURE__ */ __name((...record) => {
  const index = record.sort(([p0], [p1]) => p0 < p1 ? -1 : p0 > p1 ? 1 : 0);
  return {
    shouldYield(fiber) {
      for (const scheduler of record) {
        const priority = scheduler[1].shouldYield(fiber);
        if (priority !== false) {
          return priority;
        }
      }
      return false;
    },
    scheduleTask(task, priority) {
      let scheduler = void 0;
      for (const i of index) {
        if (priority >= i[0]) {
          scheduler = i[1];
        } else {
          return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
        }
      }
      return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
    }
  };
}, "makeMatrix");
var defaultShouldYield = /* @__PURE__ */ __name((fiber) => {
  return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
}, "defaultShouldYield");
var make38 = /* @__PURE__ */ __name((scheduleTask, shouldYield = defaultShouldYield) => ({
  scheduleTask,
  shouldYield
}), "make");
var makeBatched = /* @__PURE__ */ __name((callback, shouldYield = defaultShouldYield) => {
  let running4 = false;
  const tasks = new PriorityBuckets();
  const starveInternal = /* @__PURE__ */ __name(() => {
    const tasksToRun = tasks.buckets;
    tasks.buckets = [];
    for (const [_, toRun] of tasksToRun) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (tasks.buckets.length === 0) {
      running4 = false;
    } else {
      starve();
    }
  }, "starveInternal");
  const starve = /* @__PURE__ */ __name(() => callback(starveInternal), "starve");
  return make38((task, priority) => {
    tasks.scheduleTask(task, priority);
    if (!running4) {
      running4 = true;
      starve();
    }
  }, shouldYield);
}, "makeBatched");
var timer = /* @__PURE__ */ __name((ms, shouldYield = defaultShouldYield) => make38((task) => setTimeout(task, ms), shouldYield), "timer");
var timerBatched = /* @__PURE__ */ __name((ms, shouldYield = defaultShouldYield) => makeBatched((task) => setTimeout(task, ms), shouldYield), "timerBatched");
var currentScheduler = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentScheduler"), () => fiberRefUnsafeMake(defaultScheduler));
var withScheduler = /* @__PURE__ */ dual(2, (self, scheduler) => fiberRefLocally(self, currentScheduler, scheduler));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/completedRequestMap.js
var currentRequestMap = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentRequestMap"), () => fiberRefUnsafeMake(/* @__PURE__ */ new Map()));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/concurrency.js
var match12 = /* @__PURE__ */ __name((concurrency, sequential5, unbounded10, bounded10) => {
  switch (concurrency) {
    case void 0:
      return sequential5();
    case "unbounded":
      return unbounded10();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" ? unbounded10() : concurrency2 > 1 ? bounded10(concurrency2) : sequential5());
    default:
      return concurrency > 1 ? bounded10(concurrency) : sequential5();
  }
}, "match");
var matchSimple = /* @__PURE__ */ __name((concurrency, sequential5, concurrent) => {
  switch (concurrency) {
    case void 0:
      return sequential5();
    case "unbounded":
      return concurrent();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" || concurrency2 > 1 ? concurrent() : sequential5());
    default:
      return concurrency > 1 ? concurrent() : sequential5();
  }
}, "matchSimple");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberMessage.js
var OP_INTERRUPT_SIGNAL = "InterruptSignal";
var OP_STATEFUL = "Stateful";
var OP_RESUME = "Resume";
var OP_YIELD_NOW = "YieldNow";
var interruptSignal = /* @__PURE__ */ __name((cause3) => ({
  _tag: OP_INTERRUPT_SIGNAL,
  cause: cause3
}), "interruptSignal");
var stateful = /* @__PURE__ */ __name((onFiber) => ({
  _tag: OP_STATEFUL,
  onFiber
}), "stateful");
var resume = /* @__PURE__ */ __name((effect4) => ({
  _tag: OP_RESUME,
  effect: effect4
}), "resume");
var yieldNow3 = /* @__PURE__ */ __name(() => ({
  _tag: OP_YIELD_NOW
}), "yieldNow");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberScope.js
var FiberScopeSymbolKey = "effect/FiberScope";
var FiberScopeTypeId = /* @__PURE__ */ Symbol.for(FiberScopeSymbolKey);
var Global = class {
  static {
    __name(this, "Global");
  }
  [FiberScopeTypeId] = FiberScopeTypeId;
  fiberId = none4;
  roots = /* @__PURE__ */ new Set();
  add(_runtimeFlags, child) {
    this.roots.add(child);
    child.addObserver(() => {
      this.roots.delete(child);
    });
  }
};
var Local = class {
  static {
    __name(this, "Local");
  }
  fiberId;
  parent;
  [FiberScopeTypeId] = FiberScopeTypeId;
  constructor(fiberId4, parent) {
    this.fiberId = fiberId4;
    this.parent = parent;
  }
  add(_runtimeFlags, child) {
    this.parent.tell(stateful((parentFiber) => {
      parentFiber.addChild(child);
      child.addObserver(() => {
        parentFiber.removeChild(child);
      });
    }));
  }
};
var unsafeMake9 = /* @__PURE__ */ __name((fiber) => {
  return new Local(fiber.id(), fiber);
}, "unsafeMake");
var globalScope = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberScope/Global"), () => new Global());

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiber.js
var FiberSymbolKey = "effect/Fiber";
var FiberTypeId = /* @__PURE__ */ Symbol.for(FiberSymbolKey);
var fiberVariance2 = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var fiberProto = {
  [FiberTypeId]: fiberVariance2,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var RuntimeFiberSymbolKey = "effect/Fiber";
var RuntimeFiberTypeId = /* @__PURE__ */ Symbol.for(RuntimeFiberSymbolKey);
var Order5 = /* @__PURE__ */ pipe(/* @__PURE__ */ tuple3(Order, Order), /* @__PURE__ */ mapInput3((fiber) => [fiber.id().startTimeMillis, fiber.id().id]));
var isFiber = /* @__PURE__ */ __name((u) => hasProperty(u, FiberTypeId), "isFiber");
var isRuntimeFiber = /* @__PURE__ */ __name((self) => RuntimeFiberTypeId in self, "isRuntimeFiber");
var _await2 = /* @__PURE__ */ __name((self) => self.await, "_await");
var children = /* @__PURE__ */ __name((self) => self.children, "children");
var done5 = /* @__PURE__ */ __name((exit4) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join3(this);
    },
    ...fiberProto,
    id: /* @__PURE__ */ __name(() => none4, "id"),
    await: succeed(exit4),
    children: succeed([]),
    inheritAll: void_3,
    poll: succeed(some3(exit4)),
    interruptAsFork: /* @__PURE__ */ __name(() => void_3, "interruptAsFork")
  };
  return _fiber;
}, "done");
var dump = /* @__PURE__ */ __name((self) => map14(self.status, (status3) => ({
  id: self.id(),
  status: status3
})), "dump");
var dumpAll = /* @__PURE__ */ __name((fibers) => forEachSequential(fibers, dump), "dumpAll");
var fail6 = /* @__PURE__ */ __name((error2) => done5(fail4(error2)), "fail");
var failCause5 = /* @__PURE__ */ __name((cause3) => done5(failCause3(cause3)), "failCause");
var fromEffect = /* @__PURE__ */ __name((effect4) => map14(exit(effect4), done5), "fromEffect");
var id = /* @__PURE__ */ __name((self) => self.id(), "id");
var inheritAll = /* @__PURE__ */ __name((self) => self.inheritAll, "inheritAll");
var interrupted = /* @__PURE__ */ __name((fiberId4) => done5(interrupt4(fiberId4)), "interrupted");
var interruptAll = /* @__PURE__ */ __name((fibers) => flatMap12(fiberId, (fiberId4) => pipe(fibers, interruptAllAs(fiberId4))), "interruptAll");
var interruptAllAs = /* @__PURE__ */ dual(2, /* @__PURE__ */ fnUntraced(function* (fibers, fiberId4) {
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber)) {
      fiber.unsafeInterruptAsFork(fiberId4);
      continue;
    }
    yield* fiber.interruptAsFork(fiberId4);
  }
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber) && fiber.unsafePoll()) {
      continue;
    }
    yield* fiber.await;
  }
}));
var interruptAsFork = /* @__PURE__ */ dual(2, (self, fiberId4) => self.interruptAsFork(fiberId4));
var join3 = /* @__PURE__ */ __name((self) => zipLeft2(flatten7(self.await), self.inheritAll), "join");
var map18 = /* @__PURE__ */ dual(2, (self, f) => mapEffect2(self, (a) => sync(() => f(a))));
var mapEffect2 = /* @__PURE__ */ dual(2, (self, f) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join3(this);
    },
    ...fiberProto,
    id: /* @__PURE__ */ __name(() => self.id(), "id"),
    await: flatMap12(self.await, forEachEffect(f)),
    children: self.children,
    inheritAll: self.inheritAll,
    poll: flatMap12(self.poll, (result) => {
      switch (result._tag) {
        case "None":
          return succeed(none2());
        case "Some":
          return pipe(forEachEffect(result.value, f), map14(some3));
      }
    }),
    interruptAsFork: /* @__PURE__ */ __name((id3) => self.interruptAsFork(id3), "interruptAsFork")
  };
  return _fiber;
});
var mapFiber = /* @__PURE__ */ dual(2, (self, f) => map14(self.await, match7({
  onFailure: /* @__PURE__ */ __name((cause3) => failCause5(cause3), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => f(a), "onSuccess")
})));
var match13 = /* @__PURE__ */ dual(2, (self, {
  onFiber,
  onRuntimeFiber
}) => {
  if (isRuntimeFiber(self)) {
    return onRuntimeFiber(self);
  }
  return onFiber(self);
});
var _never = {
  ...CommitPrototype,
  commit() {
    return join3(this);
  },
  ...fiberProto,
  id: /* @__PURE__ */ __name(() => none4, "id"),
  await: never,
  children: /* @__PURE__ */ succeed([]),
  inheritAll: never,
  poll: /* @__PURE__ */ succeed(/* @__PURE__ */ none2()),
  interruptAsFork: /* @__PURE__ */ __name(() => never, "interruptAsFork")
};
var never3 = _never;
var orElse5 = /* @__PURE__ */ dual(2, (self, that) => ({
  ...CommitPrototype,
  commit() {
    return join3(this);
  },
  ...fiberProto,
  id: /* @__PURE__ */ __name(() => getOrElse6(self.id(), that.id()), "id"),
  await: zipWith6(self.await, that.await, (exit1, exit22) => isSuccess(exit1) ? exit1 : exit22),
  children: self.children,
  inheritAll: zipRight2(that.inheritAll, self.inheritAll),
  poll: zipWith6(self.poll, that.poll, (option1, option22) => {
    switch (option1._tag) {
      case "None": {
        return none2();
      }
      case "Some": {
        return isSuccess(option1.value) ? option1 : option22;
      }
    }
  }),
  interruptAsFork: /* @__PURE__ */ __name((id3) => pipe(interruptAsFiber(self, id3), zipRight2(pipe(that, interruptAsFiber(id3))), asVoid2), "interruptAsFork")
}));
var orElseEither3 = /* @__PURE__ */ dual(2, (self, that) => orElse5(map18(self, left2), map18(that, right2)));
var poll3 = /* @__PURE__ */ __name((self) => self.poll, "poll");
var parseMs = /* @__PURE__ */ __name((milliseconds) => {
  const roundTowardsZero = milliseconds > 0 ? Math.floor : Math.ceil;
  return {
    days: roundTowardsZero(milliseconds / 864e5),
    hours: roundTowardsZero(milliseconds / 36e5) % 24,
    minutes: roundTowardsZero(milliseconds / 6e4) % 60,
    seconds: roundTowardsZero(milliseconds / 1e3) % 60,
    milliseconds: roundTowardsZero(milliseconds) % 1e3,
    microseconds: roundTowardsZero(milliseconds * 1e3) % 1e3,
    nanoseconds: roundTowardsZero(milliseconds * 1e6) % 1e3
  };
}, "parseMs");
var renderStatus = /* @__PURE__ */ __name((status3) => {
  if (isDone3(status3)) {
    return "Done";
  }
  if (isRunning2(status3)) {
    return "Running";
  }
  const isInterruptible = interruptible(status3.runtimeFlags) ? "interruptible" : "uninterruptible";
  return `Suspended(${isInterruptible})`;
}, "renderStatus");
var pretty2 = /* @__PURE__ */ __name((self) => flatMap12(currentTimeMillis2, (now2) => map14(dump(self), (dump3) => {
  const time2 = now2 - dump3.id.startTimeMillis;
  const {
    days: days2,
    hours: hours2,
    milliseconds,
    minutes: minutes2,
    seconds: seconds2
  } = parseMs(time2);
  const lifeMsg = (days2 === 0 ? "" : `${days2}d`) + (days2 === 0 && hours2 === 0 ? "" : `${hours2}h`) + (days2 === 0 && hours2 === 0 && minutes2 === 0 ? "" : `${minutes2}m`) + (days2 === 0 && hours2 === 0 && minutes2 === 0 && seconds2 === 0 ? "" : `${seconds2}s`) + `${milliseconds}ms`;
  const waitMsg = isSuspended2(dump3.status) ? (() => {
    const ids3 = ids2(dump3.status.blockingOn);
    return size6(ids3) > 0 ? `waiting on ` + Array.from(ids3).map((id3) => `${id3}`).join(", ") : "";
  })() : "";
  const statusMsg = renderStatus(dump3.status);
  return `[Fiber](#${dump3.id.id}) (${lifeMsg}) ${waitMsg}
   Status: ${statusMsg}`;
})), "pretty");
var unsafeRoots = /* @__PURE__ */ __name(() => Array.from(globalScope.roots), "unsafeRoots");
var roots = /* @__PURE__ */ sync(unsafeRoots);
var status = /* @__PURE__ */ __name((self) => self.status, "status");
var succeed5 = /* @__PURE__ */ __name((value2) => done5(succeed3(value2)), "succeed");
var void_6 = /* @__PURE__ */ succeed5(void 0);
var currentFiberURI = "effect/FiberCurrent";
var getCurrentFiber = /* @__PURE__ */ __name(() => fromNullable2(globalThis[currentFiberURI]), "getCurrentFiber");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/logger.js
var LoggerSymbolKey = "effect/Logger";
var LoggerTypeId = /* @__PURE__ */ Symbol.for(LoggerSymbolKey);
var loggerVariance = {
  /* c8 ignore next */
  _Message: /* @__PURE__ */ __name((_) => _, "_Message"),
  /* c8 ignore next */
  _Output: /* @__PURE__ */ __name((_) => _, "_Output")
};
var makeLogger = /* @__PURE__ */ __name((log4) => ({
  [LoggerTypeId]: loggerVariance,
  log: log4,
  pipe() {
    return pipeArguments(this, arguments);
  }
}), "makeLogger");
var mapInput4 = /* @__PURE__ */ dual(2, (self, f) => makeLogger((options) => self.log({
  ...options,
  message: f(options.message)
})));
var mapInputOptions = /* @__PURE__ */ dual(2, (self, f) => makeLogger((options) => self.log(f(options))));
var filterLogLevel = /* @__PURE__ */ dual(2, (self, f) => makeLogger((options) => f(options.logLevel) ? some3(self.log(options)) : none2()));
var map19 = /* @__PURE__ */ dual(2, (self, f) => makeLogger((options) => f(self.log(options))));
var none7 = {
  [LoggerTypeId]: loggerVariance,
  log: constVoid,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var simple = /* @__PURE__ */ __name((log4) => ({
  [LoggerTypeId]: loggerVariance,
  log: /* @__PURE__ */ __name(({
    message
  }) => log4(message), "log"),
  pipe() {
    return pipeArguments(this, arguments);
  }
}), "simple");
var succeed6 = /* @__PURE__ */ __name((value2) => {
  return simple(() => value2);
}, "succeed");
var sync4 = /* @__PURE__ */ __name((evaluate3) => {
  return simple(evaluate3);
}, "sync");
var zip8 = /* @__PURE__ */ dual(2, (self, that) => makeLogger((options) => [self.log(options), that.log(options)]));
var zipLeft4 = /* @__PURE__ */ dual(2, (self, that) => map19(zip8(self, that), (tuple5) => tuple5[0]));
var zipRight4 = /* @__PURE__ */ dual(2, (self, that) => map19(zip8(self, that), (tuple5) => tuple5[1]));
var textOnly = /^[^\s"=]*$/;
var format3 = /* @__PURE__ */ __name((quoteValue, whitespace) => ({
  annotations,
  cause: cause3,
  date,
  fiberId: fiberId4,
  logLevel,
  message,
  spans
}) => {
  const formatValue = /* @__PURE__ */ __name((value2) => value2.match(textOnly) ? value2 : quoteValue(value2), "formatValue");
  const format5 = /* @__PURE__ */ __name((label, value2) => `${formatLabel(label)}=${formatValue(value2)}`, "format");
  const append6 = /* @__PURE__ */ __name((label, value2) => " " + format5(label, value2), "append");
  let out = format5("timestamp", date.toISOString());
  out += append6("level", logLevel.label);
  out += append6("fiber", threadName(fiberId4));
  const messages = ensure(message);
  for (let i = 0; i < messages.length; i++) {
    out += append6("message", toStringUnknown(messages[i], whitespace));
  }
  if (!isEmptyType(cause3)) {
    out += append6("cause", pretty(cause3, {
      renderErrorCause: true
    }));
  }
  for (const span4 of spans) {
    out += " " + render3(date.getTime())(span4);
  }
  for (const [label, value2] of annotations) {
    out += append6(label, toStringUnknown(value2, whitespace));
  }
  return out;
}, "format");
var escapeDoubleQuotes = /* @__PURE__ */ __name((s) => `"${s.replace(/\\([\s\S])|(")/g, "\\$1$2")}"`, "escapeDoubleQuotes");
var stringLogger = /* @__PURE__ */ makeLogger(/* @__PURE__ */ format3(escapeDoubleQuotes));
var logfmtLogger = /* @__PURE__ */ makeLogger(/* @__PURE__ */ format3(JSON.stringify, 0));
var structuredLogger = /* @__PURE__ */ makeLogger(({
  annotations,
  cause: cause3,
  date,
  fiberId: fiberId4,
  logLevel,
  message,
  spans
}) => {
  const now2 = date.getTime();
  const annotationsObj = {};
  const spansObj = {};
  if (size7(annotations) > 0) {
    for (const [k, v] of annotations) {
      annotationsObj[k] = structuredMessage(v);
    }
  }
  if (isCons(spans)) {
    for (const span4 of spans) {
      spansObj[span4.label] = now2 - span4.startTime;
    }
  }
  const messageArr = ensure(message);
  return {
    message: messageArr.length === 1 ? structuredMessage(messageArr[0]) : messageArr.map(structuredMessage),
    logLevel: logLevel.label,
    timestamp: date.toISOString(),
    cause: isEmpty7(cause3) ? void 0 : pretty(cause3, {
      renderErrorCause: true
    }),
    annotations: annotationsObj,
    spans: spansObj,
    fiberId: threadName(fiberId4)
  };
});
var structuredMessage = /* @__PURE__ */ __name((u) => {
  switch (typeof u) {
    case "bigint":
    case "function":
    case "symbol": {
      return String(u);
    }
    default: {
      return toJSON(u);
    }
  }
}, "structuredMessage");
var jsonLogger = /* @__PURE__ */ map19(structuredLogger, stringifyCircular);
var isLogger = /* @__PURE__ */ __name((u) => {
  return typeof u === "object" && u != null && LoggerTypeId in u;
}, "isLogger");
var withColor = /* @__PURE__ */ __name((text, ...colors2) => {
  let out = "";
  for (let i = 0; i < colors2.length; i++) {
    out += `\x1B[${colors2[i]}m`;
  }
  return out + text + "\x1B[0m";
}, "withColor");
var withColorNoop = /* @__PURE__ */ __name((text, ..._colors) => text, "withColorNoop");
var colors = {
  bold: "1",
  red: "31",
  green: "32",
  yellow: "33",
  blue: "34",
  cyan: "36",
  white: "37",
  gray: "90",
  black: "30",
  bgBrightRed: "101"
};
var logLevelColors = {
  None: [],
  All: [],
  Trace: [colors.gray],
  Debug: [colors.blue],
  Info: [colors.green],
  Warning: [colors.yellow],
  Error: [colors.red],
  Fatal: [colors.bgBrightRed, colors.black]
};
var logLevelStyle = {
  None: "",
  All: "",
  Trace: "color:gray",
  Debug: "color:blue",
  Info: "color:green",
  Warning: "color:orange",
  Error: "color:red",
  Fatal: "background-color:red;color:white"
};
var defaultDateFormat = /* @__PURE__ */ __name((date) => `${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}:${date.getSeconds().toString().padStart(2, "0")}.${date.getMilliseconds().toString().padStart(3, "0")}`, "defaultDateFormat");
var hasProcessStdout = typeof process === "object" && process !== null && typeof process.stdout === "object" && process.stdout !== null;
var processStdoutIsTTY = hasProcessStdout && process.stdout.isTTY === true;
var hasProcessStdoutOrDeno = hasProcessStdout || "Deno" in globalThis;
var prettyLogger = /* @__PURE__ */ __name((options) => {
  const mode_ = options?.mode ?? "auto";
  const mode = mode_ === "auto" ? hasProcessStdoutOrDeno ? "tty" : "browser" : mode_;
  const isBrowser = mode === "browser";
  const showColors = typeof options?.colors === "boolean" ? options.colors : processStdoutIsTTY || isBrowser;
  const formatDate2 = options?.formatDate ?? defaultDateFormat;
  return isBrowser ? prettyLoggerBrowser({
    colors: showColors,
    formatDate: formatDate2
  }) : prettyLoggerTty({
    colors: showColors,
    formatDate: formatDate2,
    stderr: options?.stderr === true
  });
}, "prettyLogger");
var prettyLoggerTty = /* @__PURE__ */ __name((options) => {
  const color = options.colors ? withColor : withColorNoop;
  return makeLogger(({
    annotations,
    cause: cause3,
    context: context11,
    date,
    fiberId: fiberId4,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context11, currentServices);
    const console4 = get4(services, consoleTag).unsafe;
    const log4 = options.stderr === true ? console4.error : console4.log;
    const message = ensure(message_);
    let firstLine = color(`[${options.formatDate(date)}]`, colors.white) + ` ${color(logLevel.label, ...logLevelColors[logLevel._tag])} (${threadName(fiberId4)})`;
    if (isCons(spans)) {
      const now2 = date.getTime();
      const render5 = render3(now2);
      for (const span4 of spans) {
        firstLine += " " + render5(span4);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += " " + color(firstMaybeString, colors.bold, colors.cyan);
        messageIndex++;
      }
    }
    log4(firstLine);
    console4.group();
    if (!isEmpty7(cause3)) {
      log4(pretty(cause3, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        log4(redact(message[messageIndex]));
      }
    }
    if (size7(annotations) > 0) {
      for (const [key, value2] of annotations) {
        log4(color(`${key}:`, colors.bold, colors.white), redact(value2));
      }
    }
    console4.groupEnd();
  });
}, "prettyLoggerTty");
var prettyLoggerBrowser = /* @__PURE__ */ __name((options) => {
  const color = options.colors ? "%c" : "";
  return makeLogger(({
    annotations,
    cause: cause3,
    context: context11,
    date,
    fiberId: fiberId4,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context11, currentServices);
    const console4 = get4(services, consoleTag).unsafe;
    const message = ensure(message_);
    let firstLine = `${color}[${options.formatDate(date)}]`;
    const firstParams = [];
    if (options.colors) {
      firstParams.push("color:gray");
    }
    firstLine += ` ${color}${logLevel.label}${color} (${threadName(fiberId4)})`;
    if (options.colors) {
      firstParams.push(logLevelStyle[logLevel._tag], "");
    }
    if (isCons(spans)) {
      const now2 = date.getTime();
      const render5 = render3(now2);
      for (const span4 of spans) {
        firstLine += " " + render5(span4);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += ` ${color}${firstMaybeString}`;
        if (options.colors) {
          firstParams.push("color:deepskyblue");
        }
        messageIndex++;
      }
    }
    console4.groupCollapsed(firstLine, ...firstParams);
    if (!isEmpty7(cause3)) {
      console4.error(pretty(cause3, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        console4.log(redact(message[messageIndex]));
      }
    }
    if (size7(annotations) > 0) {
      for (const [key, value2] of annotations) {
        const redacted = redact(value2);
        if (options.colors) {
          console4.log(`%c${key}:`, "color:gray", redacted);
        } else {
          console4.log(`${key}:`, redacted);
        }
      }
    }
    console4.groupEnd();
  });
}, "prettyLoggerBrowser");
var prettyLoggerDefault = /* @__PURE__ */ globalValue("effect/Logger/prettyLoggerDefault", () => prettyLogger());

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/boundaries.js
var MetricBoundariesSymbolKey = "effect/MetricBoundaries";
var MetricBoundariesTypeId = /* @__PURE__ */ Symbol.for(MetricBoundariesSymbolKey);
var MetricBoundariesImpl = class {
  static {
    __name(this, "MetricBoundariesImpl");
  }
  values;
  [MetricBoundariesTypeId] = MetricBoundariesTypeId;
  constructor(values10) {
    this.values = values10;
    this._hash = pipe(string2(MetricBoundariesSymbolKey), combine2(array2(this.values)));
  }
  _hash;
  [symbol2]() {
    return this._hash;
  }
  [symbol3](u) {
    return isMetricBoundaries(u) && equals(this.values, u.values);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricBoundaries = /* @__PURE__ */ __name((u) => hasProperty(u, MetricBoundariesTypeId), "isMetricBoundaries");
var fromIterable11 = /* @__PURE__ */ __name((iterable) => {
  const values10 = pipe(iterable, appendAll2(of3(Number.POSITIVE_INFINITY)), dedupe);
  return new MetricBoundariesImpl(values10);
}, "fromIterable");
var linear = /* @__PURE__ */ __name((options) => pipe(makeBy2(options.count - 1, (i) => options.start + i * options.width), unsafeFromArray, fromIterable11), "linear");
var exponential = /* @__PURE__ */ __name((options) => pipe(makeBy2(options.count - 1, (i) => options.start * Math.pow(options.factor, i)), unsafeFromArray, fromIterable11), "exponential");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/keyType.js
var MetricKeyTypeSymbolKey = "effect/MetricKeyType";
var MetricKeyTypeTypeId = /* @__PURE__ */ Symbol.for(MetricKeyTypeSymbolKey);
var CounterKeyTypeSymbolKey = "effect/MetricKeyType/Counter";
var CounterKeyTypeTypeId = /* @__PURE__ */ Symbol.for(CounterKeyTypeSymbolKey);
var FrequencyKeyTypeSymbolKey = "effect/MetricKeyType/Frequency";
var FrequencyKeyTypeTypeId = /* @__PURE__ */ Symbol.for(FrequencyKeyTypeSymbolKey);
var GaugeKeyTypeSymbolKey = "effect/MetricKeyType/Gauge";
var GaugeKeyTypeTypeId = /* @__PURE__ */ Symbol.for(GaugeKeyTypeSymbolKey);
var HistogramKeyTypeSymbolKey = "effect/MetricKeyType/Histogram";
var HistogramKeyTypeTypeId = /* @__PURE__ */ Symbol.for(HistogramKeyTypeSymbolKey);
var SummaryKeyTypeSymbolKey = "effect/MetricKeyType/Summary";
var SummaryKeyTypeTypeId = /* @__PURE__ */ Symbol.for(SummaryKeyTypeSymbolKey);
var metricKeyTypeVariance = {
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out")
};
var CounterKeyType = class {
  static {
    __name(this, "CounterKeyType");
  }
  incremental;
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [CounterKeyTypeTypeId] = CounterKeyTypeTypeId;
  constructor(incremental, bigint3) {
    this.incremental = incremental;
    this.bigint = bigint3;
    this._hash = string2(CounterKeyTypeSymbolKey);
  }
  _hash;
  [symbol2]() {
    return this._hash;
  }
  [symbol3](that) {
    return isCounterKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var FrequencyKeyTypeHash = /* @__PURE__ */ string2(FrequencyKeyTypeSymbolKey);
var FrequencyKeyType = class {
  static {
    __name(this, "FrequencyKeyType");
  }
  preregisteredWords;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [FrequencyKeyTypeTypeId] = FrequencyKeyTypeTypeId;
  constructor(preregisteredWords) {
    this.preregisteredWords = preregisteredWords;
  }
  [symbol2]() {
    return FrequencyKeyTypeHash;
  }
  [symbol3](that) {
    return isFrequencyKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeKeyTypeHash = /* @__PURE__ */ string2(GaugeKeyTypeSymbolKey);
var GaugeKeyType = class {
  static {
    __name(this, "GaugeKeyType");
  }
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [GaugeKeyTypeTypeId] = GaugeKeyTypeTypeId;
  constructor(bigint3) {
    this.bigint = bigint3;
  }
  [symbol2]() {
    return GaugeKeyTypeHash;
  }
  [symbol3](that) {
    return isGaugeKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramKeyType = class {
  static {
    __name(this, "HistogramKeyType");
  }
  boundaries;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [HistogramKeyTypeTypeId] = HistogramKeyTypeTypeId;
  constructor(boundaries) {
    this.boundaries = boundaries;
    this._hash = pipe(string2(HistogramKeyTypeSymbolKey), combine2(hash(this.boundaries)));
  }
  _hash;
  [symbol2]() {
    return this._hash;
  }
  [symbol3](that) {
    return isHistogramKey(that) && equals(this.boundaries, that.boundaries);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryKeyType = class {
  static {
    __name(this, "SummaryKeyType");
  }
  maxAge;
  maxSize;
  error;
  quantiles;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [SummaryKeyTypeTypeId] = SummaryKeyTypeTypeId;
  constructor(maxAge, maxSize, error2, quantiles) {
    this.maxAge = maxAge;
    this.maxSize = maxSize;
    this.error = error2;
    this.quantiles = quantiles;
    this._hash = pipe(string2(SummaryKeyTypeSymbolKey), combine2(hash(this.maxAge)), combine2(hash(this.maxSize)), combine2(hash(this.error)), combine2(array2(this.quantiles)));
  }
  _hash;
  [symbol2]() {
    return this._hash;
  }
  [symbol3](that) {
    return isSummaryKey(that) && equals(this.maxAge, that.maxAge) && this.maxSize === that.maxSize && this.error === that.error && equals(this.quantiles, that.quantiles);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter = /* @__PURE__ */ __name((options) => new CounterKeyType(options?.incremental ?? false, options?.bigint ?? false), "counter");
var frequency = /* @__PURE__ */ __name((options) => new FrequencyKeyType(options?.preregisteredWords ?? []), "frequency");
var gauge = /* @__PURE__ */ __name((options) => new GaugeKeyType(options?.bigint ?? false), "gauge");
var histogram = /* @__PURE__ */ __name((boundaries) => {
  return new HistogramKeyType(boundaries);
}, "histogram");
var summary = /* @__PURE__ */ __name((options) => {
  return new SummaryKeyType(decode(options.maxAge), options.maxSize, options.error, options.quantiles);
}, "summary");
var isMetricKeyType = /* @__PURE__ */ __name((u) => hasProperty(u, MetricKeyTypeTypeId), "isMetricKeyType");
var isCounterKey = /* @__PURE__ */ __name((u) => hasProperty(u, CounterKeyTypeTypeId), "isCounterKey");
var isFrequencyKey = /* @__PURE__ */ __name((u) => hasProperty(u, FrequencyKeyTypeTypeId), "isFrequencyKey");
var isGaugeKey = /* @__PURE__ */ __name((u) => hasProperty(u, GaugeKeyTypeTypeId), "isGaugeKey");
var isHistogramKey = /* @__PURE__ */ __name((u) => hasProperty(u, HistogramKeyTypeTypeId), "isHistogramKey");
var isSummaryKey = /* @__PURE__ */ __name((u) => hasProperty(u, SummaryKeyTypeTypeId), "isSummaryKey");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/key.js
var MetricKeySymbolKey = "effect/MetricKey";
var MetricKeyTypeId = /* @__PURE__ */ Symbol.for(MetricKeySymbolKey);
var metricKeyVariance = {
  /* c8 ignore next */
  _Type: /* @__PURE__ */ __name((_) => _, "_Type")
};
var arrayEquivilence = /* @__PURE__ */ getEquivalence5(equals);
var MetricKeyImpl = class {
  static {
    __name(this, "MetricKeyImpl");
  }
  name;
  keyType;
  description;
  tags;
  [MetricKeyTypeId] = metricKeyVariance;
  constructor(name, keyType, description, tags = []) {
    this.name = name;
    this.keyType = keyType;
    this.description = description;
    this.tags = tags;
    this._hash = pipe(string2(this.name + this.description), combine2(hash(this.keyType)), combine2(array2(this.tags)));
  }
  _hash;
  [symbol2]() {
    return this._hash;
  }
  [symbol3](u) {
    return isMetricKey(u) && this.name === u.name && equals(this.keyType, u.keyType) && equals(this.description, u.description) && arrayEquivilence(this.tags, u.tags);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricKey = /* @__PURE__ */ __name((u) => hasProperty(u, MetricKeyTypeId), "isMetricKey");
var counter2 = /* @__PURE__ */ __name((name, options) => new MetricKeyImpl(name, counter(options), fromNullable2(options?.description)), "counter");
var frequency2 = /* @__PURE__ */ __name((name, options) => new MetricKeyImpl(name, frequency(options), fromNullable2(options?.description)), "frequency");
var gauge2 = /* @__PURE__ */ __name((name, options) => new MetricKeyImpl(name, gauge(options), fromNullable2(options?.description)), "gauge");
var histogram2 = /* @__PURE__ */ __name((name, boundaries, description) => new MetricKeyImpl(name, histogram(boundaries), fromNullable2(description)), "histogram");
var summary2 = /* @__PURE__ */ __name((options) => new MetricKeyImpl(options.name, summary(options), fromNullable2(options.description)), "summary");
var tagged = /* @__PURE__ */ dual(3, (self, key, value2) => taggedWithLabels(self, [make37(key, value2)]));
var taggedWithLabels = /* @__PURE__ */ dual(2, (self, extraTags) => extraTags.length === 0 ? self : new MetricKeyImpl(self.name, self.keyType, self.description, union2(self.tags, extraTags)));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/state.js
var MetricStateSymbolKey = "effect/MetricState";
var MetricStateTypeId = /* @__PURE__ */ Symbol.for(MetricStateSymbolKey);
var CounterStateSymbolKey = "effect/MetricState/Counter";
var CounterStateTypeId = /* @__PURE__ */ Symbol.for(CounterStateSymbolKey);
var FrequencyStateSymbolKey = "effect/MetricState/Frequency";
var FrequencyStateTypeId = /* @__PURE__ */ Symbol.for(FrequencyStateSymbolKey);
var GaugeStateSymbolKey = "effect/MetricState/Gauge";
var GaugeStateTypeId = /* @__PURE__ */ Symbol.for(GaugeStateSymbolKey);
var HistogramStateSymbolKey = "effect/MetricState/Histogram";
var HistogramStateTypeId = /* @__PURE__ */ Symbol.for(HistogramStateSymbolKey);
var SummaryStateSymbolKey = "effect/MetricState/Summary";
var SummaryStateTypeId = /* @__PURE__ */ Symbol.for(SummaryStateSymbolKey);
var metricStateVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var CounterState = class {
  static {
    __name(this, "CounterState");
  }
  count;
  [MetricStateTypeId] = metricStateVariance;
  [CounterStateTypeId] = CounterStateTypeId;
  constructor(count5) {
    this.count = count5;
  }
  [symbol2]() {
    return pipe(hash(CounterStateSymbolKey), combine2(hash(this.count)), cached(this));
  }
  [symbol3](that) {
    return isCounterState(that) && this.count === that.count;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var arrayEquals = /* @__PURE__ */ getEquivalence5(equals);
var FrequencyState = class {
  static {
    __name(this, "FrequencyState");
  }
  occurrences;
  [MetricStateTypeId] = metricStateVariance;
  [FrequencyStateTypeId] = FrequencyStateTypeId;
  constructor(occurrences) {
    this.occurrences = occurrences;
  }
  _hash;
  [symbol2]() {
    return pipe(string2(FrequencyStateSymbolKey), combine2(array2(fromIterable2(this.occurrences.entries()))), cached(this));
  }
  [symbol3](that) {
    return isFrequencyState(that) && arrayEquals(fromIterable2(this.occurrences.entries()), fromIterable2(that.occurrences.entries()));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeState = class {
  static {
    __name(this, "GaugeState");
  }
  value;
  [MetricStateTypeId] = metricStateVariance;
  [GaugeStateTypeId] = GaugeStateTypeId;
  constructor(value2) {
    this.value = value2;
  }
  [symbol2]() {
    return pipe(hash(GaugeStateSymbolKey), combine2(hash(this.value)), cached(this));
  }
  [symbol3](u) {
    return isGaugeState(u) && this.value === u.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramState = class {
  static {
    __name(this, "HistogramState");
  }
  buckets;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [HistogramStateTypeId] = HistogramStateTypeId;
  constructor(buckets, count5, min8, max10, sum4) {
    this.buckets = buckets;
    this.count = count5;
    this.min = min8;
    this.max = max10;
    this.sum = sum4;
  }
  [symbol2]() {
    return pipe(hash(HistogramStateSymbolKey), combine2(hash(this.buckets)), combine2(hash(this.count)), combine2(hash(this.min)), combine2(hash(this.max)), combine2(hash(this.sum)), cached(this));
  }
  [symbol3](that) {
    return isHistogramState(that) && equals(this.buckets, that.buckets) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryState = class {
  static {
    __name(this, "SummaryState");
  }
  error;
  quantiles;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [SummaryStateTypeId] = SummaryStateTypeId;
  constructor(error2, quantiles, count5, min8, max10, sum4) {
    this.error = error2;
    this.quantiles = quantiles;
    this.count = count5;
    this.min = min8;
    this.max = max10;
    this.sum = sum4;
  }
  [symbol2]() {
    return pipe(hash(SummaryStateSymbolKey), combine2(hash(this.error)), combine2(hash(this.quantiles)), combine2(hash(this.count)), combine2(hash(this.min)), combine2(hash(this.max)), combine2(hash(this.sum)), cached(this));
  }
  [symbol3](that) {
    return isSummaryState(that) && this.error === that.error && equals(this.quantiles, that.quantiles) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter3 = /* @__PURE__ */ __name((count5) => new CounterState(count5), "counter");
var frequency3 = /* @__PURE__ */ __name((occurrences) => {
  return new FrequencyState(occurrences);
}, "frequency");
var gauge3 = /* @__PURE__ */ __name((count5) => new GaugeState(count5), "gauge");
var histogram3 = /* @__PURE__ */ __name((options) => new HistogramState(options.buckets, options.count, options.min, options.max, options.sum), "histogram");
var summary3 = /* @__PURE__ */ __name((options) => new SummaryState(options.error, options.quantiles, options.count, options.min, options.max, options.sum), "summary");
var isMetricState = /* @__PURE__ */ __name((u) => hasProperty(u, MetricStateTypeId), "isMetricState");
var isCounterState = /* @__PURE__ */ __name((u) => hasProperty(u, CounterStateTypeId), "isCounterState");
var isFrequencyState = /* @__PURE__ */ __name((u) => hasProperty(u, FrequencyStateTypeId), "isFrequencyState");
var isGaugeState = /* @__PURE__ */ __name((u) => hasProperty(u, GaugeStateTypeId), "isGaugeState");
var isHistogramState = /* @__PURE__ */ __name((u) => hasProperty(u, HistogramStateTypeId), "isHistogramState");
var isSummaryState = /* @__PURE__ */ __name((u) => hasProperty(u, SummaryStateTypeId), "isSummaryState");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/hook.js
var MetricHookSymbolKey = "effect/MetricHook";
var MetricHookTypeId = /* @__PURE__ */ Symbol.for(MetricHookSymbolKey);
var metricHookVariance = {
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out")
};
var make39 = /* @__PURE__ */ __name((options) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
}), "make");
var onModify = /* @__PURE__ */ dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: self.update,
  modify: /* @__PURE__ */ __name((input) => {
    self.modify(input);
    return f(input);
  }, "modify")
}));
var onUpdate = /* @__PURE__ */ dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: /* @__PURE__ */ __name((input) => {
    self.update(input);
    return f(input);
  }, "update"),
  modify: self.modify
}));
var bigint03 = /* @__PURE__ */ BigInt(0);
var counter4 = /* @__PURE__ */ __name((key) => {
  let sum4 = key.keyType.bigint ? bigint03 : 0;
  const canUpdate = key.keyType.incremental ? key.keyType.bigint ? (value2) => value2 >= bigint03 : (value2) => value2 >= 0 : (_value) => true;
  const update11 = /* @__PURE__ */ __name((value2) => {
    if (canUpdate(value2)) {
      sum4 = sum4 + value2;
    }
  }, "update");
  return make39({
    get: /* @__PURE__ */ __name(() => counter3(sum4), "get"),
    update: update11,
    modify: update11
  });
}, "counter");
var frequency4 = /* @__PURE__ */ __name((key) => {
  const values10 = /* @__PURE__ */ new Map();
  for (const word of key.keyType.preregisteredWords) {
    values10.set(word, 0);
  }
  const update11 = /* @__PURE__ */ __name((word) => {
    const slotCount = values10.get(word) ?? 0;
    values10.set(word, slotCount + 1);
  }, "update");
  return make39({
    get: /* @__PURE__ */ __name(() => frequency3(values10), "get"),
    update: update11,
    modify: update11
  });
}, "frequency");
var gauge4 = /* @__PURE__ */ __name((_key, startAt) => {
  let value2 = startAt;
  return make39({
    get: /* @__PURE__ */ __name(() => gauge3(value2), "get"),
    update: /* @__PURE__ */ __name((v) => {
      value2 = v;
    }, "update"),
    modify: /* @__PURE__ */ __name((v) => {
      value2 = value2 + v;
    }, "modify")
  });
}, "gauge");
var histogram4 = /* @__PURE__ */ __name((key) => {
  const bounds = key.keyType.boundaries.values;
  const size25 = bounds.length;
  const values10 = new Uint32Array(size25 + 1);
  const boundaries = new Float64Array(size25);
  let count5 = 0;
  let sum4 = 0;
  let min8 = Number.MAX_VALUE;
  let max10 = Number.MIN_VALUE;
  pipe(bounds, sort(Order), map6((n, i) => {
    boundaries[i] = n;
  }));
  const update11 = /* @__PURE__ */ __name((value2) => {
    let from = 0;
    let to = size25;
    while (from !== to) {
      const mid = Math.floor(from + (to - from) / 2);
      const boundary = boundaries[mid];
      if (value2 <= boundary) {
        to = mid;
      } else {
        from = mid;
      }
      if (to === from + 1) {
        if (value2 <= boundaries[from]) {
          to = from;
        } else {
          from = to;
        }
      }
    }
    values10[from] = values10[from] + 1;
    count5 = count5 + 1;
    sum4 = sum4 + value2;
    if (value2 < min8) {
      min8 = value2;
    }
    if (value2 > max10) {
      max10 = value2;
    }
  }, "update");
  const getBuckets = /* @__PURE__ */ __name(() => {
    const builder = allocate(size25);
    let cumulated = 0;
    for (let i = 0; i < size25; i++) {
      const boundary = boundaries[i];
      const value2 = values10[i];
      cumulated = cumulated + value2;
      builder[i] = [boundary, cumulated];
    }
    return builder;
  }, "getBuckets");
  return make39({
    get: /* @__PURE__ */ __name(() => histogram3({
      buckets: getBuckets(),
      count: count5,
      min: min8,
      max: max10,
      sum: sum4
    }), "get"),
    update: update11,
    modify: update11
  });
}, "histogram");
var summary4 = /* @__PURE__ */ __name((key) => {
  const {
    error: error2,
    maxAge,
    maxSize,
    quantiles
  } = key.keyType;
  const sortedQuantiles = pipe(quantiles, sort(Order));
  const values10 = allocate(maxSize);
  let head10 = 0;
  let count5 = 0;
  let sum4 = 0;
  let min8 = 0;
  let max10 = 0;
  const snapshot2 = /* @__PURE__ */ __name((now2) => {
    const builder = [];
    let i = 0;
    while (i !== maxSize - 1) {
      const item = values10[i];
      if (item != null) {
        const [t, v] = item;
        const age = millis(now2 - t);
        if (greaterThanOrEqualTo3(age, zero) && lessThanOrEqualTo3(age, maxAge)) {
          builder.push(v);
        }
      }
      i = i + 1;
    }
    return calculateQuantiles(error2, sortedQuantiles, sort(builder, Order));
  }, "snapshot");
  const observe = /* @__PURE__ */ __name((value2, timestamp) => {
    if (maxSize > 0) {
      head10 = head10 + 1;
      const target = head10 % maxSize;
      values10[target] = [timestamp, value2];
    }
    min8 = count5 === 0 ? value2 : Math.min(min8, value2);
    max10 = count5 === 0 ? value2 : Math.max(max10, value2);
    count5 = count5 + 1;
    sum4 = sum4 + value2;
  }, "observe");
  return make39({
    get: /* @__PURE__ */ __name(() => summary3({
      error: error2,
      quantiles: snapshot2(Date.now()),
      count: count5,
      min: min8,
      max: max10,
      sum: sum4
    }), "get"),
    update: /* @__PURE__ */ __name(([value2, timestamp]) => observe(value2, timestamp), "update"),
    modify: /* @__PURE__ */ __name(([value2, timestamp]) => observe(value2, timestamp), "modify")
  });
}, "summary");
var calculateQuantiles = /* @__PURE__ */ __name((error2, sortedQuantiles, sortedSamples) => {
  const sampleCount = sortedSamples.length;
  if (!isNonEmptyReadonlyArray(sortedQuantiles)) {
    return empty4();
  }
  const head10 = sortedQuantiles[0];
  const tail5 = sortedQuantiles.slice(1);
  const resolvedHead = resolveQuantile(error2, sampleCount, none2(), 0, head10, sortedSamples);
  const resolved = of2(resolvedHead);
  tail5.forEach((quantile) => {
    resolved.push(resolveQuantile(error2, sampleCount, resolvedHead.value, resolvedHead.consumed, quantile, resolvedHead.rest));
  });
  return map6(resolved, (rq) => [rq.quantile, rq.value]);
}, "calculateQuantiles");
var resolveQuantile = /* @__PURE__ */ __name((error2, sampleCount, current2, consumed, quantile, rest) => {
  let error_1 = error2;
  let sampleCount_1 = sampleCount;
  let current_1 = current2;
  let consumed_1 = consumed;
  let quantile_1 = quantile;
  let rest_1 = rest;
  let error_2 = error2;
  let sampleCount_2 = sampleCount;
  let current_2 = current2;
  let consumed_2 = consumed;
  let quantile_2 = quantile;
  let rest_2 = rest;
  while (1) {
    if (!isNonEmptyReadonlyArray(rest_1)) {
      return {
        quantile: quantile_1,
        value: none2(),
        consumed: consumed_1,
        rest: []
      };
    }
    if (quantile_1 === 1) {
      return {
        quantile: quantile_1,
        value: some3(lastNonEmpty(rest_1)),
        consumed: consumed_1 + rest_1.length,
        rest: []
      };
    }
    const headValue = headNonEmpty(rest_1);
    const sameHead = span(rest_1, (n) => n === headValue);
    const desired = quantile_1 * sampleCount_1;
    const allowedError = error_1 / 2 * desired;
    const candConsumed = consumed_1 + sameHead[0].length;
    const candError = Math.abs(candConsumed - desired);
    if (candConsumed < desired - allowedError) {
      error_2 = error_1;
      sampleCount_2 = sampleCount_1;
      current_2 = head2(rest_1);
      consumed_2 = candConsumed;
      quantile_2 = quantile_1;
      rest_2 = sameHead[1];
      error_1 = error_2;
      sampleCount_1 = sampleCount_2;
      current_1 = current_2;
      consumed_1 = consumed_2;
      quantile_1 = quantile_2;
      rest_1 = rest_2;
      continue;
    }
    if (candConsumed > desired + allowedError) {
      const valueToReturn = isNone2(current_1) ? some3(headValue) : current_1;
      return {
        quantile: quantile_1,
        value: valueToReturn,
        consumed: consumed_1,
        rest: rest_1
      };
    }
    switch (current_1._tag) {
      case "None": {
        error_2 = error_1;
        sampleCount_2 = sampleCount_1;
        current_2 = head2(rest_1);
        consumed_2 = candConsumed;
        quantile_2 = quantile_1;
        rest_2 = sameHead[1];
        error_1 = error_2;
        sampleCount_1 = sampleCount_2;
        current_1 = current_2;
        consumed_1 = consumed_2;
        quantile_1 = quantile_2;
        rest_1 = rest_2;
        continue;
      }
      case "Some": {
        const prevError = Math.abs(desired - current_1.value);
        if (candError < prevError) {
          error_2 = error_1;
          sampleCount_2 = sampleCount_1;
          current_2 = head2(rest_1);
          consumed_2 = candConsumed;
          quantile_2 = quantile_1;
          rest_2 = sameHead[1];
          error_1 = error_2;
          sampleCount_1 = sampleCount_2;
          current_1 = current_2;
          consumed_1 = consumed_2;
          quantile_1 = quantile_2;
          rest_1 = rest_2;
          continue;
        }
        return {
          quantile: quantile_1,
          value: some3(current_1.value),
          consumed: consumed_1,
          rest: rest_1
        };
      }
    }
  }
  throw new Error("BUG: MetricHook.resolveQuantiles - please report an issue at https://github.com/Effect-TS/effect/issues");
}, "resolveQuantile");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/pair.js
var MetricPairSymbolKey = "effect/MetricPair";
var MetricPairTypeId = /* @__PURE__ */ Symbol.for(MetricPairSymbolKey);
var metricPairVariance = {
  /* c8 ignore next */
  _Type: /* @__PURE__ */ __name((_) => _, "_Type")
};
var make40 = /* @__PURE__ */ __name((metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
}, "make");
var unsafeMake10 = /* @__PURE__ */ __name((metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
}, "unsafeMake");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric/registry.js
var MetricRegistrySymbolKey = "effect/MetricRegistry";
var MetricRegistryTypeId = /* @__PURE__ */ Symbol.for(MetricRegistrySymbolKey);
var MetricRegistryImpl = class {
  static {
    __name(this, "MetricRegistryImpl");
  }
  [MetricRegistryTypeId] = MetricRegistryTypeId;
  map = /* @__PURE__ */ empty23();
  snapshot() {
    const result = [];
    for (const [key, hook] of this.map) {
      result.push(unsafeMake10(key, hook.get()));
    }
    return result;
  }
  get(key) {
    const hook = pipe(this.map, get9(key), getOrUndefined2);
    if (hook == null) {
      if (isCounterKey(key.keyType)) {
        return this.getCounter(key);
      }
      if (isGaugeKey(key.keyType)) {
        return this.getGauge(key);
      }
      if (isFrequencyKey(key.keyType)) {
        return this.getFrequency(key);
      }
      if (isHistogramKey(key.keyType)) {
        return this.getHistogram(key);
      }
      if (isSummaryKey(key.keyType)) {
        return this.getSummary(key);
      }
      throw new Error("BUG: MetricRegistry.get - unknown MetricKeyType - please report an issue at https://github.com/Effect-TS/effect/issues");
    } else {
      return hook;
    }
  }
  getCounter(key) {
    let value2 = pipe(this.map, get9(key), getOrUndefined2);
    if (value2 == null) {
      const counter6 = counter4(key);
      if (!pipe(this.map, has6(key))) {
        pipe(this.map, set5(key, counter6));
      }
      value2 = counter6;
    }
    return value2;
  }
  getFrequency(key) {
    let value2 = pipe(this.map, get9(key), getOrUndefined2);
    if (value2 == null) {
      const frequency6 = frequency4(key);
      if (!pipe(this.map, has6(key))) {
        pipe(this.map, set5(key, frequency6));
      }
      value2 = frequency6;
    }
    return value2;
  }
  getGauge(key) {
    let value2 = pipe(this.map, get9(key), getOrUndefined2);
    if (value2 == null) {
      const gauge6 = gauge4(key, key.keyType.bigint ? BigInt(0) : 0);
      if (!pipe(this.map, has6(key))) {
        pipe(this.map, set5(key, gauge6));
      }
      value2 = gauge6;
    }
    return value2;
  }
  getHistogram(key) {
    let value2 = pipe(this.map, get9(key), getOrUndefined2);
    if (value2 == null) {
      const histogram6 = histogram4(key);
      if (!pipe(this.map, has6(key))) {
        pipe(this.map, set5(key, histogram6));
      }
      value2 = histogram6;
    }
    return value2;
  }
  getSummary(key) {
    let value2 = pipe(this.map, get9(key), getOrUndefined2);
    if (value2 == null) {
      const summary6 = summary4(key);
      if (!pipe(this.map, has6(key))) {
        pipe(this.map, set5(key, summary6));
      }
      value2 = summary6;
    }
    return value2;
  }
};
var make41 = /* @__PURE__ */ __name(() => {
  return new MetricRegistryImpl();
}, "make");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/metric.js
var MetricSymbolKey = "effect/Metric";
var MetricTypeId = /* @__PURE__ */ Symbol.for(MetricSymbolKey);
var metricVariance = {
  /* c8 ignore next */
  _Type: /* @__PURE__ */ __name((_) => _, "_Type"),
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out")
};
var globalMetricRegistry = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Metric/globalMetricRegistry"), () => make41());
var make42 = /* @__PURE__ */ __name(function(keyType, unsafeUpdate, unsafeValue, unsafeModify) {
  const metric = Object.assign((effect4) => tap2(effect4, (a) => update5(metric, a)), {
    [MetricTypeId]: metricVariance,
    keyType,
    unsafeUpdate,
    unsafeValue,
    unsafeModify,
    register() {
      this.unsafeValue([]);
      return this;
    },
    pipe() {
      return pipeArguments(this, arguments);
    }
  });
  return metric;
}, "make");
var mapInput5 = /* @__PURE__ */ dual(2, (self, f) => make42(self.keyType, (input, extraTags) => self.unsafeUpdate(f(input), extraTags), self.unsafeValue, (input, extraTags) => self.unsafeModify(f(input), extraTags)));
var counter5 = /* @__PURE__ */ __name((name, options) => fromMetricKey(counter2(name, options)), "counter");
var frequency5 = /* @__PURE__ */ __name((name, options) => fromMetricKey(frequency2(name, options)), "frequency");
var withConstantInput = /* @__PURE__ */ dual(2, (self, input) => mapInput5(self, () => input));
var fromMetricKey = /* @__PURE__ */ __name((key) => {
  let untaggedHook;
  const hookCache = /* @__PURE__ */ new WeakMap();
  const hook = /* @__PURE__ */ __name((extraTags) => {
    if (extraTags.length === 0) {
      if (untaggedHook !== void 0) {
        return untaggedHook;
      }
      untaggedHook = globalMetricRegistry.get(key);
      return untaggedHook;
    }
    let hook2 = hookCache.get(extraTags);
    if (hook2 !== void 0) {
      return hook2;
    }
    hook2 = globalMetricRegistry.get(taggedWithLabels(key, extraTags));
    hookCache.set(extraTags, hook2);
    return hook2;
  }, "hook");
  return make42(key.keyType, (input, extraTags) => hook(extraTags).update(input), (extraTags) => hook(extraTags).get(), (input, extraTags) => hook(extraTags).modify(input));
}, "fromMetricKey");
var gauge5 = /* @__PURE__ */ __name((name, options) => fromMetricKey(gauge2(name, options)), "gauge");
var histogram5 = /* @__PURE__ */ __name((name, boundaries, description) => fromMetricKey(histogram2(name, boundaries, description)), "histogram");
var increment3 = /* @__PURE__ */ __name((self) => isCounterKey(self.keyType) ? update5(self, self.keyType.bigint ? BigInt(1) : 1) : modify9(self, self.keyType.bigint ? BigInt(1) : 1), "increment");
var incrementBy = /* @__PURE__ */ dual(2, (self, amount) => isCounterKey(self.keyType) ? update5(self, amount) : modify9(self, amount));
var map20 = /* @__PURE__ */ dual(2, (self, f) => make42(self.keyType, self.unsafeUpdate, (extraTags) => f(self.unsafeValue(extraTags)), self.unsafeModify));
var mapType = /* @__PURE__ */ dual(2, (self, f) => make42(f(self.keyType), self.unsafeUpdate, self.unsafeValue, self.unsafeModify));
var modify9 = /* @__PURE__ */ dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeModify(input, tags))));
var set8 = /* @__PURE__ */ dual(2, (self, value2) => update5(self, value2));
var succeed7 = /* @__PURE__ */ __name((out) => make42(void 0, constVoid, () => out, constVoid), "succeed");
var sync5 = /* @__PURE__ */ __name((evaluate3) => make42(void 0, constVoid, evaluate3, constVoid), "sync");
var summary5 = /* @__PURE__ */ __name((options) => withNow(summaryTimestamp(options)), "summary");
var summaryTimestamp = /* @__PURE__ */ __name((options) => fromMetricKey(summary2(options)), "summaryTimestamp");
var tagged2 = /* @__PURE__ */ dual(3, (self, key, value2) => taggedWithLabels2(self, [make37(key, value2)]));
var taggedWithLabelsInput = /* @__PURE__ */ dual(2, (self, f) => map20(make42(self.keyType, (input, extraTags) => self.unsafeUpdate(input, union2(f(input), extraTags)), self.unsafeValue, (input, extraTags) => self.unsafeModify(input, union2(f(input), extraTags))), constVoid));
var taggedWithLabels2 = /* @__PURE__ */ dual(2, (self, extraTags) => {
  return make42(self.keyType, (input, extraTags1) => self.unsafeUpdate(input, union2(extraTags, extraTags1)), (extraTags1) => self.unsafeValue(union2(extraTags, extraTags1)), (input, extraTags1) => self.unsafeModify(input, union2(extraTags, extraTags1)));
});
var timer2 = /* @__PURE__ */ __name((name, description) => {
  const boundaries = exponential({
    start: 0.5,
    factor: 2,
    count: 35
  });
  const base = pipe(histogram5(name, boundaries, description), tagged2("time_unit", "milliseconds"));
  return mapInput5(base, toMillis);
}, "timer");
var timerWithBoundaries = /* @__PURE__ */ __name((name, boundaries, description) => {
  const base = pipe(histogram5(name, fromIterable11(boundaries), description), tagged2("time_unit", "milliseconds"));
  return mapInput5(base, toMillis);
}, "timerWithBoundaries");
var trackAll = /* @__PURE__ */ dual(2, (self, input) => (effect4) => matchCauseEffect(effect4, {
  onFailure: /* @__PURE__ */ __name((cause3) => zipRight2(update5(self, input), failCause(cause3)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((value2) => zipRight2(update5(self, input), succeed(value2)), "onSuccess")
}));
var trackDefect = /* @__PURE__ */ dual(2, (self, metric) => trackDefectWith(self, metric, identity));
var trackDefectWith = /* @__PURE__ */ dual(3, (self, metric, f) => {
  const updater = /* @__PURE__ */ __name((defect) => update5(metric, f(defect)), "updater");
  return tapDefect(self, (cause3) => forEachSequentialDiscard(defects(cause3), updater));
});
var trackDuration = /* @__PURE__ */ dual(2, (self, metric) => trackDurationWith(self, metric, identity));
var trackDurationWith = /* @__PURE__ */ dual(3, (self, metric, f) => clockWith2((clock3) => {
  const startTime = clock3.unsafeCurrentTimeNanos();
  return tap2(self, (_) => {
    const endTime = clock3.unsafeCurrentTimeNanos();
    const duration3 = nanos(endTime - startTime);
    return update5(metric, f(duration3));
  });
}));
var trackError = /* @__PURE__ */ dual(2, (self, metric) => trackErrorWith(self, metric, (a) => a));
var trackErrorWith = /* @__PURE__ */ dual(3, (self, metric, f) => {
  const updater = /* @__PURE__ */ __name((error2) => update5(metric, f(error2)), "updater");
  return tapError(self, updater);
});
var trackSuccess = /* @__PURE__ */ dual(2, (self, metric) => trackSuccessWith(self, metric, (a) => a));
var trackSuccessWith = /* @__PURE__ */ dual(3, (self, metric, f) => {
  const updater = /* @__PURE__ */ __name((value2) => update5(metric, f(value2)), "updater");
  return tap2(self, updater);
});
var update5 = /* @__PURE__ */ dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeUpdate(input, tags))));
var value = /* @__PURE__ */ __name((self) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeValue(tags))), "value");
var withNow = /* @__PURE__ */ __name((self) => mapInput5(self, (input) => [input, Date.now()]), "withNow");
var zip9 = /* @__PURE__ */ dual(2, (self, that) => make42([self.keyType, that.keyType], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeUpdate(l, extraTags);
  that.unsafeUpdate(r, extraTags);
}, (extraTags) => [self.unsafeValue(extraTags), that.unsafeValue(extraTags)], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeModify(l, extraTags);
  that.unsafeModify(r, extraTags);
}));
var unsafeSnapshot = /* @__PURE__ */ __name(() => globalMetricRegistry.snapshot(), "unsafeSnapshot");
var snapshot = /* @__PURE__ */ sync(unsafeSnapshot);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/request.js
var RequestSymbolKey = "effect/Request";
var RequestTypeId = /* @__PURE__ */ Symbol.for(RequestSymbolKey);
var requestVariance = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var RequestPrototype = {
  ...StructuralPrototype,
  [RequestTypeId]: requestVariance
};
var isRequest = /* @__PURE__ */ __name((u) => hasProperty(u, RequestTypeId), "isRequest");
var of5 = /* @__PURE__ */ __name(() => (args2) => Object.assign(Object.create(RequestPrototype), args2), "of");
var tagged3 = /* @__PURE__ */ __name((tag) => (args2) => {
  const request2 = Object.assign(Object.create(RequestPrototype), args2);
  request2._tag = tag;
  return request2;
}, "tagged");
var Class4 = /* @__PURE__ */ (function() {
  function Class7(args2) {
    if (args2) {
      Object.assign(this, args2);
    }
  }
  __name(Class7, "Class");
  Class7.prototype = RequestPrototype;
  return Class7;
})();
var TaggedClass = /* @__PURE__ */ __name((tag) => {
  return class TaggedClass extends Class4 {
    static {
      __name(this, "TaggedClass");
    }
    _tag = tag;
  };
}, "TaggedClass");
var complete2 = /* @__PURE__ */ dual(2, (self, result) => fiberRefGetWith(currentRequestMap, (map36) => sync(() => {
  if (map36.has(self)) {
    const entry = map36.get(self);
    if (!entry.state.completed) {
      entry.state.completed = true;
      deferredUnsafeDone(entry.result, result);
    }
  }
})));
var completeEffect = /* @__PURE__ */ dual(2, (self, effect4) => matchEffect(effect4, {
  onFailure: /* @__PURE__ */ __name((error2) => complete2(self, exitFail(error2)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((value2) => complete2(self, exitSucceed(value2)), "onSuccess")
}));
var fail7 = /* @__PURE__ */ dual(2, (self, error2) => complete2(self, exitFail(error2)));
var failCause6 = /* @__PURE__ */ dual(2, (self, cause3) => complete2(self, exitFailCause(cause3)));
var succeed8 = /* @__PURE__ */ dual(2, (self, value2) => complete2(self, exitSucceed(value2)));
var Listeners = class {
  static {
    __name(this, "Listeners");
  }
  count = 0;
  observers = /* @__PURE__ */ new Set();
  interrupted = false;
  addObserver(f) {
    this.observers.add(f);
  }
  removeObserver(f) {
    this.observers.delete(f);
  }
  increment() {
    this.count++;
    this.observers.forEach((f) => f(this.count));
  }
  decrement() {
    this.count--;
    this.observers.forEach((f) => f(this.count));
  }
};
var filterOutCompleted = /* @__PURE__ */ __name((requests) => fiberRefGetWith(currentRequestMap, (map36) => succeed(requests.filter((request2) => !(map36.get(request2)?.state.completed === true)))), "filterOutCompleted");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/redBlackTree/iterator.js
var Direction = {
  Forward: 0,
  Backward: 1 << 0
};
var RedBlackTreeIterator = class _RedBlackTreeIterator {
  static {
    __name(this, "RedBlackTreeIterator");
  }
  self;
  stack;
  direction;
  count = 0;
  constructor(self, stack, direction) {
    this.self = self;
    this.stack = stack;
    this.direction = direction;
  }
  /**
   * Clones the iterator
   */
  clone() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction);
  }
  /**
   * Reverse the traversal direction
   */
  reversed() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction === Direction.Forward ? Direction.Backward : Direction.Forward);
  }
  /**
   * Iterator next
   */
  next() {
    const entry = this.entry;
    this.count++;
    if (this.direction === Direction.Forward) {
      this.moveNext();
    } else {
      this.movePrev();
    }
    switch (entry._tag) {
      case "None": {
        return {
          done: true,
          value: this.count
        };
      }
      case "Some": {
        return {
          done: false,
          value: entry.value
        };
      }
    }
  }
  /**
   * Returns the key
   */
  get key() {
    if (this.stack.length > 0) {
      return some3(this.stack[this.stack.length - 1].key);
    }
    return none2();
  }
  /**
   * Returns the value
   */
  get value() {
    if (this.stack.length > 0) {
      return some3(this.stack[this.stack.length - 1].value);
    }
    return none2();
  }
  /**
   * Returns the key
   */
  get entry() {
    return map2(last(this.stack), (node) => [node.key, node.value]);
  }
  /**
   * Returns the position of this iterator in the sorted list
   */
  get index() {
    let idx = 0;
    const stack = this.stack;
    if (stack.length === 0) {
      const r = this.self._root;
      if (r != null) {
        return r.count;
      }
      return 0;
    } else if (stack[stack.length - 1].left != null) {
      idx = stack[stack.length - 1].left.count;
    }
    for (let s = stack.length - 2; s >= 0; --s) {
      if (stack[s + 1] === stack[s].right) {
        ++idx;
        if (stack[s].left != null) {
          idx += stack[s].left.count;
        }
      }
    }
    return idx;
  }
  /**
   * Advances iterator to next element in list
   */
  moveNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n.right != null) {
      n = n.right;
      while (n != null) {
        stack.push(n);
        n = n.left;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].right === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a next element
   */
  get hasNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].right != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].left === stack[s]) {
        return true;
      }
    }
    return false;
  }
  /**
   * Advances iterator to previous element in list
   */
  movePrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n != null && n.left != null) {
      n = n.left;
      while (n != null) {
        stack.push(n);
        n = n.right;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].left === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a previous element
   */
  get hasPrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].left != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].right === stack[s]) {
        return true;
      }
    }
    return false;
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/redBlackTree/node.js
var Color = {
  Red: 0,
  Black: 1 << 0
};
var clone = /* @__PURE__ */ __name(({
  color,
  count: count5,
  key,
  left: left3,
  right: right3,
  value: value2
}) => ({
  color,
  key,
  value: value2,
  left: left3,
  right: right3,
  count: count5
}), "clone");
function swap3(n, v) {
  n.key = v.key;
  n.value = v.value;
  n.left = v.left;
  n.right = v.right;
  n.color = v.color;
  n.count = v.count;
}
__name(swap3, "swap");
var repaint = /* @__PURE__ */ __name(({
  count: count5,
  key,
  left: left3,
  right: right3,
  value: value2
}, color) => ({
  color,
  key,
  value: value2,
  left: left3,
  right: right3,
  count: count5
}), "repaint");
var recount = /* @__PURE__ */ __name((node) => {
  node.count = 1 + (node.left?.count ?? 0) + (node.right?.count ?? 0);
}, "recount");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/redBlackTree.js
var RedBlackTreeSymbolKey = "effect/RedBlackTree";
var RedBlackTreeTypeId = /* @__PURE__ */ Symbol.for(RedBlackTreeSymbolKey);
var redBlackTreeVariance = {
  /* c8 ignore next */
  _Key: /* @__PURE__ */ __name((_) => _, "_Key"),
  /* c8 ignore next */
  _Value: /* @__PURE__ */ __name((_) => _, "_Value")
};
var RedBlackTreeProto = {
  [RedBlackTreeTypeId]: redBlackTreeVariance,
  [symbol2]() {
    let hash2 = hash(RedBlackTreeSymbolKey);
    for (const item of this) {
      hash2 ^= pipe(hash(item[0]), combine2(hash(item[1])));
    }
    return cached(this, hash2);
  },
  [symbol3](that) {
    if (isRedBlackTree(that)) {
      if ((this._root?.count ?? 0) !== (that._root?.count ?? 0)) {
        return false;
      }
      const entries3 = Array.from(that);
      return Array.from(this).every((itemSelf, i) => {
        const itemThat = entries3[i];
        return equals(itemSelf[0], itemThat[0]) && equals(itemSelf[1], itemThat[1]);
      });
    }
    return false;
  },
  [Symbol.iterator]() {
    const stack = [];
    let n = this._root;
    while (n != null) {
      stack.push(n);
      n = n.left;
    }
    return new RedBlackTreeIterator(this, stack, Direction.Forward);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "RedBlackTree",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl3 = /* @__PURE__ */ __name((ord, root) => {
  const tree = Object.create(RedBlackTreeProto);
  tree._ord = ord;
  tree._root = root;
  return tree;
}, "makeImpl");
var isRedBlackTree = /* @__PURE__ */ __name((u) => hasProperty(u, RedBlackTreeTypeId), "isRedBlackTree");
var empty30 = /* @__PURE__ */ __name((ord) => makeImpl3(ord, void 0), "empty");
var fromIterable12 = /* @__PURE__ */ dual(2, (entries3, ord) => {
  let tree = empty30(ord);
  for (const [key, value2] of entries3) {
    tree = insert(tree, key, value2);
  }
  return tree;
});
var make43 = /* @__PURE__ */ __name((ord) => (...entries3) => {
  return fromIterable12(entries3, ord);
}, "make");
var atBackwards = /* @__PURE__ */ dual(2, (self, index) => at2(self, index, Direction.Backward));
var atForwards = /* @__PURE__ */ dual(2, (self, index) => at2(self, index, Direction.Forward));
var at2 = /* @__PURE__ */ __name((self, index, direction) => {
  return {
    [Symbol.iterator]: () => {
      if (index < 0) {
        return new RedBlackTreeIterator(self, [], direction);
      }
      let node = self._root;
      const stack = [];
      while (node !== void 0) {
        stack.push(node);
        if (node.left !== void 0) {
          if (index < node.left.count) {
            node = node.left;
            continue;
          }
          index -= node.left.count;
        }
        if (!index) {
          return new RedBlackTreeIterator(self, stack, direction);
        }
        index -= 1;
        if (node.right !== void 0) {
          if (index >= node.right.count) {
            break;
          }
          node = node.right;
        } else {
          break;
        }
      }
      return new RedBlackTreeIterator(self, [], direction);
    }
  };
}, "at");
var findAll = /* @__PURE__ */ dual(2, (self, key) => {
  const stack = [];
  let node = self._root;
  let result = empty7();
  while (node !== void 0 || stack.length > 0) {
    if (node) {
      stack.push(node);
      node = node.left;
    } else {
      const current2 = stack.pop();
      if (equals(key, current2.key)) {
        result = prepend3(current2.value)(result);
      }
      node = current2.right;
    }
  }
  return result;
});
var findFirst9 = /* @__PURE__ */ dual(2, (self, key) => {
  const cmp = self._ord;
  let node = self._root;
  while (node !== void 0) {
    const d = cmp(key, node.key);
    if (equals(key, node.key)) {
      return some3(node.value);
    }
    if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  return none2();
});
var first = /* @__PURE__ */ __name((self) => {
  let node = self._root;
  let current2 = self._root;
  while (node !== void 0) {
    current2 = node;
    node = node.left;
  }
  return current2 ? some3([current2.key, current2.value]) : none2();
}, "first");
var getAt = /* @__PURE__ */ dual(2, (self, index) => {
  if (index < 0) {
    return none2();
  }
  let root = self._root;
  let node = void 0;
  while (root !== void 0) {
    node = root;
    if (root.left) {
      if (index < root.left.count) {
        root = root.left;
        continue;
      }
      index -= root.left.count;
    }
    if (!index) {
      return some3([node.key, node.value]);
    }
    index -= 1;
    if (root.right) {
      if (index >= root.right.count) {
        break;
      }
      root = root.right;
    } else {
      break;
    }
  }
  return none2();
});
var getOrder4 = /* @__PURE__ */ __name((tree) => tree._ord, "getOrder");
var has7 = /* @__PURE__ */ dual(2, (self, key) => isSome2(findFirst9(self, key)));
var insert = /* @__PURE__ */ dual(3, (self, key, value2) => {
  const cmp = self._ord;
  let n = self._root;
  const n_stack = [];
  const d_stack = [];
  while (n != null) {
    const d = cmp(key, n.key);
    n_stack.push(n);
    d_stack.push(d);
    if (d <= 0) {
      n = n.left;
    } else {
      n = n.right;
    }
  }
  n_stack.push({
    color: Color.Red,
    key,
    value: value2,
    left: void 0,
    right: void 0,
    count: 1
  });
  for (let s = n_stack.length - 2; s >= 0; --s) {
    const n2 = n_stack[s];
    if (d_stack[s] <= 0) {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n_stack[s + 1],
        right: n2.right,
        count: n2.count + 1
      };
    } else {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n2.left,
        right: n_stack[s + 1],
        count: n2.count + 1
      };
    }
  }
  for (let s = n_stack.length - 1; s > 1; --s) {
    const p = n_stack[s - 1];
    const n3 = n_stack[s];
    if (p.color === Color.Black || n3.color === Color.Black) {
      break;
    }
    const pp = n_stack[s - 2];
    if (pp.left === p) {
      if (p.left === n3) {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.left = p.right;
          p.color = Color.Black;
          p.right = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = p;
            } else {
              ppp.right = p;
            }
          }
          break;
        }
      } else {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.right = n3.left;
          pp.color = Color.Red;
          pp.left = n3.right;
          n3.color = Color.Black;
          n3.left = p;
          n3.right = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = n3;
            } else {
              ppp.right = n3;
            }
          }
          break;
        }
      }
    } else {
      if (p.right === n3) {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.right = p.left;
          p.color = Color.Black;
          p.left = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = p;
            } else {
              ppp.left = p;
            }
          }
          break;
        }
      } else {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.left = n3.right;
          pp.color = Color.Red;
          pp.right = n3.left;
          n3.color = Color.Black;
          n3.right = p;
          n3.left = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = n3;
            } else {
              ppp.left = n3;
            }
          }
          break;
        }
      }
    }
  }
  n_stack[0].color = Color.Black;
  return makeImpl3(self._ord, n_stack[0]);
});
var keysForward = /* @__PURE__ */ __name((self) => keys5(self, Direction.Forward), "keysForward");
var keysBackward = /* @__PURE__ */ __name((self) => keys5(self, Direction.Backward), "keysBackward");
var keys5 = /* @__PURE__ */ __name((self, direction) => {
  const begin = self[Symbol.iterator]();
  let count5 = 0;
  return {
    [Symbol.iterator]: () => keys5(self, direction),
    next: /* @__PURE__ */ __name(() => {
      count5++;
      const entry = begin.key;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count5
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }, "next")
  };
}, "keys");
var last4 = /* @__PURE__ */ __name((self) => {
  let node = self._root;
  let current2 = self._root;
  while (node !== void 0) {
    current2 = node;
    node = node.right;
  }
  return current2 ? some3([current2.key, current2.value]) : none2();
}, "last");
var reversed = /* @__PURE__ */ __name((self) => {
  return {
    [Symbol.iterator]: () => {
      const stack = [];
      let node = self._root;
      while (node !== void 0) {
        stack.push(node);
        node = node.right;
      }
      return new RedBlackTreeIterator(self, stack, Direction.Backward);
    }
  };
}, "reversed");
var greaterThanBackwards = /* @__PURE__ */ dual(2, (self, key) => greaterThan5(self, key, Direction.Backward));
var greaterThanForwards = /* @__PURE__ */ dual(2, (self, key) => greaterThan5(self, key, Direction.Forward));
var greaterThan5 = /* @__PURE__ */ __name((self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d < 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
}, "greaterThan");
var greaterThanEqualBackwards = /* @__PURE__ */ dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Backward));
var greaterThanEqualForwards = /* @__PURE__ */ dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Forward));
var greaterThanEqual2 = /* @__PURE__ */ __name((self, key, direction = Direction.Forward) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d <= 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
}, "greaterThanEqual");
var lessThanBackwards = /* @__PURE__ */ dual(2, (self, key) => lessThan5(self, key, Direction.Backward));
var lessThanForwards = /* @__PURE__ */ dual(2, (self, key) => lessThan5(self, key, Direction.Forward));
var lessThan5 = /* @__PURE__ */ __name((self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d > 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
}, "lessThan");
var lessThanEqualBackwards = /* @__PURE__ */ dual(2, (self, key) => lessThanEqual2(self, key, Direction.Backward));
var lessThanEqualForwards = /* @__PURE__ */ dual(2, (self, key) => lessThanEqual2(self, key, Direction.Forward));
var lessThanEqual2 = /* @__PURE__ */ __name((self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d >= 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
}, "lessThanEqual");
var forEach12 = /* @__PURE__ */ dual(2, (self, f) => {
  const root = self._root;
  if (root !== void 0) {
    visitFull(root, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachGreaterThanEqual = /* @__PURE__ */ dual(3, (self, min8, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitGreaterThanEqual(root, min8, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachLessThan = /* @__PURE__ */ dual(3, (self, max10, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitLessThan(root, max10, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachBetween = /* @__PURE__ */ dual(2, (self, {
  body,
  max: max10,
  min: min8
}) => {
  const root = self._root;
  const ord = self._ord;
  if (root) {
    visitBetween(root, min8, max10, ord, (key, value2) => {
      body(key, value2);
      return none2();
    });
  }
});
var reduce13 = /* @__PURE__ */ dual(3, (self, zero2, f) => {
  let accumulator = zero2;
  for (const entry of self) {
    accumulator = f(accumulator, entry[1], entry[0]);
  }
  return accumulator;
});
var removeFirst = /* @__PURE__ */ dual(2, (self, key) => {
  if (!has7(self, key)) {
    return self;
  }
  const ord = self._ord;
  const cmp = ord;
  let node = self._root;
  const stack = [];
  while (node !== void 0) {
    const d = cmp(key, node.key);
    stack.push(node);
    if (equals(key, node.key)) {
      node = void 0;
    } else if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  if (stack.length === 0) {
    return self;
  }
  const cstack = new Array(stack.length);
  let n = stack[stack.length - 1];
  cstack[cstack.length - 1] = {
    color: n.color,
    key: n.key,
    value: n.value,
    left: n.left,
    right: n.right,
    count: n.count
  };
  for (let i = stack.length - 2; i >= 0; --i) {
    n = stack[i];
    if (n.left === stack[i + 1]) {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: cstack[i + 1],
        right: n.right,
        count: n.count
      };
    } else {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
  }
  n = cstack[cstack.length - 1];
  if (n.left !== void 0 && n.right !== void 0) {
    const split6 = cstack.length;
    n = n.left;
    while (n.right != null) {
      cstack.push(n);
      n = n.right;
    }
    const v = cstack[split6 - 1];
    cstack.push({
      color: n.color,
      key: v.key,
      value: v.value,
      left: n.left,
      right: n.right,
      count: n.count
    });
    cstack[split6 - 1].key = n.key;
    cstack[split6 - 1].value = n.value;
    for (let i = cstack.length - 2; i >= split6; --i) {
      n = cstack[i];
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
    cstack[split6 - 1].left = cstack[split6];
  }
  n = cstack[cstack.length - 1];
  if (n.color === Color.Red) {
    const p = cstack[cstack.length - 2];
    if (p.left === n) {
      p.left = void 0;
    } else if (p.right === n) {
      p.right = void 0;
    }
    cstack.pop();
    for (let i = 0; i < cstack.length; ++i) {
      cstack[i].count--;
    }
    return makeImpl3(ord, cstack[0]);
  } else {
    if (n.left !== void 0 || n.right !== void 0) {
      if (n.left !== void 0) {
        swap3(n, n.left);
      } else if (n.right !== void 0) {
        swap3(n, n.right);
      }
      n.color = Color.Black;
      for (let i = 0; i < cstack.length - 1; ++i) {
        cstack[i].count--;
      }
      return makeImpl3(ord, cstack[0]);
    } else if (cstack.length === 1) {
      return makeImpl3(ord, void 0);
    } else {
      for (let i = 0; i < cstack.length; ++i) {
        cstack[i].count--;
      }
      const parent = cstack[cstack.length - 2];
      fixDoubleBlack(cstack);
      if (parent.left === n) {
        parent.left = void 0;
      } else {
        parent.right = void 0;
      }
    }
  }
  return makeImpl3(ord, cstack[0]);
});
var size11 = /* @__PURE__ */ __name((self) => self._root?.count ?? 0, "size");
var valuesForward = /* @__PURE__ */ __name((self) => values7(self, Direction.Forward), "valuesForward");
var valuesBackward = /* @__PURE__ */ __name((self) => values7(self, Direction.Backward), "valuesBackward");
var values7 = /* @__PURE__ */ __name((self, direction) => {
  const begin = self[Symbol.iterator]();
  let count5 = 0;
  return {
    [Symbol.iterator]: () => values7(self, direction),
    next: /* @__PURE__ */ __name(() => {
      count5++;
      const entry = begin.value;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count5
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }, "next")
  };
}, "values");
var visitFull = /* @__PURE__ */ __name((node, visit) => {
  let current2 = node;
  let stack = void 0;
  let done12 = false;
  while (!done12) {
    if (current2 != null) {
      stack = make9(current2, stack);
      current2 = current2.left;
    } else if (stack != null) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome2(value2)) {
        return value2;
      }
      current2 = stack.value.right;
      stack = stack.previous;
    } else {
      done12 = true;
    }
  }
  return none2();
}, "visitFull");
var visitGreaterThanEqual = /* @__PURE__ */ __name((node, min8, ord, visit) => {
  let current2 = node;
  let stack = void 0;
  let done12 = false;
  while (!done12) {
    if (current2 !== void 0) {
      stack = make9(current2, stack);
      if (ord(min8, current2.key) <= 0) {
        current2 = current2.left;
      } else {
        current2 = void 0;
      }
    } else if (stack !== void 0) {
      if (ord(min8, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome2(value2)) {
          return value2;
        }
      }
      current2 = stack.value.right;
      stack = stack.previous;
    } else {
      done12 = true;
    }
  }
  return none2();
}, "visitGreaterThanEqual");
var visitLessThan = /* @__PURE__ */ __name((node, max10, ord, visit) => {
  let current2 = node;
  let stack = void 0;
  let done12 = false;
  while (!done12) {
    if (current2 !== void 0) {
      stack = make9(current2, stack);
      current2 = current2.left;
    } else if (stack !== void 0 && ord(max10, stack.value.key) > 0) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome2(value2)) {
        return value2;
      }
      current2 = stack.value.right;
      stack = stack.previous;
    } else {
      done12 = true;
    }
  }
  return none2();
}, "visitLessThan");
var visitBetween = /* @__PURE__ */ __name((node, min8, max10, ord, visit) => {
  let current2 = node;
  let stack = void 0;
  let done12 = false;
  while (!done12) {
    if (current2 !== void 0) {
      stack = make9(current2, stack);
      if (ord(min8, current2.key) <= 0) {
        current2 = current2.left;
      } else {
        current2 = void 0;
      }
    } else if (stack !== void 0 && ord(max10, stack.value.key) > 0) {
      if (ord(min8, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome2(value2)) {
          return value2;
        }
      }
      current2 = stack.value.right;
      stack = stack.previous;
    } else {
      done12 = true;
    }
  }
  return none2();
}, "visitBetween");
var fixDoubleBlack = /* @__PURE__ */ __name((stack) => {
  let n, p, s, z;
  for (let i = stack.length - 1; i >= 0; --i) {
    n = stack[i];
    if (i === 0) {
      n.color = Color.Black;
      return;
    }
    p = stack[i - 1];
    if (p.left === n) {
      s = p.right;
      if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.right = clone(s);
        z = s.right = clone(s.right);
        p.right = s.left;
        s.left = p;
        s.right = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.right = clone(s);
        z = s.left = clone(s.left);
        p.right = z.left;
        s.left = z.right;
        z.left = p;
        z.right = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = z;
          } else {
            pp.right = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.right = repaint(s, Color.Red);
          return;
        } else {
          p.right = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.right = s.left;
        s.left = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    } else {
      s = p.left;
      if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.left = clone(s);
        z = s.left = clone(s.left);
        p.left = s.right;
        s.right = p;
        s.left = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.left = clone(s);
        z = s.right = clone(s.right);
        p.left = z.right;
        s.right = z.left;
        z.right = p;
        z.left = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = z;
          } else {
            pp.left = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.left = repaint(s, Color.Red);
          return;
        } else {
          p.left = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.left = s.right;
        s.right = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    }
  }
}, "fixDoubleBlack");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/RedBlackTree.js
var TypeId19 = RedBlackTreeTypeId;
var Direction2 = Direction;
var isRedBlackTree2 = isRedBlackTree;
var empty31 = empty30;
var fromIterable13 = fromIterable12;
var make44 = make43;
var at3 = atForwards;
var atReversed = atBackwards;
var findAll2 = findAll;
var findFirst10 = findFirst9;
var first2 = first;
var getAt2 = getAt;
var getOrder5 = getOrder4;
var greaterThan6 = greaterThanForwards;
var greaterThanReversed = greaterThanBackwards;
var greaterThanEqual3 = greaterThanEqualForwards;
var greaterThanEqualReversed = greaterThanEqualBackwards;
var has8 = has7;
var insert2 = insert;
var keys6 = keysForward;
var keysReversed = keysBackward;
var last5 = last4;
var lessThan6 = lessThanForwards;
var lessThanReversed = lessThanBackwards;
var lessThanEqual3 = lessThanEqualForwards;
var lessThanEqualReversed = lessThanEqualBackwards;
var forEach13 = forEach12;
var forEachGreaterThanEqual2 = forEachGreaterThanEqual;
var forEachLessThan2 = forEachLessThan;
var forEachBetween2 = forEachBetween;
var reduce14 = reduce13;
var removeFirst2 = removeFirst;
var reversed2 = reversed;
var size12 = size11;
var values8 = valuesForward;
var valuesReversed = valuesBackward;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/SortedSet.js
var TypeId20 = /* @__PURE__ */ Symbol.for("effect/SortedSet");
var SortedSetProto = {
  [TypeId20]: {
    _A: /* @__PURE__ */ __name((_) => _, "_A")
  },
  [symbol2]() {
    return pipe(hash(this.keyTree), combine2(hash(TypeId20)), cached(this));
  },
  [symbol3](that) {
    return isSortedSet(that) && equals(this.keyTree, that.keyTree);
  },
  [Symbol.iterator]() {
    return keys6(this.keyTree);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "SortedSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var fromTree = /* @__PURE__ */ __name((keyTree) => {
  const a = Object.create(SortedSetProto);
  a.keyTree = keyTree;
  return a;
}, "fromTree");
var isSortedSet = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId20), "isSortedSet");
var empty32 = /* @__PURE__ */ __name((O) => fromTree(empty31(O)), "empty");
var fromIterable14 = /* @__PURE__ */ dual(2, (iterable, ord) => fromTree(fromIterable13(Array.from(iterable).map((k) => [k, true]), ord)));
var make45 = /* @__PURE__ */ __name((ord) => (...entries3) => fromIterable14(entries3, ord), "make");
var add5 = /* @__PURE__ */ dual(2, (self, value2) => has8(self.keyTree, value2) ? self : fromTree(insert2(self.keyTree, value2, true)));
var difference6 = /* @__PURE__ */ dual(2, (self, that) => {
  let out = self;
  for (const value2 of that) {
    out = remove10(out, value2);
  }
  return out;
});
var every12 = /* @__PURE__ */ dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (!predicate(value2)) {
      return false;
    }
  }
  return true;
});
var filter13 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const ord = getOrder5(self.keyTree);
  let out = empty32(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      out = add5(out, value2);
    }
  }
  return out;
});
var flatMap15 = /* @__PURE__ */ dual(3, (self, O, f) => {
  let out = empty32(O);
  forEach14(self, (a) => {
    for (const b of f(a)) {
      out = add5(out, b);
    }
  });
  return out;
});
var forEach14 = /* @__PURE__ */ dual(2, (self, f) => forEach13(self.keyTree, f));
var has9 = /* @__PURE__ */ dual(2, (self, value2) => has8(self.keyTree, value2));
var intersection6 = /* @__PURE__ */ dual(2, (self, that) => {
  const ord = getOrder5(self.keyTree);
  let out = empty32(ord);
  for (const value2 of that) {
    if (has9(self, value2)) {
      out = add5(out, value2);
    }
  }
  return out;
});
var isSubset3 = /* @__PURE__ */ dual(2, (self, that) => every12(self, (a) => has9(that, a)));
var map21 = /* @__PURE__ */ dual(3, (self, O, f) => {
  let out = empty32(O);
  forEach14(self, (a) => {
    const b = f(a);
    if (!has9(out, b)) {
      out = add5(out, b);
    }
  });
  return out;
});
var partition7 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const ord = getOrder5(self.keyTree);
  let right3 = empty32(ord);
  let left3 = empty32(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      right3 = add5(right3, value2);
    } else {
      left3 = add5(left3, value2);
    }
  }
  return [left3, right3];
});
var remove10 = /* @__PURE__ */ dual(2, (self, value2) => fromTree(removeFirst2(self.keyTree, value2)));
var size13 = /* @__PURE__ */ __name((self) => size12(self.keyTree), "size");
var some14 = /* @__PURE__ */ dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (predicate(value2)) {
      return true;
    }
  }
  return false;
});
var toggle4 = /* @__PURE__ */ dual(2, (self, value2) => has9(self, value2) ? remove10(self, value2) : add5(self, value2));
var union8 = /* @__PURE__ */ dual(2, (self, that) => {
  const ord = getOrder5(self.keyTree);
  let out = empty32(ord);
  for (const value2 of self) {
    out = add5(value2)(out);
  }
  for (const value2 of that) {
    out = add5(value2)(out);
  }
  return out;
});
var values9 = /* @__PURE__ */ __name((self) => keys6(self.keyTree), "values");
var getEquivalence8 = /* @__PURE__ */ __name(() => (a, b) => isSubset3(a, b) && isSubset3(b, a), "getEquivalence");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/supervisor.js
var SupervisorSymbolKey = "effect/Supervisor";
var SupervisorTypeId = /* @__PURE__ */ Symbol.for(SupervisorSymbolKey);
var supervisorVariance = {
  /* c8 ignore next */
  _T: /* @__PURE__ */ __name((_) => _, "_T")
};
var ProxySupervisor = class _ProxySupervisor {
  static {
    __name(this, "ProxySupervisor");
  }
  underlying;
  value0;
  [SupervisorTypeId] = supervisorVariance;
  constructor(underlying, value0) {
    this.underlying = underlying;
    this.value0 = value0;
  }
  get value() {
    return this.value0;
  }
  onStart(context11, effect4, parent, fiber) {
    this.underlying.onStart(context11, effect4, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.underlying.onEnd(value2, fiber);
  }
  onEffect(fiber, effect4) {
    this.underlying.onEffect(fiber, effect4);
  }
  onSuspend(fiber) {
    this.underlying.onSuspend(fiber);
  }
  onResume(fiber) {
    this.underlying.onResume(fiber);
  }
  map(f) {
    return new _ProxySupervisor(this, pipe(this.value, map14(f)));
  }
  zip(right3) {
    return new Zip(this, right3);
  }
};
var Zip = class _Zip {
  static {
    __name(this, "Zip");
  }
  left;
  right;
  _tag = "Zip";
  [SupervisorTypeId] = supervisorVariance;
  constructor(left3, right3) {
    this.left = left3;
    this.right = right3;
  }
  get value() {
    return zip5(this.left.value, this.right.value);
  }
  onStart(context11, effect4, parent, fiber) {
    this.left.onStart(context11, effect4, parent, fiber);
    this.right.onStart(context11, effect4, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.left.onEnd(value2, fiber);
    this.right.onEnd(value2, fiber);
  }
  onEffect(fiber, effect4) {
    this.left.onEffect(fiber, effect4);
    this.right.onEffect(fiber, effect4);
  }
  onSuspend(fiber) {
    this.left.onSuspend(fiber);
    this.right.onSuspend(fiber);
  }
  onResume(fiber) {
    this.left.onResume(fiber);
    this.right.onResume(fiber);
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map14(f)));
  }
  zip(right3) {
    return new _Zip(this, right3);
  }
};
var isZip = /* @__PURE__ */ __name((self) => hasProperty(self, SupervisorTypeId) && isTagged(self, "Zip"), "isZip");
var Track = class {
  static {
    __name(this, "Track");
  }
  [SupervisorTypeId] = supervisorVariance;
  fibers = /* @__PURE__ */ new Set();
  get value() {
    return sync(() => Array.from(this.fibers));
  }
  onStart(_context, _effect, _parent, fiber) {
    this.fibers.add(fiber);
  }
  onEnd(_value, fiber) {
    this.fibers.delete(fiber);
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map14(f)));
  }
  zip(right3) {
    return new Zip(this, right3);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var Const = class {
  static {
    __name(this, "Const");
  }
  effect;
  [SupervisorTypeId] = supervisorVariance;
  constructor(effect4) {
    this.effect = effect4;
  }
  get value() {
    return this.effect;
  }
  onStart(_context, _effect, _parent, _fiber) {
  }
  onEnd(_value, _fiber) {
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map14(f)));
  }
  zip(right3) {
    return new Zip(this, right3);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var FibersIn = class {
  static {
    __name(this, "FibersIn");
  }
  ref;
  [SupervisorTypeId] = supervisorVariance;
  constructor(ref) {
    this.ref = ref;
  }
  get value() {
    return sync(() => get7(this.ref));
  }
  onStart(_context, _effect, _parent, fiber) {
    pipe(this.ref, set3(pipe(get7(this.ref), add5(fiber))));
  }
  onEnd(_value, fiber) {
    pipe(this.ref, set3(pipe(get7(this.ref), remove10(fiber))));
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map14(f)));
  }
  zip(right3) {
    return new Zip(this, right3);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var unsafeTrack = /* @__PURE__ */ __name(() => {
  return new Track();
}, "unsafeTrack");
var track = /* @__PURE__ */ sync(unsafeTrack);
var fromEffect2 = /* @__PURE__ */ __name((effect4) => {
  return new Const(effect4);
}, "fromEffect");
var none8 = /* @__PURE__ */ globalValue("effect/Supervisor/none", () => fromEffect2(void_3));
var fibersIn = /* @__PURE__ */ __name((ref) => sync(() => new FibersIn(ref)), "fibersIn");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Differ.js
var TypeId21 = DifferTypeId;
var ChunkPatchTypeId2 = ChunkPatchTypeId;
var ContextPatchTypeId2 = ContextPatchTypeId;
var HashMapPatchTypeId2 = HashMapPatchTypeId;
var HashSetPatchTypeId2 = HashSetPatchTypeId;
var OrPatchTypeId2 = OrPatchTypeId;
var ReadonlyArrayPatchTypeId2 = ReadonlyArrayPatchTypeId;
var empty33 = /* @__PURE__ */ __name((self) => self.empty, "empty");
var diff10 = /* @__PURE__ */ dual(3, (self, oldValue, newValue) => self.diff(oldValue, newValue));
var combine14 = /* @__PURE__ */ dual(3, (self, first4, second) => self.combine(first4, second));
var patch11 = /* @__PURE__ */ dual(3, (self, patch13, oldValue) => self.patch(patch13, oldValue));
var make46 = make18;
var environment2 = environment;
var chunk2 = chunk;
var hashMap2 = hashMap;
var hashSet2 = hashSet;
var orElseEither4 = orElseEither2;
var readonlyArray2 = readonlyArray;
var transform2 = transform;
var update6 = update2;
var updateWith2 = updateWith;
var zip10 = zip4;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/supervisor/patch.js
var OP_EMPTY3 = "Empty";
var OP_ADD_SUPERVISOR = "AddSupervisor";
var OP_REMOVE_SUPERVISOR = "RemoveSupervisor";
var OP_AND_THEN2 = "AndThen";
var empty34 = {
  _tag: OP_EMPTY3
};
var combine15 = /* @__PURE__ */ __name((self, that) => {
  return {
    _tag: OP_AND_THEN2,
    first: self,
    second: that
  };
}, "combine");
var patch12 = /* @__PURE__ */ __name((self, supervisor) => {
  return patchLoop(supervisor, of3(self));
}, "patch");
var patchLoop = /* @__PURE__ */ __name((_supervisor, _patches) => {
  let supervisor = _supervisor;
  let patches = _patches;
  while (isNonEmpty(patches)) {
    const head10 = headNonEmpty2(patches);
    switch (head10._tag) {
      case OP_EMPTY3: {
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_ADD_SUPERVISOR: {
        supervisor = supervisor.zip(head10.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_REMOVE_SUPERVISOR: {
        supervisor = removeSupervisor(supervisor, head10.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_AND_THEN2: {
        patches = prepend3(head10.first)(prepend3(head10.second)(tailNonEmpty2(patches)));
        break;
      }
    }
  }
  return supervisor;
}, "patchLoop");
var removeSupervisor = /* @__PURE__ */ __name((self, that) => {
  if (equals(self, that)) {
    return none8;
  } else {
    if (isZip(self)) {
      return removeSupervisor(self.left, that).zip(removeSupervisor(self.right, that));
    } else {
      return self;
    }
  }
}, "removeSupervisor");
var toSet4 = /* @__PURE__ */ __name((self) => {
  if (equals(self, none8)) {
    return empty10();
  } else {
    if (isZip(self)) {
      return pipe(toSet4(self.left), union6(toSet4(self.right)));
    } else {
      return make12(self);
    }
  }
}, "toSet");
var diff11 = /* @__PURE__ */ __name((oldValue, newValue) => {
  if (equals(oldValue, newValue)) {
    return empty34;
  }
  const oldSupervisors = toSet4(oldValue);
  const newSupervisors = toSet4(newValue);
  const added = pipe(newSupervisors, difference5(oldSupervisors), reduce7(empty34, (patch13, supervisor) => combine15(patch13, {
    _tag: OP_ADD_SUPERVISOR,
    supervisor
  })));
  const removed = pipe(oldSupervisors, difference5(newSupervisors), reduce7(empty34, (patch13, supervisor) => combine15(patch13, {
    _tag: OP_REMOVE_SUPERVISOR,
    supervisor
  })));
  return combine15(added, removed);
}, "diff");
var differ2 = /* @__PURE__ */ make46({
  empty: empty34,
  patch: patch12,
  combine: combine15,
  diff: diff11
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/fiberRuntime.js
var fiberStarted = /* @__PURE__ */ counter5("effect_fiber_started", {
  incremental: true
});
var fiberActive = /* @__PURE__ */ counter5("effect_fiber_active");
var fiberSuccesses = /* @__PURE__ */ counter5("effect_fiber_successes", {
  incremental: true
});
var fiberFailures = /* @__PURE__ */ counter5("effect_fiber_failures", {
  incremental: true
});
var fiberLifetimes = /* @__PURE__ */ tagged2(/* @__PURE__ */ histogram5("effect_fiber_lifetimes", /* @__PURE__ */ exponential({
  start: 0.5,
  factor: 2,
  count: 35
})), "time_unit", "milliseconds");
var EvaluationSignalContinue = "Continue";
var EvaluationSignalDone = "Done";
var EvaluationSignalYieldNow = "Yield";
var runtimeFiberVariance = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var absurd2 = /* @__PURE__ */ __name((_) => {
  throw new Error(`BUG: FiberRuntime - ${toStringUnknown(_)} - please report an issue at https://github.com/Effect-TS/effect/issues`);
}, "absurd");
var YieldedOp = /* @__PURE__ */ Symbol.for("effect/internal/fiberRuntime/YieldedOp");
var yieldedOpChannel = /* @__PURE__ */ globalValue("effect/internal/fiberRuntime/yieldedOpChannel", () => ({
  currentOp: null
}));
var contOpSuccess = {
  [OP_ON_SUCCESS]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i1(value2));
  },
  ["OnStep"]: /* @__PURE__ */ __name((_, _cont, value2) => {
    return exitSucceed(exitSucceed(value2));
  }, "OnStep"),
  [OP_ON_SUCCESS_AND_FAILURE]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i2(value2));
  },
  [OP_REVERT_FLAGS]: (self, cont, value2) => {
    self.patchRuntimeFlags(self.currentRuntimeFlags, cont.patch);
    if (interruptible(self.currentRuntimeFlags) && self.isInterrupted()) {
      return exitFailCause(self.getInterruptedCause());
    } else {
      return exitSucceed(value2);
    }
  },
  [OP_WHILE]: (self, cont, value2) => {
    internalCall(() => cont.effect_instruction_i2(value2));
    if (internalCall(() => cont.effect_instruction_i0())) {
      self.pushStack(cont);
      return internalCall(() => cont.effect_instruction_i1());
    } else {
      return void_3;
    }
  },
  [OP_ITERATOR]: (self, cont, value2) => {
    while (true) {
      const state = internalCall(() => cont.effect_instruction_i0.next(value2));
      if (state.done) {
        return exitSucceed(state.value);
      }
      const primitive = yieldWrapGet(state.value);
      if (!exitIsExit(primitive)) {
        self.pushStack(cont);
        return primitive;
      } else if (primitive._tag === "Failure") {
        return primitive;
      }
      value2 = primitive.value;
    }
  }
};
var drainQueueWhileRunningTable = {
  [OP_INTERRUPT_SIGNAL]: (self, runtimeFlags2, cur, message) => {
    self.processNewInterruptSignal(message.cause);
    return interruptible(runtimeFlags2) ? exitFailCause(message.cause) : cur;
  },
  [OP_RESUME]: (_self, _runtimeFlags, _cur, _message) => {
    throw new Error("It is illegal to have multiple concurrent run loops in a single fiber");
  },
  [OP_STATEFUL]: (self, runtimeFlags2, cur, message) => {
    message.onFiber(self, running2(runtimeFlags2));
    return cur;
  },
  [OP_YIELD_NOW]: (_self, _runtimeFlags, cur, _message) => {
    return flatMap12(yieldNow(), () => cur);
  }
};
var runBlockedRequests = /* @__PURE__ */ __name((self) => forEachSequentialDiscard(flatten5(self), (requestsByRequestResolver) => forEachConcurrentDiscard(sequentialCollectionToChunk(requestsByRequestResolver), ([dataSource, sequential5]) => {
  const map36 = /* @__PURE__ */ new Map();
  const arr = [];
  for (const block of sequential5) {
    arr.push(toReadonlyArray(block));
    for (const entry of block) {
      map36.set(entry.request, entry);
    }
  }
  const flat = arr.flat();
  return fiberRefLocally(invokeWithInterrupt(dataSource.runAll(arr), flat, () => flat.forEach((entry) => {
    entry.listeners.interrupted = true;
  })), currentRequestMap, map36);
}, false, false)), "runBlockedRequests");
var _version = /* @__PURE__ */ getCurrentVersion();
var FiberRuntime = class extends Class3 {
  static {
    __name(this, "FiberRuntime");
  }
  [FiberTypeId] = fiberVariance2;
  [RuntimeFiberTypeId] = runtimeFiberVariance;
  _fiberRefs;
  _fiberId;
  _queue = /* @__PURE__ */ new Array();
  _children = null;
  _observers = /* @__PURE__ */ new Array();
  _running = false;
  _stack = [];
  _asyncInterruptor = null;
  _asyncBlockingOn = null;
  _exitValue = null;
  _steps = [];
  _isYielding = false;
  currentRuntimeFlags;
  currentOpCount = 0;
  currentSupervisor;
  currentScheduler;
  currentTracer;
  currentSpan;
  currentContext;
  currentDefaultServices;
  constructor(fiberId4, fiberRefs0, runtimeFlags0) {
    super();
    this.currentRuntimeFlags = runtimeFlags0;
    this._fiberId = fiberId4;
    this._fiberRefs = fiberRefs0;
    if (runtimeMetrics(runtimeFlags0)) {
      const tags = this.getFiberRef(currentMetricLabels);
      fiberStarted.unsafeUpdate(1, tags);
      fiberActive.unsafeUpdate(1, tags);
    }
    this.refreshRefCache();
  }
  commit() {
    return join3(this);
  }
  /**
   * The identity of the fiber.
   */
  id() {
    return this._fiberId;
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background. This can be called to "kick off" execution of a fiber after
   * it has been created.
   */
  resume(effect4) {
    this.tell(resume(effect4));
  }
  /**
   * The status of the fiber.
   */
  get status() {
    return this.ask((_, status3) => status3);
  }
  /**
   * Gets the fiber runtime flags.
   */
  get runtimeFlags() {
    return this.ask((state, status3) => {
      if (isDone3(status3)) {
        return state.currentRuntimeFlags;
      }
      return status3.runtimeFlags;
    });
  }
  /**
   * Returns the current `FiberScope` for the fiber.
   */
  scope() {
    return unsafeMake9(this);
  }
  /**
   * Retrieves the immediate children of the fiber.
   */
  get children() {
    return this.ask((fiber) => Array.from(fiber.getChildren()));
  }
  /**
   * Gets the fiber's set of children.
   */
  getChildren() {
    if (this._children === null) {
      this._children = /* @__PURE__ */ new Set();
    }
    return this._children;
  }
  /**
   * Retrieves the interrupted cause of the fiber, which will be `Cause.empty`
   * if the fiber has not been interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getInterruptedCause() {
    return this.getFiberRef(currentInterruptedCause);
  }
  /**
   * Retrieves the whole set of fiber refs.
   */
  fiberRefs() {
    return this.ask((fiber) => fiber.getFiberRefs());
  }
  /**
   * Returns an effect that will contain information computed from the fiber
   * state and status while running on the fiber.
   *
   * This allows the outside world to interact safely with mutable fiber state
   * without locks or immutable data.
   */
  ask(f) {
    return suspend(() => {
      const deferred = deferredUnsafeMake(this._fiberId);
      this.tell(stateful((fiber, status3) => {
        deferredUnsafeDone(deferred, sync(() => f(fiber, status3)));
      }));
      return deferredAwait(deferred);
    });
  }
  /**
   * Adds a message to be processed by the fiber on the fiber.
   */
  tell(message) {
    this._queue.push(message);
    if (!this._running) {
      this._running = true;
      this.drainQueueLaterOnExecutor();
    }
  }
  get await() {
    return async_((resume2) => {
      const cb = /* @__PURE__ */ __name((exit4) => resume2(succeed(exit4)), "cb");
      this.tell(stateful((fiber, _) => {
        if (fiber._exitValue !== null) {
          cb(this._exitValue);
        } else {
          fiber.addObserver(cb);
        }
      }));
      return sync(() => this.tell(stateful((fiber, _) => {
        fiber.removeObserver(cb);
      })));
    }, this.id());
  }
  get inheritAll() {
    return withFiberRuntime((parentFiber, parentStatus) => {
      const parentFiberId = parentFiber.id();
      const parentFiberRefs = parentFiber.getFiberRefs();
      const parentRuntimeFlags = parentStatus.runtimeFlags;
      const childFiberRefs = this.getFiberRefs();
      const updatedFiberRefs = joinAs(parentFiberRefs, parentFiberId, childFiberRefs);
      parentFiber.setFiberRefs(updatedFiberRefs);
      const updatedRuntimeFlags = parentFiber.getFiberRef(currentRuntimeFlags);
      const patch13 = pipe(
        diff7(parentRuntimeFlags, updatedRuntimeFlags),
        // Do not inherit WindDown or Interruption!
        exclude2(Interruption),
        exclude2(WindDown)
      );
      return updateRuntimeFlags(patch13);
    });
  }
  /**
   * Tentatively observes the fiber, but returns immediately if it is not
   * already done.
   */
  get poll() {
    return sync(() => fromNullable2(this._exitValue));
  }
  /**
   * Unsafely observes the fiber, but returns immediately if it is not
   * already done.
   */
  unsafePoll() {
    return this._exitValue;
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  interruptAsFork(fiberId4) {
    return sync(() => this.tell(interruptSignal(interrupt(fiberId4))));
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  unsafeInterruptAsFork(fiberId4) {
    this.tell(interruptSignal(interrupt(fiberId4)));
  }
  /**
   * Adds an observer to the list of observers.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addObserver(observer) {
    if (this._exitValue !== null) {
      observer(this._exitValue);
    } else {
      this._observers.push(observer);
    }
  }
  /**
   * Removes the specified observer from the list of observers that will be
   * notified when the fiber exits.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeObserver(observer) {
    this._observers = this._observers.filter((o) => o !== observer);
  }
  /**
   * Retrieves all fiber refs of the fiber.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRefs() {
    this.setFiberRef(currentRuntimeFlags, this.currentRuntimeFlags);
    return this._fiberRefs;
  }
  /**
   * Deletes the specified fiber ref.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  unsafeDeleteFiberRef(fiberRef) {
    this._fiberRefs = delete_(this._fiberRefs, fiberRef);
  }
  /**
   * Retrieves the state of the fiber ref, or else its initial value.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRef(fiberRef) {
    if (this._fiberRefs.locals.has(fiberRef)) {
      return this._fiberRefs.locals.get(fiberRef)[0][1];
    }
    return fiberRef.initial;
  }
  /**
   * Sets the fiber ref to the specified value.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRef(fiberRef, value2) {
    this._fiberRefs = updateAs(this._fiberRefs, {
      fiberId: this._fiberId,
      fiberRef,
      value: value2
    });
    this.refreshRefCache();
  }
  refreshRefCache() {
    this.currentDefaultServices = this.getFiberRef(currentServices);
    this.currentTracer = this.currentDefaultServices.unsafeMap.get(tracerTag.key);
    this.currentSupervisor = this.getFiberRef(currentSupervisor);
    this.currentScheduler = this.getFiberRef(currentScheduler);
    this.currentContext = this.getFiberRef(currentContext);
    this.currentSpan = this.currentContext.unsafeMap.get(spanTag.key);
  }
  /**
   * Wholesale replaces all fiber refs of this fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRefs(fiberRefs4) {
    this._fiberRefs = fiberRefs4;
    this.refreshRefCache();
  }
  /**
   * Adds a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addChild(child) {
    this.getChildren().add(child);
  }
  /**
   * Removes a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeChild(child) {
    this.getChildren().delete(child);
  }
  /**
   * Transfers all children of this fiber that are currently running to the
   * specified fiber scope.
   *
   * **NOTE**: This method must be invoked by the fiber itself after it has
   * evaluated the effects but prior to exiting.
   */
  transferChildren(scope6) {
    const children3 = this._children;
    this._children = null;
    if (children3 !== null && children3.size > 0) {
      for (const child of children3) {
        if (child._exitValue === null) {
          scope6.add(this.currentRuntimeFlags, child);
        }
      }
    }
  }
  /**
   * On the current thread, executes all messages in the fiber's inbox. This
   * method may return before all work is done, in the event the fiber executes
   * an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueOnCurrentThread() {
    let recurse = true;
    while (recurse) {
      let evaluationSignal = EvaluationSignalContinue;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        while (evaluationSignal === EvaluationSignalContinue) {
          evaluationSignal = this._queue.length === 0 ? EvaluationSignalDone : this.evaluateMessageWhileSuspended(this._queue.splice(0, 1)[0]);
        }
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
      }
      if (this._queue.length > 0 && !this._running) {
        this._running = true;
        if (evaluationSignal === EvaluationSignalYieldNow) {
          this.drainQueueLaterOnExecutor();
          recurse = false;
        } else {
          recurse = true;
        }
      } else {
        recurse = false;
      }
    }
  }
  /**
   * Schedules the execution of all messages in the fiber's inbox.
   *
   * This method will return immediately after the scheduling
   * operation is completed, but potentially before such messages have been
   * executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueLaterOnExecutor() {
    this.currentScheduler.scheduleTask(this.run, this.getFiberRef(currentSchedulingPriority));
  }
  /**
   * Drains the fiber's message queue while the fiber is actively running,
   * returning the next effect to execute, which may be the input effect if no
   * additional effect needs to be executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueWhileRunning(runtimeFlags2, cur0) {
    let cur = cur0;
    while (this._queue.length > 0) {
      const message = this._queue.splice(0, 1)[0];
      cur = drainQueueWhileRunningTable[message._tag](this, runtimeFlags2, cur, message);
    }
    return cur;
  }
  /**
   * Determines if the fiber is interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  isInterrupted() {
    return !isEmpty7(this.getFiberRef(currentInterruptedCause));
  }
  /**
   * Adds an interruptor to the set of interruptors that are interrupting this
   * fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addInterruptedCause(cause3) {
    const oldSC = this.getFiberRef(currentInterruptedCause);
    this.setFiberRef(currentInterruptedCause, sequential(oldSC, cause3));
  }
  /**
   * Processes a new incoming interrupt signal.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  processNewInterruptSignal(cause3) {
    this.addInterruptedCause(cause3);
    this.sendInterruptSignalToAllChildren();
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  sendInterruptSignalToAllChildren() {
    if (this._children === null || this._children.size === 0) {
      return false;
    }
    let told = false;
    for (const child of this._children) {
      child.tell(interruptSignal(interrupt(this.id())));
      told = true;
    }
    return told;
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  interruptAllChildren() {
    if (this.sendInterruptSignalToAllChildren()) {
      const it = this._children.values();
      this._children = null;
      let isDone9 = false;
      const body = /* @__PURE__ */ __name(() => {
        const next4 = it.next();
        if (!next4.done) {
          return asVoid2(next4.value.await);
        } else {
          return sync(() => {
            isDone9 = true;
          });
        }
      }, "body");
      return whileLoop({
        while: /* @__PURE__ */ __name(() => !isDone9, "while"),
        body,
        step: /* @__PURE__ */ __name(() => {
        }, "step")
      });
    }
    return null;
  }
  reportExitValue(exit4) {
    if (runtimeMetrics(this.currentRuntimeFlags)) {
      const tags = this.getFiberRef(currentMetricLabels);
      const startTimeMillis = this.id().startTimeMillis;
      const endTimeMillis = Date.now();
      fiberLifetimes.unsafeUpdate(endTimeMillis - startTimeMillis, tags);
      fiberActive.unsafeUpdate(-1, tags);
      switch (exit4._tag) {
        case OP_SUCCESS: {
          fiberSuccesses.unsafeUpdate(1, tags);
          break;
        }
        case OP_FAILURE: {
          fiberFailures.unsafeUpdate(1, tags);
          break;
        }
      }
    }
    if (exit4._tag === "Failure") {
      const level = this.getFiberRef(currentUnhandledErrorLogLevel);
      if (!isInterruptedOnly(exit4.cause) && level._tag === "Some") {
        this.log("Fiber terminated with an unhandled error", exit4.cause, level);
      }
    }
  }
  setExitValue(exit4) {
    this._exitValue = exit4;
    this.reportExitValue(exit4);
    for (let i = this._observers.length - 1; i >= 0; i--) {
      this._observers[i](exit4);
    }
    this._observers = [];
  }
  getLoggers() {
    return this.getFiberRef(currentLoggers);
  }
  log(message, cause3, overrideLogLevel) {
    const logLevel = isSome2(overrideLogLevel) ? overrideLogLevel.value : this.getFiberRef(currentLogLevel);
    const minimumLogLevel2 = this.getFiberRef(currentMinimumLogLevel);
    if (greaterThan4(minimumLogLevel2, logLevel)) {
      return;
    }
    const spans = this.getFiberRef(currentLogSpan);
    const annotations = this.getFiberRef(currentLogAnnotations);
    const loggers = this.getLoggers();
    const contextMap = this.getFiberRefs();
    if (size6(loggers) > 0) {
      const clockService = get4(this.getFiberRef(currentServices), clockTag);
      const date = new Date(clockService.unsafeCurrentTimeMillis());
      withRedactableContext(contextMap, () => {
        for (const logger of loggers) {
          logger.log({
            fiberId: this.id(),
            logLevel,
            message,
            cause: cause3,
            context: contextMap,
            spans,
            annotations,
            date
          });
        }
      });
    }
  }
  /**
   * Evaluates a single message on the current thread, while the fiber is
   * suspended. This method should only be called while evaluation of the
   * fiber's effect is suspended due to an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateMessageWhileSuspended(message) {
    switch (message._tag) {
      case OP_YIELD_NOW: {
        return EvaluationSignalYieldNow;
      }
      case OP_INTERRUPT_SIGNAL: {
        this.processNewInterruptSignal(message.cause);
        if (this._asyncInterruptor !== null) {
          this._asyncInterruptor(exitFailCause(message.cause));
          this._asyncInterruptor = null;
        }
        return EvaluationSignalContinue;
      }
      case OP_RESUME: {
        this._asyncInterruptor = null;
        this._asyncBlockingOn = null;
        this.evaluateEffect(message.effect);
        return EvaluationSignalContinue;
      }
      case OP_STATEFUL: {
        message.onFiber(this, this._exitValue !== null ? done4 : suspended2(this.currentRuntimeFlags, this._asyncBlockingOn));
        return EvaluationSignalContinue;
      }
      default: {
        return absurd2(message);
      }
    }
  }
  /**
   * Evaluates an effect until completion, potentially asynchronously.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateEffect(effect0) {
    this.currentSupervisor.onResume(this);
    try {
      let effect4 = interruptible(this.currentRuntimeFlags) && this.isInterrupted() ? exitFailCause(this.getInterruptedCause()) : effect0;
      while (effect4 !== null) {
        const eff = effect4;
        const exit4 = this.runLoop(eff);
        if (exit4 === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          yieldedOpChannel.currentOp = null;
          if (op._op === OP_YIELD) {
            if (cooperativeYielding(this.currentRuntimeFlags)) {
              this.tell(yieldNow3());
              this.tell(resume(exitVoid));
              effect4 = null;
            } else {
              effect4 = exitVoid;
            }
          } else if (op._op === OP_ASYNC) {
            effect4 = null;
          }
        } else {
          this.currentRuntimeFlags = pipe(this.currentRuntimeFlags, enable2(WindDown));
          const interruption2 = this.interruptAllChildren();
          if (interruption2 !== null) {
            effect4 = flatMap12(interruption2, () => exit4);
          } else {
            if (this._queue.length === 0) {
              this.setExitValue(exit4);
            } else {
              this.tell(resume(exit4));
            }
            effect4 = null;
          }
        }
      }
    } finally {
      this.currentSupervisor.onSuspend(this);
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on the current
   * thread. This can be called to "kick off" execution of a fiber after it has
   * been created, in hopes that the effect can be executed synchronously.
   *
   * This is not the normal way of starting a fiber, but it is useful when the
   * express goal of executing the fiber is to synchronously produce its exit.
   */
  start(effect4) {
    if (!this._running) {
      this._running = true;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        this.evaluateEffect(effect4);
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
        if (this._queue.length > 0) {
          this.drainQueueLaterOnExecutor();
        }
      }
    } else {
      this.tell(resume(effect4));
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background, and on the correct thread pool. This can be called to "kick
   * off" execution of a fiber after it has been created, in hopes that the
   * effect can be executed synchronously.
   */
  startFork(effect4) {
    this.tell(resume(effect4));
  }
  /**
   * Takes the current runtime flags, patches them to return the new runtime
   * flags, and then makes any changes necessary to fiber state based on the
   * specified patch.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  patchRuntimeFlags(oldRuntimeFlags, patch13) {
    const newRuntimeFlags = patch7(oldRuntimeFlags, patch13);
    globalThis[currentFiberURI] = this;
    this.currentRuntimeFlags = newRuntimeFlags;
    return newRuntimeFlags;
  }
  /**
   * Initiates an asynchronous operation, by building a callback that will
   * resume execution, and then feeding that callback to the registration
   * function, handling error cases and repeated resumptions appropriately.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  initiateAsync(runtimeFlags2, asyncRegister) {
    let alreadyCalled = false;
    const callback = /* @__PURE__ */ __name((effect4) => {
      if (!alreadyCalled) {
        alreadyCalled = true;
        this.tell(resume(effect4));
      }
    }, "callback");
    if (interruptible(runtimeFlags2)) {
      this._asyncInterruptor = callback;
    }
    try {
      asyncRegister(callback);
    } catch (e) {
      callback(failCause(die(e)));
    }
  }
  pushStack(cont) {
    this._stack.push(cont);
    if (cont._op === "OnStep") {
      this._steps.push({
        refs: this.getFiberRefs(),
        flags: this.currentRuntimeFlags
      });
    }
  }
  popStack() {
    const item = this._stack.pop();
    if (item) {
      if (item._op === "OnStep") {
        this._steps.pop();
      }
      return item;
    }
    return;
  }
  getNextSuccessCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_FAILURE) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  getNextFailCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_SUCCESS && frame._op !== OP_WHILE && frame._op !== OP_ITERATOR) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  [OP_TAG](op) {
    return sync(() => unsafeGet3(this.currentContext, op));
  }
  ["Left"](op) {
    return fail2(op.left);
  }
  ["None"](_) {
    return fail2(new NoSuchElementException());
  }
  ["Right"](op) {
    return exitSucceed(op.right);
  }
  ["Some"](op) {
    return exitSucceed(op.value);
  }
  ["Micro"](op) {
    return unsafeAsync((microResume) => {
      let resume2 = microResume;
      const fiber = runFork(provideContext2(op, this.currentContext));
      fiber.addObserver((exit4) => {
        if (exit4._tag === "Success") {
          return resume2(exitSucceed(exit4.value));
        }
        switch (exit4.cause._tag) {
          case "Interrupt": {
            return resume2(exitFailCause(interrupt(none4)));
          }
          case "Fail": {
            return resume2(fail2(exit4.cause.error));
          }
          case "Die": {
            return resume2(die2(exit4.cause.defect));
          }
        }
      });
      return unsafeAsync((abortResume) => {
        resume2 = /* @__PURE__ */ __name((_) => {
          abortResume(void_3);
        }, "resume");
        fiber.unsafeInterrupt();
      });
    });
  }
  [OP_SYNC](op) {
    const value2 = internalCall(() => op.effect_instruction_i0());
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd2(cont);
      }
      return contOpSuccess[cont._op](this, cont, value2);
    } else {
      yieldedOpChannel.currentOp = exitSucceed(value2);
      return YieldedOp;
    }
  }
  [OP_SUCCESS](op) {
    const oldCur = op;
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd2(cont);
      }
      return contOpSuccess[cont._op](this, cont, oldCur.effect_instruction_i0);
    } else {
      yieldedOpChannel.currentOp = oldCur;
      return YieldedOp;
    }
  }
  [OP_FAILURE](op) {
    const cause3 = op.effect_instruction_i0;
    const cont = this.getNextFailCont();
    if (cont !== void 0) {
      switch (cont._op) {
        case OP_ON_FAILURE:
        case OP_ON_SUCCESS_AND_FAILURE: {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return internalCall(() => cont.effect_instruction_i1(cause3));
          } else {
            return exitFailCause(stripFailures(cause3));
          }
        }
        case "OnStep": {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return exitSucceed(exitFailCause(cause3));
          } else {
            return exitFailCause(stripFailures(cause3));
          }
        }
        case OP_REVERT_FLAGS: {
          this.patchRuntimeFlags(this.currentRuntimeFlags, cont.patch);
          if (interruptible(this.currentRuntimeFlags) && this.isInterrupted()) {
            return exitFailCause(sequential(cause3, this.getInterruptedCause()));
          } else {
            return exitFailCause(cause3);
          }
        }
        default: {
          absurd2(cont);
        }
      }
    } else {
      yieldedOpChannel.currentOp = exitFailCause(cause3);
      return YieldedOp;
    }
  }
  [OP_WITH_RUNTIME](op) {
    return internalCall(() => op.effect_instruction_i0(this, running2(this.currentRuntimeFlags)));
  }
  ["Blocked"](op) {
    const refs = this.getFiberRefs();
    const flags = this.currentRuntimeFlags;
    if (this._steps.length > 0) {
      const frames = [];
      const snap = this._steps[this._steps.length - 1];
      let frame = this.popStack();
      while (frame && frame._op !== "OnStep") {
        frames.push(frame);
        frame = this.popStack();
      }
      this.setFiberRefs(snap.refs);
      this.currentRuntimeFlags = snap.flags;
      const patchRefs = diff9(snap.refs, refs);
      const patchFlags = diff7(snap.flags, flags);
      return exitSucceed(blocked(op.effect_instruction_i0, withFiberRuntime((newFiber) => {
        while (frames.length > 0) {
          newFiber.pushStack(frames.pop());
        }
        newFiber.setFiberRefs(patch10(newFiber.id(), newFiber.getFiberRefs())(patchRefs));
        newFiber.currentRuntimeFlags = patch7(patchFlags)(newFiber.currentRuntimeFlags);
        return op.effect_instruction_i1;
      })));
    }
    return uninterruptibleMask((restore) => flatMap12(forkDaemon2(runRequestBlock(op.effect_instruction_i0)), () => restore(op.effect_instruction_i1)));
  }
  ["RunBlocked"](op) {
    return runBlockedRequests(op.effect_instruction_i0);
  }
  [OP_UPDATE_RUNTIME_FLAGS](op) {
    const updateFlags = op.effect_instruction_i0;
    const oldRuntimeFlags = this.currentRuntimeFlags;
    const newRuntimeFlags = patch7(oldRuntimeFlags, updateFlags);
    if (interruptible(newRuntimeFlags) && this.isInterrupted()) {
      return exitFailCause(this.getInterruptedCause());
    } else {
      this.patchRuntimeFlags(this.currentRuntimeFlags, updateFlags);
      if (op.effect_instruction_i1) {
        const revertFlags = diff7(newRuntimeFlags, oldRuntimeFlags);
        this.pushStack(new RevertFlags(revertFlags, op));
        return internalCall(() => op.effect_instruction_i1(oldRuntimeFlags));
      } else {
        return exitVoid;
      }
    }
  }
  [OP_ON_SUCCESS](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  ["OnStep"](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_SUCCESS_AND_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ASYNC](op) {
    this._asyncBlockingOn = op.effect_instruction_i1;
    this.initiateAsync(this.currentRuntimeFlags, op.effect_instruction_i0);
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_YIELD](op) {
    this._isYielding = false;
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_WHILE](op) {
    const check4 = op.effect_instruction_i0;
    const body = op.effect_instruction_i1;
    if (check4()) {
      this.pushStack(op);
      return body();
    } else {
      return exitVoid;
    }
  }
  [OP_ITERATOR](op) {
    return contOpSuccess[OP_ITERATOR](this, op, void 0);
  }
  [OP_COMMIT](op) {
    return internalCall(() => op.commit());
  }
  /**
   * The main run-loop for evaluating effects.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  runLoop(effect0) {
    let cur = effect0;
    this.currentOpCount = 0;
    while (true) {
      if ((this.currentRuntimeFlags & OpSupervision) !== 0) {
        this.currentSupervisor.onEffect(this, cur);
      }
      if (this._queue.length > 0) {
        cur = this.drainQueueWhileRunning(this.currentRuntimeFlags, cur);
      }
      if (!this._isYielding) {
        this.currentOpCount += 1;
        const shouldYield = this.currentScheduler.shouldYield(this);
        if (shouldYield !== false) {
          this._isYielding = true;
          this.currentOpCount = 0;
          const oldCur = cur;
          cur = flatMap12(yieldNow({
            priority: shouldYield
          }), () => oldCur);
        }
      }
      try {
        cur = this.currentTracer.context(() => {
          if (_version !== cur[EffectTypeId2]._V) {
            const level = this.getFiberRef(currentVersionMismatchErrorLogLevel);
            if (level._tag === "Some") {
              const effectVersion = cur[EffectTypeId2]._V;
              this.log(`Executing an Effect versioned ${effectVersion} with a Runtime of version ${getCurrentVersion()}, you may want to dedupe the effect dependencies, you can use the language service plugin to detect this at compile time: https://github.com/Effect-TS/language-service`, empty22, level);
            }
          }
          return this[cur._op](cur);
        }, this);
        if (cur === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          if (op._op === OP_YIELD || op._op === OP_ASYNC) {
            return YieldedOp;
          }
          yieldedOpChannel.currentOp = null;
          return op._op === OP_SUCCESS || op._op === OP_FAILURE ? op : exitFailCause(die(op));
        }
      } catch (e) {
        if (cur !== YieldedOp && !hasProperty(cur, "_op") || !(cur._op in this)) {
          cur = dieMessage(`Not a valid effect: ${toStringUnknown(cur)}`);
        } else if (isInterruptedException(e)) {
          cur = exitFailCause(sequential(die(e), interrupt(none4)));
        } else {
          cur = die2(e);
        }
      }
    }
  }
  run = /* @__PURE__ */ __name(() => {
    this.drainQueueOnCurrentThread();
  }, "run");
};
var currentMinimumLogLevel = /* @__PURE__ */ globalValue("effect/FiberRef/currentMinimumLogLevel", () => fiberRefUnsafeMake(fromLiteral("Info")));
var loggerWithConsoleLog = /* @__PURE__ */ __name((self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get4(services, consoleTag).unsafe.log(self.log(opts));
}), "loggerWithConsoleLog");
var loggerWithLeveledLog = /* @__PURE__ */ __name((self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  const unsafeLogger = get4(services, consoleTag).unsafe;
  switch (opts.logLevel._tag) {
    case "Debug":
      return unsafeLogger.debug(self.log(opts));
    case "Info":
      return unsafeLogger.info(self.log(opts));
    case "Trace":
      return unsafeLogger.trace(self.log(opts));
    case "Warning":
      return unsafeLogger.warn(self.log(opts));
    case "Error":
    case "Fatal":
      return unsafeLogger.error(self.log(opts));
    default:
      return unsafeLogger.log(self.log(opts));
  }
}), "loggerWithLeveledLog");
var loggerWithConsoleError = /* @__PURE__ */ __name((self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get4(services, consoleTag).unsafe.error(self.log(opts));
}), "loggerWithConsoleError");
var defaultLogger = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/defaultLogger"), () => loggerWithConsoleLog(stringLogger));
var jsonLogger2 = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/jsonLogger"), () => loggerWithConsoleLog(jsonLogger));
var logFmtLogger = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/logFmtLogger"), () => loggerWithConsoleLog(logfmtLogger));
var prettyLogger2 = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/prettyLogger"), () => prettyLoggerDefault);
var structuredLogger2 = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/structuredLogger"), () => loggerWithConsoleLog(structuredLogger));
var tracerLogger = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/Logger/tracerLogger"), () => makeLogger(({
  annotations,
  cause: cause3,
  context: context11,
  fiberId: fiberId4,
  logLevel,
  message
}) => {
  const span4 = getOption2(getOrDefault(context11, currentContext), spanTag);
  if (span4._tag === "None" || span4.value._tag === "ExternalSpan") {
    return;
  }
  const clockService = unsafeGet3(getOrDefault(context11, currentServices), clockTag);
  const attributes = {};
  for (const [key, value2] of annotations) {
    attributes[key] = value2;
  }
  attributes["effect.fiberId"] = threadName2(fiberId4);
  attributes["effect.logLevel"] = logLevel.label;
  if (cause3 !== null && cause3._tag !== "Empty") {
    attributes["effect.cause"] = pretty(cause3, {
      renderErrorCause: true
    });
  }
  span4.value.event(toStringUnknown(Array.isArray(message) && message.length === 1 ? message[0] : message), clockService.unsafeCurrentTimeNanos(), attributes);
}));
var loggerWithSpanAnnotations = /* @__PURE__ */ __name((self) => mapInputOptions(self, (options) => {
  const span4 = flatMap2(get10(options.context, currentContext), getOption2(spanTag));
  if (span4._tag === "None") {
    return options;
  }
  return {
    ...options,
    annotations: pipe(options.annotations, set4("effect.traceId", span4.value.traceId), set4("effect.spanId", span4.value.spanId), span4.value._tag === "Span" ? set4("effect.spanName", span4.value.name) : identity)
  };
}), "loggerWithSpanAnnotations");
var currentLoggers = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentLoggers"), () => fiberRefUnsafeMakeHashSet(make12(defaultLogger, tracerLogger)));
var batchedLogger = /* @__PURE__ */ dual(3, (self, window3, f) => flatMap12(scope2, (scope6) => {
  let buffer4 = [];
  const flush = suspend(() => {
    if (buffer4.length === 0) {
      return void_3;
    }
    const arr = buffer4;
    buffer4 = [];
    return f(arr);
  });
  return uninterruptibleMask((restore) => pipe(sleep3(window3), zipRight2(flush), forever, restore, forkDaemon2, flatMap12((fiber) => scopeAddFinalizer(scope6, interruptFiber(fiber))), zipRight2(addFinalizer2(() => flush)), as3(makeLogger((options) => {
    buffer4.push(self.log(options));
  }))));
}));
var annotateLogsScoped = /* @__PURE__ */ __name(function() {
  if (typeof arguments[0] === "string") {
    return fiberRefLocallyScopedWith(currentLogAnnotations, set4(arguments[0], arguments[1]));
  }
  const entries3 = Object.entries(arguments[0]);
  return fiberRefLocallyScopedWith(currentLogAnnotations, mutate4((annotations) => {
    for (let i = 0; i < entries3.length; i++) {
      const [key, value2] = entries3[i];
      set4(annotations, key, value2);
    }
    return annotations;
  }));
}, "annotateLogsScoped");
var whenLogLevel = /* @__PURE__ */ dual(2, (effect4, level) => {
  const requiredLogLevel = typeof level === "string" ? fromLiteral(level) : level;
  return withFiberRuntime((fiberState) => {
    const minimumLogLevel2 = fiberState.getFiberRef(currentMinimumLogLevel);
    if (greaterThan4(minimumLogLevel2, requiredLogLevel)) {
      return succeed(none2());
    }
    return map14(effect4, some3);
  });
});
var acquireRelease2 = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (acquire, release) => uninterruptible(tap2(acquire, (a) => addFinalizer2((exit4) => release(a, exit4)))));
var acquireReleaseInterruptible = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (acquire, release) => ensuring2(acquire, addFinalizer2((exit4) => release(exit4))));
var addFinalizer2 = /* @__PURE__ */ __name((finalizer3) => withFiberRuntime((runtime5) => {
  const acquireRefs = runtime5.getFiberRefs();
  const acquireFlags = disable2(runtime5.currentRuntimeFlags, Interruption);
  return flatMap12(scope2, (scope6) => scopeAddFinalizerExit(scope6, (exit4) => withFiberRuntime((runtimeFinalizer) => {
    const preRefs = runtimeFinalizer.getFiberRefs();
    const preFlags = runtimeFinalizer.currentRuntimeFlags;
    const patchRefs = diff9(preRefs, acquireRefs);
    const patchFlags = diff7(preFlags, acquireFlags);
    const inverseRefs = diff9(acquireRefs, preRefs);
    runtimeFinalizer.setFiberRefs(patch10(patchRefs, runtimeFinalizer.id(), acquireRefs));
    return ensuring2(withRuntimeFlags(finalizer3(exit4), patchFlags), sync(() => {
      runtimeFinalizer.setFiberRefs(patch10(inverseRefs, runtimeFinalizer.id(), runtimeFinalizer.getFiberRefs()));
    }));
  })));
}), "addFinalizer");
var daemonChildren = /* @__PURE__ */ __name((self) => {
  const forkScope = fiberRefLocally(currentForkScopeOverride, some3(globalScope));
  return forkScope(self);
}, "daemonChildren");
var _existsParFound = /* @__PURE__ */ Symbol.for("effect/Effect/existsPar/found");
var exists3 = /* @__PURE__ */ dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => matchSimple(options?.concurrency, () => suspend(() => existsLoop(elements[Symbol.iterator](), 0, predicate)), () => matchEffect(forEach15(elements, (a, i) => if_(predicate(a, i), {
  onTrue: /* @__PURE__ */ __name(() => fail2(_existsParFound), "onTrue"),
  onFalse: /* @__PURE__ */ __name(() => void_3, "onFalse")
}), options), {
  onFailure: /* @__PURE__ */ __name((e) => e === _existsParFound ? succeed(true) : fail2(e), "onFailure"),
  onSuccess: /* @__PURE__ */ __name(() => succeed(false), "onSuccess")
})));
var existsLoop = /* @__PURE__ */ __name((iterator, index, f) => {
  const next4 = iterator.next();
  if (next4.done) {
    return succeed(false);
  }
  return flatMap12(f(next4.value, index), (b) => b ? succeed(b) : existsLoop(iterator, index + 1, f));
}, "existsLoop");
var filter14 = /* @__PURE__ */ dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => {
  const predicate_ = options?.negate ? (a, i) => map14(predicate(a, i), not2) : predicate;
  return matchSimple(options?.concurrency, () => suspend(() => fromIterable2(elements).reduceRight((effect4, a, i) => zipWith6(effect4, suspend(() => predicate_(a, i)), (list, b) => b ? [a, ...list] : list), sync(() => new Array()))), () => map14(forEach15(elements, (a, i) => map14(predicate_(a, i), (b) => b ? some3(a) : none2()), options), getSomes3));
});
var allResolveInput = /* @__PURE__ */ __name((input) => {
  if (Array.isArray(input) || isIterable(input)) {
    return [input, none2()];
  }
  const keys7 = Object.keys(input);
  const size25 = keys7.length;
  return [keys7.map((k) => input[k]), some3((values10) => {
    const res = {};
    for (let i = 0; i < size25; i++) {
      ;
      res[keys7[i]] = values10[i];
    }
    return res;
  })];
}, "allResolveInput");
var allValidate = /* @__PURE__ */ __name((effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect4 of effects) {
    eitherEffects.push(either3(effect4));
  }
  return flatMap12(forEach15(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => {
    const none11 = none2();
    const size25 = eithers.length;
    const errors = new Array(size25);
    const successes = new Array(size25);
    let errored = false;
    for (let i = 0; i < size25; i++) {
      const either11 = eithers[i];
      if (either11._tag === "Left") {
        errors[i] = some3(either11.left);
        errored = true;
      } else {
        successes[i] = either11.right;
        errors[i] = none11;
      }
    }
    if (errored) {
      return reconcile._tag === "Some" ? fail2(reconcile.value(errors)) : fail2(errors);
    } else if (options?.discard) {
      return void_3;
    }
    return reconcile._tag === "Some" ? succeed(reconcile.value(successes)) : succeed(successes);
  });
}, "allValidate");
var allEither = /* @__PURE__ */ __name((effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect4 of effects) {
    eitherEffects.push(either3(effect4));
  }
  if (options?.discard) {
    return forEach15(eitherEffects, identity, {
      concurrency: options?.concurrency,
      batching: options?.batching,
      discard: true,
      concurrentFinalizers: options?.concurrentFinalizers
    });
  }
  return map14(forEach15(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => reconcile._tag === "Some" ? reconcile.value(eithers) : eithers);
}, "allEither");
var all8 = /* @__PURE__ */ __name((arg, options) => {
  const [effects, reconcile] = allResolveInput(arg);
  if (options?.mode === "validate") {
    return allValidate(effects, reconcile, options);
  } else if (options?.mode === "either") {
    return allEither(effects, reconcile, options);
  }
  return options?.discard !== true && reconcile._tag === "Some" ? map14(forEach15(effects, identity, options), reconcile.value) : forEach15(effects, identity, options);
}, "all");
var allWith = /* @__PURE__ */ __name((options) => (arg) => all8(arg, options), "allWith");
var allSuccesses = /* @__PURE__ */ __name((elements, options) => map14(all8(fromIterable2(elements).map(exit), options), filterMap4((exit4) => exitIsSuccess(exit4) ? some3(exit4.effect_instruction_i0) : none2())), "allSuccesses");
var replicate4 = /* @__PURE__ */ dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect2 = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, n, options) => all8(replicate4(self, n), options));
var forEach15 = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (self, f, options) => withFiberRuntime((r) => {
  const isRequestBatchingEnabled = options?.batching === true || options?.batching === "inherit" && r.getFiberRef(currentRequestBatching);
  if (options?.discard) {
    return match12(options.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), true, false, 1) : forEachSequentialDiscard(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false, n)));
  }
  return match12(options?.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachParN(self, 1, (a, i) => restore(f(a, i)), true) : forEachSequential(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachParUnbounded(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachParN(self, n, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)));
}));
var forEachParUnbounded = /* @__PURE__ */ __name((self, f, batching) => suspend(() => {
  const as15 = fromIterable2(self);
  const array5 = new Array(as15.length);
  const fn2 = /* @__PURE__ */ __name((a, i) => flatMap12(f(a, i), (b) => sync(() => array5[i] = b)), "fn");
  return zipRight2(forEachConcurrentDiscard(as15, fn2, batching, false), succeed(array5));
}), "forEachParUnbounded");
var forEachConcurrentDiscard = /* @__PURE__ */ __name((self, f, batching, processAll, n) => uninterruptibleMask((restore) => transplant((graft) => withFiberRuntime((parent) => {
  let todos = Array.from(self).reverse();
  let target = todos.length;
  if (target === 0) {
    return void_3;
  }
  let counter6 = 0;
  let interrupted4 = false;
  const fibersCount = n ? Math.min(todos.length, n) : todos.length;
  const fibers = /* @__PURE__ */ new Set();
  const results = new Array();
  const interruptAll3 = /* @__PURE__ */ __name(() => fibers.forEach((fiber) => {
    fiber.currentScheduler.scheduleTask(() => {
      fiber.unsafeInterruptAsFork(parent.id());
    }, 0);
  }), "interruptAll");
  const startOrder = new Array();
  const joinOrder = new Array();
  const residual = new Array();
  const collectExits = /* @__PURE__ */ __name(() => {
    const exits = results.filter(({
      exit: exit4
    }) => exit4._tag === "Failure").sort((a, b) => a.index < b.index ? -1 : a.index === b.index ? 0 : 1).map(({
      exit: exit4
    }) => exit4);
    if (exits.length === 0) {
      exits.push(exitVoid);
    }
    return exits;
  }, "collectExits");
  const runFiber = /* @__PURE__ */ __name((eff, interruptImmediately = false) => {
    const runnable = uninterruptible(graft(eff));
    const fiber = unsafeForkUnstarted(runnable, parent, parent.currentRuntimeFlags, globalScope);
    parent.currentScheduler.scheduleTask(() => {
      if (interruptImmediately) {
        fiber.unsafeInterruptAsFork(parent.id());
      }
      fiber.resume(runnable);
    }, 0);
    return fiber;
  }, "runFiber");
  const onInterruptSignal = /* @__PURE__ */ __name(() => {
    if (!processAll) {
      target -= todos.length;
      todos = [];
    }
    interrupted4 = true;
    interruptAll3();
  }, "onInterruptSignal");
  const stepOrExit = batching ? step2 : exit;
  const processingFiber = runFiber(async_((resume2) => {
    const pushResult = /* @__PURE__ */ __name((res, index) => {
      if (res._op === "Blocked") {
        residual.push(res);
      } else {
        results.push({
          index,
          exit: res
        });
        if (res._op === "Failure" && !interrupted4) {
          onInterruptSignal();
        }
      }
    }, "pushResult");
    const next4 = /* @__PURE__ */ __name(() => {
      if (todos.length > 0) {
        const a = todos.pop();
        let index = counter6++;
        const returnNextElement = /* @__PURE__ */ __name(() => {
          const a2 = todos.pop();
          index = counter6++;
          return flatMap12(yieldNow(), () => flatMap12(stepOrExit(restore(f(a2, index))), onRes));
        }, "returnNextElement");
        const onRes = /* @__PURE__ */ __name((res) => {
          if (todos.length > 0) {
            pushResult(res, index);
            if (todos.length > 0) {
              return returnNextElement();
            }
          }
          return succeed(res);
        }, "onRes");
        const todo = flatMap12(stepOrExit(restore(f(a, index))), onRes);
        const fiber = runFiber(todo);
        startOrder.push(fiber);
        fibers.add(fiber);
        if (interrupted4) {
          fiber.currentScheduler.scheduleTask(() => {
            fiber.unsafeInterruptAsFork(parent.id());
          }, 0);
        }
        fiber.addObserver((wrapped) => {
          let exit4;
          if (wrapped._op === "Failure") {
            exit4 = wrapped;
          } else {
            exit4 = wrapped.effect_instruction_i0;
          }
          joinOrder.push(fiber);
          fibers.delete(fiber);
          pushResult(exit4, index);
          if (results.length === target) {
            resume2(succeed(getOrElse2(exitCollectAll(collectExits(), {
              parallel: true
            }), () => exitVoid)));
          } else if (residual.length + results.length === target) {
            const exits = collectExits();
            const requests = residual.map((blocked3) => blocked3.effect_instruction_i0).reduce(par);
            resume2(succeed(blocked(requests, forEachConcurrentDiscard([getOrElse2(exitCollectAll(exits, {
              parallel: true
            }), () => exitVoid), ...residual.map((blocked3) => blocked3.effect_instruction_i1)], (i) => i, batching, true, n))));
          } else {
            next4();
          }
        });
      }
    }, "next");
    for (let i = 0; i < fibersCount; i++) {
      next4();
    }
  }));
  return asVoid2(onExit(flatten7(restore(join3(processingFiber))), exitMatch({
    onFailure: /* @__PURE__ */ __name((cause3) => {
      onInterruptSignal();
      const target2 = residual.length + 1;
      const concurrency = Math.min(typeof n === "number" ? n : residual.length, residual.length);
      const toPop = Array.from(residual);
      return async_((cb) => {
        const exits = [];
        let count5 = 0;
        let index = 0;
        const check4 = /* @__PURE__ */ __name((index2, hitNext) => (exit4) => {
          exits[index2] = exit4;
          count5++;
          if (count5 === target2) {
            cb(exitSucceed(exitFailCause(cause3)));
          }
          if (toPop.length > 0 && hitNext) {
            next4();
          }
        }, "check");
        const next4 = /* @__PURE__ */ __name(() => {
          runFiber(toPop.pop(), true).addObserver(check4(index, true));
          index++;
        }, "next");
        processingFiber.addObserver(check4(index, false));
        index++;
        for (let i = 0; i < concurrency; i++) {
          next4();
        }
      });
    }, "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => forEachSequential(joinOrder, (f2) => f2.inheritAll), "onSuccess")
  })));
}))), "forEachConcurrentDiscard");
var forEachParN = /* @__PURE__ */ __name((self, n, f, batching) => suspend(() => {
  const as15 = fromIterable2(self);
  const array5 = new Array(as15.length);
  const fn2 = /* @__PURE__ */ __name((a, i) => map14(f(a, i), (b) => array5[i] = b), "fn");
  return zipRight2(forEachConcurrentDiscard(as15, fn2, batching, false, n), succeed(array5));
}), "forEachParN");
var fork2 = /* @__PURE__ */ __name((self) => withFiberRuntime((state, status3) => succeed(unsafeFork2(self, state, status3.runtimeFlags))), "fork");
var forkDaemon2 = /* @__PURE__ */ __name((self) => forkWithScopeOverride(self, globalScope), "forkDaemon");
var forkWithErrorHandler = /* @__PURE__ */ dual(2, (self, handler) => fork2(onError(self, (cause3) => {
  const either11 = failureOrCause(cause3);
  switch (either11._tag) {
    case "Left":
      return handler(either11.left);
    case "Right":
      return failCause(either11.right);
  }
})));
var unsafeFork2 = /* @__PURE__ */ __name((effect4, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect4, parentFiber, parentRuntimeFlags, overrideScope);
  childFiber.resume(effect4);
  return childFiber;
}, "unsafeFork");
var unsafeForkUnstarted = /* @__PURE__ */ __name((effect4, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect4, parentFiber, parentRuntimeFlags, overrideScope);
  return childFiber;
}, "unsafeForkUnstarted");
var unsafeMakeChildFiber = /* @__PURE__ */ __name((effect4, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childId = unsafeMake3();
  const parentFiberRefs = parentFiber.getFiberRefs();
  const childFiberRefs = forkAs(parentFiberRefs, childId);
  const childFiber = new FiberRuntime(childId, childFiberRefs, parentRuntimeFlags);
  const childContext = getOrDefault(childFiberRefs, currentContext);
  const supervisor = childFiber.currentSupervisor;
  supervisor.onStart(childContext, effect4, some3(parentFiber), childFiber);
  childFiber.addObserver((exit4) => supervisor.onEnd(exit4, childFiber));
  const parentScope = overrideScope !== null ? overrideScope : pipe(parentFiber.getFiberRef(currentForkScopeOverride), getOrElse2(() => parentFiber.scope()));
  parentScope.add(parentRuntimeFlags, childFiber);
  return childFiber;
}, "unsafeMakeChildFiber");
var forkWithScopeOverride = /* @__PURE__ */ __name((self, scopeOverride) => withFiberRuntime((parentFiber, parentStatus) => succeed(unsafeFork2(self, parentFiber, parentStatus.runtimeFlags, scopeOverride))), "forkWithScopeOverride");
var mergeAll3 = /* @__PURE__ */ dual((args2) => isFunction2(args2[2]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable2(elements).reduce((acc, a, i) => zipWith6(acc, a, (acc2, a2) => f(acc2, a2, i)), succeed(zero2)), () => flatMap12(make35(zero2), (acc) => flatMap12(forEach15(elements, (effect4, i) => flatMap12(effect4, (a) => update4(acc, (b) => f(b, a, i))), options), () => get13(acc)))));
var partition8 = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (elements, f, options) => pipe(forEach15(elements, (a, i) => either3(f(a, i)), options), map14((chunk4) => partitionMap6(chunk4, identity))));
var validateAll = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (elements, f, options) => flatMap12(partition8(elements, f, {
  concurrency: options?.concurrency,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([es, bs]) => isNonEmptyArray2(es) ? fail2(es) : options?.discard ? void_3 : succeed(bs)));
var raceAll2 = /* @__PURE__ */ __name((all12) => withFiberRuntime((state, status3) => async_((resume2) => {
  const fibers = /* @__PURE__ */ new Set();
  let winner;
  let failures3 = empty22;
  const interruptAll3 = /* @__PURE__ */ __name(() => {
    for (const fiber of fibers) {
      fiber.unsafeInterruptAsFork(state.id());
    }
  }, "interruptAll");
  let latch = false;
  let empty46 = true;
  for (const self of all12) {
    empty46 = false;
    const fiber = unsafeFork2(interruptible2(self), state, status3.runtimeFlags);
    fibers.add(fiber);
    fiber.addObserver((exit4) => {
      fibers.delete(fiber);
      if (!winner) {
        if (exit4._tag === "Success") {
          latch = true;
          winner = fiber;
          failures3 = empty22;
          interruptAll3();
        } else {
          failures3 = parallel(exit4.cause, failures3);
        }
      }
      if (latch && fibers.size === 0) {
        resume2(winner ? zipRight2(inheritAll(winner), winner.unsafePoll()) : failCause(failures3));
      }
    });
    if (winner) break;
  }
  if (empty46) {
    return resume2(dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`)));
  }
  latch = true;
  return interruptAllAs(fibers, state.id());
})), "raceAll");
var reduceEffect = /* @__PURE__ */ dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable2(elements).reduce((acc, a, i) => zipWith6(acc, a, (acc2, a2) => f(acc2, a2, i)), zero2), () => suspend(() => pipe(mergeAll3([zero2, ...elements], none2(), (acc, elem, i) => {
  switch (acc._tag) {
    case "None": {
      return some3(elem);
    }
    case "Some": {
      return some3(f(acc.value, elem, i));
    }
  }
}, options), map14((option5) => {
  switch (option5._tag) {
    case "None": {
      throw new Error("BUG: Effect.reduceEffect - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
    case "Some": {
      return option5.value;
    }
  }
})))));
var parallelFinalizers = /* @__PURE__ */ __name((self) => contextWithEffect((context11) => match2(getOption2(context11, scopeTag), {
  onNone: /* @__PURE__ */ __name(() => self, "onNone"),
  onSome: /* @__PURE__ */ __name((scope6) => {
    switch (scope6.strategy._tag) {
      case "Parallel":
        return self;
      case "Sequential":
      case "ParallelN":
        return flatMap12(scopeFork(scope6, parallel3), (inner) => scopeExtend(self, inner));
    }
  }, "onSome")
})), "parallelFinalizers");
var parallelNFinalizers = /* @__PURE__ */ __name((parallelism) => (self) => contextWithEffect((context11) => match2(getOption2(context11, scopeTag), {
  onNone: /* @__PURE__ */ __name(() => self, "onNone"),
  onSome: /* @__PURE__ */ __name((scope6) => {
    if (scope6.strategy._tag === "ParallelN" && scope6.strategy.parallelism === parallelism) {
      return self;
    }
    return flatMap12(scopeFork(scope6, parallelN2(parallelism)), (inner) => scopeExtend(self, inner));
  }, "onSome")
})), "parallelNFinalizers");
var finalizersMask = /* @__PURE__ */ __name((strategy) => (self) => finalizersMaskInternal(strategy, true)(self), "finalizersMask");
var finalizersMaskInternal = /* @__PURE__ */ __name((strategy, concurrentFinalizers) => (self) => contextWithEffect((context11) => match2(getOption2(context11, scopeTag), {
  onNone: /* @__PURE__ */ __name(() => self(identity), "onNone"),
  onSome: /* @__PURE__ */ __name((scope6) => {
    if (concurrentFinalizers === true) {
      const patch13 = strategy._tag === "Parallel" ? parallelFinalizers : strategy._tag === "Sequential" ? sequentialFinalizers : parallelNFinalizers(strategy.parallelism);
      switch (scope6.strategy._tag) {
        case "Parallel":
          return patch13(self(parallelFinalizers));
        case "Sequential":
          return patch13(self(sequentialFinalizers));
        case "ParallelN":
          return patch13(self(parallelNFinalizers(scope6.strategy.parallelism)));
      }
    } else {
      return self(identity);
    }
  }, "onSome")
})), "finalizersMaskInternal");
var scopeWith = /* @__PURE__ */ __name((f) => flatMap12(scopeTag, f), "scopeWith");
var scopedWith = /* @__PURE__ */ __name((f) => flatMap12(scopeMake2(), (scope6) => onExit(f(scope6), (exit4) => scope6.close(exit4))), "scopedWith");
var scopedEffect = /* @__PURE__ */ __name((effect4) => flatMap12(scopeMake2(), (scope6) => scopeUse(effect4, scope6)), "scopedEffect");
var sequentialFinalizers = /* @__PURE__ */ __name((self) => contextWithEffect((context11) => match2(getOption2(context11, scopeTag), {
  onNone: /* @__PURE__ */ __name(() => self, "onNone"),
  onSome: /* @__PURE__ */ __name((scope6) => {
    switch (scope6.strategy._tag) {
      case "Sequential":
        return self;
      case "Parallel":
      case "ParallelN":
        return flatMap12(scopeFork(scope6, sequential3), (inner) => scopeExtend(self, inner));
    }
  }, "onSome")
})), "sequentialFinalizers");
var tagMetricsScoped = /* @__PURE__ */ __name((key, value2) => labelMetricsScoped([make37(key, value2)]), "tagMetricsScoped");
var labelMetricsScoped = /* @__PURE__ */ __name((labels) => fiberRefLocallyScopedWith(currentMetricLabels, (old) => union2(old, labels)), "labelMetricsScoped");
var using = /* @__PURE__ */ dual(2, (self, use2) => scopedWith((scope6) => flatMap12(scopeExtend(self, scope6), use2)));
var validate = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, options) => validateWith(self, that, (a, b) => [a, b], options));
var validateWith = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, f, options) => flatten7(zipWithOptions(exit(self), exit(that), (ea, eb) => exitZipWith(ea, eb, {
  onSuccess: f,
  onFailure: /* @__PURE__ */ __name((ca, cb) => options?.concurrent ? parallel(ca, cb) : sequential(ca, cb), "onFailure")
}), options)));
var validateAllPar = /* @__PURE__ */ dual(2, (elements, f) => flatMap12(partition8(elements, f), ([es, bs]) => es.length === 0 ? succeed(bs) : fail2(es)));
var validateAllParDiscard = /* @__PURE__ */ dual(2, (elements, f) => flatMap12(partition8(elements, f), ([es, _]) => es.length === 0 ? void_3 : fail2(es)));
var validateFirst = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (elements, f, options) => flip3(forEach15(elements, (a, i) => flip3(f(a, i)), options)));
var withClockScoped = /* @__PURE__ */ __name((c) => fiberRefLocallyScopedWith(currentServices, add2(clockTag, c)), "withClockScoped");
var withRandomScoped = /* @__PURE__ */ __name((value2) => fiberRefLocallyScopedWith(currentServices, add2(randomTag, value2)), "withRandomScoped");
var withConfigProviderScoped = /* @__PURE__ */ __name((provider) => fiberRefLocallyScopedWith(currentServices, add2(configProviderTag, provider)), "withConfigProviderScoped");
var withEarlyRelease = /* @__PURE__ */ __name((self) => scopeWith((parent) => flatMap12(scopeFork(parent, sequential2), (child) => pipe(self, scopeExtend(child), map14((value2) => [fiberIdWith((fiberId4) => scopeClose(child, exitInterrupt(fiberId4))), value2])))), "withEarlyRelease");
var zipOptions = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, options) => zipWithOptions(self, that, (a, b) => [a, b], options));
var zipLeftOptions = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipLeft2(self, that);
  }
  return zipWithOptions(self, that, (a, _) => a, options);
});
var zipRightOptions = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipRight2(self, that);
  }
  return zipWithOptions(self, that, (_, b) => b, options);
});
var zipWithOptions = /* @__PURE__ */ dual((args2) => isEffect(args2[1]), (self, that, f, options) => map14(all8([self, that], {
  concurrency: options?.concurrent ? 2 : 1,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([a, a2]) => f(a, a2)));
var withRuntimeFlagsScoped = /* @__PURE__ */ __name((update11) => {
  if (update11 === empty20) {
    return void_3;
  }
  return pipe(runtimeFlags, flatMap12((runtimeFlags2) => {
    const updatedRuntimeFlags = patch7(runtimeFlags2, update11);
    const revertRuntimeFlags = diff7(updatedRuntimeFlags, runtimeFlags2);
    return pipe(updateRuntimeFlags(update11), zipRight2(addFinalizer2(() => updateRuntimeFlags(revertRuntimeFlags))), asVoid2);
  }), uninterruptible);
}, "withRuntimeFlagsScoped");
var scopeTag = /* @__PURE__ */ GenericTag("effect/Scope");
var scope2 = scopeTag;
var scopeUnsafeAddFinalizer = /* @__PURE__ */ __name((scope6, fin) => {
  if (scope6.state._tag === "Open") {
    scope6.state.finalizers.set({}, fin);
  }
}, "scopeUnsafeAddFinalizer");
var ScopeImplProto = {
  [ScopeTypeId]: ScopeTypeId,
  [CloseableScopeTypeId]: CloseableScopeTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  fork(strategy) {
    return sync(() => {
      const newScope = scopeUnsafeMake2(strategy);
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      const key = {};
      const fin = /* @__PURE__ */ __name((exit4) => newScope.close(exit4), "fin");
      this.state.finalizers.set(key, fin);
      scopeUnsafeAddFinalizer(newScope, (_) => sync(() => {
        if (this.state._tag === "Open") {
          this.state.finalizers.delete(key);
        }
      }));
      return newScope;
    });
  },
  close(exit4) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return void_3;
      }
      const finalizers = Array.from(this.state.finalizers.values()).reverse();
      this.state = {
        _tag: "Closed",
        exit: exit4
      };
      if (finalizers.length === 0) {
        return void_3;
      }
      return isSequential(this.strategy) ? pipe(forEachSequential(finalizers, (fin) => exit(fin(exit4))), flatMap12((results) => pipe(exitCollectAll(results), map2(exitAsVoid), getOrElse2(() => exitVoid)))) : isParallel(this.strategy) ? pipe(forEachParUnbounded(finalizers, (fin) => exit(fin(exit4)), false), flatMap12((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map2(exitAsVoid), getOrElse2(() => exitVoid)))) : pipe(forEachParN(finalizers, this.strategy.parallelism, (fin) => exit(fin(exit4)), false), flatMap12((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map2(exitAsVoid), getOrElse2(() => exitVoid))));
    });
  },
  addFinalizer(fin) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return fin(this.state.exit);
      }
      this.state.finalizers.set({}, fin);
      return void_3;
    });
  }
};
var scopeUnsafeMake2 = /* @__PURE__ */ __name((strategy = sequential2) => {
  const scope6 = Object.create(ScopeImplProto);
  scope6.strategy = strategy;
  scope6.state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Map()
  };
  return scope6;
}, "scopeUnsafeMake");
var scopeMake2 = /* @__PURE__ */ __name((strategy = sequential2) => sync(() => scopeUnsafeMake2(strategy)), "scopeMake");
var scopeExtend = /* @__PURE__ */ dual(2, (effect4, scope6) => mapInputContext(
  effect4,
  // @ts-expect-error
  merge3(make6(scopeTag, scope6))
));
var scopeUse = /* @__PURE__ */ dual(2, (effect4, scope6) => pipe(effect4, scopeExtend(scope6), onExit((exit4) => scope6.close(exit4))));
var fiberRefUnsafeMakeSupervisor = /* @__PURE__ */ __name((initial) => fiberRefUnsafeMakePatch(initial, {
  differ: differ2,
  fork: empty34
}), "fiberRefUnsafeMakeSupervisor");
var fiberRefLocallyScoped = /* @__PURE__ */ dual(2, (self, value2) => asVoid2(acquireRelease2(flatMap12(fiberRefGet(self), (oldValue) => as3(fiberRefSet(self, value2), oldValue)), (oldValue) => fiberRefSet(self, oldValue))));
var fiberRefLocallyScopedWith = /* @__PURE__ */ dual(2, (self, f) => fiberRefGetWith(self, (a) => fiberRefLocallyScoped(self, f(a))));
var fiberRefMake = /* @__PURE__ */ __name((initial, options) => fiberRefMakeWith(() => fiberRefUnsafeMake(initial, options)), "fiberRefMake");
var fiberRefMakeWith = /* @__PURE__ */ __name((ref) => acquireRelease2(tap2(sync(ref), (ref2) => fiberRefUpdate(ref2, identity)), (fiberRef) => fiberRefDelete(fiberRef)), "fiberRefMakeWith");
var fiberRefMakeContext = /* @__PURE__ */ __name((initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeContext(initial)), "fiberRefMakeContext");
var fiberRefMakeRuntimeFlags = /* @__PURE__ */ __name((initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeRuntimeFlags(initial)), "fiberRefMakeRuntimeFlags");
var currentRuntimeFlags = /* @__PURE__ */ fiberRefUnsafeMakeRuntimeFlags(none5);
var currentSupervisor = /* @__PURE__ */ fiberRefUnsafeMakeSupervisor(none8);
var fiberAwaitAll = /* @__PURE__ */ __name((fibers) => forEach15(fibers, _await2), "fiberAwaitAll");
var fiberAll = /* @__PURE__ */ __name((fibers) => {
  const _fiberAll = {
    ...CommitPrototype2,
    commit() {
      return join3(this);
    },
    [FiberTypeId]: fiberVariance2,
    id: /* @__PURE__ */ __name(() => fromIterable2(fibers).reduce((id3, fiber) => combine5(id3, fiber.id()), none4), "id"),
    await: exit(forEachParUnbounded(fibers, (fiber) => flatten7(fiber.await), false)),
    children: map14(forEachParUnbounded(fibers, (fiber) => fiber.children, false), flatten3),
    inheritAll: forEachSequentialDiscard(fibers, (fiber) => fiber.inheritAll),
    poll: map14(forEachSequential(fibers, (fiber) => fiber.poll), reduceRight(some3(exitSucceed(new Array())), (optionB, optionA) => {
      switch (optionA._tag) {
        case "None": {
          return none2();
        }
        case "Some": {
          switch (optionB._tag) {
            case "None": {
              return none2();
            }
            case "Some": {
              return some3(exitZipWith(optionA.value, optionB.value, {
                onSuccess: /* @__PURE__ */ __name((a, chunk4) => [a, ...chunk4], "onSuccess"),
                onFailure: parallel
              }));
            }
          }
        }
      }
    })),
    interruptAsFork: /* @__PURE__ */ __name((fiberId4) => forEachSequentialDiscard(fibers, (fiber) => fiber.interruptAsFork(fiberId4)), "interruptAsFork")
  };
  return _fiberAll;
}, "fiberAll");
var fiberInterruptFork = /* @__PURE__ */ __name((self) => asVoid2(forkDaemon2(interruptFiber(self))), "fiberInterruptFork");
var fiberJoinAll = /* @__PURE__ */ __name((fibers) => join3(fiberAll(fibers)), "fiberJoinAll");
var fiberScoped = /* @__PURE__ */ __name((self) => acquireRelease2(succeed(self), interruptFiber), "fiberScoped");
var raceWith = /* @__PURE__ */ dual(3, (self, other, options) => raceFibersWith(self, other, {
  onSelfWin: /* @__PURE__ */ __name((winner, loser) => flatMap12(winner.await, (exit4) => {
    switch (exit4._tag) {
      case OP_SUCCESS: {
        return flatMap12(winner.inheritAll, () => options.onSelfDone(exit4, loser));
      }
      case OP_FAILURE: {
        return options.onSelfDone(exit4, loser);
      }
    }
  }), "onSelfWin"),
  onOtherWin: /* @__PURE__ */ __name((winner, loser) => flatMap12(winner.await, (exit4) => {
    switch (exit4._tag) {
      case OP_SUCCESS: {
        return flatMap12(winner.inheritAll, () => options.onOtherDone(exit4, loser));
      }
      case OP_FAILURE: {
        return options.onOtherDone(exit4, loser);
      }
    }
  }), "onOtherWin")
}));
var disconnect = /* @__PURE__ */ __name((self) => uninterruptibleMask((restore) => fiberIdWith((fiberId4) => flatMap12(forkDaemon2(restore(self)), (fiber) => pipe(restore(join3(fiber)), onInterrupt(() => pipe(fiber, interruptAsFork(fiberId4))))))), "disconnect");
var race2 = /* @__PURE__ */ dual(2, (self, that) => fiberIdWith((parentFiberId) => raceWith(self, that, {
  onSelfDone: /* @__PURE__ */ __name((exit4, right3) => exitMatchEffect(exit4, {
    onFailure: /* @__PURE__ */ __name((cause3) => pipe(join3(right3), mapErrorCause2((cause22) => parallel(cause3, cause22))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((value2) => pipe(right3, interruptAsFiber(parentFiberId), as3(value2)), "onSuccess")
  }), "onSelfDone"),
  onOtherDone: /* @__PURE__ */ __name((exit4, left3) => exitMatchEffect(exit4, {
    onFailure: /* @__PURE__ */ __name((cause3) => pipe(join3(left3), mapErrorCause2((cause22) => parallel(cause22, cause3))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((value2) => pipe(left3, interruptAsFiber(parentFiberId), as3(value2)), "onSuccess")
  }), "onOtherDone")
})));
var raceFibersWith = /* @__PURE__ */ dual(3, (self, other, options) => withFiberRuntime((parentFiber, parentStatus) => {
  const parentRuntimeFlags = parentStatus.runtimeFlags;
  const raceIndicator = make13(true);
  const leftFiber = unsafeMakeChildFiber(self, parentFiber, parentRuntimeFlags, options.selfScope);
  const rightFiber = unsafeMakeChildFiber(other, parentFiber, parentRuntimeFlags, options.otherScope);
  return async_((cb) => {
    leftFiber.addObserver(() => completeRace(leftFiber, rightFiber, options.onSelfWin, raceIndicator, cb));
    rightFiber.addObserver(() => completeRace(rightFiber, leftFiber, options.onOtherWin, raceIndicator, cb));
    leftFiber.startFork(self);
    rightFiber.startFork(other);
  }, combine5(leftFiber.id(), rightFiber.id()));
}));
var completeRace = /* @__PURE__ */ __name((winner, loser, cont, ab, cb) => {
  if (compareAndSet(true, false)(ab)) {
    cb(cont(winner, loser));
  }
}, "completeRace");
var ensuring2 = /* @__PURE__ */ dual(2, (self, finalizer3) => uninterruptibleMask((restore) => matchCauseEffect(restore(self), {
  onFailure: /* @__PURE__ */ __name((cause1) => matchCauseEffect(finalizer3, {
    onFailure: /* @__PURE__ */ __name((cause22) => failCause(sequential(cause1, cause22)), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => failCause(cause1), "onSuccess")
  }), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => as3(finalizer3, a), "onSuccess")
})));
var invokeWithInterrupt = /* @__PURE__ */ __name((self, entries3, onInterrupt4) => fiberIdWith((id3) => flatMap12(flatMap12(forkDaemon2(interruptible2(self)), (processing) => async_((cb) => {
  const counts = entries3.map((_) => _.listeners.count);
  const checkDone = /* @__PURE__ */ __name(() => {
    if (counts.every((count5) => count5 === 0)) {
      if (entries3.every((_) => {
        if (_.result.state.current._tag === "Pending") {
          return true;
        } else if (_.result.state.current._tag === "Done" && exitIsExit(_.result.state.current.effect) && _.result.state.current.effect._tag === "Failure" && isInterrupted(_.result.state.current.effect.cause)) {
          return true;
        } else {
          return false;
        }
      })) {
        cleanup.forEach((f) => f());
        onInterrupt4?.();
        cb(interruptFiber(processing));
      }
    }
  }, "checkDone");
  processing.addObserver((exit4) => {
    cleanup.forEach((f) => f());
    cb(exit4);
  });
  const cleanup = entries3.map((r, i) => {
    const observer = /* @__PURE__ */ __name((count5) => {
      counts[i] = count5;
      checkDone();
    }, "observer");
    r.listeners.addObserver(observer);
    return () => r.listeners.removeObserver(observer);
  });
  checkDone();
  return sync(() => {
    cleanup.forEach((f) => f());
  });
})), () => suspend(() => {
  const residual = entries3.flatMap((entry) => {
    if (!entry.state.completed) {
      return [entry];
    }
    return [];
  });
  return forEachSequentialDiscard(residual, (entry) => complete2(entry.request, exitInterrupt(id3)));
}))), "invokeWithInterrupt");
var interruptWhenPossible = /* @__PURE__ */ dual(2, (self, all12) => fiberRefGetWith(currentRequestMap, (map36) => suspend(() => {
  const entries3 = fromIterable2(all12).flatMap((_) => map36.has(_) ? [map36.get(_)] : []);
  return invokeWithInterrupt(self, entries3);
})));
var makeSpanScoped = /* @__PURE__ */ __name((name, options) => {
  options = addSpanStackTrace(options);
  return uninterruptible(withFiberRuntime((fiber) => {
    const scope6 = unsafeGet3(fiber.getFiberRef(currentContext), scopeTag);
    const span4 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock_ = get4(fiber.getFiberRef(currentServices), clockTag);
    return as3(scopeAddFinalizerExit(scope6, (exit4) => endSpan(span4, exit4, clock_, timingEnabled)), span4);
  }));
}, "makeSpanScoped");
var withTracerScoped = /* @__PURE__ */ __name((value2) => fiberRefLocallyScopedWith(currentServices, add2(tracerTag, value2)), "withTracerScoped");
var withSpanScoped = /* @__PURE__ */ __name(function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return flatMap12(makeSpanScoped(name, addSpanStackTrace(options)), (span4) => provideService(self, spanTag, span4));
  }
  return (self) => flatMap12(makeSpanScoped(name, addSpanStackTrace(options)), (span4) => provideService(self, spanTag, span4));
}, "withSpanScoped");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/cache.js
var complete3 = /* @__PURE__ */ __name((key, exit4, entryStats, timeToLiveMillis) => struct4({
  _tag: "Complete",
  key,
  exit: exit4,
  entryStats,
  timeToLiveMillis
}), "complete");
var pending2 = /* @__PURE__ */ __name((key, deferred) => struct4({
  _tag: "Pending",
  key,
  deferred
}), "pending");
var refreshing = /* @__PURE__ */ __name((deferred, complete5) => struct4({
  _tag: "Refreshing",
  deferred,
  complete: complete5
}), "refreshing");
var MapKeyTypeId = /* @__PURE__ */ Symbol.for("effect/Cache/MapKey");
var MapKeyImpl = class {
  static {
    __name(this, "MapKeyImpl");
  }
  current;
  [MapKeyTypeId] = MapKeyTypeId;
  previous = void 0;
  next = void 0;
  constructor(current2) {
    this.current = current2;
  }
  [symbol2]() {
    return pipe(hash(this.current), combine2(hash(this.previous)), combine2(hash(this.next)), cached(this));
  }
  [symbol3](that) {
    if (this === that) {
      return true;
    }
    return isMapKey(that) && equals(this.current, that.current) && equals(this.previous, that.previous) && equals(this.next, that.next);
  }
};
var makeMapKey = /* @__PURE__ */ __name((current2) => new MapKeyImpl(current2), "makeMapKey");
var isMapKey = /* @__PURE__ */ __name((u) => hasProperty(u, MapKeyTypeId), "isMapKey");
var KeySetImpl = class {
  static {
    __name(this, "KeySetImpl");
  }
  head = void 0;
  tail = void 0;
  add(key) {
    if (key !== this.tail) {
      if (this.tail === void 0) {
        this.head = key;
        this.tail = key;
      } else {
        const previous2 = key.previous;
        const next4 = key.next;
        if (next4 !== void 0) {
          key.next = void 0;
          if (previous2 !== void 0) {
            previous2.next = next4;
            next4.previous = previous2;
          } else {
            this.head = next4;
            this.head.previous = void 0;
          }
        }
        this.tail.next = key;
        key.previous = this.tail;
        this.tail = key;
      }
    }
  }
  remove() {
    const key = this.head;
    if (key !== void 0) {
      const next4 = key.next;
      if (next4 !== void 0) {
        key.next = void 0;
        this.head = next4;
        this.head.previous = void 0;
      } else {
        this.head = void 0;
        this.tail = void 0;
      }
    }
    return key;
  }
};
var makeKeySet = /* @__PURE__ */ __name(() => new KeySetImpl(), "makeKeySet");
var makeCacheState = /* @__PURE__ */ __name((map36, keys7, accesses, updating, hits, misses) => ({
  map: map36,
  keys: keys7,
  accesses,
  updating,
  hits,
  misses
}), "makeCacheState");
var initialCacheState = /* @__PURE__ */ __name(() => makeCacheState(empty23(), makeKeySet(), unbounded(), make13(false), 0, 0), "initialCacheState");
var CacheSymbolKey = "effect/Cache";
var CacheTypeId = /* @__PURE__ */ Symbol.for(CacheSymbolKey);
var cacheVariance = {
  /* c8 ignore next */
  _Key: /* @__PURE__ */ __name((_) => _, "_Key"),
  /* c8 ignore next */
  _Error: /* @__PURE__ */ __name((_) => _, "_Error"),
  /* c8 ignore next */
  _Value: /* @__PURE__ */ __name((_) => _, "_Value")
};
var ConsumerCacheSymbolKey = "effect/ConsumerCache";
var ConsumerCacheTypeId = /* @__PURE__ */ Symbol.for(ConsumerCacheSymbolKey);
var consumerCacheVariance = {
  /* c8 ignore next */
  _Key: /* @__PURE__ */ __name((_) => _, "_Key"),
  /* c8 ignore next */
  _Error: /* @__PURE__ */ __name((_) => _, "_Error"),
  /* c8 ignore next */
  _Value: /* @__PURE__ */ __name((_) => _, "_Value")
};
var makeCacheStats = /* @__PURE__ */ __name((options) => options, "makeCacheStats");
var makeEntryStats = /* @__PURE__ */ __name((loadedMillis) => ({
  loadedMillis
}), "makeEntryStats");
var CacheImpl = class {
  static {
    __name(this, "CacheImpl");
  }
  capacity;
  context;
  fiberId;
  lookup;
  timeToLive;
  [CacheTypeId] = cacheVariance;
  [ConsumerCacheTypeId] = consumerCacheVariance;
  cacheState;
  constructor(capacity10, context11, fiberId4, lookup, timeToLive) {
    this.capacity = capacity10;
    this.context = context11;
    this.fiberId = fiberId4;
    this.lookup = lookup;
    this.timeToLive = timeToLive;
    this.cacheState = initialCacheState();
  }
  get(key) {
    return map14(this.getEither(key), merge);
  }
  get cacheStats() {
    return sync(() => makeCacheStats({
      hits: this.cacheState.hits,
      misses: this.cacheState.misses,
      size: size10(this.cacheState.map)
    }));
  }
  getOption(key) {
    return suspend(() => match2(get9(this.cacheState.map, key), {
      onNone: /* @__PURE__ */ __name(() => {
        const mapKey = makeMapKey(key);
        this.trackAccess(mapKey);
        this.trackMiss();
        return succeed(none2());
      }, "onNone"),
      onSome: /* @__PURE__ */ __name((value2) => this.resolveMapValue(value2), "onSome")
    }));
  }
  getOptionComplete(key) {
    return suspend(() => match2(get9(this.cacheState.map, key), {
      onNone: /* @__PURE__ */ __name(() => {
        const mapKey = makeMapKey(key);
        this.trackAccess(mapKey);
        this.trackMiss();
        return succeed(none2());
      }, "onNone"),
      onSome: /* @__PURE__ */ __name((value2) => this.resolveMapValue(value2, true), "onSome")
    }));
  }
  contains(key) {
    return sync(() => has6(this.cacheState.map, key));
  }
  entryStats(key) {
    return sync(() => {
      const option5 = get9(this.cacheState.map, key);
      if (isSome2(option5)) {
        switch (option5.value._tag) {
          case "Complete": {
            const loaded = option5.value.entryStats.loadedMillis;
            return some3(makeEntryStats(loaded));
          }
          case "Pending": {
            return none2();
          }
          case "Refreshing": {
            const loaded = option5.value.complete.entryStats.loadedMillis;
            return some3(makeEntryStats(loaded));
          }
        }
      }
      return none2();
    });
  }
  getEither(key) {
    return suspend(() => {
      const k = key;
      let mapKey = void 0;
      let deferred = void 0;
      let value2 = getOrUndefined2(get9(this.cacheState.map, k));
      if (value2 === void 0) {
        deferred = unsafeMake4(this.fiberId);
        mapKey = makeMapKey(k);
        if (has6(this.cacheState.map, k)) {
          value2 = getOrUndefined2(get9(this.cacheState.map, k));
        } else {
          set5(this.cacheState.map, k, pending2(mapKey, deferred));
        }
      }
      if (value2 === void 0) {
        this.trackAccess(mapKey);
        this.trackMiss();
        return map14(this.lookupValueOf(key, deferred), right2);
      } else {
        return flatMap12(this.resolveMapValue(value2), match2({
          onNone: /* @__PURE__ */ __name(() => this.getEither(key), "onNone"),
          onSome: /* @__PURE__ */ __name((value3) => succeed(left2(value3)), "onSome")
        }));
      }
    });
  }
  invalidate(key) {
    return sync(() => {
      remove8(this.cacheState.map, key);
    });
  }
  invalidateWhen(key, when7) {
    return sync(() => {
      const value2 = get9(this.cacheState.map, key);
      if (isSome2(value2) && value2.value._tag === "Complete") {
        if (value2.value.exit._tag === "Success") {
          if (when7(value2.value.exit.value)) {
            remove8(this.cacheState.map, key);
          }
        }
      }
    });
  }
  get invalidateAll() {
    return sync(() => {
      this.cacheState.map = empty23();
    });
  }
  refresh(key) {
    return clockWith3((clock3) => suspend(() => {
      const k = key;
      const deferred = unsafeMake4(this.fiberId);
      let value2 = getOrUndefined2(get9(this.cacheState.map, k));
      if (value2 === void 0) {
        if (has6(this.cacheState.map, k)) {
          value2 = getOrUndefined2(get9(this.cacheState.map, k));
        } else {
          set5(this.cacheState.map, k, pending2(makeMapKey(k), deferred));
        }
      }
      if (value2 === void 0) {
        return asVoid2(this.lookupValueOf(key, deferred));
      } else {
        switch (value2._tag) {
          case "Complete": {
            if (this.hasExpired(clock3, value2.timeToLiveMillis)) {
              const found = getOrUndefined2(get9(this.cacheState.map, k));
              if (equals(found, value2)) {
                remove8(this.cacheState.map, k);
              }
              return asVoid2(this.get(key));
            }
            return pipe(this.lookupValueOf(key, deferred), when(() => {
              const current2 = getOrUndefined2(get9(this.cacheState.map, k));
              if (equals(current2, value2)) {
                const mapValue = refreshing(deferred, value2);
                set5(this.cacheState.map, k, mapValue);
                return true;
              }
              return false;
            }), asVoid2);
          }
          case "Pending": {
            return _await(value2.deferred);
          }
          case "Refreshing": {
            return _await(value2.deferred);
          }
        }
      }
    }));
  }
  set(key, value2) {
    return clockWith3((clock3) => sync(() => {
      const now2 = clock3.unsafeCurrentTimeMillis();
      const k = key;
      const lookupResult = succeed3(value2);
      const mapValue = complete3(makeMapKey(k), lookupResult, makeEntryStats(now2), now2 + toMillis(decode(this.timeToLive(lookupResult))));
      set5(this.cacheState.map, k, mapValue);
    }));
  }
  get size() {
    return sync(() => {
      return size10(this.cacheState.map);
    });
  }
  get values() {
    return sync(() => {
      const values10 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          values10.push(entry[1].exit.value);
        }
      }
      return values10;
    });
  }
  get entries() {
    return sync(() => {
      const values10 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          values10.push([entry[0], entry[1].exit.value]);
        }
      }
      return values10;
    });
  }
  get keys() {
    return sync(() => {
      const keys7 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          keys7.push(entry[0]);
        }
      }
      return keys7;
    });
  }
  resolveMapValue(value2, ignorePending = false) {
    return clockWith3((clock3) => {
      switch (value2._tag) {
        case "Complete": {
          this.trackAccess(value2.key);
          if (this.hasExpired(clock3, value2.timeToLiveMillis)) {
            remove8(this.cacheState.map, value2.key.current);
            return succeed(none2());
          }
          this.trackHit();
          return map14(value2.exit, some3);
        }
        case "Pending": {
          this.trackAccess(value2.key);
          this.trackHit();
          if (ignorePending) {
            return succeed(none2());
          }
          return map14(_await(value2.deferred), some3);
        }
        case "Refreshing": {
          this.trackAccess(value2.complete.key);
          this.trackHit();
          if (this.hasExpired(clock3, value2.complete.timeToLiveMillis)) {
            if (ignorePending) {
              return succeed(none2());
            }
            return map14(_await(value2.deferred), some3);
          }
          return map14(value2.complete.exit, some3);
        }
      }
    });
  }
  trackHit() {
    this.cacheState.hits = this.cacheState.hits + 1;
  }
  trackMiss() {
    this.cacheState.misses = this.cacheState.misses + 1;
  }
  trackAccess(key) {
    offer(this.cacheState.accesses, key);
    if (compareAndSet(this.cacheState.updating, false, true)) {
      let loop4 = true;
      while (loop4) {
        const key2 = poll2(this.cacheState.accesses, EmptyMutableQueue);
        if (key2 === EmptyMutableQueue) {
          loop4 = false;
        } else {
          this.cacheState.keys.add(key2);
        }
      }
      let size25 = size10(this.cacheState.map);
      loop4 = size25 > this.capacity;
      while (loop4) {
        const key2 = this.cacheState.keys.remove();
        if (key2 !== void 0) {
          if (has6(this.cacheState.map, key2.current)) {
            remove8(this.cacheState.map, key2.current);
            size25 = size25 - 1;
            loop4 = size25 > this.capacity;
          }
        } else {
          loop4 = false;
        }
      }
      set3(this.cacheState.updating, false);
    }
  }
  hasExpired(clock3, timeToLiveMillis) {
    return clock3.unsafeCurrentTimeMillis() > timeToLiveMillis;
  }
  lookupValueOf(input, deferred) {
    return clockWith3((clock3) => suspend(() => {
      const key = input;
      return pipe(this.lookup(input), provideContext(this.context), exit, flatMap12((exit4) => {
        const now2 = clock3.unsafeCurrentTimeMillis();
        const stats = makeEntryStats(now2);
        const value2 = complete3(makeMapKey(key), exit4, stats, now2 + toMillis(decode(this.timeToLive(exit4))));
        set5(this.cacheState.map, key, value2);
        return zipRight2(done2(deferred, exit4), exit4);
      }), onInterrupt(() => zipRight2(interrupt3(deferred), sync(() => {
        remove8(this.cacheState.map, key);
      }))));
    }));
  }
};
var make47 = /* @__PURE__ */ __name((options) => {
  const timeToLive = decode(options.timeToLive);
  return makeWith({
    capacity: options.capacity,
    lookup: options.lookup,
    timeToLive: /* @__PURE__ */ __name(() => timeToLive, "timeToLive")
  });
}, "make");
var makeWith = /* @__PURE__ */ __name((options) => map14(all8([context(), fiberId]), ([context11, fiberId4]) => new CacheImpl(options.capacity, context11, fiberId4, options.lookup, (exit4) => decode(options.timeToLive(exit4)))), "makeWith");
var unsafeMakeWith = /* @__PURE__ */ __name((capacity10, lookup, timeToLive) => new CacheImpl(capacity10, empty6(), none3, lookup, (exit4) => decode(timeToLive(exit4))), "unsafeMakeWith");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Cause.js
var CauseTypeId2 = CauseTypeId;
var RuntimeExceptionTypeId2 = RuntimeExceptionTypeId;
var InterruptedExceptionTypeId2 = InterruptedExceptionTypeId;
var IllegalArgumentExceptionTypeId2 = IllegalArgumentExceptionTypeId;
var NoSuchElementExceptionTypeId2 = NoSuchElementExceptionTypeId;
var InvalidPubSubCapacityExceptionTypeId2 = InvalidPubSubCapacityExceptionTypeId;
var ExceededCapacityExceptionTypeId2 = ExceededCapacityExceptionTypeId;
var TimeoutExceptionTypeId2 = TimeoutExceptionTypeId;
var UnknownExceptionTypeId2 = UnknownExceptionTypeId;
var YieldableError3 = YieldableError;
var empty35 = empty22;
var fail8 = fail;
var die6 = die;
var interrupt6 = interrupt;
var parallel4 = parallel;
var sequential4 = sequential;
var isCause2 = isCause;
var isEmptyType2 = isEmptyType;
var isFailType2 = isFailType;
var isDieType2 = isDieType;
var isInterruptType2 = isInterruptType;
var isSequentialType2 = isSequentialType;
var isParallelType2 = isParallelType;
var size14 = size9;
var isEmpty11 = isEmpty7;
var isFailure4 = isFailure;
var isDie2 = isDie;
var isInterrupted3 = isInterrupted;
var isInterruptedOnly2 = isInterruptedOnly;
var failures2 = failures;
var defects2 = defects;
var interruptors2 = interruptors;
var failureOption2 = failureOption;
var failureOrCause2 = failureOrCause;
var flipCauseOption2 = flipCauseOption;
var dieOption2 = dieOption;
var interruptOption2 = interruptOption;
var keepDefects2 = keepDefects;
var linearize2 = linearize;
var stripFailures2 = stripFailures;
var stripSomeDefects2 = stripSomeDefects;
var as6 = as2;
var map22 = map13;
var flatMap16 = flatMap11;
var andThen9 = andThen5;
var flatten10 = flatten6;
var contains6 = contains5;
var squash = causeSquash;
var squashWith = causeSquashWith;
var find2 = find;
var filter15 = filter11;
var match14 = match6;
var reduce15 = reduce11;
var reduceWithContext3 = reduceWithContext;
var InterruptedException2 = InterruptedException;
var isInterruptedException2 = isInterruptedException;
var IllegalArgumentException2 = IllegalArgumentException;
var isIllegalArgumentException2 = isIllegalArgumentException;
var NoSuchElementException3 = NoSuchElementException;
var isNoSuchElementException2 = isNoSuchElementException;
var RuntimeException2 = RuntimeException;
var isRuntimeException2 = isRuntimeException;
var TimeoutException3 = TimeoutException;
var isTimeoutException2 = isTimeoutException;
var UnknownException2 = UnknownException;
var isUnknownException2 = isUnknownException;
var ExceededCapacityException2 = ExceededCapacityException;
var isExceededCapacityException2 = isExceededCapacityException;
var pretty3 = pretty;
var prettyErrors2 = prettyErrors;
var originalError = originalInstance;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Effect.js
var Effect_exports = {};
__export(Effect_exports, {
  Do: () => Do6,
  EffectTypeId: () => EffectTypeId4,
  Service: () => Service,
  Tag: () => Tag3,
  acquireRelease: () => acquireRelease3,
  acquireReleaseInterruptible: () => acquireReleaseInterruptible2,
  acquireUseRelease: () => acquireUseRelease3,
  addFinalizer: () => addFinalizer4,
  all: () => all10,
  allSuccesses: () => allSuccesses2,
  allWith: () => allWith2,
  allowInterrupt: () => allowInterrupt2,
  andThen: () => andThen11,
  annotateCurrentSpan: () => annotateCurrentSpan2,
  annotateLogs: () => annotateLogs3,
  annotateLogsScoped: () => annotateLogsScoped2,
  annotateSpans: () => annotateSpans3,
  ap: () => ap3,
  as: () => as8,
  asSome: () => asSome3,
  asSomeError: () => asSomeError2,
  asVoid: () => asVoid6,
  async: () => async2,
  asyncEffect: () => asyncEffect2,
  awaitAllChildren: () => awaitAllChildren2,
  bind: () => bind7,
  bindAll: () => bindAll2,
  bindTo: () => bindTo7,
  blocked: () => blocked2,
  cacheRequestResult: () => cacheRequestResult,
  cached: () => cached3,
  cachedFunction: () => cachedFunction2,
  cachedInvalidateWithTTL: () => cachedInvalidateWithTTL2,
  cachedWithTTL: () => cachedWithTTL,
  catch: () => _catch2,
  catchAll: () => catchAll4,
  catchAllCause: () => catchAllCause4,
  catchAllDefect: () => catchAllDefect3,
  catchIf: () => catchIf3,
  catchSome: () => catchSome2,
  catchSomeCause: () => catchSomeCause2,
  catchSomeDefect: () => catchSomeDefect2,
  catchTag: () => catchTag3,
  catchTags: () => catchTags2,
  cause: () => cause2,
  checkInterruptible: () => checkInterruptible2,
  clock: () => clock2,
  clockWith: () => clockWith4,
  configProviderWith: () => configProviderWith2,
  console: () => console3,
  consoleWith: () => consoleWith2,
  context: () => context4,
  contextWith: () => contextWith3,
  contextWithEffect: () => contextWithEffect2,
  currentParentSpan: () => currentParentSpan2,
  currentSpan: () => currentSpan2,
  custom: () => custom2,
  daemonChildren: () => daemonChildren2,
  delay: () => delay3,
  descriptor: () => descriptor2,
  descriptorWith: () => descriptorWith2,
  die: () => die8,
  dieMessage: () => dieMessage2,
  dieSync: () => dieSync4,
  diffFiberRefs: () => diffFiberRefs2,
  disconnect: () => disconnect2,
  dropUntil: () => dropUntil2,
  dropWhile: () => dropWhile4,
  either: () => either6,
  ensureErrorType: () => ensureErrorType,
  ensureRequirementsType: () => ensureRequirementsType,
  ensureSuccessType: () => ensureSuccessType,
  ensuring: () => ensuring4,
  ensuringChild: () => ensuringChild2,
  ensuringChildren: () => ensuringChildren2,
  eventually: () => eventually2,
  every: () => every13,
  exists: () => exists4,
  exit: () => exit3,
  fail: () => fail12,
  failCause: () => failCause10,
  failCauseSync: () => failCauseSync5,
  failSync: () => failSync5,
  fiberId: () => fiberId2,
  fiberIdWith: () => fiberIdWith2,
  filter: () => filter16,
  filterEffectOrElse: () => filterEffectOrElse2,
  filterEffectOrFail: () => filterEffectOrFail2,
  filterMap: () => filterMap11,
  filterOrDie: () => filterOrDie2,
  filterOrDieMessage: () => filterOrDieMessage2,
  filterOrElse: () => filterOrElse2,
  filterOrFail: () => filterOrFail3,
  finalizersMask: () => finalizersMask2,
  findFirst: () => findFirst11,
  firstSuccessOf: () => firstSuccessOf2,
  flatMap: () => flatMap18,
  flatten: () => flatten12,
  flip: () => flip5,
  flipWith: () => flipWith2,
  fn: () => fn,
  fnUntraced: () => fnUntraced2,
  forEach: () => forEach16,
  forever: () => forever4,
  fork: () => fork4,
  forkAll: () => forkAll2,
  forkDaemon: () => forkDaemon3,
  forkIn: () => forkIn3,
  forkScoped: () => forkScoped3,
  forkWithErrorHandler: () => forkWithErrorHandler2,
  fromFiber: () => fromFiber2,
  fromFiberEffect: () => fromFiberEffect2,
  fromNullable: () => fromNullable5,
  functionWithSpan: () => functionWithSpan2,
  gen: () => gen5,
  getFiberRefs: () => getFiberRefs,
  getRuntimeFlags: () => getRuntimeFlags,
  head: () => head7,
  if: () => if_2,
  ignore: () => ignore3,
  ignoreLogged: () => ignoreLogged3,
  inheritFiberRefs: () => inheritFiberRefs2,
  interrupt: () => interrupt8,
  interruptWith: () => interruptWith3,
  interruptible: () => interruptible4,
  interruptibleMask: () => interruptibleMask2,
  intoDeferred: () => intoDeferred2,
  isEffect: () => isEffect2,
  isFailure: () => isFailure5,
  isSuccess: () => isSuccess3,
  iterate: () => iterate2,
  labelMetrics: () => labelMetrics2,
  labelMetricsScoped: () => labelMetricsScoped2,
  let: () => let_7,
  liftPredicate: () => liftPredicate5,
  linkSpanCurrent: () => linkSpanCurrent2,
  linkSpans: () => linkSpans2,
  locally: () => locally2,
  locallyScoped: () => locallyScoped,
  locallyScopedWith: () => locallyScopedWith,
  locallyWith: () => locallyWith,
  log: () => log3,
  logAnnotations: () => logAnnotations2,
  logDebug: () => logDebug2,
  logError: () => logError2,
  logFatal: () => logFatal2,
  logInfo: () => logInfo2,
  logTrace: () => logTrace2,
  logWarning: () => logWarning2,
  logWithLevel: () => logWithLevel2,
  loop: () => loop2,
  makeLatch: () => makeLatch2,
  makeSemaphore: () => makeSemaphore2,
  makeSpan: () => makeSpan2,
  makeSpanScoped: () => makeSpanScoped2,
  map: () => map26,
  mapAccum: () => mapAccum4,
  mapBoth: () => mapBoth6,
  mapError: () => mapError5,
  mapErrorCause: () => mapErrorCause4,
  mapInputContext: () => mapInputContext3,
  match: () => match20,
  matchCause: () => matchCause4,
  matchCauseEffect: () => matchCauseEffect3,
  matchEffect: () => matchEffect4,
  merge: () => merge7,
  mergeAll: () => mergeAll5,
  metricLabels: () => metricLabels2,
  negate: () => negate3,
  never: () => never5,
  none: () => none9,
  onError: () => onError3,
  onExit: () => onExit3,
  onInterrupt: () => onInterrupt3,
  once: () => once4,
  option: () => option3,
  optionFromOptional: () => optionFromOptional2,
  orDie: () => orDie4,
  orDieWith: () => orDieWith2,
  orElse: () => orElse8,
  orElseFail: () => orElseFail2,
  orElseSucceed: () => orElseSucceed3,
  parallelErrors: () => parallelErrors2,
  parallelFinalizers: () => parallelFinalizers2,
  partition: () => partition9,
  patchFiberRefs: () => patchFiberRefs2,
  patchRuntimeFlags: () => patchRuntimeFlags,
  promise: () => promise3,
  provide: () => provide2,
  provideService: () => provideService5,
  provideServiceEffect: () => provideServiceEffect3,
  race: () => race3,
  raceAll: () => raceAll3,
  raceFirst: () => raceFirst3,
  raceWith: () => raceWith2,
  random: () => random3,
  randomWith: () => randomWith3,
  reduce: () => reduce17,
  reduceEffect: () => reduceEffect3,
  reduceRight: () => reduceRight5,
  reduceWhile: () => reduceWhile2,
  repeat: () => repeat3,
  repeatN: () => repeatN2,
  repeatOrElse: () => repeatOrElse,
  replicate: () => replicate5,
  replicateEffect: () => replicateEffect3,
  request: () => request,
  retry: () => retry3,
  retryOrElse: () => retryOrElse,
  runCallback: () => runCallback,
  runFork: () => runFork2,
  runPromise: () => runPromise2,
  runPromiseExit: () => runPromiseExit2,
  runRequestBlock: () => runRequestBlock2,
  runSync: () => runSync2,
  runSyncExit: () => runSyncExit2,
  runtime: () => runtime4,
  sandbox: () => sandbox3,
  schedule: () => schedule,
  scheduleForked: () => scheduleForked2,
  scheduleFrom: () => scheduleFrom,
  scope: () => scope4,
  scopeWith: () => scopeWith2,
  scoped: () => scoped4,
  scopedWith: () => scopedWith2,
  sequentialFinalizers: () => sequentialFinalizers2,
  serviceConstants: () => serviceConstants2,
  serviceFunction: () => serviceFunction2,
  serviceFunctionEffect: () => serviceFunctionEffect2,
  serviceFunctions: () => serviceFunctions2,
  serviceMembers: () => serviceMembers2,
  serviceOption: () => serviceOption3,
  serviceOptional: () => serviceOptional2,
  setFiberRefs: () => setFiberRefs2,
  sleep: () => sleep5,
  spanAnnotations: () => spanAnnotations2,
  spanLinks: () => spanLinks2,
  step: () => step3,
  succeed: () => succeed13,
  succeedNone: () => succeedNone3,
  succeedSome: () => succeedSome3,
  summarized: () => summarized2,
  supervised: () => supervised2,
  suspend: () => suspend4,
  sync: () => sync8,
  tagMetrics: () => tagMetrics2,
  tagMetricsScoped: () => tagMetricsScoped2,
  takeUntil: () => takeUntil2,
  takeWhile: () => takeWhile5,
  tap: () => tap5,
  tapBoth: () => tapBoth2,
  tapDefect: () => tapDefect3,
  tapError: () => tapError4,
  tapErrorCause: () => tapErrorCause4,
  tapErrorTag: () => tapErrorTag2,
  timed: () => timed2,
  timedWith: () => timedWith2,
  timeout: () => timeout3,
  timeoutFail: () => timeoutFail2,
  timeoutFailCause: () => timeoutFailCause2,
  timeoutOption: () => timeoutOption3,
  timeoutTo: () => timeoutTo2,
  tracer: () => tracer2,
  tracerWith: () => tracerWith4,
  transplant: () => transplant2,
  transposeMapOption: () => transposeMapOption2,
  transposeOption: () => transposeOption2,
  try: () => try_4,
  tryMap: () => tryMap2,
  tryMapPromise: () => tryMapPromise2,
  tryPromise: () => tryPromise3,
  uninterruptible: () => uninterruptible3,
  uninterruptibleMask: () => uninterruptibleMask3,
  unless: () => unless2,
  unlessEffect: () => unlessEffect2,
  unsafeMakeLatch: () => unsafeMakeLatch2,
  unsafeMakeSemaphore: () => unsafeMakeSemaphore2,
  unsandbox: () => unsandbox2,
  updateFiberRefs: () => updateFiberRefs3,
  updateService: () => updateService3,
  useSpan: () => useSpan2,
  using: () => using2,
  validate: () => validate2,
  validateAll: () => validateAll2,
  validateFirst: () => validateFirst2,
  validateWith: () => validateWith2,
  void: () => _void,
  when: () => when3,
  whenEffect: () => whenEffect2,
  whenFiberRef: () => whenFiberRef2,
  whenLogLevel: () => whenLogLevel2,
  whenRef: () => whenRef2,
  whileLoop: () => whileLoop3,
  withClock: () => withClock2,
  withClockScoped: () => withClockScoped2,
  withConcurrency: () => withConcurrency3,
  withConfigProvider: () => withConfigProvider2,
  withConfigProviderScoped: () => withConfigProviderScoped2,
  withConsole: () => withConsole2,
  withConsoleScoped: () => withConsoleScoped2,
  withEarlyRelease: () => withEarlyRelease2,
  withExecutionPlan: () => withExecutionPlan2,
  withFiberRuntime: () => withFiberRuntime2,
  withLogSpan: () => withLogSpan2,
  withMaxOpsBeforeYield: () => withMaxOpsBeforeYield2,
  withMetric: () => withMetric2,
  withParentSpan: () => withParentSpan3,
  withRandom: () => withRandom2,
  withRandomFixed: () => withRandomFixed,
  withRandomScoped: () => withRandomScoped2,
  withRequestBatching: () => withRequestBatching2,
  withRequestCache: () => withRequestCache2,
  withRequestCaching: () => withRequestCaching2,
  withRuntimeFlagsPatch: () => withRuntimeFlagsPatch,
  withRuntimeFlagsPatchScoped: () => withRuntimeFlagsPatchScoped,
  withScheduler: () => withScheduler2,
  withSchedulingPriority: () => withSchedulingPriority2,
  withSpan: () => withSpan3,
  withSpanScoped: () => withSpanScoped2,
  withTracer: () => withTracer2,
  withTracerEnabled: () => withTracerEnabled2,
  withTracerScoped: () => withTracerScoped2,
  withTracerTiming: () => withTracerTiming2,
  withUnhandledErrorLogLevel: () => withUnhandledErrorLogLevel2,
  yieldNow: () => yieldNow4,
  zip: () => zip12,
  zipLeft: () => zipLeft7,
  zipRight: () => zipRight7,
  zipWith: () => zipWith12
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/schedule/interval.js
var IntervalSymbolKey = "effect/ScheduleInterval";
var IntervalTypeId = /* @__PURE__ */ Symbol.for(IntervalSymbolKey);
var empty36 = {
  [IntervalTypeId]: IntervalTypeId,
  startMillis: 0,
  endMillis: 0
};
var make48 = /* @__PURE__ */ __name((startMillis, endMillis) => {
  if (startMillis > endMillis) {
    return empty36;
  }
  return {
    [IntervalTypeId]: IntervalTypeId,
    startMillis,
    endMillis
  };
}, "make");
var lessThan7 = /* @__PURE__ */ dual(2, (self, that) => min5(self, that) === self);
var min5 = /* @__PURE__ */ dual(2, (self, that) => {
  if (self.endMillis <= that.startMillis) return self;
  if (that.endMillis <= self.startMillis) return that;
  if (self.startMillis < that.startMillis) return self;
  if (that.startMillis < self.startMillis) return that;
  if (self.endMillis <= that.endMillis) return self;
  return that;
});
var max5 = /* @__PURE__ */ dual(2, (self, that) => min5(self, that) === self ? that : self);
var isEmpty12 = /* @__PURE__ */ __name((self) => {
  return self.startMillis >= self.endMillis;
}, "isEmpty");
var isNonEmpty2 = /* @__PURE__ */ __name((self) => {
  return !isEmpty12(self);
}, "isNonEmpty");
var intersect = /* @__PURE__ */ dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end6 = Math.min(self.endMillis, that.endMillis);
  return make48(start3, end6);
});
var size15 = /* @__PURE__ */ __name((self) => {
  return millis(self.endMillis - self.startMillis);
}, "size");
var union9 = /* @__PURE__ */ dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end6 = Math.min(self.endMillis, that.endMillis);
  return start3 < end6 ? none2() : some3(make48(start3, end6));
});
var after = /* @__PURE__ */ __name((startMilliseconds) => {
  return make48(startMilliseconds, Number.POSITIVE_INFINITY);
}, "after");
var before = /* @__PURE__ */ __name((endMilliseconds) => {
  return make48(Number.NEGATIVE_INFINITY, endMilliseconds);
}, "before");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/ScheduleInterval.js
var IntervalTypeId2 = IntervalTypeId;
var make49 = make48;
var empty37 = empty36;
var lessThan8 = lessThan7;
var min6 = min5;
var max6 = max5;
var isEmpty13 = isEmpty12;
var isNonEmpty3 = isNonEmpty2;
var intersect2 = intersect;
var size16 = size15;
var union10 = union9;
var after2 = after;
var before2 = before;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/schedule/intervals.js
var IntervalsSymbolKey = "effect/ScheduleIntervals";
var IntervalsTypeId = /* @__PURE__ */ Symbol.for(IntervalsSymbolKey);
var make50 = /* @__PURE__ */ __name((intervals) => {
  return {
    [IntervalsTypeId]: IntervalsTypeId,
    intervals
  };
}, "make");
var empty38 = /* @__PURE__ */ make50(/* @__PURE__ */ empty7());
var fromIterable15 = /* @__PURE__ */ __name((intervals) => Array.from(intervals).reduce((intervals2, interval) => pipe(intervals2, union11(make50(of3(interval)))), empty38), "fromIterable");
var union11 = /* @__PURE__ */ dual(2, (self, that) => {
  if (!isNonEmpty(that.intervals)) {
    return self;
  }
  if (!isNonEmpty(self.intervals)) {
    return that;
  }
  if (headNonEmpty2(self.intervals).startMillis < headNonEmpty2(that.intervals).startMillis) {
    return unionLoop(tailNonEmpty2(self.intervals), that.intervals, headNonEmpty2(self.intervals), empty7());
  }
  return unionLoop(self.intervals, tailNonEmpty2(that.intervals), headNonEmpty2(that.intervals), empty7());
});
var unionLoop = /* @__PURE__ */ __name((_self, _that, _interval, _acc) => {
  let self = _self;
  let that = _that;
  let interval = _interval;
  let acc = _acc;
  while (isNonEmpty(self) || isNonEmpty(that)) {
    if (!isNonEmpty(self) && isNonEmpty(that)) {
      if (interval.endMillis < headNonEmpty2(that).startMillis) {
        acc = pipe(acc, prepend3(interval));
        interval = headNonEmpty2(that);
        that = tailNonEmpty2(that);
        self = empty7();
      } else {
        interval = make49(interval.startMillis, headNonEmpty2(that).endMillis);
        that = tailNonEmpty2(that);
        self = empty7();
      }
    } else if (isNonEmpty(self) && isEmpty2(that)) {
      if (interval.endMillis < headNonEmpty2(self).startMillis) {
        acc = pipe(acc, prepend3(interval));
        interval = headNonEmpty2(self);
        that = empty7();
        self = tailNonEmpty2(self);
      } else {
        interval = make49(interval.startMillis, headNonEmpty2(self).endMillis);
        that = empty7();
        self = tailNonEmpty2(self);
      }
    } else if (isNonEmpty(self) && isNonEmpty(that)) {
      if (headNonEmpty2(self).startMillis < headNonEmpty2(that).startMillis) {
        if (interval.endMillis < headNonEmpty2(self).startMillis) {
          acc = pipe(acc, prepend3(interval));
          interval = headNonEmpty2(self);
          self = tailNonEmpty2(self);
        } else {
          interval = make49(interval.startMillis, headNonEmpty2(self).endMillis);
          self = tailNonEmpty2(self);
        }
      } else if (interval.endMillis < headNonEmpty2(that).startMillis) {
        acc = pipe(acc, prepend3(interval));
        interval = headNonEmpty2(that);
        that = tailNonEmpty2(that);
      } else {
        interval = make49(interval.startMillis, headNonEmpty2(that).endMillis);
        that = tailNonEmpty2(that);
      }
    } else {
      throw new Error(getBugErrorMessage("Intervals.unionLoop"));
    }
  }
  return make50(pipe(acc, prepend3(interval), reverse3));
}, "unionLoop");
var intersect3 = /* @__PURE__ */ dual(2, (self, that) => intersectLoop(self.intervals, that.intervals, empty7()));
var intersectLoop = /* @__PURE__ */ __name((_left, _right, _acc) => {
  let left3 = _left;
  let right3 = _right;
  let acc = _acc;
  while (isNonEmpty(left3) && isNonEmpty(right3)) {
    const interval = pipe(headNonEmpty2(left3), intersect2(headNonEmpty2(right3)));
    const intervals = isEmpty13(interval) ? acc : pipe(acc, prepend3(interval));
    if (pipe(headNonEmpty2(left3), lessThan8(headNonEmpty2(right3)))) {
      left3 = tailNonEmpty2(left3);
    } else {
      right3 = tailNonEmpty2(right3);
    }
    acc = intervals;
  }
  return make50(reverse3(acc));
}, "intersectLoop");
var start = /* @__PURE__ */ __name((self) => {
  return pipe(self.intervals, head3, getOrElse2(() => empty37)).startMillis;
}, "start");
var end = /* @__PURE__ */ __name((self) => {
  return pipe(self.intervals, head3, getOrElse2(() => empty37)).endMillis;
}, "end");
var lessThan9 = /* @__PURE__ */ dual(2, (self, that) => start(self) < start(that));
var isNonEmpty4 = /* @__PURE__ */ __name((self) => {
  return isNonEmpty(self.intervals);
}, "isNonEmpty");
var max7 = /* @__PURE__ */ dual(2, (self, that) => lessThan9(self, that) ? that : self);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/ScheduleIntervals.js
var IntervalsTypeId2 = IntervalsTypeId;
var make51 = make50;
var empty39 = empty38;
var fromIterable16 = fromIterable15;
var union12 = union11;
var intersect4 = intersect3;
var start2 = start;
var end2 = end;
var lessThan10 = lessThan9;
var isNonEmpty5 = isNonEmpty4;
var max8 = max7;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/schedule/decision.js
var OP_CONTINUE = "Continue";
var OP_DONE2 = "Done";
var _continue = /* @__PURE__ */ __name((intervals) => {
  return {
    _tag: OP_CONTINUE,
    intervals
  };
}, "_continue");
var continueWith = /* @__PURE__ */ __name((interval) => {
  return {
    _tag: OP_CONTINUE,
    intervals: make51(of3(interval))
  };
}, "continueWith");
var done6 = {
  _tag: OP_DONE2
};
var isContinue = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_CONTINUE;
}, "isContinue");
var isDone4 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_DONE2;
}, "isDone");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/ScheduleDecision.js
var _continue2 = _continue;
var continueWith2 = continueWith;
var done7 = done6;
var isContinue2 = isContinue;
var isDone5 = isDone4;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Scope.js
var ScopeTypeId2 = ScopeTypeId;
var CloseableScopeTypeId2 = CloseableScopeTypeId;
var Scope = scopeTag;
var addFinalizer3 = scopeAddFinalizer;
var addFinalizerExit = scopeAddFinalizerExit;
var close = scopeClose;
var extend3 = scopeExtend;
var fork3 = scopeFork;
var use = scopeUse;
var make52 = scopeMake2;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/effect/circular.js
var Semaphore = class {
  static {
    __name(this, "Semaphore");
  }
  permits;
  waiters = /* @__PURE__ */ new Set();
  taken = 0;
  constructor(permits) {
    this.permits = permits;
  }
  get free() {
    return this.permits - this.taken;
  }
  take = /* @__PURE__ */ __name((n) => asyncInterrupt((resume2) => {
    if (this.free < n) {
      const observer = /* @__PURE__ */ __name(() => {
        if (this.free < n) {
          return;
        }
        this.waiters.delete(observer);
        this.taken += n;
        resume2(succeed(n));
      }, "observer");
      this.waiters.add(observer);
      return sync(() => {
        this.waiters.delete(observer);
      });
    }
    this.taken += n;
    return resume2(succeed(n));
  }), "take");
  updateTakenUnsafe(fiber, f) {
    this.taken = f(this.taken);
    if (this.waiters.size > 0) {
      fiber.getFiberRef(currentScheduler).scheduleTask(() => {
        const iter = this.waiters.values();
        let item = iter.next();
        while (item.done === false && this.free > 0) {
          item.value();
          item = iter.next();
        }
      }, fiber.getFiberRef(currentSchedulingPriority));
    }
    return succeed(this.free);
  }
  updateTaken(f) {
    return withFiberRuntime((fiber) => this.updateTakenUnsafe(fiber, f));
  }
  resize = /* @__PURE__ */ __name((permits) => asVoid2(withFiberRuntime((fiber) => {
    this.permits = permits;
    if (this.free < 0) {
      return void_3;
    }
    return this.updateTakenUnsafe(fiber, (taken) => taken);
  })), "resize");
  release = /* @__PURE__ */ __name((n) => this.updateTaken((taken) => taken - n), "release");
  releaseAll = /* @__PURE__ */ this.updateTaken((_) => 0);
  withPermits = /* @__PURE__ */ __name((n) => (self) => uninterruptibleMask((restore) => flatMap12(restore(this.take(n)), (permits) => ensuring2(restore(self), this.release(permits)))), "withPermits");
  withPermitsIfAvailable = /* @__PURE__ */ __name((n) => (self) => uninterruptibleMask((restore) => suspend(() => {
    if (this.free < n) {
      return succeedNone;
    }
    this.taken += n;
    return ensuring2(restore(asSome(self)), this.release(n));
  })), "withPermitsIfAvailable");
};
var unsafeMakeSemaphore = /* @__PURE__ */ __name((permits) => new Semaphore(permits), "unsafeMakeSemaphore");
var makeSemaphore = /* @__PURE__ */ __name((permits) => sync(() => unsafeMakeSemaphore(permits)), "makeSemaphore");
var Latch = class extends Class3 {
  static {
    __name(this, "Latch");
  }
  isOpen;
  waiters = [];
  scheduled = false;
  constructor(isOpen) {
    super();
    this.isOpen = isOpen;
  }
  commit() {
    return this.await;
  }
  unsafeSchedule(fiber) {
    if (this.scheduled || this.waiters.length === 0) {
      return void_3;
    }
    this.scheduled = true;
    fiber.currentScheduler.scheduleTask(this.flushWaiters, fiber.getFiberRef(currentSchedulingPriority));
    return void_3;
  }
  flushWaiters = /* @__PURE__ */ __name(() => {
    this.scheduled = false;
    const waiters = this.waiters;
    this.waiters = [];
    for (let i = 0; i < waiters.length; i++) {
      waiters[i](exitVoid);
    }
  }, "flushWaiters");
  open = /* @__PURE__ */ withFiberRuntime((fiber) => {
    if (this.isOpen) {
      return void_3;
    }
    this.isOpen = true;
    return this.unsafeSchedule(fiber);
  });
  unsafeOpen() {
    if (this.isOpen) return;
    this.isOpen = true;
    this.flushWaiters();
  }
  release = /* @__PURE__ */ withFiberRuntime((fiber) => {
    if (this.isOpen) {
      return void_3;
    }
    return this.unsafeSchedule(fiber);
  });
  await = /* @__PURE__ */ asyncInterrupt((resume2) => {
    if (this.isOpen) {
      return resume2(void_3);
    }
    this.waiters.push(resume2);
    return sync(() => {
      const index = this.waiters.indexOf(resume2);
      if (index !== -1) {
        this.waiters.splice(index, 1);
      }
    });
  });
  unsafeClose() {
    this.isOpen = false;
  }
  close = /* @__PURE__ */ sync(() => {
    this.isOpen = false;
  });
  whenOpen = /* @__PURE__ */ __name((self) => {
    return zipRight2(this.await, self);
  }, "whenOpen");
};
var unsafeMakeLatch = /* @__PURE__ */ __name((open) => new Latch(open ?? false), "unsafeMakeLatch");
var makeLatch = /* @__PURE__ */ __name((open) => sync(() => unsafeMakeLatch(open)), "makeLatch");
var awaitAllChildren = /* @__PURE__ */ __name((self) => ensuringChildren(self, fiberAwaitAll), "awaitAllChildren");
var cached2 = /* @__PURE__ */ dual(2, (self, timeToLive) => map14(cachedInvalidateWithTTL(self, timeToLive), (tuple5) => tuple5[0]));
var cachedInvalidateWithTTL = /* @__PURE__ */ dual(2, (self, timeToLive) => {
  const duration3 = decode(timeToLive);
  return flatMap12(context(), (env) => map14(makeSynchronized(none2()), (cache) => [provideContext(getCachedValue(self, duration3, cache), env), invalidateCache(cache)]));
});
var computeCachedValue = /* @__PURE__ */ __name((self, timeToLive, start3) => {
  const timeToLiveMillis = toMillis(decode(timeToLive));
  return pipe(deferredMake(), tap2((deferred) => intoDeferred(self, deferred)), map14((deferred) => some3([start3 + timeToLiveMillis, deferred])));
}, "computeCachedValue");
var getCachedValue = /* @__PURE__ */ __name((self, timeToLive, cache) => uninterruptibleMask((restore) => pipe(clockWith3((clock3) => clock3.currentTimeMillis), flatMap12((time2) => updateSomeAndGetEffectSynchronized(cache, (option5) => {
  switch (option5._tag) {
    case "None": {
      return some3(computeCachedValue(self, timeToLive, time2));
    }
    case "Some": {
      const [end6] = option5.value;
      return end6 - time2 <= 0 ? some3(computeCachedValue(self, timeToLive, time2)) : none2();
    }
  }
})), flatMap12((option5) => isNone2(option5) ? dieMessage("BUG: Effect.cachedInvalidate - please report an issue at https://github.com/Effect-TS/effect/issues") : restore(deferredAwait(option5.value[1]))))), "getCachedValue");
var invalidateCache = /* @__PURE__ */ __name((cache) => set6(cache, none2()), "invalidateCache");
var ensuringChild = /* @__PURE__ */ dual(2, (self, f) => ensuringChildren(self, (children3) => f(fiberAll(children3))));
var ensuringChildren = /* @__PURE__ */ dual(2, (self, children3) => flatMap12(track, (supervisor) => pipe(supervised(self, supervisor), ensuring2(flatMap12(supervisor.value, children3)))));
var forkAll = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (effects, options) => options?.discard ? forEachSequentialDiscard(effects, fork2) : map14(forEachSequential(effects, fork2), fiberAll));
var forkIn2 = /* @__PURE__ */ dual(2, (self, scope6) => withFiberRuntime((parent, parentStatus) => {
  const scopeImpl = scope6;
  const fiber = unsafeFork2(self, parent, parentStatus.runtimeFlags, globalScope);
  if (scopeImpl.state._tag === "Open") {
    const finalizer3 = /* @__PURE__ */ __name(() => fiberIdWith((fiberId4) => equals(fiberId4, fiber.id()) ? void_3 : asVoid2(interruptFiber(fiber))), "finalizer");
    const key = {};
    scopeImpl.state.finalizers.set(key, finalizer3);
    fiber.addObserver(() => {
      if (scopeImpl.state._tag === "Closed") return;
      scopeImpl.state.finalizers.delete(key);
    });
  } else {
    fiber.unsafeInterruptAsFork(parent.id());
  }
  return succeed(fiber);
}));
var forkScoped2 = /* @__PURE__ */ __name((self) => scopeWith((scope6) => forkIn2(self, scope6)), "forkScoped");
var fromFiber = /* @__PURE__ */ __name((fiber) => join3(fiber), "fromFiber");
var fromFiberEffect = /* @__PURE__ */ __name((fiber) => suspend(() => flatMap12(fiber, join3)), "fromFiberEffect");
var memoKeySymbol = /* @__PURE__ */ Symbol.for("effect/Effect/memoizeFunction.key");
var Key = class {
  static {
    __name(this, "Key");
  }
  a;
  eq;
  [memoKeySymbol] = memoKeySymbol;
  constructor(a, eq) {
    this.a = a;
    this.eq = eq;
  }
  [symbol3](that) {
    if (hasProperty(that, memoKeySymbol)) {
      if (this.eq) {
        return this.eq(this.a, that.a);
      } else {
        return equals(this.a, that.a);
      }
    }
    return false;
  }
  [symbol2]() {
    return this.eq ? 0 : cached(this, hash(this.a));
  }
};
var cachedFunction = /* @__PURE__ */ __name((f, eq) => {
  return pipe(sync(() => empty23()), flatMap12(makeSynchronized), map14((ref) => (a) => pipe(ref.modifyEffect((map36) => {
    const result = pipe(map36, get9(new Key(a, eq)));
    if (isNone2(result)) {
      return pipe(deferredMake(), tap2((deferred) => pipe(diffFiberRefs(f(a)), intoDeferred(deferred), fork2)), map14((deferred) => [deferred, pipe(map36, set5(new Key(a, eq), deferred))]));
    }
    return succeed([result.value, map36]);
  }), flatMap12(deferredAwait), flatMap12(([patch13, b]) => pipe(patchFiberRefs(patch13), as3(b))))));
}, "cachedFunction");
var raceFirst2 = /* @__PURE__ */ dual(2, (self, that) => pipe(exit(self), race2(exit(that)), (effect4) => flatten7(effect4)));
var supervised = /* @__PURE__ */ dual(2, (self, supervisor) => {
  const supervise = fiberRefLocallyWith(currentSupervisor, (s) => s.zip(supervisor));
  return supervise(self);
});
var timeout2 = /* @__PURE__ */ dual(2, (self, duration3) => timeoutFail(self, {
  onTimeout: /* @__PURE__ */ __name(() => timeoutExceptionFromDuration(duration3), "onTimeout"),
  duration: duration3
}));
var timeoutFail = /* @__PURE__ */ dual(2, (self, {
  duration: duration3,
  onTimeout
}) => flatten7(timeoutTo(self, {
  onTimeout: /* @__PURE__ */ __name(() => failSync(onTimeout), "onTimeout"),
  onSuccess: succeed,
  duration: duration3
})));
var timeoutFailCause = /* @__PURE__ */ dual(2, (self, {
  duration: duration3,
  onTimeout
}) => flatten7(timeoutTo(self, {
  onTimeout: /* @__PURE__ */ __name(() => failCauseSync(onTimeout), "onTimeout"),
  onSuccess: succeed,
  duration: duration3
})));
var timeoutOption2 = /* @__PURE__ */ dual(2, (self, duration3) => timeoutTo(self, {
  duration: duration3,
  onSuccess: some3,
  onTimeout: none2
}));
var timeoutTo = /* @__PURE__ */ dual(2, (self, {
  duration: duration3,
  onSuccess,
  onTimeout
}) => fiberIdWith((parentFiberId) => uninterruptibleMask((restore) => raceFibersWith(restore(self), interruptible2(sleep3(duration3)), {
  onSelfWin: /* @__PURE__ */ __name((winner, loser) => flatMap12(winner.await, (exit4) => {
    if (exit4._tag === "Success") {
      return flatMap12(winner.inheritAll, () => as3(interruptAsFiber(loser, parentFiberId), onSuccess(exit4.value)));
    } else {
      return flatMap12(interruptAsFiber(loser, parentFiberId), () => exitFailCause(exit4.cause));
    }
  }), "onSelfWin"),
  onOtherWin: /* @__PURE__ */ __name((winner, loser) => flatMap12(winner.await, (exit4) => {
    if (exit4._tag === "Success") {
      return flatMap12(winner.inheritAll, () => as3(interruptAsFiber(loser, parentFiberId), onTimeout()));
    } else {
      return flatMap12(interruptAsFiber(loser, parentFiberId), () => exitFailCause(exit4.cause));
    }
  }), "onOtherWin"),
  otherScope: globalScope
}))));
var SynchronizedSymbolKey = "effect/Ref/SynchronizedRef";
var SynchronizedTypeId = /* @__PURE__ */ Symbol.for(SynchronizedSymbolKey);
var synchronizedVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var SynchronizedImpl = class extends Class3 {
  static {
    __name(this, "SynchronizedImpl");
  }
  ref;
  withLock;
  [SynchronizedTypeId] = synchronizedVariance;
  [RefTypeId] = refVariance;
  [TypeId17] = TypeId17;
  constructor(ref, withLock) {
    super();
    this.ref = ref;
    this.withLock = withLock;
    this.get = get12(this.ref);
  }
  get;
  commit() {
    return this.get;
  }
  modify(f) {
    return this.modifyEffect((a) => succeed(f(a)));
  }
  modifyEffect(f) {
    return this.withLock(pipe(flatMap12(get12(this.ref), f), flatMap12(([b, a]) => as3(set6(this.ref, a), b))));
  }
};
var makeSynchronized = /* @__PURE__ */ __name((value2) => sync(() => unsafeMakeSynchronized(value2)), "makeSynchronized");
var unsafeMakeSynchronized = /* @__PURE__ */ __name((value2) => {
  const ref = unsafeMake7(value2);
  const sem = unsafeMakeSemaphore(1);
  return new SynchronizedImpl(ref, sem.withPermits(1));
}, "unsafeMakeSynchronized");
var updateSomeAndGetEffectSynchronized = /* @__PURE__ */ dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([value2, value2]);
    }
    case "Some": {
      return map14(result.value, (a) => [a, a]);
    }
  }
}));
var zipFiber = /* @__PURE__ */ dual(2, (self, that) => zipWithFiber(self, that, (a, b) => [a, b]));
var zipLeftFiber = /* @__PURE__ */ dual(2, (self, that) => zipWithFiber(self, that, (a, _) => a));
var zipRightFiber = /* @__PURE__ */ dual(2, (self, that) => zipWithFiber(self, that, (_, b) => b));
var zipWithFiber = /* @__PURE__ */ dual(3, (self, that, f) => ({
  ...CommitPrototype2,
  commit() {
    return join3(this);
  },
  [FiberTypeId]: fiberVariance2,
  id: /* @__PURE__ */ __name(() => pipe(self.id(), getOrElse6(that.id())), "id"),
  await: pipe(self.await, flatten7, zipWithOptions(flatten7(that.await), f, {
    concurrent: true
  }), exit),
  children: self.children,
  inheritAll: zipRight2(that.inheritAll, self.inheritAll),
  poll: zipWith6(self.poll, that.poll, (optionA, optionB) => pipe(optionA, flatMap2((exitA) => pipe(optionB, map2((exitB) => zipWith7(exitA, exitB, {
    onSuccess: f,
    onFailure: parallel
  })))))),
  interruptAsFork: /* @__PURE__ */ __name((id3) => zipRight2(self.interruptAsFork(id3), that.interruptAsFork(id3)), "interruptAsFork"),
  pipe() {
    return pipeArguments(this, arguments);
  }
}));
var bindAll = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, f, options) => flatMap12(self, (a) => all8(f(a), options).pipe(map14((record) => Object.assign({}, a, record)))));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/managedRuntime/circular.js
var TypeId22 = /* @__PURE__ */ Symbol.for("effect/ManagedRuntime");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/layer.js
var OP_EXTEND_SCOPE = "ExtendScope";
var OP_FOLD = "Fold";
var OP_FRESH = "Fresh";
var OP_FROM_EFFECT = "FromEffect";
var OP_SCOPED = "Scoped";
var OP_SUSPEND = "Suspend";
var OP_PROVIDE = "Provide";
var OP_PROVIDE_MERGE = "ProvideMerge";
var OP_MERGE_ALL = "MergeAll";
var OP_ZIP_WITH2 = "ZipWith";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Fiber.js
var Fiber_exports = {};
__export(Fiber_exports, {
  FiberTypeId: () => FiberTypeId2,
  Order: () => Order6,
  RuntimeFiberTypeId: () => RuntimeFiberTypeId2,
  all: () => all9,
  await: () => _await3,
  awaitAll: () => awaitAll,
  children: () => children2,
  done: () => done8,
  dump: () => dump2,
  dumpAll: () => dumpAll2,
  fail: () => fail9,
  failCause: () => failCause7,
  fromEffect: () => fromEffect3,
  getCurrentFiber: () => getCurrentFiber2,
  id: () => id2,
  inheritAll: () => inheritAll2,
  interrupt: () => interrupt7,
  interruptAll: () => interruptAll2,
  interruptAllAs: () => interruptAllAs2,
  interruptAs: () => interruptAs,
  interruptAsFork: () => interruptAsFork2,
  interruptFork: () => interruptFork,
  interrupted: () => interrupted2,
  isFiber: () => isFiber2,
  isRuntimeFiber: () => isRuntimeFiber2,
  join: () => join4,
  joinAll: () => joinAll,
  map: () => map23,
  mapEffect: () => mapEffect3,
  mapFiber: () => mapFiber2,
  match: () => match15,
  never: () => never4,
  orElse: () => orElse6,
  orElseEither: () => orElseEither5,
  poll: () => poll4,
  pretty: () => pretty4,
  roots: () => roots2,
  scoped: () => scoped2,
  status: () => status2,
  succeed: () => succeed9,
  unsafeRoots: () => unsafeRoots2,
  void: () => void_7,
  zip: () => zip11,
  zipLeft: () => zipLeft5,
  zipRight: () => zipRight5,
  zipWith: () => zipWith9
});
var FiberTypeId2 = FiberTypeId;
var RuntimeFiberTypeId2 = RuntimeFiberTypeId;
var Order6 = Order5;
var isFiber2 = isFiber;
var isRuntimeFiber2 = isRuntimeFiber;
var id2 = id;
var _await3 = _await2;
var awaitAll = fiberAwaitAll;
var children2 = children;
var all9 = fiberAll;
var done8 = done5;
var dump2 = dump;
var dumpAll2 = dumpAll;
var fail9 = fail6;
var failCause7 = failCause5;
var fromEffect3 = fromEffect;
var getCurrentFiber2 = getCurrentFiber;
var inheritAll2 = inheritAll;
var interrupt7 = interruptFiber;
var interrupted2 = interrupted;
var interruptAs = interruptAsFiber;
var interruptAsFork2 = interruptAsFork;
var interruptAll2 = interruptAll;
var interruptAllAs2 = interruptAllAs;
var interruptFork = fiberInterruptFork;
var join4 = join3;
var joinAll = fiberJoinAll;
var map23 = map18;
var mapEffect3 = mapEffect2;
var mapFiber2 = mapFiber;
var match15 = match13;
var never4 = never3;
var orElse6 = orElse5;
var orElseEither5 = orElseEither3;
var poll4 = poll3;
var pretty4 = pretty2;
var roots2 = roots;
var unsafeRoots2 = unsafeRoots;
var scoped2 = fiberScoped;
var status2 = status;
var succeed9 = succeed5;
var void_7 = void_6;
var zip11 = zipFiber;
var zipLeft5 = zipLeftFiber;
var zipRight5 = zipRightFiber;
var zipWith9 = zipWithFiber;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/runtime.js
var makeDual = /* @__PURE__ */ __name((f) => function() {
  if (arguments.length === 1) {
    const runtime5 = arguments[0];
    return (effect4, ...args2) => f(runtime5, effect4, ...args2);
  }
  return f.apply(this, arguments);
}, "makeDual");
var unsafeFork3 = /* @__PURE__ */ makeDual((runtime5, self, options) => {
  const fiberId4 = unsafeMake3();
  const fiberRefUpdates = [[currentContext, [[fiberId4, runtime5.context]]]];
  if (options?.scheduler) {
    fiberRefUpdates.push([currentScheduler, [[fiberId4, options.scheduler]]]);
  }
  let fiberRefs4 = updateManyAs2(runtime5.fiberRefs, {
    entries: fiberRefUpdates,
    forkAs: fiberId4
  });
  if (options?.updateRefs) {
    fiberRefs4 = options.updateRefs(fiberRefs4, fiberId4);
  }
  const fiberRuntime = new FiberRuntime(fiberId4, fiberRefs4, runtime5.runtimeFlags);
  let effect4 = self;
  if (options?.scope) {
    effect4 = flatMap12(fork3(options.scope, sequential2), (closeableScope) => zipRight2(scopeAddFinalizer(closeableScope, fiberIdWith((id3) => equals(id3, fiberRuntime.id()) ? void_3 : interruptAsFiber(fiberRuntime, id3))), onExit(self, (exit4) => close(closeableScope, exit4))));
  }
  const supervisor = fiberRuntime.currentSupervisor;
  if (supervisor !== none8) {
    supervisor.onStart(runtime5.context, effect4, none2(), fiberRuntime);
    fiberRuntime.addObserver((exit4) => supervisor.onEnd(exit4, fiberRuntime));
  }
  globalScope.add(runtime5.runtimeFlags, fiberRuntime);
  if (options?.immediate === false) {
    fiberRuntime.resume(effect4);
  } else {
    fiberRuntime.start(effect4);
  }
  return fiberRuntime;
});
var unsafeRunCallback = /* @__PURE__ */ makeDual((runtime5, effect4, options = {}) => {
  const fiberRuntime = unsafeFork3(runtime5, effect4, options);
  if (options.onExit) {
    fiberRuntime.addObserver((exit4) => {
      options.onExit(exit4);
    });
  }
  return (id3, cancelOptions) => unsafeRunCallback(runtime5)(pipe(fiberRuntime, interruptAs(id3 ?? none4)), {
    ...cancelOptions,
    onExit: cancelOptions?.onExit ? (exit4) => cancelOptions.onExit(flatten8(exit4)) : void 0
  });
});
var unsafeRunSync = /* @__PURE__ */ makeDual((runtime5, effect4) => {
  const result = unsafeRunSyncExit(runtime5)(effect4);
  if (result._tag === "Failure") {
    throw fiberFailure(result.effect_instruction_i0);
  }
  return result.effect_instruction_i0;
});
var AsyncFiberExceptionImpl = class extends Error {
  static {
    __name(this, "AsyncFiberExceptionImpl");
  }
  fiber;
  _tag = "AsyncFiberException";
  constructor(fiber) {
    super(`Fiber #${fiber.id().id} cannot be resolved synchronously. This is caused by using runSync on an effect that performs async work`);
    this.fiber = fiber;
    this.name = this._tag;
    this.stack = this.message;
  }
};
var asyncFiberException = /* @__PURE__ */ __name((fiber) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 0;
  const error2 = new AsyncFiberExceptionImpl(fiber);
  Error.stackTraceLimit = limit;
  return error2;
}, "asyncFiberException");
var isAsyncFiberException = /* @__PURE__ */ __name((u) => isTagged(u, "AsyncFiberException") && "fiber" in u, "isAsyncFiberException");
var FiberFailureId = /* @__PURE__ */ Symbol.for("effect/Runtime/FiberFailure");
var FiberFailureCauseId = /* @__PURE__ */ Symbol.for("effect/Runtime/FiberFailure/Cause");
var FiberFailureImpl = class extends Error {
  static {
    __name(this, "FiberFailureImpl");
  }
  [FiberFailureId];
  [FiberFailureCauseId];
  constructor(cause3) {
    const head10 = prettyErrors(cause3)[0];
    super(head10?.message || "An error has occurred");
    this[FiberFailureId] = FiberFailureId;
    this[FiberFailureCauseId] = cause3;
    this.name = head10 ? `(FiberFailure) ${head10.name}` : "FiberFailure";
    if (head10?.stack) {
      this.stack = head10.stack;
    }
  }
  toJSON() {
    return {
      _id: "FiberFailure",
      cause: this[FiberFailureCauseId].toJSON()
    };
  }
  toString() {
    return "(FiberFailure) " + pretty(this[FiberFailureCauseId], {
      renderErrorCause: true
    });
  }
  [NodeInspectSymbol]() {
    return this.toString();
  }
};
var fiberFailure = /* @__PURE__ */ __name((cause3) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 0;
  const error2 = new FiberFailureImpl(cause3);
  Error.stackTraceLimit = limit;
  return error2;
}, "fiberFailure");
var isFiberFailure = /* @__PURE__ */ __name((u) => hasProperty(u, FiberFailureId), "isFiberFailure");
var fastPath = /* @__PURE__ */ __name((effect4) => {
  const op = effect4;
  switch (op._op) {
    case "Failure":
    case "Success": {
      return op;
    }
    case "Left": {
      return exitFail(op.left);
    }
    case "Right": {
      return exitSucceed(op.right);
    }
    case "Some": {
      return exitSucceed(op.value);
    }
    case "None": {
      return exitFail(new NoSuchElementException());
    }
  }
}, "fastPath");
var unsafeRunSyncExit = /* @__PURE__ */ makeDual((runtime5, effect4) => {
  const op = fastPath(effect4);
  if (op) {
    return op;
  }
  const scheduler = new SyncScheduler();
  const fiberRuntime = unsafeFork3(runtime5)(effect4, {
    scheduler
  });
  scheduler.flush();
  const result = fiberRuntime.unsafePoll();
  if (result) {
    return result;
  }
  return exitDie(capture(asyncFiberException(fiberRuntime), currentSpanFromFiber(fiberRuntime)));
});
var unsafeRunPromise = /* @__PURE__ */ makeDual((runtime5, effect4, options) => unsafeRunPromiseExit(runtime5, effect4, options).then((result) => {
  switch (result._tag) {
    case OP_SUCCESS: {
      return result.effect_instruction_i0;
    }
    case OP_FAILURE: {
      throw fiberFailure(result.effect_instruction_i0);
    }
  }
}));
var unsafeRunPromiseExit = /* @__PURE__ */ makeDual((runtime5, effect4, options) => new Promise((resolve) => {
  const op = fastPath(effect4);
  if (op) {
    resolve(op);
  }
  const fiber = unsafeFork3(runtime5)(effect4);
  fiber.addObserver((exit4) => {
    resolve(exit4);
  });
  if (options?.signal !== void 0) {
    if (options.signal.aborted) {
      fiber.unsafeInterruptAsFork(fiber.id());
    } else {
      options.signal.addEventListener("abort", () => {
        fiber.unsafeInterruptAsFork(fiber.id());
      }, {
        once: true
      });
    }
  }
}));
var RuntimeImpl = class {
  static {
    __name(this, "RuntimeImpl");
  }
  context;
  runtimeFlags;
  fiberRefs;
  constructor(context11, runtimeFlags2, fiberRefs4) {
    this.context = context11;
    this.runtimeFlags = runtimeFlags2;
    this.fiberRefs = fiberRefs4;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make53 = /* @__PURE__ */ __name((options) => new RuntimeImpl(options.context, options.runtimeFlags, options.fiberRefs), "make");
var runtime3 = /* @__PURE__ */ __name(() => withFiberRuntime((state, status3) => succeed(new RuntimeImpl(state.getFiberRef(currentContext), status3.runtimeFlags, state.getFiberRefs()))), "runtime");
var defaultRuntimeFlags = /* @__PURE__ */ make20(Interruption, CooperativeYielding, RuntimeMetrics);
var defaultRuntime = /* @__PURE__ */ make53({
  context: /* @__PURE__ */ empty6(),
  runtimeFlags: defaultRuntimeFlags,
  fiberRefs: /* @__PURE__ */ empty27()
});
var updateRuntimeFlags2 = /* @__PURE__ */ dual(2, (self, f) => make53({
  context: self.context,
  runtimeFlags: f(self.runtimeFlags),
  fiberRefs: self.fiberRefs
}));
var disableRuntimeFlag = /* @__PURE__ */ dual(2, (self, flag) => updateRuntimeFlags2(self, disable2(flag)));
var enableRuntimeFlag = /* @__PURE__ */ dual(2, (self, flag) => updateRuntimeFlags2(self, enable2(flag)));
var updateContext2 = /* @__PURE__ */ dual(2, (self, f) => make53({
  context: f(self.context),
  runtimeFlags: self.runtimeFlags,
  fiberRefs: self.fiberRefs
}));
var provideService3 = /* @__PURE__ */ dual(3, (self, tag, service6) => updateContext2(self, add2(tag, service6)));
var updateFiberRefs2 = /* @__PURE__ */ dual(2, (self, f) => make53({
  context: self.context,
  runtimeFlags: self.runtimeFlags,
  fiberRefs: f(self.fiberRefs)
}));
var setFiberRef = /* @__PURE__ */ dual(3, (self, fiberRef, value2) => updateFiberRefs2(self, updateAs2({
  fiberId: none4,
  fiberRef,
  value: value2
})));
var deleteFiberRef = /* @__PURE__ */ dual(2, (self, fiberRef) => updateFiberRefs2(self, delete_2(fiberRef)));
var unsafeRunEffect = /* @__PURE__ */ unsafeRunCallback(defaultRuntime);
var unsafeForkEffect = /* @__PURE__ */ unsafeFork3(defaultRuntime);
var unsafeRunPromiseEffect = /* @__PURE__ */ unsafeRunPromise(defaultRuntime);
var unsafeRunPromiseExitEffect = /* @__PURE__ */ unsafeRunPromiseExit(defaultRuntime);
var unsafeRunSyncEffect = /* @__PURE__ */ unsafeRunSync(defaultRuntime);
var unsafeRunSyncExitEffect = /* @__PURE__ */ unsafeRunSyncExit(defaultRuntime);
var asyncEffect = /* @__PURE__ */ __name((register) => suspend(() => {
  let cleanup = void 0;
  return flatMap12(deferredMake(), (deferred) => flatMap12(runtime3(), (runtime5) => uninterruptibleMask((restore) => zipRight2(fork2(restore(matchCauseEffect(register((cb) => unsafeRunCallback(runtime5)(intoDeferred(cb, deferred))), {
    onFailure: /* @__PURE__ */ __name((cause3) => deferredFailCause(deferred, cause3), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((cleanup_) => {
      cleanup = cleanup_;
      return void_3;
    }, "onSuccess")
  }))), restore(onInterrupt(deferredAwait(deferred), () => cleanup ?? void_3))))));
}), "asyncEffect");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/synchronizedRef.js
var getAndUpdateEffect = /* @__PURE__ */ dual(2, (self, f) => self.modifyEffect((value2) => map14(f(value2), (result) => [value2, result])));
var getAndUpdateSomeEffect = /* @__PURE__ */ dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([value2, value2]);
    }
    case "Some": {
      return map14(result.value, (newValue) => [value2, newValue]);
    }
  }
}));
var modify10 = /* @__PURE__ */ dual(2, (self, f) => self.modify(f));
var modifyEffect = /* @__PURE__ */ dual(2, (self, f) => self.modifyEffect(f));
var modifySomeEffect = /* @__PURE__ */ dual(3, (self, fallback, pf) => self.modifyEffect((value2) => pipe(pf(value2), getOrElse2(() => succeed([fallback, value2])))));
var updateEffect = /* @__PURE__ */ dual(2, (self, f) => self.modifyEffect((value2) => map14(f(value2), (result) => [void 0, result])));
var updateAndGetEffect = /* @__PURE__ */ dual(2, (self, f) => self.modifyEffect((value2) => map14(f(value2), (result) => [result, result])));
var updateSomeEffect = /* @__PURE__ */ dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([void 0, value2]);
    }
    case "Some": {
      return map14(result.value, (a) => [void 0, a]);
    }
  }
}));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/layer.js
var LayerSymbolKey = "effect/Layer";
var LayerTypeId = /* @__PURE__ */ Symbol.for(LayerSymbolKey);
var layerVariance = {
  /* c8 ignore next */
  _RIn: /* @__PURE__ */ __name((_) => _, "_RIn"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _ROut: /* @__PURE__ */ __name((_) => _, "_ROut")
};
var proto3 = {
  [LayerTypeId]: layerVariance,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var MemoMapTypeIdKey = "effect/Layer/MemoMap";
var MemoMapTypeId = /* @__PURE__ */ Symbol.for(MemoMapTypeIdKey);
var CurrentMemoMap = /* @__PURE__ */ Reference2()("effect/Layer/CurrentMemoMap", {
  defaultValue: /* @__PURE__ */ __name(() => unsafeMakeMemoMap(), "defaultValue")
});
var isLayer = /* @__PURE__ */ __name((u) => hasProperty(u, LayerTypeId), "isLayer");
var isFresh = /* @__PURE__ */ __name((self) => {
  return self._op_layer === OP_FRESH;
}, "isFresh");
var MemoMapImpl = class {
  static {
    __name(this, "MemoMapImpl");
  }
  ref;
  [MemoMapTypeId];
  constructor(ref) {
    this.ref = ref;
    this[MemoMapTypeId] = MemoMapTypeId;
  }
  /**
   * Checks the memo map to see if a layer exists. If it is, immediately
   * returns it. Otherwise, obtains the layer, stores it in the memo map,
   * and adds a finalizer to the `Scope`.
   */
  getOrElseMemoize(layer, scope6) {
    return pipe(modifyEffect(this.ref, (map36) => {
      const inMap = map36.get(layer);
      if (inMap !== void 0) {
        const [acquire, release] = inMap;
        const cached4 = pipe(acquire, flatMap12(([patch13, b]) => pipe(patchFiberRefs(patch13), as3(b))), onExit(exitMatch({
          onFailure: /* @__PURE__ */ __name(() => void_3, "onFailure"),
          onSuccess: /* @__PURE__ */ __name(() => scopeAddFinalizerExit(scope6, release), "onSuccess")
        })));
        return succeed([cached4, map36]);
      }
      return pipe(make34(0), flatMap12((observers) => pipe(deferredMake(), flatMap12((deferred) => pipe(make34(() => void_3), map14((finalizerRef) => {
        const resource = uninterruptibleMask((restore) => pipe(scopeMake2(), flatMap12((innerScope) => pipe(restore(flatMap12(makeBuilder(layer, innerScope, true), (f) => diffFiberRefs(f(this)))), exit, flatMap12((exit4) => {
          switch (exit4._tag) {
            case OP_FAILURE: {
              return pipe(deferredFailCause(deferred, exit4.effect_instruction_i0), zipRight2(scopeClose(innerScope, exit4)), zipRight2(failCause(exit4.effect_instruction_i0)));
            }
            case OP_SUCCESS: {
              return pipe(set6(finalizerRef, (exit5) => pipe(scopeClose(innerScope, exit5), whenEffect(modify7(observers, (n) => [n === 1, n - 1])), asVoid2)), zipRight2(update3(observers, (n) => n + 1)), zipRight2(scopeAddFinalizerExit(scope6, (exit5) => pipe(sync(() => map36.delete(layer)), zipRight2(get12(finalizerRef)), flatMap12((finalizer3) => finalizer3(exit5))))), zipRight2(deferredSucceed(deferred, exit4.effect_instruction_i0)), as3(exit4.effect_instruction_i0[1]));
            }
          }
        })))));
        const memoized = [pipe(deferredAwait(deferred), onExit(exitMatchEffect({
          onFailure: /* @__PURE__ */ __name(() => void_3, "onFailure"),
          onSuccess: /* @__PURE__ */ __name(() => update3(observers, (n) => n + 1), "onSuccess")
        }))), (exit4) => pipe(get12(finalizerRef), flatMap12((finalizer3) => finalizer3(exit4)))];
        return [resource, isFresh(layer) ? map36 : map36.set(layer, memoized)];
      }))))));
    }), flatten7);
  }
};
var makeMemoMap = /* @__PURE__ */ suspend(() => map14(makeSynchronized(/* @__PURE__ */ new Map()), (ref) => new MemoMapImpl(ref)));
var unsafeMakeMemoMap = /* @__PURE__ */ __name(() => new MemoMapImpl(unsafeMakeSynchronized(/* @__PURE__ */ new Map())), "unsafeMakeMemoMap");
var build = /* @__PURE__ */ __name((self) => scopeWith((scope6) => buildWithScope(self, scope6)), "build");
var buildWithScope = /* @__PURE__ */ dual(2, (self, scope6) => flatMap12(makeMemoMap, (memoMap) => buildWithMemoMap(self, memoMap, scope6)));
var buildWithMemoMap = /* @__PURE__ */ dual(3, (self, memoMap, scope6) => flatMap12(makeBuilder(self, scope6), (run6) => provideService(run6(memoMap), CurrentMemoMap, memoMap)));
var makeBuilder = /* @__PURE__ */ __name((self, scope6, inMemoMap = false) => {
  const op = self;
  switch (op._op_layer) {
    case "Locally": {
      return sync(() => (memoMap) => op.f(memoMap.getOrElseMemoize(op.self, scope6)));
    }
    case "ExtendScope": {
      return sync(() => (memoMap) => scopeWith((scope7) => memoMap.getOrElseMemoize(op.layer, scope7)));
    }
    case "Fold": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.layer, scope6), matchCauseEffect({
        onFailure: /* @__PURE__ */ __name((cause3) => memoMap.getOrElseMemoize(op.failureK(cause3), scope6), "onFailure"),
        onSuccess: /* @__PURE__ */ __name((value2) => memoMap.getOrElseMemoize(op.successK(value2), scope6), "onSuccess")
      })));
    }
    case "Fresh": {
      return sync(() => (_) => pipe(op.layer, buildWithScope(scope6)));
    }
    case "FromEffect": {
      return inMemoMap ? sync(() => (_) => op.effect) : sync(() => (memoMap) => memoMap.getOrElseMemoize(self, scope6));
    }
    case "Provide": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, scope6), flatMap12((env) => pipe(memoMap.getOrElseMemoize(op.second, scope6), provideContext(env)))));
    }
    case "Scoped": {
      return inMemoMap ? sync(() => (_) => scopeExtend(op.effect, scope6)) : sync(() => (memoMap) => memoMap.getOrElseMemoize(self, scope6));
    }
    case "Suspend": {
      return sync(() => (memoMap) => memoMap.getOrElseMemoize(op.evaluate(), scope6));
    }
    case "ProvideMerge": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, scope6), zipWith6(memoMap.getOrElseMemoize(op.second, scope6), op.zipK)));
    }
    case "ZipWith": {
      return gen3(function* () {
        const parallelScope = yield* scopeFork(scope6, parallel2);
        const firstScope = yield* scopeFork(parallelScope, sequential2);
        const secondScope = yield* scopeFork(parallelScope, sequential2);
        return (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, firstScope), zipWithOptions(memoMap.getOrElseMemoize(op.second, secondScope), op.zipK, {
          concurrent: true
        }));
      });
    }
    case "MergeAll": {
      const layers = op.layers;
      return map14(scopeFork(scope6, parallel2), (parallelScope) => (memoMap) => {
        const contexts = new Array(layers.length);
        return map14(forEachConcurrentDiscard(layers, fnUntraced(function* (layer, i) {
          const scope7 = yield* scopeFork(parallelScope, sequential2);
          const context11 = yield* memoMap.getOrElseMemoize(layer, scope7);
          contexts[i] = context11;
        }), false, false), () => mergeAll2(...contexts));
      });
    }
  }
}, "makeBuilder");
var catchAll3 = /* @__PURE__ */ dual(2, (self, onFailure) => match16(self, {
  onFailure,
  onSuccess: succeedContext
}));
var catchAllCause3 = /* @__PURE__ */ dual(2, (self, onFailure) => matchCause3(self, {
  onFailure,
  onSuccess: succeedContext
}));
var die7 = /* @__PURE__ */ __name((defect) => failCause8(die6(defect)), "die");
var dieSync3 = /* @__PURE__ */ __name((evaluate3) => failCauseSync4(() => die6(evaluate3())), "dieSync");
var discard = /* @__PURE__ */ __name((self) => map24(self, () => empty6()), "discard");
var context3 = /* @__PURE__ */ __name(() => fromEffectContext(context()), "context");
var extendScope = /* @__PURE__ */ __name((self) => {
  const extendScope3 = Object.create(proto3);
  extendScope3._op_layer = OP_EXTEND_SCOPE;
  extendScope3.layer = self;
  return extendScope3;
}, "extendScope");
var fail10 = /* @__PURE__ */ __name((error2) => failCause8(fail8(error2)), "fail");
var failSync4 = /* @__PURE__ */ __name((evaluate3) => failCauseSync4(() => fail8(evaluate3())), "failSync");
var failCause8 = /* @__PURE__ */ __name((cause3) => fromEffectContext(failCause(cause3)), "failCause");
var failCauseSync4 = /* @__PURE__ */ __name((evaluate3) => fromEffectContext(failCauseSync(evaluate3)), "failCauseSync");
var flatMap17 = /* @__PURE__ */ dual(2, (self, f) => match16(self, {
  onFailure: fail10,
  onSuccess: f
}));
var flatten11 = /* @__PURE__ */ dual(2, (self, tag) => flatMap17(self, get4(tag)));
var fresh = /* @__PURE__ */ __name((self) => {
  const fresh3 = Object.create(proto3);
  fresh3._op_layer = OP_FRESH;
  fresh3.layer = self;
  return fresh3;
}, "fresh");
var fromEffect4 = /* @__PURE__ */ dual(2, (a, b) => {
  const tagFirst = isTag2(a);
  const tag = tagFirst ? a : b;
  const effect4 = tagFirst ? b : a;
  return fromEffectContext(map14(effect4, (service6) => make6(tag, service6)));
});
var fromEffectDiscard = /* @__PURE__ */ __name((effect4) => fromEffectContext(map14(effect4, () => empty6())), "fromEffectDiscard");
function fromEffectContext(effect4) {
  const fromEffect11 = Object.create(proto3);
  fromEffect11._op_layer = OP_FROM_EFFECT;
  fromEffect11.effect = effect4;
  return fromEffect11;
}
__name(fromEffectContext, "fromEffectContext");
var fiberRefLocally2 = /* @__PURE__ */ dual(3, (self, ref, value2) => locallyEffect(self, fiberRefLocally(ref, value2)));
var locallyEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const locally4 = Object.create(proto3);
  locally4._op_layer = "Locally";
  locally4.self = self;
  locally4.f = f;
  return locally4;
});
var fiberRefLocallyWith2 = /* @__PURE__ */ dual(3, (self, ref, value2) => locallyEffect(self, fiberRefLocallyWith(ref, value2)));
var fiberRefLocallyScoped2 = /* @__PURE__ */ __name((self, value2) => scopedDiscard(fiberRefLocallyScoped(self, value2)), "fiberRefLocallyScoped");
var fiberRefLocallyScopedWith2 = /* @__PURE__ */ __name((self, value2) => scopedDiscard(fiberRefLocallyScopedWith(self, value2)), "fiberRefLocallyScopedWith");
var fromFunction = /* @__PURE__ */ __name((tagA, tagB, f) => fromEffectContext(map14(tagA, (a) => make6(tagB, f(a)))), "fromFunction");
var launch = /* @__PURE__ */ __name((self) => scopedEffect(zipRight2(scopeWith((scope6) => pipe(self, buildWithScope(scope6))), never)), "launch");
var mock = /* @__PURE__ */ __name(function() {
  if (arguments.length === 1) {
    return (service6) => mockImpl(arguments[0], service6);
  }
  return mockImpl(arguments[0], arguments[1]);
}, "mock");
var mockImpl = /* @__PURE__ */ __name((tag, service6) => succeed10(tag, new Proxy({
  ...service6
}, {
  get(target, prop, _receiver) {
    if (prop in target) {
      return target[prop];
    }
    const prevLimit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const error2 = new Error(`${tag.key}: Unimplemented method "${prop.toString()}"`);
    Error.stackTraceLimit = prevLimit;
    error2.name = "UnimplementedError";
    return makeUnimplemented(error2);
  },
  has: constTrue
})), "mockImpl");
var makeUnimplemented = /* @__PURE__ */ __name((error2) => {
  const dead = die2(error2);
  function unimplemented() {
    return dead;
  }
  __name(unimplemented, "unimplemented");
  Object.assign(unimplemented, dead);
  Object.setPrototypeOf(unimplemented, Object.getPrototypeOf(dead));
  return unimplemented;
}, "makeUnimplemented");
var map24 = /* @__PURE__ */ dual(2, (self, f) => flatMap17(self, (context11) => succeedContext(f(context11))));
var mapError4 = /* @__PURE__ */ dual(2, (self, f) => catchAll3(self, (error2) => failSync4(() => f(error2))));
var matchCause3 = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  const fold2 = Object.create(proto3);
  fold2._op_layer = OP_FOLD;
  fold2.layer = self;
  fold2.failureK = onFailure;
  fold2.successK = onSuccess;
  return fold2;
});
var match16 = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => matchCause3(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const failureOrCause3 = failureOrCause2(cause3);
    switch (failureOrCause3._tag) {
      case "Left": {
        return onFailure(failureOrCause3.left);
      }
      case "Right": {
        return failCause8(failureOrCause3.right);
      }
    }
  }, "onFailure"),
  onSuccess
}));
var memoize2 = /* @__PURE__ */ __name((self) => scopeWith((scope6) => map14(memoize(buildWithScope(self, scope6)), fromEffectContext)), "memoize");
var merge6 = /* @__PURE__ */ dual(2, (self, that) => zipWith10(self, that, (a, b) => merge3(a, b)));
var mergeAll4 = /* @__PURE__ */ __name((...layers) => {
  const mergeAll11 = Object.create(proto3);
  mergeAll11._op_layer = OP_MERGE_ALL;
  mergeAll11.layers = layers;
  return mergeAll11;
}, "mergeAll");
var orDie3 = /* @__PURE__ */ __name((self) => catchAll3(self, (defect) => die7(defect)), "orDie");
var orElse7 = /* @__PURE__ */ dual(2, (self, that) => catchAll3(self, that));
var passthrough = /* @__PURE__ */ __name((self) => merge6(context3(), self), "passthrough");
var project = /* @__PURE__ */ dual(4, (self, tagA, tagB, f) => map24(self, (context11) => make6(tagB, f(unsafeGet3(context11, tagA)))));
var retry2 = /* @__PURE__ */ dual(2, (self, schedule4) => suspend3(() => {
  const stateTag = GenericTag("effect/Layer/retry/{ state: unknown }");
  return pipe(succeed10(stateTag, {
    state: schedule4.initial
  }), flatMap17((env) => retryLoop(self, schedule4, stateTag, pipe(env, get4(stateTag)).state)));
}));
var retryLoop = /* @__PURE__ */ __name((self, schedule4, stateTag, state) => {
  return pipe(self, catchAll3((error2) => pipe(retryUpdate(schedule4, stateTag, error2, state), flatMap17((env) => fresh(retryLoop(self, schedule4, stateTag, pipe(env, get4(stateTag)).state))))));
}, "retryLoop");
var retryUpdate = /* @__PURE__ */ __name((schedule4, stateTag, error2, state) => {
  return fromEffect4(stateTag, pipe(currentTimeMillis2, flatMap12((now2) => pipe(schedule4.step(now2, error2, state), flatMap12(([state2, _, decision]) => isDone5(decision) ? fail2(error2) : pipe(sleep2(millis(start2(decision.intervals) - now2)), as3({
    state: state2
  })))))));
}, "retryUpdate");
var scoped3 = /* @__PURE__ */ dual(2, (a, b) => {
  const tagFirst = isTag2(a);
  const tag = tagFirst ? a : b;
  const effect4 = tagFirst ? b : a;
  return scopedContext(map14(effect4, (service6) => make6(tag, service6)));
});
var scopedDiscard = /* @__PURE__ */ __name((effect4) => scopedContext(pipe(effect4, as3(empty6()))), "scopedDiscard");
var scopedContext = /* @__PURE__ */ __name((effect4) => {
  const scoped9 = Object.create(proto3);
  scoped9._op_layer = OP_SCOPED;
  scoped9.effect = effect4;
  return scoped9;
}, "scopedContext");
var scope3 = /* @__PURE__ */ scopedContext(/* @__PURE__ */ map14(/* @__PURE__ */ acquireRelease2(/* @__PURE__ */ scopeMake2(), (scope6, exit4) => scope6.close(exit4)), (scope6) => make6(Scope, scope6)));
var service2 = /* @__PURE__ */ __name((tag) => fromEffect4(tag, tag), "service");
var succeed10 = /* @__PURE__ */ dual(2, (a, b) => {
  const tagFirst = isTag2(a);
  const tag = tagFirst ? a : b;
  const resource = tagFirst ? b : a;
  return fromEffectContext(succeed(make6(tag, resource)));
});
var succeedContext = /* @__PURE__ */ __name((context11) => {
  return fromEffectContext(succeed(context11));
}, "succeedContext");
var empty40 = /* @__PURE__ */ succeedContext(/* @__PURE__ */ empty6());
var suspend3 = /* @__PURE__ */ __name((evaluate3) => {
  const suspend12 = Object.create(proto3);
  suspend12._op_layer = OP_SUSPEND;
  suspend12.evaluate = evaluate3;
  return suspend12;
}, "suspend");
var sync6 = /* @__PURE__ */ dual(2, (a, b) => {
  const tagFirst = isTag2(a);
  const tag = tagFirst ? a : b;
  const evaluate3 = tagFirst ? b : a;
  return fromEffectContext(sync(() => make6(tag, evaluate3())));
});
var syncContext = /* @__PURE__ */ __name((evaluate3) => {
  return fromEffectContext(sync(evaluate3));
}, "syncContext");
var tap4 = /* @__PURE__ */ dual(2, (self, f) => flatMap17(self, (context11) => fromEffectContext(as3(f(context11), context11))));
var tapError3 = /* @__PURE__ */ dual(2, (self, f) => catchAll3(self, (e) => fromEffectContext(flatMap12(f(e), () => fail2(e)))));
var tapErrorCause3 = /* @__PURE__ */ dual(2, (self, f) => catchAllCause3(self, (cause3) => fromEffectContext(flatMap12(f(cause3), () => failCause(cause3)))));
var toRuntime = /* @__PURE__ */ __name((self) => pipe(scopeWith((scope6) => buildWithScope(self, scope6)), flatMap12((context11) => pipe(runtime3(), provideContext(context11)))), "toRuntime");
var toRuntimeWithMemoMap = /* @__PURE__ */ dual(2, (self, memoMap) => flatMap12(scopeWith((scope6) => buildWithMemoMap(self, memoMap, scope6)), (context11) => pipe(runtime3(), provideContext(context11))));
var provide = /* @__PURE__ */ dual(2, (self, that) => suspend3(() => {
  const provideTo = Object.create(proto3);
  provideTo._op_layer = OP_PROVIDE;
  provideTo.first = Object.create(proto3, {
    _op_layer: {
      value: OP_PROVIDE_MERGE,
      enumerable: true
    },
    first: {
      value: context3(),
      enumerable: true
    },
    second: {
      value: Array.isArray(that) ? mergeAll4(...that) : that
    },
    zipK: {
      value: /* @__PURE__ */ __name((a, b) => pipe(a, merge3(b)), "value")
    }
  });
  provideTo.second = self;
  return provideTo;
}));
var provideMerge = /* @__PURE__ */ dual(2, (that, self) => {
  const zipWith19 = Object.create(proto3);
  zipWith19._op_layer = OP_PROVIDE_MERGE;
  zipWith19.first = self;
  zipWith19.second = provide(that, self);
  zipWith19.zipK = (a, b) => {
    return pipe(a, merge3(b));
  };
  return zipWith19;
});
var zipWith10 = /* @__PURE__ */ dual(3, (self, that, f) => suspend3(() => {
  const zipWith19 = Object.create(proto3);
  zipWith19._op_layer = OP_ZIP_WITH2;
  zipWith19.first = self;
  zipWith19.second = that;
  zipWith19.zipK = f;
  return zipWith19;
}));
var unwrapEffect = /* @__PURE__ */ __name((self) => {
  const tag = GenericTag("effect/Layer/unwrapEffect/Layer.Layer<R1, E1, A>");
  return flatMap17(fromEffect4(tag, self), (context11) => get4(context11, tag));
}, "unwrapEffect");
var unwrapScoped = /* @__PURE__ */ __name((self) => {
  const tag = GenericTag("effect/Layer/unwrapScoped/Layer.Layer<R1, E1, A>");
  return flatMap17(scoped3(tag, self), (context11) => get4(context11, tag));
}, "unwrapScoped");
var annotateLogs2 = /* @__PURE__ */ dual((args2) => isLayer(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith2(args2[0], currentLogAnnotations, typeof args2[1] === "string" ? set4(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set4(acc, key, value2), annotations));
});
var annotateSpans2 = /* @__PURE__ */ dual((args2) => isLayer(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith2(args2[0], currentTracerSpanAnnotations, typeof args2[1] === "string" ? set4(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set4(acc, key, value2), annotations));
});
var withSpan2 = /* @__PURE__ */ __name(function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return unwrapScoped(map14(options?.onEnd ? tap2(makeSpanScoped(name, options), (span4) => addFinalizer2((exit4) => options.onEnd(span4, exit4))) : makeSpanScoped(name, options), (span4) => withParentSpan2(self, span4)));
  }
  return (self) => unwrapScoped(map14(options?.onEnd ? tap2(makeSpanScoped(name, options), (span4) => addFinalizer2((exit4) => options.onEnd(span4, exit4))) : makeSpanScoped(name, options), (span4) => withParentSpan2(self, span4)));
}, "withSpan");
var withParentSpan2 = /* @__PURE__ */ dual(2, (self, span4) => provide(self, succeedContext(make6(spanTag, span4))));
var provideSomeLayer = /* @__PURE__ */ dual(2, (self, layer) => scopedWith((scope6) => flatMap12(buildWithScope(layer, scope6), (context11) => provideSomeContext(self, context11))));
var provideSomeRuntime = /* @__PURE__ */ dual(2, (self, rt) => {
  const patchRefs = diff9(defaultRuntime.fiberRefs, rt.fiberRefs);
  const patchFlags = diff7(defaultRuntime.runtimeFlags, rt.runtimeFlags);
  return uninterruptibleMask((restore) => withFiberRuntime((fiber) => {
    const oldContext = fiber.getFiberRef(currentContext);
    const oldRefs = fiber.getFiberRefs();
    const newRefs = patch10(fiber.id(), oldRefs)(patchRefs);
    const oldFlags = fiber.currentRuntimeFlags;
    const newFlags = patch7(patchFlags)(oldFlags);
    const rollbackRefs = diff9(newRefs, oldRefs);
    const rollbackFlags = diff7(newFlags, oldFlags);
    fiber.setFiberRefs(newRefs);
    fiber.currentRuntimeFlags = newFlags;
    return ensuring2(provideSomeContext(restore(self), merge3(oldContext, rt.context)), withFiberRuntime((fiber2) => {
      fiber2.setFiberRefs(patch10(fiber2.id(), fiber2.getFiberRefs())(rollbackRefs));
      fiber2.currentRuntimeFlags = patch7(rollbackFlags)(fiber2.currentRuntimeFlags);
      return void_3;
    }));
  }));
});
var effect_provide = /* @__PURE__ */ dual(2, (self, source) => {
  if (Array.isArray(source)) {
    return provideSomeLayer(self, mergeAll4(...source));
  } else if (isLayer(source)) {
    return provideSomeLayer(self, source);
  } else if (isContext2(source)) {
    return provideSomeContext(self, source);
  } else if (TypeId22 in source) {
    return flatMap12(source.runtimeEffect, (rt) => provideSomeRuntime(self, rt));
  } else {
    return provideSomeRuntime(self, source);
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/console.js
var console2 = /* @__PURE__ */ map14(/* @__PURE__ */ fiberRefGet(currentServices), /* @__PURE__ */ get4(consoleTag));
var consoleWith = /* @__PURE__ */ __name((f) => fiberRefGetWith(currentServices, (services) => f(get4(services, consoleTag))), "consoleWith");
var withConsole = /* @__PURE__ */ dual(2, (effect4, value2) => fiberRefLocallyWith(effect4, currentServices, add2(consoleTag, value2)));
var withConsoleScoped = /* @__PURE__ */ __name((console4) => fiberRefLocallyScopedWith(currentServices, add2(consoleTag, console4)), "withConsoleScoped");
var setConsole = /* @__PURE__ */ __name((console4) => scopedDiscard(fiberRefLocallyScopedWith(currentServices, add2(consoleTag, console4))), "setConsole");
var assert = /* @__PURE__ */ __name((condition, ...args2) => consoleWith((_) => _.assert(condition, ...args2)), "assert");
var clear2 = /* @__PURE__ */ consoleWith((_) => _.clear);
var count = /* @__PURE__ */ __name((label) => consoleWith((_) => _.count(label)), "count");
var countReset = /* @__PURE__ */ __name((label) => consoleWith((_) => _.countReset(label)), "countReset");
var debug = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.debug(...args2)), "debug");
var dir = /* @__PURE__ */ __name((item, options) => consoleWith((_) => _.dir(item, options)), "dir");
var dirxml = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.dirxml(...args2)), "dirxml");
var error = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.error(...args2)), "error");
var group3 = /* @__PURE__ */ __name((options) => consoleWith((_) => acquireRelease2(_.group(options), () => _.groupEnd)), "group");
var info = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.info(...args2)), "info");
var log2 = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.log(...args2)), "log");
var table = /* @__PURE__ */ __name((tabularData, properties) => consoleWith((_) => _.table(tabularData, properties)), "table");
var time = /* @__PURE__ */ __name((label) => consoleWith((_) => acquireRelease2(_.time(label), () => _.timeEnd(label))), "time");
var timeLog = /* @__PURE__ */ __name((label, ...args2) => consoleWith((_) => _.timeLog(label, ...args2)), "timeLog");
var trace = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.trace(...args2)), "trace");
var warn = /* @__PURE__ */ __name((...args2) => consoleWith((_) => _.warn(...args2)), "warn");
var withGroup = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, options) => consoleWith((_) => acquireUseRelease(_.group(options), () => self, () => _.groupEnd)));
var withTime = /* @__PURE__ */ dual((args2) => isEffect(args2[0]), (self, label) => consoleWith((_) => acquireUseRelease(_.time(label), () => self, () => _.timeEnd(label))));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Data.js
var struct5 = struct4;
var unsafeStruct = /* @__PURE__ */ __name((as15) => Object.setPrototypeOf(as15, StructuralPrototype), "unsafeStruct");
var tuple4 = /* @__PURE__ */ __name((...as15) => unsafeArray(as15), "tuple");
var array4 = /* @__PURE__ */ __name((as15) => unsafeArray(as15.slice(0)), "array");
var unsafeArray = /* @__PURE__ */ __name((as15) => Object.setPrototypeOf(as15, ArrayProto), "unsafeArray");
var _case = /* @__PURE__ */ __name(() => (args2) => args2 === void 0 ? Object.create(StructuralPrototype) : struct5(args2), "_case");
var tagged4 = /* @__PURE__ */ __name((tag) => (args2) => {
  const value2 = args2 === void 0 ? Object.create(StructuralPrototype) : struct5(args2);
  value2._tag = tag;
  return value2;
}, "tagged");
var Class5 = Structural;
var TaggedClass2 = /* @__PURE__ */ __name((tag) => {
  class Base4 extends Class5 {
    static {
      __name(this, "Base");
    }
    _tag = tag;
  }
  return Base4;
}, "TaggedClass");
var Structural2 = Structural;
var taggedEnum = /* @__PURE__ */ __name(() => new Proxy({}, {
  get(_target, tag, _receiver) {
    if (tag === "$is") {
      return isTagged;
    } else if (tag === "$match") {
      return taggedMatch;
    }
    return tagged4(tag);
  }
}), "taggedEnum");
function taggedMatch() {
  if (arguments.length === 1) {
    const cases2 = arguments[0];
    return function(value3) {
      return cases2[value3._tag](value3);
    };
  }
  const value2 = arguments[0];
  const cases = arguments[1];
  return cases[value2._tag](value2);
}
__name(taggedMatch, "taggedMatch");
var Error4 = /* @__PURE__ */ (function() {
  const plainArgsSymbol = /* @__PURE__ */ Symbol.for("effect/Data/Error/plainArgs");
  const O = {
    BaseEffectError: class extends YieldableError {
      static {
        __name(this, "BaseEffectError");
      }
      constructor(args2) {
        super(args2?.message, args2?.cause ? {
          cause: args2.cause
        } : void 0);
        if (args2) {
          Object.assign(this, args2);
          Object.defineProperty(this, plainArgsSymbol, {
            value: args2,
            enumerable: false
          });
        }
      }
      toJSON() {
        return {
          ...this[plainArgsSymbol],
          ...this
        };
      }
    }
  };
  return O.BaseEffectError;
})();
var TaggedError2 = /* @__PURE__ */ __name((tag) => {
  const O = {
    BaseEffectError: class extends Error4 {
      static {
        __name(this, "BaseEffectError");
      }
      _tag = tag;
    }
  };
  O.BaseEffectError.prototype.name = tag;
  return O.BaseEffectError;
}, "TaggedError");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/dateTime.js
var TypeId23 = /* @__PURE__ */ Symbol.for("effect/DateTime");
var TimeZoneTypeId = /* @__PURE__ */ Symbol.for("effect/DateTime/TimeZone");
var Proto2 = {
  [TypeId23]: TypeId23,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [NodeInspectSymbol]() {
    return this.toString();
  },
  toJSON() {
    return toDateUtc(this).toJSON();
  }
};
var ProtoUtc = {
  ...Proto2,
  _tag: "Utc",
  [symbol2]() {
    return cached(this, number2(this.epochMillis));
  },
  [symbol3](that) {
    return isDateTime(that) && that._tag === "Utc" && this.epochMillis === that.epochMillis;
  },
  toString() {
    return `DateTime.Utc(${toDateUtc(this).toJSON()})`;
  }
};
var ProtoZoned = {
  ...Proto2,
  _tag: "Zoned",
  [symbol2]() {
    return pipe(number2(this.epochMillis), combine2(hash(this.zone)), cached(this));
  },
  [symbol3](that) {
    return isDateTime(that) && that._tag === "Zoned" && this.epochMillis === that.epochMillis && equals(this.zone, that.zone);
  },
  toString() {
    return `DateTime.Zoned(${formatIsoZoned(this)})`;
  }
};
var ProtoTimeZone = {
  [TimeZoneTypeId]: TimeZoneTypeId,
  [NodeInspectSymbol]() {
    return this.toString();
  }
};
var ProtoTimeZoneNamed = {
  ...ProtoTimeZone,
  _tag: "Named",
  [symbol2]() {
    return cached(this, string2(`Named:${this.id}`));
  },
  [symbol3](that) {
    return isTimeZone(that) && that._tag === "Named" && this.id === that.id;
  },
  toString() {
    return `TimeZone.Named(${this.id})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Named",
      id: this.id
    };
  }
};
var ProtoTimeZoneOffset = {
  ...ProtoTimeZone,
  _tag: "Offset",
  [symbol2]() {
    return cached(this, string2(`Offset:${this.offset}`));
  },
  [symbol3](that) {
    return isTimeZone(that) && that._tag === "Offset" && this.offset === that.offset;
  },
  toString() {
    return `TimeZone.Offset(${offsetToString(this.offset)})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Offset",
      offset: this.offset
    };
  }
};
var makeZonedProto = /* @__PURE__ */ __name((epochMillis, zone, partsUtc) => {
  const self = Object.create(ProtoZoned);
  self.epochMillis = epochMillis;
  self.zone = zone;
  Object.defineProperty(self, "partsUtc", {
    value: partsUtc,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "adjustedEpochMillis", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "partsAdjusted", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
}, "makeZonedProto");
var isDateTime = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId23), "isDateTime");
var isDateTimeArgs = /* @__PURE__ */ __name((args2) => isDateTime(args2[0]), "isDateTimeArgs");
var isTimeZone = /* @__PURE__ */ __name((u) => hasProperty(u, TimeZoneTypeId), "isTimeZone");
var isTimeZoneOffset = /* @__PURE__ */ __name((u) => isTimeZone(u) && u._tag === "Offset", "isTimeZoneOffset");
var isTimeZoneNamed = /* @__PURE__ */ __name((u) => isTimeZone(u) && u._tag === "Named", "isTimeZoneNamed");
var isUtc = /* @__PURE__ */ __name((self) => self._tag === "Utc", "isUtc");
var isZoned = /* @__PURE__ */ __name((self) => self._tag === "Zoned", "isZoned");
var Equivalence4 = /* @__PURE__ */ make((a, b) => a.epochMillis === b.epochMillis);
var Order7 = /* @__PURE__ */ make2((self, that) => self.epochMillis < that.epochMillis ? -1 : self.epochMillis > that.epochMillis ? 1 : 0);
var clamp5 = /* @__PURE__ */ clamp(Order7);
var makeUtc = /* @__PURE__ */ __name((epochMillis) => {
  const self = Object.create(ProtoUtc);
  self.epochMillis = epochMillis;
  Object.defineProperty(self, "partsUtc", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
}, "makeUtc");
var unsafeFromDate = /* @__PURE__ */ __name((date) => {
  const epochMillis = date.getTime();
  if (Number.isNaN(epochMillis)) {
    throw new IllegalArgumentException2("Invalid date");
  }
  return makeUtc(epochMillis);
}, "unsafeFromDate");
var unsafeMake11 = /* @__PURE__ */ __name((input) => {
  if (isDateTime(input)) {
    return input;
  } else if (input instanceof Date) {
    return unsafeFromDate(input);
  } else if (typeof input === "object") {
    const date = /* @__PURE__ */ new Date(0);
    setPartsDate(date, input);
    return unsafeFromDate(date);
  } else if (typeof input === "string" && !hasZone(input)) {
    return unsafeFromDate(/* @__PURE__ */ new Date(input + "Z"));
  }
  return unsafeFromDate(new Date(input));
}, "unsafeMake");
var hasZone = /* @__PURE__ */ __name((input) => /Z|[+-]\d{2}$|[+-]\d{2}:?\d{2}$|\]$/.test(input), "hasZone");
var minEpochMillis = -864e13 + 12 * 60 * 60 * 1e3;
var maxEpochMillis = 864e13 - 14 * 60 * 60 * 1e3;
var unsafeMakeZoned = /* @__PURE__ */ __name((input, options) => {
  if (options?.timeZone === void 0 && isDateTime(input) && isZoned(input)) {
    return input;
  }
  const self = unsafeMake11(input);
  if (self.epochMillis < minEpochMillis || self.epochMillis > maxEpochMillis) {
    throw new RangeError(`Epoch millis out of range: ${self.epochMillis}`);
  }
  let zone;
  if (options?.timeZone === void 0) {
    const offset = new Date(self.epochMillis).getTimezoneOffset() * -60 * 1e3;
    zone = zoneMakeOffset(offset);
  } else if (isTimeZone(options?.timeZone)) {
    zone = options.timeZone;
  } else if (typeof options?.timeZone === "number") {
    zone = zoneMakeOffset(options.timeZone);
  } else {
    const parsedZone = zoneFromString(options.timeZone);
    if (isNone2(parsedZone)) {
      throw new IllegalArgumentException2(`Invalid time zone: ${options.timeZone}`);
    }
    zone = parsedZone.value;
  }
  if (options?.adjustForTimeZone !== true) {
    return makeZonedProto(self.epochMillis, zone, self.partsUtc);
  }
  return makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible");
}, "unsafeMakeZoned");
var makeZoned = /* @__PURE__ */ liftThrowable(unsafeMakeZoned);
var make54 = /* @__PURE__ */ liftThrowable(unsafeMake11);
var zonedStringRegex = /^(.{17,35})\[(.+)\]$/;
var makeZonedFromString = /* @__PURE__ */ __name((input) => {
  const match33 = zonedStringRegex.exec(input);
  if (match33 === null) {
    const offset = parseOffset(input);
    return offset !== null ? makeZoned(input, {
      timeZone: offset
    }) : none2();
  }
  const [, isoString, timeZone] = match33;
  return makeZoned(isoString, {
    timeZone
  });
}, "makeZonedFromString");
var now = /* @__PURE__ */ map14(currentTimeMillis2, makeUtc);
var nowAsDate = /* @__PURE__ */ map14(currentTimeMillis2, (millis2) => new Date(millis2));
var unsafeNow = /* @__PURE__ */ __name(() => makeUtc(Date.now()), "unsafeNow");
var toUtc = /* @__PURE__ */ __name((self) => makeUtc(self.epochMillis), "toUtc");
var setZone = /* @__PURE__ */ dual(isDateTimeArgs, (self, zone, options) => options?.adjustForTimeZone === true ? makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible") : makeZonedProto(self.epochMillis, zone, self.partsUtc));
var setZoneOffset = /* @__PURE__ */ dual(isDateTimeArgs, (self, offset, options) => setZone(self, zoneMakeOffset(offset), options));
var validZoneCache = /* @__PURE__ */ globalValue("effect/DateTime/validZoneCache", () => /* @__PURE__ */ new Map());
var formatOptions = {
  day: "numeric",
  month: "numeric",
  year: "numeric",
  hour: "numeric",
  minute: "numeric",
  second: "numeric",
  timeZoneName: "longOffset",
  fractionalSecondDigits: 3,
  hourCycle: "h23"
};
var zoneMakeIntl = /* @__PURE__ */ __name((format5) => {
  const zoneId = format5.resolvedOptions().timeZone;
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  const zone = Object.create(ProtoTimeZoneNamed);
  zone.id = zoneId;
  zone.format = format5;
  validZoneCache.set(zoneId, zone);
  return zone;
}, "zoneMakeIntl");
var zoneUnsafeMakeNamed = /* @__PURE__ */ __name((zoneId) => {
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  try {
    return zoneMakeIntl(new Intl.DateTimeFormat("en-US", {
      ...formatOptions,
      timeZone: zoneId
    }));
  } catch {
    throw new IllegalArgumentException2(`Invalid time zone: ${zoneId}`);
  }
}, "zoneUnsafeMakeNamed");
var zoneMakeOffset = /* @__PURE__ */ __name((offset) => {
  const zone = Object.create(ProtoTimeZoneOffset);
  zone.offset = offset;
  return zone;
}, "zoneMakeOffset");
var zoneMakeNamed = /* @__PURE__ */ liftThrowable(zoneUnsafeMakeNamed);
var zoneMakeNamedEffect = /* @__PURE__ */ __name((zoneId) => try_2({
  try: /* @__PURE__ */ __name(() => zoneUnsafeMakeNamed(zoneId), "try"),
  catch: /* @__PURE__ */ __name((e) => e, "catch")
}), "zoneMakeNamedEffect");
var zoneMakeLocal = /* @__PURE__ */ __name(() => zoneMakeIntl(new Intl.DateTimeFormat("en-US", formatOptions)), "zoneMakeLocal");
var offsetZoneRegex = /^(?:GMT|[+-])/;
var zoneFromString = /* @__PURE__ */ __name((zone) => {
  if (offsetZoneRegex.test(zone)) {
    const offset = parseOffset(zone);
    return offset === null ? none2() : some3(zoneMakeOffset(offset));
  }
  return zoneMakeNamed(zone);
}, "zoneFromString");
var zoneToString = /* @__PURE__ */ __name((self) => {
  if (self._tag === "Offset") {
    return offsetToString(self.offset);
  }
  return self.id;
}, "zoneToString");
var setZoneNamed = /* @__PURE__ */ dual(isDateTimeArgs, (self, zoneId, options) => map2(zoneMakeNamed(zoneId), (zone) => setZone(self, zone, options)));
var unsafeSetZoneNamed = /* @__PURE__ */ dual(isDateTimeArgs, (self, zoneId, options) => setZone(self, zoneUnsafeMakeNamed(zoneId), options));
var distance = /* @__PURE__ */ dual(2, (self, other) => toEpochMillis(other) - toEpochMillis(self));
var distanceDurationEither = /* @__PURE__ */ dual(2, (self, other) => {
  const diffMillis = distance(self, other);
  return diffMillis > 0 ? right2(millis(diffMillis)) : left2(millis(-diffMillis));
});
var distanceDuration = /* @__PURE__ */ dual(2, (self, other) => millis(Math.abs(distance(self, other))));
var min7 = /* @__PURE__ */ min(Order7);
var max9 = /* @__PURE__ */ max(Order7);
var greaterThan7 = /* @__PURE__ */ greaterThan(Order7);
var greaterThanOrEqualTo4 = /* @__PURE__ */ greaterThanOrEqualTo(Order7);
var lessThan11 = /* @__PURE__ */ lessThan(Order7);
var lessThanOrEqualTo4 = /* @__PURE__ */ lessThanOrEqualTo(Order7);
var between4 = /* @__PURE__ */ between(Order7);
var isFuture = /* @__PURE__ */ __name((self) => map14(now, lessThan11(self)), "isFuture");
var unsafeIsFuture = /* @__PURE__ */ __name((self) => lessThan11(unsafeNow(), self), "unsafeIsFuture");
var isPast = /* @__PURE__ */ __name((self) => map14(now, greaterThan7(self)), "isPast");
var unsafeIsPast = /* @__PURE__ */ __name((self) => greaterThan7(unsafeNow(), self), "unsafeIsPast");
var toDateUtc = /* @__PURE__ */ __name((self) => new Date(self.epochMillis), "toDateUtc");
var toDate = /* @__PURE__ */ __name((self) => {
  if (self._tag === "Utc") {
    return new Date(self.epochMillis);
  } else if (self.zone._tag === "Offset") {
    return new Date(self.epochMillis + self.zone.offset);
  } else if (self.adjustedEpochMillis !== void 0) {
    return new Date(self.adjustedEpochMillis);
  }
  const parts2 = self.zone.format.formatToParts(self.epochMillis).filter((_) => _.type !== "literal");
  const date = /* @__PURE__ */ new Date(0);
  date.setUTCFullYear(Number(parts2[2].value), Number(parts2[0].value) - 1, Number(parts2[1].value));
  date.setUTCHours(Number(parts2[3].value), Number(parts2[4].value), Number(parts2[5].value), Number(parts2[6].value));
  self.adjustedEpochMillis = date.getTime();
  return date;
}, "toDate");
var zonedOffset = /* @__PURE__ */ __name((self) => {
  const date = toDate(self);
  return date.getTime() - toEpochMillis(self);
}, "zonedOffset");
var offsetToString = /* @__PURE__ */ __name((offset) => {
  const abs = Math.abs(offset);
  let hours2 = Math.floor(abs / (60 * 60 * 1e3));
  let minutes2 = Math.round(abs % (60 * 60 * 1e3) / (60 * 1e3));
  if (minutes2 === 60) {
    hours2 += 1;
    minutes2 = 0;
  }
  return `${offset < 0 ? "-" : "+"}${String(hours2).padStart(2, "0")}:${String(minutes2).padStart(2, "0")}`;
}, "offsetToString");
var zonedOffsetIso = /* @__PURE__ */ __name((self) => offsetToString(zonedOffset(self)), "zonedOffsetIso");
var toEpochMillis = /* @__PURE__ */ __name((self) => self.epochMillis, "toEpochMillis");
var removeTime = /* @__PURE__ */ __name((self) => withDate(self, (date) => {
  date.setUTCHours(0, 0, 0, 0);
  return makeUtc(date.getTime());
}), "removeTime");
var dateToParts = /* @__PURE__ */ __name((date) => ({
  millis: date.getUTCMilliseconds(),
  seconds: date.getUTCSeconds(),
  minutes: date.getUTCMinutes(),
  hours: date.getUTCHours(),
  day: date.getUTCDate(),
  weekDay: date.getUTCDay(),
  month: date.getUTCMonth() + 1,
  year: date.getUTCFullYear()
}), "dateToParts");
var toParts = /* @__PURE__ */ __name((self) => {
  if (self._tag === "Utc") {
    return toPartsUtc(self);
  } else if (self.partsAdjusted !== void 0) {
    return self.partsAdjusted;
  }
  self.partsAdjusted = withDate(self, dateToParts);
  return self.partsAdjusted;
}, "toParts");
var toPartsUtc = /* @__PURE__ */ __name((self) => {
  if (self.partsUtc !== void 0) {
    return self.partsUtc;
  }
  self.partsUtc = withDateUtc(self, dateToParts);
  return self.partsUtc;
}, "toPartsUtc");
var getPartUtc = /* @__PURE__ */ dual(2, (self, part) => toPartsUtc(self)[part]);
var getPart = /* @__PURE__ */ dual(2, (self, part) => toParts(self)[part]);
var setPartsDate = /* @__PURE__ */ __name((date, parts2) => {
  if (parts2.year !== void 0) {
    date.setUTCFullYear(parts2.year);
  }
  if (parts2.month !== void 0) {
    date.setUTCMonth(parts2.month - 1);
  }
  if (parts2.day !== void 0) {
    date.setUTCDate(parts2.day);
  }
  if (parts2.weekDay !== void 0) {
    const diff12 = parts2.weekDay - date.getUTCDay();
    date.setUTCDate(date.getUTCDate() + diff12);
  }
  if (parts2.hours !== void 0) {
    date.setUTCHours(parts2.hours);
  }
  if (parts2.minutes !== void 0) {
    date.setUTCMinutes(parts2.minutes);
  }
  if (parts2.seconds !== void 0) {
    date.setUTCSeconds(parts2.seconds);
  }
  if (parts2.millis !== void 0) {
    date.setUTCMilliseconds(parts2.millis);
  }
}, "setPartsDate");
var setParts = /* @__PURE__ */ dual(2, (self, parts2) => mutate5(self, (date) => setPartsDate(date, parts2)));
var setPartsUtc = /* @__PURE__ */ dual(2, (self, parts2) => mutateUtc(self, (date) => setPartsDate(date, parts2)));
var constDayMillis = 24 * 60 * 60 * 1e3;
var makeZonedFromAdjusted = /* @__PURE__ */ __name((adjustedMillis, zone, disambiguation) => {
  if (zone._tag === "Offset") {
    return makeZonedProto(adjustedMillis - zone.offset, zone);
  }
  const beforeOffset = calculateNamedOffset(adjustedMillis - constDayMillis, adjustedMillis, zone);
  const afterOffset = calculateNamedOffset(adjustedMillis + constDayMillis, adjustedMillis, zone);
  if (beforeOffset === afterOffset) {
    return makeZonedProto(adjustedMillis - beforeOffset, zone);
  }
  const isForwards = beforeOffset < afterOffset;
  const transitionMillis = beforeOffset - afterOffset;
  if (isForwards) {
    const currentAfterOffset = calculateNamedOffset(adjustedMillis - afterOffset, adjustedMillis, zone);
    if (currentAfterOffset === afterOffset) {
      return makeZonedProto(adjustedMillis - afterOffset, zone);
    }
    const before3 = makeZonedProto(adjustedMillis - beforeOffset, zone);
    const beforeAdjustedMillis = toDate(before3).getTime();
    if (adjustedMillis !== beforeAdjustedMillis) {
      switch (disambiguation) {
        case "reject": {
          const formatted = new Date(adjustedMillis).toISOString();
          throw new RangeError(`Gap time: ${formatted} does not exist in time zone ${zone.id}`);
        }
        case "earlier":
          return makeZonedProto(adjustedMillis - afterOffset, zone);
        case "compatible":
        case "later":
          return before3;
      }
    }
    return before3;
  }
  const currentBeforeOffset = calculateNamedOffset(adjustedMillis - beforeOffset, adjustedMillis, zone);
  if (currentBeforeOffset === beforeOffset) {
    if (disambiguation === "earlier" || disambiguation === "compatible") {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    const laterOffset = calculateNamedOffset(adjustedMillis - beforeOffset + transitionMillis, adjustedMillis + transitionMillis, zone);
    if (laterOffset === beforeOffset) {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    if (disambiguation === "reject") {
      const formatted = new Date(adjustedMillis).toISOString();
      throw new RangeError(`Ambiguous time: ${formatted} occurs twice in time zone ${zone.id}`);
    }
  }
  return makeZonedProto(adjustedMillis - afterOffset, zone);
}, "makeZonedFromAdjusted");
var offsetRegex = /([+-])(\d{2}):(\d{2})$/;
var parseOffset = /* @__PURE__ */ __name((offset) => {
  const match33 = offsetRegex.exec(offset);
  if (match33 === null) {
    return null;
  }
  const [, sign2, hours2, minutes2] = match33;
  return (sign2 === "+" ? 1 : -1) * (Number(hours2) * 60 + Number(minutes2)) * 60 * 1e3;
}, "parseOffset");
var calculateNamedOffset = /* @__PURE__ */ __name((utcMillis, adjustedMillis, zone) => {
  const offset = zone.format.formatToParts(utcMillis).find((_) => _.type === "timeZoneName")?.value ?? "";
  if (offset === "GMT") {
    return 0;
  }
  const result = parseOffset(offset);
  if (result === null) {
    return zonedOffset(makeZonedProto(adjustedMillis, zone));
  }
  return result;
}, "calculateNamedOffset");
var mutate5 = /* @__PURE__ */ dual(isDateTimeArgs, (self, f, options) => {
  if (self._tag === "Utc") {
    const date = toDateUtc(self);
    f(date);
    return makeUtc(date.getTime());
  }
  const adjustedDate = toDate(self);
  const newAdjustedDate = new Date(adjustedDate.getTime());
  f(newAdjustedDate);
  return makeZonedFromAdjusted(newAdjustedDate.getTime(), self.zone, options?.disambiguation ?? "compatible");
});
var mutateUtc = /* @__PURE__ */ dual(2, (self, f) => mapEpochMillis(self, (millis2) => {
  const date = new Date(millis2);
  f(date);
  return date.getTime();
}));
var mapEpochMillis = /* @__PURE__ */ dual(2, (self, f) => {
  const millis2 = f(toEpochMillis(self));
  return self._tag === "Utc" ? makeUtc(millis2) : makeZonedProto(millis2, self.zone);
});
var withDate = /* @__PURE__ */ dual(2, (self, f) => f(toDate(self)));
var withDateUtc = /* @__PURE__ */ dual(2, (self, f) => f(toDateUtc(self)));
var match17 = /* @__PURE__ */ dual(2, (self, options) => self._tag === "Utc" ? options.onUtc(self) : options.onZoned(self));
var addDuration = /* @__PURE__ */ dual(2, (self, duration3) => mapEpochMillis(self, (millis2) => millis2 + toMillis(duration3)));
var subtractDuration = /* @__PURE__ */ dual(2, (self, duration3) => mapEpochMillis(self, (millis2) => millis2 - toMillis(duration3)));
var addMillis = /* @__PURE__ */ __name((date, amount) => {
  date.setTime(date.getTime() + amount);
}, "addMillis");
var add6 = /* @__PURE__ */ dual(2, (self, parts2) => mutate5(self, (date) => {
  if (parts2.millis) {
    addMillis(date, parts2.millis);
  }
  if (parts2.seconds) {
    addMillis(date, parts2.seconds * 1e3);
  }
  if (parts2.minutes) {
    addMillis(date, parts2.minutes * 60 * 1e3);
  }
  if (parts2.hours) {
    addMillis(date, parts2.hours * 60 * 60 * 1e3);
  }
  if (parts2.days) {
    date.setUTCDate(date.getUTCDate() + parts2.days);
  }
  if (parts2.weeks) {
    date.setUTCDate(date.getUTCDate() + parts2.weeks * 7);
  }
  if (parts2.months) {
    const day = date.getUTCDate();
    date.setUTCMonth(date.getUTCMonth() + parts2.months + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
  if (parts2.years) {
    const day = date.getUTCDate();
    const month = date.getUTCMonth();
    date.setUTCFullYear(date.getUTCFullYear() + parts2.years, month + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
}));
var subtract3 = /* @__PURE__ */ dual(2, (self, parts2) => {
  const newParts = {};
  for (const key in parts2) {
    newParts[key] = -1 * parts2[key];
  }
  return add6(self, newParts);
});
var startOfDate = /* @__PURE__ */ __name((date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(0);
      break;
    }
    case "minute": {
      date.setUTCSeconds(0, 0);
      break;
    }
    case "hour": {
      date.setUTCMinutes(0, 0, 0);
      break;
    }
    case "day": {
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff12 = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff12);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "month": {
      date.setUTCDate(1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "year": {
      date.setUTCMonth(0, 1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
  }
}, "startOfDate");
var startOf = /* @__PURE__ */ dual(isDateTimeArgs, (self, part, options) => mutate5(self, (date) => startOfDate(date, part, options)));
var endOfDate = /* @__PURE__ */ __name((date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(999);
      break;
    }
    case "minute": {
      date.setUTCSeconds(59, 999);
      break;
    }
    case "hour": {
      date.setUTCMinutes(59, 59, 999);
      break;
    }
    case "day": {
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff12 = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff12 + 6);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "month": {
      date.setUTCMonth(date.getUTCMonth() + 1, 0);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "year": {
      date.setUTCMonth(11, 31);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
  }
}, "endOfDate");
var endOf = /* @__PURE__ */ dual(isDateTimeArgs, (self, part, options) => mutate5(self, (date) => endOfDate(date, part, options)));
var nearest = /* @__PURE__ */ dual(isDateTimeArgs, (self, part, options) => mutate5(self, (date) => {
  if (part === "milli") return;
  const millis2 = date.getTime();
  const start3 = new Date(millis2);
  startOfDate(start3, part, options);
  const startMillis = start3.getTime();
  const end6 = new Date(millis2);
  endOfDate(end6, part, options);
  const endMillis = end6.getTime() + 1;
  const diffStart = millis2 - startMillis;
  const diffEnd = endMillis - millis2;
  if (diffStart < diffEnd) {
    date.setTime(startMillis);
  } else {
    date.setTime(endMillis);
  }
}));
var intlTimeZone = /* @__PURE__ */ __name((self) => {
  if (self._tag === "Named") {
    return self.id;
  }
  return offsetToString(self.offset);
}, "intlTimeZone");
var format4 = /* @__PURE__ */ dual(isDateTimeArgs, (self, options) => {
  try {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: self._tag === "Utc" ? "UTC" : intlTimeZone(self.zone),
      ...options
    }).format(self.epochMillis);
  } catch {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: "UTC",
      ...options
    }).format(toDate(self));
  }
});
var formatLocal = /* @__PURE__ */ dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, options).format(self.epochMillis));
var formatUtc = /* @__PURE__ */ dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, {
  ...options,
  timeZone: "UTC"
}).format(self.epochMillis));
var formatIntl = /* @__PURE__ */ dual(2, (self, format5) => format5.format(self.epochMillis));
var formatIso2 = /* @__PURE__ */ __name((self) => toDateUtc(self).toISOString(), "formatIso");
var formatIsoDate = /* @__PURE__ */ __name((self) => toDate(self).toISOString().slice(0, 10), "formatIsoDate");
var formatIsoDateUtc = /* @__PURE__ */ __name((self) => toDateUtc(self).toISOString().slice(0, 10), "formatIsoDateUtc");
var formatIsoOffset = /* @__PURE__ */ __name((self) => {
  const date = toDate(self);
  return self._tag === "Utc" ? date.toISOString() : `${date.toISOString().slice(0, -1)}${zonedOffsetIso(self)}`;
}, "formatIsoOffset");
var formatIsoZoned = /* @__PURE__ */ __name((self) => self.zone._tag === "Offset" ? formatIsoOffset(self) : `${formatIsoOffset(self)}[${self.zone.id}]`, "formatIsoZoned");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/String.js
var isString2 = isString;
var Equivalence5 = string;
var Order8 = string3;
var empty41 = "";
var concat2 = /* @__PURE__ */ dual(2, (self, that) => self + that);
var toUpperCase = /* @__PURE__ */ __name((self) => self.toUpperCase(), "toUpperCase");
var toLowerCase = /* @__PURE__ */ __name((self) => self.toLowerCase(), "toLowerCase");
var capitalize = /* @__PURE__ */ __name((self) => {
  if (self.length === 0) return self;
  return toUpperCase(self[0]) + self.slice(1);
}, "capitalize");
var uncapitalize = /* @__PURE__ */ __name((self) => {
  if (self.length === 0) return self;
  return toLowerCase(self[0]) + self.slice(1);
}, "uncapitalize");
var replace5 = /* @__PURE__ */ __name((searchValue, replaceValue) => (self) => self.replace(searchValue, replaceValue), "replace");
var trim = /* @__PURE__ */ __name((self) => self.trim(), "trim");
var trimStart = /* @__PURE__ */ __name((self) => self.trimStart(), "trimStart");
var trimEnd = /* @__PURE__ */ __name((self) => self.trimEnd(), "trimEnd");
var slice = /* @__PURE__ */ __name((start3, end6) => (self) => self.slice(start3, end6), "slice");
var isEmpty14 = /* @__PURE__ */ __name((self) => self.length === 0, "isEmpty");
var isNonEmpty6 = /* @__PURE__ */ __name((self) => self.length > 0, "isNonEmpty");
var length4 = /* @__PURE__ */ __name((self) => self.length, "length");
var split3 = /* @__PURE__ */ dual(2, (self, separator) => {
  const out = self.split(separator);
  return isNonEmptyArray(out) ? out : [self];
});
var includes2 = /* @__PURE__ */ __name((searchString, position) => (self) => self.includes(searchString, position), "includes");
var startsWith = /* @__PURE__ */ __name((searchString, position) => (self) => self.startsWith(searchString, position), "startsWith");
var endsWith = /* @__PURE__ */ __name((searchString, position) => (self) => self.endsWith(searchString, position), "endsWith");
var charCodeAt = /* @__PURE__ */ dual(2, (self, index) => filter(some3(self.charCodeAt(index)), (charCode) => !isNaN(charCode)));
var substring = /* @__PURE__ */ __name((start3, end6) => (self) => self.substring(start3, end6), "substring");
var at4 = /* @__PURE__ */ dual(2, (self, index) => fromNullable2(self.at(index)));
var charAt = /* @__PURE__ */ dual(2, (self, index) => filter(some3(self.charAt(index)), isNonEmpty6));
var codePointAt = /* @__PURE__ */ dual(2, (self, index) => fromNullable2(self.codePointAt(index)));
var indexOf = /* @__PURE__ */ __name((searchString) => (self) => filter(some3(self.indexOf(searchString)), greaterThanOrEqualTo2(0)), "indexOf");
var lastIndexOf = /* @__PURE__ */ __name((searchString) => (self) => filter(some3(self.lastIndexOf(searchString)), greaterThanOrEqualTo2(0)), "lastIndexOf");
var localeCompare = /* @__PURE__ */ __name((that, locales, options) => (self) => sign(self.localeCompare(that, locales, options)), "localeCompare");
var match18 = /* @__PURE__ */ __name((regexp) => (self) => fromNullable2(self.match(regexp)), "match");
var matchAll = /* @__PURE__ */ __name((regexp) => (self) => self.matchAll(regexp), "matchAll");
var normalize = /* @__PURE__ */ __name((form) => (self) => self.normalize(form), "normalize");
var padEnd = /* @__PURE__ */ __name((maxLength, fillString) => (self) => self.padEnd(maxLength, fillString), "padEnd");
var padStart = /* @__PURE__ */ __name((maxLength, fillString) => (self) => self.padStart(maxLength, fillString), "padStart");
var repeat2 = /* @__PURE__ */ __name((count5) => (self) => self.repeat(count5), "repeat");
var replaceAll = /* @__PURE__ */ __name((searchValue, replaceValue) => (self) => self.replaceAll(searchValue, replaceValue), "replaceAll");
var search = /* @__PURE__ */ dual(2, (self, regexp) => filter(some3(self.search(regexp)), greaterThanOrEqualTo2(0)));
var toLocaleLowerCase = /* @__PURE__ */ __name((locale) => (self) => self.toLocaleLowerCase(locale), "toLocaleLowerCase");
var toLocaleUpperCase = /* @__PURE__ */ __name((locale) => (self) => self.toLocaleUpperCase(locale), "toLocaleUpperCase");
var takeLeft = /* @__PURE__ */ dual(2, (self, n) => self.slice(0, Math.max(n, 0)));
var takeRight3 = /* @__PURE__ */ dual(2, (self, n) => self.slice(Math.max(0, self.length - Math.floor(n)), Infinity));
var CR = 13;
var LF = 10;
var linesIterator = /* @__PURE__ */ __name((self) => linesSeparated(self, true), "linesIterator");
var linesWithSeparators = /* @__PURE__ */ __name((s) => linesSeparated(s, false), "linesWithSeparators");
var stripMarginWith = /* @__PURE__ */ dual(2, (self, marginChar) => {
  let out = "";
  for (const line of linesWithSeparators(self)) {
    let index = 0;
    while (index < line.length && line.charAt(index) <= " ") {
      index = index + 1;
    }
    const stripped = index < line.length && line.charAt(index) === marginChar ? line.substring(index + 1) : line;
    out = out + stripped;
  }
  return out;
});
var stripMargin = /* @__PURE__ */ __name((self) => stripMarginWith(self, "|"), "stripMargin");
var snakeToCamel = /* @__PURE__ */ __name((self) => {
  let str = self[0];
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
}, "snakeToCamel");
var snakeToPascal = /* @__PURE__ */ __name((self) => {
  let str = self[0].toUpperCase();
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
}, "snakeToPascal");
var snakeToKebab = /* @__PURE__ */ __name((self) => self.replace(/_/g, "-"), "snakeToKebab");
var camelToSnake = /* @__PURE__ */ __name((self) => self.replace(/([A-Z])/g, "_$1").toLowerCase(), "camelToSnake");
var pascalToSnake = /* @__PURE__ */ __name((self) => (self.slice(0, 1) + self.slice(1).replace(/([A-Z])/g, "_$1")).toLowerCase(), "pascalToSnake");
var kebabToSnake = /* @__PURE__ */ __name((self) => self.replace(/-/g, "_"), "kebabToSnake");
var LinesIterator = class _LinesIterator {
  static {
    __name(this, "LinesIterator");
  }
  s;
  stripped;
  index;
  length;
  constructor(s, stripped = false) {
    this.s = s;
    this.stripped = stripped;
    this.index = 0;
    this.length = s.length;
  }
  next() {
    if (this.done) {
      return {
        done: true,
        value: void 0
      };
    }
    const start3 = this.index;
    while (!this.done && !isLineBreak(this.s[this.index])) {
      this.index = this.index + 1;
    }
    let end6 = this.index;
    if (!this.done) {
      const char = this.s[this.index];
      this.index = this.index + 1;
      if (!this.done && isLineBreak2(char, this.s[this.index])) {
        this.index = this.index + 1;
      }
      if (!this.stripped) {
        end6 = this.index;
      }
    }
    return {
      done: false,
      value: this.s.substring(start3, end6)
    };
  }
  [Symbol.iterator]() {
    return new _LinesIterator(this.s, this.stripped);
  }
  get done() {
    return this.index >= this.length;
  }
};
var isLineBreak = /* @__PURE__ */ __name((char) => {
  const code = char.charCodeAt(0);
  return code === CR || code === LF;
}, "isLineBreak");
var isLineBreak2 = /* @__PURE__ */ __name((char0, char1) => char0.charCodeAt(0) === CR && char1.charCodeAt(0) === LF, "isLineBreak2");
var linesSeparated = /* @__PURE__ */ __name((self, stripped) => new LinesIterator(self, stripped), "linesSeparated");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Cron.js
var TypeId24 = /* @__PURE__ */ Symbol.for("effect/Cron");
var CronProto = {
  [TypeId24]: TypeId24,
  [symbol3](that) {
    return isCron(that) && equals3(this, that);
  },
  [symbol2]() {
    return pipe(hash(this.tz), combine2(array2(fromIterable2(this.seconds))), combine2(array2(fromIterable2(this.minutes))), combine2(array2(fromIterable2(this.hours))), combine2(array2(fromIterable2(this.days))), combine2(array2(fromIterable2(this.months))), combine2(array2(fromIterable2(this.weekdays))), cached(this));
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Cron",
      tz: this.tz,
      seconds: fromIterable2(this.seconds),
      minutes: fromIterable2(this.minutes),
      hours: fromIterable2(this.hours),
      days: fromIterable2(this.days),
      months: fromIterable2(this.months),
      weekdays: fromIterable2(this.weekdays)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isCron = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId24), "isCron");
var make55 = /* @__PURE__ */ __name((values10) => {
  const o = Object.create(CronProto);
  o.seconds = new Set(sort(values10.seconds ?? [0], Order));
  o.minutes = new Set(sort(values10.minutes, Order));
  o.hours = new Set(sort(values10.hours, Order));
  o.days = new Set(sort(values10.days, Order));
  o.months = new Set(sort(values10.months, Order));
  o.weekdays = new Set(sort(values10.weekdays, Order));
  o.tz = fromNullable2(values10.tz);
  const seconds2 = Array.from(o.seconds);
  const minutes2 = Array.from(o.minutes);
  const hours2 = Array.from(o.hours);
  const days2 = Array.from(o.days);
  const months = Array.from(o.months);
  const weekdays = Array.from(o.weekdays);
  o.first = {
    second: seconds2[0] ?? 0,
    minute: minutes2[0] ?? 0,
    hour: hours2[0] ?? 0,
    day: days2[0] ?? 1,
    month: (months[0] ?? 1) - 1,
    weekday: weekdays[0] ?? 0
  };
  o.next = {
    second: nextLookupTable(seconds2, 60),
    minute: nextLookupTable(minutes2, 60),
    hour: nextLookupTable(hours2, 24),
    day: nextLookupTable(days2, 32),
    month: nextLookupTable(months, 13),
    weekday: nextLookupTable(weekdays, 7)
  };
  return o;
}, "make");
var nextLookupTable = /* @__PURE__ */ __name((values10, size25) => {
  const result = new Array(size25).fill(void 0);
  if (values10.length === 0) {
    return result;
  }
  let current2 = void 0;
  let index = values10.length - 1;
  for (let i = size25 - 1; i >= 0; i--) {
    while (index >= 0 && values10[index] >= i) {
      current2 = values10[index--];
    }
    result[i] = current2;
  }
  return result;
}, "nextLookupTable");
var ParseErrorTypeId = /* @__PURE__ */ Symbol.for("effect/Cron/errors/ParseError");
var ParseError = class extends (/* @__PURE__ */ TaggedError2("CronParseError")) {
  static {
    __name(this, "ParseError");
  }
  /**
   * @since 2.0.0
   */
  [ParseErrorTypeId] = ParseErrorTypeId;
};
var isParseError = /* @__PURE__ */ __name((u) => hasProperty(u, ParseErrorTypeId), "isParseError");
var parse2 = /* @__PURE__ */ __name((cron3, tz) => {
  const segments = cron3.split(" ").filter(isNonEmpty6);
  if (segments.length !== 5 && segments.length !== 6) {
    return left2(new ParseError({
      message: `Invalid number of segments in cron expression`,
      input: cron3
    }));
  }
  if (segments.length === 5) {
    segments.unshift("0");
  }
  const [seconds2, minutes2, hours2, days2, months, weekdays] = segments;
  const zone = tz === void 0 || isTimeZone(tz) ? right2(tz) : fromOption2(zoneFromString(tz), () => new ParseError({
    message: `Invalid time zone in cron expression`,
    input: tz
  }));
  return all3({
    tz: zone,
    seconds: parseSegment(seconds2, secondOptions),
    minutes: parseSegment(minutes2, minuteOptions),
    hours: parseSegment(hours2, hourOptions),
    days: parseSegment(days2, dayOptions),
    months: parseSegment(months, monthOptions),
    weekdays: parseSegment(weekdays, weekdayOptions)
  }).pipe(map(make55));
}, "parse");
var unsafeParse = /* @__PURE__ */ __name((cron3, tz) => getOrThrowWith(parse2(cron3, tz), identity), "unsafeParse");
var match19 = /* @__PURE__ */ __name((cron3, date) => {
  const parts2 = unsafeMakeZoned(date, {
    timeZone: getOrUndefined2(cron3.tz)
  }).pipe(toParts);
  if (cron3.seconds.size !== 0 && !cron3.seconds.has(parts2.seconds)) {
    return false;
  }
  if (cron3.minutes.size !== 0 && !cron3.minutes.has(parts2.minutes)) {
    return false;
  }
  if (cron3.hours.size !== 0 && !cron3.hours.has(parts2.hours)) {
    return false;
  }
  if (cron3.months.size !== 0 && !cron3.months.has(parts2.month)) {
    return false;
  }
  if (cron3.days.size === 0 && cron3.weekdays.size === 0) {
    return true;
  }
  if (cron3.weekdays.size === 0) {
    return cron3.days.has(parts2.day);
  }
  if (cron3.days.size === 0) {
    return cron3.weekdays.has(parts2.weekDay);
  }
  return cron3.days.has(parts2.day) || cron3.weekdays.has(parts2.weekDay);
}, "match");
var daysInMonth = /* @__PURE__ */ __name((date) => new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth() + 1, 0)).getUTCDate(), "daysInMonth");
var next2 = /* @__PURE__ */ __name((cron3, startFrom) => {
  const tz = getOrUndefined2(cron3.tz);
  const zoned = unsafeMakeZoned(startFrom ?? /* @__PURE__ */ new Date(), {
    timeZone: tz
  });
  const utc = tz !== void 0 && isTimeZoneNamed(tz) && tz.id === "UTC";
  const adjustDst = utc ? constVoid : (current2) => {
    const adjusted = unsafeMakeZoned(current2, {
      timeZone: zoned.zone,
      adjustForTimeZone: true
    }).pipe(toDate);
    const drift = current2.getTime() - adjusted.getTime();
    if (drift > 0) {
      current2.setTime(current2.getTime() + drift);
    }
  };
  const result = mutate5(zoned, (current2) => {
    current2.setUTCSeconds(current2.getUTCSeconds() + 1, 0);
    for (let i = 0; i < 1e4; i++) {
      if (cron3.seconds.size !== 0) {
        const currentSecond = current2.getUTCSeconds();
        const nextSecond2 = cron3.next.second[currentSecond];
        if (nextSecond2 === void 0) {
          current2.setUTCMinutes(current2.getUTCMinutes() + 1, cron3.first.second);
          adjustDst(current2);
          continue;
        }
        if (nextSecond2 > currentSecond) {
          current2.setUTCSeconds(nextSecond2);
          adjustDst(current2);
          continue;
        }
      }
      if (cron3.minutes.size !== 0) {
        const currentMinute = current2.getUTCMinutes();
        const nextMinute2 = cron3.next.minute[currentMinute];
        if (nextMinute2 === void 0) {
          current2.setUTCHours(current2.getUTCHours() + 1, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
        if (nextMinute2 > currentMinute) {
          current2.setUTCMinutes(nextMinute2, cron3.first.second);
          adjustDst(current2);
          continue;
        }
      }
      if (cron3.hours.size !== 0) {
        const currentHour = current2.getUTCHours();
        const nextHour2 = cron3.next.hour[currentHour];
        if (nextHour2 === void 0) {
          current2.setUTCDate(current2.getUTCDate() + 1);
          current2.setUTCHours(cron3.first.hour, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
        if (nextHour2 > currentHour) {
          current2.setUTCHours(nextHour2, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
      }
      if (cron3.weekdays.size !== 0 || cron3.days.size !== 0) {
        let a = Infinity;
        let b = Infinity;
        if (cron3.weekdays.size !== 0) {
          const currentWeekday = current2.getUTCDay();
          const nextWeekday = cron3.next.weekday[currentWeekday];
          a = nextWeekday === void 0 ? 7 - currentWeekday + cron3.first.weekday : nextWeekday - currentWeekday;
        }
        if (cron3.days.size !== 0 && a !== 0) {
          const currentDay = current2.getUTCDate();
          const nextDay2 = cron3.next.day[currentDay];
          b = nextDay2 === void 0 ? daysInMonth(current2) - currentDay + cron3.first.day : nextDay2 - currentDay;
        }
        const addDays = Math.min(a, b);
        if (addDays !== 0) {
          current2.setUTCDate(current2.getUTCDate() + addDays);
          current2.setUTCHours(cron3.first.hour, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
      }
      if (cron3.months.size !== 0) {
        const currentMonth = current2.getUTCMonth() + 1;
        const nextMonth = cron3.next.month[currentMonth];
        if (nextMonth === void 0) {
          current2.setUTCFullYear(current2.getUTCFullYear() + 1);
          current2.setUTCMonth(cron3.first.month, cron3.first.day);
          current2.setUTCHours(cron3.first.hour, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
        if (nextMonth > currentMonth) {
          current2.setUTCMonth(nextMonth - 1, cron3.first.day);
          current2.setUTCHours(cron3.first.hour, cron3.first.minute, cron3.first.second);
          adjustDst(current2);
          continue;
        }
      }
      return;
    }
    throw new Error("Unable to find next cron date");
  });
  return toDateUtc(result);
}, "next");
var sequence = /* @__PURE__ */ __name(function* (cron3, startFrom) {
  while (true) {
    yield startFrom = next2(cron3, startFrom);
  }
}, "sequence");
var Equivalence6 = /* @__PURE__ */ make((self, that) => restrictionsEquals(self.seconds, that.seconds) && restrictionsEquals(self.minutes, that.minutes) && restrictionsEquals(self.hours, that.hours) && restrictionsEquals(self.days, that.days) && restrictionsEquals(self.months, that.months) && restrictionsEquals(self.weekdays, that.weekdays));
var restrictionsArrayEquals = /* @__PURE__ */ array(number);
var restrictionsEquals = /* @__PURE__ */ __name((self, that) => restrictionsArrayEquals(fromIterable2(self), fromIterable2(that)), "restrictionsEquals");
var equals3 = /* @__PURE__ */ dual(2, (self, that) => Equivalence6(self, that));
var secondOptions = {
  min: 0,
  max: 59
};
var minuteOptions = {
  min: 0,
  max: 59
};
var hourOptions = {
  min: 0,
  max: 23
};
var dayOptions = {
  min: 1,
  max: 31
};
var monthOptions = {
  min: 1,
  max: 12,
  aliases: {
    jan: 1,
    feb: 2,
    mar: 3,
    apr: 4,
    may: 5,
    jun: 6,
    jul: 7,
    aug: 8,
    sep: 9,
    oct: 10,
    nov: 11,
    dec: 12
  }
};
var weekdayOptions = {
  min: 0,
  max: 6,
  aliases: {
    sun: 0,
    mon: 1,
    tue: 2,
    wed: 3,
    thu: 4,
    fri: 5,
    sat: 6
  }
};
var parseSegment = /* @__PURE__ */ __name((input, options) => {
  const capacity10 = options.max - options.min + 1;
  const values10 = /* @__PURE__ */ new Set();
  const fields = input.split(",");
  for (const field of fields) {
    const [raw, step4] = splitStep(field);
    if (raw === "*" && step4 === void 0) {
      return right2(/* @__PURE__ */ new Set());
    }
    if (step4 !== void 0) {
      if (!Number.isInteger(step4)) {
        return left2(new ParseError({
          message: `Expected step value to be a positive integer`,
          input
        }));
      }
      if (step4 < 1) {
        return left2(new ParseError({
          message: `Expected step value to be greater than 0`,
          input
        }));
      }
      if (step4 > options.max) {
        return left2(new ParseError({
          message: `Expected step value to be less than ${options.max}`,
          input
        }));
      }
    }
    if (raw === "*") {
      for (let i = options.min; i <= options.max; i += step4 ?? 1) {
        values10.add(i);
      }
    } else {
      const [left3, right3] = splitRange(raw, options.aliases);
      if (!Number.isInteger(left3)) {
        return left2(new ParseError({
          message: `Expected a positive integer`,
          input
        }));
      }
      if (left3 < options.min || left3 > options.max) {
        return left2(new ParseError({
          message: `Expected a value between ${options.min} and ${options.max}`,
          input
        }));
      }
      if (right3 === void 0) {
        values10.add(left3);
      } else {
        if (!Number.isInteger(right3)) {
          return left2(new ParseError({
            message: `Expected a positive integer`,
            input
          }));
        }
        if (right3 < options.min || right3 > options.max) {
          return left2(new ParseError({
            message: `Expected a value between ${options.min} and ${options.max}`,
            input
          }));
        }
        if (left3 > right3) {
          return left2(new ParseError({
            message: `Invalid value range`,
            input
          }));
        }
        for (let i = left3; i <= right3; i += step4 ?? 1) {
          values10.add(i);
        }
      }
    }
    if (values10.size >= capacity10) {
      return right2(/* @__PURE__ */ new Set());
    }
  }
  return right2(values10);
}, "parseSegment");
var splitStep = /* @__PURE__ */ __name((input) => {
  const seperator = input.indexOf("/");
  if (seperator !== -1) {
    return [input.slice(0, seperator), Number(input.slice(seperator + 1))];
  }
  return [input, void 0];
}, "splitStep");
var splitRange = /* @__PURE__ */ __name((input, aliases) => {
  const seperator = input.indexOf("-");
  if (seperator !== -1) {
    return [aliasOrValue(input.slice(0, seperator), aliases), aliasOrValue(input.slice(seperator + 1), aliases)];
  }
  return [aliasOrValue(input, aliases), void 0];
}, "splitRange");
function aliasOrValue(field, aliases) {
  return aliases?.[field.toLocaleLowerCase()] ?? Number(field);
}
__name(aliasOrValue, "aliasOrValue");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Random.js
var RandomTypeId2 = RandomTypeId;
var next3 = next;
var nextInt2 = nextInt;
var nextBoolean2 = nextBoolean;
var nextRange2 = nextRange;
var nextIntBetween2 = nextIntBetween;
var shuffle2 = shuffle;
var choice2 = choice;
var randomWith2 = randomWith;
var Random = randomTag;
var make56 = make28;
var fixed2 = fixed;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/schedule.js
var ScheduleSymbolKey = "effect/Schedule";
var ScheduleTypeId = /* @__PURE__ */ Symbol.for(ScheduleSymbolKey);
var isSchedule = /* @__PURE__ */ __name((u) => hasProperty(u, ScheduleTypeId), "isSchedule");
var ScheduleDriverSymbolKey = "effect/ScheduleDriver";
var ScheduleDriverTypeId = /* @__PURE__ */ Symbol.for(ScheduleDriverSymbolKey);
var defaultIterationMetadata = {
  start: 0,
  now: 0,
  input: void 0,
  output: void 0,
  elapsed: zero,
  elapsedSincePrevious: zero,
  recurrence: 0
};
var CurrentIterationMetadata = /* @__PURE__ */ Reference2()("effect/Schedule/CurrentIterationMetadata", {
  defaultValue: /* @__PURE__ */ __name(() => defaultIterationMetadata, "defaultValue")
});
var scheduleVariance = {
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out"),
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var scheduleDriverVariance = {
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out"),
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var ScheduleImpl = class {
  static {
    __name(this, "ScheduleImpl");
  }
  initial;
  step;
  [ScheduleTypeId] = scheduleVariance;
  constructor(initial, step4) {
    this.initial = initial;
    this.step = step4;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var updateInfo = /* @__PURE__ */ __name((iterationMetaRef, now2, input, output) => update3(iterationMetaRef, (prev) => prev.recurrence === 0 ? {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: zero,
  elapsedSincePrevious: zero,
  start: now2
} : {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: millis(now2 - prev.start),
  elapsedSincePrevious: millis(now2 - prev.now),
  start: prev.start
}), "updateInfo");
var ScheduleDriverImpl = class {
  static {
    __name(this, "ScheduleDriverImpl");
  }
  schedule;
  ref;
  [ScheduleDriverTypeId] = scheduleDriverVariance;
  constructor(schedule4, ref) {
    this.schedule = schedule4;
    this.ref = ref;
  }
  get state() {
    return map14(get12(this.ref), (tuple5) => tuple5[1]);
  }
  get last() {
    return flatMap12(get12(this.ref), ([element, _]) => {
      switch (element._tag) {
        case "None": {
          return failSync(() => new NoSuchElementException());
        }
        case "Some": {
          return succeed(element.value);
        }
      }
    });
  }
  iterationMeta = /* @__PURE__ */ unsafeMake7(defaultIterationMetadata);
  get reset() {
    return set6(this.ref, [none2(), this.schedule.initial]).pipe(zipLeft2(set6(this.iterationMeta, defaultIterationMetadata)));
  }
  next(input) {
    return pipe(map14(get12(this.ref), (tuple5) => tuple5[1]), flatMap12((state) => pipe(currentTimeMillis2, flatMap12((now2) => pipe(suspend(() => this.schedule.step(now2, input, state)), flatMap12(([state2, out, decision]) => {
      const setState = set6(this.ref, [some3(out), state2]);
      if (isDone5(decision)) {
        return setState.pipe(zipRight2(fail2(none2())));
      }
      const millis2 = start2(decision.intervals) - now2;
      if (millis2 <= 0) {
        return setState.pipe(zipRight2(updateInfo(this.iterationMeta, now2, input, out)), as3(out));
      }
      const duration3 = millis(millis2);
      return pipe(setState, zipRight2(updateInfo(this.iterationMeta, now2, input, out)), zipRight2(sleep3(duration3)), as3(out));
    }))))));
  }
};
var makeWithState = /* @__PURE__ */ __name((initial, step4) => new ScheduleImpl(initial, step4), "makeWithState");
var addDelay = /* @__PURE__ */ dual(2, (self, f) => addDelayEffect(self, (out) => sync(() => f(out))));
var addDelayEffect = /* @__PURE__ */ dual(2, (self, f) => modifyDelayEffect(self, (out, duration3) => map14(f(out), (delay4) => sum2(duration3, decode(delay4)))));
var andThen10 = /* @__PURE__ */ dual(2, (self, that) => map25(andThenEither(self, that), merge));
var andThenEither = /* @__PURE__ */ dual(2, (self, that) => makeWithState([self.initial, that.initial, true], (now2, input, state) => state[2] ? flatMap12(self.step(now2, input, state[0]), ([lState, out, decision]) => {
  if (isDone5(decision)) {
    return map14(that.step(now2, input, state[1]), ([rState, out2, decision2]) => [[lState, rState, false], right2(out2), decision2]);
  }
  return succeed([[lState, state[1], true], left2(out), decision]);
}) : map14(that.step(now2, input, state[1]), ([rState, out, decision]) => [[state[0], rState, false], right2(out), decision])));
var as7 = /* @__PURE__ */ dual(2, (self, out) => map25(self, () => out));
var asVoid5 = /* @__PURE__ */ __name((self) => map25(self, constVoid), "asVoid");
var bothInOut = /* @__PURE__ */ dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, [in1, in2], state) => zipWith6(self.step(now2, in1, state[0]), that.step(now2, in2, state[1]), ([lState, out, lDecision], [rState, out2, rDecision]) => {
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    const interval = pipe(lDecision.intervals, union12(rDecision.intervals));
    return [[lState, rState], [out, out2], _continue2(interval)];
  }
  return [[lState, rState], [out, out2], done7];
})));
var check = /* @__PURE__ */ dual(2, (self, test) => checkEffect(self, (input, out) => sync(() => test(input, out))));
var checkEffect = /* @__PURE__ */ dual(2, (self, test) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone5(decision)) {
    return succeed([state2, out, done7]);
  }
  return map14(test(input, out), (cont) => cont ? [state2, out, decision] : [state2, out, done7]);
})));
var collectAllInputs = /* @__PURE__ */ __name(() => collectAllOutputs(identity2()), "collectAllInputs");
var collectAllOutputs = /* @__PURE__ */ __name((self) => reduce16(self, empty7(), (outs, out) => pipe(outs, append3(out))), "collectAllOutputs");
var collectUntil = /* @__PURE__ */ __name((f) => collectAllOutputs(recurUntil(f)), "collectUntil");
var collectUntilEffect = /* @__PURE__ */ __name((f) => collectAllOutputs(recurUntilEffect(f)), "collectUntilEffect");
var collectWhile = /* @__PURE__ */ __name((f) => collectAllOutputs(recurWhile(f)), "collectWhile");
var collectWhileEffect = /* @__PURE__ */ __name((f) => collectAllOutputs(recurWhileEffect(f)), "collectWhileEffect");
var compose3 = /* @__PURE__ */ dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, input, state) => flatMap12(self.step(now2, input, state[0]), ([lState, out, lDecision]) => map14(that.step(now2, out, state[1]), ([rState, out2, rDecision]) => isDone5(lDecision) ? [[lState, rState], out2, done7] : isDone5(rDecision) ? [[lState, rState], out2, done7] : [[lState, rState], out2, _continue2(pipe(lDecision.intervals, max8(rDecision.intervals)))]))));
var mapInput6 = /* @__PURE__ */ dual(2, (self, f) => mapInputEffect(self, (input2) => sync(() => f(input2))));
var mapInputContext2 = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => mapInputContext(self.step(now2, input, state), f)));
var mapInputEffect = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input2, state) => flatMap12(f(input2), (input) => self.step(now2, input, state))));
var cron = /* @__PURE__ */ __name((expression, tz) => {
  const parsed = isCron(expression) ? right2(expression) : parse2(expression, tz);
  return makeWithState([true, [Number.MIN_SAFE_INTEGER, 0, 0]], (now2, _, [initial, previous2]) => {
    if (now2 < previous2[0]) {
      return succeed([[false, previous2], [previous2[1], previous2[2]], continueWith2(make49(previous2[1], previous2[2]))]);
    }
    if (isLeft2(parsed)) {
      return die2(parsed.left);
    }
    const cron3 = parsed.right;
    const date = new Date(now2);
    let next4;
    if (initial && match19(cron3, date)) {
      next4 = now2;
    }
    next4 = next2(cron3, date).getTime();
    const start3 = beginningOfSecond(next4);
    const end6 = endOfSecond(next4);
    return succeed([[false, [next4, start3, end6]], [start3, end6], continueWith2(make49(start3, end6))]);
  });
}, "cron");
var dayOfMonth = /* @__PURE__ */ __name((day) => {
  return makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 31 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfMonth(${day}). Must be in range 1...31`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDayOfMonth(now2, day, initial);
    const start3 = beginningOfDay(day0);
    const end6 = endOfDay(day0);
    const interval = make49(start3, end6);
    return succeed([[end6, n + 1], n, continueWith2(interval)]);
  });
}, "dayOfMonth");
var dayOfWeek = /* @__PURE__ */ __name((day) => {
  return makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 7 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfWeek(${day}). Must be in range 1 (Monday)...7 (Sunday)`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDay(now2, day, initial);
    const start3 = beginningOfDay(day0);
    const end6 = endOfDay(day0);
    const interval = make49(start3, end6);
    return succeed([[end6, n + 1], n, continueWith2(interval)]);
  });
}, "dayOfWeek");
var delayed = /* @__PURE__ */ dual(2, (self, f) => delayedEffect(self, (duration3) => sync(() => f(duration3))));
var delayedEffect = /* @__PURE__ */ dual(2, (self, f) => modifyDelayEffect(self, (_, delay4) => f(delay4)));
var delayedSchedule = /* @__PURE__ */ __name((schedule4) => addDelay(schedule4, (x) => x), "delayedSchedule");
var delays = /* @__PURE__ */ __name((self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), flatMap12(([state2, _, decision]) => {
  if (isDone5(decision)) {
    return succeed([state2, zero, decision]);
  }
  return succeed([state2, millis(start2(decision.intervals) - now2), decision]);
}))), "delays");
var mapBoth5 = /* @__PURE__ */ dual(2, (self, {
  onInput,
  onOutput
}) => map25(mapInput6(self, onInput), onOutput));
var mapBothEffect = /* @__PURE__ */ dual(2, (self, {
  onInput,
  onOutput
}) => mapEffect4(mapInputEffect(self, onInput), onOutput));
var driver = /* @__PURE__ */ __name((self) => pipe(make34([none2(), self.initial]), map14((ref) => new ScheduleDriverImpl(self, ref))), "driver");
var duration = /* @__PURE__ */ __name((durationInput) => {
  const duration3 = decode(durationInput);
  const durationMillis = toMillis(duration3);
  return makeWithState(true, (now2, _, state) => succeed(state ? [false, duration3, continueWith2(after2(now2 + durationMillis))] : [false, zero, done7]));
}, "duration");
var either5 = /* @__PURE__ */ dual(2, (self, that) => union13(self, that));
var eitherWith = /* @__PURE__ */ dual(3, (self, that, f) => unionWith2(self, that, f));
var ensuring3 = /* @__PURE__ */ dual(2, (self, finalizer3) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => isDone5(decision) ? as3(finalizer3, [state2, out, decision]) : succeed([state2, out, decision]))));
var exponential2 = /* @__PURE__ */ __name((baseInput, factor = 2) => {
  const base = decode(baseInput);
  return delayedSchedule(map25(forever3, (i) => times(base, Math.pow(factor, i))));
}, "exponential");
var fibonacci = /* @__PURE__ */ __name((oneInput) => {
  const one = decode(oneInput);
  return delayedSchedule(pipe(unfold3([one, one], ([a, b]) => [b, sum2(a, b)]), map25((out) => out[0])));
}, "fibonacci");
var fixed3 = /* @__PURE__ */ __name((intervalInput) => {
  const interval = decode(intervalInput);
  const intervalMillis = toMillis(interval);
  return makeWithState([none2(), 0], (now2, _, [option5, n]) => sync(() => {
    switch (option5._tag) {
      case "None": {
        return [[some3([now2, now2 + intervalMillis]), n + 1], n, continueWith2(after2(now2 + intervalMillis))];
      }
      case "Some": {
        const [startMillis, lastRun] = option5.value;
        const runningBehind = now2 > lastRun + intervalMillis;
        const boundary = equals(interval, zero) ? interval : millis(intervalMillis - (now2 - startMillis) % intervalMillis);
        const sleepTime = equals(boundary, zero) ? interval : boundary;
        const nextRun = runningBehind ? now2 : now2 + toMillis(sleepTime);
        return [[some3([startMillis, nextRun]), n + 1], n, continueWith2(after2(nextRun))];
      }
    }
  }));
}, "fixed");
var fromDelay = /* @__PURE__ */ __name((delay4) => duration(delay4), "fromDelay");
var fromDelays = /* @__PURE__ */ __name((delay4, ...delays3) => makeWithState([[delay4, ...delays3].map((_) => decode(_)), true], (now2, _, [durations, cont]) => sync(() => {
  if (cont) {
    const x = durations[0];
    const interval = after2(now2 + toMillis(x));
    if (durations.length >= 2) {
      return [[durations.slice(1), true], x, continueWith2(interval)];
    }
    const y = durations.slice(1);
    return [[[x, ...y], false], x, continueWith2(interval)];
  }
  return [[durations, false], zero, done7];
})), "fromDelays");
var fromFunction2 = /* @__PURE__ */ __name((f) => map25(identity2(), f), "fromFunction");
var hourOfDay = /* @__PURE__ */ __name((hour) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(hour) || hour < 0 || 23 < hour) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: hourOfDay(${hour}). Must be in range 0...23`));
  }
  const n = state[1];
  const initial = n === 0;
  const hour0 = nextHour(now2, hour, initial);
  const start3 = beginningOfHour(hour0);
  const end6 = endOfHour(hour0);
  const interval = make49(start3, end6);
  return succeed([[end6, n + 1], n, continueWith2(interval)]);
}), "hourOfDay");
var identity2 = /* @__PURE__ */ __name(() => makeWithState(void 0, (now2, input, state) => succeed([state, input, continueWith2(after2(now2))])), "identity");
var intersect5 = /* @__PURE__ */ dual(2, (self, that) => intersectWith(self, that, intersect4));
var intersectWith = /* @__PURE__ */ dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => pipe(zipWith6(self.step(now2, input, state[0]), that.step(now2, input, state[1]), (a, b) => [a, b]), flatMap12(([[lState, out, lDecision], [rState, out2, rDecision]]) => {
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    return intersectWithLoop(self, that, input, lState, out, lDecision.intervals, rState, out2, rDecision.intervals, f);
  }
  return succeed([[lState, rState], [out, out2], done7]);
}))));
var intersectWithLoop = /* @__PURE__ */ __name((self, that, input, lState, out, lInterval, rState, out2, rInterval, f) => {
  const combined = f(lInterval, rInterval);
  if (isNonEmpty5(combined)) {
    return succeed([[lState, rState], [out, out2], _continue2(combined)]);
  }
  if (pipe(lInterval, lessThan10(rInterval))) {
    return flatMap12(self.step(end2(lInterval), input, lState), ([lState2, out3, decision]) => {
      if (isDone5(decision)) {
        return succeed([[lState2, rState], [out3, out2], done7]);
      }
      return intersectWithLoop(self, that, input, lState2, out3, decision.intervals, rState, out2, rInterval, f);
    });
  }
  return flatMap12(that.step(end2(rInterval), input, rState), ([rState2, out22, decision]) => {
    if (isDone5(decision)) {
      return succeed([[lState, rState2], [out, out22], done7]);
    }
    return intersectWithLoop(self, that, input, lState, out, lInterval, rState2, out22, decision.intervals, f);
  });
}, "intersectWithLoop");
var jittered = /* @__PURE__ */ __name((self) => jitteredWith(self, {
  min: 0.8,
  max: 1.2
}), "jittered");
var jitteredWith = /* @__PURE__ */ dual(2, (self, options) => {
  const {
    max: max10,
    min: min8
  } = Object.assign({
    min: 0.8,
    max: 1.2
  }, options);
  return delayedEffect(self, (duration3) => map14(next3, (random4) => {
    const d = toMillis(duration3);
    const jittered3 = d * min8 * (1 - random4) + d * max10 * random4;
    return millis(jittered3);
  }));
});
var linear2 = /* @__PURE__ */ __name((baseInput) => {
  const base = decode(baseInput);
  return delayedSchedule(map25(forever3, (i) => times(base, i + 1)));
}, "linear");
var map25 = /* @__PURE__ */ dual(2, (self, f) => mapEffect4(self, (out) => sync(() => f(out))));
var mapEffect4 = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => map14(f(out), (out2) => [state2, out2, decision]))));
var minuteOfHour = /* @__PURE__ */ __name((minute) => makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
  if (!Number.isInteger(minute) || minute < 0 || 59 < minute) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: minuteOfHour(${minute}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const minute0 = nextMinute(now2, minute, initial);
  const start3 = beginningOfMinute(minute0);
  const end6 = endOfMinute(minute0);
  const interval = make49(start3, end6);
  return succeed([[end6, n + 1], n, continueWith2(interval)]);
}), "minuteOfHour");
var modifyDelay = /* @__PURE__ */ dual(2, (self, f) => modifyDelayEffect(self, (out, duration3) => sync(() => f(out, duration3))));
var modifyDelayEffect = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone5(decision)) {
    return succeed([state2, out, decision]);
  }
  const intervals = decision.intervals;
  const delay4 = size16(make49(now2, start2(intervals)));
  return map14(f(out, delay4), (durationInput) => {
    const duration3 = decode(durationInput);
    const oldStart = start2(intervals);
    const newStart = now2 + toMillis(duration3);
    const delta = newStart - oldStart;
    const newEnd = Math.max(0, end2(intervals) + delta);
    const newInterval = make49(newStart, newEnd);
    return [state2, out, continueWith2(newInterval)];
  });
})));
var onDecision = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => as3(f(out, decision), [state2, out, decision]))));
var passthrough2 = /* @__PURE__ */ __name((self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), map14(([state2, _, decision]) => [state2, input, decision]))), "passthrough");
var provideContext3 = /* @__PURE__ */ dual(2, (self, context11) => makeWithState(self.initial, (now2, input, state) => provideContext(self.step(now2, input, state), context11)));
var provideService4 = /* @__PURE__ */ dual(3, (self, tag, service6) => makeWithState(self.initial, (now2, input, state) => contextWithEffect((env) => provideContext(
  // @ts-expect-error
  self.step(now2, input, state),
  add2(env, tag, service6)
))));
var recurUntil = /* @__PURE__ */ __name((f) => untilInput(identity2(), f), "recurUntil");
var recurUntilEffect = /* @__PURE__ */ __name((f) => untilInputEffect(identity2(), f), "recurUntilEffect");
var recurUntilOption = /* @__PURE__ */ __name((pf) => untilOutput(map25(identity2(), pf), isSome2), "recurUntilOption");
var recurUpTo = /* @__PURE__ */ __name((durationInput) => {
  const duration3 = decode(durationInput);
  return whileOutput(elapsed, (elapsed3) => lessThan3(elapsed3, duration3));
}, "recurUpTo");
var recurWhile = /* @__PURE__ */ __name((f) => whileInput(identity2(), f), "recurWhile");
var recurWhileEffect = /* @__PURE__ */ __name((f) => whileInputEffect(identity2(), f), "recurWhileEffect");
var recurs = /* @__PURE__ */ __name((n) => whileOutput(forever3, (out) => out < n), "recurs");
var reduce16 = /* @__PURE__ */ dual(3, (self, zero2, f) => reduceEffect2(self, zero2, (z, out) => sync(() => f(z, out))));
var reduceEffect2 = /* @__PURE__ */ dual(3, (self, zero2, f) => makeWithState([self.initial, zero2], (now2, input, [s, z]) => flatMap12(self.step(now2, input, s), ([s2, out, decision]) => isDone5(decision) ? succeed([[s2, z], z, decision]) : map14(f(z, out), (z2) => [[s2, z2], z, decision]))));
var repeatForever = /* @__PURE__ */ __name((self) => makeWithState(self.initial, (now2, input, state) => {
  const step4 = /* @__PURE__ */ __name((now3, input2, state2) => flatMap12(self.step(now3, input2, state2), ([state3, out, decision]) => isDone5(decision) ? step4(now3, input2, self.initial) : succeed([state3, out, decision])), "step");
  return step4(now2, input, state);
}), "repeatForever");
var repetitions = /* @__PURE__ */ __name((self) => reduce16(self, 0, (n, _) => n + 1), "repetitions");
var resetAfter = /* @__PURE__ */ dual(2, (self, durationInput) => {
  const duration3 = decode(durationInput);
  return pipe(self, intersect5(elapsed), resetWhen(([, time2]) => greaterThanOrEqualTo3(time2, duration3)), map25((out) => out[0]));
});
var resetWhen = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap12(self.step(now2, input, state), ([state2, out, decision]) => f(out) ? self.step(now2, input, self.initial) : succeed([state2, out, decision]))));
var run = /* @__PURE__ */ dual(3, (self, now2, input) => pipe(runLoop(self, now2, fromIterable3(input), self.initial, empty7()), map14((list) => reverse3(list))));
var runLoop = /* @__PURE__ */ __name((self, now2, inputs, state, acc) => {
  if (!isNonEmpty(inputs)) {
    return succeed(acc);
  }
  const input = headNonEmpty2(inputs);
  const nextInputs = tailNonEmpty2(inputs);
  return flatMap12(self.step(now2, input, state), ([state2, out, decision]) => {
    if (isDone5(decision)) {
      return sync(() => pipe(acc, prepend3(out)));
    }
    return runLoop(self, start2(decision.intervals), nextInputs, state2, prepend3(acc, out));
  });
}, "runLoop");
var secondOfMinute = /* @__PURE__ */ __name((second) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(second) || second < 0 || 59 < second) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: secondOfMinute(${second}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const second0 = nextSecond(now2, second, initial);
  const start3 = beginningOfSecond(second0);
  const end6 = endOfSecond(second0);
  const interval = make49(start3, end6);
  return succeed([[end6, n + 1], n, continueWith2(interval)]);
}), "secondOfMinute");
var spaced = /* @__PURE__ */ __name((duration3) => addDelay(forever3, () => duration3), "spaced");
var succeed11 = /* @__PURE__ */ __name((value2) => map25(forever3, () => value2), "succeed");
var sync7 = /* @__PURE__ */ __name((evaluate3) => map25(forever3, evaluate3), "sync");
var tapInput = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => zipRight2(f(input), self.step(now2, input, state))));
var tapOutput = /* @__PURE__ */ dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => tap2(self.step(now2, input, state), ([, out]) => f(out))));
var unfold3 = /* @__PURE__ */ __name((initial, f) => makeWithState(initial, (now2, _, state) => sync(() => [f(state), state, continueWith2(after2(now2))])), "unfold");
var union13 = /* @__PURE__ */ dual(2, (self, that) => unionWith2(self, that, union12));
var unionWith2 = /* @__PURE__ */ dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => zipWith6(self.step(now2, input, state[0]), that.step(now2, input, state[1]), ([lState, l, lDecision], [rState, r, rDecision]) => {
  if (isDone5(lDecision) && isDone5(rDecision)) {
    return [[lState, rState], [l, r], done7];
  }
  if (isDone5(lDecision) && isContinue2(rDecision)) {
    return [[lState, rState], [l, r], _continue2(rDecision.intervals)];
  }
  if (isContinue2(lDecision) && isDone5(rDecision)) {
    return [[lState, rState], [l, r], _continue2(lDecision.intervals)];
  }
  if (isContinue2(lDecision) && isContinue2(rDecision)) {
    const combined = f(lDecision.intervals, rDecision.intervals);
    return [[lState, rState], [l, r], _continue2(combined)];
  }
  throw new Error("BUG: Schedule.unionWith - please report an issue at https://github.com/Effect-TS/effect/issues");
})));
var untilInput = /* @__PURE__ */ dual(2, (self, f) => check(self, (input, _) => !f(input)));
var untilInputEffect = /* @__PURE__ */ dual(2, (self, f) => checkEffect(self, (input, _) => negate2(f(input))));
var untilOutput = /* @__PURE__ */ dual(2, (self, f) => check(self, (_, out) => !f(out)));
var untilOutputEffect = /* @__PURE__ */ dual(2, (self, f) => checkEffect(self, (_, out) => negate2(f(out))));
var upTo = /* @__PURE__ */ dual(2, (self, duration3) => zipLeft6(self, recurUpTo(duration3)));
var whileInput = /* @__PURE__ */ dual(2, (self, f) => check(self, (input, _) => f(input)));
var whileInputEffect = /* @__PURE__ */ dual(2, (self, f) => checkEffect(self, (input, _) => f(input)));
var whileOutput = /* @__PURE__ */ dual(2, (self, f) => check(self, (_, out) => f(out)));
var whileOutputEffect = /* @__PURE__ */ dual(2, (self, f) => checkEffect(self, (_, out) => f(out)));
var windowed = /* @__PURE__ */ __name((intervalInput) => {
  const interval = decode(intervalInput);
  const millis2 = toMillis(interval);
  return makeWithState([none2(), 0], (now2, _, [option5, n]) => {
    switch (option5._tag) {
      case "None": {
        return succeed([[some3(now2), n + 1], n, continueWith2(after2(now2 + millis2))]);
      }
      case "Some": {
        return succeed([[some3(option5.value), n + 1], n, continueWith2(after2(now2 + (millis2 - (now2 - option5.value) % millis2)))]);
      }
    }
  });
}, "windowed");
var zipLeft6 = /* @__PURE__ */ dual(2, (self, that) => map25(intersect5(self, that), (out) => out[0]));
var zipRight6 = /* @__PURE__ */ dual(2, (self, that) => map25(intersect5(self, that), (out) => out[1]));
var zipWith11 = /* @__PURE__ */ dual(3, (self, that, f) => map25(intersect5(self, that), ([out, out2]) => f(out, out2)));
var beginningOfSecond = /* @__PURE__ */ __name((now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds(), 0).getTime();
}, "beginningOfSecond");
var endOfSecond = /* @__PURE__ */ __name((now2) => {
  const date = new Date(beginningOfSecond(now2));
  return date.setSeconds(date.getSeconds() + 1);
}, "endOfSecond");
var nextSecond = /* @__PURE__ */ __name((now2, second, initial) => {
  const date = new Date(now2);
  if (date.getSeconds() === second && initial) {
    return now2;
  }
  if (date.getSeconds() < second) {
    return date.setSeconds(second);
  }
  const newDate = new Date(date.setSeconds(second));
  return newDate.setTime(newDate.getTime() + 1e3 * 60);
}, "nextSecond");
var beginningOfMinute = /* @__PURE__ */ __name((now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), 0, 0).getTime();
}, "beginningOfMinute");
var endOfMinute = /* @__PURE__ */ __name((now2) => {
  const date = new Date(beginningOfMinute(now2));
  return date.setMinutes(date.getMinutes() + 1);
}, "endOfMinute");
var nextMinute = /* @__PURE__ */ __name((now2, minute, initial) => {
  const date = new Date(now2);
  if (date.getMinutes() === minute && initial) {
    return now2;
  }
  if (date.getMinutes() < minute) {
    return date.setMinutes(minute);
  }
  const newDate = new Date(date.setMinutes(minute));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60);
}, "nextMinute");
var beginningOfHour = /* @__PURE__ */ __name((now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), 0, 0, 0).getTime();
}, "beginningOfHour");
var endOfHour = /* @__PURE__ */ __name((now2) => {
  const date = new Date(beginningOfHour(now2));
  return date.setHours(date.getHours() + 1);
}, "endOfHour");
var nextHour = /* @__PURE__ */ __name((now2, hour, initial) => {
  const date = new Date(now2);
  if (date.getHours() === hour && initial) {
    return now2;
  }
  if (date.getHours() < hour) {
    return date.setHours(hour);
  }
  const newDate = new Date(date.setHours(hour));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60 * 24);
}, "nextHour");
var beginningOfDay = /* @__PURE__ */ __name((now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), 0, 0, 0, 0).getTime();
}, "beginningOfDay");
var endOfDay = /* @__PURE__ */ __name((now2) => {
  const date = new Date(beginningOfDay(now2));
  return date.setDate(date.getDate() + 1);
}, "endOfDay");
var nextDay = /* @__PURE__ */ __name((now2, dayOfWeek3, initial) => {
  const date = new Date(now2);
  if (date.getDay() === dayOfWeek3 && initial) {
    return now2;
  }
  const nextDayOfWeek = (7 + dayOfWeek3 - date.getDay()) % 7;
  return date.setDate(date.getDate() + (nextDayOfWeek === 0 ? 7 : nextDayOfWeek));
}, "nextDay");
var nextDayOfMonth = /* @__PURE__ */ __name((now2, day, initial) => {
  const date = new Date(now2);
  if (date.getDate() === day && initial) {
    return now2;
  }
  if (date.getDate() < day) {
    return date.setDate(day);
  }
  return findNextMonth(now2, day, 1);
}, "nextDayOfMonth");
var findNextMonth = /* @__PURE__ */ __name((now2, day, months) => {
  const d = new Date(now2);
  const tmp1 = new Date(d.setDate(day));
  const tmp2 = new Date(tmp1.setMonth(tmp1.getMonth() + months));
  if (tmp2.getDate() === day) {
    const d2 = new Date(now2);
    const tmp3 = new Date(d2.setDate(day));
    return tmp3.setMonth(tmp3.getMonth() + months);
  }
  return findNextMonth(now2, day, months + 1);
}, "findNextMonth");
var ScheduleDefectTypeId = /* @__PURE__ */ Symbol.for("effect/Schedule/ScheduleDefect");
var ScheduleDefect = class {
  static {
    __name(this, "ScheduleDefect");
  }
  error;
  [ScheduleDefectTypeId];
  constructor(error2) {
    this.error = error2;
    this[ScheduleDefectTypeId] = ScheduleDefectTypeId;
  }
};
var isScheduleDefect = /* @__PURE__ */ __name((u) => hasProperty(u, ScheduleDefectTypeId), "isScheduleDefect");
var scheduleDefectWrap = /* @__PURE__ */ __name((self) => catchAll(self, (e) => die2(new ScheduleDefect(e))), "scheduleDefectWrap");
var scheduleDefectRefailCause = /* @__PURE__ */ __name((cause3) => match2(find(cause3, (_) => isDieType(_) && isScheduleDefect(_.defect) ? some3(_.defect) : none2()), {
  onNone: /* @__PURE__ */ __name(() => cause3, "onNone"),
  onSome: /* @__PURE__ */ __name((error2) => fail(error2.error), "onSome")
}), "scheduleDefectRefailCause");
var scheduleDefectRefail = /* @__PURE__ */ __name((effect4) => catchAllCause(effect4, (cause3) => failCause(scheduleDefectRefailCause(cause3))), "scheduleDefectRefail");
var repeat_Effect = /* @__PURE__ */ dual(2, (self, schedule4) => repeatOrElse_Effect(self, schedule4, (e, _) => fail2(e)));
var repeat_combined = /* @__PURE__ */ dual(2, (self, options) => {
  if (isSchedule(options)) {
    return repeat_Effect(self, options);
  }
  const base = options.schedule ?? passthrough2(forever3);
  const withWhile = options.while ? whileInputEffect(base, (a) => {
    const applied = options.while(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (a) => {
    const applied = options.until(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  const withTimes = options.times ? intersect5(withUntil, recurs(options.times)).pipe(map25((intersectionPair) => intersectionPair[0])) : withUntil;
  return scheduleDefectRefail(repeat_Effect(self, withTimes));
});
var repeatOrElse_Effect = /* @__PURE__ */ dual(3, (self, schedule4, orElse15) => flatMap12(driver(schedule4), (driver3) => matchEffect(self, {
  onFailure: /* @__PURE__ */ __name((error2) => orElse15(error2, none2()), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((value2) => repeatOrElseEffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get12(driver3.iterationMeta)), driver3, (error2, option5) => provideServiceEffect(orElse15(error2, option5), CurrentIterationMetadata, get12(driver3.iterationMeta)), value2), "onSuccess")
})));
var repeatOrElseEffectLoop = /* @__PURE__ */ __name((self, driver3, orElse15, value2) => matchEffect(driver3.next(value2), {
  onFailure: /* @__PURE__ */ __name(() => orDie(driver3.last), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((b) => matchEffect(self, {
    onFailure: /* @__PURE__ */ __name((error2) => orElse15(error2, some3(b)), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((value3) => repeatOrElseEffectLoop(self, driver3, orElse15, value3), "onSuccess")
  }), "onSuccess")
}), "repeatOrElseEffectLoop");
var retry_Effect = /* @__PURE__ */ dual(2, (self, policy) => retryOrElse_Effect(self, policy, (e, _) => fail2(e)));
var retry_combined = /* @__PURE__ */ dual(2, (self, options) => {
  if (isSchedule(options)) {
    return retry_Effect(self, options);
  }
  return scheduleDefectRefail(retry_Effect(self, fromRetryOptions(options)));
});
var fromRetryOptions = /* @__PURE__ */ __name((options) => {
  const base = options.schedule ?? forever3;
  const withWhile = options.while ? whileInputEffect(base, (e) => {
    const applied = options.while(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (e) => {
    const applied = options.until(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  return options.times !== void 0 ? intersect5(withUntil, recurs(options.times)) : withUntil;
}, "fromRetryOptions");
var retryOrElse_Effect = /* @__PURE__ */ dual(3, (self, policy, orElse15) => flatMap12(driver(policy), (driver3) => retryOrElse_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get12(driver3.iterationMeta)), driver3, (e, out) => provideServiceEffect(orElse15(e, out), CurrentIterationMetadata, get12(driver3.iterationMeta)))));
var retryOrElse_EffectLoop = /* @__PURE__ */ __name((self, driver3, orElse15) => {
  return catchAll(self, (e) => matchEffect(driver3.next(e), {
    onFailure: /* @__PURE__ */ __name(() => pipe(driver3.last, orDie, flatMap12((out) => orElse15(e, out))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => retryOrElse_EffectLoop(self, driver3, orElse15), "onSuccess")
  }));
}, "retryOrElse_EffectLoop");
var schedule_Effect = /* @__PURE__ */ dual(2, (self, schedule4) => scheduleFrom_Effect(self, void 0, schedule4));
var scheduleFrom_Effect = /* @__PURE__ */ dual(3, (self, initial, schedule4) => flatMap12(driver(schedule4), (driver3) => scheduleFrom_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get12(driver3.iterationMeta)), initial, driver3)));
var scheduleFrom_EffectLoop = /* @__PURE__ */ __name((self, initial, driver3) => matchEffect(driver3.next(initial), {
  onFailure: /* @__PURE__ */ __name(() => orDie(driver3.last), "onFailure"),
  onSuccess: /* @__PURE__ */ __name(() => flatMap12(self, (a) => scheduleFrom_EffectLoop(self, a, driver3)), "onSuccess")
}), "scheduleFrom_EffectLoop");
var count2 = /* @__PURE__ */ unfold3(0, (n) => n + 1);
var elapsed = /* @__PURE__ */ makeWithState(/* @__PURE__ */ none2(), (now2, _, state) => {
  switch (state._tag) {
    case "None": {
      return succeed([some3(now2), zero, continueWith2(after2(now2))]);
    }
    case "Some": {
      return succeed([some3(state.value), millis(now2 - state.value), continueWith2(after2(now2))]);
    }
  }
});
var forever3 = /* @__PURE__ */ unfold3(0, (n) => n + 1);
var once3 = /* @__PURE__ */ asVoid5(/* @__PURE__ */ recurs(1));
var stop = /* @__PURE__ */ asVoid5(/* @__PURE__ */ recurs(0));
var scheduleForked = /* @__PURE__ */ dual(2, (self, schedule4) => forkScoped2(schedule_Effect(self, schedule4)));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/executionPlan.js
var TypeId25 = /* @__PURE__ */ Symbol.for("effect/ExecutionPlan");
var isExecutionPlan = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId25), "isExecutionPlan");
var withExecutionPlan = /* @__PURE__ */ dual(2, (effect4, plan) => suspend(() => {
  let i = 0;
  let result;
  return flatMap12(whileLoop({
    while: /* @__PURE__ */ __name(() => i < plan.steps.length && (result === void 0 || isLeft2(result)), "while"),
    body: /* @__PURE__ */ __name(() => {
      const step4 = plan.steps[i];
      let nextEffect = effect_provide(effect4, step4.provide);
      if (result) {
        let attempted = false;
        const wrapped = nextEffect;
        nextEffect = suspend(() => {
          if (attempted) return wrapped;
          attempted = true;
          return result;
        });
        nextEffect = scheduleDefectRefail(retry_Effect(nextEffect, scheduleFromStep(step4, false)));
      } else {
        const schedule4 = scheduleFromStep(step4, true);
        nextEffect = schedule4 ? scheduleDefectRefail(retry_Effect(nextEffect, schedule4)) : nextEffect;
      }
      return either3(nextEffect);
    }, "body"),
    step: /* @__PURE__ */ __name((either11) => {
      result = either11;
      i++;
    }, "step")
  }), () => result);
}));
var scheduleFromStep = /* @__PURE__ */ __name((step4, first4) => {
  if (!first4) {
    return fromRetryOptions({
      schedule: step4.schedule ? step4.schedule : step4.attempts ? void 0 : once3,
      times: step4.attempts,
      while: step4.while
    });
  } else if (step4.attempts === 1 || !(step4.schedule || step4.attempts)) {
    return void 0;
  }
  return fromRetryOptions({
    schedule: step4.schedule,
    while: step4.while,
    times: step4.attempts ? step4.attempts - 1 : void 0
  });
}, "scheduleFromStep");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/query.js
var currentCache = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentCache"), () => fiberRefUnsafeMake(unsafeMakeWith(65536, () => map14(deferredMake(), (handle) => ({
  listeners: new Listeners(),
  handle
})), () => seconds(60))));
var currentCacheEnabled = /* @__PURE__ */ globalValue(/* @__PURE__ */ Symbol.for("effect/FiberRef/currentCacheEnabled"), () => fiberRefUnsafeMake(false));
var fromRequest = /* @__PURE__ */ __name((request2, dataSource) => flatMap12(isEffect(dataSource) ? dataSource : succeed(dataSource), (ds) => fiberIdWith((id3) => {
  const proxy = new Proxy(request2, {});
  return fiberRefGetWith(currentCacheEnabled, (cacheEnabled) => {
    if (cacheEnabled) {
      const cached4 = fiberRefGetWith(currentCache, (cache) => flatMap12(cache.getEither(proxy), (orNew) => {
        switch (orNew._tag) {
          case "Left": {
            if (orNew.left.listeners.interrupted) {
              return flatMap12(cache.invalidateWhen(proxy, (entry) => entry.handle === orNew.left.handle), () => cached4);
            }
            orNew.left.listeners.increment();
            return uninterruptibleMask((restore) => flatMap12(exit(blocked(empty21, restore(deferredAwait(orNew.left.handle)))), (exit4) => {
              orNew.left.listeners.decrement();
              return exit4;
            }));
          }
          case "Right": {
            orNew.right.listeners.increment();
            return uninterruptibleMask((restore) => flatMap12(exit(blocked(single(ds, makeEntry({
              request: proxy,
              result: orNew.right.handle,
              listeners: orNew.right.listeners,
              ownerId: id3,
              state: {
                completed: false
              }
            })), restore(deferredAwait(orNew.right.handle)))), () => {
              orNew.right.listeners.decrement();
              return deferredAwait(orNew.right.handle);
            }));
          }
        }
      }));
      return cached4;
    }
    const listeners = new Listeners();
    listeners.increment();
    return flatMap12(deferredMake(), (ref) => ensuring2(blocked(single(ds, makeEntry({
      request: proxy,
      result: ref,
      listeners,
      ownerId: id3,
      state: {
        completed: false
      }
    })), deferredAwait(ref)), sync(() => listeners.decrement())));
  });
})), "fromRequest");
var cacheRequest = /* @__PURE__ */ __name((request2, result) => {
  return fiberRefGetWith(currentCacheEnabled, (cacheEnabled) => {
    if (cacheEnabled) {
      return fiberRefGetWith(currentCache, (cache) => flatMap12(cache.getEither(request2), (orNew) => {
        switch (orNew._tag) {
          case "Left": {
            return void_3;
          }
          case "Right": {
            return deferredComplete(orNew.right.handle, result);
          }
        }
      }));
    }
    return void_3;
  });
}, "cacheRequest");
var withRequestCaching = /* @__PURE__ */ dual(2, (self, strategy) => fiberRefLocally(self, currentCacheEnabled, strategy));
var withRequestCache = /* @__PURE__ */ dual(
  2,
  // @ts-expect-error
  (self, cache) => fiberRefLocally(self, currentCache, cache)
);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Request.js
var RequestTypeId2 = RequestTypeId;
var isRequest2 = isRequest;
var of6 = of5;
var tagged5 = tagged3;
var Class6 = Class4;
var TaggedClass3 = TaggedClass;
var complete4 = complete2;
var interruptWhenPossible2 = interruptWhenPossible;
var completeEffect2 = completeEffect;
var fail11 = fail7;
var failCause9 = failCause6;
var succeed12 = succeed8;
var makeCache = /* @__PURE__ */ __name((options) => make47({
  ...options,
  lookup: /* @__PURE__ */ __name(() => map14(deferredMake(), (handle) => ({
    listeners: new Listeners(),
    handle
  })), "lookup")
}), "makeCache");
var EntryTypeId2 = /* @__PURE__ */ Symbol.for("effect/RequestBlock.Entry");
var isEntry2 = isEntry;
var makeEntry2 = makeEntry;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Effect.js
var EffectTypeId4 = EffectTypeId2;
var isEffect2 = isEffect;
var cachedWithTTL = cached2;
var cachedInvalidateWithTTL2 = cachedInvalidateWithTTL;
var cached3 = memoize;
var cachedFunction2 = cachedFunction;
var once4 = once2;
var all10 = all8;
var allWith2 = allWith;
var allSuccesses2 = allSuccesses;
var dropUntil2 = dropUntil;
var dropWhile4 = dropWhile3;
var takeUntil2 = takeUntil;
var takeWhile5 = takeWhile4;
var every13 = every11;
var exists4 = exists3;
var filter16 = filter14;
var filterMap11 = filterMap9;
var findFirst11 = findFirst8;
var forEach16 = forEach15;
var head7 = head6;
var mergeAll5 = mergeAll3;
var partition9 = partition8;
var reduce17 = reduce12;
var reduceWhile2 = reduceWhile;
var reduceRight5 = reduceRight4;
var reduceEffect3 = reduceEffect;
var replicate5 = replicate4;
var replicateEffect3 = replicateEffect2;
var validateAll2 = validateAll;
var validateFirst2 = validateFirst;
var async2 = async_;
var asyncEffect2 = asyncEffect;
var custom2 = custom;
var withFiberRuntime2 = withFiberRuntime;
var fail12 = fail2;
var failSync5 = failSync;
var failCause10 = failCause;
var failCauseSync5 = failCauseSync;
var die8 = die2;
var dieMessage2 = dieMessage;
var dieSync4 = dieSync;
var gen5 = gen3;
var never5 = never;
var none9 = none6;
var promise3 = promise;
var succeed13 = succeed;
var succeedNone3 = succeedNone;
var succeedSome3 = succeedSome;
var suspend4 = suspend;
var sync8 = sync;
var _void = void_3;
var yieldNow4 = yieldNow;
var _catch2 = _catch;
var catchAll4 = catchAll;
var catchAllCause4 = catchAllCause;
var catchAllDefect3 = catchAllDefect;
var catchIf3 = catchIf;
var catchSome2 = catchSome;
var catchSomeCause2 = catchSomeCause;
var catchSomeDefect2 = catchSomeDefect;
var catchTag3 = catchTag;
var catchTags2 = catchTags;
var cause2 = cause;
var eventually2 = eventually;
var ignore3 = ignore;
var ignoreLogged3 = ignoreLogged;
var parallelErrors2 = parallelErrors;
var sandbox3 = sandbox;
var retry3 = retry_combined;
var withExecutionPlan2 = withExecutionPlan;
var retryOrElse = retryOrElse_Effect;
var try_4 = try_2;
var tryMap2 = tryMap;
var tryMapPromise2 = tryMapPromise;
var tryPromise3 = tryPromise;
var unsandbox2 = unsandbox;
var allowInterrupt2 = allowInterrupt;
var checkInterruptible2 = checkInterruptible;
var disconnect2 = disconnect;
var interrupt8 = interrupt2;
var interruptWith3 = interruptWith;
var interruptible4 = interruptible2;
var interruptibleMask2 = interruptibleMask;
var onInterrupt3 = onInterrupt;
var uninterruptible3 = uninterruptible;
var uninterruptibleMask3 = uninterruptibleMask;
var liftPredicate5 = liftPredicate4;
var as8 = as3;
var asSome3 = asSome;
var asSomeError2 = asSomeError;
var asVoid6 = asVoid2;
var flip5 = flip3;
var flipWith2 = flipWith;
var map26 = map14;
var mapAccum4 = mapAccum3;
var mapBoth6 = mapBoth3;
var mapError5 = mapError;
var mapErrorCause4 = mapErrorCause2;
var merge7 = merge5;
var negate3 = negate2;
var acquireRelease3 = acquireRelease2;
var acquireReleaseInterruptible2 = acquireReleaseInterruptible;
var acquireUseRelease3 = acquireUseRelease;
var addFinalizer4 = addFinalizer2;
var ensuring4 = ensuring2;
var onError3 = onError;
var onExit3 = onExit;
var parallelFinalizers2 = parallelFinalizers;
var sequentialFinalizers2 = sequentialFinalizers;
var finalizersMask2 = finalizersMask;
var scope4 = scope2;
var scopeWith2 = scopeWith;
var scopedWith2 = scopedWith;
var scoped4 = scopedEffect;
var using2 = using;
var withEarlyRelease2 = withEarlyRelease;
var awaitAllChildren2 = awaitAllChildren;
var daemonChildren2 = daemonChildren;
var descriptor2 = descriptor;
var descriptorWith2 = descriptorWith;
var diffFiberRefs2 = diffFiberRefs;
var ensuringChild2 = ensuringChild;
var ensuringChildren2 = ensuringChildren;
var fiberId2 = fiberId;
var fiberIdWith2 = fiberIdWith;
var fork4 = fork2;
var forkDaemon3 = forkDaemon2;
var forkAll2 = forkAll;
var forkIn3 = forkIn2;
var forkScoped3 = forkScoped2;
var forkWithErrorHandler2 = forkWithErrorHandler;
var fromFiber2 = fromFiber;
var fromFiberEffect2 = fromFiberEffect;
var supervised2 = supervised;
var transplant2 = transplant;
var withConcurrency3 = withConcurrency;
var withScheduler2 = withScheduler;
var withSchedulingPriority2 = withSchedulingPriority;
var withMaxOpsBeforeYield2 = withMaxOpsBeforeYield;
var clock2 = clock;
var clockWith4 = clockWith3;
var withClockScoped2 = withClockScoped;
var withClock2 = withClock;
var console3 = console2;
var consoleWith2 = consoleWith;
var withConsoleScoped2 = withConsoleScoped;
var withConsole2 = withConsole;
var delay3 = delay;
var sleep5 = sleep3;
var timed2 = timed;
var timedWith2 = timedWith;
var timeout3 = timeout2;
var timeoutOption3 = timeoutOption2;
var timeoutFail2 = timeoutFail;
var timeoutFailCause2 = timeoutFailCause;
var timeoutTo2 = timeoutTo;
var configProviderWith2 = configProviderWith;
var withConfigProvider2 = withConfigProvider;
var withConfigProviderScoped2 = withConfigProviderScoped;
var context4 = context;
var contextWith3 = contextWith2;
var contextWithEffect2 = contextWithEffect;
var mapInputContext3 = mapInputContext;
var provide2 = effect_provide;
var provideService5 = provideService;
var provideServiceEffect3 = provideServiceEffect;
var serviceFunction2 = serviceFunction;
var serviceFunctionEffect2 = serviceFunctionEffect;
var serviceFunctions2 = serviceFunctions;
var serviceConstants2 = serviceConstants;
var serviceMembers2 = serviceMembers;
var serviceOption3 = serviceOption;
var serviceOptional2 = serviceOptional;
var updateService3 = updateService;
var Do6 = Do4;
var bind7 = bind5;
var bindAll2 = bindAll;
var bindTo7 = bindTo5;
var let_7 = let_5;
var option3 = option;
var either6 = either3;
var exit3 = exit;
var intoDeferred2 = intoDeferred;
var if_2 = if_;
var filterOrDie2 = filterOrDie;
var filterOrDieMessage2 = filterOrDieMessage;
var filterOrElse2 = filterOrElse;
var filterOrFail3 = filterOrFail;
var filterEffectOrElse2 = filterEffectOrElse;
var filterEffectOrFail2 = filterEffectOrFail;
var unless2 = unless;
var unlessEffect2 = unlessEffect;
var when3 = when;
var whenEffect2 = whenEffect;
var whenFiberRef2 = whenFiberRef;
var whenRef2 = whenRef;
var flatMap18 = flatMap12;
var andThen11 = andThen6;
var flatten12 = flatten7;
var race3 = race2;
var raceAll3 = raceAll2;
var raceFirst3 = raceFirst2;
var raceWith2 = raceWith;
var summarized2 = summarized;
var tap5 = tap2;
var tapBoth2 = tapBoth;
var tapDefect3 = tapDefect;
var tapError4 = tapError;
var tapErrorTag2 = tapErrorTag;
var tapErrorCause4 = tapErrorCause;
var forever4 = forever;
var iterate2 = iterate;
var loop2 = loop;
var repeat3 = repeat_combined;
var repeatN2 = repeatN;
var repeatOrElse = repeatOrElse_Effect;
var schedule = schedule_Effect;
var scheduleForked2 = scheduleForked;
var scheduleFrom = scheduleFrom_Effect;
var whileLoop3 = whileLoop;
var getFiberRefs = fiberRefs3;
var inheritFiberRefs2 = inheritFiberRefs;
var locally2 = fiberRefLocally;
var locallyWith = fiberRefLocallyWith;
var locallyScoped = fiberRefLocallyScoped;
var locallyScopedWith = fiberRefLocallyScopedWith;
var patchFiberRefs2 = patchFiberRefs;
var setFiberRefs2 = setFiberRefs;
var updateFiberRefs3 = updateFiberRefs;
var isFailure5 = isFailure3;
var isSuccess3 = isSuccess2;
var match20 = match8;
var matchCause4 = matchCause;
var matchCauseEffect3 = matchCauseEffect;
var matchEffect4 = matchEffect;
var log3 = log;
var logWithLevel2 = /* @__PURE__ */ __name((level, ...message) => logWithLevel(level)(...message), "logWithLevel");
var logTrace2 = logTrace;
var logDebug2 = logDebug;
var logInfo2 = logInfo;
var logWarning2 = logWarning;
var logError2 = logError;
var logFatal2 = logFatal;
var withLogSpan2 = withLogSpan;
var annotateLogs3 = annotateLogs;
var annotateLogsScoped2 = annotateLogsScoped;
var logAnnotations2 = logAnnotations;
var withUnhandledErrorLogLevel2 = withUnhandledErrorLogLevel;
var whenLogLevel2 = whenLogLevel;
var orDie4 = orDie;
var orDieWith2 = orDieWith;
var orElse8 = orElse3;
var orElseFail2 = orElseFail;
var orElseSucceed3 = orElseSucceed;
var firstSuccessOf2 = firstSuccessOf;
var random3 = random2;
var randomWith3 = randomWith;
var withRandom2 = withRandom;
var withRandomFixed = /* @__PURE__ */ dual(2, (effect4, values10) => withRandom2(effect4, fixed2(values10)));
var withRandomScoped2 = withRandomScoped;
var runtime4 = runtime3;
var getRuntimeFlags = runtimeFlags;
var patchRuntimeFlags = updateRuntimeFlags;
var withRuntimeFlagsPatch = withRuntimeFlags;
var withRuntimeFlagsPatchScoped = withRuntimeFlagsScoped;
var tagMetrics2 = tagMetrics;
var labelMetrics2 = labelMetrics;
var tagMetricsScoped2 = tagMetricsScoped;
var labelMetricsScoped2 = labelMetricsScoped;
var metricLabels2 = metricLabels;
var withMetric2 = withMetric;
var unsafeMakeSemaphore2 = unsafeMakeSemaphore;
var makeSemaphore2 = makeSemaphore;
var unsafeMakeLatch2 = unsafeMakeLatch;
var makeLatch2 = makeLatch;
var runFork2 = unsafeForkEffect;
var runCallback = unsafeRunEffect;
var runPromise2 = unsafeRunPromiseEffect;
var runPromiseExit2 = unsafeRunPromiseExitEffect;
var runSync2 = unsafeRunSyncEffect;
var runSyncExit2 = unsafeRunSyncExitEffect;
var validate2 = validate;
var validateWith2 = validateWith;
var zip12 = zipOptions;
var zipLeft7 = zipLeftOptions;
var zipRight7 = zipRightOptions;
var zipWith12 = zipWithOptions;
var ap3 = /* @__PURE__ */ dual(2, (self, that) => zipWith12(self, that, (f, a) => f(a)));
var blocked2 = blocked;
var runRequestBlock2 = runRequestBlock;
var step3 = step2;
var request = /* @__PURE__ */ dual((args2) => isRequest2(args2[0]), fromRequest);
var cacheRequestResult = cacheRequest;
var withRequestBatching2 = withRequestBatching;
var withRequestCaching2 = withRequestCaching;
var withRequestCache2 = withRequestCache;
var tracer2 = tracer;
var tracerWith4 = tracerWith;
var withTracer2 = withTracer;
var withTracerScoped2 = withTracerScoped;
var withTracerEnabled2 = withTracerEnabled;
var withTracerTiming2 = withTracerTiming;
var annotateSpans3 = annotateSpans;
var annotateCurrentSpan2 = annotateCurrentSpan;
var currentSpan2 = currentSpan;
var currentParentSpan2 = currentParentSpan;
var spanAnnotations2 = spanAnnotations;
var spanLinks2 = spanLinks;
var linkSpans2 = linkSpans;
var linkSpanCurrent2 = linkSpanCurrent;
var makeSpan2 = makeSpan;
var makeSpanScoped2 = makeSpanScoped;
var useSpan2 = useSpan;
var withSpan3 = withSpan;
var functionWithSpan2 = functionWithSpan;
var withSpanScoped2 = withSpanScoped;
var withParentSpan3 = withParentSpan;
var fromNullable5 = fromNullable4;
var optionFromOptional2 = optionFromOptional;
var transposeOption2 = /* @__PURE__ */ __name((self) => {
  return isNone(self) ? succeedNone3 : map26(self.value, some2);
}, "transposeOption");
var transposeMapOption2 = /* @__PURE__ */ dual(2, (self, f) => isNone(self) ? succeedNone3 : map26(f(self.value), some2));
var makeTagProxy = /* @__PURE__ */ __name((TagClass) => {
  const cache = /* @__PURE__ */ new Map();
  return new Proxy(TagClass, {
    get(target, prop, receiver) {
      if (prop in target) {
        return Reflect.get(target, prop, receiver);
      }
      if (cache.has(prop)) {
        return cache.get(prop);
      }
      const fn2 = /* @__PURE__ */ __name((...args2) => andThen6(target, (s) => {
        if (typeof s[prop] === "function") {
          cache.set(prop, (...args3) => andThen6(target, (s2) => s2[prop](...args3)));
          return s[prop](...args2);
        }
        cache.set(prop, andThen6(target, (s2) => s2[prop]));
        return s[prop];
      }), "fn");
      const cn = andThen6(target, (s) => s[prop]);
      Object.assign(fn2, cn);
      const apply2 = fn2.apply;
      const bind11 = fn2.bind;
      const call = fn2.call;
      const proto12 = Object.setPrototypeOf({}, Object.getPrototypeOf(cn));
      proto12.apply = apply2;
      proto12.bind = bind11;
      proto12.call = call;
      Object.setPrototypeOf(fn2, proto12);
      cache.set(prop, fn2);
      return fn2;
    }
  });
}, "makeTagProxy");
var Tag3 = /* @__PURE__ */ __name((id3) => () => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const creationError = new Error();
  Error.stackTraceLimit = limit;
  function TagClass() {
  }
  __name(TagClass, "TagClass");
  Object.setPrototypeOf(TagClass, TagProto);
  TagClass.key = id3;
  Object.defineProperty(TagClass, "use", {
    get() {
      return (body) => andThen6(this, body);
    }
  });
  Object.defineProperty(TagClass, "stack", {
    get() {
      return creationError.stack;
    }
  });
  return makeTagProxy(TagClass);
}, "Tag");
var Service = /* @__PURE__ */ __name(function() {
  return function() {
    const [id3, maker] = arguments;
    const proxy = "accessors" in maker ? maker["accessors"] : false;
    const limit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const creationError = new Error();
    Error.stackTraceLimit = limit;
    let patchState = "unchecked";
    const TagClass = /* @__PURE__ */ __name(function(service6) {
      if (patchState === "unchecked") {
        const proto12 = Object.getPrototypeOf(service6);
        if (proto12 === Object.prototype || proto12 === null) {
          patchState = "plain";
        } else {
          const selfProto = Object.getPrototypeOf(this);
          Object.setPrototypeOf(selfProto, proto12);
          patchState = "patched";
        }
      }
      if (patchState === "plain") {
        Object.assign(this, service6);
      } else if (patchState === "patched") {
        Object.setPrototypeOf(service6, Object.getPrototypeOf(this));
        return service6;
      }
    }, "TagClass");
    TagClass.prototype._tag = id3;
    Object.defineProperty(TagClass, "make", {
      get() {
        return (service6) => new this(service6);
      }
    });
    Object.defineProperty(TagClass, "use", {
      get() {
        return (body) => andThen6(this, body);
      }
    });
    TagClass.key = id3;
    Object.assign(TagClass, TagProto);
    Object.defineProperty(TagClass, "stack", {
      get() {
        return creationError.stack;
      }
    });
    const hasDeps = "dependencies" in maker && maker.dependencies.length > 0;
    const layerName = hasDeps ? "DefaultWithoutDependencies" : "Default";
    let layerCache;
    let isFunction3 = false;
    if ("effect" in maker) {
      isFunction3 = typeof maker.effect === "function";
      Object.defineProperty(TagClass, layerName, {
        get() {
          if (isFunction3) {
            return function() {
              return fromEffect4(TagClass, map26(maker.effect.apply(null, arguments), (_) => new this(_)));
            }.bind(this);
          }
          return layerCache ??= fromEffect4(TagClass, map26(maker.effect, (_) => new this(_)));
        }
      });
    } else if ("scoped" in maker) {
      isFunction3 = typeof maker.scoped === "function";
      Object.defineProperty(TagClass, layerName, {
        get() {
          if (isFunction3) {
            return function() {
              return scoped3(TagClass, map26(maker.scoped.apply(null, arguments), (_) => new this(_)));
            }.bind(this);
          }
          return layerCache ??= scoped3(TagClass, map26(maker.scoped, (_) => new this(_)));
        }
      });
    } else if ("sync" in maker) {
      Object.defineProperty(TagClass, layerName, {
        get() {
          return layerCache ??= sync6(TagClass, () => new this(maker.sync()));
        }
      });
    } else {
      Object.defineProperty(TagClass, layerName, {
        get() {
          return layerCache ??= succeed10(TagClass, new this(maker.succeed));
        }
      });
    }
    if (hasDeps) {
      let layerWithDepsCache;
      Object.defineProperty(TagClass, "Default", {
        get() {
          if (isFunction3) {
            return function() {
              return provide(this.DefaultWithoutDependencies.apply(null, arguments), maker.dependencies);
            };
          }
          return layerWithDepsCache ??= provide(this.DefaultWithoutDependencies, maker.dependencies);
        }
      });
    }
    return proxy === true ? makeTagProxy(TagClass) : TagClass;
  };
}, "Service");
var fn = /* @__PURE__ */ __name(function(nameOrBody, ...pipeables) {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const errorDef = new Error();
  Error.stackTraceLimit = limit;
  if (typeof nameOrBody !== "string") {
    return defineLength(nameOrBody.length, function(...args2) {
      const limit2 = Error.stackTraceLimit;
      Error.stackTraceLimit = 2;
      const errorCall = new Error();
      Error.stackTraceLimit = limit2;
      return fnApply({
        self: this,
        body: nameOrBody,
        args: args2,
        pipeables,
        spanName: "<anonymous>",
        spanOptions: {
          context: DisablePropagation.context(true)
        },
        errorDef,
        errorCall
      });
    });
  }
  const name = nameOrBody;
  const options = pipeables[0];
  return (body, ...pipeables2) => defineLength(body.length, {
    [name](...args2) {
      const limit2 = Error.stackTraceLimit;
      Error.stackTraceLimit = 2;
      const errorCall = new Error();
      Error.stackTraceLimit = limit2;
      return fnApply({
        self: this,
        body,
        args: args2,
        pipeables: pipeables2,
        spanName: name,
        spanOptions: options,
        errorDef,
        errorCall
      });
    }
  }[name]);
}, "fn");
function defineLength(length5, fn2) {
  return Object.defineProperty(fn2, "length", {
    value: length5,
    configurable: true
  });
}
__name(defineLength, "defineLength");
function fnApply(options) {
  let effect4;
  let fnError = void 0;
  if (isGeneratorFunction(options.body)) {
    effect4 = fromIterator(() => options.body.apply(options.self, options.args));
  } else {
    try {
      effect4 = options.body.apply(options.self, options.args);
    } catch (error2) {
      fnError = error2;
      effect4 = die8(error2);
    }
  }
  if (options.pipeables.length > 0) {
    try {
      for (const x of options.pipeables) {
        effect4 = x(effect4, ...options.args);
      }
    } catch (error2) {
      effect4 = fnError ? failCause10(sequential(die(fnError), die(error2))) : die8(error2);
    }
  }
  let cache = false;
  const captureStackTrace = /* @__PURE__ */ __name(() => {
    if (cache !== false) {
      return cache;
    }
    if (options.errorCall.stack) {
      const stackDef = options.errorDef.stack.trim().split("\n");
      const stackCall = options.errorCall.stack.trim().split("\n");
      let endStackDef = stackDef.slice(2).join("\n").trim();
      if (!endStackDef.includes(`(`)) {
        endStackDef = endStackDef.replace(/at (.*)/, "at ($1)");
      }
      let endStackCall = stackCall.slice(2).join("\n").trim();
      if (!endStackCall.includes(`(`)) {
        endStackCall = endStackCall.replace(/at (.*)/, "at ($1)");
      }
      cache = `${endStackDef}
${endStackCall}`;
      return cache;
    }
  }, "captureStackTrace");
  const opts = options.spanOptions && "captureStackTrace" in options.spanOptions ? options.spanOptions : {
    captureStackTrace,
    ...options.spanOptions
  };
  return withSpan3(effect4, options.spanName, opts);
}
__name(fnApply, "fnApply");
var fnUntraced2 = fnUntraced;
var ensureSuccessType = /* @__PURE__ */ __name(() => (effect4) => effect4, "ensureSuccessType");
var ensureErrorType = /* @__PURE__ */ __name(() => (effect4) => effect4, "ensureErrorType");
var ensureRequirementsType = /* @__PURE__ */ __name(() => (effect4) => effect4, "ensureRequirementsType");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/FiberRef.js
var FiberRefTypeId2 = FiberRefTypeId;
var make57 = fiberRefMake;
var makeWith2 = fiberRefMakeWith;
var makeContext2 = fiberRefMakeContext;
var makeRuntimeFlags = fiberRefMakeRuntimeFlags;
var unsafeMake12 = fiberRefUnsafeMake;
var unsafeMakeHashSet = fiberRefUnsafeMakeHashSet;
var unsafeMakeContext = fiberRefUnsafeMakeContext;
var unsafeMakeSupervisor = fiberRefUnsafeMakeSupervisor;
var unsafeMakePatch = fiberRefUnsafeMakePatch;
var get14 = fiberRefGet;
var getAndSet4 = fiberRefGetAndSet;
var getAndUpdate4 = fiberRefGetAndUpdate;
var getAndUpdateSome3 = fiberRefGetAndUpdateSome;
var getWith = fiberRefGetWith;
var set9 = fiberRefSet;
var _delete = fiberRefDelete;
var reset2 = fiberRefReset;
var modify11 = fiberRefModify;
var modifySome3 = fiberRefModifySome;
var update7 = fiberRefUpdate;
var updateSome3 = fiberRefUpdateSome;
var updateAndGet4 = fiberRefUpdateAndGet;
var updateSomeAndGet3 = fiberRefUpdateSomeAndGet;
var currentConcurrency2 = currentConcurrency;
var currentRequestBatchingEnabled = currentRequestBatching;
var currentRequestCache = currentCache;
var currentRequestCacheEnabled = currentCacheEnabled;
var currentContext2 = currentContext;
var currentSchedulingPriority2 = currentSchedulingPriority;
var currentMaxOpsBeforeYield2 = currentMaxOpsBeforeYield;
var unhandledErrorLogLevel = currentUnhandledErrorLogLevel;
var versionMismatchErrorLogLevel = currentVersionMismatchErrorLogLevel;
var currentLogAnnotations2 = currentLogAnnotations;
var currentLoggers2 = currentLoggers;
var currentLogLevel2 = currentLogLevel;
var currentMinimumLogLevel2 = currentMinimumLogLevel;
var currentLogSpan2 = currentLogSpan;
var currentRuntimeFlags2 = currentRuntimeFlags;
var currentScheduler2 = currentScheduler;
var currentSupervisor2 = currentSupervisor;
var currentMetricLabels2 = currentMetricLabels;
var currentTracerEnabled2 = currentTracerEnabled;
var currentTracerTimingEnabled2 = currentTracerTimingEnabled;
var currentTracerSpanAnnotations2 = currentTracerSpanAnnotations;
var currentTracerSpanLinks2 = currentTracerSpanLinks;
var interruptedCause = currentInterruptedCause;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Layer.js
var Layer_exports = {};
__export(Layer_exports, {
  CurrentMemoMap: () => CurrentMemoMap2,
  LayerTypeId: () => LayerTypeId2,
  MemoMapTypeId: () => MemoMapTypeId2,
  annotateLogs: () => annotateLogs4,
  annotateSpans: () => annotateSpans4,
  build: () => build2,
  buildWithMemoMap: () => buildWithMemoMap2,
  buildWithScope: () => buildWithScope2,
  catchAll: () => catchAll5,
  catchAllCause: () => catchAllCause5,
  context: () => context5,
  die: () => die9,
  dieSync: () => dieSync5,
  discard: () => discard2,
  effect: () => effect,
  effectContext: () => effectContext,
  effectDiscard: () => effectDiscard,
  empty: () => empty42,
  extendScope: () => extendScope2,
  fail: () => fail13,
  failCause: () => failCause11,
  failCauseSync: () => failCauseSync6,
  failSync: () => failSync6,
  fiberRefLocallyScopedWith: () => fiberRefLocallyScopedWith3,
  flatMap: () => flatMap19,
  flatten: () => flatten13,
  fresh: () => fresh2,
  function: () => fromFunction3,
  isFresh: () => isFresh2,
  isLayer: () => isLayer2,
  launch: () => launch2,
  locally: () => locally3,
  locallyEffect: () => locallyEffect2,
  locallyScoped: () => locallyScoped2,
  locallyWith: () => locallyWith2,
  makeMemoMap: () => makeMemoMap2,
  map: () => map27,
  mapError: () => mapError6,
  match: () => match21,
  matchCause: () => matchCause5,
  memoize: () => memoize3,
  merge: () => merge8,
  mergeAll: () => mergeAll6,
  mock: () => mock2,
  orDie: () => orDie5,
  orElse: () => orElse9,
  parentSpan: () => parentSpan2,
  passthrough: () => passthrough3,
  project: () => project2,
  provide: () => provide3,
  provideMerge: () => provideMerge2,
  retry: () => retry4,
  scope: () => scope5,
  scoped: () => scoped5,
  scopedContext: () => scopedContext2,
  scopedDiscard: () => scopedDiscard2,
  service: () => service3,
  setClock: () => setClock,
  setConfigProvider: () => setConfigProvider2,
  setRandom: () => setRandom,
  setRequestBatching: () => setRequestBatching,
  setRequestCache: () => setRequestCache,
  setRequestCaching: () => setRequestCaching,
  setScheduler: () => setScheduler,
  setTracer: () => setTracer2,
  setTracerEnabled: () => setTracerEnabled,
  setTracerTiming: () => setTracerTiming,
  setUnhandledErrorLogLevel: () => setUnhandledErrorLogLevel,
  setVersionMismatchErrorLogLevel: () => setVersionMismatchErrorLogLevel,
  span: () => span3,
  succeed: () => succeed14,
  succeedContext: () => succeedContext2,
  suspend: () => suspend5,
  sync: () => sync9,
  syncContext: () => syncContext2,
  tap: () => tap6,
  tapError: () => tapError5,
  tapErrorCause: () => tapErrorCause5,
  toRuntime: () => toRuntime2,
  toRuntimeWithMemoMap: () => toRuntimeWithMemoMap2,
  unwrapEffect: () => unwrapEffect2,
  unwrapScoped: () => unwrapScoped2,
  updateService: () => updateService4,
  withParentSpan: () => withParentSpan4,
  withSpan: () => withSpan4,
  zipWith: () => zipWith13
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/layer/circular.js
var minimumLogLevel = /* @__PURE__ */ __name((level) => scopedDiscard(fiberRefLocallyScoped(currentMinimumLogLevel, level)), "minimumLogLevel");
var withMinimumLogLevel = /* @__PURE__ */ dual(2, (self, level) => fiberRefLocally(currentMinimumLogLevel, level)(self));
var addLogger = /* @__PURE__ */ __name((logger) => scopedDiscard(fiberRefLocallyScopedWith(currentLoggers, add4(logger))), "addLogger");
var addLoggerEffect = /* @__PURE__ */ __name((effect4) => unwrapEffect(map14(effect4, addLogger)), "addLoggerEffect");
var addLoggerScoped = /* @__PURE__ */ __name((effect4) => unwrapScoped(map14(effect4, addLogger)), "addLoggerScoped");
var removeLogger = /* @__PURE__ */ __name((logger) => scopedDiscard(fiberRefLocallyScopedWith(currentLoggers, remove6(logger))), "removeLogger");
var replaceLogger = /* @__PURE__ */ dual(2, (self, that) => flatMap17(removeLogger(self), () => addLogger(that)));
var replaceLoggerEffect = /* @__PURE__ */ dual(2, (self, that) => flatMap17(removeLogger(self), () => addLoggerEffect(that)));
var replaceLoggerScoped = /* @__PURE__ */ dual(2, (self, that) => flatMap17(removeLogger(self), () => addLoggerScoped(that)));
var addSupervisor = /* @__PURE__ */ __name((supervisor) => scopedDiscard(fiberRefLocallyScopedWith(currentSupervisor, (current2) => new Zip(current2, supervisor))), "addSupervisor");
var enableCooperativeYielding = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ enable(CooperativeYielding)));
var enableInterruption = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ enable(Interruption)));
var enableOpSupervision = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ enable(OpSupervision)));
var enableRuntimeMetrics = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ enable(RuntimeMetrics)));
var enableWindDown = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ enable(WindDown)));
var disableCooperativeYielding = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ disable(CooperativeYielding)));
var disableInterruption = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ disable(Interruption)));
var disableOpSupervision = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ disable(OpSupervision)));
var disableRuntimeMetrics = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ disable(RuntimeMetrics)));
var disableWindDown = /* @__PURE__ */ scopedDiscard(/* @__PURE__ */ withRuntimeFlagsScoped(/* @__PURE__ */ disable(WindDown)));
var setConfigProvider = /* @__PURE__ */ __name((configProvider) => scopedDiscard(withConfigProviderScoped(configProvider)), "setConfigProvider");
var parentSpan = /* @__PURE__ */ __name((span4) => succeedContext(make6(spanTag, span4)), "parentSpan");
var span2 = /* @__PURE__ */ __name((name, options) => {
  options = addSpanStackTrace(options);
  return scoped3(spanTag, options?.onEnd ? tap2(makeSpanScoped(name, options), (span4) => addFinalizer2((exit4) => options.onEnd(span4, exit4))) : makeSpanScoped(name, options));
}, "span");
var setTracer = /* @__PURE__ */ __name((tracer3) => scopedDiscard(withTracerScoped(tracer3)), "setTracer");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Layer.js
var LayerTypeId2 = LayerTypeId;
var MemoMapTypeId2 = MemoMapTypeId;
var CurrentMemoMap2 = CurrentMemoMap;
var isLayer2 = isLayer;
var isFresh2 = isFresh;
var annotateLogs4 = annotateLogs2;
var annotateSpans4 = annotateSpans2;
var build2 = build;
var buildWithScope2 = buildWithScope;
var catchAll5 = catchAll3;
var catchAllCause5 = catchAllCause3;
var context5 = context3;
var die9 = die7;
var dieSync5 = dieSync3;
var discard2 = discard;
var effect = fromEffect4;
var effectDiscard = fromEffectDiscard;
var effectContext = fromEffectContext;
var empty42 = empty40;
var extendScope2 = extendScope;
var fail13 = fail10;
var failSync6 = failSync4;
var failCause11 = failCause8;
var failCauseSync6 = failCauseSync4;
var flatMap19 = flatMap17;
var flatten13 = flatten11;
var fresh2 = fresh;
var mock2 = mock;
var fromFunction3 = fromFunction;
var launch2 = launch;
var map27 = map24;
var mapError6 = mapError4;
var match21 = match16;
var matchCause5 = matchCause3;
var memoize3 = memoize2;
var merge8 = merge6;
var mergeAll6 = mergeAll4;
var orDie5 = orDie3;
var orElse9 = orElse7;
var passthrough3 = passthrough;
var project2 = project;
var locallyEffect2 = locallyEffect;
var locally3 = fiberRefLocally2;
var locallyWith2 = fiberRefLocallyWith2;
var locallyScoped2 = fiberRefLocallyScoped2;
var fiberRefLocallyScopedWith3 = fiberRefLocallyScopedWith2;
var retry4 = retry2;
var scope5 = scope3;
var scoped5 = scoped3;
var scopedDiscard2 = scopedDiscard;
var scopedContext2 = scopedContext;
var service3 = service2;
var succeed14 = succeed10;
var succeedContext2 = succeedContext;
var suspend5 = suspend3;
var sync9 = sync6;
var syncContext2 = syncContext;
var tap6 = tap4;
var tapError5 = tapError3;
var tapErrorCause5 = tapErrorCause3;
var toRuntime2 = toRuntime;
var toRuntimeWithMemoMap2 = toRuntimeWithMemoMap;
var provide3 = provide;
var provideMerge2 = provideMerge;
var zipWith13 = zipWith10;
var unwrapEffect2 = unwrapEffect;
var unwrapScoped2 = unwrapScoped;
var setClock = /* @__PURE__ */ __name((clock3) => scopedDiscard2(fiberRefLocallyScopedWith(currentServices, add2(clockTag, clock3))), "setClock");
var setConfigProvider2 = setConfigProvider;
var parentSpan2 = parentSpan;
var setRandom = /* @__PURE__ */ __name((random4) => scopedDiscard2(fiberRefLocallyScopedWith(currentServices, add2(randomTag, random4))), "setRandom");
var setRequestBatching = /* @__PURE__ */ __name((requestBatching) => scopedDiscard2(fiberRefLocallyScoped(currentRequestBatching, requestBatching)), "setRequestBatching");
var setRequestCaching = /* @__PURE__ */ __name((requestCaching) => scopedDiscard2(fiberRefLocallyScoped(currentCacheEnabled, requestCaching)), "setRequestCaching");
var setRequestCache = /* @__PURE__ */ __name((cache) => scopedDiscard2(isEffect(cache) ? flatMap12(cache, (x) => fiberRefLocallyScoped(currentCache, x)) : fiberRefLocallyScoped(currentCache, cache)), "setRequestCache");
var setScheduler = /* @__PURE__ */ __name((scheduler) => scopedDiscard2(fiberRefLocallyScoped(currentScheduler, scheduler)), "setScheduler");
var span3 = span2;
var setTracer2 = setTracer;
var setTracerEnabled = /* @__PURE__ */ __name((enabled2) => scopedDiscard2(fiberRefLocallyScoped(currentTracerEnabled, enabled2)), "setTracerEnabled");
var setTracerTiming = /* @__PURE__ */ __name((enabled2) => scopedDiscard2(fiberRefLocallyScoped(currentTracerTimingEnabled, enabled2)), "setTracerTiming");
var setUnhandledErrorLogLevel = /* @__PURE__ */ __name((level) => scopedDiscard2(fiberRefLocallyScoped(currentUnhandledErrorLogLevel, level)), "setUnhandledErrorLogLevel");
var setVersionMismatchErrorLogLevel = /* @__PURE__ */ __name((level) => scopedDiscard2(fiberRefLocallyScoped(currentVersionMismatchErrorLogLevel, level)), "setVersionMismatchErrorLogLevel");
var withSpan4 = withSpan2;
var withParentSpan4 = withParentSpan2;
var makeMemoMap2 = makeMemoMap;
var buildWithMemoMap2 = buildWithMemoMap;
var updateService4 = /* @__PURE__ */ dual(3, (layer, tag, f) => provide3(layer, map27(context5(), (c) => add2(c, tag, f(unsafeGet3(c, tag))))));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/queue.js
var EnqueueSymbolKey = "effect/QueueEnqueue";
var EnqueueTypeId = /* @__PURE__ */ Symbol.for(EnqueueSymbolKey);
var DequeueSymbolKey = "effect/QueueDequeue";
var DequeueTypeId = /* @__PURE__ */ Symbol.for(DequeueSymbolKey);
var QueueStrategySymbolKey = "effect/QueueStrategy";
var QueueStrategyTypeId = /* @__PURE__ */ Symbol.for(QueueStrategySymbolKey);
var BackingQueueSymbolKey = "effect/BackingQueue";
var BackingQueueTypeId = /* @__PURE__ */ Symbol.for(BackingQueueSymbolKey);
var queueStrategyVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var backingQueueVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var enqueueVariance = {
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In")
};
var dequeueVariance = {
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out")
};
var QueueImpl = class extends Class3 {
  static {
    __name(this, "QueueImpl");
  }
  queue;
  takers;
  shutdownHook;
  shutdownFlag;
  strategy;
  [EnqueueTypeId] = enqueueVariance;
  [DequeueTypeId] = dequeueVariance;
  constructor(queue, takers, shutdownHook, shutdownFlag, strategy) {
    super();
    this.queue = queue;
    this.takers = takers;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  commit() {
    return this.take;
  }
  capacity() {
    return this.queue.capacity();
  }
  get size() {
    return suspend(() => catchAll(this.unsafeSize(), () => interrupt2));
  }
  unsafeSize() {
    if (get7(this.shutdownFlag)) {
      return none2();
    }
    return some3(this.queue.length() - length3(this.takers) + this.strategy.surplusSize());
  }
  get isEmpty() {
    return map14(this.size, (size25) => size25 <= 0);
  }
  get isFull() {
    return map14(this.size, (size25) => size25 >= this.capacity());
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      pipe(this.shutdownFlag, set3(true));
      return pipe(forEachConcurrentDiscard(unsafePollAll(this.takers), (d) => deferredInterruptWith(d, state.id()), false, false), zipRight2(this.strategy.shutdown), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid2);
    }));
  }
  get isShutdown() {
    return sync(() => get7(this.shutdownFlag));
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  isActive() {
    return !get7(this.shutdownFlag);
  }
  unsafeOffer(value2) {
    if (get7(this.shutdownFlag)) {
      return false;
    }
    let noRemaining;
    if (this.queue.length() === 0) {
      const taker = pipe(this.takers, poll2(EmptyMutableQueue));
      if (taker !== EmptyMutableQueue) {
        unsafeCompleteDeferred(taker, value2);
        noRemaining = true;
      } else {
        noRemaining = false;
      }
    } else {
      noRemaining = false;
    }
    if (noRemaining) {
      return true;
    }
    const succeeded = this.queue.offer(value2);
    unsafeCompleteTakers(this.strategy, this.queue, this.takers);
    return succeeded;
  }
  offer(value2) {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      let noRemaining;
      if (this.queue.length() === 0) {
        const taker = pipe(this.takers, poll2(EmptyMutableQueue));
        if (taker !== EmptyMutableQueue) {
          unsafeCompleteDeferred(taker, value2);
          noRemaining = true;
        } else {
          noRemaining = false;
        }
      } else {
        noRemaining = false;
      }
      if (noRemaining) {
        return succeed(true);
      }
      const succeeded = this.queue.offer(value2);
      unsafeCompleteTakers(this.strategy, this.queue, this.takers);
      return succeeded ? succeed(true) : this.strategy.handleSurplus([value2], this.queue, this.takers, this.shutdownFlag);
    });
  }
  offerAll(iterable) {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      const values10 = fromIterable2(iterable);
      const pTakers = this.queue.length() === 0 ? fromIterable2(unsafePollN(this.takers, values10.length)) : empty4;
      const [forTakers, remaining] = pipe(values10, splitAt(pTakers.length));
      for (let i = 0; i < pTakers.length; i++) {
        const taker = pTakers[i];
        const item = forTakers[i];
        unsafeCompleteDeferred(taker, item);
      }
      if (remaining.length === 0) {
        return succeed(true);
      }
      const surplus = this.queue.offerAll(remaining);
      unsafeCompleteTakers(this.strategy, this.queue, this.takers);
      return isEmpty2(surplus) ? succeed(true) : this.strategy.handleSurplus(surplus, this.queue, this.takers, this.shutdownFlag);
    });
  }
  get take() {
    return withFiberRuntime((state) => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      const item = this.queue.poll(EmptyMutableQueue);
      if (item !== EmptyMutableQueue) {
        this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
        return succeed(item);
      } else {
        const deferred = deferredUnsafeMake(state.id());
        return pipe(suspend(() => {
          pipe(this.takers, offer(deferred));
          unsafeCompleteTakers(this.strategy, this.queue, this.takers);
          return get7(this.shutdownFlag) ? interrupt2 : deferredAwait(deferred);
        }), onInterrupt(() => {
          return sync(() => unsafeRemove(this.takers, deferred));
        }));
      }
    });
  }
  get takeAll() {
    return suspend(() => {
      return get7(this.shutdownFlag) ? interrupt2 : sync(() => {
        const values10 = this.queue.pollUpTo(Number.POSITIVE_INFINITY);
        this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
        return fromIterable3(values10);
      });
    });
  }
  takeUpTo(max10) {
    return suspend(() => get7(this.shutdownFlag) ? interrupt2 : sync(() => {
      const values10 = this.queue.pollUpTo(max10);
      this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
      return fromIterable3(values10);
    }));
  }
  takeBetween(min8, max10) {
    return suspend(() => takeRemainderLoop(this, min8, max10, empty7()));
  }
};
var takeRemainderLoop = /* @__PURE__ */ __name((self, min8, max10, acc) => {
  if (max10 < min8) {
    return succeed(acc);
  }
  return pipe(takeUpTo(self, max10), flatMap12((bs) => {
    const remaining = min8 - bs.length;
    if (remaining === 1) {
      return pipe(take5(self), map14((b) => pipe(acc, appendAll3(bs), append3(b))));
    }
    if (remaining > 1) {
      return pipe(take5(self), flatMap12((b) => takeRemainderLoop(self, remaining - 1, max10 - bs.length - 1, pipe(acc, appendAll3(bs), append3(b)))));
    }
    return succeed(pipe(acc, appendAll3(bs)));
  }));
}, "takeRemainderLoop");
var isQueue = /* @__PURE__ */ __name((u) => isEnqueue(u) && isDequeue(u), "isQueue");
var isEnqueue = /* @__PURE__ */ __name((u) => hasProperty(u, EnqueueTypeId), "isEnqueue");
var isDequeue = /* @__PURE__ */ __name((u) => hasProperty(u, DequeueTypeId), "isDequeue");
var bounded2 = /* @__PURE__ */ __name((requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap12((queue) => make58(backingQueueFromMutableQueue(queue), backPressureStrategy()))), "bounded");
var dropping = /* @__PURE__ */ __name((requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap12((queue) => make58(backingQueueFromMutableQueue(queue), droppingStrategy()))), "dropping");
var sliding = /* @__PURE__ */ __name((requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap12((queue) => make58(backingQueueFromMutableQueue(queue), slidingStrategy()))), "sliding");
var unbounded2 = /* @__PURE__ */ __name(() => pipe(sync(() => unbounded()), flatMap12((queue) => make58(backingQueueFromMutableQueue(queue), droppingStrategy()))), "unbounded");
var unsafeMake13 = /* @__PURE__ */ __name((queue, takers, shutdownHook, shutdownFlag, strategy) => {
  return new QueueImpl(queue, takers, shutdownHook, shutdownFlag, strategy);
}, "unsafeMake");
var make58 = /* @__PURE__ */ __name((queue, strategy) => pipe(deferredMake(), map14((deferred) => unsafeMake13(queue, unbounded(), deferred, make13(false), strategy))), "make");
var BackingQueueFromMutableQueue = class {
  static {
    __name(this, "BackingQueueFromMutableQueue");
  }
  mutable;
  [BackingQueueTypeId] = backingQueueVariance;
  constructor(mutable) {
    this.mutable = mutable;
  }
  poll(def) {
    return poll2(this.mutable, def);
  }
  pollUpTo(limit) {
    return pollUpTo(this.mutable, limit);
  }
  offerAll(elements) {
    return offerAll(this.mutable, elements);
  }
  offer(element) {
    return offer(this.mutable, element);
  }
  capacity() {
    return capacity(this.mutable);
  }
  length() {
    return length3(this.mutable);
  }
};
var backingQueueFromMutableQueue = /* @__PURE__ */ __name((mutable) => new BackingQueueFromMutableQueue(mutable), "backingQueueFromMutableQueue");
var capacity2 = /* @__PURE__ */ __name((self) => self.capacity(), "capacity");
var size17 = /* @__PURE__ */ __name((self) => self.size, "size");
var isFull2 = /* @__PURE__ */ __name((self) => self.isFull, "isFull");
var isEmpty15 = /* @__PURE__ */ __name((self) => self.isEmpty, "isEmpty");
var isShutdown = /* @__PURE__ */ __name((self) => self.isShutdown, "isShutdown");
var awaitShutdown = /* @__PURE__ */ __name((self) => self.awaitShutdown, "awaitShutdown");
var shutdown = /* @__PURE__ */ __name((self) => self.shutdown, "shutdown");
var offer2 = /* @__PURE__ */ dual(2, (self, value2) => self.offer(value2));
var unsafeOffer = /* @__PURE__ */ dual(2, (self, value2) => self.unsafeOffer(value2));
var offerAll2 = /* @__PURE__ */ dual(2, (self, iterable) => self.offerAll(iterable));
var poll5 = /* @__PURE__ */ __name((self) => map14(self.takeUpTo(1), head3), "poll");
var take5 = /* @__PURE__ */ __name((self) => self.take, "take");
var takeAll = /* @__PURE__ */ __name((self) => self.takeAll, "takeAll");
var takeUpTo = /* @__PURE__ */ dual(2, (self, max10) => self.takeUpTo(max10));
var takeBetween = /* @__PURE__ */ dual(3, (self, min8, max10) => self.takeBetween(min8, max10));
var takeN = /* @__PURE__ */ dual(2, (self, n) => self.takeBetween(n, n));
var backPressureStrategy = /* @__PURE__ */ __name(() => new BackPressureStrategy(), "backPressureStrategy");
var droppingStrategy = /* @__PURE__ */ __name(() => new DroppingStrategy(), "droppingStrategy");
var slidingStrategy = /* @__PURE__ */ __name(() => new SlidingStrategy(), "slidingStrategy");
var BackPressureStrategy = class {
  static {
    __name(this, "BackPressureStrategy");
  }
  [QueueStrategyTypeId] = queueStrategyVariance;
  putters = /* @__PURE__ */ unbounded();
  surplusSize() {
    return length3(this.putters);
  }
  onCompleteTakersWithEmptyQueue(takers) {
    while (!isEmpty10(this.putters) && !isEmpty10(takers)) {
      const taker = poll2(takers, void 0);
      const putter = poll2(this.putters, void 0);
      if (putter[2]) {
        unsafeCompleteDeferred(putter[1], true);
      }
      unsafeCompleteDeferred(taker, putter[0]);
    }
  }
  get shutdown() {
    return pipe(fiberId, flatMap12((fiberId4) => pipe(sync(() => unsafePollAll(this.putters)), flatMap12((putters) => forEachConcurrentDiscard(putters, ([_, deferred, isLastItem]) => isLastItem ? pipe(deferredInterruptWith(deferred, fiberId4), asVoid2) : void_3, false, false)))));
  }
  handleSurplus(iterable, queue, takers, isShutdown9) {
    return withFiberRuntime((state) => {
      const deferred = deferredUnsafeMake(state.id());
      return pipe(suspend(() => {
        this.unsafeOffer(iterable, deferred);
        this.unsafeOnQueueEmptySpace(queue, takers);
        unsafeCompleteTakers(this, queue, takers);
        return get7(isShutdown9) ? interrupt2 : deferredAwait(deferred);
      }), onInterrupt(() => sync(() => this.unsafeRemove(deferred))));
    });
  }
  unsafeOnQueueEmptySpace(queue, takers) {
    let keepPolling = true;
    while (keepPolling && (queue.capacity() === Number.POSITIVE_INFINITY || queue.length() < queue.capacity())) {
      const putter = pipe(this.putters, poll2(EmptyMutableQueue));
      if (putter === EmptyMutableQueue) {
        keepPolling = false;
      } else {
        const offered = queue.offer(putter[0]);
        if (offered && putter[2]) {
          unsafeCompleteDeferred(putter[1], true);
        } else if (!offered) {
          unsafeOfferAll(this.putters, pipe(unsafePollAll(this.putters), prepend3(putter)));
        }
        unsafeCompleteTakers(this, queue, takers);
      }
    }
  }
  unsafeOffer(iterable, deferred) {
    const stuff = fromIterable2(iterable);
    for (let i = 0; i < stuff.length; i++) {
      const value2 = stuff[i];
      if (i === stuff.length - 1) {
        pipe(this.putters, offer([value2, deferred, true]));
      } else {
        pipe(this.putters, offer([value2, deferred, false]));
      }
    }
  }
  unsafeRemove(deferred) {
    unsafeOfferAll(this.putters, pipe(unsafePollAll(this.putters), filter5(([, _]) => _ !== deferred)));
  }
};
var DroppingStrategy = class {
  static {
    __name(this, "DroppingStrategy");
  }
  [QueueStrategyTypeId] = queueStrategyVariance;
  surplusSize() {
    return 0;
  }
  get shutdown() {
    return void_3;
  }
  onCompleteTakersWithEmptyQueue() {
  }
  handleSurplus(_iterable, _queue, _takers, _isShutdown) {
    return succeed(false);
  }
  unsafeOnQueueEmptySpace(_queue, _takers) {
  }
};
var SlidingStrategy = class {
  static {
    __name(this, "SlidingStrategy");
  }
  [QueueStrategyTypeId] = queueStrategyVariance;
  surplusSize() {
    return 0;
  }
  get shutdown() {
    return void_3;
  }
  onCompleteTakersWithEmptyQueue() {
  }
  handleSurplus(iterable, queue, takers, _isShutdown) {
    return sync(() => {
      this.unsafeOffer(queue, iterable);
      unsafeCompleteTakers(this, queue, takers);
      return true;
    });
  }
  unsafeOnQueueEmptySpace(_queue, _takers) {
  }
  unsafeOffer(queue, iterable) {
    const iterator = iterable[Symbol.iterator]();
    let next4;
    let offering = true;
    while (!(next4 = iterator.next()).done && offering) {
      if (queue.capacity() === 0) {
        return;
      }
      queue.poll(EmptyMutableQueue);
      offering = queue.offer(next4.value);
    }
  }
};
var unsafeCompleteDeferred = /* @__PURE__ */ __name((deferred, a) => {
  return deferredUnsafeDone(deferred, succeed(a));
}, "unsafeCompleteDeferred");
var unsafeOfferAll = /* @__PURE__ */ __name((queue, as15) => {
  return pipe(queue, offerAll(as15));
}, "unsafeOfferAll");
var unsafePollAll = /* @__PURE__ */ __name((queue) => {
  return pipe(queue, pollUpTo(Number.POSITIVE_INFINITY));
}, "unsafePollAll");
var unsafePollN = /* @__PURE__ */ __name((queue, max10) => {
  return pipe(queue, pollUpTo(max10));
}, "unsafePollN");
var unsafeRemove = /* @__PURE__ */ __name((queue, a) => {
  unsafeOfferAll(queue, pipe(unsafePollAll(queue), filter5((b) => a !== b)));
}, "unsafeRemove");
var unsafeCompleteTakers = /* @__PURE__ */ __name((strategy, queue, takers) => {
  let keepPolling = true;
  while (keepPolling && queue.length() !== 0) {
    const taker = pipe(takers, poll2(EmptyMutableQueue));
    if (taker !== EmptyMutableQueue) {
      const element = queue.poll(EmptyMutableQueue);
      if (element !== EmptyMutableQueue) {
        unsafeCompleteDeferred(taker, element);
        strategy.unsafeOnQueueEmptySpace(queue, takers);
      } else {
        unsafeOfferAll(takers, pipe(unsafePollAll(takers), prepend3(taker)));
      }
      keepPolling = true;
    } else {
      keepPolling = false;
    }
  }
  if (keepPolling && queue.length() === 0 && !isEmpty10(takers)) {
    strategy.onCompleteTakersWithEmptyQueue(takers);
  }
}, "unsafeCompleteTakers");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/pubsub.js
var AbsentValue = /* @__PURE__ */ Symbol.for("effect/PubSub/AbsentValue");
var addSubscribers = /* @__PURE__ */ __name((subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    subscribers.set(subscription, /* @__PURE__ */ new Set());
  }
  const set14 = subscribers.get(subscription);
  set14.add(pollers);
}, "addSubscribers");
var removeSubscribers = /* @__PURE__ */ __name((subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    return;
  }
  const set14 = subscribers.get(subscription);
  set14.delete(pollers);
  if (set14.size === 0) {
    subscribers.delete(subscription);
  }
}, "removeSubscribers");
var bounded3 = /* @__PURE__ */ __name((capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new BackPressureStrategy2());
}), "bounded");
var dropping2 = /* @__PURE__ */ __name((capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new DroppingStrategy2());
}), "dropping");
var sliding2 = /* @__PURE__ */ __name((capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new SlidingStrategy2());
}), "sliding");
var unbounded3 = /* @__PURE__ */ __name((options) => suspend(() => {
  const pubsub = makeUnboundedPubSub(options);
  return makePubSub(pubsub, new DroppingStrategy2());
}), "unbounded");
var capacity3 = /* @__PURE__ */ __name((self) => self.capacity(), "capacity");
var size18 = /* @__PURE__ */ __name((self) => self.size, "size");
var isFull3 = /* @__PURE__ */ __name((self) => self.isFull, "isFull");
var isEmpty16 = /* @__PURE__ */ __name((self) => self.isEmpty, "isEmpty");
var shutdown2 = /* @__PURE__ */ __name((self) => self.shutdown, "shutdown");
var isShutdown2 = /* @__PURE__ */ __name((self) => self.isShutdown, "isShutdown");
var awaitShutdown2 = /* @__PURE__ */ __name((self) => self.awaitShutdown, "awaitShutdown");
var publish = /* @__PURE__ */ dual(2, (self, value2) => self.publish(value2));
var publishAll = /* @__PURE__ */ dual(2, (self, elements) => self.publishAll(elements));
var subscribe = /* @__PURE__ */ __name((self) => self.subscribe, "subscribe");
var makeBoundedPubSub = /* @__PURE__ */ __name((capacity10) => {
  const options = typeof capacity10 === "number" ? {
    capacity: capacity10
  } : capacity10;
  ensureCapacity(options.capacity);
  const replayBuffer = options.replay && options.replay > 0 ? new ReplayBuffer(Math.ceil(options.replay)) : void 0;
  if (options.capacity === 1) {
    return new BoundedPubSubSingle(replayBuffer);
  } else if (nextPow2(options.capacity) === options.capacity) {
    return new BoundedPubSubPow2(options.capacity, replayBuffer);
  } else {
    return new BoundedPubSubArb(options.capacity, replayBuffer);
  }
}, "makeBoundedPubSub");
var makeUnboundedPubSub = /* @__PURE__ */ __name((options) => new UnboundedPubSub(options?.replay ? new ReplayBuffer(options.replay) : void 0), "makeUnboundedPubSub");
var makeSubscription = /* @__PURE__ */ __name((pubsub, subscribers, strategy) => map14(deferredMake(), (deferred) => unsafeMakeSubscription(pubsub, subscribers, pubsub.subscribe(), unbounded(), deferred, make13(false), strategy)), "makeSubscription");
var unsafeMakeSubscription = /* @__PURE__ */ __name((pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy) => new SubscriptionImpl(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, pubsub.replayWindow()), "unsafeMakeSubscription");
var BoundedPubSubArb = class {
  static {
    __name(this, "BoundedPubSubArb");
  }
  capacity;
  replayBuffer;
  array;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity10, replayBuffer) {
    this.capacity = capacity10;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity10
    });
    this.subscribers = Array.from({
      length: capacity10
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value2) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex % this.capacity;
      this.array[index] = value2;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value2);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty7();
    }
    const chunk4 = fromIterable3(elements);
    const n = chunk4.length;
    const size25 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size25;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk4;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const a = unsafeGet4(chunk4, iteratorIndex++);
      const index = this.publisherIndex % this.capacity;
      this.array[index] = a;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(a);
      }
    }
    return drop3(chunk4, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex % this.capacity;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubArbSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubArbSubscription = class {
  static {
    __name(this, "BoundedPubSubArbSubscription");
  }
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty7();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size25 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size25);
    if (toPoll <= 0) {
      return empty7();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const a = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(a);
      this.subscriberIndex += 1;
    }
    return fromIterable3(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex % this.self.capacity;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubPow2 = class {
  static {
    __name(this, "BoundedPubSubPow2");
  }
  capacity;
  replayBuffer;
  array;
  mask;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity10, replayBuffer) {
    this.capacity = capacity10;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity10
    });
    this.mask = capacity10 - 1;
    this.subscribers = Array.from({
      length: capacity10
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value2) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex & this.mask;
      this.array[index] = value2;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value2);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty7();
    }
    const chunk4 = fromIterable3(elements);
    const n = chunk4.length;
    const size25 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size25;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk4;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const elem = unsafeGet4(chunk4, iteratorIndex++);
      const index = this.publisherIndex & this.mask;
      this.array[index] = elem;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(elem);
      }
    }
    return drop3(chunk4, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex & this.mask;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubPow2Subscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubPow2Subscription = class {
  static {
    __name(this, "BoundedPubSubPow2Subscription");
  }
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty7();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size25 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size25);
    if (toPoll <= 0) {
      return empty7();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(elem);
      this.subscriberIndex += 1;
    }
    return fromIterable3(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex & this.self.mask;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubSingle = class {
  static {
    __name(this, "BoundedPubSubSingle");
  }
  replayBuffer;
  publisherIndex = 0;
  subscriberCount = 0;
  subscribers = 0;
  value = AbsentValue;
  capacity = 1;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  isEmpty() {
    return this.subscribers === 0;
  }
  isFull() {
    return !this.isEmpty();
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  publish(value2) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      this.value = value2;
      this.subscribers = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value2);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty7();
    }
    const chunk4 = fromIterable3(elements);
    if (isEmpty2(chunk4)) {
      return chunk4;
    }
    if (this.publish(unsafeHead2(chunk4))) {
      return drop3(chunk4, 1);
    } else {
      return chunk4;
    }
  }
  slide() {
    if (this.isFull()) {
      this.subscribers = 0;
      this.value = AbsentValue;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubSingleSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubSingleSubscription = class {
  static {
    __name(this, "BoundedPubSubSingleSubscription");
  }
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.subscribers === 0 || this.subscriberIndex === this.self.publisherIndex;
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  poll(default_) {
    if (this.isEmpty()) {
      return default_;
    }
    const elem = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return elem;
  }
  pollUpTo(n) {
    if (this.isEmpty() || n < 1) {
      return empty7();
    }
    const a = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return of3(a);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      if (this.subscriberIndex !== this.self.publisherIndex) {
        this.self.subscribers -= 1;
        if (this.self.subscribers === 0) {
          this.self.value = AbsentValue;
        }
      }
    }
  }
};
var UnboundedPubSub = class {
  static {
    __name(this, "UnboundedPubSub");
  }
  replayBuffer;
  publisherHead = {
    value: AbsentValue,
    subscribers: 0,
    next: null
  };
  publisherTail = this.publisherHead;
  publisherIndex = 0;
  subscribersIndex = 0;
  capacity = Number.MAX_SAFE_INTEGER;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherHead === this.publisherTail;
  }
  isFull() {
    return false;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value2) {
    const subscribers = this.publisherTail.subscribers;
    if (subscribers !== 0) {
      this.publisherTail.next = {
        value: value2,
        subscribers,
        next: null
      };
      this.publisherTail = this.publisherTail.next;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value2);
    }
    return true;
  }
  publishAll(elements) {
    if (this.publisherTail.subscribers !== 0) {
      for (const a of elements) {
        this.publish(a);
      }
    } else if (this.replayBuffer) {
      this.replayBuffer.offerAll(elements);
    }
    return empty7();
  }
  slide() {
    if (this.publisherHead !== this.publisherTail) {
      this.publisherHead = this.publisherHead.next;
      this.publisherHead.value = AbsentValue;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.publisherTail.subscribers += 1;
    return new UnboundedPubSubSubscription(this, this.publisherTail, this.publisherIndex, false);
  }
};
var UnboundedPubSubSubscription = class {
  static {
    __name(this, "UnboundedPubSubSubscription");
  }
  self;
  subscriberHead;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberHead, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberHead = subscriberHead;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    if (this.unsubscribed) {
      return true;
    }
    let empty46 = true;
    let loop4 = true;
    while (loop4) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop4 = false;
      } else {
        if (this.subscriberHead.next.value !== AbsentValue) {
          empty46 = false;
          loop4 = false;
        } else {
          this.subscriberHead = this.subscriberHead.next;
          this.subscriberIndex += 1;
        }
      }
    }
    return empty46;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    let loop4 = true;
    let polled = default_;
    while (loop4) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop4 = false;
      } else {
        const elem = this.subscriberHead.next.value;
        if (elem !== AbsentValue) {
          polled = elem;
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
          loop4 = false;
        }
        this.subscriberHead = this.subscriberHead.next;
        this.subscriberIndex += 1;
      }
    }
    return polled;
  }
  pollUpTo(n) {
    const builder = [];
    const default_ = AbsentValue;
    let i = 0;
    while (i !== n) {
      const a = this.poll(default_);
      if (a === default_) {
        i = n;
      } else {
        builder.push(a);
        i += 1;
      }
    }
    return fromIterable3(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.publisherTail.subscribers -= 1;
      while (this.subscriberHead !== this.self.publisherTail) {
        if (this.subscriberHead.next.value !== AbsentValue) {
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
        }
        this.subscriberHead = this.subscriberHead.next;
      }
    }
  }
};
var SubscriptionImpl = class extends Class3 {
  static {
    __name(this, "SubscriptionImpl");
  }
  pubsub;
  subscribers;
  subscription;
  pollers;
  shutdownHook;
  shutdownFlag;
  strategy;
  replayWindow;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, replayWindow) {
    super();
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.subscription = subscription;
    this.pollers = pollers;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
    this.replayWindow = replayWindow;
  }
  commit() {
    return this.take;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  capacity() {
    return this.pubsub.capacity;
  }
  isActive() {
    return !get7(this.shutdownFlag);
  }
  get size() {
    return suspend(() => get7(this.shutdownFlag) ? interrupt2 : succeed(this.subscription.size() + this.replayWindow.remaining));
  }
  unsafeSize() {
    if (get7(this.shutdownFlag)) {
      return none2();
    }
    return some3(this.subscription.size() + this.replayWindow.remaining);
  }
  get isFull() {
    return suspend(() => get7(this.shutdownFlag) ? interrupt2 : succeed(this.subscription.size() === this.capacity()));
  }
  get isEmpty() {
    return map14(this.size, (size25) => size25 === 0);
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      set3(this.shutdownFlag, true);
      return pipe(forEachParUnbounded(unsafePollAllQueue(this.pollers), (d) => deferredInterruptWith(d, state.id()), false), zipRight2(sync(() => {
        this.subscribers.delete(this.subscription);
        this.subscription.unsubscribe();
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      })), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid2);
    }));
  }
  get isShutdown() {
    return sync(() => get7(this.shutdownFlag));
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get take() {
    return withFiberRuntime((state) => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      if (this.replayWindow.remaining > 0) {
        const message2 = this.replayWindow.take();
        return succeed(message2);
      }
      const message = isEmpty10(this.pollers) ? this.subscription.poll(EmptyMutableQueue) : EmptyMutableQueue;
      if (message === EmptyMutableQueue) {
        const deferred = deferredUnsafeMake(state.id());
        return pipe(suspend(() => {
          pipe(this.pollers, offer(deferred));
          pipe(this.subscribers, addSubscribers(this.subscription, this.pollers));
          this.strategy.unsafeCompletePollers(this.pubsub, this.subscribers, this.subscription, this.pollers);
          return get7(this.shutdownFlag) ? interrupt2 : deferredAwait(deferred);
        }), onInterrupt(() => sync(() => unsafeRemove2(this.pollers, deferred))));
      } else {
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
        return succeed(message);
      }
    });
  }
  get takeAll() {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      const as15 = isEmpty10(this.pollers) ? unsafePollAllSubscription(this.subscription) : empty7();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      if (this.replayWindow.remaining > 0) {
        return succeed(appendAll3(this.replayWindow.takeAll(), as15));
      }
      return succeed(as15);
    });
  }
  takeUpTo(max10) {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      let replay = void 0;
      if (this.replayWindow.remaining >= max10) {
        const as16 = this.replayWindow.takeN(max10);
        return succeed(as16);
      } else if (this.replayWindow.remaining > 0) {
        replay = this.replayWindow.takeAll();
        max10 = max10 - replay.length;
      }
      const as15 = isEmpty10(this.pollers) ? unsafePollN2(this.subscription, max10) : empty7();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      return replay ? succeed(appendAll3(replay, as15)) : succeed(as15);
    });
  }
  takeBetween(min8, max10) {
    return suspend(() => takeRemainderLoop2(this, min8, max10, empty7()));
  }
};
var takeRemainderLoop2 = /* @__PURE__ */ __name((self, min8, max10, acc) => {
  if (max10 < min8) {
    return succeed(acc);
  }
  return pipe(self.takeUpTo(max10), flatMap12((bs) => {
    const remaining = min8 - bs.length;
    if (remaining === 1) {
      return pipe(self.take, map14((b) => pipe(acc, appendAll3(bs), append3(b))));
    }
    if (remaining > 1) {
      return pipe(self.take, flatMap12((b) => takeRemainderLoop2(self, remaining - 1, max10 - bs.length - 1, pipe(acc, appendAll3(bs), append3(b)))));
    }
    return succeed(pipe(acc, appendAll3(bs)));
  }));
}, "takeRemainderLoop");
var PubSubImpl = class {
  static {
    __name(this, "PubSubImpl");
  }
  pubsub;
  subscribers;
  scope;
  shutdownHook;
  shutdownFlag;
  strategy;
  [EnqueueTypeId] = enqueueVariance;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, scope6, shutdownHook, shutdownFlag, strategy) {
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.scope = scope6;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
  }
  capacity() {
    return this.pubsub.capacity;
  }
  get size() {
    return suspend(() => get7(this.shutdownFlag) ? interrupt2 : sync(() => this.pubsub.size()));
  }
  unsafeSize() {
    if (get7(this.shutdownFlag)) {
      return none2();
    }
    return some3(this.pubsub.size());
  }
  get isFull() {
    return map14(this.size, (size25) => size25 === this.capacity());
  }
  get isEmpty() {
    return map14(this.size, (size25) => size25 === 0);
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get isShutdown() {
    return sync(() => get7(this.shutdownFlag));
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      pipe(this.shutdownFlag, set3(true));
      return pipe(this.scope.close(exitInterrupt(state.id())), zipRight2(this.strategy.shutdown), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid2);
    }));
  }
  publish(value2) {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      if (this.pubsub.publish(value2)) {
        this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, of3(value2), this.shutdownFlag);
    });
  }
  isActive() {
    return !get7(this.shutdownFlag);
  }
  unsafeOffer(value2) {
    if (get7(this.shutdownFlag)) {
      return false;
    }
    if (this.pubsub.publish(value2)) {
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      return true;
    }
    return false;
  }
  publishAll(elements) {
    return suspend(() => {
      if (get7(this.shutdownFlag)) {
        return interrupt2;
      }
      const surplus = unsafePublishAll(this.pubsub, elements);
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      if (isEmpty2(surplus)) {
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, surplus, this.shutdownFlag);
    });
  }
  get subscribe() {
    const acquire = tap2(all8([this.scope.fork(sequential2), makeSubscription(this.pubsub, this.subscribers, this.strategy)]), (tuple5) => tuple5[0].addFinalizer(() => tuple5[1].shutdown));
    return map14(acquireRelease2(acquire, (tuple5, exit4) => tuple5[0].close(exit4)), (tuple5) => tuple5[1]);
  }
  offer(value2) {
    return this.publish(value2);
  }
  offerAll(elements) {
    return this.publishAll(elements);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makePubSub = /* @__PURE__ */ __name((pubsub, strategy) => flatMap12(scopeMake2(), (scope6) => map14(deferredMake(), (deferred) => unsafeMakePubSub(pubsub, /* @__PURE__ */ new Map(), scope6, deferred, make13(false), strategy))), "makePubSub");
var unsafeMakePubSub = /* @__PURE__ */ __name((pubsub, subscribers, scope6, shutdownHook, shutdownFlag, strategy) => new PubSubImpl(pubsub, subscribers, scope6, shutdownHook, shutdownFlag, strategy), "unsafeMakePubSub");
var ensureCapacity = /* @__PURE__ */ __name((capacity10) => {
  if (capacity10 <= 0) {
    throw new InvalidPubSubCapacityException(`Cannot construct PubSub with capacity of ${capacity10}`);
  }
}, "ensureCapacity");
var unsafeCompleteDeferred2 = /* @__PURE__ */ __name((deferred, a) => {
  deferredUnsafeDone(deferred, succeed(a));
}, "unsafeCompleteDeferred");
var unsafeOfferAll2 = /* @__PURE__ */ __name((queue, as15) => {
  return pipe(queue, offerAll(as15));
}, "unsafeOfferAll");
var unsafePollAllQueue = /* @__PURE__ */ __name((queue) => {
  return pipe(queue, pollUpTo(Number.POSITIVE_INFINITY));
}, "unsafePollAllQueue");
var unsafePollAllSubscription = /* @__PURE__ */ __name((subscription) => {
  return subscription.pollUpTo(Number.POSITIVE_INFINITY);
}, "unsafePollAllSubscription");
var unsafePollN2 = /* @__PURE__ */ __name((subscription, max10) => {
  return subscription.pollUpTo(max10);
}, "unsafePollN");
var unsafePublishAll = /* @__PURE__ */ __name((pubsub, as15) => {
  return pubsub.publishAll(as15);
}, "unsafePublishAll");
var unsafeRemove2 = /* @__PURE__ */ __name((queue, value2) => {
  unsafeOfferAll2(queue, pipe(unsafePollAllQueue(queue), filter5((elem) => elem !== value2)));
}, "unsafeRemove");
var BackPressureStrategy2 = class {
  static {
    __name(this, "BackPressureStrategy");
  }
  publishers = /* @__PURE__ */ unbounded();
  get shutdown() {
    return flatMap12(fiberId, (fiberId4) => flatMap12(sync(() => unsafePollAllQueue(this.publishers)), (publishers) => forEachConcurrentDiscard(publishers, ([_, deferred, last7]) => last7 ? pipe(deferredInterruptWith(deferred, fiberId4), asVoid2) : void_3, false, false)));
  }
  handleSurplus(pubsub, subscribers, elements, isShutdown9) {
    return withFiberRuntime((state) => {
      const deferred = deferredUnsafeMake(state.id());
      return pipe(suspend(() => {
        this.unsafeOffer(elements, deferred);
        this.unsafeOnPubSubEmptySpace(pubsub, subscribers);
        this.unsafeCompleteSubscribers(pubsub, subscribers);
        return get7(isShutdown9) ? interrupt2 : deferredAwait(deferred);
      }), onInterrupt(() => sync(() => this.unsafeRemove(deferred))));
    });
  }
  unsafeOnPubSubEmptySpace(pubsub, subscribers) {
    let keepPolling = true;
    while (keepPolling && !pubsub.isFull()) {
      const publisher = pipe(this.publishers, poll2(EmptyMutableQueue));
      if (publisher === EmptyMutableQueue) {
        keepPolling = false;
      } else {
        const published = pubsub.publish(publisher[0]);
        if (published && publisher[2]) {
          unsafeCompleteDeferred2(publisher[1], true);
        } else if (!published) {
          unsafeOfferAll2(this.publishers, pipe(unsafePollAllQueue(this.publishers), prepend3(publisher)));
        }
        this.unsafeCompleteSubscribers(pubsub, subscribers);
      }
    }
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeOffer(elements, deferred) {
    const iterator = elements[Symbol.iterator]();
    let next4 = iterator.next();
    if (!next4.done) {
      while (1) {
        const value2 = next4.value;
        next4 = iterator.next();
        if (next4.done) {
          pipe(this.publishers, offer([value2, deferred, true]));
          break;
        }
        pipe(this.publishers, offer([value2, deferred, false]));
      }
    }
  }
  unsafeRemove(deferred) {
    unsafeOfferAll2(this.publishers, pipe(unsafePollAllQueue(this.publishers), filter5(([_, a]) => a !== deferred)));
  }
};
var DroppingStrategy2 = class {
  static {
    __name(this, "DroppingStrategy");
  }
  get shutdown() {
    return void_3;
  }
  handleSurplus(_pubsub, _subscribers, _elements, _isShutdown) {
    return succeed(false);
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
};
var SlidingStrategy2 = class {
  static {
    __name(this, "SlidingStrategy");
  }
  get shutdown() {
    return void_3;
  }
  handleSurplus(pubsub, subscribers, elements, _isShutdown) {
    return sync(() => {
      this.unsafeSlidingPublish(pubsub, elements);
      this.unsafeCompleteSubscribers(pubsub, subscribers);
      return true;
    });
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeSlidingPublish(pubsub, elements) {
    const it = elements[Symbol.iterator]();
    let next4 = it.next();
    if (!next4.done && pubsub.capacity > 0) {
      let a = next4.value;
      let loop4 = true;
      while (loop4) {
        pubsub.slide();
        const pub = pubsub.publish(a);
        if (pub && (next4 = it.next()) && !next4.done) {
          a = next4.value;
        } else if (pub) {
          loop4 = false;
        }
      }
    }
  }
};
var unsafeStrategyCompletePollers = /* @__PURE__ */ __name((strategy, pubsub, subscribers, subscription, pollers) => {
  let keepPolling = true;
  while (keepPolling && !subscription.isEmpty()) {
    const poller = pipe(pollers, poll2(EmptyMutableQueue));
    if (poller === EmptyMutableQueue) {
      pipe(subscribers, removeSubscribers(subscription, pollers));
      if (isEmpty10(pollers)) {
        keepPolling = false;
      } else {
        pipe(subscribers, addSubscribers(subscription, pollers));
      }
    } else {
      const pollResult = subscription.poll(EmptyMutableQueue);
      if (pollResult === EmptyMutableQueue) {
        unsafeOfferAll2(pollers, pipe(unsafePollAllQueue(pollers), prepend3(poller)));
      } else {
        unsafeCompleteDeferred2(poller, pollResult);
        strategy.unsafeOnPubSubEmptySpace(pubsub, subscribers);
      }
    }
  }
}, "unsafeStrategyCompletePollers");
var unsafeStrategyCompleteSubscribers = /* @__PURE__ */ __name((strategy, pubsub, subscribers) => {
  for (const [subscription, pollersSet] of subscribers) {
    for (const pollers of pollersSet) {
      strategy.unsafeCompletePollers(pubsub, subscribers, subscription, pollers);
    }
  }
}, "unsafeStrategyCompleteSubscribers");
var ReplayBuffer = class {
  static {
    __name(this, "ReplayBuffer");
  }
  capacity;
  constructor(capacity10) {
    this.capacity = capacity10;
  }
  head = {
    value: AbsentValue,
    next: null
  };
  tail = this.head;
  size = 0;
  index = 0;
  slide() {
    this.index++;
  }
  offer(a) {
    this.tail.value = a;
    this.tail.next = {
      value: AbsentValue,
      next: null
    };
    this.tail = this.tail.next;
    if (this.size === this.capacity) {
      this.head = this.head.next;
    } else {
      this.size += 1;
    }
  }
  offerAll(as15) {
    for (const a of as15) {
      this.offer(a);
    }
  }
};
var ReplayWindowImpl = class {
  static {
    __name(this, "ReplayWindowImpl");
  }
  buffer;
  head;
  index;
  remaining;
  constructor(buffer4) {
    this.buffer = buffer4;
    this.index = buffer4.index;
    this.remaining = buffer4.size;
    this.head = buffer4.head;
  }
  fastForward() {
    while (this.index < this.buffer.index) {
      this.head = this.head.next;
      this.index++;
    }
  }
  take() {
    if (this.remaining === 0) {
      return void 0;
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    this.remaining--;
    const value2 = this.head.value;
    this.head = this.head.next;
    return value2;
  }
  takeN(n) {
    if (this.remaining === 0) {
      return empty7();
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    const len = Math.min(n, this.remaining);
    const items = new Array(len);
    for (let i = 0; i < len; i++) {
      const value2 = this.head.value;
      this.head = this.head.next;
      items[i] = value2;
    }
    this.remaining -= len;
    return unsafeFromArray(items);
  }
  takeAll() {
    return this.takeN(this.remaining);
  }
};
var emptyReplayWindow = {
  remaining: 0,
  take: /* @__PURE__ */ __name(() => void 0, "take"),
  takeN: /* @__PURE__ */ __name(() => empty7(), "takeN"),
  takeAll: /* @__PURE__ */ __name(() => empty7(), "takeAll")
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/PubSub.js
var bounded4 = bounded3;
var dropping3 = dropping2;
var sliding3 = sliding2;
var unbounded4 = unbounded3;
var capacity4 = capacity3;
var size19 = size18;
var isFull4 = isFull3;
var isEmpty17 = isEmpty16;
var shutdown3 = shutdown2;
var isShutdown3 = isShutdown2;
var awaitShutdown3 = awaitShutdown2;
var publish2 = publish;
var publishAll2 = publishAll;
var subscribe2 = subscribe;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Queue.js
var EnqueueTypeId2 = EnqueueTypeId;
var DequeueTypeId2 = DequeueTypeId;
var QueueStrategyTypeId2 = QueueStrategyTypeId;
var BackingQueueTypeId2 = BackingQueueTypeId;
var isQueue2 = isQueue;
var isDequeue2 = isDequeue;
var isEnqueue2 = isEnqueue;
var backPressureStrategy2 = backPressureStrategy;
var droppingStrategy2 = droppingStrategy;
var slidingStrategy2 = slidingStrategy;
var make59 = make58;
var bounded5 = bounded2;
var dropping4 = dropping;
var sliding4 = sliding;
var unbounded5 = unbounded2;
var capacity5 = capacity2;
var size20 = size17;
var isEmpty18 = isEmpty15;
var isFull5 = isFull2;
var isShutdown4 = isShutdown;
var awaitShutdown4 = awaitShutdown;
var shutdown4 = shutdown;
var offer3 = offer2;
var unsafeOffer2 = unsafeOffer;
var offerAll3 = offerAll2;
var poll6 = poll5;
var take6 = take5;
var takeAll2 = takeAll;
var takeUpTo2 = takeUpTo;
var takeBetween2 = takeBetween;
var takeN2 = takeN;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelChildExecutorDecision.js
var OP_CONTINUE2 = "Continue";
var OP_CLOSE = "Close";
var OP_YIELD2 = "Yield";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/childExecutorDecision.js
var ChildExecutorDecisionSymbolKey = "effect/ChannelChildExecutorDecision";
var ChildExecutorDecisionTypeId = /* @__PURE__ */ Symbol.for(ChildExecutorDecisionSymbolKey);
var proto4 = {
  [ChildExecutorDecisionTypeId]: ChildExecutorDecisionTypeId
};
var Continue = /* @__PURE__ */ __name((_) => {
  const op = Object.create(proto4);
  op._tag = OP_CONTINUE2;
  return op;
}, "Continue");
var Close = /* @__PURE__ */ __name((value2) => {
  const op = Object.create(proto4);
  op._tag = OP_CLOSE;
  op.value = value2;
  return op;
}, "Close");
var Yield2 = /* @__PURE__ */ __name((_) => {
  const op = Object.create(proto4);
  op._tag = OP_YIELD2;
  return op;
}, "Yield");
var isChildExecutorDecision = /* @__PURE__ */ __name((u) => hasProperty(u, ChildExecutorDecisionTypeId), "isChildExecutorDecision");
var isContinue3 = /* @__PURE__ */ __name((self) => self._tag === OP_CONTINUE2, "isContinue");
var isClose = /* @__PURE__ */ __name((self) => self._tag === OP_CLOSE, "isClose");
var isYield = /* @__PURE__ */ __name((self) => self._tag === OP_YIELD2, "isYield");
var match22 = /* @__PURE__ */ dual(2, (self, {
  onClose,
  onContinue,
  onYield
}) => {
  switch (self._tag) {
    case OP_CONTINUE2: {
      return onContinue();
    }
    case OP_CLOSE: {
      return onClose(self.value);
    }
    case OP_YIELD2: {
      return onYield();
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/continuation.js
var OP_CONTINUATION_K = "ContinuationK";
var OP_CONTINUATION_FINALIZER = "ContinuationFinalizer";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/continuation.js
var ContinuationTypeId = /* @__PURE__ */ Symbol.for("effect/ChannelContinuation");
var continuationVariance = {
  /* c8 ignore next */
  _Env: /* @__PURE__ */ __name((_) => _, "_Env"),
  /* c8 ignore next */
  _InErr: /* @__PURE__ */ __name((_) => _, "_InErr"),
  /* c8 ignore next */
  _InElem: /* @__PURE__ */ __name((_) => _, "_InElem"),
  /* c8 ignore next */
  _InDone: /* @__PURE__ */ __name((_) => _, "_InDone"),
  /* c8 ignore next */
  _OutErr: /* @__PURE__ */ __name((_) => _, "_OutErr"),
  /* c8 ignore next */
  _OutDone: /* @__PURE__ */ __name((_) => _, "_OutDone"),
  /* c8 ignore next */
  _OutErr2: /* @__PURE__ */ __name((_) => _, "_OutErr2"),
  /* c8 ignore next */
  _OutElem: /* @__PURE__ */ __name((_) => _, "_OutElem"),
  /* c8 ignore next */
  _OutDone2: /* @__PURE__ */ __name((_) => _, "_OutDone2")
};
var ContinuationKImpl = class {
  static {
    __name(this, "ContinuationKImpl");
  }
  onSuccess;
  onHalt;
  _tag = OP_CONTINUATION_K;
  [ContinuationTypeId] = continuationVariance;
  constructor(onSuccess, onHalt) {
    this.onSuccess = onSuccess;
    this.onHalt = onHalt;
  }
  onExit(exit4) {
    return isFailure2(exit4) ? this.onHalt(exit4.cause) : this.onSuccess(exit4.value);
  }
};
var ContinuationFinalizerImpl = class {
  static {
    __name(this, "ContinuationFinalizerImpl");
  }
  finalizer;
  _tag = OP_CONTINUATION_FINALIZER;
  [ContinuationTypeId] = continuationVariance;
  constructor(finalizer3) {
    this.finalizer = finalizer3;
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullStrategy.js
var OP_PULL_AFTER_NEXT = "PullAfterNext";
var OP_PULL_AFTER_ALL_ENQUEUED = "PullAfterAllEnqueued";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/upstreamPullStrategy.js
var UpstreamPullStrategySymbolKey = "effect/ChannelUpstreamPullStrategy";
var UpstreamPullStrategyTypeId = /* @__PURE__ */ Symbol.for(UpstreamPullStrategySymbolKey);
var upstreamPullStrategyVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var proto5 = {
  [UpstreamPullStrategyTypeId]: upstreamPullStrategyVariance
};
var PullAfterNext = /* @__PURE__ */ __name((emitSeparator) => {
  const op = Object.create(proto5);
  op._tag = OP_PULL_AFTER_NEXT;
  op.emitSeparator = emitSeparator;
  return op;
}, "PullAfterNext");
var PullAfterAllEnqueued = /* @__PURE__ */ __name((emitSeparator) => {
  const op = Object.create(proto5);
  op._tag = OP_PULL_AFTER_ALL_ENQUEUED;
  op.emitSeparator = emitSeparator;
  return op;
}, "PullAfterAllEnqueued");
var isUpstreamPullStrategy = /* @__PURE__ */ __name((u) => hasProperty(u, UpstreamPullStrategyTypeId), "isUpstreamPullStrategy");
var isPullAfterNext = /* @__PURE__ */ __name((self) => self._tag === OP_PULL_AFTER_NEXT, "isPullAfterNext");
var isPullAfterAllEnqueued = /* @__PURE__ */ __name((self) => self._tag === OP_PULL_AFTER_ALL_ENQUEUED, "isPullAfterAllEnqueued");
var match23 = /* @__PURE__ */ dual(2, (self, {
  onAllEnqueued,
  onNext
}) => {
  switch (self._tag) {
    case OP_PULL_AFTER_NEXT: {
      return onNext(self.emitSeparator);
    }
    case OP_PULL_AFTER_ALL_ENQUEUED: {
      return onAllEnqueued(self.emitSeparator);
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channel.js
var OP_BRACKET_OUT = "BracketOut";
var OP_BRIDGE = "Bridge";
var OP_CONCAT_ALL = "ConcatAll";
var OP_EMIT = "Emit";
var OP_ENSURING = "Ensuring";
var OP_FAIL3 = "Fail";
var OP_FOLD2 = "Fold";
var OP_FROM_EFFECT2 = "FromEffect";
var OP_PIPE_TO = "PipeTo";
var OP_PROVIDE2 = "Provide";
var OP_READ = "Read";
var OP_SUCCEED = "Succeed";
var OP_SUCCEED_NOW = "SucceedNow";
var OP_SUSPEND2 = "Suspend";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/core-stream.js
var ChannelSymbolKey = "effect/Channel";
var ChannelTypeId3 = /* @__PURE__ */ Symbol.for(ChannelSymbolKey);
var channelVariance2 = {
  /* c8 ignore next */
  _Env: /* @__PURE__ */ __name((_) => _, "_Env"),
  /* c8 ignore next */
  _InErr: /* @__PURE__ */ __name((_) => _, "_InErr"),
  /* c8 ignore next */
  _InElem: /* @__PURE__ */ __name((_) => _, "_InElem"),
  /* c8 ignore next */
  _InDone: /* @__PURE__ */ __name((_) => _, "_InDone"),
  /* c8 ignore next */
  _OutErr: /* @__PURE__ */ __name((_) => _, "_OutErr"),
  /* c8 ignore next */
  _OutElem: /* @__PURE__ */ __name((_) => _, "_OutElem"),
  /* c8 ignore next */
  _OutDone: /* @__PURE__ */ __name((_) => _, "_OutDone")
};
var proto6 = {
  [ChannelTypeId3]: channelVariance2,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isChannel = /* @__PURE__ */ __name((u) => hasProperty(u, ChannelTypeId3) || isEffect2(u), "isChannel");
var acquireReleaseOut = /* @__PURE__ */ dual(2, (self, release) => {
  const op = Object.create(proto6);
  op._tag = OP_BRACKET_OUT;
  op.acquire = () => self;
  op.finalizer = release;
  return op;
});
var catchAllCause6 = /* @__PURE__ */ dual(2, (self, f) => {
  const op = Object.create(proto6);
  op._tag = OP_FOLD2;
  op.channel = self;
  op.k = new ContinuationKImpl(succeed15, f);
  return op;
});
var collectElements = /* @__PURE__ */ __name((self) => {
  return suspend6(() => {
    const builder = [];
    return flatMap20(pipeTo(self, collectElementsReader(builder)), (value2) => sync10(() => [fromIterable3(builder), value2]));
  });
}, "collectElements");
var collectElementsReader = /* @__PURE__ */ __name((builder) => readWith({
  onInput: /* @__PURE__ */ __name((outElem) => flatMap20(sync10(() => {
    builder.push(outElem);
  }), () => collectElementsReader(builder)), "onInput"),
  onFailure: fail14,
  onDone: succeedNow
}), "collectElementsReader");
var concatAll = /* @__PURE__ */ __name((channels) => concatAllWith(channels, constVoid, constVoid), "concatAll");
var concatAllWith = /* @__PURE__ */ __name((channels, f, g) => {
  const op = Object.create(proto6);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = f;
  op.combineAll = g;
  op.onPull = () => PullAfterNext(none2());
  op.onEmit = () => Continue;
  op.value = () => channels;
  op.k = identity;
  return op;
}, "concatAllWith");
var concatMapWith = /* @__PURE__ */ dual(4, (self, f, g, h) => {
  const op = Object.create(proto6);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = () => PullAfterNext(none2());
  op.onEmit = () => Continue;
  op.value = () => self;
  op.k = f;
  return op;
});
var concatMapWithCustom = /* @__PURE__ */ dual(6, (self, f, g, h, onPull, onEmit) => {
  const op = Object.create(proto6);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = onPull;
  op.onEmit = onEmit;
  op.value = () => self;
  op.k = f;
  return op;
});
var embedInput = /* @__PURE__ */ dual(2, (self, input) => {
  const op = Object.create(proto6);
  op._tag = OP_BRIDGE;
  op.input = input;
  op.channel = self;
  return op;
});
var ensuringWith = /* @__PURE__ */ dual(2, (self, finalizer3) => {
  const op = Object.create(proto6);
  op._tag = OP_ENSURING;
  op.channel = self;
  op.finalizer = finalizer3;
  return op;
});
var fail14 = /* @__PURE__ */ __name((error2) => failCause12(fail8(error2)), "fail");
var failSync7 = /* @__PURE__ */ __name((evaluate3) => failCauseSync7(() => fail8(evaluate3())), "failSync");
var failCause12 = /* @__PURE__ */ __name((cause3) => failCauseSync7(() => cause3), "failCause");
var failCauseSync7 = /* @__PURE__ */ __name((evaluate3) => {
  const op = Object.create(proto6);
  op._tag = OP_FAIL3;
  op.error = evaluate3;
  return op;
}, "failCauseSync");
var flatMap20 = /* @__PURE__ */ dual(2, (self, f) => {
  const op = Object.create(proto6);
  op._tag = OP_FOLD2;
  op.channel = self;
  op.k = new ContinuationKImpl(f, failCause12);
  return op;
});
var foldCauseChannel = /* @__PURE__ */ dual(2, (self, options) => {
  const op = Object.create(proto6);
  op._tag = OP_FOLD2;
  op.channel = self;
  op.k = new ContinuationKImpl(options.onSuccess, options.onFailure);
  return op;
});
var fromEffect5 = /* @__PURE__ */ __name((effect4) => {
  const op = Object.create(proto6);
  op._tag = OP_FROM_EFFECT2;
  op.effect = () => effect4;
  return op;
}, "fromEffect");
var pipeTo = /* @__PURE__ */ dual(2, (self, that) => {
  const op = Object.create(proto6);
  op._tag = OP_PIPE_TO;
  op.left = () => self;
  op.right = () => that;
  return op;
});
var provideContext4 = /* @__PURE__ */ dual(2, (self, env) => {
  const op = Object.create(proto6);
  op._tag = OP_PROVIDE2;
  op.context = () => env;
  op.inner = self;
  return op;
});
var readOrFail = /* @__PURE__ */ __name((error2) => {
  const op = Object.create(proto6);
  op._tag = OP_READ;
  op.more = succeed15;
  op.done = new ContinuationKImpl(() => fail14(error2), () => fail14(error2));
  return op;
}, "readOrFail");
var readWith = /* @__PURE__ */ __name((options) => readWithCause({
  onInput: options.onInput,
  onFailure: /* @__PURE__ */ __name((cause3) => match(failureOrCause2(cause3), {
    onLeft: options.onFailure,
    onRight: failCause12
  }), "onFailure"),
  onDone: options.onDone
}), "readWith");
var readWithCause = /* @__PURE__ */ __name((options) => {
  const op = Object.create(proto6);
  op._tag = OP_READ;
  op.more = options.onInput;
  op.done = new ContinuationKImpl(options.onDone, options.onFailure);
  return op;
}, "readWithCause");
var succeed15 = /* @__PURE__ */ __name((value2) => sync10(() => value2), "succeed");
var succeedNow = /* @__PURE__ */ __name((result) => {
  const op = Object.create(proto6);
  op._tag = OP_SUCCEED_NOW;
  op.terminal = result;
  return op;
}, "succeedNow");
var suspend6 = /* @__PURE__ */ __name((evaluate3) => {
  const op = Object.create(proto6);
  op._tag = OP_SUSPEND2;
  op.channel = evaluate3;
  return op;
}, "suspend");
var sync10 = /* @__PURE__ */ __name((evaluate3) => {
  const op = Object.create(proto6);
  op._tag = OP_SUCCEED;
  op.evaluate = evaluate3;
  return op;
}, "sync");
var void_8 = /* @__PURE__ */ succeedNow(void 0);
var write = /* @__PURE__ */ __name((out) => {
  const op = Object.create(proto6);
  op._tag = OP_EMIT;
  op.out = out;
  return op;
}, "write");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelState.js
var OP_DONE3 = "Done";
var OP_EMIT2 = "Emit";
var OP_FROM_EFFECT3 = "FromEffect";
var OP_READ2 = "Read";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/channelState.js
var ChannelStateTypeId = /* @__PURE__ */ Symbol.for("effect/ChannelState");
var channelStateVariance = {
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var proto7 = {
  [ChannelStateTypeId]: channelStateVariance
};
var Done2 = /* @__PURE__ */ __name(() => {
  const op = Object.create(proto7);
  op._tag = OP_DONE3;
  return op;
}, "Done");
var Emit = /* @__PURE__ */ __name(() => {
  const op = Object.create(proto7);
  op._tag = OP_EMIT2;
  return op;
}, "Emit");
var fromEffect6 = /* @__PURE__ */ __name((effect4) => {
  const op = Object.create(proto7);
  op._tag = OP_FROM_EFFECT3;
  op.effect = effect4;
  return op;
}, "fromEffect");
var Read = /* @__PURE__ */ __name((upstream, onEffect, onEmit, onDone3) => {
  const op = Object.create(proto7);
  op._tag = OP_READ2;
  op.upstream = upstream;
  op.onEffect = onEffect;
  op.onEmit = onEmit;
  op.onDone = onDone3;
  return op;
}, "Read");
var isChannelState = /* @__PURE__ */ __name((u) => hasProperty(u, ChannelStateTypeId), "isChannelState");
var isDone6 = /* @__PURE__ */ __name((self) => self._tag === OP_DONE3, "isDone");
var isEmit = /* @__PURE__ */ __name((self) => self._tag === OP_EMIT2, "isEmit");
var isFromEffect = /* @__PURE__ */ __name((self) => self._tag === OP_FROM_EFFECT3, "isFromEffect");
var isRead = /* @__PURE__ */ __name((self) => self._tag === OP_READ2, "isRead");
var effect2 = /* @__PURE__ */ __name((self) => isFromEffect(self) ? self.effect : _void, "effect");
var effectOrUndefinedIgnored = /* @__PURE__ */ __name((self) => isFromEffect(self) ? ignore3(self.effect) : void 0, "effectOrUndefinedIgnored");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/subexecutor.js
var OP_PULL_FROM_CHILD = "PullFromChild";
var OP_PULL_FROM_UPSTREAM = "PullFromUpstream";
var OP_DRAIN_CHILD_EXECUTORS = "DrainChildExecutors";
var OP_EMIT3 = "Emit";
var PullFromChild = class {
  static {
    __name(this, "PullFromChild");
  }
  childExecutor;
  parentSubexecutor;
  onEmit;
  _tag = OP_PULL_FROM_CHILD;
  constructor(childExecutor, parentSubexecutor, onEmit) {
    this.childExecutor = childExecutor;
    this.parentSubexecutor = parentSubexecutor;
    this.onEmit = onEmit;
  }
  close(exit4) {
    const fin1 = this.childExecutor.close(exit4);
    const fin2 = this.parentSubexecutor.close(exit4);
    if (fin1 !== void 0 && fin2 !== void 0) {
      return zipWith12(exit3(fin1), exit3(fin2), (exit1, exit22) => pipe(exit1, zipRight3(exit22)));
    } else if (fin1 !== void 0) {
      return fin1;
    } else if (fin2 !== void 0) {
      return fin2;
    } else {
      return void 0;
    }
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};
var PullFromUpstream = class _PullFromUpstream {
  static {
    __name(this, "PullFromUpstream");
  }
  upstreamExecutor;
  createChild;
  lastDone;
  activeChildExecutors;
  combineChildResults;
  combineWithChildResult;
  onPull;
  onEmit;
  _tag = OP_PULL_FROM_UPSTREAM;
  constructor(upstreamExecutor, createChild, lastDone, activeChildExecutors, combineChildResults, combineWithChildResult, onPull, onEmit) {
    this.upstreamExecutor = upstreamExecutor;
    this.createChild = createChild;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
    this.onEmit = onEmit;
  }
  close(exit4) {
    const fin1 = this.upstreamExecutor.close(exit4);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit4) : void 0), fin1];
    const result = fins.reduce((acc, next4) => {
      if (acc !== void 0 && next4 !== void 0) {
        return zipWith12(acc, exit3(next4), (exit1, exit22) => zipRight3(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next4 !== void 0) {
        return exit3(next4);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _PullFromUpstream(this.upstreamExecutor, this.createChild, this.lastDone, [...this.activeChildExecutors, child], this.combineChildResults, this.combineWithChildResult, this.onPull, this.onEmit);
  }
};
var DrainChildExecutors = class _DrainChildExecutors {
  static {
    __name(this, "DrainChildExecutors");
  }
  upstreamExecutor;
  lastDone;
  activeChildExecutors;
  upstreamDone;
  combineChildResults;
  combineWithChildResult;
  onPull;
  _tag = OP_DRAIN_CHILD_EXECUTORS;
  constructor(upstreamExecutor, lastDone, activeChildExecutors, upstreamDone, combineChildResults, combineWithChildResult, onPull) {
    this.upstreamExecutor = upstreamExecutor;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.upstreamDone = upstreamDone;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
  }
  close(exit4) {
    const fin1 = this.upstreamExecutor.close(exit4);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit4) : void 0), fin1];
    const result = fins.reduce((acc, next4) => {
      if (acc !== void 0 && next4 !== void 0) {
        return zipWith12(acc, exit3(next4), (exit1, exit22) => zipRight3(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next4 !== void 0) {
        return exit3(next4);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _DrainChildExecutors(this.upstreamExecutor, this.lastDone, [...this.activeChildExecutors, child], this.upstreamDone, this.combineChildResults, this.combineWithChildResult, this.onPull);
  }
};
var Emit2 = class {
  static {
    __name(this, "Emit");
  }
  value;
  next;
  _tag = OP_EMIT3;
  constructor(value2, next4) {
    this.value = value2;
    this.next = next4;
  }
  close(exit4) {
    const result = this.next.close(exit4);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullRequest.js
var OP_PULLED = "Pulled";
var OP_NO_UPSTREAM = "NoUpstream";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/upstreamPullRequest.js
var UpstreamPullRequestSymbolKey = "effect/ChannelUpstreamPullRequest";
var UpstreamPullRequestTypeId = /* @__PURE__ */ Symbol.for(UpstreamPullRequestSymbolKey);
var upstreamPullRequestVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var proto8 = {
  [UpstreamPullRequestTypeId]: upstreamPullRequestVariance
};
var Pulled = /* @__PURE__ */ __name((value2) => {
  const op = Object.create(proto8);
  op._tag = OP_PULLED;
  op.value = value2;
  return op;
}, "Pulled");
var NoUpstream = /* @__PURE__ */ __name((activeDownstreamCount) => {
  const op = Object.create(proto8);
  op._tag = OP_NO_UPSTREAM;
  op.activeDownstreamCount = activeDownstreamCount;
  return op;
}, "NoUpstream");
var isUpstreamPullRequest = /* @__PURE__ */ __name((u) => hasProperty(u, UpstreamPullRequestTypeId), "isUpstreamPullRequest");
var isPulled = /* @__PURE__ */ __name((self) => self._tag === OP_PULLED, "isPulled");
var isNoUpstream = /* @__PURE__ */ __name((self) => self._tag === OP_NO_UPSTREAM, "isNoUpstream");
var match24 = /* @__PURE__ */ dual(2, (self, {
  onNoUpstream,
  onPulled
}) => {
  switch (self._tag) {
    case OP_PULLED: {
      return onPulled(self.value);
    }
    case OP_NO_UPSTREAM: {
      return onNoUpstream(self.activeDownstreamCount);
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/channelExecutor.js
var ChannelExecutor = class _ChannelExecutor {
  static {
    __name(this, "ChannelExecutor");
  }
  _activeSubexecutor = void 0;
  _cancelled = void 0;
  _closeLastSubstream = void 0;
  _currentChannel;
  _done = void 0;
  _doneStack = [];
  _emitted = void 0;
  _executeCloseLastSubstream;
  _input = void 0;
  _inProgressFinalizer = void 0;
  _providedEnv;
  constructor(initialChannel, providedEnv, executeCloseLastSubstream) {
    this._currentChannel = initialChannel;
    this._executeCloseLastSubstream = executeCloseLastSubstream;
    this._providedEnv = providedEnv;
  }
  run() {
    let result = void 0;
    while (result === void 0) {
      if (this._cancelled !== void 0) {
        result = this.processCancellation();
      } else if (this._activeSubexecutor !== void 0) {
        result = this.runSubexecutor();
      } else {
        try {
          if (this._currentChannel === void 0) {
            result = Done2();
          } else {
            if (isEffect2(this._currentChannel)) {
              this._currentChannel = fromEffect5(this._currentChannel);
            }
            switch (this._currentChannel._tag) {
              case OP_BRACKET_OUT: {
                result = this.runBracketOut(this._currentChannel);
                break;
              }
              case OP_BRIDGE: {
                const bridgeInput = this._currentChannel.input;
                this._currentChannel = this._currentChannel.channel;
                if (this._input !== void 0) {
                  const inputExecutor = this._input;
                  this._input = void 0;
                  const drainer = /* @__PURE__ */ __name(() => flatMap18(bridgeInput.awaitRead(), () => suspend4(() => {
                    const state = inputExecutor.run();
                    switch (state._tag) {
                      case OP_DONE3: {
                        return match7(inputExecutor.getDone(), {
                          onFailure: /* @__PURE__ */ __name((cause3) => bridgeInput.error(cause3), "onFailure"),
                          onSuccess: /* @__PURE__ */ __name((value2) => bridgeInput.done(value2), "onSuccess")
                        });
                      }
                      case OP_EMIT2: {
                        return flatMap18(bridgeInput.emit(inputExecutor.getEmit()), () => drainer());
                      }
                      case OP_FROM_EFFECT3: {
                        return matchCauseEffect3(state.effect, {
                          onFailure: /* @__PURE__ */ __name((cause3) => bridgeInput.error(cause3), "onFailure"),
                          onSuccess: /* @__PURE__ */ __name(() => drainer(), "onSuccess")
                        });
                      }
                      case OP_READ2: {
                        return readUpstream(state, () => drainer(), (cause3) => bridgeInput.error(cause3));
                      }
                    }
                  })), "drainer");
                  result = fromEffect6(flatMap18(forkDaemon3(interruptible4(drainer())), (fiber) => sync8(() => this.addFinalizer((exit4) => flatMap18(interrupt7(fiber), () => suspend4(() => {
                    const effect4 = this.restorePipe(exit4, inputExecutor);
                    return effect4 !== void 0 ? effect4 : _void;
                  }))))));
                }
                break;
              }
              case OP_CONCAT_ALL: {
                const executor = new _ChannelExecutor(this._currentChannel.value(), this._providedEnv, (effect4) => sync8(() => {
                  const prevLastClose = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
                  this._closeLastSubstream = pipe(prevLastClose, zipRight7(effect4));
                }));
                executor._input = this._input;
                const channel = this._currentChannel;
                this._activeSubexecutor = new PullFromUpstream(executor, (value2) => channel.k(value2), void 0, [], (x, y) => channel.combineInners(x, y), (x, y) => channel.combineAll(x, y), (request2) => channel.onPull(request2), (value2) => channel.onEmit(value2));
                this._closeLastSubstream = void 0;
                this._currentChannel = void 0;
                break;
              }
              case OP_EMIT: {
                this._emitted = this._currentChannel.out;
                this._currentChannel = this._activeSubexecutor !== void 0 ? void 0 : void_8;
                result = Emit();
                break;
              }
              case OP_ENSURING: {
                this.runEnsuring(this._currentChannel);
                break;
              }
              case OP_FAIL3: {
                result = this.doneHalt(this._currentChannel.error());
                break;
              }
              case OP_FOLD2: {
                this._doneStack.push(this._currentChannel.k);
                this._currentChannel = this._currentChannel.channel;
                break;
              }
              case OP_FROM_EFFECT2: {
                const effect4 = this._providedEnv === void 0 ? this._currentChannel.effect() : pipe(this._currentChannel.effect(), provide2(this._providedEnv));
                result = fromEffect6(matchCauseEffect3(effect4, {
                  onFailure: /* @__PURE__ */ __name((cause3) => {
                    const state = this.doneHalt(cause3);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  }, "onFailure"),
                  onSuccess: /* @__PURE__ */ __name((value2) => {
                    const state = this.doneSucceed(value2);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  }, "onSuccess")
                }));
                break;
              }
              case OP_PIPE_TO: {
                const previousInput = this._input;
                const leftExec = new _ChannelExecutor(this._currentChannel.left(), this._providedEnv, (effect4) => this._executeCloseLastSubstream(effect4));
                leftExec._input = previousInput;
                this._input = leftExec;
                this.addFinalizer((exit4) => {
                  const effect4 = this.restorePipe(exit4, previousInput);
                  return effect4 !== void 0 ? effect4 : _void;
                });
                this._currentChannel = this._currentChannel.right();
                break;
              }
              case OP_PROVIDE2: {
                const previousEnv = this._providedEnv;
                this._providedEnv = this._currentChannel.context();
                this._currentChannel = this._currentChannel.inner;
                this.addFinalizer(() => sync8(() => {
                  this._providedEnv = previousEnv;
                }));
                break;
              }
              case OP_READ: {
                const read2 = this._currentChannel;
                result = Read(this._input, identity, (emitted) => {
                  try {
                    this._currentChannel = read2.more(emitted);
                  } catch (error2) {
                    this._currentChannel = read2.done.onExit(die4(error2));
                  }
                  return void 0;
                }, (exit4) => {
                  const onExit4 = /* @__PURE__ */ __name((exit5) => {
                    return read2.done.onExit(exit5);
                  }, "onExit");
                  this._currentChannel = onExit4(exit4);
                  return void 0;
                });
                break;
              }
              case OP_SUCCEED: {
                result = this.doneSucceed(this._currentChannel.evaluate());
                break;
              }
              case OP_SUCCEED_NOW: {
                result = this.doneSucceed(this._currentChannel.terminal);
                break;
              }
              case OP_SUSPEND2: {
                this._currentChannel = this._currentChannel.channel();
                break;
              }
            }
          }
        } catch (error2) {
          this._currentChannel = failCause12(die6(error2));
        }
      }
    }
    return result;
  }
  getDone() {
    return this._done;
  }
  getEmit() {
    return this._emitted;
  }
  cancelWith(exit4) {
    this._cancelled = exit4;
  }
  clearInProgressFinalizer() {
    this._inProgressFinalizer = void 0;
  }
  storeInProgressFinalizer(finalizer3) {
    this._inProgressFinalizer = finalizer3;
  }
  popAllFinalizers(exit4) {
    const finalizers = [];
    let next4 = this._doneStack.pop();
    while (next4) {
      if (next4._tag === "ContinuationFinalizer") {
        finalizers.push(next4.finalizer);
      }
      next4 = this._doneStack.pop();
    }
    const effect4 = finalizers.length === 0 ? _void : runFinalizers(finalizers, exit4);
    this.storeInProgressFinalizer(effect4);
    return effect4;
  }
  popNextFinalizers() {
    const builder = [];
    while (this._doneStack.length !== 0) {
      const cont = this._doneStack[this._doneStack.length - 1];
      if (cont._tag === OP_CONTINUATION_K) {
        return builder;
      }
      builder.push(cont);
      this._doneStack.pop();
    }
    return builder;
  }
  restorePipe(exit4, prev) {
    const currInput = this._input;
    this._input = prev;
    if (currInput !== void 0) {
      const effect4 = currInput.close(exit4);
      return effect4;
    }
    return _void;
  }
  close(exit4) {
    let runInProgressFinalizers = void 0;
    const finalizer3 = this._inProgressFinalizer;
    if (finalizer3 !== void 0) {
      runInProgressFinalizers = pipe(finalizer3, ensuring4(sync8(() => this.clearInProgressFinalizer())));
    }
    let closeSelf = void 0;
    const selfFinalizers = this.popAllFinalizers(exit4);
    if (selfFinalizers !== void 0) {
      closeSelf = pipe(selfFinalizers, ensuring4(sync8(() => this.clearInProgressFinalizer())));
    }
    const closeSubexecutors = this._activeSubexecutor === void 0 ? void 0 : this._activeSubexecutor.close(exit4);
    if (closeSubexecutors === void 0 && runInProgressFinalizers === void 0 && closeSelf === void 0) {
      return void 0;
    }
    return pipe(
      exit3(ifNotNull(closeSubexecutors)),
      zip12(exit3(ifNotNull(runInProgressFinalizers))),
      zip12(exit3(ifNotNull(closeSelf))),
      map26(([[exit1, exit22], exit32]) => pipe(exit1, zipRight3(exit22), zipRight3(exit32))),
      uninterruptible3,
      // TODO: remove
      flatMap18((exit5) => suspend4(() => exit5))
    );
  }
  doneSucceed(value2) {
    if (this._doneStack.length === 0) {
      this._done = succeed3(value2);
      this._currentChannel = void 0;
      return Done2();
    }
    const head10 = this._doneStack[this._doneStack.length - 1];
    if (head10._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      this._currentChannel = head10.onSuccess(value2);
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = succeed3(value2);
      this._currentChannel = void 0;
      return Done2();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), succeed3(value2));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect4 = pipe(finalizerEffect, ensuring4(sync8(() => this.clearInProgressFinalizer())), uninterruptible3, flatMap18(() => sync8(() => this.doneSucceed(value2))));
    return fromEffect6(effect4);
  }
  doneHalt(cause3) {
    if (this._doneStack.length === 0) {
      this._done = failCause3(cause3);
      this._currentChannel = void 0;
      return Done2();
    }
    const head10 = this._doneStack[this._doneStack.length - 1];
    if (head10._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      try {
        this._currentChannel = head10.onHalt(cause3);
      } catch (error2) {
        this._currentChannel = failCause12(die6(error2));
      }
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = failCause3(cause3);
      this._currentChannel = void 0;
      return Done2();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), failCause3(cause3));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect4 = pipe(finalizerEffect, ensuring4(sync8(() => this.clearInProgressFinalizer())), uninterruptible3, flatMap18(() => sync8(() => this.doneHalt(cause3))));
    return fromEffect6(effect4);
  }
  processCancellation() {
    this._currentChannel = void 0;
    this._done = this._cancelled;
    this._cancelled = void 0;
    return Done2();
  }
  runBracketOut(bracketOut) {
    const effect4 = uninterruptible3(matchCauseEffect3(this.provide(bracketOut.acquire()), {
      onFailure: /* @__PURE__ */ __name((cause3) => sync8(() => {
        this._currentChannel = failCause12(cause3);
      }), "onFailure"),
      onSuccess: /* @__PURE__ */ __name((out) => sync8(() => {
        this.addFinalizer((exit4) => this.provide(bracketOut.finalizer(out, exit4)));
        this._currentChannel = write(out);
      }), "onSuccess")
    }));
    return fromEffect6(effect4);
  }
  provide(effect4) {
    if (this._providedEnv === void 0) {
      return effect4;
    }
    return pipe(effect4, provide2(this._providedEnv));
  }
  runEnsuring(ensuring11) {
    this.addFinalizer(ensuring11.finalizer);
    this._currentChannel = ensuring11.channel;
  }
  addFinalizer(f) {
    this._doneStack.push(new ContinuationFinalizerImpl(f));
  }
  runSubexecutor() {
    const subexecutor = this._activeSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_CHILD: {
        return this.pullFromChild(subexecutor.childExecutor, subexecutor.parentSubexecutor, subexecutor.onEmit, subexecutor);
      }
      case OP_PULL_FROM_UPSTREAM: {
        return this.pullFromUpstream(subexecutor);
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        return this.drainChildExecutors(subexecutor);
      }
      case OP_EMIT3: {
        this._emitted = subexecutor.value;
        this._activeSubexecutor = subexecutor.next;
        return Emit();
      }
    }
  }
  replaceSubexecutor(nextSubExec) {
    this._currentChannel = void 0;
    this._activeSubexecutor = nextSubExec;
  }
  finishWithExit(exit4) {
    const state = match7(exit4, {
      onFailure: /* @__PURE__ */ __name((cause3) => this.doneHalt(cause3), "onFailure"),
      onSuccess: /* @__PURE__ */ __name((value2) => this.doneSucceed(value2), "onSuccess")
    });
    this._activeSubexecutor = void 0;
    return state === void 0 ? _void : effect2(state);
  }
  finishSubexecutorWithCloseEffect(subexecutorDone, ...closeFuncs) {
    this.addFinalizer(() => pipe(closeFuncs, forEach16((closeFunc) => pipe(sync8(() => closeFunc(subexecutorDone)), flatMap18((closeEffect) => closeEffect !== void 0 ? closeEffect : _void)), {
      discard: true
    })));
    const state = pipe(subexecutorDone, match7({
      onFailure: /* @__PURE__ */ __name((cause3) => this.doneHalt(cause3), "onFailure"),
      onSuccess: /* @__PURE__ */ __name((value2) => this.doneSucceed(value2), "onSuccess")
    }));
    this._activeSubexecutor = void 0;
    return state;
  }
  applyUpstreamPullStrategy(upstreamFinished, queue, strategy) {
    switch (strategy._tag) {
      case OP_PULL_AFTER_NEXT: {
        const shouldPrepend = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldPrepend ? [void 0, ...queue] : queue];
      }
      case OP_PULL_AFTER_ALL_ENQUEUED: {
        const shouldEnqueue = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldEnqueue ? [...queue, void 0] : queue];
      }
    }
  }
  pullFromChild(childExecutor, parentSubexecutor, onEmitted, subexecutor) {
    return Read(childExecutor, identity, (emitted) => {
      const childExecutorDecision = onEmitted(emitted);
      switch (childExecutorDecision._tag) {
        case OP_CONTINUE2: {
          break;
        }
        case OP_CLOSE: {
          this.finishWithDoneValue(childExecutor, parentSubexecutor, childExecutorDecision.value);
          break;
        }
        case OP_YIELD2: {
          const modifiedParent = parentSubexecutor.enqueuePullFromChild(subexecutor);
          this.replaceSubexecutor(modifiedParent);
          break;
        }
      }
      this._activeSubexecutor = new Emit2(emitted, this._activeSubexecutor);
      return void 0;
    }, match7({
      onFailure: /* @__PURE__ */ __name((cause3) => {
        const state = this.handleSubexecutorFailure(childExecutor, parentSubexecutor, cause3);
        return state === void 0 ? void 0 : effectOrUndefinedIgnored(state);
      }, "onFailure"),
      onSuccess: /* @__PURE__ */ __name((doneValue) => {
        this.finishWithDoneValue(childExecutor, parentSubexecutor, doneValue);
        return void 0;
      }, "onSuccess")
    }));
  }
  finishWithDoneValue(childExecutor, parentSubexecutor, doneValue) {
    const subexecutor = parentSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_UPSTREAM: {
        const modifiedParent = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
        this._closeLastSubstream = childExecutor.close(succeed3(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        const modifiedParent = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        this._closeLastSubstream = childExecutor.close(succeed3(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      default: {
        break;
      }
    }
  }
  handleSubexecutorFailure(childExecutor, parentSubexecutor, cause3) {
    return this.finishSubexecutorWithCloseEffect(failCause3(cause3), (exit4) => parentSubexecutor.close(exit4), (exit4) => childExecutor.close(exit4));
  }
  pullFromUpstream(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      return this.performPullFromUpstream(subexecutor);
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const parentSubexecutor = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, subexecutor.activeChildExecutors.slice(1), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
    if (activeChild === void 0) {
      return this.performPullFromUpstream(parentSubexecutor);
    }
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
  performPullFromUpstream(subexecutor) {
    return Read(subexecutor.upstreamExecutor, (effect4) => {
      const closeLastSubstream = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
      this._closeLastSubstream = void 0;
      return pipe(this._executeCloseLastSubstream(closeLastSubstream), zipRight7(effect4));
    }, (emitted) => {
      if (this._closeLastSubstream !== void 0) {
        const closeLastSubstream = this._closeLastSubstream;
        this._closeLastSubstream = void 0;
        return pipe(this._executeCloseLastSubstream(closeLastSubstream), map26(() => {
          const childExecutor2 = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
          childExecutor2._input = this._input;
          const [emitSeparator2, updatedChildExecutors2] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
          this._activeSubexecutor = new PullFromChild(childExecutor2, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors2, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
          if (isSome2(emitSeparator2)) {
            this._activeSubexecutor = new Emit2(emitSeparator2.value, this._activeSubexecutor);
          }
          return void 0;
        }));
      }
      const childExecutor = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
      childExecutor._input = this._input;
      const [emitSeparator, updatedChildExecutors] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
      this._activeSubexecutor = new PullFromChild(childExecutor, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
      if (isSome2(emitSeparator)) {
        this._activeSubexecutor = new Emit2(emitSeparator.value, this._activeSubexecutor);
      }
      return void 0;
    }, (exit4) => {
      if (subexecutor.activeChildExecutors.some((subexecutor2) => subexecutor2 !== void 0)) {
        const drain5 = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, [void 0, ...subexecutor.activeChildExecutors], subexecutor.upstreamExecutor.getDone(), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        if (this._closeLastSubstream !== void 0) {
          const closeLastSubstream2 = this._closeLastSubstream;
          this._closeLastSubstream = void 0;
          return pipe(this._executeCloseLastSubstream(closeLastSubstream2), map26(() => this.replaceSubexecutor(drain5)));
        }
        this.replaceSubexecutor(drain5);
        return void 0;
      }
      const closeLastSubstream = this._closeLastSubstream;
      const state = this.finishSubexecutorWithCloseEffect(pipe(exit4, map15((a) => subexecutor.combineWithChildResult(subexecutor.lastDone, a))), () => closeLastSubstream, (exit5) => subexecutor.upstreamExecutor.close(exit5));
      return state === void 0 ? void 0 : (
        // NOTE: assuming finalizers cannot fail
        effectOrUndefinedIgnored(state)
      );
    });
  }
  drainChildExecutors(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      const lastClose = this._closeLastSubstream;
      if (lastClose !== void 0) {
        this.addFinalizer(() => succeed13(lastClose));
      }
      return this.finishSubexecutorWithCloseEffect(subexecutor.upstreamDone, () => lastClose, (exit4) => subexecutor.upstreamExecutor.close(exit4));
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const rest = subexecutor.activeChildExecutors.slice(1);
    if (activeChild === void 0) {
      const [emitSeparator, remainingExecutors] = this.applyUpstreamPullStrategy(true, rest, subexecutor.onPull(NoUpstream(rest.reduce((n, curr) => curr !== void 0 ? n + 1 : n, 0))));
      this.replaceSubexecutor(new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, remainingExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull));
      if (isSome2(emitSeparator)) {
        this._emitted = emitSeparator.value;
        return Emit();
      }
      return void 0;
    }
    const parentSubexecutor = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, rest, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
};
var ifNotNull = /* @__PURE__ */ __name((effect4) => effect4 !== void 0 ? effect4 : _void, "ifNotNull");
var runFinalizers = /* @__PURE__ */ __name((finalizers, exit4) => {
  return pipe(forEach16(finalizers, (fin) => exit3(fin(exit4))), map26((exits) => pipe(all6(exits), getOrElse2(() => void_4))), flatMap18((exit5) => suspend4(() => exit5)));
}, "runFinalizers");
var readUpstream = /* @__PURE__ */ __name((r, onSuccess, onFailure) => {
  const readStack = [r];
  const read2 = /* @__PURE__ */ __name(() => {
    const current2 = readStack.pop();
    if (current2 === void 0 || current2.upstream === void 0) {
      return dieMessage2("Unexpected end of input for channel execution");
    }
    const state = current2.upstream.run();
    switch (state._tag) {
      case OP_EMIT2: {
        const emitEffect = current2.onEmit(current2.upstream.getEmit());
        if (readStack.length === 0) {
          if (emitEffect === void 0) {
            return suspend4(onSuccess);
          }
          return pipe(emitEffect, matchCauseEffect3({
            onFailure,
            onSuccess
          }));
        }
        if (emitEffect === void 0) {
          return suspend4(() => read2());
        }
        return pipe(emitEffect, matchCauseEffect3({
          onFailure,
          onSuccess: /* @__PURE__ */ __name(() => read2(), "onSuccess")
        }));
      }
      case OP_DONE3: {
        const doneEffect = current2.onDone(current2.upstream.getDone());
        if (readStack.length === 0) {
          if (doneEffect === void 0) {
            return suspend4(onSuccess);
          }
          return pipe(doneEffect, matchCauseEffect3({
            onFailure,
            onSuccess
          }));
        }
        if (doneEffect === void 0) {
          return suspend4(() => read2());
        }
        return pipe(doneEffect, matchCauseEffect3({
          onFailure,
          onSuccess: /* @__PURE__ */ __name(() => read2(), "onSuccess")
        }));
      }
      case OP_FROM_EFFECT3: {
        readStack.push(current2);
        return pipe(current2.onEffect(state.effect), catchAllCause4((cause3) => suspend4(() => {
          const doneEffect = current2.onDone(failCause3(cause3));
          return doneEffect === void 0 ? _void : doneEffect;
        })), matchCauseEffect3({
          onFailure,
          onSuccess: /* @__PURE__ */ __name(() => read2(), "onSuccess")
        }));
      }
      case OP_READ2: {
        readStack.push(current2);
        readStack.push(state);
        return suspend4(() => read2());
      }
    }
  }, "read");
  return read2();
}, "readUpstream");
var runIn = /* @__PURE__ */ dual(2, (self, scope6) => {
  const run6 = /* @__PURE__ */ __name((channelDeferred, scopeDeferred, scope7) => acquireUseRelease3(sync8(() => new ChannelExecutor(self, void 0, identity)), (exec) => suspend4(() => runScopedInterpret(exec.run(), exec).pipe(intoDeferred2(channelDeferred), zipRight7(_await(channelDeferred)), zipLeft7(_await(scopeDeferred)))), (exec, exit4) => {
    const finalize = exec.close(exit4);
    if (finalize === void 0) {
      return _void;
    }
    return tapErrorCause4(finalize, (cause3) => addFinalizer3(scope7, failCause10(cause3)));
  }), "run");
  return uninterruptibleMask3((restore) => all10([fork3(scope6, sequential3), make22(), make22()]).pipe(flatMap18(([child, channelDeferred, scopeDeferred]) => restore(run6(channelDeferred, scopeDeferred, child)).pipe(forkIn3(scope6), flatMap18((fiber) => scope6.addFinalizer((exit4) => {
    const interruptors3 = isFailure2(exit4) ? interruptors2(exit4.cause) : void 0;
    return isDone(channelDeferred).pipe(flatMap18((isDone9) => isDone9 ? succeed2(scopeDeferred, void 0).pipe(zipRight7(_await3(fiber)), zipRight7(inheritAll2(fiber))) : succeed2(scopeDeferred, void 0).pipe(zipRight7(interruptors3 && size6(interruptors3) > 0 ? interruptAs(fiber, combineAll4(interruptors3)) : interrupt7(fiber)), zipRight7(inheritAll2(fiber)))));
  }).pipe(zipRight7(restore(_await(channelDeferred)))))))));
});
var runScopedInterpret = /* @__PURE__ */ __name((channelState, exec) => {
  const op = channelState;
  switch (op._tag) {
    case OP_FROM_EFFECT3: {
      return pipe(op.effect, flatMap18(() => runScopedInterpret(exec.run(), exec)));
    }
    case OP_EMIT2: {
      return runScopedInterpret(exec.run(), exec);
    }
    case OP_DONE3: {
      return suspend4(() => exec.getDone());
    }
    case OP_READ2: {
      return readUpstream(op, () => runScopedInterpret(exec.run(), exec), failCause10);
    }
  }
}, "runScopedInterpret");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelMergeDecision.js
var OP_DONE4 = "Done";
var OP_AWAIT = "Await";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/mergeDecision.js
var MergeDecisionSymbolKey = "effect/ChannelMergeDecision";
var MergeDecisionTypeId = /* @__PURE__ */ Symbol.for(MergeDecisionSymbolKey);
var proto9 = {
  [MergeDecisionTypeId]: {
    _R: /* @__PURE__ */ __name((_) => _, "_R"),
    _E0: /* @__PURE__ */ __name((_) => _, "_E0"),
    _Z0: /* @__PURE__ */ __name((_) => _, "_Z0"),
    _E: /* @__PURE__ */ __name((_) => _, "_E"),
    _Z: /* @__PURE__ */ __name((_) => _, "_Z")
  }
};
var Done3 = /* @__PURE__ */ __name((effect4) => {
  const op = Object.create(proto9);
  op._tag = OP_DONE4;
  op.effect = effect4;
  return op;
}, "Done");
var Await = /* @__PURE__ */ __name((f) => {
  const op = Object.create(proto9);
  op._tag = OP_AWAIT;
  op.f = f;
  return op;
}, "Await");
var AwaitConst = /* @__PURE__ */ __name((effect4) => Await(() => effect4), "AwaitConst");
var isMergeDecision = /* @__PURE__ */ __name((u) => hasProperty(u, MergeDecisionTypeId), "isMergeDecision");
var match25 = /* @__PURE__ */ dual(2, (self, {
  onAwait,
  onDone: onDone3
}) => {
  const op = self;
  switch (op._tag) {
    case OP_DONE4:
      return onDone3(op.effect);
    case OP_AWAIT:
      return onAwait(op.f);
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelMergeState.js
var OP_BOTH_RUNNING = "BothRunning";
var OP_LEFT_DONE = "LeftDone";
var OP_RIGHT_DONE = "RightDone";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/mergeState.js
var MergeStateSymbolKey = "effect/ChannelMergeState";
var MergeStateTypeId = /* @__PURE__ */ Symbol.for(MergeStateSymbolKey);
var proto10 = {
  [MergeStateTypeId]: MergeStateTypeId
};
var BothRunning = /* @__PURE__ */ __name((left3, right3) => {
  const op = Object.create(proto10);
  op._tag = OP_BOTH_RUNNING;
  op.left = left3;
  op.right = right3;
  return op;
}, "BothRunning");
var LeftDone = /* @__PURE__ */ __name((f) => {
  const op = Object.create(proto10);
  op._tag = OP_LEFT_DONE;
  op.f = f;
  return op;
}, "LeftDone");
var RightDone = /* @__PURE__ */ __name((f) => {
  const op = Object.create(proto10);
  op._tag = OP_RIGHT_DONE;
  op.f = f;
  return op;
}, "RightDone");
var isMergeState = /* @__PURE__ */ __name((u) => hasProperty(u, MergeStateTypeId), "isMergeState");
var isBothRunning = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_BOTH_RUNNING;
}, "isBothRunning");
var isLeftDone = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_LEFT_DONE;
}, "isLeftDone");
var isRightDone = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_RIGHT_DONE;
}, "isRightDone");
var match26 = /* @__PURE__ */ dual(2, (self, {
  onBothRunning,
  onLeftDone,
  onRightDone
}) => {
  switch (self._tag) {
    case OP_BOTH_RUNNING: {
      return onBothRunning(self.left, self.right);
    }
    case OP_LEFT_DONE: {
      return onLeftDone(self.f);
    }
    case OP_RIGHT_DONE: {
      return onRightDone(self.f);
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/channelMergeStrategy.js
var OP_BACK_PRESSURE = "BackPressure";
var OP_BUFFER_SLIDING = "BufferSliding";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/mergeStrategy.js
var MergeStrategySymbolKey = "effect/ChannelMergeStrategy";
var MergeStrategyTypeId = /* @__PURE__ */ Symbol.for(MergeStrategySymbolKey);
var proto11 = {
  [MergeStrategyTypeId]: MergeStrategyTypeId
};
var BackPressure = /* @__PURE__ */ __name((_) => {
  const op = Object.create(proto11);
  op._tag = OP_BACK_PRESSURE;
  return op;
}, "BackPressure");
var BufferSliding = /* @__PURE__ */ __name((_) => {
  const op = Object.create(proto11);
  op._tag = OP_BUFFER_SLIDING;
  return op;
}, "BufferSliding");
var isMergeStrategy = /* @__PURE__ */ __name((u) => hasProperty(u, MergeStrategyTypeId), "isMergeStrategy");
var isBackPressure = /* @__PURE__ */ __name((self) => self._tag === OP_BACK_PRESSURE, "isBackPressure");
var isBufferSliding = /* @__PURE__ */ __name((self) => self._tag === OP_BUFFER_SLIDING, "isBufferSliding");
var match27 = /* @__PURE__ */ dual(2, (self, {
  onBackPressure,
  onBufferSliding
}) => {
  switch (self._tag) {
    case OP_BACK_PRESSURE: {
      return onBackPressure();
    }
    case OP_BUFFER_SLIDING: {
      return onBufferSliding();
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel/singleProducerAsyncInput.js
var OP_STATE_EMPTY = "Empty";
var OP_STATE_EMIT = "Emit";
var OP_STATE_ERROR = "Error";
var OP_STATE_DONE2 = "Done";
var stateEmpty = /* @__PURE__ */ __name((notifyProducer) => ({
  _tag: OP_STATE_EMPTY,
  notifyProducer
}), "stateEmpty");
var stateEmit = /* @__PURE__ */ __name((notifyConsumers) => ({
  _tag: OP_STATE_EMIT,
  notifyConsumers
}), "stateEmit");
var stateError = /* @__PURE__ */ __name((cause3) => ({
  _tag: OP_STATE_ERROR,
  cause: cause3
}), "stateError");
var stateDone = /* @__PURE__ */ __name((done12) => ({
  _tag: OP_STATE_DONE2,
  done: done12
}), "stateDone");
var SingleProducerAsyncInputImpl = class {
  static {
    __name(this, "SingleProducerAsyncInputImpl");
  }
  ref;
  constructor(ref) {
    this.ref = ref;
  }
  awaitRead() {
    return flatten12(modify8(this.ref, (state) => state._tag === OP_STATE_EMPTY ? [_await(state.notifyProducer), state] : [_void, state]));
  }
  get close() {
    return fiberIdWith2((fiberId4) => this.error(interrupt6(fiberId4)));
  }
  done(value2) {
    return flatten12(modify8(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach16(state.notifyConsumers, (deferred) => succeed2(deferred, left2(value2)), {
            discard: true
          }), stateDone(value2)];
        }
        case OP_STATE_ERROR: {
          return [interrupt8, state];
        }
        case OP_STATE_DONE2: {
          return [interrupt8, state];
        }
      }
    }));
  }
  emit(element) {
    return flatMap18(make22(), (deferred) => flatten12(modify8(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          const notifyConsumer = state.notifyConsumers[0];
          const notifyConsumers = state.notifyConsumers.slice(1);
          if (notifyConsumer !== void 0) {
            return [succeed2(notifyConsumer, right2(element)), notifyConsumers.length === 0 ? stateEmpty(deferred) : stateEmit(notifyConsumers)];
          }
          throw new Error("Bug: Channel.SingleProducerAsyncInput.emit - Queue was empty! please report an issue at https://github.com/Effect-TS/effect/issues");
        }
        case OP_STATE_ERROR: {
          return [interrupt8, state];
        }
        case OP_STATE_DONE2: {
          return [interrupt8, state];
        }
      }
    })));
  }
  error(cause3) {
    return flatten12(modify8(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach16(state.notifyConsumers, (deferred) => failCause2(deferred, cause3), {
            discard: true
          }), stateError(cause3)];
        }
        case OP_STATE_ERROR: {
          return [interrupt8, state];
        }
        case OP_STATE_DONE2: {
          return [interrupt8, state];
        }
      }
    }));
  }
  get take() {
    return this.takeWith((cause3) => failCause3(map22(cause3, left2)), (elem) => succeed3(elem), (done12) => fail4(right2(done12)));
  }
  takeWith(onError6, onElement, onDone3) {
    return flatMap18(make22(), (deferred) => flatten12(modify8(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [zipRight7(succeed2(state.notifyProducer, void 0), matchCause4(_await(deferred), {
            onFailure: onError6,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          })), stateEmit([deferred])];
        }
        case OP_STATE_EMIT: {
          return [matchCause4(_await(deferred), {
            onFailure: onError6,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          }), stateEmit([...state.notifyConsumers, deferred])];
        }
        case OP_STATE_ERROR: {
          return [succeed13(onError6(state.cause)), state];
        }
        case OP_STATE_DONE2: {
          return [succeed13(onDone3(state.done)), state];
        }
      }
    })));
  }
};
var make60 = /* @__PURE__ */ __name(() => pipe(make22(), flatMap18((deferred) => make35(stateEmpty(deferred))), map26((ref) => new SingleProducerAsyncInputImpl(ref))), "make");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/channel.js
var acquireUseRelease4 = /* @__PURE__ */ __name((acquire, use2, release) => flatMap20(fromEffect5(make35(() => _void)), (ref) => pipe(fromEffect5(uninterruptible3(tap5(acquire, (a) => set7(ref, (exit4) => release(a, exit4))))), flatMap20(use2), ensuringWith((exit4) => flatMap18(get13(ref), (f) => f(exit4))))), "acquireUseRelease");
var as9 = /* @__PURE__ */ dual(2, (self, value2) => map28(self, () => value2));
var asVoid7 = /* @__PURE__ */ __name((self) => map28(self, constVoid), "asVoid");
var buffer = /* @__PURE__ */ __name((options) => suspend6(() => {
  const doBuffer = /* @__PURE__ */ __name((empty46, isEmpty23, ref) => unwrap2(modify8(ref, (inElem) => isEmpty23(inElem) ? [readWith({
    onInput: /* @__PURE__ */ __name((input) => flatMap20(write(input), () => doBuffer(empty46, isEmpty23, ref)), "onInput"),
    onFailure: /* @__PURE__ */ __name((error2) => fail14(error2), "onFailure"),
    onDone: /* @__PURE__ */ __name((done12) => succeedNow(done12), "onDone")
  }), inElem] : [flatMap20(write(inElem), () => doBuffer(empty46, isEmpty23, ref)), empty46])), "doBuffer");
  return doBuffer(options.empty, options.isEmpty, options.ref);
}), "buffer");
var bufferChunk = /* @__PURE__ */ __name((ref) => buffer({
  empty: empty7(),
  isEmpty: isEmpty2,
  ref
}), "bufferChunk");
var catchAll6 = /* @__PURE__ */ dual(2, (self, f) => catchAllCause6(self, (cause3) => match(failureOrCause2(cause3), {
  onLeft: f,
  onRight: failCause12
})));
var concatMap = /* @__PURE__ */ dual(2, (self, f) => concatMapWith(self, f, () => void 0, () => void 0));
var collect2 = /* @__PURE__ */ dual(2, (self, pf) => {
  const collector = readWith({
    onInput: /* @__PURE__ */ __name((out) => match2(pf(out), {
      onNone: /* @__PURE__ */ __name(() => collector, "onNone"),
      onSome: /* @__PURE__ */ __name((out2) => flatMap20(write(out2), () => collector), "onSome")
    }), "onInput"),
    onFailure: fail14,
    onDone: succeedNow
  });
  return pipeTo(self, collector);
});
var concatOut = /* @__PURE__ */ __name((self) => concatAll(self), "concatOut");
var mapInput7 = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(write(inElem), () => reader), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name((done12) => succeedNow(f(done12)), "onDone")
  });
  return pipeTo(reader, self);
});
var mapInputEffect2 = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(write(inElem), () => reader), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name((done12) => fromEffect5(f(done12)), "onDone")
  });
  return pipeTo(reader, self);
});
var mapInputError = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(write(inElem), () => reader), "onInput"),
    onFailure: /* @__PURE__ */ __name((error2) => fail14(f(error2)), "onFailure"),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputErrorEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(write(inElem), () => reader), "onInput"),
    onFailure: /* @__PURE__ */ __name((error2) => fromEffect5(f(error2)), "onFailure"),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputIn = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(write(f(inElem)), () => reader), "onInput"),
    onFailure: fail14,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputInEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((inElem) => flatMap20(flatMap20(fromEffect5(f(inElem)), write), () => reader), "onInput"),
    onFailure: fail14,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var doneCollect = /* @__PURE__ */ __name((self) => suspend6(() => {
  const builder = [];
  return pipe(pipeTo(self, doneCollectReader(builder)), flatMap20((outDone) => succeed15([unsafeFromArray(builder), outDone])));
}), "doneCollect");
var doneCollectReader = /* @__PURE__ */ __name((builder) => {
  return readWith({
    onInput: /* @__PURE__ */ __name((outElem) => flatMap20(sync10(() => {
      builder.push(outElem);
    }), () => doneCollectReader(builder)), "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
}, "doneCollectReader");
var drain = /* @__PURE__ */ __name((self) => {
  const drainer = readWithCause({
    onInput: /* @__PURE__ */ __name(() => drainer, "onInput"),
    onFailure: failCause12,
    onDone: succeed15
  });
  return pipeTo(self, drainer);
}, "drain");
var emitCollect = /* @__PURE__ */ __name((self) => flatMap20(doneCollect(self), write), "emitCollect");
var ensuring5 = /* @__PURE__ */ dual(2, (self, finalizer3) => ensuringWith(self, () => finalizer3));
var context6 = /* @__PURE__ */ __name(() => fromEffect5(context4()), "context");
var contextWith4 = /* @__PURE__ */ __name((f) => map28(context6(), f), "contextWith");
var contextWithChannel = /* @__PURE__ */ __name((f) => flatMap20(context6(), f), "contextWithChannel");
var contextWithEffect3 = /* @__PURE__ */ __name((f) => mapEffect5(context6(), f), "contextWithEffect");
var flatten14 = /* @__PURE__ */ __name((self) => flatMap20(self, identity), "flatten");
var foldChannel = /* @__PURE__ */ dual(2, (self, options) => foldCauseChannel(self, {
  onFailure: /* @__PURE__ */ __name((cause3) => {
    const either11 = failureOrCause2(cause3);
    switch (either11._tag) {
      case "Left": {
        return options.onFailure(either11.left);
      }
      case "Right": {
        return failCause12(either11.right);
      }
    }
  }, "onFailure"),
  onSuccess: options.onSuccess
}));
var fromEither3 = /* @__PURE__ */ __name((either11) => suspend6(() => match(either11, {
  onLeft: fail14,
  onRight: succeed15
})), "fromEither");
var fromInput = /* @__PURE__ */ __name((input) => unwrap2(input.takeWith(failCause12, (elem) => flatMap20(write(elem), () => fromInput(input)), succeed15)), "fromInput");
var fromPubSub = /* @__PURE__ */ __name((pubsub) => unwrapScoped3(map26(subscribe2(pubsub), fromQueue)), "fromPubSub");
var fromPubSubScoped = /* @__PURE__ */ __name((pubsub) => map26(subscribe2(pubsub), fromQueue), "fromPubSubScoped");
var fromOption6 = /* @__PURE__ */ __name((option5) => suspend6(() => match2(option5, {
  onNone: /* @__PURE__ */ __name(() => fail14(none2()), "onNone"),
  onSome: succeed15
})), "fromOption");
var fromQueue = /* @__PURE__ */ __name((queue) => suspend6(() => fromQueueInternal(queue)), "fromQueue");
var fromQueueInternal = /* @__PURE__ */ __name((queue) => pipe(fromEffect5(take6(queue)), flatMap20(match({
  onLeft: match7({
    onFailure: failCause12,
    onSuccess: succeedNow
  }),
  onRight: /* @__PURE__ */ __name((elem) => flatMap20(write(elem), () => fromQueueInternal(queue)), "onRight")
}))), "fromQueueInternal");
var identityChannel = /* @__PURE__ */ __name(() => readWith({
  onInput: /* @__PURE__ */ __name((input) => flatMap20(write(input), () => identityChannel()), "onInput"),
  onFailure: fail14,
  onDone: succeedNow
}), "identityChannel");
var interruptWhen = /* @__PURE__ */ dual(2, (self, effect4) => mergeWith2(self, {
  other: fromEffect5(effect4),
  onSelfDone: /* @__PURE__ */ __name((selfDone) => Done3(suspend4(() => selfDone)), "onSelfDone"),
  onOtherDone: /* @__PURE__ */ __name((effectDone) => Done3(suspend4(() => effectDone)), "onOtherDone")
}));
var interruptWhenDeferred = /* @__PURE__ */ dual(2, (self, deferred) => interruptWhen(self, _await(deferred)));
var map28 = /* @__PURE__ */ dual(2, (self, f) => flatMap20(self, (a) => sync10(() => f(a))));
var mapEffect5 = /* @__PURE__ */ dual(2, (self, f) => flatMap20(self, (z) => fromEffect5(f(z))));
var mapError7 = /* @__PURE__ */ dual(2, (self, f) => mapErrorCause5(self, map22(f)));
var mapErrorCause5 = /* @__PURE__ */ dual(2, (self, f) => catchAllCause6(self, (cause3) => failCause12(f(cause3))));
var mapOut = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((outElem) => flatMap20(write(f(outElem)), () => reader), "onInput"),
    onFailure: fail14,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const reader = readWithCause({
    onInput: /* @__PURE__ */ __name((outElem) => pipe(fromEffect5(f(outElem)), flatMap20(write), flatMap20(() => reader)), "onInput"),
    onFailure: failCause12,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffectPar = /* @__PURE__ */ dual(3, (self, f, n) => unwrapScopedWith((scope6) => gen5(function* () {
  const input = yield* make60();
  const queueReader = fromInput(input);
  const queue = yield* bounded5(n);
  yield* addFinalizer3(scope6, shutdown4(queue));
  const errorSignal = yield* make22();
  const withPermits = n === Number.POSITIVE_INFINITY ? (_) => identity : (yield* makeSemaphore2(n)).withPermits;
  const pull = yield* queueReader.pipe(pipeTo(self), toPullIn(scope6));
  yield* pull.pipe(matchCauseEffect3({
    onFailure: /* @__PURE__ */ __name((cause3) => offer3(queue, failCause10(cause3)), "onFailure"),
    onSuccess: match({
      onLeft: /* @__PURE__ */ __name((outDone) => zipRight7(interruptible4(withPermits(n)(_void)), asVoid6(offer3(queue, succeed13(left2(outDone))))), "onLeft"),
      onRight: /* @__PURE__ */ __name((outElem) => gen5(function* () {
        const deferred = yield* make22();
        const latch = yield* make22();
        yield* offer3(queue, map26(_await(deferred), right2));
        yield* succeed2(latch, void 0).pipe(zipRight7(uninterruptibleMask3((restore) => exit3(restore(_await(errorSignal))).pipe(raceFirst3(exit3(restore(f(outElem)))), flatMap18(identity))).pipe(tapErrorCause4((cause3) => failCause2(errorSignal, cause3)), intoDeferred2(deferred))), withPermits(1), forkIn3(scope6));
        yield* _await(latch);
      }), "onRight")
    })
  }), forever4, interruptible4, forkIn3(scope6));
  const consumer = unwrap2(matchCause4(flatten12(take6(queue)), {
    onFailure: failCause12,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: /* @__PURE__ */ __name((outElem) => flatMap20(write(outElem), () => consumer), "onRight")
    })
  }));
  return embedInput(consumer, input);
})));
var mergeAll7 = /* @__PURE__ */ __name((options) => {
  return (channels) => mergeAllWith(options)(channels, constVoid);
}, "mergeAll");
var mergeAllUnbounded = /* @__PURE__ */ __name((channels) => mergeAllWith({
  concurrency: "unbounded"
})(channels, constVoid), "mergeAllUnbounded");
var mergeAllUnboundedWith = /* @__PURE__ */ __name((channels, f) => mergeAllWith({
  concurrency: "unbounded"
})(channels, f), "mergeAllUnboundedWith");
var mergeAllWith = /* @__PURE__ */ __name(({
  bufferSize = 16,
  concurrency,
  mergeStrategy = BackPressure()
}) => (channels, f) => unwrapScopedWith((scope6) => gen5(function* () {
  const concurrencyN = concurrency === "unbounded" ? Number.MAX_SAFE_INTEGER : concurrency;
  const input = yield* make60();
  const queueReader = fromInput(input);
  const queue = yield* bounded5(bufferSize);
  yield* addFinalizer3(scope6, shutdown4(queue));
  const cancelers = yield* unbounded5();
  yield* addFinalizer3(scope6, shutdown4(cancelers));
  const lastDone = yield* make35(none2());
  const errorSignal = yield* make22();
  const withPermits = (yield* makeSemaphore2(concurrencyN)).withPermits;
  const pull = yield* toPullIn(pipeTo(queueReader, channels), scope6);
  function evaluatePull(pull2) {
    return pull2.pipe(flatMap18(match({
      onLeft: /* @__PURE__ */ __name((done12) => succeed13(some3(done12)), "onLeft"),
      onRight: /* @__PURE__ */ __name((outElem) => as8(offer3(queue, succeed13(right2(outElem))), none2()), "onRight")
    })), repeat3({
      until: /* @__PURE__ */ __name((_) => isSome2(_), "until")
    }), flatMap18((outDone) => update4(lastDone, match2({
      onNone: /* @__PURE__ */ __name(() => some3(outDone.value), "onNone"),
      onSome: /* @__PURE__ */ __name((lastDone2) => some3(f(lastDone2, outDone.value)), "onSome")
    }))), catchAllCause4((cause3) => isInterrupted3(cause3) ? failCause10(cause3) : offer3(queue, failCause10(cause3)).pipe(zipRight7(succeed2(errorSignal, void 0)), asVoid6)));
  }
  __name(evaluatePull, "evaluatePull");
  yield* pull.pipe(matchCauseEffect3({
    onFailure: /* @__PURE__ */ __name((cause3) => offer3(queue, failCause10(cause3)).pipe(zipRight7(succeed13(false))), "onFailure"),
    onSuccess: match({
      onLeft: /* @__PURE__ */ __name((outDone) => raceWith2(interruptible4(_await(errorSignal)), interruptible4(withPermits(concurrencyN)(_void)), {
        onSelfDone: /* @__PURE__ */ __name((_, permitAcquisition) => as8(interrupt7(permitAcquisition), false), "onSelfDone"),
        onOtherDone: /* @__PURE__ */ __name((_, failureAwait) => zipRight7(interrupt7(failureAwait), get13(lastDone).pipe(flatMap18(match2({
          onNone: /* @__PURE__ */ __name(() => offer3(queue, succeed13(left2(outDone))), "onNone"),
          onSome: /* @__PURE__ */ __name((lastDone2) => offer3(queue, succeed13(left2(f(lastDone2, outDone)))), "onSome")
        })), as8(false))), "onOtherDone")
      }), "onLeft"),
      onRight: /* @__PURE__ */ __name((channel) => match27(mergeStrategy, {
        onBackPressure: /* @__PURE__ */ __name(() => gen5(function* () {
          const latch = yield* make22();
          const raceEffects = scopedWith2((scope7) => toPullIn(pipeTo(queueReader, channel), scope7).pipe(flatMap18((pull2) => race3(exit3(evaluatePull(pull2)), exit3(interruptible4(_await(errorSignal))))), flatMap18(identity)));
          yield* succeed2(latch, void 0).pipe(zipRight7(raceEffects), withPermits(1), forkIn3(scope6));
          yield* _await(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        }), "onBackPressure"),
        onBufferSliding: /* @__PURE__ */ __name(() => gen5(function* () {
          const canceler = yield* make22();
          const latch = yield* make22();
          const size25 = yield* size20(cancelers);
          yield* take6(cancelers).pipe(flatMap18((canceler2) => succeed2(canceler2, void 0)), when3(() => size25 >= concurrencyN));
          yield* offer3(cancelers, canceler);
          const raceEffects = scopedWith2((scope7) => toPullIn(pipeTo(queueReader, channel), scope7).pipe(flatMap18((pull2) => exit3(evaluatePull(pull2)).pipe(race3(exit3(interruptible4(_await(errorSignal)))), race3(exit3(interruptible4(_await(canceler)))))), flatMap18(identity)));
          yield* succeed2(latch, void 0).pipe(zipRight7(raceEffects), withPermits(1), forkIn3(scope6));
          yield* _await(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        }), "onBufferSliding")
      }), "onRight")
    })
  }), repeat3({
    while: /* @__PURE__ */ __name((_) => _, "while")
  }), forkIn3(scope6));
  const consumer = pipe(take6(queue), flatten12, matchCause4({
    onFailure: failCause12,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: /* @__PURE__ */ __name((outElem) => flatMap20(write(outElem), () => consumer), "onRight")
    })
  }), unwrap2);
  return embedInput(consumer, input);
})), "mergeAllWith");
var mergeMap = /* @__PURE__ */ dual(3, (self, f, options) => mergeAll7(options)(mapOut(self, f)));
var mergeOut = /* @__PURE__ */ dual(2, (self, n) => mergeAll7({
  concurrency: n
})(mapOut(self, identity)));
var mergeOutWith = /* @__PURE__ */ dual(3, (self, n, f) => mergeAllWith({
  concurrency: n
})(mapOut(self, identity), f));
var mergeWith2 = /* @__PURE__ */ dual(2, (self, options) => {
  function merge12(scope6) {
    return gen5(function* () {
      const input = yield* make60();
      const queueReader = fromInput(input);
      const pullL = yield* toPullIn(pipeTo(queueReader, self), scope6);
      const pullR = yield* toPullIn(pipeTo(queueReader, options.other), scope6);
      function handleSide(exit4, fiber, pull) {
        return (done12, both3, single2) => {
          function onDecision3(decision) {
            const op = decision;
            if (op._tag === OP_DONE4) {
              return succeed13(fromEffect5(zipRight7(interrupt7(fiber), op.effect)));
            }
            return map26(_await3(fiber), match7({
              onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(op.f(failCause3(cause3))), "onFailure"),
              onSuccess: match({
                onLeft: /* @__PURE__ */ __name((done13) => fromEffect5(op.f(succeed3(done13))), "onLeft"),
                onRight: /* @__PURE__ */ __name((elem) => zipRight8(write(elem), go(single2(op.f))), "onRight")
              })
            }));
          }
          __name(onDecision3, "onDecision");
          return match7(exit4, {
            onFailure: /* @__PURE__ */ __name((cause3) => onDecision3(done12(failCause3(cause3))), "onFailure"),
            onSuccess: match({
              onLeft: /* @__PURE__ */ __name((z) => onDecision3(done12(succeed3(z))), "onLeft"),
              onRight: /* @__PURE__ */ __name((elem) => succeed13(flatMap20(write(elem), () => flatMap20(fromEffect5(forkIn3(interruptible4(pull), scope6)), (leftFiber) => go(both3(leftFiber, fiber))))), "onRight")
            })
          });
        };
      }
      __name(handleSide, "handleSide");
      function go(state) {
        switch (state._tag) {
          case OP_BOTH_RUNNING: {
            const leftJoin = interruptible4(join4(state.left));
            const rightJoin = interruptible4(join4(state.right));
            return unwrap2(raceWith2(leftJoin, rightJoin, {
              onSelfDone: /* @__PURE__ */ __name((leftExit, rf) => zipRight7(interrupt7(rf), handleSide(leftExit, state.right, pullL)(options.onSelfDone, BothRunning, (f) => LeftDone(f))), "onSelfDone"),
              onOtherDone: /* @__PURE__ */ __name((rightExit, lf) => zipRight7(interrupt7(lf), handleSide(rightExit, state.left, pullR)(options.onOtherDone, (left3, right3) => BothRunning(right3, left3), (f) => RightDone(f))), "onOtherDone")
            }));
          }
          case OP_LEFT_DONE: {
            return unwrap2(map26(exit3(pullR), match7({
              onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(state.f(failCause3(cause3))), "onFailure"),
              onSuccess: match({
                onLeft: /* @__PURE__ */ __name((done12) => fromEffect5(state.f(succeed3(done12))), "onLeft"),
                onRight: /* @__PURE__ */ __name((elem) => flatMap20(write(elem), () => go(LeftDone(state.f))), "onRight")
              })
            })));
          }
          case OP_RIGHT_DONE: {
            return unwrap2(map26(exit3(pullL), match7({
              onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(state.f(failCause3(cause3))), "onFailure"),
              onSuccess: match({
                onLeft: /* @__PURE__ */ __name((done12) => fromEffect5(state.f(succeed3(done12))), "onLeft"),
                onRight: /* @__PURE__ */ __name((elem) => flatMap20(write(elem), () => go(RightDone(state.f))), "onRight")
              })
            })));
          }
        }
      }
      __name(go, "go");
      return fromEffect5(withFiberRuntime2((parent) => {
        const inherit = withFiberRuntime2((state) => {
          ;
          state.transferChildren(parent.scope());
          return _void;
        });
        const leftFiber = interruptible4(pullL).pipe(ensuring4(inherit), forkIn3(scope6));
        const rightFiber = interruptible4(pullR).pipe(ensuring4(inherit), forkIn3(scope6));
        return zipWith12(leftFiber, rightFiber, (left3, right3) => BothRunning(left3, right3));
      })).pipe(flatMap20(go), embedInput(input));
    });
  }
  __name(merge12, "merge");
  return unwrapScopedWith(merge12);
});
var never6 = /* @__PURE__ */ fromEffect5(never5);
var orDie6 = /* @__PURE__ */ dual(2, (self, error2) => orDieWith3(self, error2));
var orDieWith3 = /* @__PURE__ */ dual(2, (self, f) => catchAll6(self, (e) => failCauseSync7(() => die6(f(e)))));
var orElse10 = /* @__PURE__ */ dual(2, (self, that) => catchAll6(self, that));
var pipeToOrFail = /* @__PURE__ */ dual(2, (self, that) => suspend6(() => {
  let channelException = void 0;
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((outElem) => flatMap20(write(outElem), () => reader), "onInput"),
    onFailure: /* @__PURE__ */ __name((outErr) => {
      channelException = ChannelException(outErr);
      return failCause12(die6(channelException));
    }, "onFailure"),
    onDone: succeedNow
  });
  const writer = readWithCause({
    onInput: /* @__PURE__ */ __name((outElem) => pipe(write(outElem), flatMap20(() => writer)), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => isDieType2(cause3) && isChannelException(cause3.defect) && equals(cause3.defect, channelException) ? fail14(cause3.defect.error) : failCause12(cause3), "onFailure"),
    onDone: succeedNow
  });
  return pipeTo(pipeTo(pipeTo(self, reader), that), writer);
}));
var provideService6 = /* @__PURE__ */ dual(3, (self, tag, service6) => {
  return flatMap20(context6(), (context11) => provideContext4(self, add2(context11, tag, service6)));
});
var provideLayer = /* @__PURE__ */ dual(2, (self, layer) => unwrapScopedWith((scope6) => map26(buildWithScope2(layer, scope6), (context11) => provideContext4(self, context11))));
var mapInputContext4 = /* @__PURE__ */ dual(2, (self, f) => contextWithChannel((context11) => provideContext4(self, f(context11))));
var provideSomeLayer2 = /* @__PURE__ */ dual(2, (self, layer) => (
  // @ts-expect-error
  provideLayer(self, merge8(context5(), layer))
));
var read = /* @__PURE__ */ __name(() => readOrFail(none2()), "read");
var repeated = /* @__PURE__ */ __name((self) => flatMap20(self, () => repeated(self)), "repeated");
var run2 = /* @__PURE__ */ __name((self) => scopedWith2((scope6) => runIn(self, scope6)), "run");
var runCollect = /* @__PURE__ */ __name((self) => run2(collectElements(self)), "runCollect");
var runDrain = /* @__PURE__ */ __name((self) => run2(drain(self)), "runDrain");
var runScoped = /* @__PURE__ */ __name((self) => scopeWith2((scope6) => runIn(self, scope6)), "runScoped");
var scoped6 = /* @__PURE__ */ __name((effect4) => unwrap2(uninterruptibleMask3((restore) => map26(make52(), (scope6) => acquireReleaseOut(tapErrorCause4(restore(extend3(effect4, scope6)), (cause3) => close(scope6, failCause3(cause3))), (_, exit4) => close(scope6, exit4))))), "scoped");
var scopedWith3 = /* @__PURE__ */ __name((f) => unwrapScoped3(map26(scope4, (scope6) => flatMap20(fromEffect5(f(scope6)), write))), "scopedWith");
var service4 = /* @__PURE__ */ __name((tag) => fromEffect5(tag), "service");
var serviceWith = /* @__PURE__ */ __name((tag) => (f) => map28(service4(tag), f), "serviceWith");
var serviceWithChannel = /* @__PURE__ */ __name((tag) => (f) => flatMap20(service4(tag), f), "serviceWithChannel");
var serviceWithEffect = /* @__PURE__ */ __name((tag) => (f) => mapEffect5(service4(tag), f), "serviceWithEffect");
var splitLines = /* @__PURE__ */ __name(() => suspend6(() => {
  let stringBuilder = "";
  let midCRLF = false;
  const splitLinesChunk = /* @__PURE__ */ __name((chunk4) => {
    const chunkBuilder = [];
    map7(chunk4, (str) => {
      if (str.length !== 0) {
        let from = 0;
        let indexOfCR = str.indexOf("\r");
        let indexOfLF = str.indexOf("\n");
        if (midCRLF) {
          if (indexOfLF === 0) {
            chunkBuilder.push(stringBuilder);
            stringBuilder = "";
            from = 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            stringBuilder = stringBuilder + "\r";
          }
          midCRLF = false;
        }
        while (indexOfCR !== -1 || indexOfLF !== -1) {
          if (indexOfCR === -1 || indexOfLF !== -1 && indexOfLF < indexOfCR) {
            if (stringBuilder.length === 0) {
              chunkBuilder.push(str.substring(from, indexOfLF));
            } else {
              chunkBuilder.push(stringBuilder + str.substring(from, indexOfLF));
              stringBuilder = "";
            }
            from = indexOfLF + 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            if (str.length === indexOfCR + 1) {
              midCRLF = true;
              indexOfCR = -1;
            } else {
              if (indexOfLF === indexOfCR + 1) {
                if (stringBuilder.length === 0) {
                  chunkBuilder.push(str.substring(from, indexOfCR));
                } else {
                  stringBuilder = stringBuilder + str.substring(from, indexOfCR);
                  chunkBuilder.push(stringBuilder);
                  stringBuilder = "";
                }
                from = indexOfCR + 2;
                indexOfCR = str.indexOf("\r", from);
                indexOfLF = str.indexOf("\n", from);
              } else {
                indexOfCR = str.indexOf("\r", indexOfCR + 1);
              }
            }
          }
        }
        if (midCRLF) {
          stringBuilder = stringBuilder + str.substring(from, str.length - 1);
        } else {
          stringBuilder = stringBuilder + str.substring(from, str.length);
        }
      }
    });
    return unsafeFromArray(chunkBuilder);
  }, "splitLinesChunk");
  const loop4 = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => {
      const out = splitLinesChunk(input);
      return isEmpty2(out) ? loop4 : flatMap20(write(out), () => loop4);
    }, "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => stringBuilder.length === 0 ? failCause12(cause3) : flatMap20(write(of3(stringBuilder)), () => failCause12(cause3)), "onFailure"),
    onDone: /* @__PURE__ */ __name((done12) => stringBuilder.length === 0 ? succeed15(done12) : flatMap20(write(of3(stringBuilder)), () => succeed15(done12)), "onDone")
  });
  return loop4;
}), "splitLines");
var toPubSub = /* @__PURE__ */ __name((pubsub) => toQueue(pubsub), "toPubSub");
var toPull = /* @__PURE__ */ __name((self) => flatMap18(scope4, (scope6) => toPullIn(self, scope6)), "toPull");
var toPullIn = /* @__PURE__ */ dual(2, (self, scope6) => zip12(sync8(() => new ChannelExecutor(self, void 0, identity)), runtime4()).pipe(tap5(([executor, runtime5]) => addFinalizerExit(scope6, (exit4) => {
  const finalizer3 = executor.close(exit4);
  return finalizer3 !== void 0 ? provide2(finalizer3, runtime5) : _void;
})), uninterruptible3, map26(([executor]) => suspend4(() => interpretToPull(executor.run(), executor)))));
var interpretToPull = /* @__PURE__ */ __name((channelState, exec) => {
  const state = channelState;
  switch (state._tag) {
    case OP_DONE3: {
      return match7(exec.getDone(), {
        onFailure: failCause10,
        onSuccess: /* @__PURE__ */ __name((done12) => succeed13(left2(done12)), "onSuccess")
      });
    }
    case OP_EMIT2: {
      return succeed13(right2(exec.getEmit()));
    }
    case OP_FROM_EFFECT3: {
      return pipe(state.effect, flatMap18(() => interpretToPull(exec.run(), exec)));
    }
    case OP_READ2: {
      return readUpstream(state, () => interpretToPull(exec.run(), exec), (cause3) => failCause10(cause3));
    }
  }
}, "interpretToPull");
var toQueue = /* @__PURE__ */ __name((queue) => suspend6(() => toQueueInternal(queue)), "toQueue");
var toQueueInternal = /* @__PURE__ */ __name((queue) => {
  return readWithCause({
    onInput: /* @__PURE__ */ __name((elem) => flatMap20(fromEffect5(offer3(queue, right2(elem))), () => toQueueInternal(queue)), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer3(queue, left2(failCause3(cause3)))), "onFailure"),
    onDone: /* @__PURE__ */ __name((done12) => fromEffect5(offer3(queue, left2(succeed3(done12)))), "onDone")
  });
}, "toQueueInternal");
var unwrap2 = /* @__PURE__ */ __name((channel) => flatten14(fromEffect5(channel)), "unwrap");
var unwrapScoped3 = /* @__PURE__ */ __name((self) => concatAllWith(scoped6(self), (d, _) => d, (d, _) => d), "unwrapScoped");
var unwrapScopedWith = /* @__PURE__ */ __name((f) => concatAllWith(scopedWith3(f), (d, _) => d, (d, _) => d), "unwrapScopedWith");
var updateService5 = /* @__PURE__ */ dual(3, (self, tag, f) => mapInputContext4(self, (context11) => merge3(context11, make6(tag, f(unsafeGet3(context11, tag))))));
var withSpan5 = /* @__PURE__ */ __name(function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  const acquire = all10([makeSpan2(name, options), context4(), clock2, get14(currentTracerTimingEnabled2)]);
  if (dataFirst) {
    const self = arguments[0];
    return acquireUseRelease4(acquire, ([span4, context11]) => provideContext4(self, add2(context11, spanTag, span4)), ([span4, , clock3, timingEnabled], exit4) => endSpan(span4, exit4, clock3, timingEnabled));
  }
  return (self) => acquireUseRelease4(acquire, ([span4, context11]) => provideContext4(self, add2(context11, spanTag, span4)), ([span4, , clock3, timingEnabled], exit4) => endSpan(span4, exit4, clock3, timingEnabled));
}, "withSpan");
var writeAll = /* @__PURE__ */ __name((...outs) => writeChunk(fromIterable3(outs)), "writeAll");
var writeChunk = /* @__PURE__ */ __name((outs) => writeChunkWriter(0, outs.length, outs), "writeChunk");
var writeChunkWriter = /* @__PURE__ */ __name((idx, len, chunk4) => {
  return idx === len ? void_8 : pipe(write(pipe(chunk4, unsafeGet4(idx))), flatMap20(() => writeChunkWriter(idx + 1, len, chunk4)));
}, "writeChunkWriter");
var zip13 = /* @__PURE__ */ dual((args2) => isChannel(args2[1]), (self, that, options) => options?.concurrent ? mergeWith2(self, {
  other: that,
  onSelfDone: /* @__PURE__ */ __name((exit1) => Await((exit22) => suspend4(() => zip6(exit1, exit22))), "onSelfDone"),
  onOtherDone: /* @__PURE__ */ __name((exit22) => Await((exit1) => suspend4(() => zip6(exit1, exit22))), "onOtherDone")
}) : flatMap20(self, (a) => map28(that, (b) => [a, b])));
var zipLeft8 = /* @__PURE__ */ dual((args2) => isChannel(args2[1]), (self, that, options) => options?.concurrent ? map28(zip13(self, that, {
  concurrent: true
}), (tuple5) => tuple5[0]) : flatMap20(self, (z) => as9(that, z)));
var zipRight8 = /* @__PURE__ */ dual((args2) => isChannel(args2[1]), (self, that, options) => options?.concurrent ? map28(zip13(self, that, {
  concurrent: true
}), (tuple5) => tuple5[1]) : flatMap20(self, () => that));
var ChannelExceptionTypeId = /* @__PURE__ */ Symbol.for("effect/Channel/ChannelException");
var ChannelException = /* @__PURE__ */ __name((error2) => ({
  _tag: "ChannelException",
  [ChannelExceptionTypeId]: ChannelExceptionTypeId,
  error: error2
}), "ChannelException");
var isChannelException = /* @__PURE__ */ __name((u) => hasProperty(u, ChannelExceptionTypeId), "isChannelException");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/sink.js
var SinkTypeId3 = /* @__PURE__ */ Symbol.for("effect/Sink");
var sinkVariance2 = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In"),
  /* c8 ignore next */
  _L: /* @__PURE__ */ __name((_) => _, "_L"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R")
};
var SinkImpl = class {
  static {
    __name(this, "SinkImpl");
  }
  channel;
  [SinkTypeId3] = sinkVariance2;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSink = /* @__PURE__ */ __name((u) => hasProperty(u, SinkTypeId3), "isSink");
var suspend7 = /* @__PURE__ */ __name((evaluate3) => new SinkImpl(suspend6(() => toChannel(evaluate3()))), "suspend");
var as10 = /* @__PURE__ */ dual(2, (self, a) => pipe(self, map29(() => a)));
var collectAll = /* @__PURE__ */ __name(() => new SinkImpl(collectAllLoop(empty7())), "collectAll");
var collectAllLoop = /* @__PURE__ */ __name((acc) => readWithCause({
  onInput: /* @__PURE__ */ __name((chunk4) => collectAllLoop(pipe(acc, appendAll3(chunk4))), "onInput"),
  onFailure: failCause12,
  onDone: /* @__PURE__ */ __name(() => succeed15(acc), "onDone")
}), "collectAllLoop");
var collectAllN = /* @__PURE__ */ __name((n) => suspend7(() => fromChannel(collectAllNLoop(n, empty7()))), "collectAllN");
var collectAllNLoop = /* @__PURE__ */ __name((n, acc) => readWithCause({
  onInput: /* @__PURE__ */ __name((chunk4) => {
    const [collected, leftovers] = splitAt2(chunk4, n);
    if (collected.length < n) {
      return collectAllNLoop(n - collected.length, appendAll3(acc, collected));
    }
    if (isEmpty2(leftovers)) {
      return succeed15(appendAll3(acc, collected));
    }
    return flatMap20(write(leftovers), () => succeed15(appendAll3(acc, collected)));
  }, "onInput"),
  onFailure: failCause12,
  onDone: /* @__PURE__ */ __name(() => succeed15(acc), "onDone")
}), "collectAllNLoop");
var collectAllFrom = /* @__PURE__ */ __name((self) => collectAllWhileWith(self, {
  initial: empty7(),
  while: constTrue,
  body: /* @__PURE__ */ __name((chunk4, a) => pipe(chunk4, append3(a)), "body")
}), "collectAllFrom");
var collectAllToMap = /* @__PURE__ */ __name((key, merge12) => {
  return foldLeftChunks(empty11(), (map36, chunk4) => pipe(chunk4, reduce4(map36, (map37, input) => {
    const k = key(input);
    const v = pipe(map37, has5(k)) ? merge12(pipe(map37, unsafeGet6(k)), input) : input;
    return pipe(map37, set4(k, v));
  })));
}, "collectAllToMap");
var collectAllToMapN = /* @__PURE__ */ __name((n, key, merge12) => {
  return foldWeighted({
    initial: empty11(),
    maxCost: n,
    cost: /* @__PURE__ */ __name((acc, input) => pipe(acc, has5(key(input))) ? 0 : 1, "cost"),
    body: /* @__PURE__ */ __name((acc, input) => {
      const k = key(input);
      const v = pipe(acc, has5(k)) ? merge12(pipe(acc, unsafeGet6(k)), input) : input;
      return pipe(acc, set4(k, v));
    }, "body")
  });
}, "collectAllToMapN");
var collectAllToSet = /* @__PURE__ */ __name(() => foldLeftChunks(empty10(), (acc, chunk4) => pipe(chunk4, reduce4(acc, (acc2, input) => pipe(acc2, add4(input))))), "collectAllToSet");
var collectAllToSetN = /* @__PURE__ */ __name((n) => foldWeighted({
  initial: empty10(),
  maxCost: n,
  cost: /* @__PURE__ */ __name((acc, input) => has4(acc, input) ? 0 : 1, "cost"),
  body: /* @__PURE__ */ __name((acc, input) => add4(acc, input), "body")
}), "collectAllToSetN");
var collectAllUntil = /* @__PURE__ */ __name((p) => {
  return pipe(fold([empty7(), true], (tuple5) => tuple5[1], ([chunk4, _], input) => [pipe(chunk4, append3(input)), !p(input)]), map29((tuple5) => tuple5[0]));
}, "collectAllUntil");
var collectAllUntilEffect = /* @__PURE__ */ __name((p) => {
  return pipe(foldEffect([empty7(), true], (tuple5) => tuple5[1], ([chunk4, _], input) => pipe(p(input), map26((bool) => [pipe(chunk4, append3(input)), !bool]))), map29((tuple5) => tuple5[0]));
}, "collectAllUntilEffect");
var collectAllWhile = /* @__PURE__ */ __name((predicate) => fromChannel(collectAllWhileReader(predicate, empty7())), "collectAllWhile");
var collectAllWhileReader = /* @__PURE__ */ __name((predicate, done12) => readWith({
  onInput: /* @__PURE__ */ __name((input) => {
    const [collected, leftovers] = pipe(toReadonlyArray(input), span(predicate));
    if (leftovers.length === 0) {
      return collectAllWhileReader(predicate, pipe(done12, appendAll3(unsafeFromArray(collected))));
    }
    return pipe(write(unsafeFromArray(leftovers)), zipRight8(succeed15(pipe(done12, appendAll3(unsafeFromArray(collected))))));
  }, "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => succeed15(done12), "onDone")
}), "collectAllWhileReader");
var collectAllWhileEffect = /* @__PURE__ */ __name((predicate) => fromChannel(collectAllWhileEffectReader(predicate, empty7())), "collectAllWhileEffect");
var collectAllWhileEffectReader = /* @__PURE__ */ __name((predicate, done12) => readWith({
  onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(pipe(input, takeWhile5(predicate), map26(unsafeFromArray))), flatMap20((collected) => {
    const leftovers = pipe(input, drop3(collected.length));
    if (isEmpty2(leftovers)) {
      return collectAllWhileEffectReader(predicate, pipe(done12, appendAll3(collected)));
    }
    return pipe(write(leftovers), zipRight8(succeed15(pipe(done12, appendAll3(collected)))));
  })), "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => succeed15(done12), "onDone")
}), "collectAllWhileEffectReader");
var collectAllWhileWith = /* @__PURE__ */ dual(2, (self, options) => {
  const refs = pipe(make35(empty7()), zip12(make35(false)));
  const newChannel = pipe(fromEffect5(refs), flatMap20(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: /* @__PURE__ */ __name((input) => pipe(write(input), flatMap20(() => upstreamMarker)), "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name((done12) => pipe(fromEffect5(set7(upstreamDoneRef, true)), as9(done12)), "onDone")
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, options.initial, options.while, options.body)));
  }));
  return new SinkImpl(newChannel);
});
var collectAllWhileWithLoop = /* @__PURE__ */ __name((self, leftoversRef, upstreamDoneRef, currentResult, p, f) => {
  return pipe(toChannel(self), doneCollect, foldChannel({
    onFailure: fail14,
    onSuccess: /* @__PURE__ */ __name(([leftovers, doneValue]) => p(doneValue) ? pipe(fromEffect5(set7(leftoversRef, flatten4(leftovers))), flatMap20(() => pipe(fromEffect5(get13(upstreamDoneRef)), flatMap20((upstreamDone) => {
      const accumulatedResult = f(currentResult, doneValue);
      return upstreamDone ? pipe(write(flatten4(leftovers)), as9(accumulatedResult)) : collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, accumulatedResult, p, f);
    })))) : pipe(write(flatten4(leftovers)), as9(currentResult)), "onSuccess")
  }));
}, "collectAllWhileWithLoop");
var collectLeftover = /* @__PURE__ */ __name((self) => new SinkImpl(pipe(collectElements(toChannel(self)), map28(([chunks3, z]) => [z, flatten4(chunks3)]))), "collectLeftover");
var mapInput8 = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapInputChunks(map7(f))));
var mapInputEffect3 = /* @__PURE__ */ dual(2, (self, f) => mapInputChunksEffect(self, (chunk4) => map26(forEach16(chunk4, (v) => f(v)), unsafeFromArray)));
var mapInputChunks = /* @__PURE__ */ dual(2, (self, f) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((chunk4) => pipe(write(f(chunk4)), flatMap20(() => loop4)), "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
  return new SinkImpl(pipe(loop4, pipeTo(toChannel(self))));
});
var mapInputChunksEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((chunk4) => pipe(fromEffect5(f(chunk4)), flatMap20(write), flatMap20(() => loop4)), "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
  return new SinkImpl(pipe(loop4, pipeToOrFail(toChannel(self))));
});
var die10 = /* @__PURE__ */ __name((defect) => failCause13(die6(defect)), "die");
var dieMessage3 = /* @__PURE__ */ __name((message) => failCause13(die6(new RuntimeException2(message))), "dieMessage");
var dieSync6 = /* @__PURE__ */ __name((evaluate3) => failCauseSync8(() => die6(evaluate3())), "dieSync");
var dimap = /* @__PURE__ */ dual(2, (self, options) => map29(mapInput8(self, options.onInput), options.onDone));
var dimapEffect = /* @__PURE__ */ dual(2, (self, options) => mapEffect6(mapInputEffect3(self, options.onInput), options.onDone));
var dimapChunks = /* @__PURE__ */ dual(2, (self, options) => map29(mapInputChunks(self, options.onInput), options.onDone));
var dimapChunksEffect = /* @__PURE__ */ dual(2, (self, options) => mapEffect6(mapInputChunksEffect(self, options.onInput), options.onDone));
var drain2 = /* @__PURE__ */ new SinkImpl(/* @__PURE__ */ drain(/* @__PURE__ */ identityChannel()));
var drop5 = /* @__PURE__ */ __name((n) => suspend7(() => new SinkImpl(dropLoop(n))), "drop");
var dropLoop = /* @__PURE__ */ __name((n) => readWith({
  onInput: /* @__PURE__ */ __name((input) => {
    const dropped = pipe(input, drop3(n));
    const leftover2 = Math.max(n - input.length, 0);
    const more = isEmpty2(input) || leftover2 > 0;
    if (more) {
      return dropLoop(leftover2);
    }
    return pipe(write(dropped), zipRight8(identityChannel()));
  }, "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
}), "dropLoop");
var dropUntil3 = /* @__PURE__ */ __name((predicate) => new SinkImpl(pipe(toChannel(dropWhile5((input) => !predicate(input))), pipeToOrFail(toChannel(drop5(1))))), "dropUntil");
var dropUntilEffect = /* @__PURE__ */ __name((predicate) => suspend7(() => new SinkImpl(dropUntilEffectReader(predicate))), "dropUntilEffect");
var dropUntilEffectReader = /* @__PURE__ */ __name((predicate) => readWith({
  onInput: /* @__PURE__ */ __name((input) => pipe(input, dropUntil2(predicate), map26((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropUntilEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight8(identityChannel()));
  }), unwrap2), "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
}), "dropUntilEffectReader");
var dropWhile5 = /* @__PURE__ */ __name((predicate) => new SinkImpl(dropWhileReader(predicate)), "dropWhile");
var dropWhileReader = /* @__PURE__ */ __name((predicate) => readWith({
  onInput: /* @__PURE__ */ __name((input) => {
    const out = pipe(input, dropWhile2(predicate));
    if (isEmpty2(out)) {
      return dropWhileReader(predicate);
    }
    return pipe(write(out), zipRight8(identityChannel()));
  }, "onInput"),
  onFailure: fail14,
  onDone: succeedNow
}), "dropWhileReader");
var dropWhileEffect = /* @__PURE__ */ __name((predicate) => suspend7(() => new SinkImpl(dropWhileEffectReader(predicate))), "dropWhileEffect");
var dropWhileEffectReader = /* @__PURE__ */ __name((predicate) => readWith({
  onInput: /* @__PURE__ */ __name((input) => pipe(input, dropWhile4(predicate), map26((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropWhileEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight8(identityChannel()));
  }), unwrap2), "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
}), "dropWhileEffectReader");
var ensuring6 = /* @__PURE__ */ dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuring5(finalizer3))));
var ensuringWith2 = /* @__PURE__ */ dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuringWith(finalizer3))));
var context7 = /* @__PURE__ */ __name(() => fromEffect7(context4()), "context");
var contextWith5 = /* @__PURE__ */ __name((f) => pipe(context7(), map29(f)), "contextWith");
var contextWithEffect4 = /* @__PURE__ */ __name((f) => pipe(context7(), mapEffect6(f)), "contextWithEffect");
var contextWithSink = /* @__PURE__ */ __name((f) => new SinkImpl(unwrap2(contextWith3((context11) => toChannel(f(context11))))), "contextWithSink");
var every14 = /* @__PURE__ */ __name((predicate) => fold(true, identity, (acc, input) => acc && predicate(input)), "every");
var fail15 = /* @__PURE__ */ __name((e) => new SinkImpl(fail14(e)), "fail");
var failSync8 = /* @__PURE__ */ __name((evaluate3) => new SinkImpl(failSync7(evaluate3)), "failSync");
var failCause13 = /* @__PURE__ */ __name((cause3) => new SinkImpl(failCause12(cause3)), "failCause");
var failCauseSync8 = /* @__PURE__ */ __name((evaluate3) => new SinkImpl(failCauseSync7(evaluate3)), "failCauseSync");
var filterInput = /* @__PURE__ */ __name((f) => {
  return (self) => pipe(self, mapInputChunks(filter5(f)));
}, "filterInput");
var filterInputEffect = /* @__PURE__ */ dual(2, (self, f) => mapInputChunksEffect(self, (chunk4) => map26(filter16(chunk4, f), unsafeFromArray)));
var findEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const newChannel = pipe(fromEffect5(pipe(make35(empty7()), zip12(make35(false)))), flatMap20(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: /* @__PURE__ */ __name((input) => pipe(write(input), flatMap20(() => upstreamMarker)), "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name((done12) => pipe(fromEffect5(set7(upstreamDoneRef, true)), as9(done12)), "onDone")
    });
    const loop4 = foldChannel(collectElements(toChannel(self)), {
      onFailure: fail14,
      onSuccess: /* @__PURE__ */ __name(([leftovers, doneValue]) => pipe(fromEffect5(f(doneValue)), flatMap20((satisfied) => pipe(fromEffect5(set7(leftoversRef, flatten4(leftovers))), zipRight8(pipe(fromEffect5(get13(upstreamDoneRef)), flatMap20((upstreamDone) => {
        if (satisfied) {
          return pipe(write(flatten4(leftovers)), as9(some3(doneValue)));
        }
        if (upstreamDone) {
          return pipe(write(flatten4(leftovers)), as9(none2()));
        }
        return loop4;
      })))))), "onSuccess")
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(loop4));
  }));
  return new SinkImpl(newChannel);
});
var fold = /* @__PURE__ */ __name((s, contFn, f) => suspend7(() => new SinkImpl(foldReader(s, contFn, f))), "fold");
var foldReader = /* @__PURE__ */ __name((s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const [nextS, leftovers] = foldChunkSplit(s, input, contFn, f, 0, input.length);
      if (isNonEmpty(leftovers)) {
        return pipe(write(leftovers), as9(nextS));
      }
      return foldReader(nextS, contFn, f);
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
  });
}, "foldReader");
var foldChunkSplit = /* @__PURE__ */ __name((s, chunk4, contFn, f, index, length5) => {
  if (index === length5) {
    return [s, empty7()];
  }
  const s1 = f(s, pipe(chunk4, unsafeGet4(index)));
  if (contFn(s1)) {
    return foldChunkSplit(s1, chunk4, contFn, f, index + 1, length5);
  }
  return [s1, pipe(chunk4, drop3(index + 1))];
}, "foldChunkSplit");
var foldSink = /* @__PURE__ */ dual(2, (self, options) => {
  const newChannel = pipe(toChannel(self), collectElements, foldChannel({
    onFailure: /* @__PURE__ */ __name((error2) => toChannel(options.onFailure(error2)), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(([leftovers, z]) => suspend6(() => {
      const leftoversRef = {
        ref: pipe(leftovers, filter5(isNonEmpty))
      };
      const refReader = pipe(
        sync10(() => {
          const ref = leftoversRef.ref;
          leftoversRef.ref = empty7();
          return ref;
        }),
        // This cast is safe because of the L1 >: L <: In1 bound. It follows that
        // L <: In1 and therefore Chunk[L] can be safely cast to Chunk[In1].
        flatMap20((chunk4) => writeChunk(chunk4))
      );
      const passthrough5 = identityChannel();
      const continuationSink = pipe(refReader, zipRight8(passthrough5), pipeTo(toChannel(options.onSuccess(z))));
      return flatMap20(collectElements(continuationSink), ([newLeftovers, z1]) => pipe(succeed15(leftoversRef.ref), flatMap20(writeChunk), zipRight8(writeChunk(newLeftovers)), as9(z1)));
    }), "onSuccess")
  }));
  return new SinkImpl(newChannel);
});
var foldChunks = /* @__PURE__ */ __name((s, contFn, f) => suspend7(() => new SinkImpl(foldChunksReader(s, contFn, f))), "foldChunks");
var foldChunksReader = /* @__PURE__ */ __name((s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: /* @__PURE__ */ __name((input) => foldChunksReader(f(s, input), contFn, f), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
  });
}, "foldChunksReader");
var foldChunksEffect = /* @__PURE__ */ __name((s, contFn, f) => suspend7(() => new SinkImpl(foldChunksEffectReader(s, contFn, f))), "foldChunksEffect");
var foldChunksEffectReader = /* @__PURE__ */ __name((s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(f(s, input)), flatMap20((s2) => foldChunksEffectReader(s2, contFn, f))), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
  });
}, "foldChunksEffectReader");
var foldEffect = /* @__PURE__ */ __name((s, contFn, f) => suspend7(() => new SinkImpl(foldEffectReader(s, contFn, f))), "foldEffect");
var foldEffectReader = /* @__PURE__ */ __name((s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(foldChunkSplitEffect(s, input, contFn, f)), flatMap20(([nextS, leftovers]) => pipe(leftovers, match2({
      onNone: /* @__PURE__ */ __name(() => foldEffectReader(nextS, contFn, f), "onNone"),
      onSome: /* @__PURE__ */ __name((leftover2) => pipe(write(leftover2), as9(nextS)), "onSome")
    })))), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
  });
}, "foldEffectReader");
var foldChunkSplitEffect = /* @__PURE__ */ __name((s, chunk4, contFn, f) => foldChunkSplitEffectInternal(s, chunk4, 0, chunk4.length, contFn, f), "foldChunkSplitEffect");
var foldChunkSplitEffectInternal = /* @__PURE__ */ __name((s, chunk4, index, length5, contFn, f) => {
  if (index === length5) {
    return succeed13([s, none2()]);
  }
  return pipe(f(s, pipe(chunk4, unsafeGet4(index))), flatMap18((s1) => contFn(s1) ? foldChunkSplitEffectInternal(s1, chunk4, index + 1, length5, contFn, f) : succeed13([s1, some3(pipe(chunk4, drop3(index + 1)))])));
}, "foldChunkSplitEffectInternal");
var foldLeft = /* @__PURE__ */ __name((s, f) => ignoreLeftover(fold(s, constTrue, f)), "foldLeft");
var foldLeftChunks = /* @__PURE__ */ __name((s, f) => foldChunks(s, constTrue, f), "foldLeftChunks");
var foldLeftChunksEffect = /* @__PURE__ */ __name((s, f) => ignoreLeftover(foldChunksEffect(s, constTrue, f)), "foldLeftChunksEffect");
var foldLeftEffect = /* @__PURE__ */ __name((s, f) => foldEffect(s, constTrue, f), "foldLeftEffect");
var foldUntil = /* @__PURE__ */ __name((s, max10, f) => pipe(fold([s, 0], (tuple5) => tuple5[1] < max10, ([output, count5], input) => [f(output, input), count5 + 1]), map29((tuple5) => tuple5[0])), "foldUntil");
var foldUntilEffect = /* @__PURE__ */ __name((s, max10, f) => pipe(foldEffect([s, 0], (tuple5) => tuple5[1] < max10, ([output, count5], input) => pipe(f(output, input), map26((s2) => [s2, count5 + 1]))), map29((tuple5) => tuple5[0])), "foldUntilEffect");
var foldWeighted = /* @__PURE__ */ __name((options) => foldWeightedDecompose({
  ...options,
  decompose: of3
}), "foldWeighted");
var foldWeightedDecompose = /* @__PURE__ */ __name((options) => suspend7(() => new SinkImpl(foldWeightedDecomposeLoop(options.initial, 0, false, options.maxCost, options.cost, options.decompose, options.body))), "foldWeightedDecompose");
var foldWeightedDecomposeLoop = /* @__PURE__ */ __name((s, cost, dirty, max10, costFn, decompose, f) => readWith({
  onInput: /* @__PURE__ */ __name((input) => {
    const [nextS, nextCost, nextDirty, leftovers] = foldWeightedDecomposeFold(input, s, cost, dirty, max10, costFn, decompose, f);
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight8(succeedNow(nextS)));
    }
    if (cost > max10) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeLoop(nextS, nextCost, nextDirty, max10, costFn, decompose, f);
  }, "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
}), "foldWeightedDecomposeLoop");
var foldWeightedDecomposeFold = /* @__PURE__ */ __name((input, s, cost, dirty, max10, costFn, decompose, f) => {
  for (let index = 0; index < input.length; index++) {
    const elem = unsafeGet4(input, index);
    const prevCost = cost;
    cost = cost + costFn(s, elem);
    if (cost <= max10) {
      s = f(s, elem);
      dirty = true;
      continue;
    }
    const decomposed = decompose(elem);
    if (decomposed.length <= 1 && !dirty) {
      return [f(s, elem), cost, true, drop3(input, index + 1)];
    }
    if (decomposed.length <= 1 && dirty) {
      return [s, prevCost, dirty, drop3(input, index)];
    }
    input = appendAll3(decomposed, drop3(input, index + 1));
    cost = prevCost;
    index = -1;
  }
  return [s, cost, dirty, empty7()];
}, "foldWeightedDecomposeFold");
var foldWeightedDecomposeEffect = /* @__PURE__ */ __name((options) => suspend7(() => new SinkImpl(foldWeightedDecomposeEffectLoop(options.initial, options.maxCost, options.cost, options.decompose, options.body, 0, false))), "foldWeightedDecomposeEffect");
var foldWeightedEffect = /* @__PURE__ */ __name((options) => foldWeightedDecomposeEffect({
  ...options,
  decompose: /* @__PURE__ */ __name((input) => succeed13(of3(input)), "decompose")
}), "foldWeightedEffect");
var foldWeightedDecomposeEffectLoop = /* @__PURE__ */ __name((s, max10, costFn, decompose, f, cost, dirty) => readWith({
  onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(foldWeightedDecomposeEffectFold(s, max10, costFn, decompose, f, input, dirty, cost, 0)), flatMap20(([nextS, nextCost, nextDirty, leftovers]) => {
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight8(succeedNow(nextS)));
    }
    if (cost > max10) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeEffectLoop(nextS, max10, costFn, decompose, f, nextCost, nextDirty);
  })), "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => succeedNow(s), "onDone")
}), "foldWeightedDecomposeEffectLoop");
var foldWeightedDecomposeEffectFold = /* @__PURE__ */ __name((s, max10, costFn, decompose, f, input, dirty, cost, index) => {
  if (index === input.length) {
    return succeed13([s, cost, dirty, empty7()]);
  }
  const elem = pipe(input, unsafeGet4(index));
  return pipe(costFn(s, elem), map26((newCost) => cost + newCost), flatMap18((total) => {
    if (total <= max10) {
      return pipe(f(s, elem), flatMap18((s2) => foldWeightedDecomposeEffectFold(s2, max10, costFn, decompose, f, input, true, total, index + 1)));
    }
    return pipe(decompose(elem), flatMap18((decomposed) => {
      if (decomposed.length <= 1 && !dirty) {
        return pipe(f(s, elem), map26((s2) => [s2, total, true, pipe(input, drop3(index + 1))]));
      }
      if (decomposed.length <= 1 && dirty) {
        return succeed13([s, cost, dirty, pipe(input, drop3(index))]);
      }
      const next4 = pipe(decomposed, appendAll3(pipe(input, drop3(index + 1))));
      return foldWeightedDecomposeEffectFold(s, max10, costFn, decompose, f, next4, dirty, cost, 0);
    }));
  }));
}, "foldWeightedDecomposeEffectFold");
var flatMap21 = /* @__PURE__ */ dual(2, (self, f) => foldSink(self, {
  onFailure: fail15,
  onSuccess: f
}));
var forEach17 = /* @__PURE__ */ __name((f) => {
  const process2 = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(forEach16(input, (v) => f(v), {
      discard: true
    })), flatMap20(() => process2)), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new SinkImpl(process2);
}, "forEach");
var forEachChunk = /* @__PURE__ */ __name((f) => {
  const process2 = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(f(input)), flatMap20(() => process2)), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new SinkImpl(process2);
}, "forEachChunk");
var forEachWhile = /* @__PURE__ */ __name((f) => {
  const process2 = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => forEachWhileReader(f, input, 0, input.length, process2), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new SinkImpl(process2);
}, "forEachWhile");
var forEachWhileReader = /* @__PURE__ */ __name((f, input, index, length5, cont) => {
  if (index === length5) {
    return cont;
  }
  return pipe(fromEffect5(f(pipe(input, unsafeGet4(index)))), flatMap20((bool) => bool ? forEachWhileReader(f, input, index + 1, length5, cont) : write(pipe(input, drop3(index)))), catchAll6((error2) => pipe(write(pipe(input, drop3(index))), zipRight8(fail14(error2)))));
}, "forEachWhileReader");
var forEachChunkWhile = /* @__PURE__ */ __name((f) => {
  const reader = readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(fromEffect5(f(input)), flatMap20((cont) => cont ? reader : void_8)), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new SinkImpl(reader);
}, "forEachChunkWhile");
var fromChannel = /* @__PURE__ */ __name((channel) => new SinkImpl(channel), "fromChannel");
var fromEffect7 = /* @__PURE__ */ __name((effect4) => new SinkImpl(fromEffect5(effect4)), "fromEffect");
var fromPubSub2 = /* @__PURE__ */ __name((pubsub, options) => fromQueue2(pubsub, options), "fromPubSub");
var fromPush = /* @__PURE__ */ __name((push) => new SinkImpl(unwrapScoped3(pipe(push, map26(fromPushPull)))), "fromPush");
var fromPushPull = /* @__PURE__ */ __name((push) => readWith({
  onInput: /* @__PURE__ */ __name((input) => foldChannel(fromEffect5(push(some3(input))), {
    onFailure: /* @__PURE__ */ __name(([either11, leftovers]) => match(either11, {
      onLeft: /* @__PURE__ */ __name((error2) => pipe(write(leftovers), zipRight8(fail14(error2))), "onLeft"),
      onRight: /* @__PURE__ */ __name((z) => pipe(write(leftovers), zipRight8(succeedNow(z))), "onRight")
    }), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => fromPushPull(push), "onSuccess")
  }), "onInput"),
  onFailure: fail14,
  onDone: /* @__PURE__ */ __name(() => foldChannel(fromEffect5(push(none2())), {
    onFailure: /* @__PURE__ */ __name(([either11, leftovers]) => match(either11, {
      onLeft: /* @__PURE__ */ __name((error2) => pipe(write(leftovers), zipRight8(fail14(error2))), "onLeft"),
      onRight: /* @__PURE__ */ __name((z) => pipe(write(leftovers), zipRight8(succeedNow(z))), "onRight")
    }), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => fromEffect5(dieMessage2("BUG: Sink.fromPush - please report an issue at https://github.com/Effect-TS/effect/issues")), "onSuccess")
  }), "onDone")
}), "fromPushPull");
var fromQueue2 = /* @__PURE__ */ __name((queue, options) => options?.shutdown ? unwrapScoped4(map26(acquireRelease3(succeed13(queue), shutdown4), fromQueue2)) : forEachChunk((input) => offerAll3(queue, input)), "fromQueue");
var head8 = /* @__PURE__ */ __name(() => fold(none2(), isNone2, (option5, input) => match2(option5, {
  onNone: /* @__PURE__ */ __name(() => some3(input), "onNone"),
  onSome: /* @__PURE__ */ __name(() => option5, "onSome")
})), "head");
var ignoreLeftover = /* @__PURE__ */ __name((self) => new SinkImpl(drain(toChannel(self))), "ignoreLeftover");
var last6 = /* @__PURE__ */ __name(() => foldLeftChunks(none2(), (s, input) => orElse2(last2(input), () => s)), "last");
var leftover = /* @__PURE__ */ __name((chunk4) => new SinkImpl(suspend6(() => write(chunk4))), "leftover");
var map29 = /* @__PURE__ */ dual(2, (self, f) => {
  return new SinkImpl(pipe(toChannel(self), map28(f)));
});
var mapEffect6 = /* @__PURE__ */ dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapEffect5(f))));
var mapError8 = /* @__PURE__ */ dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapError7(f))));
var mapLeftover = /* @__PURE__ */ dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapOut(map7(f)))));
var never7 = /* @__PURE__ */ fromEffect7(never5);
var orElse11 = /* @__PURE__ */ dual(2, (self, that) => new SinkImpl(pipe(toChannel(self), orElse10(() => toChannel(that())))));
var provideContext5 = /* @__PURE__ */ dual(2, (self, context11) => new SinkImpl(pipe(toChannel(self), provideContext4(context11))));
var race4 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, raceBoth(that), map29(merge)));
var raceBoth = /* @__PURE__ */ dual((args2) => isSink(args2[1]), (self, that, options) => raceWith3(self, {
  other: that,
  onSelfDone: /* @__PURE__ */ __name((selfDone) => Done3(map26(selfDone, left2)), "onSelfDone"),
  onOtherDone: /* @__PURE__ */ __name((thatDone) => Done3(map26(thatDone, right2)), "onOtherDone"),
  capacity: options?.capacity ?? 16
}));
var raceWith3 = /* @__PURE__ */ dual(2, (self, options) => {
  function race7(scope6) {
    return gen5(function* () {
      const pubsub = yield* bounded4(options?.capacity ?? 16);
      const subscription1 = yield* extend3(subscribe2(pubsub), scope6);
      const subscription2 = yield* extend3(subscribe2(pubsub), scope6);
      const reader = toPubSub(pubsub);
      const writer = fromQueue(subscription1).pipe(pipeTo(toChannel(self)), zipLeft8(fromEffect5(shutdown4(subscription1))), mergeWith2({
        other: fromQueue(subscription2).pipe(pipeTo(toChannel(options.other)), zipLeft8(fromEffect5(shutdown4(subscription2)))),
        onSelfDone: options.onSelfDone,
        onOtherDone: options.onOtherDone
      }));
      const racedChannel = mergeWith2(reader, {
        other: writer,
        onSelfDone: /* @__PURE__ */ __name(() => Await(identity), "onSelfDone"),
        onOtherDone: /* @__PURE__ */ __name((exit4) => Done3(exit4), "onOtherDone")
      });
      return new SinkImpl(racedChannel);
    });
  }
  __name(race7, "race");
  return unwrapScopedWith2(race7);
});
var refineOrDie = /* @__PURE__ */ dual(2, (self, pf) => pipe(self, refineOrDieWith(pf, identity)));
var refineOrDieWith = /* @__PURE__ */ dual(3, (self, pf, f) => {
  const newChannel = pipe(self, toChannel, catchAll6((error2) => match2(pf(error2), {
    onNone: /* @__PURE__ */ __name(() => failCauseSync7(() => die6(f(error2))), "onNone"),
    onSome: fail14
  })));
  return new SinkImpl(newChannel);
});
var service5 = /* @__PURE__ */ __name((tag) => serviceWith2(tag, identity), "service");
var serviceWith2 = /* @__PURE__ */ __name((tag, f) => fromEffect7(map26(tag, f)), "serviceWith");
var serviceWithEffect2 = /* @__PURE__ */ __name((tag, f) => fromEffect7(flatMap18(tag, f)), "serviceWithEffect");
var serviceWithSink = /* @__PURE__ */ __name((tag, f) => new SinkImpl(pipe(map26(tag, (service6) => toChannel(f(service6))), unwrap2)), "serviceWithSink");
var some15 = /* @__PURE__ */ __name((predicate) => fold(false, (bool) => !bool, (acc, input) => acc || predicate(input)), "some");
var splitWhere3 = /* @__PURE__ */ dual(2, (self, f) => {
  const newChannel = pipe(fromEffect5(make35(empty7())), flatMap20((ref) => pipe(splitWhereSplitter(false, ref, f), pipeToOrFail(toChannel(self)), collectElements, flatMap20(([leftovers, z]) => pipe(fromEffect5(get13(ref)), flatMap20((leftover2) => pipe(write(pipe(leftover2, appendAll3(flatten4(leftovers)))), zipRight8(succeed15(z)))))))));
  return new SinkImpl(newChannel);
});
var splitWhereSplitter = /* @__PURE__ */ __name((written, leftovers, f) => readWithCause({
  onInput: /* @__PURE__ */ __name((input) => {
    if (isEmpty2(input)) {
      return splitWhereSplitter(written, leftovers, f);
    }
    if (written) {
      const index2 = indexWhere(input, f);
      if (index2 === -1) {
        return zipRight8(write(input), splitWhereSplitter(true, leftovers, f));
      }
      const [left4, right4] = splitAt2(input, index2);
      return zipRight8(write(left4), fromEffect5(set7(leftovers, right4)));
    }
    const index = indexWhere(input, f, 1);
    if (index === -1) {
      return zipRight8(write(input), splitWhereSplitter(true, leftovers, f));
    }
    const [left3, right3] = pipe(input, splitAt2(Math.max(index, 1)));
    return zipRight8(write(left3), fromEffect5(set7(leftovers, right3)));
  }, "onInput"),
  onFailure: failCause12,
  onDone: succeed15
}), "splitWhereSplitter");
var indexWhere = /* @__PURE__ */ __name((self, predicate, from = 0) => {
  const iterator = self[Symbol.iterator]();
  let index = 0;
  let result = -1;
  let next4;
  while (result < 0 && (next4 = iterator.next()) && !next4.done) {
    const a = next4.value;
    if (index >= from && predicate(a)) {
      result = index;
    }
    index = index + 1;
  }
  return result;
}, "indexWhere");
var succeed16 = /* @__PURE__ */ __name((a) => new SinkImpl(succeed15(a)), "succeed");
var sum3 = /* @__PURE__ */ foldLeftChunks(0, (acc, chunk4) => acc + reduce4(chunk4, 0, (s, a) => s + a));
var summarized3 = /* @__PURE__ */ dual(3, (self, summary6, f) => {
  const newChannel = pipe(fromEffect5(summary6), flatMap20((start3) => pipe(self, toChannel, flatMap20((done12) => pipe(fromEffect5(summary6), map28((end6) => [done12, f(start3, end6)]))))));
  return new SinkImpl(newChannel);
});
var sync11 = /* @__PURE__ */ __name((evaluate3) => new SinkImpl(sync10(evaluate3)), "sync");
var take7 = /* @__PURE__ */ __name((n) => pipe(foldChunks(empty7(), (chunk4) => chunk4.length < n, (acc, chunk4) => pipe(acc, appendAll3(chunk4))), flatMap21((acc) => {
  const [taken, leftover2] = pipe(acc, splitAt2(n));
  return new SinkImpl(pipe(write(leftover2), zipRight8(succeedNow(taken))));
})), "take");
var toChannel = /* @__PURE__ */ __name((self) => isEffect2(self) ? toChannel(fromEffect7(self)) : self.channel, "toChannel");
var unwrap3 = /* @__PURE__ */ __name((effect4) => new SinkImpl(unwrap2(pipe(effect4, map26((sink) => toChannel(sink))))), "unwrap");
var unwrapScoped4 = /* @__PURE__ */ __name((effect4) => new SinkImpl(unwrapScoped3(effect4.pipe(map26((sink) => toChannel(sink))))), "unwrapScoped");
var unwrapScopedWith2 = /* @__PURE__ */ __name((f) => new SinkImpl(unwrapScopedWith((scope6) => f(scope6).pipe(map26((sink) => toChannel(sink))))), "unwrapScopedWith");
var withDuration = /* @__PURE__ */ __name((self) => pipe(self, summarized3(currentTimeMillis2, (start3, end6) => millis(end6 - start3))), "withDuration");
var zip14 = /* @__PURE__ */ dual((args2) => isSink(args2[1]), (self, that, options) => zipWith14(self, that, (z, z2) => [z, z2], options));
var zipLeft9 = /* @__PURE__ */ dual((args2) => isSink(args2[1]), (self, that, options) => zipWith14(self, that, (z, _) => z, options));
var zipRight9 = /* @__PURE__ */ dual((args2) => isSink(args2[1]), (self, that, options) => zipWith14(self, that, (_, z2) => z2, options));
var zipWith14 = /* @__PURE__ */ dual((args2) => isSink(args2[1]), (self, that, f, options) => options?.concurrent ? raceWith3(self, {
  other: that,
  onSelfDone: match7({
    onFailure: /* @__PURE__ */ __name((cause3) => Done3(failCause10(cause3)), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((leftZ) => Await(match7({
      onFailure: failCause10,
      onSuccess: /* @__PURE__ */ __name((rightZ) => succeed13(f(leftZ, rightZ)), "onSuccess")
    })), "onSuccess")
  }),
  onOtherDone: match7({
    onFailure: /* @__PURE__ */ __name((cause3) => Done3(failCause10(cause3)), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((rightZ) => Await(match7({
      onFailure: failCause10,
      onSuccess: /* @__PURE__ */ __name((leftZ) => succeed13(f(leftZ, rightZ)), "onSuccess")
    })), "onSuccess")
  })
}) : flatMap21(self, (z) => map29(that, (z2) => f(z, z2))));
var channelToSink = /* @__PURE__ */ __name((self) => new SinkImpl(self), "channelToSink");
var count3 = /* @__PURE__ */ foldLeftChunks(0, (acc, chunk4) => acc + chunk4.length);
var mkString = /* @__PURE__ */ suspend7(() => {
  const strings = [];
  return pipe(foldLeftChunks(void 0, (_, elems) => map7(elems, (elem) => {
    strings.push(String(elem));
  })), map29(() => strings.join("")));
});
var timed3 = /* @__PURE__ */ pipe(/* @__PURE__ */ withDuration(drain2), /* @__PURE__ */ map29((tuple5) => tuple5[1]));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/MergeDecision.js
var MergeDecisionTypeId2 = MergeDecisionTypeId;
var Done4 = Done3;
var Await2 = Await;
var AwaitConst2 = AwaitConst;
var isMergeDecision2 = isMergeDecision;
var match28 = match25;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/rcRef.js
var TypeId26 = /* @__PURE__ */ Symbol.for("effect/RcRef");
var stateEmpty2 = {
  _tag: "Empty"
};
var stateClosed = {
  _tag: "Closed"
};
var variance8 = {
  _A: identity,
  _E: identity
};
var RcRefImpl = class extends Class3 {
  static {
    __name(this, "RcRefImpl");
  }
  acquire;
  context;
  scope;
  idleTimeToLive;
  [TypeId26] = variance8;
  [TypeId17] = TypeId17;
  state = stateEmpty2;
  semaphore = /* @__PURE__ */ unsafeMakeSemaphore(1);
  constructor(acquire, context11, scope6, idleTimeToLive) {
    super();
    this.acquire = acquire;
    this.context = context11;
    this.scope = scope6;
    this.idleTimeToLive = idleTimeToLive;
    this.get = get15(this);
  }
  get;
  commit() {
    return this.get;
  }
};
var make61 = /* @__PURE__ */ __name((options) => withFiberRuntime((fiber) => {
  const context11 = fiber.getFiberRef(currentContext);
  const scope6 = get4(context11, scopeTag);
  const ref = new RcRefImpl(options.acquire, context11, scope6, options.idleTimeToLive ? decode(options.idleTimeToLive) : void 0);
  return as3(scope6.addFinalizer(() => ref.semaphore.withPermits(1)(suspend(() => {
    const close2 = ref.state._tag === "Acquired" ? scopeClose(ref.state.scope, exitVoid) : void_3;
    ref.state = stateClosed;
    return close2;
  }))), ref);
}), "make");
var get15 = /* @__PURE__ */ __name((self_) => {
  const self = self_;
  const isInfinite = self.idleTimeToLive && !isFinite(self.idleTimeToLive);
  return uninterruptibleMask((restore) => suspend(() => {
    switch (self.state._tag) {
      case "Closed": {
        return interrupt2;
      }
      case "Acquired": {
        self.state.refCount++;
        return self.state.fiber ? as3(interruptFiber(self.state.fiber), self.state) : succeed(self.state);
      }
      case "Empty": {
        return scopeMake2().pipe(bindTo5("scope"), bind5("value", ({
          scope: scope6
        }) => restore(fiberRefLocally(self.acquire, currentContext, add2(self.context, scopeTag, scope6)))), map14(({
          scope: scope6,
          value: value2
        }) => {
          const state = {
            _tag: "Acquired",
            value: value2,
            scope: scope6,
            fiber: void 0,
            refCount: 1
          };
          self.state = state;
          return state;
        }));
      }
    }
  })).pipe(self.semaphore.withPermits(1), bindTo5("state"), bind5("scope", () => scopeTag), tap2(({
    scope: scope6,
    state
  }) => scope6.addFinalizer(() => suspend(() => {
    state.refCount--;
    if (state.refCount > 0 || isInfinite) {
      return void_3;
    }
    if (self.idleTimeToLive === void 0) {
      self.state = stateEmpty2;
      return scopeClose(state.scope, exitVoid);
    }
    return sleep3(self.idleTimeToLive).pipe(interruptible2, zipRight2(suspend(() => {
      if (self.state._tag === "Acquired" && self.state.refCount === 0) {
        self.state = stateEmpty2;
        return scopeClose(state.scope, exitVoid);
      }
      return void_3;
    })), ensuring2(sync(() => {
      state.fiber = void 0;
    })), forkIn2(self.scope), tap2((fiber) => {
      state.fiber = fiber;
    }), self.semaphore.withPermits(1));
  }))), map14(({
    state
  }) => state.value));
}, "get");
var invalidate = /* @__PURE__ */ __name((self_) => {
  const self = self_;
  return uninterruptible(suspend(() => {
    if (self.state._tag !== "Acquired") {
      return void_3;
    }
    const state = self.state;
    self.state = stateEmpty2;
    return state.scope.close(exitVoid);
  }));
}, "invalidate");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/RcRef.js
var TypeId27 = TypeId26;
var make62 = make61;
var get16 = get15;
var invalidate2 = invalidate;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Runtime.js
var runFork3 = unsafeFork3;
var runSyncExit3 = unsafeRunSyncExit;
var runSync3 = unsafeRunSync;
var runCallback2 = unsafeRunCallback;
var runPromise3 = unsafeRunPromise;
var runPromiseExit3 = unsafeRunPromiseExit;
var defaultRuntime2 = defaultRuntime;
var defaultRuntimeFlags2 = defaultRuntimeFlags;
var make63 = make53;
var FiberFailureId2 = /* @__PURE__ */ Symbol.for("effect/Runtime/FiberFailure");
var FiberFailureCauseId2 = FiberFailureCauseId;
var isAsyncFiberException2 = isAsyncFiberException;
var isFiberFailure2 = isFiberFailure;
var makeFiberFailure = fiberFailure;
var updateRuntimeFlags3 = updateRuntimeFlags2;
var enableRuntimeFlag2 = enableRuntimeFlag;
var disableRuntimeFlag2 = disableRuntimeFlag;
var updateContext3 = updateContext2;
var provideService7 = provideService3;
var updateFiberRefs4 = updateFiberRefs2;
var setFiberRef2 = setFiberRef;
var deleteFiberRef2 = deleteFiberRef;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Schedule.js
var Schedule_exports = {};
__export(Schedule_exports, {
  CurrentIterationMetadata: () => CurrentIterationMetadata2,
  ScheduleDriverTypeId: () => ScheduleDriverTypeId2,
  ScheduleTypeId: () => ScheduleTypeId2,
  addDelay: () => addDelay2,
  addDelayEffect: () => addDelayEffect2,
  andThen: () => andThen12,
  andThenEither: () => andThenEither2,
  as: () => as11,
  asVoid: () => asVoid8,
  bothInOut: () => bothInOut2,
  check: () => check2,
  checkEffect: () => checkEffect2,
  collectAllInputs: () => collectAllInputs2,
  collectAllOutputs: () => collectAllOutputs2,
  collectUntil: () => collectUntil2,
  collectUntilEffect: () => collectUntilEffect2,
  collectWhile: () => collectWhile2,
  collectWhileEffect: () => collectWhileEffect2,
  compose: () => compose4,
  count: () => count4,
  cron: () => cron2,
  dayOfMonth: () => dayOfMonth2,
  dayOfWeek: () => dayOfWeek2,
  delayed: () => delayed2,
  delayedEffect: () => delayedEffect2,
  delayedSchedule: () => delayedSchedule2,
  delays: () => delays2,
  driver: () => driver2,
  duration: () => duration2,
  either: () => either7,
  eitherWith: () => eitherWith2,
  elapsed: () => elapsed2,
  ensuring: () => ensuring7,
  exponential: () => exponential3,
  fibonacci: () => fibonacci2,
  fixed: () => fixed4,
  forever: () => forever5,
  fromDelay: () => fromDelay2,
  fromDelays: () => fromDelays2,
  fromFunction: () => fromFunction4,
  hourOfDay: () => hourOfDay2,
  identity: () => identity3,
  intersect: () => intersect6,
  intersectWith: () => intersectWith2,
  isSchedule: () => isSchedule2,
  jittered: () => jittered2,
  jitteredWith: () => jitteredWith2,
  linear: () => linear3,
  makeWithState: () => makeWithState2,
  map: () => map30,
  mapBoth: () => mapBoth7,
  mapBothEffect: () => mapBothEffect2,
  mapEffect: () => mapEffect7,
  mapInput: () => mapInput9,
  mapInputContext: () => mapInputContext5,
  mapInputEffect: () => mapInputEffect4,
  minuteOfHour: () => minuteOfHour2,
  modifyDelay: () => modifyDelay2,
  modifyDelayEffect: () => modifyDelayEffect2,
  onDecision: () => onDecision2,
  once: () => once5,
  passthrough: () => passthrough4,
  provideContext: () => provideContext6,
  provideService: () => provideService8,
  recurUntil: () => recurUntil2,
  recurUntilEffect: () => recurUntilEffect2,
  recurUntilOption: () => recurUntilOption2,
  recurUpTo: () => recurUpTo2,
  recurWhile: () => recurWhile2,
  recurWhileEffect: () => recurWhileEffect2,
  recurs: () => recurs2,
  reduce: () => reduce18,
  reduceEffect: () => reduceEffect4,
  repeatForever: () => repeatForever2,
  repetitions: () => repetitions2,
  resetAfter: () => resetAfter2,
  resetWhen: () => resetWhen2,
  run: () => run3,
  secondOfMinute: () => secondOfMinute2,
  spaced: () => spaced2,
  stop: () => stop2,
  succeed: () => succeed17,
  sync: () => sync12,
  tapInput: () => tapInput2,
  tapOutput: () => tapOutput2,
  unfold: () => unfold4,
  union: () => union14,
  unionWith: () => unionWith3,
  untilInput: () => untilInput2,
  untilInputEffect: () => untilInputEffect2,
  untilOutput: () => untilOutput2,
  untilOutputEffect: () => untilOutputEffect2,
  upTo: () => upTo2,
  whileInput: () => whileInput2,
  whileInputEffect: () => whileInputEffect2,
  whileOutput: () => whileOutput2,
  whileOutputEffect: () => whileOutputEffect2,
  windowed: () => windowed2,
  zipLeft: () => zipLeft10,
  zipRight: () => zipRight10,
  zipWith: () => zipWith15
});
var ScheduleTypeId2 = ScheduleTypeId;
var ScheduleDriverTypeId2 = ScheduleDriverTypeId;
var makeWithState2 = makeWithState;
var isSchedule2 = isSchedule;
var addDelay2 = addDelay;
var addDelayEffect2 = addDelayEffect;
var andThen12 = andThen10;
var andThenEither2 = andThenEither;
var as11 = as7;
var asVoid8 = asVoid5;
var bothInOut2 = bothInOut;
var check2 = check;
var checkEffect2 = checkEffect;
var collectAllInputs2 = collectAllInputs;
var collectAllOutputs2 = collectAllOutputs;
var collectUntil2 = collectUntil;
var collectUntilEffect2 = collectUntilEffect;
var collectWhile2 = collectWhile;
var collectWhileEffect2 = collectWhileEffect;
var compose4 = compose3;
var mapInput9 = mapInput6;
var mapInputEffect4 = mapInputEffect;
var mapInputContext5 = mapInputContext2;
var count4 = count2;
var cron2 = cron;
var secondOfMinute2 = secondOfMinute;
var minuteOfHour2 = minuteOfHour;
var hourOfDay2 = hourOfDay;
var dayOfMonth2 = dayOfMonth;
var dayOfWeek2 = dayOfWeek;
var delayed2 = delayed;
var delayedEffect2 = delayedEffect;
var delayedSchedule2 = delayedSchedule;
var delays2 = delays;
var mapBoth7 = mapBoth5;
var mapBothEffect2 = mapBothEffect;
var driver2 = driver;
var duration2 = duration;
var either7 = either5;
var eitherWith2 = eitherWith;
var elapsed2 = elapsed;
var ensuring7 = ensuring3;
var exponential3 = exponential2;
var fibonacci2 = fibonacci;
var fixed4 = fixed3;
var forever5 = forever3;
var fromDelay2 = fromDelay;
var fromDelays2 = fromDelays;
var fromFunction4 = fromFunction2;
var identity3 = identity2;
var passthrough4 = passthrough2;
var intersect6 = intersect5;
var intersectWith2 = intersectWith;
var jittered2 = jittered;
var jitteredWith2 = jitteredWith;
var linear3 = linear2;
var map30 = map25;
var mapEffect7 = mapEffect4;
var modifyDelay2 = modifyDelay;
var modifyDelayEffect2 = modifyDelayEffect;
var onDecision2 = onDecision;
var once5 = once3;
var provideContext6 = provideContext3;
var provideService8 = provideService4;
var recurUntil2 = recurUntil;
var recurUntilEffect2 = recurUntilEffect;
var recurUntilOption2 = recurUntilOption;
var recurUpTo2 = recurUpTo;
var recurWhile2 = recurWhile;
var recurWhileEffect2 = recurWhileEffect;
var recurs2 = recurs;
var reduce18 = reduce16;
var reduceEffect4 = reduceEffect2;
var repeatForever2 = forever3;
var repetitions2 = repetitions;
var resetAfter2 = resetAfter;
var resetWhen2 = resetWhen;
var run3 = run;
var spaced2 = spaced;
var stop2 = stop;
var succeed17 = succeed11;
var sync12 = sync7;
var tapInput2 = tapInput;
var tapOutput2 = tapOutput;
var unfold4 = unfold3;
var union14 = union13;
var unionWith3 = unionWith2;
var untilInput2 = untilInput;
var untilInputEffect2 = untilInputEffect;
var untilOutput2 = untilOutput;
var untilOutputEffect2 = untilOutputEffect;
var upTo2 = upTo;
var whileInput2 = whileInput;
var whileInputEffect2 = whileInputEffect;
var whileOutput2 = whileOutput;
var whileOutputEffect2 = whileOutputEffect;
var windowed2 = windowed;
var zipLeft10 = zipLeft6;
var zipRight10 = zipRight6;
var zipWith15 = zipWith11;
var CurrentIterationMetadata2 = CurrentIterationMetadata;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/opCodes/streamHaltStrategy.js
var OP_LEFT = "Left";
var OP_RIGHT = "Right";
var OP_BOTH = "Both";
var OP_EITHER = "Either";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/haltStrategy.js
var Left = {
  _tag: OP_LEFT
};
var Right = {
  _tag: OP_RIGHT
};
var Both = {
  _tag: OP_BOTH
};
var Either = {
  _tag: OP_EITHER
};
var fromInput2 = /* @__PURE__ */ __name((input) => {
  switch (input) {
    case "left":
      return Left;
    case "right":
      return Right;
    case "both":
      return Both;
    case "either":
      return Either;
    default:
      return input;
  }
}, "fromInput");
var isLeft3 = /* @__PURE__ */ __name((self) => self._tag === OP_LEFT, "isLeft");
var isRight3 = /* @__PURE__ */ __name((self) => self._tag === OP_RIGHT, "isRight");
var isBoth = /* @__PURE__ */ __name((self) => self._tag === OP_BOTH, "isBoth");
var isEither3 = /* @__PURE__ */ __name((self) => self._tag === OP_EITHER, "isEither");
var match29 = /* @__PURE__ */ dual(2, (self, options) => {
  switch (self._tag) {
    case OP_LEFT: {
      return options.onLeft();
    }
    case OP_RIGHT: {
      return options.onRight();
    }
    case OP_BOTH: {
      return options.onBoth();
    }
    case OP_EITHER: {
      return options.onEither();
    }
  }
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/StreamHaltStrategy.js
var Left2 = Left;
var Right2 = Right;
var Both2 = Both;
var Either2 = Either;
var fromInput3 = fromInput2;
var isLeft4 = isLeft3;
var isRight4 = isRight3;
var isBoth2 = isBoth;
var isEither4 = isEither3;
var match30 = match29;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/versioned.js
var Versioned = class {
  static {
    __name(this, "Versioned");
  }
  value;
  constructor(value2) {
    this.value = value2;
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/entry.js
var make64 = /* @__PURE__ */ __name((ref, isNew) => ({
  ref,
  isNew,
  isChanged: false,
  expected: ref.versioned,
  newValue: ref.versioned.value
}), "make");
var unsafeGet8 = /* @__PURE__ */ __name((self) => {
  return self.newValue;
}, "unsafeGet");
var unsafeSet = /* @__PURE__ */ __name((self, value2) => {
  self.isChanged = true;
  self.newValue = value2;
}, "unsafeSet");
var commit = /* @__PURE__ */ __name((self) => {
  self.ref.versioned = new Versioned(self.newValue);
}, "commit");
var copy3 = /* @__PURE__ */ __name((self) => ({
  ref: self.ref,
  isNew: self.isNew,
  isChanged: self.isChanged,
  expected: self.expected,
  newValue: self.newValue
}), "copy");
var isValid = /* @__PURE__ */ __name((self) => {
  return self.ref.versioned === self.expected;
}, "isValid");
var isInvalid = /* @__PURE__ */ __name((self) => {
  return self.ref.versioned !== self.expected;
}, "isInvalid");
var isChanged = /* @__PURE__ */ __name((self) => {
  return self.isChanged;
}, "isChanged");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/journal.js
var JournalAnalysisInvalid = "Invalid";
var JournalAnalysisReadWrite = "ReadWrite";
var JournalAnalysisReadOnly = "ReadOnly";
var commitJournal = /* @__PURE__ */ __name((journal) => {
  for (const entry of journal) {
    commit(entry[1]);
  }
}, "commitJournal");
var analyzeJournal = /* @__PURE__ */ __name((journal) => {
  let val = JournalAnalysisReadOnly;
  for (const [, entry] of journal) {
    val = isInvalid(entry) ? JournalAnalysisInvalid : isChanged(entry) ? JournalAnalysisReadWrite : val;
    if (val === JournalAnalysisInvalid) {
      return val;
    }
  }
  return val;
}, "analyzeJournal");
var prepareResetJournal = /* @__PURE__ */ __name((journal) => {
  const saved = /* @__PURE__ */ new Map();
  for (const entry of journal) {
    saved.set(entry[0], copy3(entry[1]));
  }
  return () => {
    journal.clear();
    for (const entry of saved) {
      journal.set(entry[0], entry[1]);
    }
  };
}, "prepareResetJournal");
var collectTodos = /* @__PURE__ */ __name((journal) => {
  const allTodos = /* @__PURE__ */ new Map();
  for (const [, entry] of journal) {
    for (const todo of entry.ref.todos) {
      allTodos.set(todo[0], todo[1]);
    }
    entry.ref.todos = /* @__PURE__ */ new Map();
  }
  return allTodos;
}, "collectTodos");
var execTodos = /* @__PURE__ */ __name((todos) => {
  const todosSorted = Array.from(todos.entries()).sort((x, y) => x[0] - y[0]);
  for (const [_, todo] of todosSorted) {
    todo();
  }
}, "execTodos");
var addTodo = /* @__PURE__ */ __name((txnId, journal, todoEffect) => {
  let added = false;
  for (const [, entry] of journal) {
    if (!entry.ref.todos.has(txnId)) {
      entry.ref.todos.set(txnId, todoEffect);
      added = true;
    }
  }
  return added;
}, "addTodo");
var isValid2 = /* @__PURE__ */ __name((journal) => {
  let valid = true;
  for (const [, entry] of journal) {
    valid = isValid(entry);
    if (!valid) {
      return valid;
    }
  }
  return valid;
}, "isValid");
var isInvalid2 = /* @__PURE__ */ __name((journal) => {
  return !isValid2(journal);
}, "isInvalid");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/opCodes/stm.js
var OP_WITH_STM_RUNTIME = "WithSTMRuntime";
var OP_ON_FAILURE2 = "OnFailure";
var OP_ON_RETRY = "OnRetry";
var OP_ON_SUCCESS2 = "OnSuccess";
var OP_PROVIDE3 = "Provide";
var OP_SYNC2 = "Sync";
var OP_SUCCEED2 = "Succeed";
var OP_RETRY = "Retry";
var OP_FAIL4 = "Fail";
var OP_DIE2 = "Die";
var OP_INTERRUPT2 = "Interrupt";
var OP_TRACED = "Traced";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/opCodes/tExit.js
var OP_FAIL5 = "Fail";
var OP_DIE3 = "Die";
var OP_INTERRUPT3 = "Interrupt";
var OP_SUCCEED3 = "Succeed";
var OP_RETRY2 = "Retry";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/opCodes/tryCommit.js
var OP_DONE5 = "Done";
var OP_SUSPEND3 = "Suspend";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/opCodes/stmState.js
var OP_DONE6 = "Done";
var OP_INTERRUPTED = "Interrupted";
var OP_RUNNING2 = "Running";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/stmState.js
var STMStateSymbolKey = "effect/STM/State";
var STMStateTypeId = /* @__PURE__ */ Symbol.for(STMStateSymbolKey);
var isSTMState = /* @__PURE__ */ __name((u) => hasProperty(u, STMStateTypeId), "isSTMState");
var isRunning3 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_RUNNING2;
}, "isRunning");
var isDone7 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_DONE6;
}, "isDone");
var isInterrupted4 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_INTERRUPTED;
}, "isInterrupted");
var done9 = /* @__PURE__ */ __name((exit4) => {
  return {
    [STMStateTypeId]: STMStateTypeId,
    _tag: OP_DONE6,
    exit: exit4,
    [symbol2]() {
      return pipe(hash(STMStateSymbolKey), combine2(hash(OP_DONE6)), combine2(hash(exit4)), cached(this));
    },
    [symbol3](that) {
      return isSTMState(that) && that._tag === OP_DONE6 && equals(exit4, that.exit);
    }
  };
}, "done");
var interruptedHash = /* @__PURE__ */ pipe(/* @__PURE__ */ hash(STMStateSymbolKey), /* @__PURE__ */ combine2(/* @__PURE__ */ hash(OP_INTERRUPTED)), /* @__PURE__ */ combine2(/* @__PURE__ */ hash("interrupted")));
var interrupted3 = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_INTERRUPTED,
  [symbol2]() {
    return interruptedHash;
  },
  [symbol3](that) {
    return isSTMState(that) && that._tag === OP_INTERRUPTED;
  }
};
var runningHash = /* @__PURE__ */ pipe(/* @__PURE__ */ hash(STMStateSymbolKey), /* @__PURE__ */ combine2(/* @__PURE__ */ hash(OP_RUNNING2)), /* @__PURE__ */ combine2(/* @__PURE__ */ hash("running")));
var running3 = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_RUNNING2,
  [symbol2]() {
    return runningHash;
  },
  [symbol3](that) {
    return isSTMState(that) && that._tag === OP_RUNNING2;
  }
};
var fromTExit = /* @__PURE__ */ __name((tExit) => {
  switch (tExit._tag) {
    case OP_FAIL5: {
      return done9(fail4(tExit.error));
    }
    case OP_DIE3: {
      return done9(die4(tExit.defect));
    }
    case OP_INTERRUPT3: {
      return done9(interrupt4(tExit.fiberId));
    }
    case OP_SUCCEED3: {
      return done9(succeed3(tExit.value));
    }
    case OP_RETRY2: {
      throw new Error("BUG: STM.STMState.fromTExit - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
  }
}, "fromTExit");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/tExit.js
var TExitSymbolKey = "effect/TExit";
var TExitTypeId = /* @__PURE__ */ Symbol.for(TExitSymbolKey);
var variance9 = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E")
};
var isExit2 = /* @__PURE__ */ __name((u) => hasProperty(u, TExitTypeId), "isExit");
var isFail = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_FAIL5;
}, "isFail");
var isDie3 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_DIE3;
}, "isDie");
var isInterrupt = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_INTERRUPT3;
}, "isInterrupt");
var isSuccess4 = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_SUCCEED3;
}, "isSuccess");
var isRetry = /* @__PURE__ */ __name((self) => {
  return self._tag === OP_RETRY2;
}, "isRetry");
var fail16 = /* @__PURE__ */ __name((error2) => ({
  [TExitTypeId]: variance9,
  _tag: OP_FAIL5,
  error: error2,
  [symbol2]() {
    return pipe(hash(TExitSymbolKey), combine2(hash(OP_FAIL5)), combine2(hash(error2)), cached(this));
  },
  [symbol3](that) {
    return isExit2(that) && that._tag === OP_FAIL5 && equals(error2, that.error);
  }
}), "fail");
var die11 = /* @__PURE__ */ __name((defect) => ({
  [TExitTypeId]: variance9,
  _tag: OP_DIE3,
  defect,
  [symbol2]() {
    return pipe(hash(TExitSymbolKey), combine2(hash(OP_DIE3)), combine2(hash(defect)), cached(this));
  },
  [symbol3](that) {
    return isExit2(that) && that._tag === OP_DIE3 && equals(defect, that.defect);
  }
}), "die");
var interrupt9 = /* @__PURE__ */ __name((fiberId4) => ({
  [TExitTypeId]: variance9,
  _tag: OP_INTERRUPT3,
  fiberId: fiberId4,
  [symbol2]() {
    return pipe(hash(TExitSymbolKey), combine2(hash(OP_INTERRUPT3)), combine2(hash(fiberId4)), cached(this));
  },
  [symbol3](that) {
    return isExit2(that) && that._tag === OP_INTERRUPT3 && equals(fiberId4, that.fiberId);
  }
}), "interrupt");
var succeed18 = /* @__PURE__ */ __name((value2) => ({
  [TExitTypeId]: variance9,
  _tag: OP_SUCCEED3,
  value: value2,
  [symbol2]() {
    return pipe(hash(TExitSymbolKey), combine2(hash(OP_SUCCEED3)), combine2(hash(value2)), cached(this));
  },
  [symbol3](that) {
    return isExit2(that) && that._tag === OP_SUCCEED3 && equals(value2, that.value);
  }
}), "succeed");
var retryHash = /* @__PURE__ */ pipe(/* @__PURE__ */ hash(TExitSymbolKey), /* @__PURE__ */ combine2(/* @__PURE__ */ hash(OP_RETRY2)), /* @__PURE__ */ combine2(/* @__PURE__ */ hash("retry")));
var retry5 = {
  [TExitTypeId]: variance9,
  _tag: OP_RETRY2,
  [symbol2]() {
    return retryHash;
  },
  [symbol3](that) {
    return isExit2(that) && isRetry(that);
  }
};
var void_9 = /* @__PURE__ */ succeed18(void 0);

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/tryCommit.js
var done10 = /* @__PURE__ */ __name((exit4) => {
  return {
    _tag: OP_DONE5,
    exit: exit4
  };
}, "done");
var suspend8 = /* @__PURE__ */ __name((journal) => {
  return {
    _tag: OP_SUSPEND3,
    journal
  };
}, "suspend");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/txnId.js
var txnCounter = {
  ref: 0
};
var make65 = /* @__PURE__ */ __name(() => {
  const newId = txnCounter.ref + 1;
  txnCounter.ref = newId;
  return newId;
}, "make");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/core.js
var STMSymbolKey2 = "effect/STM";
var STMTypeId2 = /* @__PURE__ */ Symbol.for(STMSymbolKey2);
var stmVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var STMPrimitive = class {
  static {
    __name(this, "STMPrimitive");
  }
  effect_instruction_i0;
  _op = OP_COMMIT;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  [EffectTypeId4];
  [StreamTypeId];
  [SinkTypeId3];
  [ChannelTypeId3];
  get [STMTypeId2]() {
    return stmVariance;
  }
  constructor(effect_instruction_i0) {
    this.effect_instruction_i0 = effect_instruction_i0;
    this[EffectTypeId4] = effectVariance;
    this[StreamTypeId] = stmVariance;
    this[SinkTypeId3] = stmVariance;
    this[ChannelTypeId3] = stmVariance;
  }
  [symbol3](that) {
    return this === that;
  }
  [symbol2]() {
    return cached(this, random(this));
  }
  [Symbol.iterator]() {
    return new SingleShotGen2(new YieldWrap(this));
  }
  commit() {
    return unsafeAtomically(this, constVoid, constVoid);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSTM = /* @__PURE__ */ __name((u) => hasProperty(u, STMTypeId2), "isSTM");
var commit2 = /* @__PURE__ */ __name((self) => unsafeAtomically(self, constVoid, constVoid), "commit");
var unsafeAtomically = /* @__PURE__ */ __name((self, onDone3, onInterrupt4) => withFiberRuntime((state) => {
  const fiberId4 = state.id();
  const env = state.getFiberRef(currentContext2);
  const scheduler = state.getFiberRef(currentScheduler2);
  const priority = state.getFiberRef(currentSchedulingPriority2);
  const commitResult = tryCommitSync(fiberId4, self, env, scheduler, priority);
  switch (commitResult._tag) {
    case OP_DONE5: {
      onDone3(commitResult.exit);
      return commitResult.exit;
    }
    case OP_SUSPEND3: {
      const txnId = make65();
      const state2 = {
        value: running3
      };
      const effect4 = async2((k) => tryCommitAsync(fiberId4, self, txnId, state2, env, scheduler, priority, k));
      return uninterruptibleMask3((restore) => pipe(restore(effect4), catchAllCause4((cause3) => {
        let currentState = state2.value;
        if (isRunning3(currentState)) {
          state2.value = interrupted3;
        }
        currentState = state2.value;
        if (isDone7(currentState)) {
          onDone3(currentState.exit);
          return currentState.exit;
        }
        onInterrupt4();
        return failCause10(cause3);
      })));
    }
  }
}), "unsafeAtomically");
var tryCommit = /* @__PURE__ */ __name((fiberId4, stm, state, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId4, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommit - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      state.value = fromTExit(tExit);
      return completeTodos(succeed3(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL5: {
      state.value = fromTExit(tExit);
      const cause3 = fail8(tExit.error);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_DIE3: {
      state.value = fromTExit(tExit);
      const cause3 = die6(tExit.defect);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_INTERRUPT3: {
      state.value = fromTExit(tExit);
      const cause3 = interrupt6(fiberId4);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend8(journal);
    }
  }
}, "tryCommit");
var tryCommitSync = /* @__PURE__ */ __name((fiberId4, stm, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId4, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite && isSuccess4(tExit)) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommitSync - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      return completeTodos(succeed3(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL5: {
      const cause3 = fail8(tExit.error);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_DIE3: {
      const cause3 = die6(tExit.defect);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_INTERRUPT3: {
      const cause3 = interrupt6(fiberId4);
      return completeTodos(failCause3(cause3), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend8(journal);
    }
  }
}, "tryCommitSync");
var tryCommitAsync = /* @__PURE__ */ __name((fiberId4, self, txnId, state, context11, scheduler, priority, k) => {
  if (isRunning3(state.value)) {
    const result = tryCommit(fiberId4, self, state, context11, scheduler, priority);
    switch (result._tag) {
      case OP_DONE5: {
        completeTryCommit(result.exit, k);
        break;
      }
      case OP_SUSPEND3: {
        addTodo(txnId, result.journal, () => tryCommitAsync(fiberId4, self, txnId, state, context11, scheduler, priority, k));
        break;
      }
    }
  }
}, "tryCommitAsync");
var completeTodos = /* @__PURE__ */ __name((exit4, journal, scheduler, priority) => {
  const todos = collectTodos(journal);
  if (todos.size > 0) {
    scheduler.scheduleTask(() => execTodos(todos), priority);
  }
  return done10(exit4);
}, "completeTodos");
var completeTryCommit = /* @__PURE__ */ __name((exit4, k) => {
  k(exit4);
}, "completeTryCommit");
var context8 = /* @__PURE__ */ __name(() => effect3((_, __, env) => env), "context");
var contextWith6 = /* @__PURE__ */ __name((f) => map31(context8(), f), "contextWith");
var contextWithSTM = /* @__PURE__ */ __name((f) => flatMap22(context8(), f), "contextWithSTM");
var STMDriver = class {
  static {
    __name(this, "STMDriver");
  }
  self;
  journal;
  fiberId;
  contStack = [];
  env;
  constructor(self, journal, fiberId4, r0) {
    this.self = self;
    this.journal = journal;
    this.fiberId = fiberId4;
    this.env = r0;
  }
  getEnv() {
    return this.env;
  }
  pushStack(cont) {
    this.contStack.push(cont);
  }
  popStack() {
    return this.contStack.pop();
  }
  nextSuccess() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_SUCCESS2) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextFailure() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_FAILURE2) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextRetry() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_RETRY) {
      current2 = this.popStack();
    }
    return current2;
  }
  run() {
    let curr = this.self;
    let exit4 = void 0;
    while (exit4 === void 0 && curr !== void 0) {
      try {
        const current2 = curr;
        if (current2) {
          switch (current2._op) {
            case "Tag": {
              curr = effect3((_, __, env) => unsafeGet3(env, current2));
              break;
            }
            case "Left": {
              curr = fail17(current2.left);
              break;
            }
            case "None": {
              curr = fail17(new NoSuchElementException3());
              break;
            }
            case "Right": {
              curr = succeed19(current2.right);
              break;
            }
            case "Some": {
              curr = succeed19(current2.value);
              break;
            }
            case "Commit": {
              switch (current2.effect_instruction_i0) {
                case OP_DIE2: {
                  exit4 = die11(internalCall(() => current2.effect_instruction_i1()));
                  break;
                }
                case OP_FAIL4: {
                  const cont = this.nextFailure();
                  if (cont === void 0) {
                    exit4 = fail16(internalCall(() => current2.effect_instruction_i1()));
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(internalCall(() => current2.effect_instruction_i1())));
                  }
                  break;
                }
                case OP_RETRY: {
                  const cont = this.nextRetry();
                  if (cont === void 0) {
                    exit4 = retry5;
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2());
                  }
                  break;
                }
                case OP_INTERRUPT2: {
                  exit4 = interrupt9(this.fiberId);
                  break;
                }
                case OP_WITH_STM_RUNTIME: {
                  curr = internalCall(() => current2.effect_instruction_i1(this));
                  break;
                }
                case OP_ON_SUCCESS2:
                case OP_ON_FAILURE2:
                case OP_ON_RETRY: {
                  this.pushStack(current2);
                  curr = current2.effect_instruction_i1;
                  break;
                }
                case OP_PROVIDE3: {
                  const env = this.env;
                  this.env = internalCall(() => current2.effect_instruction_i2(env));
                  curr = pipe(current2.effect_instruction_i1, ensuring8(sync13(() => this.env = env)));
                  break;
                }
                case OP_SUCCEED2: {
                  const value2 = current2.effect_instruction_i1;
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit4 = succeed18(value2);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value2));
                  }
                  break;
                }
                case OP_SYNC2: {
                  const value2 = internalCall(() => current2.effect_instruction_i1());
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit4 = succeed18(value2);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value2));
                  }
                  break;
                }
              }
              break;
            }
          }
        }
      } catch (e) {
        curr = die12(e);
      }
    }
    return exit4;
  }
};
var catchAll7 = /* @__PURE__ */ dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_FAILURE2);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var mapInputContext6 = /* @__PURE__ */ dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_PROVIDE3);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var die12 = /* @__PURE__ */ __name((defect) => dieSync7(() => defect), "die");
var dieMessage4 = /* @__PURE__ */ __name((message) => dieSync7(() => new RuntimeException2(message)), "dieMessage");
var dieSync7 = /* @__PURE__ */ __name((evaluate3) => {
  const stm = new STMPrimitive(OP_DIE2);
  stm.effect_instruction_i1 = evaluate3;
  return stm;
}, "dieSync");
var effect3 = /* @__PURE__ */ __name((f) => withSTMRuntime((_) => succeed19(f(_.journal, _.fiberId, _.getEnv()))), "effect");
var ensuring8 = /* @__PURE__ */ dual(2, (self, finalizer3) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => zipRight11(finalizer3, fail17(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => zipRight11(finalizer3, succeed19(a)), "onSuccess")
}));
var fail17 = /* @__PURE__ */ __name((error2) => failSync9(() => error2), "fail");
var failSync9 = /* @__PURE__ */ __name((evaluate3) => {
  const stm = new STMPrimitive(OP_FAIL4);
  stm.effect_instruction_i1 = evaluate3;
  return stm;
}, "failSync");
var flatMap22 = /* @__PURE__ */ dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_SUCCESS2);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var matchSTM = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => pipe(self, map31(right2), catchAll7((e) => pipe(onFailure(e), map31(left2))), flatMap22((either11) => {
  switch (either11._tag) {
    case "Left": {
      return succeed19(either11.left);
    }
    case "Right": {
      return onSuccess(either11.right);
    }
  }
})));
var withSTMRuntime = /* @__PURE__ */ __name((f) => {
  const stm = new STMPrimitive(OP_WITH_STM_RUNTIME);
  stm.effect_instruction_i1 = f;
  return stm;
}, "withSTMRuntime");
var interrupt10 = /* @__PURE__ */ withSTMRuntime((_) => {
  const stm = new STMPrimitive(OP_INTERRUPT2);
  stm.effect_instruction_i1 = _.fiberId;
  return stm;
});
var interruptAs2 = /* @__PURE__ */ __name((fiberId4) => {
  const stm = new STMPrimitive(OP_INTERRUPT2);
  stm.effect_instruction_i1 = fiberId4;
  return stm;
}, "interruptAs");
var map31 = /* @__PURE__ */ dual(2, (self, f) => pipe(self, flatMap22((a) => sync13(() => f(a)))));
var orTry = /* @__PURE__ */ dual(2, (self, that) => {
  const stm = new STMPrimitive(OP_ON_RETRY);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = that;
  return stm;
});
var retry6 = /* @__PURE__ */ new STMPrimitive(OP_RETRY);
var succeed19 = /* @__PURE__ */ __name((value2) => {
  const stm = new STMPrimitive(OP_SUCCEED2);
  stm.effect_instruction_i1 = value2;
  return stm;
}, "succeed");
var sync13 = /* @__PURE__ */ __name((evaluate3) => {
  const stm = new STMPrimitive(OP_SYNC2);
  stm.effect_instruction_i1 = evaluate3;
  return stm;
}, "sync");
var zip15 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, zipWith16(that, (a, a1) => [a, a1])));
var zipLeft11 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, flatMap22((a) => pipe(that, map31(() => a)))));
var zipRight11 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, flatMap22(() => that)));
var zipWith16 = /* @__PURE__ */ dual(3, (self, that, f) => pipe(self, flatMap22((a) => pipe(that, map31((b) => f(a, b))))));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/opCodes/strategy.js
var OP_BACKPRESSURE_STRATEGY = "BackPressure";
var OP_DROPPING_STRATEGY = "Dropping";
var OP_SLIDING_STRATEGY = "Sliding";

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/stm.js
var acquireUseRelease5 = /* @__PURE__ */ dual(3, (acquire, use2, release) => uninterruptibleMask3((restore) => {
  let state = running3;
  return pipe(restore(unsafeAtomically(acquire, (exit4) => {
    state = done9(exit4);
  }, () => {
    state = interrupted3;
  })), matchCauseEffect3({
    onFailure: /* @__PURE__ */ __name((cause3) => {
      if (isDone7(state) && isSuccess(state.exit)) {
        return pipe(release(state.exit.value), matchCauseEffect3({
          onFailure: /* @__PURE__ */ __name((cause22) => failCause10(parallel4(cause3, cause22)), "onFailure"),
          onSuccess: /* @__PURE__ */ __name(() => failCause10(cause3), "onSuccess")
        }));
      }
      return failCause10(cause3);
    }, "onFailure"),
    onSuccess: /* @__PURE__ */ __name((a) => pipe(restore(use2(a)), matchCauseEffect3({
      onFailure: /* @__PURE__ */ __name((cause3) => pipe(release(a), matchCauseEffect3({
        onFailure: /* @__PURE__ */ __name((cause22) => failCause10(parallel4(cause3, cause22)), "onFailure"),
        onSuccess: /* @__PURE__ */ __name(() => failCause10(cause3), "onSuccess")
      })), "onFailure"),
      onSuccess: /* @__PURE__ */ __name((a2) => pipe(release(a), as8(a2)), "onSuccess")
    })), "onSuccess")
  }));
}));
var as12 = /* @__PURE__ */ dual(2, (self, value2) => pipe(self, map31(() => value2)));
var asSome4 = /* @__PURE__ */ __name((self) => pipe(self, map31(some3)), "asSome");
var asSomeError3 = /* @__PURE__ */ __name((self) => pipe(self, mapError9(some3)), "asSomeError");
var asVoid9 = /* @__PURE__ */ __name((self) => pipe(self, map31(constVoid)), "asVoid");
var attempt = /* @__PURE__ */ __name((evaluate3) => suspend9(() => {
  try {
    return succeed19(evaluate3());
  } catch (defect) {
    return fail17(defect);
  }
}), "attempt");
var bind8 = /* @__PURE__ */ dual(3, (self, tag, f) => flatMap22(self, (k) => map31(f(k), (a) => ({
  ...k,
  [tag]: a
}))));
var bindTo8 = /* @__PURE__ */ dual(2, (self, tag) => map31(self, (a) => ({
  [tag]: a
})));
var let_8 = /* @__PURE__ */ dual(3, (self, tag, f) => map31(self, (k) => ({
  ...k,
  [tag]: f(k)
})));
var catchSome3 = /* @__PURE__ */ dual(2, (self, pf) => catchAll7(self, (e) => getOrElse2(pf(e), () => fail17(e))));
var catchTag4 = /* @__PURE__ */ dual(3, (self, k, f) => catchAll7(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail17(e);
}));
var catchTags3 = /* @__PURE__ */ dual(2, (self, cases) => catchAll7(self, (e) => {
  const keys7 = Object.keys(cases);
  if ("_tag" in e && keys7.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail17(e);
}));
var check3 = /* @__PURE__ */ __name((predicate) => suspend9(() => predicate() ? void_10 : retry6), "check");
var collect3 = /* @__PURE__ */ dual(2, (self, pf) => collectSTM(self, (a) => map2(pf(a), succeed19)));
var collectSTM = /* @__PURE__ */ dual(2, (self, pf) => matchSTM(self, {
  onFailure: fail17,
  onSuccess: /* @__PURE__ */ __name((a) => {
    const option5 = pf(a);
    return isSome2(option5) ? option5.value : retry6;
  }, "onSuccess")
}));
var commitEither = /* @__PURE__ */ __name((self) => flatten12(commit2(either8(self))), "commitEither");
var cond = /* @__PURE__ */ __name((predicate, error2, result) => {
  return suspend9(() => predicate() ? sync13(result) : failSync9(error2));
}, "cond");
var either8 = /* @__PURE__ */ __name((self) => match31(self, {
  onFailure: left2,
  onSuccess: right2
}), "either");
var eventually3 = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name(() => eventually3(self), "onFailure"),
  onSuccess: succeed19
}), "eventually");
var every15 = /* @__PURE__ */ dual(2, (iterable, predicate) => flatMap22(sync13(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop4 = suspend9(() => {
    const next4 = iterator.next();
    if (next4.done) {
      return succeed19(true);
    }
    return pipe(predicate(next4.value), flatMap22((bool) => bool ? loop4 : succeed19(bool)));
  });
  return loop4;
}));
var exists5 = /* @__PURE__ */ dual(2, (iterable, predicate) => flatMap22(sync13(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop4 = suspend9(() => {
    const next4 = iterator.next();
    if (next4.done) {
      return succeed19(false);
    }
    return flatMap22(predicate(next4.value), (bool) => bool ? succeed19(bool) : loop4);
  });
  return loop4;
}));
var fiberId3 = /* @__PURE__ */ effect3((_, fiberId4) => fiberId4);
var filter17 = /* @__PURE__ */ dual(2, (iterable, predicate) => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith16(predicate(curr), (as15, p) => {
  if (p) {
    as15.push(curr);
    return as15;
  }
  return as15;
})), succeed19([])));
var filterNot = /* @__PURE__ */ dual(2, (iterable, predicate) => filter17(iterable, (a) => negate4(predicate(a))));
var filterOrDie3 = /* @__PURE__ */ dual(3, (self, predicate, defect) => filterOrElse3(self, predicate, () => dieSync7(defect)));
var filterOrDieMessage3 = /* @__PURE__ */ dual(3, (self, predicate, message) => filterOrElse3(self, predicate, () => dieMessage4(message)));
var filterOrElse3 = /* @__PURE__ */ dual(3, (self, predicate, orElse15) => flatMap22(self, (a) => predicate(a) ? succeed19(a) : orElse15(a)));
var filterOrFail4 = /* @__PURE__ */ dual(3, (self, predicate, orFailWith) => filterOrElse3(self, predicate, (a) => failSync9(() => orFailWith(a))));
var flatten15 = /* @__PURE__ */ __name((self) => flatMap22(self, identity), "flatten");
var flip6 = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: succeed19,
  onSuccess: fail17
}), "flip");
var flipWith3 = /* @__PURE__ */ dual(2, (self, f) => flip6(f(flip6(self))));
var match31 = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => succeed19(onFailure(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed19(onSuccess(a)), "onSuccess")
}));
var forEach18 = /* @__PURE__ */ dual((args2) => isIterable(args2[0]), (iterable, f, options) => {
  if (options?.discard) {
    return pipe(sync13(() => iterable[Symbol.iterator]()), flatMap22((iterator) => {
      const loop4 = suspend9(() => {
        const next4 = iterator.next();
        if (next4.done) {
          return void_10;
        }
        return pipe(f(next4.value), flatMap22(() => loop4));
      });
      return loop4;
    }));
  }
  return suspend9(() => fromIterable2(iterable).reduce((acc, curr) => zipWith16(acc, f(curr), (array5, elem) => {
    array5.push(elem);
    return array5;
  }), succeed19([])));
});
var fromEither4 = /* @__PURE__ */ __name((either11) => {
  switch (either11._tag) {
    case "Left": {
      return fail17(either11.left);
    }
    case "Right": {
      return succeed19(either11.right);
    }
  }
}, "fromEither");
var fromOption7 = /* @__PURE__ */ __name((option5) => match2(option5, {
  onNone: /* @__PURE__ */ __name(() => fail17(none2()), "onNone"),
  onSome: succeed19
}), "fromOption");
var gen6 = /* @__PURE__ */ __name((...args2) => suspend9(() => {
  const f = args2.length === 1 ? args2[0] : args2[1].bind(args2[0]);
  const iterator = f(pipe);
  const state = iterator.next();
  const run6 = /* @__PURE__ */ __name((state2) => state2.done ? succeed19(state2.value) : flatMap22(yieldWrapGet(state2.value), (val) => run6(iterator.next(val))), "run");
  return run6(state);
}), "gen");
var head9 = /* @__PURE__ */ __name((self) => pipe(self, matchSTM({
  onFailure: /* @__PURE__ */ __name((e) => fail17(some3(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => {
    const i = a[Symbol.iterator]();
    const res = i.next();
    if (res.done) {
      return fail17(none2());
    } else {
      return succeed19(res.value);
    }
  }, "onSuccess")
})), "head");
var if_3 = /* @__PURE__ */ dual((args2) => typeof args2[0] === "boolean" || isSTM(args2[0]), (self, {
  onFalse,
  onTrue
}) => {
  if (typeof self === "boolean") {
    return self ? onTrue : onFalse;
  }
  return flatMap22(self, (bool) => bool ? onTrue : onFalse);
});
var ignore4 = /* @__PURE__ */ __name((self) => match31(self, {
  onFailure: /* @__PURE__ */ __name(() => void_10, "onFailure"),
  onSuccess: /* @__PURE__ */ __name(() => void_10, "onSuccess")
}), "ignore");
var isFailure6 = /* @__PURE__ */ __name((self) => match31(self, {
  onFailure: constTrue,
  onSuccess: constFalse
}), "isFailure");
var isSuccess5 = /* @__PURE__ */ __name((self) => match31(self, {
  onFailure: constFalse,
  onSuccess: constTrue
}), "isSuccess");
var iterate3 = /* @__PURE__ */ __name((initial, options) => iterateLoop(initial, options.while, options.body), "iterate");
var iterateLoop = /* @__PURE__ */ __name((initial, cont, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap22((z) => iterateLoop(z, cont, body)));
  }
  return succeed19(initial);
}, "iterateLoop");
var loop3 = /* @__PURE__ */ __name((initial, options) => options.discard ? loopDiscardLoop(initial, options.while, options.step, options.body) : map31(loopLoop(initial, options.while, options.step, options.body), (a) => Array.from(a)), "loop");
var loopLoop = /* @__PURE__ */ __name((initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap22((a) => pipe(loopLoop(inc(initial), cont, inc, body), map31(append3(a)))));
  }
  return succeed19(empty7());
}, "loopLoop");
var loopDiscardLoop = /* @__PURE__ */ __name((initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap22(() => loopDiscardLoop(inc(initial), cont, inc, body)));
  }
  return void_10;
}, "loopDiscardLoop");
var mapAttempt = /* @__PURE__ */ dual(2, (self, f) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => fail17(e), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => attempt(() => f(a)), "onSuccess")
}));
var mapBoth8 = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => fail17(onFailure(e)), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => succeed19(onSuccess(a)), "onSuccess")
}));
var mapError9 = /* @__PURE__ */ dual(2, (self, f) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => fail17(f(e)), "onFailure"),
  onSuccess: succeed19
}));
var merge9 = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => succeed19(e), "onFailure"),
  onSuccess: succeed19
}), "merge");
var mergeAll8 = /* @__PURE__ */ dual(3, (iterable, zero2, f) => suspend9(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith16(curr, f)), succeed19(zero2))));
var negate4 = /* @__PURE__ */ __name((self) => pipe(self, map31((b) => !b)), "negate");
var none10 = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => fail17(some3(e)), "onFailure"),
  onSuccess: match2({
    onNone: /* @__PURE__ */ __name(() => void_10, "onNone"),
    onSome: /* @__PURE__ */ __name(() => fail17(none2()), "onSome")
  })
}), "none");
var option4 = /* @__PURE__ */ __name((self) => match31(self, {
  onFailure: /* @__PURE__ */ __name(() => none2(), "onFailure"),
  onSuccess: some3
}), "option");
var orDie7 = /* @__PURE__ */ __name((self) => pipe(self, orDieWith4(identity)), "orDie");
var orDieWith4 = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapError9(f), catchAll7(die12)));
var orElse12 = /* @__PURE__ */ dual(2, (self, that) => flatMap22(effect3((journal) => prepareResetJournal(journal)), (reset3) => pipe(orTry(self, () => flatMap22(sync13(reset3), that)), catchAll7(() => flatMap22(sync13(reset3), that)))));
var orElseEither6 = /* @__PURE__ */ dual(2, (self, that) => orElse12(map31(self, left2), () => map31(that(), right2)));
var orElseFail3 = /* @__PURE__ */ dual(2, (self, error2) => orElse12(self, () => failSync9(error2)));
var orElseOptional = /* @__PURE__ */ dual(2, (self, that) => catchAll7(self, match2({
  onNone: that,
  onSome: /* @__PURE__ */ __name((e) => fail17(some3(e)), "onSome")
})));
var orElseSucceed4 = /* @__PURE__ */ dual(2, (self, value2) => orElse12(self, () => sync13(value2)));
var provideContext7 = /* @__PURE__ */ dual(2, (self, env) => mapInputContext6(self, (_) => env));
var provideSomeContext2 = /* @__PURE__ */ dual(2, (self, context11) => mapInputContext6(self, (parent) => merge3(parent, context11)));
var provideService9 = /* @__PURE__ */ dual(3, (self, tag, resource) => provideServiceSTM(self, tag, succeed19(resource)));
var provideServiceSTM = /* @__PURE__ */ dual(3, (self, tag, stm) => contextWithSTM((env) => flatMap22(stm, (service6) => provideContext7(self, add2(env, tag, service6)))));
var reduce19 = /* @__PURE__ */ dual(3, (iterable, zero2, f) => suspend9(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, flatMap22((s) => f(s, curr))), succeed19(zero2))));
var reduceAll = /* @__PURE__ */ dual(3, (iterable, initial, f) => suspend9(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith16(curr, f)), initial)));
var reduceRight6 = /* @__PURE__ */ dual(3, (iterable, zero2, f) => suspend9(() => Array.from(iterable).reduceRight((acc, curr) => pipe(acc, flatMap22((s) => f(s, curr))), succeed19(zero2))));
var refineOrDie2 = /* @__PURE__ */ dual(2, (self, pf) => refineOrDieWith2(self, pf, identity));
var refineOrDieWith2 = /* @__PURE__ */ dual(3, (self, pf, f) => catchAll7(self, (e) => match2(pf(e), {
  onNone: /* @__PURE__ */ __name(() => die12(f(e)), "onNone"),
  onSome: fail17
})));
var reject = /* @__PURE__ */ dual(2, (self, pf) => rejectSTM(self, (a) => map2(pf(a), fail17)));
var rejectSTM = /* @__PURE__ */ dual(2, (self, pf) => flatMap22(self, (a) => match2(pf(a), {
  onNone: /* @__PURE__ */ __name(() => succeed19(a), "onNone"),
  onSome: flatMap22(fail17)
})));
var repeatUntil = /* @__PURE__ */ dual(2, (self, predicate) => repeatUntilLoop(self, predicate));
var repeatUntilLoop = /* @__PURE__ */ __name((self, predicate) => flatMap22(self, (a) => predicate(a) ? succeed19(a) : repeatUntilLoop(self, predicate)), "repeatUntilLoop");
var repeatWhile = /* @__PURE__ */ dual(2, (self, predicate) => repeatWhileLoop(self, predicate));
var repeatWhileLoop = /* @__PURE__ */ __name((self, predicate) => flatMap22(self, (a) => predicate(a) ? repeatWhileLoop(self, predicate) : succeed19(a)), "repeatWhileLoop");
var replicate6 = /* @__PURE__ */ dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateSTM = /* @__PURE__ */ dual(2, (self, n) => all11(replicate6(self, n)));
var replicateSTMDiscard = /* @__PURE__ */ dual(2, (self, n) => all11(replicate6(self, n), {
  discard: true
}));
var retryUntil = /* @__PURE__ */ dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail17,
  onSuccess: /* @__PURE__ */ __name((a) => predicate(a) ? succeed19(a) : retry6, "onSuccess")
}));
var retryWhile = /* @__PURE__ */ dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail17,
  onSuccess: /* @__PURE__ */ __name((a) => !predicate(a) ? succeed19(a) : retry6, "onSuccess")
}));
var partition10 = /* @__PURE__ */ dual(2, (elements, f) => pipe(forEach18(elements, (a) => either8(f(a))), map31((as15) => partitionMap6(as15, identity))));
var some16 = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => fail17(some3(e)), "onFailure"),
  onSuccess: match2({
    onNone: /* @__PURE__ */ __name(() => fail17(none2()), "onNone"),
    onSome: succeed19
  })
}), "some");
var all11 = /* @__PURE__ */ __name((input, options) => {
  if (Symbol.iterator in input) {
    return forEach18(input, identity, options);
  } else if (options?.discard) {
    return forEach18(Object.values(input), identity, options);
  }
  return map31(forEach18(Object.entries(input), ([_, e]) => map31(e, (a) => [_, a])), (values10) => {
    const res = {};
    for (const [k, v] of values10) {
      ;
      res[k] = v;
    }
    return res;
  });
}, "all");
var succeedNone4 = /* @__PURE__ */ succeed19(/* @__PURE__ */ none2());
var succeedSome4 = /* @__PURE__ */ __name((value2) => succeed19(some3(value2)), "succeedSome");
var summarized4 = /* @__PURE__ */ dual(3, (self, summary6, f) => flatMap22(summary6, (start3) => flatMap22(self, (value2) => map31(summary6, (end6) => [f(start3, end6), value2]))));
var suspend9 = /* @__PURE__ */ __name((evaluate3) => flatten15(sync13(evaluate3)), "suspend");
var tap7 = /* @__PURE__ */ dual(2, (self, f) => flatMap22(self, (a) => as12(f(a), a)));
var tapBoth3 = /* @__PURE__ */ dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => pipe(onFailure(e), zipRight11(fail17(e))), "onFailure"),
  onSuccess: /* @__PURE__ */ __name((a) => pipe(onSuccess(a), as12(a)), "onSuccess")
}));
var tapError6 = /* @__PURE__ */ dual(2, (self, f) => matchSTM(self, {
  onFailure: /* @__PURE__ */ __name((e) => zipRight11(f(e), fail17(e)), "onFailure"),
  onSuccess: succeed19
}));
var try_5 = /* @__PURE__ */ __name((arg) => {
  const evaluate3 = typeof arg === "function" ? arg : arg.try;
  return suspend9(() => {
    try {
      return succeed19(evaluate3());
    } catch (error2) {
      return fail17("catch" in arg ? arg.catch(error2) : error2);
    }
  });
}, "try_");
var void_10 = /* @__PURE__ */ succeed19(void 0);
var unless3 = /* @__PURE__ */ dual(2, (self, predicate) => suspend9(() => predicate() ? succeedNone4 : asSome4(self)));
var unlessSTM = /* @__PURE__ */ dual(2, (self, predicate) => flatMap22(predicate, (bool) => bool ? succeedNone4 : asSome4(self)));
var unsome = /* @__PURE__ */ __name((self) => matchSTM(self, {
  onFailure: match2({
    onNone: /* @__PURE__ */ __name(() => succeed19(none2()), "onNone"),
    onSome: fail17
  }),
  onSuccess: /* @__PURE__ */ __name((a) => succeed19(some3(a)), "onSuccess")
}), "unsome");
var validateAll3 = /* @__PURE__ */ dual(2, (elements, f) => flatMap22(partition10(elements, f), ([errors, values10]) => isNonEmptyArray2(errors) ? fail17(errors) : succeed19(values10)));
var validateFirst3 = /* @__PURE__ */ dual(2, (elements, f) => flip6(forEach18(elements, (a) => flip6(f(a)))));
var when4 = /* @__PURE__ */ dual(2, (self, predicate) => suspend9(() => predicate() ? asSome4(self) : succeedNone4));
var whenSTM = /* @__PURE__ */ dual(2, (self, predicate) => flatMap22(predicate, (bool) => bool ? asSome4(self) : succeedNone4));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/tRef.js
var TRefSymbolKey = "effect/TRef";
var TRefTypeId = /* @__PURE__ */ Symbol.for(TRefSymbolKey);
var tRefVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var TRefImpl = class {
  static {
    __name(this, "TRefImpl");
  }
  [TRefTypeId] = tRefVariance;
  /** @internal */
  todos;
  /** @internal */
  versioned;
  constructor(value2) {
    this.versioned = new Versioned(value2);
    this.todos = /* @__PURE__ */ new Map();
  }
  modify(f) {
    return effect3((journal) => {
      const entry = getOrMakeEntry(this, journal);
      const [retValue, newValue] = f(unsafeGet8(entry));
      unsafeSet(entry, newValue);
      return retValue;
    });
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make66 = /* @__PURE__ */ __name((value2) => effect3((journal) => {
  const ref = new TRefImpl(value2);
  journal.set(ref, make64(ref, true));
  return ref;
}), "make");
var get17 = /* @__PURE__ */ __name((self) => self.modify((a) => [a, a]), "get");
var set10 = /* @__PURE__ */ dual(2, (self, value2) => self.modify(() => [void 0, value2]));
var getAndSet5 = /* @__PURE__ */ dual(2, (self, value2) => self.modify((a) => [a, value2]));
var getAndUpdate5 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome4 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: /* @__PURE__ */ __name(() => [a, a], "onNone"),
  onSome: /* @__PURE__ */ __name((b) => [a, b], "onSome")
})));
var setAndGet4 = /* @__PURE__ */ dual(2, (self, value2) => self.modify(() => [value2, value2]));
var modify12 = /* @__PURE__ */ dual(2, (self, f) => self.modify(f));
var modifySome4 = /* @__PURE__ */ dual(3, (self, fallback, f) => self.modify((a) => match2(f(a), {
  onNone: /* @__PURE__ */ __name(() => [fallback, a], "onNone"),
  onSome: /* @__PURE__ */ __name((b) => b, "onSome")
})));
var update8 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet5 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => {
  const b = f(a);
  return [b, b];
}));
var updateSome4 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => [void 0, match2(f(a), {
  onNone: /* @__PURE__ */ __name(() => a, "onNone"),
  onSome: /* @__PURE__ */ __name((b) => b, "onSome")
})]));
var updateSomeAndGet4 = /* @__PURE__ */ dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: /* @__PURE__ */ __name(() => [a, a], "onNone"),
  onSome: /* @__PURE__ */ __name((b) => [b, b], "onSome")
})));
var getOrMakeEntry = /* @__PURE__ */ __name((self, journal) => {
  if (journal.has(self)) {
    return journal.get(self);
  }
  const entry = make64(self, false);
  journal.set(self, entry);
  return entry;
}, "getOrMakeEntry");
var unsafeGet9 = /* @__PURE__ */ dual(2, (self, journal) => unsafeGet8(getOrMakeEntry(self, journal)));
var unsafeSet2 = /* @__PURE__ */ dual(3, (self, value2, journal) => {
  const entry = getOrMakeEntry(self, journal);
  unsafeSet(entry, value2);
  return void 0;
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/tQueue.js
var TEnqueueSymbolKey = "effect/TQueue/TEnqueue";
var TEnqueueTypeId = /* @__PURE__ */ Symbol.for(TEnqueueSymbolKey);
var TDequeueSymbolKey = "effect/TQueue/TDequeue";
var TDequeueTypeId = /* @__PURE__ */ Symbol.for(TDequeueSymbolKey);
var BackPressure2 = {
  _tag: OP_BACKPRESSURE_STRATEGY
};
var Dropping = {
  _tag: OP_DROPPING_STRATEGY
};
var Sliding = {
  _tag: OP_SLIDING_STRATEGY
};
var tDequeueVariance = {
  /* c8 ignore next */
  _Out: /* @__PURE__ */ __name((_) => _, "_Out")
};
var tEnqueueVariance = {
  /* c8 ignore next */
  _In: /* @__PURE__ */ __name((_) => _, "_In")
};
var TQueueImpl = class {
  static {
    __name(this, "TQueueImpl");
  }
  ref;
  requestedCapacity;
  strategy;
  [TDequeueTypeId] = tDequeueVariance;
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(ref, requestedCapacity, strategy) {
    this.ref = ref;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
  }
  capacity() {
    return this.requestedCapacity;
  }
  size = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const queue = unsafeGet9(this.ref, runtime5.journal);
    if (queue === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    return succeed19(queue.length);
  });
  isFull = /* @__PURE__ */ map31(this.size, (size25) => size25 === this.requestedCapacity);
  isEmpty = /* @__PURE__ */ map31(this.size, (size25) => size25 === 0);
  shutdown = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    unsafeSet2(this.ref, void 0, runtime5.journal);
    return void_10;
  });
  isShutdown = /* @__PURE__ */ effect3((journal) => {
    const queue = unsafeGet9(this.ref, journal);
    return queue === void 0;
  });
  awaitShutdown = /* @__PURE__ */ flatMap22(this.isShutdown, (isShutdown9) => isShutdown9 ? void_10 : retry6);
  offer(value2) {
    return withSTMRuntime((runtime5) => {
      const queue = pipe(this.ref, unsafeGet9(runtime5.journal));
      if (queue === void 0) {
        return interruptAs2(runtime5.fiberId);
      }
      if (queue.length < this.requestedCapacity) {
        queue.push(value2);
        unsafeSet2(this.ref, queue, runtime5.journal);
        return succeed19(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry6;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed19(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (queue.length === 0) {
            return succeed19(true);
          }
          queue.shift();
          queue.push(value2);
          unsafeSet2(this.ref, queue, runtime5.journal);
          return succeed19(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return withSTMRuntime((runtime5) => {
      const as15 = Array.from(iterable);
      const queue = unsafeGet9(this.ref, runtime5.journal);
      if (queue === void 0) {
        return interruptAs2(runtime5.fiberId);
      }
      if (queue.length + as15.length <= this.requestedCapacity) {
        unsafeSet2(this.ref, [...queue, ...as15], runtime5.journal);
        return succeed19(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry6;
        }
        case OP_DROPPING_STRATEGY: {
          const forQueue = as15.slice(0, this.requestedCapacity - queue.length);
          unsafeSet2(this.ref, [...queue, ...forQueue], runtime5.journal);
          return succeed19(false);
        }
        case OP_SLIDING_STRATEGY: {
          const forQueue = as15.slice(0, this.requestedCapacity - queue.length);
          const toDrop = queue.length + forQueue.length - this.requestedCapacity;
          const newQueue = queue.slice(toDrop);
          unsafeSet2(this.ref, [...newQueue, ...forQueue], runtime5.journal);
          return succeed19(true);
        }
      }
    });
  }
  peek = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const queue = unsafeGet9(this.ref, runtime5.journal);
    if (queue === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    if (queue.length === 0) {
      return retry6;
    }
    return succeed19(queue[0]);
  });
  peekOption = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const queue = unsafeGet9(this.ref, runtime5.journal);
    if (queue === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    return succeed19(fromNullable2(queue[0]));
  });
  take = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const queue = unsafeGet9(this.ref, runtime5.journal);
    if (queue === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    if (queue.length === 0) {
      return retry6;
    }
    const dequeued = queue.shift();
    unsafeSet2(this.ref, queue, runtime5.journal);
    return succeed19(dequeued);
  });
  takeAll = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const queue = unsafeGet9(this.ref, runtime5.journal);
    if (queue === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    unsafeSet2(this.ref, [], runtime5.journal);
    return succeed19(queue);
  });
  takeUpTo(max10) {
    return withSTMRuntime((runtime5) => {
      const queue = unsafeGet9(this.ref, runtime5.journal);
      if (queue === void 0) {
        return interruptAs2(runtime5.fiberId);
      }
      const [toTake, remaining] = splitAt2(unsafeFromArray(queue), max10);
      unsafeSet2(this.ref, Array.from(remaining), runtime5.journal);
      return succeed19(Array.from(toTake));
    });
  }
};
var isTQueue = /* @__PURE__ */ __name((u) => {
  return isTEnqueue(u) && isTDequeue(u);
}, "isTQueue");
var isTEnqueue = /* @__PURE__ */ __name((u) => hasProperty(u, TEnqueueTypeId), "isTEnqueue");
var isTDequeue = /* @__PURE__ */ __name((u) => hasProperty(u, TDequeueTypeId), "isTDequeue");
var awaitShutdown5 = /* @__PURE__ */ __name((self) => self.awaitShutdown, "awaitShutdown");
var bounded6 = /* @__PURE__ */ __name((requestedCapacity) => makeQueue(requestedCapacity, BackPressure2), "bounded");
var capacity6 = /* @__PURE__ */ __name((self) => {
  return self.capacity();
}, "capacity");
var dropping5 = /* @__PURE__ */ __name((requestedCapacity) => makeQueue(requestedCapacity, Dropping), "dropping");
var isEmpty19 = /* @__PURE__ */ __name((self) => self.isEmpty, "isEmpty");
var isFull6 = /* @__PURE__ */ __name((self) => self.isFull, "isFull");
var isShutdown5 = /* @__PURE__ */ __name((self) => self.isShutdown, "isShutdown");
var offer4 = /* @__PURE__ */ dual(2, (self, value2) => self.offer(value2));
var offerAll4 = /* @__PURE__ */ dual(2, (self, iterable) => self.offerAll(iterable));
var peek = /* @__PURE__ */ __name((self) => self.peek, "peek");
var peekOption = /* @__PURE__ */ __name((self) => self.peekOption, "peekOption");
var poll7 = /* @__PURE__ */ __name((self) => pipe(self.takeUpTo(1), map31(head2)), "poll");
var seek = /* @__PURE__ */ dual(2, (self, predicate) => seekLoop(self, predicate));
var seekLoop = /* @__PURE__ */ __name((self, predicate) => flatMap22(self.take, (a) => predicate(a) ? succeed19(a) : seekLoop(self, predicate)), "seekLoop");
var shutdown5 = /* @__PURE__ */ __name((self) => self.shutdown, "shutdown");
var size21 = /* @__PURE__ */ __name((self) => self.size, "size");
var sliding5 = /* @__PURE__ */ __name((requestedCapacity) => makeQueue(requestedCapacity, Sliding), "sliding");
var take8 = /* @__PURE__ */ __name((self) => self.take, "take");
var takeAll3 = /* @__PURE__ */ __name((self) => self.takeAll, "takeAll");
var takeBetween3 = /* @__PURE__ */ dual(3, (self, min8, max10) => suspend9(() => {
  const takeRemainder = /* @__PURE__ */ __name((min9, max11, acc) => {
    if (max11 < min9) {
      return succeed19(acc);
    }
    return pipe(self.takeUpTo(max11), flatMap22((taken) => {
      const remaining = min9 - taken.length;
      if (remaining === 1) {
        return pipe(self.take, map31((a) => pipe(acc, appendAll3(unsafeFromArray(taken)), append3(a))));
      }
      if (remaining > 1) {
        return pipe(self.take, flatMap22((a) => takeRemainder(remaining - 1, max11 - taken.length - 1, pipe(acc, appendAll3(unsafeFromArray(taken)), append3(a)))));
      }
      return succeed19(pipe(acc, appendAll3(unsafeFromArray(taken))));
    }));
  }, "takeRemainder");
  return map31(takeRemainder(min8, max10, empty7()), (c) => Array.from(c));
}));
var takeN3 = /* @__PURE__ */ dual(2, (self, n) => pipe(self, takeBetween3(n, n)));
var takeUpTo3 = /* @__PURE__ */ dual(2, (self, max10) => self.takeUpTo(max10));
var unbounded6 = /* @__PURE__ */ __name(() => makeQueue(Number.MAX_SAFE_INTEGER, Dropping), "unbounded");
var makeQueue = /* @__PURE__ */ __name((requestedCapacity, strategy) => map31(make66([]), (ref) => new TQueueImpl(ref, requestedCapacity, strategy)), "makeQueue");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stm/tPubSub.js
var TPubSubSymbolKey = "effect/TPubSub";
var TPubSubTypeId = /* @__PURE__ */ Symbol.for(TPubSubSymbolKey);
var AbsentValue2 = /* @__PURE__ */ Symbol.for("effect/TPubSub/AbsentValue");
var makeNode2 = /* @__PURE__ */ __name((head10, subscribers, tail5) => ({
  head: head10,
  subscribers,
  tail: tail5
}), "makeNode");
var TPubSubImpl = class {
  static {
    __name(this, "TPubSubImpl");
  }
  pubsubSize;
  publisherHead;
  publisherTail;
  requestedCapacity;
  strategy;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = {
    _A: /* @__PURE__ */ __name((_) => _, "_A")
  };
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.publisherTail = publisherTail;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = /* @__PURE__ */ effect3((journal) => {
    const currentPublisherTail = unsafeGet9(this.publisherTail, journal);
    return currentPublisherTail === void 0;
  });
  awaitShutdown = /* @__PURE__ */ flatMap22(this.isShutdown, (isShutdown9) => isShutdown9 ? void_10 : retry6);
  capacity() {
    return this.requestedCapacity;
  }
  size = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    const currentPublisherTail = unsafeGet9(this.publisherTail, runtime5.journal);
    if (currentPublisherTail === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    return succeed19(unsafeGet9(this.pubsubSize, runtime5.journal));
  });
  isEmpty = /* @__PURE__ */ map31(this.size, (size25) => size25 === 0);
  isFull = /* @__PURE__ */ map31(this.size, (size25) => size25 === this.capacity());
  offer(value2) {
    return withSTMRuntime((runtime5) => {
      const currentPublisherTail = unsafeGet9(this.publisherTail, runtime5.journal);
      if (currentPublisherTail === void 0) {
        return interruptAs2(runtime5.fiberId);
      }
      const currentSubscriberCount = unsafeGet9(this.subscriberCount, runtime5.journal);
      if (currentSubscriberCount === 0) {
        return succeed19(true);
      }
      const currentPubSubSize = unsafeGet9(this.pubsubSize, runtime5.journal);
      if (currentPubSubSize < this.requestedCapacity) {
        const updatedPublisherTail = new TRefImpl(void 0);
        const updatedNode = makeNode2(value2, currentSubscriberCount, updatedPublisherTail);
        unsafeSet2(currentPublisherTail, updatedNode, runtime5.journal);
        unsafeSet2(this.publisherTail, updatedPublisherTail, runtime5.journal);
        unsafeSet2(this.pubsubSize, currentPubSubSize + 1, runtime5.journal);
        return succeed19(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry6;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed19(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (this.requestedCapacity > 0) {
            let currentPublisherHead = unsafeGet9(this.publisherHead, runtime5.journal);
            let loop4 = true;
            while (loop4) {
              const node = unsafeGet9(currentPublisherHead, runtime5.journal);
              if (node === void 0) {
                return retry6;
              }
              const head10 = node.head;
              const tail5 = node.tail;
              if (head10 !== AbsentValue2) {
                const updatedNode2 = makeNode2(AbsentValue2, node.subscribers, node.tail);
                unsafeSet2(currentPublisherHead, updatedNode2, runtime5.journal);
                unsafeSet2(this.publisherHead, tail5, runtime5.journal);
                loop4 = false;
              } else {
                currentPublisherHead = tail5;
              }
            }
          }
          const updatedPublisherTail = new TRefImpl(void 0);
          const updatedNode = makeNode2(value2, currentSubscriberCount, updatedPublisherTail);
          unsafeSet2(currentPublisherTail, updatedNode, runtime5.journal);
          unsafeSet2(this.publisherTail, updatedPublisherTail, runtime5.journal);
          return succeed19(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return map31(forEach18(iterable, (a) => this.offer(a)), every3(identity));
  }
  shutdown = /* @__PURE__ */ effect3((journal) => {
    const currentPublisherTail = unsafeGet9(this.publisherTail, journal);
    if (currentPublisherTail !== void 0) {
      unsafeSet2(this.publisherTail, void 0, journal);
      const currentSubscribers = unsafeGet9(this.subscribers, journal);
      forEach6(currentSubscribers, (subscriber) => {
        unsafeSet2(subscriber, void 0, journal);
      });
      unsafeSet2(this.subscribers, empty10(), journal);
    }
  });
};
var TPubSubSubscriptionImpl = class {
  static {
    __name(this, "TPubSubSubscriptionImpl");
  }
  pubsubSize;
  publisherHead;
  requestedCapacity;
  subscriberHead;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = TPubSubTypeId;
  [TDequeueTypeId] = tDequeueVariance;
  constructor(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.requestedCapacity = requestedCapacity;
    this.subscriberHead = subscriberHead;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = /* @__PURE__ */ effect3((journal) => {
    const currentSubscriberHead = unsafeGet9(this.subscriberHead, journal);
    return currentSubscriberHead === void 0;
  });
  awaitShutdown = /* @__PURE__ */ flatMap22(this.isShutdown, (isShutdown9) => isShutdown9 ? void_10 : retry6);
  capacity() {
    return this.requestedCapacity;
  }
  size = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    let currentSubscriberHead = unsafeGet9(this.subscriberHead, runtime5.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    let loop4 = true;
    let size25 = 0;
    while (loop4) {
      const node = unsafeGet9(currentSubscriberHead, runtime5.journal);
      if (node === void 0) {
        loop4 = false;
      } else {
        const head10 = node.head;
        const tail5 = node.tail;
        if (head10 !== AbsentValue2) {
          size25 = size25 + 1;
          if (size25 >= Number.MAX_SAFE_INTEGER) {
            loop4 = false;
          }
        }
        currentSubscriberHead = tail5;
      }
    }
    return succeed19(size25);
  });
  isEmpty = /* @__PURE__ */ map31(this.size, (size25) => size25 === 0);
  isFull = /* @__PURE__ */ map31(this.size, (size25) => size25 === this.capacity());
  peek = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    let currentSubscriberHead = unsafeGet9(this.subscriberHead, runtime5.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    let value2 = AbsentValue2;
    let loop4 = true;
    while (loop4) {
      const node = unsafeGet9(currentSubscriberHead, runtime5.journal);
      if (node === void 0) {
        return retry6;
      }
      const head10 = node.head;
      const tail5 = node.tail;
      if (head10 !== AbsentValue2) {
        value2 = head10;
        loop4 = false;
      } else {
        currentSubscriberHead = tail5;
      }
    }
    return succeed19(value2);
  });
  peekOption = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    let currentSubscriberHead = unsafeGet9(this.subscriberHead, runtime5.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    let value2 = none2();
    let loop4 = true;
    while (loop4) {
      const node = unsafeGet9(currentSubscriberHead, runtime5.journal);
      if (node === void 0) {
        value2 = none2();
        loop4 = false;
      } else {
        const head10 = node.head;
        const tail5 = node.tail;
        if (head10 !== AbsentValue2) {
          value2 = some3(head10);
          loop4 = false;
        } else {
          currentSubscriberHead = tail5;
        }
      }
    }
    return succeed19(value2);
  });
  shutdown = /* @__PURE__ */ effect3((journal) => {
    let currentSubscriberHead = unsafeGet9(this.subscriberHead, journal);
    if (currentSubscriberHead !== void 0) {
      unsafeSet2(this.subscriberHead, void 0, journal);
      let loop4 = true;
      while (loop4) {
        const node = unsafeGet9(currentSubscriberHead, journal);
        if (node === void 0) {
          loop4 = false;
        } else {
          const head10 = node.head;
          const tail5 = node.tail;
          if (head10 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size25 = unsafeGet9(this.pubsubSize, journal);
              const updatedNode = makeNode2(AbsentValue2, 0, tail5);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
              unsafeSet2(this.publisherHead, tail5, journal);
              unsafeSet2(this.pubsubSize, size25 - 1, journal);
            } else {
              const updatedNode = makeNode2(head10, subscribers - 1, tail5);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
            }
          }
          currentSubscriberHead = tail5;
        }
      }
      const currentSubscriberCount = unsafeGet9(this.subscriberCount, journal);
      unsafeSet2(this.subscriberCount, currentSubscriberCount - 1, journal);
      unsafeSet2(this.subscribers, remove6(unsafeGet9(this.subscribers, journal), this.subscriberHead), journal);
    }
  });
  take = /* @__PURE__ */ withSTMRuntime((runtime5) => {
    let currentSubscriberHead = unsafeGet9(this.subscriberHead, runtime5.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime5.fiberId);
    }
    let value2 = AbsentValue2;
    let loop4 = true;
    while (loop4) {
      const node = unsafeGet9(currentSubscriberHead, runtime5.journal);
      if (node === void 0) {
        return retry6;
      }
      const head10 = node.head;
      const tail5 = node.tail;
      if (head10 !== AbsentValue2) {
        const subscribers = node.subscribers;
        if (subscribers === 1) {
          const size25 = unsafeGet9(this.pubsubSize, runtime5.journal);
          const updatedNode = makeNode2(AbsentValue2, 0, tail5);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime5.journal);
          unsafeSet2(this.publisherHead, tail5, runtime5.journal);
          unsafeSet2(this.pubsubSize, size25 - 1, runtime5.journal);
        } else {
          const updatedNode = makeNode2(head10, subscribers - 1, tail5);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime5.journal);
        }
        unsafeSet2(this.subscriberHead, tail5, runtime5.journal);
        value2 = head10;
        loop4 = false;
      } else {
        currentSubscriberHead = tail5;
      }
    }
    return succeed19(value2);
  });
  takeAll = /* @__PURE__ */ this.takeUpTo(Number.POSITIVE_INFINITY);
  takeUpTo(max10) {
    return withSTMRuntime((runtime5) => {
      let currentSubscriberHead = unsafeGet9(this.subscriberHead, runtime5.journal);
      if (currentSubscriberHead === void 0) {
        return interruptAs2(runtime5.fiberId);
      }
      const builder = [];
      let n = 0;
      while (n !== max10) {
        const node = unsafeGet9(currentSubscriberHead, runtime5.journal);
        if (node === void 0) {
          n = max10;
        } else {
          const head10 = node.head;
          const tail5 = node.tail;
          if (head10 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size25 = unsafeGet9(this.pubsubSize, runtime5.journal);
              const updatedNode = makeNode2(AbsentValue2, 0, tail5);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime5.journal);
              unsafeSet2(this.publisherHead, tail5, runtime5.journal);
              unsafeSet2(this.pubsubSize, size25 - 1, runtime5.journal);
            } else {
              const updatedNode = makeNode2(head10, subscribers - 1, tail5);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime5.journal);
            }
            builder.push(head10);
            n = n + 1;
          }
          currentSubscriberHead = tail5;
        }
      }
      unsafeSet2(this.subscriberHead, currentSubscriberHead, runtime5.journal);
      return succeed19(builder);
    });
  }
};
var makeTPubSub = /* @__PURE__ */ __name((requestedCapacity, strategy) => pipe(all11([make66(void 0), make66(0)]), flatMap22(([empty46, pubsubSize]) => pipe(all11([make66(empty46), make66(empty46), make66(0), make66(empty10())]), map31(([publisherHead, publisherTail, subscriberCount, subscribers]) => new TPubSubImpl(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers))))), "makeTPubSub");
var makeSubscription2 = /* @__PURE__ */ __name((pubsubSize, publisherHead, publisherTail, requestedCapacity, subscriberCount, subscribers) => pipe(get17(publisherTail), flatMap22((currentPublisherTail) => pipe(all11([make66(currentPublisherTail), get17(subscriberCount), get17(subscribers)]), tap7(([_, currentSubscriberCount]) => pipe(subscriberCount, set10(currentSubscriberCount + 1))), tap7(([subscriberHead, _, currentSubscribers]) => pipe(subscribers, set10(pipe(currentSubscribers, add4(subscriberHead))))), map31(([subscriberHead]) => new TPubSubSubscriptionImpl(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers))))), "makeSubscription");
var awaitShutdown6 = /* @__PURE__ */ __name((self) => self.awaitShutdown, "awaitShutdown");
var bounded7 = /* @__PURE__ */ __name((requestedCapacity) => makeTPubSub(requestedCapacity, BackPressure2), "bounded");
var capacity7 = /* @__PURE__ */ __name((self) => self.capacity(), "capacity");
var dropping6 = /* @__PURE__ */ __name((requestedCapacity) => makeTPubSub(requestedCapacity, Dropping), "dropping");
var isEmpty20 = /* @__PURE__ */ __name((self) => self.isEmpty, "isEmpty");
var isFull7 = /* @__PURE__ */ __name((self) => self.isFull, "isFull");
var isShutdown6 = /* @__PURE__ */ __name((self) => self.isShutdown, "isShutdown");
var publish3 = /* @__PURE__ */ dual(2, (self, value2) => self.offer(value2));
var publishAll3 = /* @__PURE__ */ dual(2, (self, iterable) => self.offerAll(iterable));
var size22 = /* @__PURE__ */ __name((self) => self.size, "size");
var shutdown6 = /* @__PURE__ */ __name((self) => self.shutdown, "shutdown");
var sliding6 = /* @__PURE__ */ __name((requestedCapacity) => makeTPubSub(requestedCapacity, Sliding), "sliding");
var subscribe3 = /* @__PURE__ */ __name((self) => makeSubscription2(self.pubsubSize, self.publisherHead, self.publisherTail, self.requestedCapacity, self.subscriberCount, self.subscribers), "subscribe");
var subscribeScoped = /* @__PURE__ */ __name((self) => acquireRelease3(subscribe3(self), (dequeue) => shutdown5(dequeue)), "subscribeScoped");
var unbounded7 = /* @__PURE__ */ __name(() => makeTPubSub(Number.MAX_SAFE_INTEGER, Dropping), "unbounded");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/TPubSub.js
var TPubSubTypeId2 = TPubSubTypeId;
var awaitShutdown7 = awaitShutdown6;
var bounded8 = bounded7;
var capacity8 = capacity7;
var dropping7 = dropping6;
var isEmpty21 = isEmpty20;
var isFull8 = isFull7;
var shutdown7 = shutdown6;
var isShutdown7 = isShutdown6;
var publish4 = publish3;
var publishAll4 = publishAll3;
var size23 = size22;
var sliding7 = sliding6;
var subscribe4 = subscribe3;
var subscribeScoped2 = subscribeScoped;
var unbounded8 = unbounded7;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/TQueue.js
var TDequeueTypeId2 = TDequeueTypeId;
var TEnqueueTypeId2 = TEnqueueTypeId;
var isTQueue2 = isTQueue;
var isTDequeue2 = isTDequeue;
var isTEnqueue2 = isTEnqueue;
var awaitShutdown8 = awaitShutdown5;
var bounded9 = bounded6;
var capacity9 = capacity6;
var dropping8 = dropping5;
var isEmpty22 = isEmpty19;
var isFull9 = isFull6;
var isShutdown8 = isShutdown5;
var offer5 = offer4;
var offerAll5 = offerAll4;
var peek2 = peek;
var peekOption2 = peekOption;
var poll8 = poll7;
var seek2 = seek;
var shutdown8 = shutdown5;
var size24 = size21;
var sliding8 = sliding5;
var take9 = take8;
var takeAll4 = takeAll3;
var takeBetween4 = takeBetween3;
var takeN4 = takeN3;
var takeUpTo4 = takeUpTo3;
var unbounded9 = unbounded6;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/ringBuffer.js
var RingBuffer = class {
  static {
    __name(this, "RingBuffer");
  }
  capacity;
  array;
  size = 0;
  current = 0;
  constructor(capacity10) {
    this.capacity = capacity10;
    this.array = Array.from({
      length: capacity10
    }, constUndefined);
  }
  head() {
    return fromNullable2(this.array[this.current]);
  }
  lastOrNull() {
    if (this.size === 0) {
      return void 0;
    }
    const index = this.current === 0 ? this.array.length - 1 : this.current - 1;
    return this.array[index] ?? void 0;
  }
  put(value2) {
    this.array[this.current] = value2;
    this.increment();
  }
  dropLast() {
    if (this.size > 0) {
      this.decrement();
      this.array[this.current] = void 0;
    }
  }
  toChunk() {
    const begin = this.current - this.size;
    const newArray = begin < 0 ? [...this.array.slice(this.capacity + begin, this.capacity), ...this.array.slice(0, this.current)] : this.array.slice(begin, this.current);
    return fromIterable3(newArray);
  }
  increment() {
    if (this.size < this.capacity) {
      this.size += 1;
    }
    this.current = (this.current + 1) % this.capacity;
  }
  decrement() {
    this.size -= 1;
    if (this.current > 0) {
      this.current -= 1;
    } else {
      this.current = this.capacity - 1;
    }
  }
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/debounceState.js
var OP_NOT_STARTED = "NotStarted";
var OP_PREVIOUS = "Previous";
var OP_CURRENT = "Current";
var notStarted = {
  _tag: OP_NOT_STARTED
};
var previous = /* @__PURE__ */ __name((fiber) => ({
  _tag: OP_PREVIOUS,
  fiber
}), "previous");
var current = /* @__PURE__ */ __name((fiber) => ({
  _tag: OP_CURRENT,
  fiber
}), "current");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/emit.js
var make67 = /* @__PURE__ */ __name((emit4) => {
  const ops = {
    chunk(as15) {
      return this(succeed13(as15));
    },
    die(defect) {
      return this(die8(defect));
    },
    dieMessage(message) {
      return this(dieMessage2(message));
    },
    done(exit4) {
      return this(suspend4(() => mapBoth4(exit4, {
        onFailure: some3,
        onSuccess: of3
      })));
    },
    end() {
      return this(fail12(none2()));
    },
    fail(e) {
      return this(fail12(some3(e)));
    },
    fromEffect(effect4) {
      return this(mapBoth6(effect4, {
        onFailure: some3,
        onSuccess: of3
      }));
    },
    fromEffectChunk(effect4) {
      return this(pipe(effect4, mapError5(some3)));
    },
    halt(cause3) {
      return this(failCause10(pipe(cause3, map22(some3))));
    },
    single(value2) {
      return this(succeed13(of3(value2)));
    }
  };
  return Object.assign(emit4, ops);
}, "make");
var makePush = /* @__PURE__ */ __name((queue, scheduler) => {
  let finished = false;
  let buffer4 = [];
  let running4 = false;
  function array5(items) {
    if (finished) return false;
    if (items.length <= 5e4) {
      buffer4.push.apply(buffer4, items);
    } else {
      for (let i = 0; i < items.length; i++) {
        buffer4.push(items[0]);
      }
    }
    if (!running4) {
      running4 = true;
      scheduler.scheduleTask(flush, 0);
    }
    return true;
  }
  __name(array5, "array");
  function flush() {
    running4 = false;
    if (buffer4.length > 0) {
      queue.unsafeOffer(buffer4);
      buffer4 = [];
    }
  }
  __name(flush, "flush");
  function done12(exit4) {
    if (finished) return;
    finished = true;
    if (exit4._tag === "Success") {
      buffer4.push(exit4.value);
    }
    flush();
    queue.unsafeOffer(exit4._tag === "Success" ? void_4 : exit4);
  }
  __name(done12, "done");
  return {
    single(value2) {
      if (finished) return false;
      buffer4.push(value2);
      if (!running4) {
        running4 = true;
        scheduler.scheduleTask(flush, 0);
      }
      return true;
    },
    array: array5,
    chunk(chunk4) {
      return array5(toReadonlyArray(chunk4));
    },
    done: done12,
    end() {
      if (finished) return;
      finished = true;
      flush();
      queue.unsafeOffer(void_4);
    },
    halt(cause3) {
      return done12(failCause3(cause3));
    },
    fail(error2) {
      return done12(fail4(error2));
    },
    die(defect) {
      return done12(die4(defect));
    },
    dieMessage(message) {
      return done12(die4(new Error(message)));
    }
  };
}, "makePush");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/handoff.js
var HandoffTypeId = /* @__PURE__ */ Symbol.for("effect/Stream/Handoff");
var OP_HANDOFF_STATE_EMPTY = "Empty";
var OP_HANDOFF_STATE_FULL = "Full";
var handoffStateEmpty = /* @__PURE__ */ __name((notifyConsumer) => ({
  _tag: OP_HANDOFF_STATE_EMPTY,
  notifyConsumer
}), "handoffStateEmpty");
var handoffStateFull = /* @__PURE__ */ __name((value2, notifyProducer) => ({
  _tag: OP_HANDOFF_STATE_FULL,
  value: value2,
  notifyProducer
}), "handoffStateFull");
var handoffStateMatch = /* @__PURE__ */ __name((onEmpty, onFull) => {
  return (self) => {
    switch (self._tag) {
      case OP_HANDOFF_STATE_EMPTY: {
        return onEmpty(self.notifyConsumer);
      }
      case OP_HANDOFF_STATE_FULL: {
        return onFull(self.value, self.notifyProducer);
      }
    }
  };
}, "handoffStateMatch");
var handoffVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var make68 = /* @__PURE__ */ __name(() => pipe(make22(), flatMap18((deferred) => make35(handoffStateEmpty(deferred))), map26((ref) => ({
  [HandoffTypeId]: handoffVariance,
  ref
}))), "make");
var offer6 = /* @__PURE__ */ dual(2, (self, value2) => {
  return flatMap18(make22(), (deferred) => flatten12(modify8(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [zipRight7(succeed2(notifyConsumer, void 0), _await(deferred)), handoffStateFull(value2, deferred)], (_, notifyProducer) => [flatMap18(_await(notifyProducer), () => pipe(self, offer6(value2))), state])))));
});
var take10 = /* @__PURE__ */ __name((self) => flatMap18(make22(), (deferred) => flatten12(modify8(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [flatMap18(_await(notifyConsumer), () => take10(self)), state], (value2, notifyProducer) => [as8(succeed2(notifyProducer, void 0), value2), handoffStateEmpty(deferred)]))))), "take");
var poll9 = /* @__PURE__ */ __name((self) => flatMap18(make22(), (deferred) => flatten12(modify8(self.ref, (state) => pipe(state, handoffStateMatch(() => [succeed13(none2()), state], (value2, notifyProducer) => [as8(succeed2(notifyProducer, void 0), some3(value2)), handoffStateEmpty(deferred)]))))), "poll");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/handoffSignal.js
var OP_EMIT4 = "Emit";
var OP_HALT = "Halt";
var OP_END = "End";
var emit2 = /* @__PURE__ */ __name((elements) => ({
  _tag: OP_EMIT4,
  elements
}), "emit");
var halt = /* @__PURE__ */ __name((cause3) => ({
  _tag: OP_HALT,
  cause: cause3
}), "halt");
var end3 = /* @__PURE__ */ __name((reason) => ({
  _tag: OP_END,
  reason
}), "end");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/take.js
var TakeSymbolKey = "effect/Take";
var TakeTypeId = /* @__PURE__ */ Symbol.for(TakeSymbolKey);
var takeVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E")
};
var TakeImpl = class {
  static {
    __name(this, "TakeImpl");
  }
  exit;
  [TakeTypeId] = takeVariance;
  constructor(exit4) {
    this.exit = exit4;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var chunk3 = /* @__PURE__ */ __name((chunk4) => new TakeImpl(succeed3(chunk4)), "chunk");
var die13 = /* @__PURE__ */ __name((defect) => new TakeImpl(die4(defect)), "die");
var dieMessage5 = /* @__PURE__ */ __name((message) => new TakeImpl(die4(new RuntimeException2(message))), "dieMessage");
var done11 = /* @__PURE__ */ __name((self) => suspend4(() => self.exit), "done");
var end4 = /* @__PURE__ */ new TakeImpl(/* @__PURE__ */ fail4(/* @__PURE__ */ none2()));
var fail18 = /* @__PURE__ */ __name((error2) => new TakeImpl(fail4(some3(error2))), "fail");
var failCause14 = /* @__PURE__ */ __name((cause3) => new TakeImpl(failCause3(pipe(cause3, map22(some3)))), "failCause");
var fromEffect8 = /* @__PURE__ */ __name((effect4) => matchCause4(effect4, {
  onFailure: failCause14,
  onSuccess: of7
}), "fromEffect");
var fromExit = /* @__PURE__ */ __name((exit4) => new TakeImpl(pipe(exit4, mapBoth4({
  onFailure: some3,
  onSuccess: of3
}))), "fromExit");
var fromPull = /* @__PURE__ */ __name((pull) => matchCause4(pull, {
  onFailure: /* @__PURE__ */ __name((cause3) => match2(flipCauseOption2(cause3), {
    onNone: /* @__PURE__ */ __name(() => end4, "onNone"),
    onSome: failCause14
  }), "onFailure"),
  onSuccess: chunk3
}), "fromPull");
var isDone8 = /* @__PURE__ */ __name((self) => match7(self.exit, {
  onFailure: /* @__PURE__ */ __name((cause3) => isNone2(flipCauseOption2(cause3)), "onFailure"),
  onSuccess: constFalse
}), "isDone");
var isFailure7 = /* @__PURE__ */ __name((self) => match7(self.exit, {
  onFailure: /* @__PURE__ */ __name((cause3) => isSome2(flipCauseOption2(cause3)), "onFailure"),
  onSuccess: constFalse
}), "isFailure");
var isSuccess6 = /* @__PURE__ */ __name((self) => match7(self.exit, {
  onFailure: constFalse,
  onSuccess: constTrue
}), "isSuccess");
var make69 = /* @__PURE__ */ __name((exit4) => new TakeImpl(exit4), "make");
var match32 = /* @__PURE__ */ dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => match7(self.exit, {
  onFailure: /* @__PURE__ */ __name((cause3) => match2(flipCauseOption2(cause3), {
    onNone: onEnd3,
    onSome: onFailure
  }), "onFailure"),
  onSuccess
}));
var matchEffect5 = /* @__PURE__ */ dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => matchEffect2(self.exit, {
  onFailure: /* @__PURE__ */ __name((cause3) => match2(flipCauseOption2(cause3), {
    onNone: /* @__PURE__ */ __name(() => onEnd3, "onNone"),
    onSome: onFailure
  }), "onFailure"),
  onSuccess
}));
var map32 = /* @__PURE__ */ dual(2, (self, f) => new TakeImpl(pipe(self.exit, map15(map7(f)))));
var of7 = /* @__PURE__ */ __name((value2) => new TakeImpl(succeed3(of3(value2))), "of");
var tap8 = /* @__PURE__ */ dual(2, (self, f) => pipe(self.exit, forEachEffect(f), asVoid6));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/pull.js
var emit3 = /* @__PURE__ */ __name((value2) => succeed13(of3(value2)), "emit");
var emitChunk = /* @__PURE__ */ __name((chunk4) => succeed13(chunk4), "emitChunk");
var empty43 = /* @__PURE__ */ __name(() => succeed13(empty7()), "empty");
var end5 = /* @__PURE__ */ __name(() => fail12(none2()), "end");
var fail19 = /* @__PURE__ */ __name((error2) => fail12(some3(error2)), "fail");
var failCause15 = /* @__PURE__ */ __name((cause3) => mapError5(failCause10(cause3), some3), "failCause");
var fromDequeue = /* @__PURE__ */ __name((dequeue) => flatMap18(take6(dequeue), done11), "fromDequeue");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/sinkEndReason.js
var OP_SCHEDULE_END = "ScheduleEnd";
var OP_UPSTREAM_END = "UpstreamEnd";
var ScheduleEnd = {
  _tag: OP_SCHEDULE_END
};
var UpstreamEnd = {
  _tag: OP_UPSTREAM_END
};

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/zipAllState.js
var OP_DRAIN_LEFT = "DrainLeft";
var OP_DRAIN_RIGHT = "DrainRight";
var OP_PULL_BOTH = "PullBoth";
var OP_PULL_LEFT = "PullLeft";
var OP_PULL_RIGHT = "PullRight";
var DrainLeft = {
  _tag: OP_DRAIN_LEFT
};
var DrainRight = {
  _tag: OP_DRAIN_RIGHT
};
var PullBoth = {
  _tag: OP_PULL_BOTH
};
var PullLeft = /* @__PURE__ */ __name((rightChunk) => ({
  _tag: OP_PULL_LEFT,
  rightChunk
}), "PullLeft");
var PullRight = /* @__PURE__ */ __name((leftChunk) => ({
  _tag: OP_PULL_RIGHT,
  leftChunk
}), "PullRight");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream/zipChunksState.js
var OP_PULL_BOTH2 = "PullBoth";
var OP_PULL_LEFT2 = "PullLet";
var OP_PULL_RIGHT2 = "PullRight";
var PullBoth2 = {
  _tag: OP_PULL_BOTH2
};
var PullLeft2 = /* @__PURE__ */ __name((rightChunk) => ({
  _tag: OP_PULL_LEFT2,
  rightChunk
}), "PullLeft");
var PullRight2 = /* @__PURE__ */ __name((leftChunk) => ({
  _tag: OP_PULL_RIGHT2,
  leftChunk
}), "PullRight");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/stream.js
var StreamSymbolKey = "effect/Stream";
var StreamTypeId3 = /* @__PURE__ */ Symbol.for(StreamSymbolKey);
var streamVariance = {
  _R: /* @__PURE__ */ __name((_) => _, "_R"),
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var StreamImpl = class {
  static {
    __name(this, "StreamImpl");
  }
  channel;
  [StreamTypeId3] = streamVariance;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isStream = /* @__PURE__ */ __name((u) => hasProperty(u, StreamTypeId3) || isEffect2(u), "isStream");
var DefaultChunkSize = 4096;
var accumulate = /* @__PURE__ */ __name((self) => chunks(accumulateChunks(self)), "accumulate");
var accumulateChunks = /* @__PURE__ */ __name((self) => {
  const accumulator = /* @__PURE__ */ __name((s) => readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const next4 = appendAll3(s, input);
      return flatMap20(write(next4), () => accumulator(next4));
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "accumulator");
  return new StreamImpl(pipeTo(toChannel2(self), accumulator(empty7())));
}, "accumulateChunks");
var acquireRelease4 = /* @__PURE__ */ __name((acquire, release) => scoped7(acquireRelease3(acquire, release)), "acquireRelease");
var aggregate = /* @__PURE__ */ dual(2, (self, sink) => aggregateWithin(self, sink, forever5));
var aggregateWithin = /* @__PURE__ */ dual(3, (self, sink, schedule4) => filterMap12(aggregateWithinEither(self, sink, schedule4), (_) => match(_, {
  onLeft: none2,
  onRight: some3
})));
var aggregateWithinEither = /* @__PURE__ */ dual(3, (self, sink, schedule4) => {
  const layer = all10([make68(), make35(ScheduleEnd), make35(empty7()), driver2(schedule4), make35(false), make35(false)]);
  return fromEffect9(layer).pipe(flatMap23(([handoff, sinkEndReason, sinkLeftovers, scheduleDriver, consumed, endAfterEmit]) => {
    const handoffProducer = readWithCause({
      onInput: /* @__PURE__ */ __name((input) => flatMap20(fromEffect5(pipe(handoff, offer6(emit2(input)), when3(() => isNonEmpty(input)))), () => handoffProducer), "onInput"),
      onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer6(handoff, halt(cause3))), "onFailure"),
      onDone: /* @__PURE__ */ __name(() => fromEffect5(offer6(handoff, end3(UpstreamEnd))), "onDone")
    });
    const handoffConsumer = pipe(getAndSet3(sinkLeftovers, empty7()), flatMap18((leftovers) => {
      if (isNonEmpty(leftovers)) {
        return pipe(set7(consumed, true), zipRight7(succeed13(pipe(write(leftovers), flatMap20(() => handoffConsumer)))));
      }
      return pipe(take10(handoff), map26((signal) => {
        switch (signal._tag) {
          case OP_EMIT4: {
            return pipe(fromEffect5(set7(consumed, true)), zipRight8(write(signal.elements)), zipRight8(fromEffect5(get13(endAfterEmit))), flatMap20((bool) => bool ? void_8 : handoffConsumer));
          }
          case OP_HALT: {
            return failCause12(signal.cause);
          }
          case OP_END: {
            if (signal.reason._tag === OP_SCHEDULE_END) {
              return pipe(get13(consumed), map26((bool) => bool ? fromEffect5(pipe(set7(sinkEndReason, ScheduleEnd), zipRight7(set7(endAfterEmit, true)))) : pipe(fromEffect5(pipe(set7(sinkEndReason, ScheduleEnd), zipRight7(set7(endAfterEmit, true)))), flatMap20(() => handoffConsumer))), unwrap2);
            }
            return pipe(set7(sinkEndReason, signal.reason), zipRight7(set7(endAfterEmit, true)), fromEffect5);
          }
        }
      }));
    }), unwrap2);
    const timeout6 = /* @__PURE__ */ __name((lastB) => scheduleDriver.next(lastB), "timeout");
    const scheduledAggregator = /* @__PURE__ */ __name((sinkFiber, scheduleFiber, scope6) => {
      const forkSink = pipe(set7(consumed, false), zipRight7(set7(endAfterEmit, false)), zipRight7(pipe(handoffConsumer, pipeToOrFail(toChannel(sink)), collectElements, run2, forkIn3(scope6))));
      const handleSide = /* @__PURE__ */ __name((leftovers, b, c) => pipe(set7(sinkLeftovers, flatten4(leftovers)), zipRight7(map26(get13(sinkEndReason), (reason) => {
        switch (reason._tag) {
          case OP_SCHEDULE_END: {
            return pipe(all10([get13(consumed), forkSink, pipe(timeout6(some3(b)), forkIn3(scope6))]), map26(([wasConsumed, sinkFiber2, scheduleFiber2]) => {
              const toWrite = pipe(c, match2({
                onNone: /* @__PURE__ */ __name(() => of3(right2(b)), "onNone"),
                onSome: /* @__PURE__ */ __name((c2) => make7(right2(b), left2(c2)), "onSome")
              }));
              if (wasConsumed) {
                return pipe(write(toWrite), flatMap20(() => scheduledAggregator(sinkFiber2, scheduleFiber2, scope6)));
              }
              return scheduledAggregator(sinkFiber2, scheduleFiber2, scope6);
            }), unwrap2);
          }
          case OP_UPSTREAM_END: {
            return pipe(get13(consumed), map26((wasConsumed) => wasConsumed ? write(of3(right2(b))) : void_8), unwrap2);
          }
        }
      })), unwrap2), "handleSide");
      return unwrap2(raceWith2(join4(sinkFiber), join4(scheduleFiber), {
        onSelfDone: /* @__PURE__ */ __name((sinkExit, _) => pipe(interrupt7(scheduleFiber), zipRight7(pipe(suspend4(() => sinkExit), map26(([leftovers, b]) => handleSide(leftovers, b, none2()))))), "onSelfDone"),
        onOtherDone: /* @__PURE__ */ __name((scheduleExit, _) => matchCauseEffect3(suspend4(() => scheduleExit), {
          onFailure: /* @__PURE__ */ __name((cause3) => match(failureOrCause2(cause3), {
            onLeft: /* @__PURE__ */ __name(() => pipe(handoff, offer6(end3(ScheduleEnd)), forkDaemon3, zipRight7(pipe(join4(sinkFiber), map26(([leftovers, b]) => handleSide(leftovers, b, none2()))))), "onLeft"),
            onRight: /* @__PURE__ */ __name((cause4) => pipe(handoff, offer6(halt(cause4)), forkDaemon3, zipRight7(pipe(join4(sinkFiber), map26(([leftovers, b]) => handleSide(leftovers, b, none2()))))), "onRight")
          }), "onFailure"),
          onSuccess: /* @__PURE__ */ __name((c) => pipe(handoff, offer6(end3(ScheduleEnd)), forkDaemon3, zipRight7(pipe(join4(sinkFiber), map26(([leftovers, b]) => handleSide(leftovers, b, some3(c)))))), "onSuccess")
        }), "onOtherDone")
      }));
    }, "scheduledAggregator");
    return unwrapScopedWith3((scope6) => pipeTo(toChannel2(self), handoffProducer).pipe(run2, forkIn3(scope6), zipRight7(pipeToOrFail(handoffConsumer, toChannel(sink)).pipe(collectElements, run2, forkIn3(scope6), flatMap18((sinkFiber) => timeout6(none2()).pipe(forkIn3(scope6), map26((scheduleFiber) => new StreamImpl(scheduledAggregator(sinkFiber, scheduleFiber, scope6)))))))));
  }));
});
var as13 = /* @__PURE__ */ dual(2, (self, value2) => map33(self, () => value2));
var queueFromBufferOptions = /* @__PURE__ */ __name((bufferSize) => {
  if (bufferSize === "unbounded") {
    return unbounded5();
  } else if (typeof bufferSize === "number" || bufferSize === void 0) {
    return bounded5(bufferSize ?? 16);
  }
  switch (bufferSize.strategy) {
    case "dropping":
      return dropping4(bufferSize.bufferSize ?? 16);
    case "sliding":
      return sliding4(bufferSize.bufferSize ?? 16);
    default:
      return bounded5(bufferSize.bufferSize ?? 16);
  }
}, "queueFromBufferOptions");
var _async = /* @__PURE__ */ __name((register, bufferSize) => acquireRelease3(queueFromBufferOptions(bufferSize), (queue) => shutdown4(queue)).pipe(flatMap18((output) => runtime4().pipe(flatMap18((runtime5) => sync8(() => {
  const runPromiseExit4 = runPromiseExit3(runtime5);
  const canceler = register(make67((resume2) => fromPull(resume2).pipe(flatMap18((take13) => offer3(output, take13)), asVoid6, runPromiseExit4).then((exit4) => {
    if (isFailure2(exit4)) {
      if (!isInterrupted3(exit4.cause)) {
        throw squash(exit4.cause);
      }
    }
  })));
  return canceler;
})), map26((value2) => {
  const loop4 = take6(output).pipe(flatMap18((take13) => done11(take13)), match20({
    onFailure: /* @__PURE__ */ __name((maybeError) => fromEffect5(shutdown4(output)).pipe(zipRight8(match2(maybeError, {
      onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
      onSome: /* @__PURE__ */ __name((error2) => fail14(error2), "onSome")
    }))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((chunk4) => write(chunk4).pipe(flatMap20(() => loop4)), "onSuccess")
  }), unwrap2);
  return fromChannel2(loop4).pipe(ensuring9(value2 ?? _void));
}))), unwrapScoped5), "_async");
var asyncEffect3 = /* @__PURE__ */ __name((register, bufferSize) => pipe(acquireRelease3(queueFromBufferOptions(bufferSize), (queue) => shutdown4(queue)), flatMap18((output) => pipe(runtime4(), flatMap18((runtime5) => pipe(register(make67((k) => pipe(fromPull(k), flatMap18((take13) => offer3(output, take13)), asVoid6, runPromiseExit3(runtime5)).then((exit4) => {
  if (isFailure2(exit4)) {
    if (!isInterrupted3(exit4.cause)) {
      throw squash(exit4.cause);
    }
  }
}))), map26(() => {
  const loop4 = pipe(take6(output), flatMap18(done11), match20({
    onFailure: /* @__PURE__ */ __name((maybeError) => pipe(fromEffect5(shutdown4(output)), zipRight8(match2(maybeError, {
      onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
      onSome: fail14
    }))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name((chunk4) => pipe(write(chunk4), flatMap20(() => loop4)), "onSuccess")
  }), unwrap2);
  return loop4;
}))))), unwrapScoped3, fromChannel2), "asyncEffect");
var queueFromBufferOptionsPush = /* @__PURE__ */ __name((options) => {
  if (options?.bufferSize === "unbounded" || options?.bufferSize === void 0 && options?.strategy === void 0) {
    return unbounded5();
  }
  switch (options?.strategy) {
    case "sliding":
      return sliding4(options.bufferSize ?? 16);
    default:
      return dropping4(options?.bufferSize ?? 16);
  }
}, "queueFromBufferOptionsPush");
var asyncPush = /* @__PURE__ */ __name((register, options) => acquireRelease3(queueFromBufferOptionsPush(options), shutdown4).pipe(tap5((queue) => getWith(currentScheduler2, (scheduler) => register(makePush(queue, scheduler)))), map26((queue) => {
  const loop4 = flatMap20(take6(queue), (item) => isExit(item) ? isSuccess(item) ? void_8 : failCause12(item.cause) : zipRight8(write(unsafeFromArray(item)), loop4));
  return loop4;
}), unwrapScoped3, fromChannel2), "asyncPush");
var asyncScoped = /* @__PURE__ */ __name((register, bufferSize) => pipe(acquireRelease3(queueFromBufferOptions(bufferSize), (queue) => shutdown4(queue)), flatMap18((output) => pipe(runtime4(), flatMap18((runtime5) => pipe(register(make67((k) => pipe(fromPull(k), flatMap18((take13) => offer3(output, take13)), asVoid6, runPromiseExit3(runtime5)).then((exit4) => {
  if (isFailure2(exit4)) {
    if (!isInterrupted3(exit4.cause)) {
      throw squash(exit4.cause);
    }
  }
}))), zipRight7(make35(false)), flatMap18((ref) => pipe(get13(ref), map26((isDone9) => isDone9 ? end5() : pipe(take6(output), flatMap18(done11), onError3(() => pipe(set7(ref, true), zipRight7(shutdown4(output)))))))))))), scoped7, flatMap23(repeatEffectChunkOption)), "asyncScoped");
var branchAfter = /* @__PURE__ */ dual(3, (self, n, f) => suspend10(() => {
  const buffering = /* @__PURE__ */ __name((acc) => readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const nextSize = acc.length + input.length;
      if (nextSize >= n) {
        const [b1, b2] = pipe(input, splitAt2(n - acc.length));
        return running4(pipe(acc, appendAll3(b1)), b2);
      }
      return buffering(pipe(acc, appendAll3(input)));
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => running4(acc, empty7()), "onDone")
  }), "buffering");
  const running4 = /* @__PURE__ */ __name((prefix, leftover2) => pipeTo(zipRight8(write(leftover2), identityChannel()), toChannel2(f(prefix))), "running");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(buffering(empty7()))));
}));
var broadcast = /* @__PURE__ */ dual(3, (self, n, maximumLag) => pipe(self, broadcastedQueues(n, maximumLag), map26((tuple5) => tuple5.map((queue) => flattenTake(fromQueue3(queue, {
  shutdown: true
}))))));
var broadcastDynamic = /* @__PURE__ */ dual(2, (self, maximumLag) => map26(toPubSub2(self, maximumLag), (pubsub) => flattenTake(fromPubSub3(pubsub))));
var share = /* @__PURE__ */ dual(2, (self, options) => map26(make62({
  acquire: broadcastDynamic(self, options),
  idleTimeToLive: options.idleTimeToLive
}), (rcRef) => unwrapScoped5(get16(rcRef))));
var broadcastedQueues = /* @__PURE__ */ dual(3, (self, n, maximumLag) => flatMap18(pubsubFromOptions(maximumLag), (pubsub) => pipe(all10(Array.from({
  length: n
}, () => subscribe2(pubsub))), tap5(() => forkScoped3(runIntoPubSubScoped(self, pubsub))))));
var broadcastedQueuesDynamic = /* @__PURE__ */ dual(2, (self, maximumLag) => map26(toPubSub2(self, maximumLag), subscribe2));
var buffer2 = /* @__PURE__ */ dual(2, (self, options) => {
  if (options.capacity === "unbounded") {
    return bufferUnbounded(self);
  } else if (options.strategy === "dropping") {
    return bufferDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferSliding(self, options.capacity);
  }
  const queue = toQueueOfElements(self, options);
  return new StreamImpl(unwrapScoped3(map26(queue, (queue2) => {
    const process2 = pipe(fromEffect5(take6(queue2)), flatMap20(match7({
      onFailure: /* @__PURE__ */ __name((cause3) => pipe(flipCauseOption2(cause3), match2({
        onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
        onSome: failCause12
      })), "onFailure"),
      onSuccess: /* @__PURE__ */ __name((value2) => flatMap20(write(of3(value2)), () => process2), "onSuccess")
    })));
    return process2;
  })));
});
var bufferChunks = /* @__PURE__ */ dual(2, (self, options) => {
  if (options.strategy === "dropping") {
    return bufferChunksDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferChunksSliding(self, options.capacity);
  }
  const queue = toQueue2(self, options);
  return new StreamImpl(unwrapScoped3(map26(queue, (queue2) => {
    const process2 = pipe(fromEffect5(take6(queue2)), flatMap20(match32({
      onEnd: /* @__PURE__ */ __name(() => void_8, "onEnd"),
      onFailure: failCause12,
      onSuccess: /* @__PURE__ */ __name((value2) => pipe(write(value2), flatMap20(() => process2)), "onSuccess")
    })));
    return process2;
  })));
});
var bufferChunksDropping = /* @__PURE__ */ dual(2, (self, capacity10) => {
  const queue = acquireRelease3(dropping4(capacity10), (queue2) => shutdown4(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferChunksSliding = /* @__PURE__ */ dual(2, (self, capacity10) => {
  const queue = acquireRelease3(sliding4(capacity10), (queue2) => shutdown4(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferDropping = /* @__PURE__ */ dual(2, (self, capacity10) => {
  const queue = acquireRelease3(dropping4(capacity10), (queue2) => shutdown4(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(rechunk(1)(self))));
});
var bufferSliding = /* @__PURE__ */ dual(2, (self, capacity10) => {
  const queue = acquireRelease3(sliding4(capacity10), (queue2) => shutdown4(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(pipe(self, rechunk(1)))));
});
var bufferUnbounded = /* @__PURE__ */ __name((self) => {
  const queue = toQueue2(self, {
    strategy: "unbounded"
  });
  return new StreamImpl(unwrapScoped3(map26(queue, (queue2) => {
    const process2 = pipe(fromEffect5(take6(queue2)), flatMap20(match32({
      onEnd: /* @__PURE__ */ __name(() => void_8, "onEnd"),
      onFailure: failCause12,
      onSuccess: /* @__PURE__ */ __name((value2) => flatMap20(write(value2), () => process2), "onSuccess")
    })));
    return process2;
  })));
}, "bufferUnbounded");
var bufferSignal = /* @__PURE__ */ __name((scoped9, bufferChannel) => {
  const producer = /* @__PURE__ */ __name((queue, ref) => {
    const terminate = /* @__PURE__ */ __name((take13) => pipe(get13(ref), tap5(_await), zipRight7(make22()), flatMap18((deferred) => pipe(offer3(queue, [take13, deferred]), zipRight7(set7(ref, deferred)), zipRight7(_await(deferred)))), asVoid6, fromEffect5), "terminate");
    return readWithCause({
      onInput: /* @__PURE__ */ __name((input) => pipe(make22(), flatMap18((deferred) => pipe(offer3(queue, [chunk3(input), deferred]), flatMap18((added) => pipe(set7(ref, deferred), when3(() => added))))), asVoid6, fromEffect5, flatMap20(() => producer(queue, ref))), "onInput"),
      onFailure: /* @__PURE__ */ __name((error2) => terminate(failCause14(error2)), "onFailure"),
      onDone: /* @__PURE__ */ __name(() => terminate(end4), "onDone")
    });
  }, "producer");
  const consumer = /* @__PURE__ */ __name((queue) => {
    const process2 = pipe(fromEffect5(take6(queue)), flatMap20(([take13, deferred]) => zipRight8(fromEffect5(succeed2(deferred, void 0)), match32(take13, {
      onEnd: /* @__PURE__ */ __name(() => void_8, "onEnd"),
      onFailure: failCause12,
      onSuccess: /* @__PURE__ */ __name((value2) => pipe(write(value2), flatMap20(() => process2)), "onSuccess")
    }))));
    return process2;
  }, "consumer");
  return unwrapScoped3(pipe(scoped9, flatMap18((queue) => pipe(make22(), tap5((start3) => succeed2(start3, void 0)), flatMap18((start3) => pipe(make35(start3), flatMap18((ref) => pipe(bufferChannel, pipeTo(producer(queue, ref)), runScoped, forkScoped3)), as8(consumer(queue))))))));
}, "bufferSignal");
var catchAll8 = /* @__PURE__ */ dual(2, (self, f) => catchAllCause7(self, (cause3) => match(failureOrCause2(cause3), {
  onLeft: f,
  onRight: failCause16
})));
var catchAllCause7 = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), catchAllCause6((cause3) => toChannel2(f(cause3))))));
var catchSome4 = /* @__PURE__ */ dual(2, (self, pf) => pipe(self, catchAll8((error2) => pipe(pf(error2), getOrElse2(() => fail20(error2))))));
var catchSomeCause3 = /* @__PURE__ */ dual(2, (self, pf) => pipe(self, catchAllCause7((cause3) => pipe(pf(cause3), getOrElse2(() => failCause16(cause3))))));
var catchTag5 = /* @__PURE__ */ dual(3, (self, k, f) => catchAll8(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail20(e);
}));
var catchTags4 = /* @__PURE__ */ dual(2, (self, cases) => catchAll8(self, (e) => {
  const keys7 = Object.keys(cases);
  if ("_tag" in e && keys7.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail20(e);
}));
var changes = /* @__PURE__ */ __name((self) => pipe(self, changesWith((x, y) => equals(y)(x))), "changes");
var changesWith = /* @__PURE__ */ dual(2, (self, f) => {
  const writer = /* @__PURE__ */ __name((last7) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => {
      const [newLast, newChunk] = reduce4(input, [last7, empty7()], ([option5, outputs], output) => {
        if (isSome2(option5) && f(option5.value, output)) {
          return [some3(output), outputs];
        }
        return [some3(output), pipe(outputs, append3(output))];
      });
      return flatMap20(write(newChunk), () => writer(newLast));
    }, "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "writer");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none2()))));
});
var changesWithEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const writer = /* @__PURE__ */ __name((last7) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => pipe(input, reduce17([last7, empty7()], ([option5, outputs], output) => {
      if (isSome2(option5)) {
        return pipe(f(option5.value, output), map26((bool) => bool ? [some3(output), outputs] : [some3(output), pipe(outputs, append3(output))]));
      }
      return succeed13([some3(output), pipe(outputs, append3(output))]);
    }), fromEffect5, flatMap20(([newLast, newChunk]) => pipe(write(newChunk), flatMap20(() => writer(newLast))))), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "writer");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none2()))));
});
var chunks = /* @__PURE__ */ __name((self) => pipe(self, mapChunks(of3)), "chunks");
var chunksWith = /* @__PURE__ */ dual(2, (self, f) => flattenChunks(f(chunks(self))));
var unsome2 = /* @__PURE__ */ __name((effect4) => catchAll4(asSome3(effect4), (o) => o._tag === "None" ? succeedNone3 : fail12(o.value)), "unsome");
var combine16 = /* @__PURE__ */ dual(4, (self, that, s, f) => {
  function producer(handoff, latch) {
    return fromEffect5(take10(latch)).pipe(zipRight8(readWithCause({
      onInput: /* @__PURE__ */ __name((input) => flatMap20(fromEffect5(offer6(handoff, succeed3(input))), () => producer(handoff, latch)), "onInput"),
      onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer6(handoff, failCause3(pipe(cause3, map22(some3))))), "onFailure"),
      onDone: /* @__PURE__ */ __name(() => flatMap20(fromEffect5(offer6(handoff, fail4(none2()))), () => producer(handoff, latch)), "onDone")
    })));
  }
  __name(producer, "producer");
  return new StreamImpl(unwrapScopedWith((scope6) => all10([make68(), make68(), make68(), make68()]).pipe(tap5(([left3, _, latchL]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left3, latchL)), runIn(scope6), forkIn3(scope6))), tap5(([, right3, _, rightL]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right3, rightL)), runIn(scope6), forkIn3(scope6))), map26(([left3, right3, latchL, latchR]) => {
    const pullLeft = offer6(latchL, void 0).pipe(zipRight7(take10(left3).pipe(flatMap18(identity))));
    const pullRight = offer6(latchR, void 0).pipe(zipRight7(take10(right3).pipe(flatMap18(identity))));
    return toChannel2(unfoldEffect(s, (s2) => flatMap18(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var combineChunks = /* @__PURE__ */ dual(4, (self, that, s, f) => {
  const producer = /* @__PURE__ */ __name((handoff, latch) => zipRight8(fromEffect5(take10(latch)), readWithCause({
    onInput: /* @__PURE__ */ __name((input) => flatMap20(fromEffect5(pipe(handoff, offer6(chunk3(input)))), () => producer(handoff, latch)), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer6(handoff, failCause14(cause3))), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => fromEffect5(offer6(handoff, end4)), "onDone")
  })), "producer");
  return new StreamImpl(unwrapScopedWith((scope6) => all10([make68(), make68(), make68(), make68()]).pipe(tap5(([left3, _, latchL]) => pipeTo(toChannel2(self), producer(left3, latchL)).pipe(runIn(scope6), forkIn3(scope6))), tap5(([_, right3, __, latchR]) => pipeTo(toChannel2(that), producer(right3, latchR)).pipe(runIn(scope6), forkIn3(scope6))), map26(([left3, right3, latchL, latchR]) => {
    const pullLeft = offer6(latchL, void 0).pipe(zipRight7(take10(left3).pipe(flatMap18(done11))));
    const pullRight = offer6(latchR, void 0).pipe(zipRight7(take10(right3).pipe(flatMap18(done11))));
    return toChannel2(unfoldChunkEffect(s, (s2) => flatMap18(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var concat3 = /* @__PURE__ */ dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), zipRight8(toChannel2(that)))));
var concatAll2 = /* @__PURE__ */ __name((streams) => suspend10(() => pipe(streams, reduce4(empty44, (x, y) => concat3(y)(x)))), "concatAll");
var cross = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, crossWith(right3, (a, a2) => [a, a2])));
var crossLeft = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, crossWith(right3, (a, _) => a)));
var crossRight = /* @__PURE__ */ dual(2, (left3, right3) => flatMap23(left3, () => right3));
var crossWith = /* @__PURE__ */ dual(3, (left3, right3, f) => pipe(left3, flatMap23((a) => pipe(right3, map33((b) => f(a, b))))));
var debounce = /* @__PURE__ */ dual(2, (self, duration3) => unwrapScopedWith3((scope6) => gen5(function* () {
  const handoff = yield* make68();
  function enqueue(last7) {
    return sleep2(duration3).pipe(as8(last7), forkIn3(scope6), map26((fiber) => consumer(previous(fiber))));
  }
  __name(enqueue, "enqueue");
  const producer = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => match2(last2(input), {
      onNone: /* @__PURE__ */ __name(() => producer, "onNone"),
      onSome: /* @__PURE__ */ __name((elem) => fromEffect5(offer6(handoff, emit2(of3(elem)))).pipe(flatMap20(() => producer)), "onSome")
    }), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer6(handoff, halt(cause3))), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => fromEffect5(offer6(handoff, end3(UpstreamEnd))), "onDone")
  });
  function consumer(state) {
    switch (state._tag) {
      case OP_NOT_STARTED: {
        return unwrap2(take10(handoff).pipe(map26((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap2(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause12(signal.cause);
            }
            case OP_END: {
              return void_8;
            }
          }
        })));
      }
      case OP_PREVIOUS: {
        return unwrap2(take10(handoff).pipe(forkIn3(scope6), flatMap18((handoffFiber) => raceWith2(join4(state.fiber), join4(handoffFiber), {
          onSelfDone: /* @__PURE__ */ __name((leftExit, current2) => match7(leftExit, {
            onFailure: /* @__PURE__ */ __name((cause3) => interrupt7(current2).pipe(as8(failCause12(cause3))), "onFailure"),
            onSuccess: /* @__PURE__ */ __name((chunk4) => interrupt7(current2).pipe(zipRight7(succeed13(write(chunk4).pipe(flatMap20(() => consumer(current(handoffFiber))))))), "onSuccess")
          }), "onSelfDone"),
          onOtherDone: /* @__PURE__ */ __name((rightExit, previous2) => match7(rightExit, {
            onFailure: /* @__PURE__ */ __name((cause3) => interrupt7(previous2).pipe(as8(failCause12(cause3))), "onFailure"),
            onSuccess: /* @__PURE__ */ __name((signal) => {
              switch (signal._tag) {
                case OP_EMIT4: {
                  return interrupt7(previous2).pipe(zipRight7(enqueue(signal.elements)));
                }
                case OP_HALT: {
                  return interrupt7(previous2).pipe(as8(failCause12(signal.cause)));
                }
                case OP_END: {
                  return join4(previous2).pipe(map26((chunk4) => write(chunk4).pipe(zipRight8(void_8))));
                }
              }
            }, "onSuccess")
          }), "onOtherDone")
        }))));
      }
      case OP_CURRENT: {
        return unwrap2(join4(state.fiber).pipe(map26((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap2(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause12(signal.cause);
            }
            case OP_END: {
              return void_8;
            }
          }
        })));
      }
    }
  }
  __name(consumer, "consumer");
  return scopedWith4((scope7) => pipeTo(toChannel2(self), producer).pipe(runIn(scope7), forkIn3(scope7))).pipe(crossRight(new StreamImpl(consumer(notStarted))));
})));
var die14 = /* @__PURE__ */ __name((defect) => fromEffect9(die8(defect)), "die");
var dieSync8 = /* @__PURE__ */ __name((evaluate3) => fromEffect9(dieSync4(evaluate3)), "dieSync");
var dieMessage6 = /* @__PURE__ */ __name((message) => fromEffect9(dieMessage2(message)), "dieMessage");
var distributedWith = /* @__PURE__ */ dual(2, (self, options) => pipe(make22(), flatMap18((deferred) => pipe(self, distributedWithDynamic({
  maximumLag: options.maximumLag,
  decide: /* @__PURE__ */ __name((a) => flatMap18(_await(deferred), (f) => f(a)), "decide")
}), flatMap18((next4) => pipe(all10(map7(range3(0, options.size - 1), (id3) => map26(next4, ([key, queue]) => [[key, id3], queue]))), map26(unsafeFromArray), flatMap18((entries3) => {
  const [mappings, queues] = reduceRight2(entries3, [/* @__PURE__ */ new Map(), empty7()], ([mappings2, queues2], [mapping, queue]) => [mappings2.set(mapping[0], mapping[1]), pipe(queues2, prepend3(queue))]);
  return pipe(succeed2(deferred, (a) => map26(options.decide(a), (f) => (key) => f(mappings.get(key)))), as8(Array.from(queues)));
})))))));
var distributedWithDynamicId = {
  ref: 0
};
var newDistributedWithDynamicId = /* @__PURE__ */ __name(() => {
  const current2 = distributedWithDynamicId.ref;
  distributedWithDynamicId.ref = current2 + 1;
  return current2;
}, "newDistributedWithDynamicId");
var distributedWithDynamic = /* @__PURE__ */ dual(2, (self, options) => distributedWithDynamicCallback(self, options.maximumLag, options.decide, () => _void));
var distributedWithDynamicCallback = /* @__PURE__ */ dual(4, (self, maximumLag, decide, done12) => pipe(acquireRelease3(make35(/* @__PURE__ */ new Map()), (ref, _) => pipe(get13(ref), flatMap18((queues) => pipe(queues.values(), forEach16(shutdown4))))), flatMap18((queuesRef) => gen5(function* () {
  const offer7 = /* @__PURE__ */ __name((a) => pipe(decide(a), flatMap18((shouldProcess) => pipe(get13(queuesRef), flatMap18((queues) => pipe(queues.entries(), reduce17(empty7(), (acc, [id3, queue]) => {
    if (shouldProcess(id3)) {
      return pipe(offer3(queue, succeed3(a)), matchCauseEffect3({
        onFailure: /* @__PURE__ */ __name((cause3) => (
          // Ignore all downstream queues that were shut
          // down and remove them later
          isInterrupted3(cause3) ? succeed13(pipe(acc, prepend3(id3))) : failCause10(cause3)
        ), "onFailure"),
        onSuccess: /* @__PURE__ */ __name(() => succeed13(acc), "onSuccess")
      }));
    }
    return succeed13(acc);
  }), flatMap18((ids3) => {
    if (isNonEmpty(ids3)) {
      return update4(queuesRef, (map36) => {
        for (const id3 of ids3) {
          map36.delete(id3);
        }
        return map36;
      });
    }
    return _void;
  }))))), asVoid6), "offer");
  const queuesLock = yield* makeSemaphore2(1);
  const newQueue = yield* make35(pipe(bounded5(maximumLag), flatMap18((queue) => {
    const id3 = newDistributedWithDynamicId();
    return pipe(update4(queuesRef, (map36) => map36.set(id3, queue)), as8([id3, queue]));
  })));
  const finalize = /* @__PURE__ */ __name((endTake) => (
    // Make sure that no queues are currently being added
    queuesLock.withPermits(1)(pipe(set7(newQueue, pipe(
      // All newly created queues should end immediately
      bounded5(1),
      tap5((queue) => offer3(queue, endTake)),
      flatMap18((queue) => {
        const id3 = newDistributedWithDynamicId();
        return pipe(update4(queuesRef, (map36) => map36.set(id3, queue)), as8(make3(id3, queue)));
      })
    )), zipRight7(pipe(get13(queuesRef), flatMap18((map36) => pipe(fromIterable3(map36.values()), forEach16((queue) => pipe(offer3(queue, endTake), catchSomeCause2((cause3) => isInterrupted3(cause3) ? some3(_void) : none2()))))))), zipRight7(done12(endTake)), asVoid6))
  ), "finalize");
  yield* pipe(self, runForEachScoped(offer7), matchCauseEffect3({
    onFailure: /* @__PURE__ */ __name((cause3) => finalize(failCause3(pipe(cause3, map22(some3)))), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => finalize(fail4(none2())), "onSuccess")
  }), forkScoped3);
  return queuesLock.withPermits(1)(flatten12(get13(newQueue)));
}))));
var drain3 = /* @__PURE__ */ __name((self) => new StreamImpl(drain(toChannel2(self))), "drain");
var drainFork = /* @__PURE__ */ dual(2, (self, that) => fromEffect9(make22()).pipe(flatMap23((backgroundDied) => scopedWith4((scope6) => toChannel2(that).pipe(drain, runIn(scope6), catchAllCause4((cause3) => failCause2(backgroundDied, cause3)), forkIn3(scope6))).pipe(crossRight(interruptWhenDeferred2(self, backgroundDied))))));
var drop6 = /* @__PURE__ */ dual(2, (self, n) => {
  const loop4 = /* @__PURE__ */ __name((r) => readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const dropped = pipe(input, drop3(r));
      const leftover2 = Math.max(0, r - input.length);
      const more = isEmpty2(input) || leftover2 > 0;
      if (more) {
        return loop4(leftover2);
      }
      return pipe(write(dropped), zipRight8(identityChannel()));
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4(n))));
});
var dropRight3 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return identityStream();
  }
  return suspend10(() => {
    const queue = new RingBuffer(n);
    const reader = readWith({
      onInput: /* @__PURE__ */ __name((input) => {
        const outputs = pipe(input, filterMap5((elem) => {
          const head10 = queue.head();
          queue.put(elem);
          return head10;
        }));
        return pipe(write(outputs), flatMap20(() => reader));
      }, "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
    });
    return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(reader)));
  });
});
var dropUntil4 = /* @__PURE__ */ dual(2, (self, predicate) => drop6(dropWhile6(self, (a) => !predicate(a)), 1));
var dropUntilEffect2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(dropUntil2(input, predicate), map26(unsafeFromArray), map26((leftover2) => {
      const more = isEmpty2(leftover2);
      if (more) {
        return suspend6(() => loop4);
      }
      return pipe(write(leftover2), zipRight8(identityChannel()));
    }), unwrap2), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4)));
});
var dropWhile6 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const output = dropWhile2(input, predicate);
      if (isEmpty2(output)) {
        return suspend6(() => loop4);
      }
      return zipRight8(write(output), identityChannel());
    }, "onInput"),
    onFailure: fail14,
    onDone: succeedNow
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop4));
});
var dropWhileEffect2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(dropWhile4(input, predicate), map26(unsafeFromArray), map26((leftover2) => {
      const more = isEmpty2(leftover2);
      if (more) {
        return suspend6(() => loop4);
      }
      return zipRight8(write(leftover2), identityChannel());
    }), unwrap2), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop4));
});
var either9 = /* @__PURE__ */ __name((self) => pipe(self, map33(right2), catchAll8((error2) => make70(left2(error2)))), "either");
var empty44 = /* @__PURE__ */ new StreamImpl(void_8);
var ensuring9 = /* @__PURE__ */ dual(2, (self, finalizer3) => new StreamImpl(pipe(toChannel2(self), ensuring5(finalizer3))));
var ensuringWith3 = /* @__PURE__ */ dual(2, (self, finalizer3) => new StreamImpl(ensuringWith(toChannel2(self), finalizer3)));
var context9 = /* @__PURE__ */ __name(() => fromEffect9(context4()), "context");
var contextWith7 = /* @__PURE__ */ __name((f) => pipe(context9(), map33(f)), "contextWith");
var contextWithEffect5 = /* @__PURE__ */ __name((f) => pipe(context9(), mapEffectSequential(f)), "contextWithEffect");
var contextWithStream = /* @__PURE__ */ __name((f) => pipe(context9(), flatMap23(f)), "contextWithStream");
var execute = /* @__PURE__ */ __name((effect4) => drain3(fromEffect9(effect4)), "execute");
var fail20 = /* @__PURE__ */ __name((error2) => fromEffectOption(fail12(some3(error2))), "fail");
var failSync10 = /* @__PURE__ */ __name((evaluate3) => fromEffectOption(failSync5(() => some3(evaluate3()))), "failSync");
var failCause16 = /* @__PURE__ */ __name((cause3) => fromEffect9(failCause10(cause3)), "failCause");
var failCauseSync9 = /* @__PURE__ */ __name((evaluate3) => fromEffect9(failCauseSync5(evaluate3)), "failCauseSync");
var filter18 = /* @__PURE__ */ dual(2, (self, predicate) => mapChunks(self, filter5(predicate)));
var filterEffect = /* @__PURE__ */ dual(2, (self, f) => {
  const loop4 = /* @__PURE__ */ __name((iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((input) => loop4(input[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeed15
      });
    } else {
      return pipe(f(next4.value), map26((bool) => bool ? pipe(write(of3(next4.value)), flatMap20(() => loop4(iterator))) : loop4(iterator)), unwrap2);
    }
  }, "loop");
  return new StreamImpl(suspend6(() => pipe(toChannel2(self), pipeTo(loop4(empty7()[Symbol.iterator]())))));
});
var filterMap12 = /* @__PURE__ */ dual(2, (self, pf) => mapChunks(self, filterMap5(pf)));
var filterMapEffect = /* @__PURE__ */ dual(2, (self, pf) => suspend10(() => {
  const loop4 = /* @__PURE__ */ __name((iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((input) => loop4(input[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeed15
      });
    } else {
      return pipe(pf(next4.value), match2({
        onNone: /* @__PURE__ */ __name(() => sync8(() => loop4(iterator)), "onNone"),
        onSome: map26((a2) => flatMap20(write(of3(a2)), () => loop4(iterator)))
      }), unwrap2);
    }
  }, "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4(empty7()[Symbol.iterator]()))));
}));
var filterMapWhile4 = /* @__PURE__ */ dual(2, (self, pf) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const mapped = filterMapWhile3(input, pf);
      if (mapped.length === input.length) {
        return pipe(write(mapped), flatMap20(() => loop4));
      }
      return write(mapped);
    }, "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4)));
});
var filterMapWhileEffect = /* @__PURE__ */ dual(2, (self, pf) => suspend10(() => {
  const loop4 = /* @__PURE__ */ __name((iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((input) => loop4(input[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeed15
      });
    } else {
      return unwrap2(match2(pf(next4.value), {
        onNone: /* @__PURE__ */ __name(() => succeed13(void_8), "onNone"),
        onSome: map26((a2) => flatMap20(write(of3(a2)), () => loop4(iterator)))
      }));
    }
  }, "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4(empty7()[Symbol.iterator]()))));
}));
var finalizer = /* @__PURE__ */ __name((finalizer3) => acquireRelease4(_void, () => finalizer3), "finalizer");
var find3 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => match2(findFirst4(input, predicate), {
      onNone: /* @__PURE__ */ __name(() => loop4, "onNone"),
      onSome: /* @__PURE__ */ __name((n) => write(of3(n)), "onSome")
    }), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4)));
});
var findEffect2 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(findFirst11(input, predicate), map26(match2({
      onNone: /* @__PURE__ */ __name(() => loop4, "onNone"),
      onSome: /* @__PURE__ */ __name((n) => write(of3(n)), "onSome")
    })), unwrap2), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4)));
});
var flatMap23 = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, f, options) => {
  const bufferSize = options?.bufferSize ?? 16;
  if (options?.switch) {
    return matchConcurrency(options?.concurrency, () => flatMapParSwitchBuffer(self, 1, bufferSize, f), (n) => flatMapParSwitchBuffer(self, n, bufferSize, f));
  }
  return matchConcurrency(options?.concurrency, () => new StreamImpl(concatMap(toChannel2(self), (as15) => pipe(as15, map7((a) => toChannel2(f(a))), reduce4(void_8, (left3, right3) => pipe(left3, zipRight8(right3)))))), (_) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), options))));
});
var matchConcurrency = /* @__PURE__ */ __name((concurrency, sequential5, bounded10) => {
  switch (concurrency) {
    case void 0:
      return sequential5();
    case "unbounded":
      return bounded10(Number.MAX_SAFE_INTEGER);
    default:
      return concurrency > 1 ? bounded10(concurrency) : sequential5();
  }
}, "matchConcurrency");
var flatMapParSwitchBuffer = /* @__PURE__ */ dual(4, (self, n, bufferSize, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), {
  concurrency: n,
  mergeStrategy: BufferSliding(),
  bufferSize
}))));
var flatten16 = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => flatMap23(self, identity, options));
var flattenChunks = /* @__PURE__ */ __name((self) => {
  const flatten18 = readWithCause({
    onInput: /* @__PURE__ */ __name((chunks3) => flatMap20(writeChunk(chunks3), () => flatten18), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(flatten18)));
}, "flattenChunks");
var flattenEffect = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => options?.unordered ? flatMap23(self, (a) => fromEffect9(a), {
  concurrency: options.concurrency
}) : matchConcurrency(options?.concurrency, () => mapEffectSequential(self, identity), (n) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(identity, n), mapOut(of3)))));
var flattenExitOption = /* @__PURE__ */ __name((self) => {
  const processChunk = /* @__PURE__ */ __name((chunk4, cont) => {
    const [toEmit, rest] = pipe(chunk4, splitWhere2((exit4) => !isSuccess(exit4)));
    const next4 = pipe(head3(rest), match2({
      onNone: /* @__PURE__ */ __name(() => cont, "onNone"),
      onSome: match7({
        onFailure: /* @__PURE__ */ __name((cause3) => match2(flipCauseOption2(cause3), {
          onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
          onSome: failCause12
        }), "onFailure"),
        onSuccess: /* @__PURE__ */ __name(() => void_8, "onSuccess")
      })
    }));
    return pipe(write(pipe(toEmit, filterMap5((exit4) => isSuccess(exit4) ? some3(exit4.value) : none2()))), flatMap20(() => next4));
  }, "processChunk");
  const process2 = readWithCause({
    onInput: /* @__PURE__ */ __name((chunk4) => processChunk(chunk4, process2), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => failCause12(cause3), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process2)));
}, "flattenExitOption");
var flattenIterables = /* @__PURE__ */ __name((self) => pipe(self, map33(fromIterable3), flattenChunks), "flattenIterables");
var flattenTake = /* @__PURE__ */ __name((self) => flattenChunks(flattenExitOption(pipe(self, map33((take13) => take13.exit)))), "flattenTake");
var forever6 = /* @__PURE__ */ __name((self) => new StreamImpl(repeated(toChannel2(self))), "forever");
var fromAsyncIterable = /* @__PURE__ */ __name((iterable, onError6) => pipe(acquireRelease3(sync8(() => iterable[Symbol.asyncIterator]()), (iterator) => iterator.return ? promise3(async () => iterator.return()) : _void), map26((iterator) => repeatEffectOption(pipe(tryPromise3({
  try: /* @__PURE__ */ __name(async () => iterator.next(), "try"),
  catch: /* @__PURE__ */ __name((reason) => some3(onError6(reason)), "catch")
}), flatMap18((result) => result.done ? fail12(none2()) : succeed13(result.value))))), unwrapScoped5), "fromAsyncIterable");
var fromChannel2 = /* @__PURE__ */ __name((channel) => new StreamImpl(channel), "fromChannel");
var toChannel2 = /* @__PURE__ */ __name((stream) => {
  if ("channel" in stream) {
    return stream.channel;
  } else if (isEffect2(stream)) {
    return toChannel2(fromEffect9(stream));
  } else {
    throw new TypeError(`Expected a Stream.`);
  }
}, "toChannel");
var fromChunk = /* @__PURE__ */ __name((chunk4) => new StreamImpl(isEmpty2(chunk4) ? void_8 : write(chunk4)), "fromChunk");
var fromChunkPubSub = /* @__PURE__ */ __name((pubsub, options) => {
  if (options?.scoped) {
    const effect4 = map26(subscribe2(pubsub), fromChunkQueue);
    return options.shutdown ? map26(effect4, ensuring9(shutdown3(pubsub))) : effect4;
  }
  const stream = flatMap23(scoped7(subscribe2(pubsub)), fromChunkQueue);
  return options?.shutdown ? ensuring9(stream, shutdown3(pubsub)) : stream;
}, "fromChunkPubSub");
var fromChunkQueue = /* @__PURE__ */ __name((queue, options) => pipe(take6(queue), catchAllCause4((cause3) => pipe(isShutdown4(queue), flatMap18((isShutdown9) => isShutdown9 && isInterrupted3(cause3) ? end5() : failCause15(cause3)))), repeatEffectChunkOption, options?.shutdown ? ensuring9(shutdown4(queue)) : identity), "fromChunkQueue");
var fromChunks = /* @__PURE__ */ __name((...chunks3) => pipe(fromIterable17(chunks3), flatMap23(fromChunk)), "fromChunks");
var fromEffect9 = /* @__PURE__ */ __name((effect4) => pipe(effect4, mapError5(some3), fromEffectOption), "fromEffect");
var fromEffectOption = /* @__PURE__ */ __name((effect4) => new StreamImpl(unwrap2(match20(effect4, {
  onFailure: match2({
    onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
    onSome: fail14
  }),
  onSuccess: /* @__PURE__ */ __name((a) => write(of3(a)), "onSuccess")
}))), "fromEffectOption");
var fromPubSub3 = /* @__PURE__ */ __name((pubsub, options) => {
  const maxChunkSize = options?.maxChunkSize ?? DefaultChunkSize;
  if (options?.scoped) {
    const effect4 = map26(subscribe2(pubsub), (queue) => fromQueue3(queue, {
      maxChunkSize,
      shutdown: true
    }));
    return options.shutdown ? map26(effect4, ensuring9(shutdown3(pubsub))) : effect4;
  }
  const stream = flatMap23(scoped7(subscribe2(pubsub)), (queue) => fromQueue3(queue, {
    maxChunkSize
  }));
  return options?.shutdown ? ensuring9(stream, shutdown3(pubsub)) : stream;
}, "fromPubSub");
var fromTPubSub = /* @__PURE__ */ __name((pubsub) => {
  return unwrapScoped5(map26(subscribeScoped2(pubsub), (queue) => fromTQueue(queue)));
}, "fromTPubSub");
var fromIterable17 = /* @__PURE__ */ __name((iterable) => suspend10(() => isChunk(iterable) ? fromChunk(iterable) : fromIteratorSucceed(iterable[Symbol.iterator]())), "fromIterable");
var fromIterableEffect = /* @__PURE__ */ __name((effect4) => pipe(effect4, map26(fromIterable17), unwrap4), "fromIterableEffect");
var fromIteratorSucceed = /* @__PURE__ */ __name((iterator, maxChunkSize = DefaultChunkSize) => {
  return pipe(sync8(() => {
    let builder = [];
    const loop4 = /* @__PURE__ */ __name((iterator2) => pipe(sync8(() => {
      let next4 = iterator2.next();
      if (maxChunkSize === 1) {
        if (next4.done) {
          return void_8;
        }
        return pipe(write(of3(next4.value)), flatMap20(() => loop4(iterator2)));
      }
      builder = [];
      let count5 = 0;
      while (next4.done === false) {
        builder.push(next4.value);
        count5 = count5 + 1;
        if (count5 >= maxChunkSize) {
          break;
        }
        next4 = iterator2.next();
      }
      if (count5 > 0) {
        return pipe(write(unsafeFromArray(builder)), flatMap20(() => loop4(iterator2)));
      }
      return void_8;
    }), unwrap2), "loop");
    return new StreamImpl(loop4(iterator));
  }), unwrap4);
}, "fromIteratorSucceed");
var fromPull2 = /* @__PURE__ */ __name((effect4) => pipe(effect4, map26(repeatEffectChunkOption), unwrapScoped5), "fromPull");
var fromQueue3 = /* @__PURE__ */ __name((queue, options) => pipe(takeBetween2(queue, 1, options?.maxChunkSize ?? DefaultChunkSize), catchAllCause4((cause3) => pipe(isShutdown4(queue), flatMap18((isShutdown9) => isShutdown9 && isInterrupted3(cause3) ? end5() : failCause15(cause3)))), repeatEffectChunkOption, options?.shutdown ? ensuring9(shutdown4(queue)) : identity), "fromQueue");
var fromTQueue = /* @__PURE__ */ __name((queue) => pipe(take9(queue), map26(of3), catchAllCause4((cause3) => pipe(isShutdown8(queue), flatMap18((isShutdown9) => isShutdown9 && isInterrupted3(cause3) ? end5() : failCause15(cause3)))), repeatEffectChunkOption), "fromTQueue");
var fromSchedule = /* @__PURE__ */ __name((schedule4) => pipe(driver2(schedule4), map26((driver3) => repeatEffectOption(driver3.next(void 0))), unwrap4), "fromSchedule");
var fromReadableStream = /* @__PURE__ */ __name((...args2) => {
  const evaluate3 = args2.length === 1 ? args2[0].evaluate : args2[0];
  const onError6 = args2.length === 1 ? args2[0].onError : args2[1];
  const releaseLockOnEnd = args2.length === 1 ? args2[0].releaseLockOnEnd === true : false;
  return unwrapScoped5(map26(acquireRelease3(sync8(() => evaluate3().getReader()), (reader) => releaseLockOnEnd ? sync8(() => reader.releaseLock()) : promise3(() => reader.cancel())), (reader) => repeatEffectOption(flatMap18(tryPromise3({
    try: /* @__PURE__ */ __name(() => reader.read(), "try"),
    catch: /* @__PURE__ */ __name((reason) => some3(onError6(reason)), "catch")
  }), ({
    done: done12,
    value: value2
  }) => done12 ? fail12(none2()) : succeed13(value2)))));
}, "fromReadableStream");
var fromReadableStreamByob = /* @__PURE__ */ __name((...args2) => {
  const evaluate3 = args2.length === 1 ? args2[0].evaluate : args2[0];
  const onError6 = args2.length === 1 ? args2[0].onError : args2[1];
  const allocSize = (args2.length === 1 ? args2[0].bufferSize : args2[2]) ?? 4096;
  const releaseLockOnEnd = args2.length === 1 ? args2[0].releaseLockOnEnd === true : false;
  return unwrapScoped5(map26(acquireRelease3(sync8(() => evaluate3().getReader({
    mode: "byob"
  })), (reader) => releaseLockOnEnd ? sync8(() => reader.releaseLock()) : promise3(() => reader.cancel())), (reader) => catchAll8(forever6(readChunkStreamByobReader(reader, onError6, allocSize)), (error2) => error2 === EOF ? empty44 : fail20(error2))));
}, "fromReadableStreamByob");
var EOF = /* @__PURE__ */ Symbol.for("effect/Stream/EOF");
var readChunkStreamByobReader = /* @__PURE__ */ __name((reader, onError6, size25) => {
  const buffer4 = new ArrayBuffer(size25);
  return paginateEffect(0, (offset) => flatMap18(tryPromise3({
    try: /* @__PURE__ */ __name(() => reader.read(new Uint8Array(buffer4, offset, buffer4.byteLength - offset)), "try"),
    catch: /* @__PURE__ */ __name((reason) => onError6(reason), "catch")
  }), ({
    done: done12,
    value: value2
  }) => {
    if (done12) {
      return fail12(EOF);
    }
    const newOffset = offset + value2.byteLength;
    return succeed13([value2, newOffset >= buffer4.byteLength ? none2() : some3(newOffset)]);
  }));
}, "readChunkStreamByobReader");
var groupAdjacentBy = /* @__PURE__ */ dual(2, (self, f) => {
  const groupAdjacentByChunk = /* @__PURE__ */ __name((state, chunk4) => {
    if (isEmpty2(chunk4)) {
      return [state, empty7()];
    }
    const builder = [];
    let from = 0;
    let until = 0;
    let key = void 0;
    let previousChunk = empty7();
    switch (state._tag) {
      case "Some": {
        const tuple5 = state.value;
        key = tuple5[0];
        let loop4 = true;
        while (loop4 && until < chunk4.length) {
          const input = unsafeGet4(chunk4, until);
          const updatedKey = f(input);
          if (!equals(key, updatedKey)) {
            const previousChunk2 = tuple5[1];
            const additionalChunk = unsafeFromArray(Array.from(chunk4).slice(from, until));
            const group4 = appendAll3(previousChunk2, additionalChunk);
            builder.push([key, group4]);
            key = updatedKey;
            from = until;
            loop4 = false;
          }
          until = until + 1;
        }
        if (loop4) {
          previousChunk = tuple5[1];
        }
        break;
      }
      case "None": {
        key = f(unsafeGet4(chunk4, until));
        until = until + 1;
        break;
      }
    }
    while (until < chunk4.length) {
      const input = unsafeGet4(chunk4, until);
      const updatedKey = f(input);
      if (!equals(key, updatedKey)) {
        builder.push([key, unsafeFromArray(Array.from(chunk4).slice(from, until))]);
        key = updatedKey;
        from = until;
      }
      until = until + 1;
    }
    const nonEmptyChunk = appendAll3(previousChunk, unsafeFromArray(Array.from(chunk4).slice(from, until)));
    const output = unsafeFromArray(builder);
    return [some3([key, nonEmptyChunk]), output];
  }, "groupAdjacentByChunk");
  const groupAdjacent = /* @__PURE__ */ __name((state) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => {
      const [updatedState, output] = groupAdjacentByChunk(state, input);
      return isEmpty2(output) ? groupAdjacent(updatedState) : flatMap20(write(output), () => groupAdjacent(updatedState));
    }, "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => match2(state, {
      onNone: /* @__PURE__ */ __name(() => failCause12(cause3), "onNone"),
      onSome: /* @__PURE__ */ __name((output) => flatMap20(write(of3(output)), () => failCause12(cause3)), "onSome")
    }), "onFailure"),
    onDone: /* @__PURE__ */ __name((done12) => match2(state, {
      onNone: /* @__PURE__ */ __name(() => succeedNow(done12), "onNone"),
      onSome: /* @__PURE__ */ __name((output) => flatMap20(write(of3(output)), () => succeedNow(done12)), "onSome")
    }), "onDone")
  }), "groupAdjacent");
  return new StreamImpl(pipeToOrFail(toChannel2(self), groupAdjacent(none2())));
});
var grouped = /* @__PURE__ */ dual(2, (self, chunkSize) => pipe(self, rechunk(chunkSize), chunks));
var groupedWithin = /* @__PURE__ */ dual(3, (self, chunkSize, duration3) => aggregateWithin(self, collectAllN(chunkSize), spaced2(duration3)));
var haltWhen = /* @__PURE__ */ dual(2, (self, effect4) => {
  const writer = /* @__PURE__ */ __name((fiber) => pipe(poll4(fiber), map26(match2({
    onNone: /* @__PURE__ */ __name(() => readWith({
      onInput: /* @__PURE__ */ __name((input) => flatMap20(write(input), () => writer(fiber)), "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
    }), "onNone"),
    onSome: match7({
      onFailure: failCause12,
      onSuccess: /* @__PURE__ */ __name(() => void_8, "onSuccess")
    })
  })), unwrap2), "writer");
  return new StreamImpl(unwrapScopedWith((scope6) => effect4.pipe(forkIn3(scope6), map26((fiber) => toChannel2(self).pipe(pipeTo(writer(fiber)))))));
});
var haltAfter = /* @__PURE__ */ dual(2, (self, duration3) => pipe(self, haltWhen(sleep2(duration3))));
var haltWhenDeferred = /* @__PURE__ */ dual(2, (self, deferred) => {
  const writer = pipe(poll(deferred), map26(match2({
    onNone: /* @__PURE__ */ __name(() => readWith({
      onInput: /* @__PURE__ */ __name((input) => pipe(write(input), flatMap20(() => writer)), "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
    }), "onNone"),
    onSome: /* @__PURE__ */ __name((effect4) => unwrap2(match20(effect4, {
      onFailure: fail14,
      onSuccess: /* @__PURE__ */ __name(() => void_8, "onSuccess")
    })), "onSome")
  })), unwrap2);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var identityStream = /* @__PURE__ */ __name(() => new StreamImpl(identityChannel()), "identityStream");
var interleave = /* @__PURE__ */ dual(2, (self, that) => pipe(self, interleaveWith(that, forever6(make70(true, false)))));
var interleaveWith = /* @__PURE__ */ dual(3, (self, that, decider) => {
  const producer = /* @__PURE__ */ __name((handoff) => readWithCause({
    onInput: /* @__PURE__ */ __name((value2) => flatMap20(fromEffect5(offer6(handoff, of7(value2))), () => producer(handoff)), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer6(handoff, failCause14(cause3))), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => fromEffect5(offer6(handoff, end4)), "onDone")
  }), "producer");
  return new StreamImpl(unwrapScopedWith((scope6) => pipe(make68(), zip12(make68()), tap5(([left3]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left3)), runIn(scope6), forkIn3(scope6))), tap5(([_, right3]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right3)), runIn(scope6), forkIn3(scope6))), map26(([left3, right3]) => {
    const process2 = /* @__PURE__ */ __name((leftDone, rightDone) => readWithCause({
      onInput: /* @__PURE__ */ __name((bool) => {
        if (bool && !leftDone) {
          return pipe(fromEffect5(take10(left3)), flatMap20(match32({
            onEnd: /* @__PURE__ */ __name(() => rightDone ? void_8 : process2(true, rightDone), "onEnd"),
            onFailure: failCause12,
            onSuccess: /* @__PURE__ */ __name((chunk4) => pipe(write(chunk4), flatMap20(() => process2(leftDone, rightDone))), "onSuccess")
          })));
        }
        if (!bool && !rightDone) {
          return pipe(fromEffect5(take10(right3)), flatMap20(match32({
            onEnd: /* @__PURE__ */ __name(() => leftDone ? void_8 : process2(leftDone, true), "onEnd"),
            onFailure: failCause12,
            onSuccess: /* @__PURE__ */ __name((chunk4) => pipe(write(chunk4), flatMap20(() => process2(leftDone, rightDone))), "onSuccess")
          })));
        }
        return process2(leftDone, rightDone);
      }, "onInput"),
      onFailure: failCause12,
      onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
    }), "process");
    return pipe(toChannel2(decider), concatMap(writeChunk), pipeTo(process2(false, false)));
  }))));
});
var intersperse3 = /* @__PURE__ */ dual(2, (self, element) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(suspend6(() => {
  const writer = /* @__PURE__ */ __name((isFirst) => readWithCause({
    onInput: /* @__PURE__ */ __name((chunk4) => {
      const builder = [];
      let flagResult = isFirst;
      for (const output of chunk4) {
        if (flagResult) {
          flagResult = false;
          builder.push(output);
        } else {
          builder.push(element);
          builder.push(output);
        }
      }
      return pipe(write(unsafeFromArray(builder)), flatMap20(() => writer(flagResult)));
    }, "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "writer");
  return writer(true);
})))));
var intersperseAffixes = /* @__PURE__ */ dual(2, (self, {
  end: end6,
  middle,
  start: start3
}) => pipe(make70(start3), concat3(pipe(self, intersperse3(middle))), concat3(make70(end6))));
var interruptAfter = /* @__PURE__ */ dual(2, (self, duration3) => pipe(self, interruptWhen2(sleep2(duration3))));
var interruptWhen2 = /* @__PURE__ */ dual(2, (self, effect4) => new StreamImpl(pipe(toChannel2(self), interruptWhen(effect4))));
var interruptWhenDeferred2 = /* @__PURE__ */ dual(2, (self, deferred) => new StreamImpl(pipe(toChannel2(self), interruptWhenDeferred(deferred))));
var iterate4 = /* @__PURE__ */ __name((value2, next4) => unfold5(value2, (a) => some3([a, next4(a)])), "iterate");
var make70 = /* @__PURE__ */ __name((...as15) => fromIterable17(as15), "make");
var map33 = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(map7(f)))));
var mapAccum5 = /* @__PURE__ */ dual(3, (self, s, f) => {
  const accumulator = /* @__PURE__ */ __name((s2) => readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const [nextS, chunk4] = mapAccum2(input, s2, f);
      return flatMap20(write(chunk4), () => accumulator(nextS));
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "accumulator");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(accumulator(s))));
});
var mapAccumEffect = /* @__PURE__ */ dual(3, (self, s, f) => suspend10(() => {
  const accumulator = /* @__PURE__ */ __name((s2) => readWith({
    onInput: /* @__PURE__ */ __name((input) => pipe(suspend4(() => {
      const outputs = [];
      const emit4 = /* @__PURE__ */ __name((output) => sync8(() => {
        outputs.push(output);
      }), "emit");
      return pipe(input, reduce17(s2, (s3, a) => pipe(f(s3, a), flatMap18(([s4, a2]) => pipe(emit4(a2), as8(s4))))), match20({
        onFailure: /* @__PURE__ */ __name((error2) => {
          if (outputs.length !== 0) {
            return zipRight8(write(unsafeFromArray(outputs)), fail14(error2));
          }
          return fail14(error2);
        }, "onFailure"),
        onSuccess: /* @__PURE__ */ __name((s3) => flatMap20(write(unsafeFromArray(outputs)), () => accumulator(s3)), "onSuccess")
      }));
    }), unwrap2), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "accumulator");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(accumulator(s))));
}));
var mapBoth9 = /* @__PURE__ */ dual(2, (self, options) => pipe(self, mapError10(options.onFailure), map33(options.onSuccess)));
var mapChunks = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(f))));
var mapChunksEffect = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOutEffect(f))));
var mapConcat = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapConcatChunk((a) => fromIterable3(f(a)))));
var mapConcatChunk = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapChunks(flatMap5(f))));
var mapConcatChunkEffect = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapEffectSequential(f), mapConcatChunk(identity)));
var mapConcatEffect = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapEffectSequential((a) => pipe(f(a), map26(fromIterable3))), mapConcatChunk(identity)));
var mapEffectSequential = /* @__PURE__ */ dual(2, (self, f) => {
  const loop4 = /* @__PURE__ */ __name((iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((elem) => loop4(elem[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeed15
      });
    } else {
      const value2 = next4.value;
      return unwrap2(map26(f(value2), (a2) => flatMap20(write(of3(a2)), () => loop4(iterator))));
    }
  }, "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(suspend6(() => loop4(empty7()[Symbol.iterator]())))));
});
var mapEffectPar = /* @__PURE__ */ dual(3, (self, n, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(f, n), mapOut(of3))));
var mapError10 = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapError7(f))));
var mapErrorCause6 = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapErrorCause5(f))));
var merge10 = /* @__PURE__ */ dual((args2) => isStream(args2[1]), (self, that, options) => mergeWith3(self, that, {
  onSelf: identity,
  onOther: identity,
  haltStrategy: options?.haltStrategy
}));
var mergeAll9 = /* @__PURE__ */ dual((args2) => Symbol.iterator in args2[0], (streams, options) => flatten16(fromIterable17(streams), options));
var mergeWithTag = /* @__PURE__ */ dual(2, (streams, options) => {
  const keys7 = Object.keys(streams);
  const values10 = keys7.map((key) => streams[key].pipe(map33((value2) => ({
    _tag: key,
    value: value2
  }))));
  return mergeAll9(values10, options);
});
var mergeEither = /* @__PURE__ */ dual(2, (self, that) => mergeWith3(self, that, {
  onSelf: left2,
  onOther: right2
}));
var mergeLeft = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, merge10(drain3(right3))));
var mergeRight = /* @__PURE__ */ dual(2, (left3, right3) => pipe(drain3(left3), merge10(right3)));
var mergeWith3 = /* @__PURE__ */ dual(3, (self, other, options) => {
  const strategy = options.haltStrategy ? fromInput2(options.haltStrategy) : Both2;
  const handler = /* @__PURE__ */ __name((terminate) => (exit4) => terminate || !isSuccess(exit4) ? (
    // TODO: remove
    Done4(suspend4(() => exit4))
  ) : Await2((exit5) => suspend4(() => exit5)), "handler");
  return new StreamImpl(mergeWith2(toChannel2(map33(self, options.onSelf)), {
    other: toChannel2(map33(other, options.onOther)),
    onSelfDone: handler(strategy._tag === "Either" || strategy._tag === "Left"),
    onOtherDone: handler(strategy._tag === "Either" || strategy._tag === "Right")
  }));
});
var mkString2 = /* @__PURE__ */ __name((self) => run4(self, mkString), "mkString");
var never8 = /* @__PURE__ */ fromEffect9(never5);
var onEnd = /* @__PURE__ */ dual(2, (self, effect4) => concat3(self, drain3(fromEffect9(effect4))));
var onError4 = /* @__PURE__ */ dual(2, (self, cleanup) => pipe(self, catchAllCause7((cause3) => fromEffect9(pipe(cleanup(cause3), zipRight7(failCause10(cause3)))))));
var onDone = /* @__PURE__ */ dual(2, (self, cleanup) => new StreamImpl(pipe(toChannel2(self), ensuringWith((exit4) => isSuccess(exit4) ? cleanup() : _void))));
var onStart = /* @__PURE__ */ dual(2, (self, effect4) => unwrap4(as8(effect4, self)));
var orDie8 = /* @__PURE__ */ __name((self) => pipe(self, orDieWith5(identity)), "orDie");
var orDieWith5 = /* @__PURE__ */ dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), orDieWith3(f))));
var orElse13 = /* @__PURE__ */ dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), orElse10(() => toChannel2(that())))));
var orElseEither7 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, map33(left2), orElse13(() => pipe(that(), map33(right2)))));
var orElseFail4 = /* @__PURE__ */ dual(2, (self, error2) => pipe(self, orElse13(() => failSync10(error2))));
var orElseIfEmpty = /* @__PURE__ */ dual(2, (self, element) => pipe(self, orElseIfEmptyChunk(() => of3(element()))));
var orElseIfEmptyChunk = /* @__PURE__ */ dual(2, (self, chunk4) => pipe(self, orElseIfEmptyStream(() => new StreamImpl(write(chunk4())))));
var orElseIfEmptyStream = /* @__PURE__ */ dual(2, (self, stream) => {
  const writer = readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      if (isEmpty2(input)) {
        return suspend6(() => writer);
      }
      return pipe(write(input), zipRight8(identityChannel()));
    }, "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => suspend6(() => toChannel2(stream())), "onDone")
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var orElseSucceed5 = /* @__PURE__ */ dual(2, (self, value2) => pipe(self, orElse13(() => sync14(value2))));
var paginate = /* @__PURE__ */ __name((s, f) => paginateChunk(s, (s2) => {
  const page = f(s2);
  return [of3(page[0]), page[1]];
}), "paginate");
var paginateChunk = /* @__PURE__ */ __name((s, f) => {
  const loop4 = /* @__PURE__ */ __name((s2) => {
    const page = f(s2);
    return match2(page[1], {
      onNone: /* @__PURE__ */ __name(() => zipRight8(write(page[0]), void_8), "onNone"),
      onSome: /* @__PURE__ */ __name((s3) => flatMap20(write(page[0]), () => loop4(s3)), "onSome")
    });
  }, "loop");
  return new StreamImpl(suspend6(() => loop4(s)));
}, "paginateChunk");
var paginateChunkEffect = /* @__PURE__ */ __name((s, f) => {
  const loop4 = /* @__PURE__ */ __name((s2) => unwrap2(map26(f(s2), ([chunk4, option5]) => match2(option5, {
    onNone: /* @__PURE__ */ __name(() => zipRight8(write(chunk4), void_8), "onNone"),
    onSome: /* @__PURE__ */ __name((s3) => flatMap20(write(chunk4), () => loop4(s3)), "onSome")
  }))), "loop");
  return new StreamImpl(suspend6(() => loop4(s)));
}, "paginateChunkEffect");
var paginateEffect = /* @__PURE__ */ __name((s, f) => paginateChunkEffect(s, (s2) => pipe(f(s2), map26(([a, s3]) => [of3(a), s3]))), "paginateEffect");
var peel = /* @__PURE__ */ dual(2, (self, sink) => {
  const OP_EMIT5 = "Emit";
  const OP_HALT2 = "Halt";
  const OP_END2 = "End";
  return pipe(make22(), flatMap18((deferred) => pipe(make68(), map26((handoff) => {
    const consumer = foldSink(collectLeftover(sink), {
      onFailure: /* @__PURE__ */ __name((error2) => zipRight9(fromEffect7(fail3(deferred, error2)), fail15(error2)), "onFailure"),
      onSuccess: /* @__PURE__ */ __name(([z, leftovers]) => {
        const loop4 = readWithCause({
          onInput: /* @__PURE__ */ __name((elements) => flatMap20(fromEffect5(offer6(handoff, {
            _tag: OP_EMIT5,
            elements
          })), () => loop4), "onInput"),
          onFailure: /* @__PURE__ */ __name((cause3) => zipRight8(fromEffect5(offer6(handoff, {
            _tag: OP_HALT2,
            cause: cause3
          })), failCause12(cause3)), "onFailure"),
          onDone: /* @__PURE__ */ __name((_) => zipRight8(fromEffect5(offer6(handoff, {
            _tag: OP_END2
          })), void_8), "onDone")
        });
        return fromChannel(pipe(fromEffect5(succeed2(deferred, z)), zipRight8(fromEffect5(pipe(handoff, offer6({
          _tag: OP_EMIT5,
          elements: leftovers
        })))), zipRight8(loop4)));
      }, "onSuccess")
    });
    const producer = pipe(take10(handoff), map26((signal) => {
      switch (signal._tag) {
        case OP_EMIT5: {
          return pipe(write(signal.elements), flatMap20(() => producer));
        }
        case OP_HALT2: {
          return failCause12(signal.cause);
        }
        case OP_END2: {
          return void_8;
        }
      }
    }), unwrap2);
    return pipe(self, tapErrorCause6((cause3) => failCause2(deferred, cause3)), run4(consumer), forkScoped3, zipRight7(_await(deferred)), map26((z) => [z, new StreamImpl(producer)]));
  }))), flatten12);
});
var partition11 = /* @__PURE__ */ dual((args2) => typeof args2[1] === "function", (self, predicate, options) => partitionEither(self, (a) => succeed13(predicate(a) ? right2(a) : left2(a)), options));
var partitionEither = /* @__PURE__ */ dual((args2) => typeof args2[1] === "function", (self, predicate, options) => pipe(mapEffectSequential(self, predicate), distributedWith({
  size: 2,
  maximumLag: options?.bufferSize ?? 16,
  decide: match({
    onLeft: /* @__PURE__ */ __name(() => succeed13((n) => n === 0), "onLeft"),
    onRight: /* @__PURE__ */ __name(() => succeed13((n) => n === 1), "onRight")
  })
}), flatMap18(([queue1, queue2]) => succeed13([filterMap12(flattenExitOption(fromQueue3(queue1, {
  shutdown: true
})), (_) => match(_, {
  onLeft: some3,
  onRight: none2
})), filterMap12(flattenExitOption(fromQueue3(queue2, {
  shutdown: true
})), (_) => match(_, {
  onLeft: none2,
  onRight: some3
}))]))));
var pipeThrough = /* @__PURE__ */ dual(2, (self, sink) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(toChannel(sink)))));
var pipeThroughChannel = /* @__PURE__ */ dual(2, (self, channel) => new StreamImpl(pipeTo(toChannel2(self), channel)));
var pipeThroughChannelOrFail = /* @__PURE__ */ dual(2, (self, chan) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(chan))));
var prepend6 = /* @__PURE__ */ dual(2, (self, values10) => new StreamImpl(zipRight8(write(values10), toChannel2(self))));
var provideContext8 = /* @__PURE__ */ dual(2, (self, context11) => new StreamImpl(pipe(toChannel2(self), provideContext4(context11))));
var provideSomeContext3 = /* @__PURE__ */ dual(2, (self, context11) => mapInputContext7(self, merge3(context11)));
var provideLayer2 = /* @__PURE__ */ dual(2, (self, layer) => new StreamImpl(unwrapScopedWith((scope6) => buildWithScope2(layer, scope6).pipe(map26((env) => pipe(toChannel2(self), provideContext4(env)))))));
var provideService10 = /* @__PURE__ */ dual(3, (self, tag, resource) => provideServiceEffect4(self, tag, succeed13(resource)));
var provideServiceEffect4 = /* @__PURE__ */ dual(3, (self, tag, effect4) => provideServiceStream(self, tag, fromEffect9(effect4)));
var provideServiceStream = /* @__PURE__ */ dual(3, (self, tag, stream) => contextWithStream((env) => flatMap23(stream, (service6) => pipe(self, provideContext8(add2(env, tag, service6))))));
var mapInputContext7 = /* @__PURE__ */ dual(2, (self, f) => contextWithStream((env) => pipe(self, provideContext8(f(env)))));
var provideSomeLayer3 = /* @__PURE__ */ dual(2, (self, layer) => (
  // @ts-expect-error
  // @effect-diagnostics-next-line missingEffectContext:off
  pipe(self, provideLayer2(pipe(context5(), merge8(layer))))
));
var range4 = /* @__PURE__ */ __name((min8, max10, chunkSize = DefaultChunkSize) => suspend10(() => {
  if (min8 > max10) {
    return empty44;
  }
  const go = /* @__PURE__ */ __name((min9, max11, chunkSize2) => {
    const remaining = max11 - min9 + 1;
    if (remaining > chunkSize2) {
      return pipe(write(range3(min9, min9 + chunkSize2 - 1)), flatMap20(() => go(min9 + chunkSize2, max11, chunkSize2)));
    }
    return write(range3(min9, min9 + remaining - 1));
  }, "go");
  return new StreamImpl(go(min8, max10, chunkSize));
}), "range");
var race5 = /* @__PURE__ */ dual(2, (left3, right3) => raceAll4(left3, right3));
var raceAll4 = /* @__PURE__ */ __name((...streams) => make22().pipe(map26((halt2) => {
  let winner = null;
  return mergeAll9(streams.map((stream, index) => stream.pipe(takeWhile6(() => {
    if (winner === null) {
      winner = index;
      unsafeDone(halt2, void_4);
      return true;
    }
    return winner === index;
  }), interruptWhen2(_await(halt2).pipe(flatMap18(() => winner === index ? never5 : _void))))), {
    concurrency: streams.length
  });
}), unwrap4), "raceAll");
var rechunk = /* @__PURE__ */ dual(2, (self, n) => suspend10(() => {
  const target = Math.max(n, 1);
  const process2 = rechunkProcess(new StreamRechunker(target), target);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process2)));
}));
var rechunkProcess = /* @__PURE__ */ __name((rechunker, target) => readWithCause({
  onInput: /* @__PURE__ */ __name((chunk4) => {
    if (chunk4.length === target && rechunker.isEmpty()) {
      return flatMap20(write(chunk4), () => rechunkProcess(rechunker, target));
    }
    if (chunk4.length > 0) {
      const chunks3 = [];
      let result = void 0;
      let index = 0;
      while (index < chunk4.length) {
        while (index < chunk4.length && result === void 0) {
          result = rechunker.write(pipe(chunk4, unsafeGet4(index)));
          index = index + 1;
        }
        if (result !== void 0) {
          chunks3.push(result);
          result = void 0;
        }
      }
      return flatMap20(writeAll(...chunks3), () => rechunkProcess(rechunker, target));
    }
    return suspend6(() => rechunkProcess(rechunker, target));
  }, "onInput"),
  onFailure: /* @__PURE__ */ __name((cause3) => zipRight8(rechunker.emitIfNotEmpty(), failCause12(cause3)), "onFailure"),
  onDone: /* @__PURE__ */ __name(() => rechunker.emitIfNotEmpty(), "onDone")
}), "rechunkProcess");
var StreamRechunker = class {
  static {
    __name(this, "StreamRechunker");
  }
  n;
  builder = [];
  pos = 0;
  constructor(n) {
    this.n = n;
  }
  isEmpty() {
    return this.pos === 0;
  }
  write(elem) {
    this.builder.push(elem);
    this.pos += 1;
    if (this.pos === this.n) {
      const result = unsafeFromArray(this.builder);
      this.builder = [];
      this.pos = 0;
      return result;
    }
    return void 0;
  }
  emitIfNotEmpty() {
    if (this.pos !== 0) {
      return write(unsafeFromArray(this.builder));
    }
    return void_8;
  }
};
var refineOrDie3 = /* @__PURE__ */ dual(2, (self, pf) => pipe(self, refineOrDieWith3(pf, identity)));
var refineOrDieWith3 = /* @__PURE__ */ dual(3, (self, pf, f) => new StreamImpl(catchAll6(toChannel2(self), (error2) => match2(pf(error2), {
  onNone: /* @__PURE__ */ __name(() => failCause12(die6(f(error2))), "onNone"),
  onSome: fail14
}))));
var repeat4 = /* @__PURE__ */ dual(2, (self, schedule4) => filterMap12(repeatEither(self, schedule4), (_) => match(_, {
  onLeft: none2,
  onRight: some3
})));
var repeatEffect = /* @__PURE__ */ __name((effect4) => repeatEffectOption(pipe(effect4, mapError5(some3))), "repeatEffect");
var repeatEffectChunk = /* @__PURE__ */ __name((effect4) => repeatEffectChunkOption(pipe(effect4, mapError5(some3))), "repeatEffectChunk");
var repeatEffectChunkOption = /* @__PURE__ */ __name((effect4) => unfoldChunkEffect(effect4, (effect5) => pipe(map26(effect5, (chunk4) => some3([chunk4, effect5])), catchAll4(match2({
  onNone: /* @__PURE__ */ __name(() => succeed13(none2()), "onNone"),
  onSome: fail12
})))), "repeatEffectChunkOption");
var repeatEffectOption = /* @__PURE__ */ __name((effect4) => repeatEffectChunkOption(pipe(effect4, map26(of3))), "repeatEffectOption");
var repeatEither = /* @__PURE__ */ dual(2, (self, schedule4) => repeatWith(self, schedule4, {
  onElement: /* @__PURE__ */ __name((a) => right2(a), "onElement"),
  onSchedule: left2
}));
var repeatElements = /* @__PURE__ */ dual(2, (self, schedule4) => filterMap12(repeatElementsWith(self, schedule4, {
  onElement: /* @__PURE__ */ __name((a) => some3(a), "onElement"),
  onSchedule: none2
}), identity));
var repeatElementsWith = /* @__PURE__ */ dual(3, (self, schedule4, options) => {
  const driver3 = pipe(driver2(schedule4), map26((driver4) => {
    const feed = /* @__PURE__ */ __name((input) => match2(head3(input), {
      onNone: /* @__PURE__ */ __name(() => loop4, "onNone"),
      onSome: /* @__PURE__ */ __name((a) => zipRight8(write(of3(options.onElement(a))), step4(pipe(input, drop3(1)), a)), "onSome")
    }), "feed");
    const step4 = /* @__PURE__ */ __name((input, a) => {
      const advance = pipe(driver4.next(a), as8(pipe(write(of3(options.onElement(a))), flatMap20(() => step4(input, a)))));
      const reset3 = pipe(driver4.last, orDie4, flatMap18((b) => pipe(driver4.reset, map26(() => pipe(write(of3(options.onSchedule(b))), zipRight8(feed(input)))))));
      return pipe(advance, orElse8(() => reset3), unwrap2);
    }, "step");
    const loop4 = readWith({
      onInput: feed,
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
    });
    return loop4;
  }), unwrap2);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(driver3)));
});
var repeatValue = /* @__PURE__ */ __name((value2) => new StreamImpl(repeated(write(of3(value2)))), "repeatValue");
var repeatWith = /* @__PURE__ */ dual(3, (self, schedule4, options) => {
  return pipe(driver2(schedule4), map26((driver3) => {
    const provideLastIterationInfo = provideServiceEffect4(CurrentIterationMetadata2, get13(driver3.iterationMeta));
    const process2 = pipe(self, provideLastIterationInfo, map33(options.onElement), toChannel2);
    const loop4 = unwrap2(match20(driver3.next(void 0), {
      onFailure: /* @__PURE__ */ __name(() => void_8, "onFailure"),
      onSuccess: /* @__PURE__ */ __name((output) => flatMap20(process2, () => zipRight8(write(of3(options.onSchedule(output))), loop4)), "onSuccess")
    }));
    return new StreamImpl(zipRight8(process2, loop4));
  }), unwrap4);
});
var repeatWithSchedule = /* @__PURE__ */ __name((value2, schedule4) => repeatEffectWithSchedule(succeed13(value2), schedule4), "repeatWithSchedule");
var repeatEffectWithSchedule = /* @__PURE__ */ __name((effect4, schedule4) => flatMap23(fromEffect9(zip12(effect4, driver2(schedule4))), ([a, driver3]) => {
  const provideLastIterationInfo = provideServiceEffect3(CurrentIterationMetadata2, get13(driver3.iterationMeta));
  return concat3(succeed20(a), unfoldEffect(a, (s) => matchEffect4(driver3.next(s), {
    onFailure: succeed13,
    onSuccess: /* @__PURE__ */ __name(() => map26(provideLastIterationInfo(effect4), (nextA) => some3([nextA, nextA])), "onSuccess")
  })));
}), "repeatEffectWithSchedule");
var retry7 = /* @__PURE__ */ dual(2, (self, policy) => driver2(policy).pipe(map26((driver3) => {
  const provideLastIterationInfo = provideServiceEffect4(CurrentIterationMetadata2, get13(driver3.iterationMeta));
  const loop4 = toChannel2(provideLastIterationInfo(self)).pipe(mapOutEffect((out) => as8(driver3.reset, out)), catchAll6((error2) => driver3.next(error2).pipe(match20({
    onFailure: /* @__PURE__ */ __name(() => fail14(error2), "onFailure"),
    onSuccess: /* @__PURE__ */ __name(() => loop4, "onSuccess")
  }), unwrap2)));
  return loop4;
}), unwrap2, fromChannel2));
var withExecutionPlan3 = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, policy, options) => suspend10(() => {
  const preventFallbackOnPartialStream = options?.preventFallbackOnPartialStream ?? false;
  let i = 0;
  let lastError = none2();
  const loop4 = suspend10(() => {
    const step4 = policy.steps[i++];
    if (!step4) {
      return fail20(getOrThrow2(lastError));
    }
    let nextStream = isContext2(step4.provide) ? provideSomeContext3(self, step4.provide) : provideSomeLayer3(self, step4.provide);
    let receivedElements = false;
    if (isSome2(lastError)) {
      const error2 = lastError.value;
      let attempted = false;
      const wrapped = nextStream;
      nextStream = suspend10(() => {
        if (attempted) return wrapped;
        attempted = true;
        return fail20(error2);
      });
      nextStream = scheduleDefectRefail2(retry7(nextStream, scheduleFromStep(step4, false)));
    } else {
      const schedule4 = scheduleFromStep(step4, true);
      nextStream = schedule4 ? scheduleDefectRefail2(retry7(nextStream, schedule4)) : nextStream;
    }
    return catchAll8(preventFallbackOnPartialStream ? mapChunks(nextStream, (chunk4) => {
      receivedElements = true;
      return chunk4;
    }) : nextStream, (error2) => {
      if (preventFallbackOnPartialStream && receivedElements) {
        return fail20(error2);
      }
      lastError = some3(error2);
      return loop4;
    });
  });
  return loop4;
}));
var scheduleDefectRefail2 = /* @__PURE__ */ __name((self) => catchAllCause7(self, (cause3) => failCause16(scheduleDefectRefailCause(cause3))), "scheduleDefectRefail");
var run4 = /* @__PURE__ */ dual(2, (self, sink) => toChannel2(self).pipe(pipeToOrFail(toChannel(sink)), runDrain));
var runCollect2 = /* @__PURE__ */ __name((self) => run4(self, collectAll()), "runCollect");
var runCount = /* @__PURE__ */ __name((self) => run4(self, count3), "runCount");
var runDrain2 = /* @__PURE__ */ __name((self) => run4(self, drain2), "runDrain");
var runFold = /* @__PURE__ */ dual(3, (self, s, f) => runFoldWhile(self, s, constTrue, f));
var runFoldEffect = /* @__PURE__ */ dual(3, (self, s, f) => runFoldWhileEffect(self, s, constTrue, f));
var runFoldScoped = /* @__PURE__ */ dual(3, (self, s, f) => pipe(self, runFoldWhileScoped(s, constTrue, f)));
var runFoldScopedEffect = /* @__PURE__ */ dual(3, (self, s, f) => pipe(self, runFoldWhileScopedEffect(s, constTrue, f)));
var runFoldWhile = /* @__PURE__ */ dual(4, (self, s, cont, f) => run4(self, fold(s, cont, f)));
var runFoldWhileEffect = /* @__PURE__ */ dual(4, (self, s, cont, f) => run4(self, foldEffect(s, cont, f)));
var runFoldWhileScoped = /* @__PURE__ */ dual(4, (self, s, cont, f) => pipe(self, runScoped2(fold(s, cont, f))));
var runFoldWhileScopedEffect = /* @__PURE__ */ dual(4, (self, s, cont, f) => pipe(self, runScoped2(foldEffect(s, cont, f))));
var runForEach = /* @__PURE__ */ dual(2, (self, f) => run4(self, forEach17(f)));
var runForEachChunk = /* @__PURE__ */ dual(2, (self, f) => run4(self, forEachChunk(f)));
var runForEachChunkScoped = /* @__PURE__ */ dual(2, (self, f) => pipe(self, runScoped2(forEachChunk(f))));
var runForEachScoped = /* @__PURE__ */ dual(2, (self, f) => pipe(self, runScoped2(forEach17(f))));
var runForEachWhile = /* @__PURE__ */ dual(2, (self, f) => run4(self, forEachWhile(f)));
var runForEachWhileScoped = /* @__PURE__ */ dual(2, (self, f) => pipe(self, runScoped2(forEachWhile(f))));
var runHead = /* @__PURE__ */ __name((self) => run4(self, head8()), "runHead");
var runIntoPubSub = /* @__PURE__ */ dual(2, (self, pubsub) => pipe(self, runIntoQueue(pubsub)));
var runIntoPubSubScoped = /* @__PURE__ */ dual(2, (self, pubsub) => pipe(self, runIntoQueueScoped(pubsub)));
var runIntoQueue = /* @__PURE__ */ dual(2, (self, queue) => pipe(self, runIntoQueueScoped(queue), scoped4));
var runIntoQueueElementsScoped = /* @__PURE__ */ dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => flatMap20(fromEffect5(offerAll3(queue, map7(input, succeed3))), () => writer), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer3(queue, failCause3(map22(cause3, some3)))), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => fromEffect5(offer3(queue, fail4(none2()))), "onDone")
  });
  return pipe(pipeTo(toChannel2(self), writer), drain, runScoped, asVoid6);
});
var runIntoQueueScoped = /* @__PURE__ */ dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: /* @__PURE__ */ __name((input) => flatMap20(write(chunk3(input)), () => writer), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => write(failCause14(cause3)), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => write(end4), "onDone")
  });
  return pipe(pipeTo(toChannel2(self), writer), mapOutEffect((take13) => offer3(queue, take13)), drain, runScoped, asVoid6);
});
var runLast = /* @__PURE__ */ __name((self) => run4(self, last6()), "runLast");
var runScoped2 = /* @__PURE__ */ dual(2, (self, sink) => pipe(toChannel2(self), pipeToOrFail(toChannel(sink)), drain, runScoped));
var runSum = /* @__PURE__ */ __name((self) => run4(self, sum3), "runSum");
var scan3 = /* @__PURE__ */ dual(3, (self, s, f) => pipe(self, scanEffect(s, (s2, a) => succeed13(f(s2, a)))));
var scanReduce = /* @__PURE__ */ dual(2, (self, f) => pipe(self, scanReduceEffect((a2, a) => succeed13(f(a2, a)))));
var scanReduceEffect = /* @__PURE__ */ dual(2, (self, f) => pipe(self, mapAccumEffect(none2(), (option5, a) => {
  switch (option5._tag) {
    case "None": {
      return succeed13([some3(a), a]);
    }
    case "Some": {
      return pipe(f(option5.value, a), map26((b) => [some3(b), b]));
    }
  }
})));
var schedule2 = /* @__PURE__ */ dual(2, (self, schedule4) => filterMap12(scheduleWith(self, schedule4, {
  onElement: some3,
  onSchedule: none2
}), identity));
var scheduleWith = /* @__PURE__ */ dual(3, (self, schedule4, options) => {
  const loop4 = /* @__PURE__ */ __name((driver3, iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((chunk4) => loop4(driver3, chunk4[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeedNow
      });
    }
    return unwrap2(matchEffect4(driver3.next(next4.value), {
      onFailure: /* @__PURE__ */ __name(() => pipe(driver3.last, orDie4, map26((b) => pipe(write(make7(options.onElement(next4.value), options.onSchedule(b))), flatMap20(() => loop4(driver3, iterator)))), zipLeft7(driver3.reset)), "onFailure"),
      onSuccess: /* @__PURE__ */ __name(() => succeed13(pipe(write(of3(options.onElement(next4.value))), flatMap20(() => loop4(driver3, iterator)))), "onSuccess")
    }));
  }, "loop");
  return new StreamImpl(pipe(fromEffect5(driver2(schedule4)), flatMap20((driver3) => pipe(toChannel2(self), pipeTo(loop4(driver3, empty7()[Symbol.iterator]()))))));
});
var scanEffect = /* @__PURE__ */ dual(3, (self, s, f) => new StreamImpl(pipe(write(of3(s)), flatMap20(() => toChannel2(pipe(self, mapAccumEffect(s, (s2, a) => pipe(f(s2, a), map26((s3) => [s3, s3])))))))));
var scoped7 = /* @__PURE__ */ __name((effect4) => new StreamImpl(ensuring5(scoped6(pipe(effect4, map26(of3))), _void)), "scoped");
var scopedWith4 = /* @__PURE__ */ __name((f) => new StreamImpl(scopedWith3((scope6) => f(scope6).pipe(map26(of3)))), "scopedWith");
var some17 = /* @__PURE__ */ __name((self) => pipe(self, mapError10(some3), someOrFail(() => none2())), "some");
var someOrElse = /* @__PURE__ */ dual(2, (self, fallback) => pipe(self, map33(getOrElse2(fallback))));
var someOrFail = /* @__PURE__ */ dual(2, (self, error2) => mapEffectSequential(self, match2({
  onNone: /* @__PURE__ */ __name(() => failSync5(error2), "onNone"),
  onSome: succeed13
})));
var sliding9 = /* @__PURE__ */ dual(2, (self, chunkSize) => slidingSize(self, chunkSize, 1));
var slidingSize = /* @__PURE__ */ dual(3, (self, chunkSize, stepSize) => {
  if (chunkSize <= 0 || stepSize <= 0) {
    return die14(new IllegalArgumentException2("Invalid bounds - `chunkSize` and `stepSize` must be greater than zero"));
  }
  return new StreamImpl(suspend6(() => {
    const queue = new RingBuffer(chunkSize);
    const emitOnStreamEnd = /* @__PURE__ */ __name((queueSize, channelEnd) => {
      if (queueSize < chunkSize) {
        const items = queue.toChunk();
        const result2 = isEmpty2(items) ? empty7() : of3(items);
        return pipe(write(result2), flatMap20(() => channelEnd));
      }
      const lastEmitIndex = queueSize - (queueSize - chunkSize) % stepSize;
      if (lastEmitIndex === queueSize) {
        return channelEnd;
      }
      const leftovers = queueSize - (lastEmitIndex - chunkSize + stepSize);
      const lastItems = pipe(queue.toChunk(), takeRight2(leftovers));
      const result = isEmpty2(lastItems) ? empty7() : of3(lastItems);
      return pipe(write(result), flatMap20(() => channelEnd));
    }, "emitOnStreamEnd");
    const reader = /* @__PURE__ */ __name((queueSize) => readWithCause({
      onInput: /* @__PURE__ */ __name((input) => flatMap20(write(filterMap5(input, (element, index) => {
        queue.put(element);
        const currentIndex = queueSize + index + 1;
        if (currentIndex < chunkSize || (currentIndex - chunkSize) % stepSize > 0) {
          return none2();
        }
        return some3(queue.toChunk());
      })), () => reader(queueSize + input.length)), "onInput"),
      onFailure: /* @__PURE__ */ __name((cause3) => emitOnStreamEnd(queueSize, failCause12(cause3)), "onFailure"),
      onDone: /* @__PURE__ */ __name(() => emitOnStreamEnd(queueSize, void_8), "onDone")
    }), "reader");
    return pipe(toChannel2(self), pipeTo(reader(0)));
  }));
});
var split4 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const split6 = /* @__PURE__ */ __name((leftovers, input) => {
    const [chunk4, remaining] = pipe(leftovers, appendAll3(input), splitWhere2(predicate));
    if (isEmpty2(chunk4) || isEmpty2(remaining)) {
      return loop4(pipe(chunk4, appendAll3(pipe(remaining, drop3(1)))));
    }
    return pipe(write(of3(chunk4)), flatMap20(() => split6(empty7(), pipe(remaining, drop3(1)))));
  }, "split");
  const loop4 = /* @__PURE__ */ __name((leftovers) => readWith({
    onInput: /* @__PURE__ */ __name((input) => split6(leftovers, input), "onInput"),
    onFailure: fail14,
    onDone: /* @__PURE__ */ __name(() => {
      if (isEmpty2(leftovers)) {
        return void_8;
      }
      if (isNone2(pipe(leftovers, findFirst4(predicate)))) {
        return zipRight8(write(of3(leftovers)), void_8);
      }
      return zipRight8(split6(empty7(), leftovers), void_8);
    }, "onDone")
  }), "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4(empty7()))));
});
var splitOnChunk = /* @__PURE__ */ dual(2, (self, delimiter) => {
  const next4 = /* @__PURE__ */ __name((leftover2, delimiterIndex) => readWithCause({
    onInput: /* @__PURE__ */ __name((inputChunk) => {
      let buffer4;
      const [carry, delimiterCursor] = pipe(inputChunk, reduce4([pipe(leftover2, getOrElse2(() => empty7())), delimiterIndex], ([carry2, delimiterCursor2], a) => {
        const concatenated = pipe(carry2, append3(a));
        if (delimiterCursor2 < delimiter.length && equals(a, pipe(delimiter, unsafeGet4(delimiterCursor2)))) {
          if (delimiterCursor2 + 1 === delimiter.length) {
            if (buffer4 === void 0) {
              buffer4 = [];
            }
            buffer4.push(pipe(concatenated, take3(concatenated.length - delimiter.length)));
            return [empty7(), 0];
          }
          return [concatenated, delimiterCursor2 + 1];
        }
        return [concatenated, equals(a, pipe(delimiter, unsafeGet4(0))) ? 1 : 0];
      }));
      const output = buffer4 === void 0 ? empty7() : unsafeFromArray(buffer4);
      return flatMap20(write(output), () => next4(isNonEmpty(carry) ? some3(carry) : none2(), delimiterCursor));
    }, "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => match2(leftover2, {
      onNone: /* @__PURE__ */ __name(() => failCause12(cause3), "onNone"),
      onSome: /* @__PURE__ */ __name((chunk4) => zipRight8(write(of3(chunk4)), failCause12(cause3)), "onSome")
    }), "onFailure"),
    onDone: /* @__PURE__ */ __name((done12) => match2(leftover2, {
      onNone: /* @__PURE__ */ __name(() => succeed15(done12), "onNone"),
      onSome: /* @__PURE__ */ __name((chunk4) => zipRight8(write(of3(chunk4)), succeed15(done12)), "onSome")
    }), "onDone")
  }), "next");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(next4(none2(), 0))));
});
var splitLines2 = /* @__PURE__ */ __name((self) => pipeThroughChannel(self, splitLines()), "splitLines");
var succeed20 = /* @__PURE__ */ __name((value2) => fromChunk(of3(value2)), "succeed");
var sync14 = /* @__PURE__ */ __name((evaluate3) => suspend10(() => fromChunk(of3(evaluate3()))), "sync");
var suspend10 = /* @__PURE__ */ __name((stream) => new StreamImpl(suspend6(() => toChannel2(stream()))), "suspend");
var take11 = /* @__PURE__ */ dual(2, (self, n) => {
  if (!Number.isInteger(n)) {
    return die14(new IllegalArgumentException2(`${n} must be an integer`));
  }
  const loop4 = /* @__PURE__ */ __name((n2) => readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const taken = pipe(input, take3(Math.min(n2, Number.POSITIVE_INFINITY)));
      const leftover2 = Math.max(0, n2 - taken.length);
      const more = leftover2 > 0;
      if (more) {
        return pipe(write(taken), flatMap20(() => loop4(leftover2)));
      }
      return write(taken);
    }, "onInput"),
    onFailure: fail14,
    onDone: succeed15
  }), "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(0 < n ? loop4(n) : void_8)));
});
var takeRight4 = /* @__PURE__ */ dual(2, (self, n) => {
  if (n <= 0) {
    return empty44;
  }
  return new StreamImpl(pipe(succeed13(new RingBuffer(n)), map26((queue) => {
    const reader = readWith({
      onInput: /* @__PURE__ */ __name((input) => {
        for (const element of input) {
          queue.put(element);
        }
        return reader;
      }, "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name(() => pipe(write(queue.toChunk()), zipRight8(void_8)), "onDone")
    });
    return pipe(toChannel2(self), pipeTo(reader));
  }), unwrap2));
});
var takeUntil3 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const taken = pipe(input, takeWhile3((a) => !predicate(a)));
      const last7 = pipe(input, drop3(taken.length), take3(1));
      if (isEmpty2(last7)) {
        return pipe(write(taken), flatMap20(() => loop4));
      }
      return write(pipe(taken, appendAll3(last7)));
    }, "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4)));
});
var takeUntilEffect = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = /* @__PURE__ */ __name((iterator) => {
    const next4 = iterator.next();
    if (next4.done) {
      return readWithCause({
        onInput: /* @__PURE__ */ __name((elem) => loop4(elem[Symbol.iterator]()), "onInput"),
        onFailure: failCause12,
        onDone: succeed15
      });
    }
    return pipe(predicate(next4.value), map26((bool) => bool ? write(of3(next4.value)) : pipe(write(of3(next4.value)), flatMap20(() => loop4(iterator)))), unwrap2);
  }, "loop");
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4(empty7()[Symbol.iterator]()))));
});
var takeWhile6 = /* @__PURE__ */ dual(2, (self, predicate) => {
  const loop4 = readWith({
    onInput: /* @__PURE__ */ __name((input) => {
      const taken = pipe(input, takeWhile3(predicate));
      const more = taken.length === input.length;
      if (more) {
        return pipe(write(taken), flatMap20(() => loop4));
      }
      return write(taken);
    }, "onInput"),
    onFailure: fail14,
    onDone: succeed15
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop4)));
});
var tap9 = /* @__PURE__ */ dual(2, (self, f) => mapEffectSequential(self, (a) => as8(f(a), a)));
var tapBoth4 = /* @__PURE__ */ dual(2, (self, options) => pipe(self, tapError7(options.onFailure), tap9(options.onSuccess)));
var tapError7 = /* @__PURE__ */ dual(2, (self, f) => catchAll8(self, (error2) => fromEffect9(zipRight7(f(error2), fail12(error2)))));
var tapErrorCause6 = /* @__PURE__ */ dual(2, (self, f) => {
  const loop4 = readWithCause({
    onInput: /* @__PURE__ */ __name((chunk4) => flatMap20(write(chunk4), () => loop4), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(zipRight7(f(cause3), failCause10(cause3))), "onFailure"),
    onDone: succeedNow
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop4)));
});
var tapSink = /* @__PURE__ */ dual(2, (self, sink) => pipe(fromEffect9(all10([bounded5(1), make22()])), flatMap23(([queue, deferred]) => {
  const right3 = flattenTake(fromQueue3(queue, {
    maxChunkSize: 1
  }));
  const loop4 = readWithCause({
    onInput: /* @__PURE__ */ __name((chunk4) => pipe(fromEffect5(offer3(queue, chunk3(chunk4))), foldCauseChannel({
      onFailure: /* @__PURE__ */ __name(() => flatMap20(write(chunk4), () => identityChannel()), "onFailure"),
      onSuccess: /* @__PURE__ */ __name(() => flatMap20(write(chunk4), () => loop4), "onSuccess")
    })), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => pipe(fromEffect5(offer3(queue, failCause14(cause3))), foldCauseChannel({
      onFailure: /* @__PURE__ */ __name(() => failCause12(cause3), "onFailure"),
      onSuccess: /* @__PURE__ */ __name(() => failCause12(cause3), "onSuccess")
    })), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => pipe(fromEffect5(offer3(queue, end4)), foldCauseChannel({
      onFailure: /* @__PURE__ */ __name(() => void_8, "onFailure"),
      onSuccess: /* @__PURE__ */ __name(() => void_8, "onSuccess")
    })), "onDone")
  });
  return pipe(new StreamImpl(pipe(pipeTo(toChannel2(self), loop4), ensuring5(zipRight7(forkDaemon3(offer3(queue, end4)), _await(deferred))))), merge10(execute(pipe(run4(right3, sink), ensuring4(zipRight7(shutdown4(queue), succeed2(deferred, void 0)))))));
})));
var throttle = /* @__PURE__ */ dual(2, (self, options) => throttleEffect(self, {
  ...options,
  cost: /* @__PURE__ */ __name((chunk4) => succeed13(options.cost(chunk4)), "cost")
}));
var throttleEffect = /* @__PURE__ */ dual(2, (self, options) => {
  if (options.strategy === "enforce") {
    return throttleEnforceEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
  }
  return throttleShapeEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
});
var throttleEnforceEffect = /* @__PURE__ */ __name((self, cost, units, duration3, burst) => {
  const loop4 = /* @__PURE__ */ __name((tokens, timestampMillis) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => pipe(cost(input), zip12(currentTimeMillis2), map26(([weight, currentTimeMillis3]) => {
      const elapsed3 = currentTimeMillis3 - timestampMillis;
      const cycles = elapsed3 / toMillis(duration3);
      const sum4 = tokens + cycles * units;
      const max10 = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum4 < 0 ? max10 : Math.min(sum4, max10);
      if (weight <= available) {
        return pipe(write(input), flatMap20(() => loop4(available - weight, currentTimeMillis3)));
      }
      return loop4(tokens, timestampMillis);
    }), unwrap2), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "loop");
  const throttled = pipe(currentTimeMillis2, map26((currentTimeMillis3) => loop4(units, currentTimeMillis3)), unwrap2);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
}, "throttleEnforceEffect");
var throttleShapeEffect = /* @__PURE__ */ __name((self, costFn, units, duration3, burst) => {
  const loop4 = /* @__PURE__ */ __name((tokens, timestampMillis) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => pipe(costFn(input), zip12(currentTimeMillis2), map26(([weight, currentTimeMillis3]) => {
      const elapsed3 = currentTimeMillis3 - timestampMillis;
      const cycles = elapsed3 / toMillis(duration3);
      const sum4 = tokens + cycles * units;
      const max10 = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum4 < 0 ? max10 : Math.min(sum4, max10);
      const remaining = available - weight;
      const waitCycles = remaining >= 0 ? 0 : -remaining / units;
      const delay4 = millis(Math.max(0, waitCycles * toMillis(duration3)));
      if (greaterThan3(delay4, zero)) {
        return pipe(fromEffect5(sleep2(delay4)), zipRight8(write(input)), flatMap20(() => loop4(remaining, currentTimeMillis3)));
      }
      return flatMap20(write(input), () => loop4(remaining, currentTimeMillis3));
    }), unwrap2), "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => void_8, "onDone")
  }), "loop");
  const throttled = pipe(currentTimeMillis2, map26((currentTimeMillis3) => loop4(units, currentTimeMillis3)), unwrap2);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
}, "throttleShapeEffect");
var tick = /* @__PURE__ */ __name((interval) => repeatWithSchedule(void 0, spaced2(interval)), "tick");
var timeout4 = /* @__PURE__ */ dual(2, (self, duration3) => pipe(toPull2(self), map26(timeoutFail2({
  onTimeout: /* @__PURE__ */ __name(() => none2(), "onTimeout"),
  duration: duration3
})), fromPull2));
var timeoutFail3 = /* @__PURE__ */ dual(3, (self, error2, duration3) => pipe(self, timeoutTo3(duration3, failSync10(error2))));
var timeoutFailCause3 = /* @__PURE__ */ dual(3, (self, cause3, duration3) => pipe(toPull2(self), map26(timeoutFailCause2({
  onTimeout: /* @__PURE__ */ __name(() => map22(cause3(), some3), "onTimeout"),
  duration: duration3
})), fromPull2));
var timeoutTo3 = /* @__PURE__ */ dual(3, (self, duration3, that) => {
  const StreamTimeout = new RuntimeException2("Stream Timeout");
  return pipe(self, timeoutFailCause3(() => die6(StreamTimeout), duration3), catchSomeCause3((cause3) => isDieType2(cause3) && isRuntimeException2(cause3.defect) && cause3.defect.message !== void 0 && cause3.defect.message === "Stream Timeout" ? some3(that) : none2()));
});
var pubsubFromOptions = /* @__PURE__ */ __name((options) => {
  if (typeof options === "number") {
    return bounded4(options);
  } else if (options.capacity === "unbounded") {
    return unbounded4({
      replay: options.replay
    });
  }
  switch (options.strategy) {
    case "dropping":
      return dropping3(options);
    case "sliding":
      return sliding3(options);
    default:
      return bounded4(options);
  }
}, "pubsubFromOptions");
var toPubSub2 = /* @__PURE__ */ dual(2, (self, capacity10) => pipe(acquireRelease3(pubsubFromOptions(capacity10), (pubsub) => shutdown3(pubsub)), tap5((pubsub) => pipe(self, runIntoPubSubScoped(pubsub), forkScoped3))));
var toPull2 = /* @__PURE__ */ __name((self) => map26(toPull(toChannel2(self)), (pull) => pipe(pull, mapError5(some3), flatMap18(match({
  onLeft: /* @__PURE__ */ __name(() => fail12(none2()), "onLeft"),
  onRight: succeed13
})))), "toPull");
var toQueue2 = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => tap5(acquireRelease3(options?.strategy === "unbounded" ? unbounded5() : options?.strategy === "dropping" ? dropping4(options.capacity ?? 2) : options?.strategy === "sliding" ? sliding4(options.capacity ?? 2) : bounded5(options?.capacity ?? 2), (queue) => shutdown4(queue)), (queue) => forkScoped3(runIntoQueueScoped(self, queue))));
var toQueueOfElements = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => tap5(acquireRelease3(bounded5(options?.capacity ?? 2), (queue) => shutdown4(queue)), (queue) => forkScoped3(runIntoQueueElementsScoped(self, queue))));
var toReadableStream = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => toReadableStreamRuntime(self, defaultRuntime2, options));
var toReadableStreamEffect = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, options) => map26(runtime4(), (runtime5) => toReadableStreamRuntime(self, runtime5, options)));
var toReadableStreamRuntime = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, runtime5, options) => {
  const runFork4 = runFork3(runtime5);
  let currentResolve = void 0;
  let fiber = void 0;
  const latch = unsafeMakeLatch2(false);
  return new ReadableStream({
    start(controller) {
      fiber = runFork4(runForEachChunk(self, (chunk4) => {
        if (chunk4.length === 0) return _void;
        return latch.whenOpen(sync8(() => {
          latch.unsafeClose();
          for (const item of chunk4) {
            controller.enqueue(item);
          }
          currentResolve();
          currentResolve = void 0;
        }));
      }));
      fiber.addObserver((exit4) => {
        try {
          if (exit4._tag === "Failure") {
            controller.error(squash(exit4.cause));
          } else {
            controller.close();
          }
        } catch {
        }
      });
    },
    pull() {
      return new Promise((resolve) => {
        currentResolve = resolve;
        runSync2(latch.open);
      });
    },
    cancel() {
      if (!fiber) return;
      return runPromise2(asVoid6(interrupt7(fiber)));
    }
  }, options?.strategy);
});
var transduce = /* @__PURE__ */ dual(2, (self, sink) => {
  const newChannel = suspend6(() => {
    const leftovers = {
      ref: empty7()
    };
    const upstreamDone = {
      ref: false
    };
    const buffer4 = suspend6(() => {
      const leftover2 = leftovers.ref;
      if (isEmpty2(leftover2)) {
        return readWith({
          onInput: /* @__PURE__ */ __name((input) => pipe(write(input), flatMap20(() => buffer4)), "onInput"),
          onFailure: fail14,
          onDone: succeedNow
        });
      }
      leftovers.ref = empty7();
      return pipe(writeChunk(leftover2), flatMap20(() => buffer4));
    });
    const concatAndGet = /* @__PURE__ */ __name((chunk4) => {
      const leftover2 = leftovers.ref;
      const concatenated = appendAll3(leftover2, filter5(chunk4, (chunk5) => chunk5.length !== 0));
      leftovers.ref = concatenated;
      return concatenated;
    }, "concatAndGet");
    const upstreamMarker = readWith({
      onInput: /* @__PURE__ */ __name((input) => flatMap20(write(input), () => upstreamMarker), "onInput"),
      onFailure: fail14,
      onDone: /* @__PURE__ */ __name((done12) => zipRight8(sync10(() => {
        upstreamDone.ref = true;
      }), succeedNow(done12)), "onDone")
    });
    const transducer = pipe(sink, toChannel, collectElements, flatMap20(([leftover2, z]) => pipe(succeed15([upstreamDone.ref, concatAndGet(leftover2)]), flatMap20(([done12, newLeftovers]) => {
      const nextChannel = done12 && isEmpty2(newLeftovers) ? void_8 : transducer;
      return pipe(write(of3(z)), flatMap20(() => nextChannel));
    }))));
    return pipe(toChannel2(self), pipeTo(upstreamMarker), pipeTo(buffer4), pipeToOrFail(transducer));
  });
  return new StreamImpl(newChannel);
});
var toAsyncIterableRuntime = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, runtime5) => {
  const runFork4 = runFork3(runtime5);
  return {
    [Symbol.asyncIterator]() {
      let currentResolve = void 0;
      let currentReject = void 0;
      let fiber = void 0;
      const latch = unsafeMakeLatch2(false);
      let returned = false;
      return {
        next() {
          if (!fiber) {
            fiber = runFork4(runForEach(self, (value2) => latch.whenOpen(sync8(() => {
              latch.unsafeClose();
              currentResolve({
                done: false,
                value: value2
              });
              currentResolve = currentReject = void 0;
            }))));
            fiber.addObserver((exit4) => {
              if (returned) return;
              fiber = runFork2(latch.whenOpen(sync8(() => {
                if (exit4._tag === "Failure") {
                  currentReject(squash(exit4.cause));
                } else {
                  currentResolve({
                    done: true,
                    value: void 0
                  });
                }
                currentResolve = currentReject = void 0;
              })));
            });
          }
          return new Promise((resolve, reject2) => {
            currentResolve = resolve;
            currentReject = reject2;
            latch.unsafeOpen();
          });
        },
        return() {
          returned = true;
          if (!fiber) return Promise.resolve({
            done: true,
            value: void 0
          });
          return runPromise2(as8(interrupt7(fiber), {
            done: true,
            value: void 0
          }));
        }
      };
    }
  };
});
var toAsyncIterable = /* @__PURE__ */ __name((self) => toAsyncIterableRuntime(self, defaultRuntime2), "toAsyncIterable");
var toAsyncIterableEffect = /* @__PURE__ */ __name((self) => map26(runtime4(), (runtime5) => toAsyncIterableRuntime(self, runtime5)), "toAsyncIterableEffect");
var unfold5 = /* @__PURE__ */ __name((s, f) => unfoldChunk(s, (s2) => pipe(f(s2), map2(([a, s3]) => [of3(a), s3]))), "unfold");
var unfoldChunk = /* @__PURE__ */ __name((s, f) => {
  const loop4 = /* @__PURE__ */ __name((s2) => match2(f(s2), {
    onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
    onSome: /* @__PURE__ */ __name(([chunk4, s3]) => flatMap20(write(chunk4), () => loop4(s3)), "onSome")
  }), "loop");
  return new StreamImpl(suspend6(() => loop4(s)));
}, "unfoldChunk");
var unfoldChunkEffect = /* @__PURE__ */ __name((s, f) => suspend10(() => {
  const loop4 = /* @__PURE__ */ __name((s2) => unwrap2(map26(f(s2), match2({
    onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
    onSome: /* @__PURE__ */ __name(([chunk4, s3]) => flatMap20(write(chunk4), () => loop4(s3)), "onSome")
  }))), "loop");
  return new StreamImpl(loop4(s));
}), "unfoldChunkEffect");
var unfoldEffect = /* @__PURE__ */ __name((s, f) => unfoldChunkEffect(s, (s2) => pipe(f(s2), map26(map2(([a, s3]) => [of3(a), s3])))), "unfoldEffect");
var void_11 = /* @__PURE__ */ succeed20(void 0);
var unwrap4 = /* @__PURE__ */ __name((effect4) => flatten16(fromEffect9(effect4)), "unwrap");
var unwrapScoped5 = /* @__PURE__ */ __name((effect4) => flatten16(scoped7(effect4)), "unwrapScoped");
var unwrapScopedWith3 = /* @__PURE__ */ __name((f) => flatten16(scopedWith4((scope6) => f(scope6))), "unwrapScopedWith");
var updateService6 = /* @__PURE__ */ dual(3, (self, tag, f) => pipe(self, mapInputContext7((context11) => pipe(context11, add2(tag, f(pipe(context11, unsafeGet3(tag))))))));
var when5 = /* @__PURE__ */ dual(2, (self, test) => pipe(self, whenEffect3(sync8(test))));
var whenCase = /* @__PURE__ */ __name((evaluate3, pf) => whenCaseEffect(pf)(sync8(evaluate3)), "whenCase");
var whenCaseEffect = /* @__PURE__ */ dual(2, (self, pf) => pipe(fromEffect9(self), flatMap23((a) => pipe(pf(a), getOrElse2(() => empty44)))));
var whenEffect3 = /* @__PURE__ */ dual(2, (self, effect4) => pipe(fromEffect9(effect4), flatMap23((bool) => bool ? self : empty44)));
var withSpan6 = /* @__PURE__ */ __name(function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return new StreamImpl(withSpan5(toChannel2(self), name, options));
  }
  return (self) => new StreamImpl(withSpan5(toChannel2(self), name, options));
}, "withSpan");
var zip16 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, zipWith17(that, (a, a2) => [a, a2])));
var zipFlatten2 = /* @__PURE__ */ dual(2, (self, that) => pipe(self, zipWith17(that, (a, a2) => [...a, a2])));
var zipAll = /* @__PURE__ */ dual(2, (self, options) => zipAllWith(self, {
  other: options.other,
  onSelf: /* @__PURE__ */ __name((a) => [a, options.defaultOther], "onSelf"),
  onOther: /* @__PURE__ */ __name((a2) => [options.defaultSelf, a2], "onOther"),
  onBoth: /* @__PURE__ */ __name((a, a2) => [a, a2], "onBoth")
}));
var zipAllLeft = /* @__PURE__ */ dual(3, (self, other, defaultSelf) => zipAllWith(self, {
  other,
  onSelf: identity,
  onOther: /* @__PURE__ */ __name(() => defaultSelf, "onOther"),
  onBoth: /* @__PURE__ */ __name((a) => a, "onBoth")
}));
var zipAllRight = /* @__PURE__ */ dual(3, (self, other, defaultRight) => zipAllWith(self, {
  other,
  onSelf: /* @__PURE__ */ __name(() => defaultRight, "onSelf"),
  onOther: identity,
  onBoth: /* @__PURE__ */ __name((_, a2) => a2, "onBoth")
}));
var zipAllSortedByKey = /* @__PURE__ */ dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: /* @__PURE__ */ __name((a) => [a, options.defaultOther], "onSelf"),
  onOther: /* @__PURE__ */ __name((a2) => [options.defaultSelf, a2], "onOther"),
  onBoth: /* @__PURE__ */ __name((a, a2) => [a, a2], "onBoth"),
  order: options.order
}));
var zipAllSortedByKeyLeft = /* @__PURE__ */ dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: identity,
  onOther: /* @__PURE__ */ __name(() => options.defaultSelf, "onOther"),
  onBoth: /* @__PURE__ */ __name((a) => a, "onBoth"),
  order: options.order
}));
var zipAllSortedByKeyRight = /* @__PURE__ */ dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: /* @__PURE__ */ __name(() => options.defaultOther, "onSelf"),
  onOther: identity,
  onBoth: /* @__PURE__ */ __name((_, a2) => a2, "onBoth"),
  order: options.order
}));
var zipAllSortedByKeyWith = /* @__PURE__ */ dual(2, (self, options) => {
  const pull = /* @__PURE__ */ __name((state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return pipe(pullLeft, match20({
          onFailure: fail4,
          onSuccess: /* @__PURE__ */ __name((leftChunk) => succeed3([map7(leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft]), "onSuccess")
        }));
      }
      case OP_DRAIN_RIGHT: {
        return pipe(pullRight, match20({
          onFailure: fail4,
          onSuccess: /* @__PURE__ */ __name((rightChunk) => succeed3([map7(rightChunk, ([k, a2]) => [k, options.onOther(a2)]), DrainRight]), "onSuccess")
        }));
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip12(unsome2(pullRight), {
          concurrent: true
        }), matchEffect4({
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onFailure"),
          onSuccess: /* @__PURE__ */ __name(([leftOption, rightOption]) => {
            if (isSome2(leftOption) && isSome2(rightOption)) {
              if (isEmpty2(leftOption.value) && isEmpty2(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty2(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty2(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed13(succeed3(merge12(leftOption.value, rightOption.value)));
            }
            if (isSome2(leftOption) && isNone2(rightOption)) {
              if (isEmpty2(leftOption.value)) {
                return pull(DrainLeft, pullLeft, pullRight);
              }
              return succeed13(succeed3([pipe(leftOption.value, map7(([k, a]) => [k, options.onSelf(a)])), DrainLeft]));
            }
            if (isNone2(leftOption) && isSome2(rightOption)) {
              if (isEmpty2(rightOption.value)) {
                return pull(DrainRight, pullLeft, pullRight);
              }
              return succeed13(succeed3([pipe(rightOption.value, map7(([k, a2]) => [k, options.onOther(a2)])), DrainRight]));
            }
            return succeed13(fail4(none2()));
          }, "onSuccess")
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect4(pullLeft, {
          onFailure: match2({
            onNone: /* @__PURE__ */ __name(() => succeed13(succeed3([pipe(state.rightChunk, map7(([k, a2]) => [k, options.onOther(a2)])), DrainRight])), "onNone"),
            onSome: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onSome")
          }),
          onSuccess: /* @__PURE__ */ __name((leftChunk) => isEmpty2(leftChunk) ? pull(PullLeft(state.rightChunk), pullLeft, pullRight) : succeed13(succeed3(merge12(leftChunk, state.rightChunk))), "onSuccess")
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect4(pullRight, {
          onFailure: match2({
            onNone: /* @__PURE__ */ __name(() => succeed13(succeed3([map7(state.leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft])), "onNone"),
            onSome: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onSome")
          }),
          onSuccess: /* @__PURE__ */ __name((rightChunk) => isEmpty2(rightChunk) ? pull(PullRight(state.leftChunk), pullLeft, pullRight) : succeed13(succeed3(merge12(state.leftChunk, rightChunk))), "onSuccess")
        });
      }
    }
  }, "pull");
  const merge12 = /* @__PURE__ */ __name((leftChunk, rightChunk) => {
    const hasNext = /* @__PURE__ */ __name((chunk4, index) => index < chunk4.length - 1, "hasNext");
    const builder = [];
    let state = void 0;
    let leftIndex = 0;
    let rightIndex = 0;
    let leftTuple = pipe(leftChunk, unsafeGet4(leftIndex));
    let rightTuple = pipe(rightChunk, unsafeGet4(rightIndex));
    let k1 = leftTuple[0];
    let a = leftTuple[1];
    let k2 = rightTuple[0];
    let a2 = rightTuple[1];
    let loop4 = true;
    while (loop4) {
      const compare = options.order(k1, k2);
      if (compare === 0) {
        builder.push([k1, options.onBoth(a, a2)]);
        if (hasNext(leftChunk, leftIndex) && hasNext(rightChunk, rightIndex)) {
          leftIndex = leftIndex + 1;
          rightIndex = rightIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet4(leftIndex));
          rightTuple = pipe(rightChunk, unsafeGet4(rightIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else if (hasNext(leftChunk, leftIndex)) {
          state = PullRight(pipe(leftChunk, drop3(leftIndex + 1)));
          loop4 = false;
        } else if (hasNext(rightChunk, rightIndex)) {
          state = PullLeft(pipe(rightChunk, drop3(rightIndex + 1)));
          loop4 = false;
        } else {
          state = PullBoth;
          loop4 = false;
        }
      } else if (compare < 0) {
        builder.push([k1, options.onSelf(a)]);
        if (hasNext(leftChunk, leftIndex)) {
          leftIndex = leftIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet4(leftIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
        } else {
          const rightBuilder = [];
          rightBuilder.push(rightTuple);
          while (hasNext(rightChunk, rightIndex)) {
            rightIndex = rightIndex + 1;
            rightTuple = pipe(rightChunk, unsafeGet4(rightIndex));
            rightBuilder.push(rightTuple);
          }
          state = PullLeft(unsafeFromArray(rightBuilder));
          loop4 = false;
        }
      } else {
        builder.push([k2, options.onOther(a2)]);
        if (hasNext(rightChunk, rightIndex)) {
          rightIndex = rightIndex + 1;
          rightTuple = pipe(rightChunk, unsafeGet4(rightIndex));
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else {
          const leftBuilder = [];
          leftBuilder.push(leftTuple);
          while (hasNext(leftChunk, leftIndex)) {
            leftIndex = leftIndex + 1;
            leftTuple = pipe(leftChunk, unsafeGet4(leftIndex));
            leftBuilder.push(leftTuple);
          }
          state = PullRight(unsafeFromArray(leftBuilder));
          loop4 = false;
        }
      }
    }
    return [unsafeFromArray(builder), state];
  }, "merge");
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipAllWith = /* @__PURE__ */ dual(2, (self, options) => {
  const pull = /* @__PURE__ */ __name((state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return matchEffect4(pullLeft, {
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(error2)), "onFailure"),
          onSuccess: /* @__PURE__ */ __name((leftChunk) => succeed13(succeed3([map7(leftChunk, options.onSelf), DrainLeft])), "onSuccess")
        });
      }
      case OP_DRAIN_RIGHT: {
        return matchEffect4(pullRight, {
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(error2)), "onFailure"),
          onSuccess: /* @__PURE__ */ __name((rightChunk) => succeed13(succeed3([map7(rightChunk, options.onOther), DrainRight])), "onSuccess")
        });
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip12(unsome2(pullRight), {
          concurrent: true
        }), matchEffect4({
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onFailure"),
          onSuccess: /* @__PURE__ */ __name(([leftOption, rightOption]) => {
            if (isSome2(leftOption) && isSome2(rightOption)) {
              if (isEmpty2(leftOption.value) && isEmpty2(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty2(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty2(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed13(succeed3(zip18(leftOption.value, rightOption.value, options.onBoth)));
            }
            if (isSome2(leftOption) && isNone2(rightOption)) {
              return succeed13(succeed3([map7(leftOption.value, options.onSelf), DrainLeft]));
            }
            if (isNone2(leftOption) && isSome2(rightOption)) {
              return succeed13(succeed3([map7(rightOption.value, options.onOther), DrainRight]));
            }
            return succeed13(fail4(none2()));
          }, "onSuccess")
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect4(pullLeft, {
          onFailure: match2({
            onNone: /* @__PURE__ */ __name(() => succeed13(succeed3([map7(state.rightChunk, options.onOther), DrainRight])), "onNone"),
            onSome: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onSome")
          }),
          onSuccess: /* @__PURE__ */ __name((leftChunk) => {
            if (isEmpty2(leftChunk)) {
              return pull(PullLeft(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty2(state.rightChunk)) {
              return pull(PullRight(leftChunk), pullLeft, pullRight);
            }
            return succeed13(succeed3(zip18(leftChunk, state.rightChunk, options.onBoth)));
          }, "onSuccess")
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect4(pullRight, {
          onFailure: match2({
            onNone: /* @__PURE__ */ __name(() => succeed13(succeed3([map7(state.leftChunk, options.onSelf), DrainLeft])), "onNone"),
            onSome: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onSome")
          }),
          onSuccess: /* @__PURE__ */ __name((rightChunk) => {
            if (isEmpty2(rightChunk)) {
              return pull(PullRight(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty2(state.leftChunk)) {
              return pull(PullLeft(rightChunk), pullLeft, pullRight);
            }
            return succeed13(succeed3(zip18(state.leftChunk, rightChunk, options.onBoth)));
          }, "onSuccess")
        });
      }
    }
  }, "pull");
  const zip18 = /* @__PURE__ */ __name((leftChunk, rightChunk, f) => {
    const [output, either11] = zipChunks(leftChunk, rightChunk, f);
    switch (either11._tag) {
      case "Left": {
        if (isEmpty2(either11.left)) {
          return [output, PullBoth];
        }
        return [output, PullRight(either11.left)];
      }
      case "Right": {
        if (isEmpty2(either11.right)) {
          return [output, PullBoth];
        }
        return [output, PullLeft(either11.right)];
      }
    }
  }, "zip");
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipLatest = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, zipLatestWith(right3, (a, a2) => [a, a2])));
var zipLatestAll = /* @__PURE__ */ __name((...streams) => {
  if (streams.length === 0) {
    return empty44;
  } else if (streams.length === 1) {
    return map33(streams[0], (x) => [x]);
  }
  const [head10, ...tail5] = streams;
  return zipLatestWith(head10, zipLatestAll(...tail5), (first4, second) => [first4, ...second]);
}, "zipLatestAll");
var zipLatestWith = /* @__PURE__ */ dual(3, (left3, right3, f) => {
  const pullNonEmpty = /* @__PURE__ */ __name((pull) => pipe(pull, flatMap18((chunk4) => isEmpty2(chunk4) ? pullNonEmpty(pull) : succeed13(chunk4))), "pullNonEmpty");
  return pipe(toPull2(left3), map26(pullNonEmpty), zip12(pipe(toPull2(right3), map26(pullNonEmpty))), flatMap18(([left4, right4]) => pipe(fromEffectOption(raceWith2(left4, right4, {
    onSelfDone: /* @__PURE__ */ __name((leftDone, rightFiber) => pipe(suspend4(() => leftDone), zipWith12(join4(rightFiber), (l, r) => [l, r, true])), "onSelfDone"),
    onOtherDone: /* @__PURE__ */ __name((rightDone, leftFiber) => pipe(suspend4(() => rightDone), zipWith12(join4(leftFiber), (l, r) => [r, l, false])), "onOtherDone")
  })), flatMap23(([l, r, leftFirst]) => pipe(fromEffect9(make35([unsafeLast(l), unsafeLast(r)])), flatMap23((latest) => pipe(fromChunk(leftFirst ? pipe(r, map7((a2) => f(unsafeLast(l), a2))) : pipe(l, map7((a) => f(a, unsafeLast(r))))), concat3(pipe(repeatEffectOption(left4), mergeEither(repeatEffectOption(right4)), mapEffectSequential(match({
    onLeft: /* @__PURE__ */ __name((leftChunk) => modify8(latest, ([_, rightLatest]) => [pipe(leftChunk, map7((a) => f(a, rightLatest))), [unsafeLast(leftChunk), rightLatest]]), "onLeft"),
    onRight: /* @__PURE__ */ __name((rightChunk) => modify8(latest, ([leftLatest, _]) => [pipe(rightChunk, map7((a2) => f(leftLatest, a2))), [leftLatest, unsafeLast(rightChunk)]]), "onRight")
  })), flatMap23(fromChunk))))))), toPull2)), fromPull2);
});
var zipLeft12 = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, zipWithChunks(right3, (left4, right4) => {
  if (left4.length > right4.length) {
    return [pipe(left4, take3(right4.length)), left2(pipe(left4, take3(right4.length)))];
  }
  return [left4, right2(pipe(right4, drop3(left4.length)))];
})));
var zipRight12 = /* @__PURE__ */ dual(2, (left3, right3) => pipe(left3, zipWithChunks(right3, (left4, right4) => {
  if (left4.length > right4.length) {
    return [right4, left2(pipe(left4, take3(right4.length)))];
  }
  return [pipe(right4, take3(left4.length)), right2(pipe(right4, drop3(left4.length)))];
})));
var zipWith17 = /* @__PURE__ */ dual(3, (left3, right3, f) => pipe(left3, zipWithChunks(right3, (leftChunk, rightChunk) => zipChunks(leftChunk, rightChunk, f))));
var zipWithChunks = /* @__PURE__ */ dual(3, (self, that, f) => {
  const pull = /* @__PURE__ */ __name((state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_PULL_BOTH2: {
        return pipe(unsome2(pullLeft), zip12(unsome2(pullRight), {
          concurrent: true
        }), matchEffect4({
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(some3(error2))), "onFailure"),
          onSuccess: /* @__PURE__ */ __name(([leftOption, rightOption]) => {
            if (isSome2(leftOption) && isSome2(rightOption)) {
              if (isEmpty2(leftOption.value) && isEmpty2(rightOption.value)) {
                return pull(PullBoth2, pullLeft, pullRight);
              }
              if (isEmpty2(leftOption.value)) {
                return pull(PullLeft2(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty2(rightOption.value)) {
                return pull(PullRight2(leftOption.value), pullLeft, pullRight);
              }
              return succeed13(succeed3(zip18(leftOption.value, rightOption.value)));
            }
            return succeed13(fail4(none2()));
          }, "onSuccess")
        }));
      }
      case OP_PULL_LEFT2: {
        return matchEffect4(pullLeft, {
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(error2)), "onFailure"),
          onSuccess: /* @__PURE__ */ __name((leftChunk) => {
            if (isEmpty2(leftChunk)) {
              return pull(PullLeft2(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty2(state.rightChunk)) {
              return pull(PullRight2(leftChunk), pullLeft, pullRight);
            }
            return succeed13(succeed3(zip18(leftChunk, state.rightChunk)));
          }, "onSuccess")
        });
      }
      case OP_PULL_RIGHT2: {
        return matchEffect4(pullRight, {
          onFailure: /* @__PURE__ */ __name((error2) => succeed13(fail4(error2)), "onFailure"),
          onSuccess: /* @__PURE__ */ __name((rightChunk) => {
            if (isEmpty2(rightChunk)) {
              return pull(PullRight2(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty2(state.leftChunk)) {
              return pull(PullLeft2(rightChunk), pullLeft, pullRight);
            }
            return succeed13(succeed3(zip18(state.leftChunk, rightChunk)));
          }, "onSuccess")
        });
      }
    }
  }, "pull");
  const zip18 = /* @__PURE__ */ __name((leftChunk, rightChunk) => {
    const [output, either11] = f(leftChunk, rightChunk);
    switch (either11._tag) {
      case "Left": {
        if (isEmpty2(either11.left)) {
          return [output, PullBoth2];
        }
        return [output, PullRight2(either11.left)];
      }
      case "Right": {
        if (isEmpty2(either11.right)) {
          return [output, PullBoth2];
        }
        return [output, PullLeft2(either11.right)];
      }
    }
  }, "zip");
  return pipe(self, combineChunks(that, PullBoth2, pull));
});
var zipWithIndex = /* @__PURE__ */ __name((self) => pipe(self, mapAccum5(0, (index, a) => [index + 1, [a, index]])), "zipWithIndex");
var zipWithNext = /* @__PURE__ */ __name((self) => {
  const process2 = /* @__PURE__ */ __name((last7) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => {
      const [newLast, chunk4] = mapAccum2(input, last7, (prev, curr) => [some3(curr), pipe(prev, map2((a) => [a, curr]))]);
      const output = filterMap5(chunk4, (option5) => isSome2(option5) ? some3([option5.value[0], some3(option5.value[1])]) : none2());
      return flatMap20(write(output), () => process2(newLast));
    }, "onInput"),
    onFailure: failCause12,
    onDone: /* @__PURE__ */ __name(() => match2(last7, {
      onNone: /* @__PURE__ */ __name(() => void_8, "onNone"),
      onSome: /* @__PURE__ */ __name((value2) => zipRight8(write(of3([value2, none2()])), void_8), "onSome")
    }), "onDone")
  }), "process");
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(process2(none2()))));
}, "zipWithNext");
var zipWithPrevious = /* @__PURE__ */ __name((self) => pipe(self, mapAccum5(none2(), (prev, curr) => [some3(curr), [prev, curr]])), "zipWithPrevious");
var zipWithPreviousAndNext = /* @__PURE__ */ __name((self) => pipe(zipWithNext(zipWithPrevious(self)), map33(([[prev, curr], next4]) => [prev, curr, pipe(next4, map2((tuple5) => tuple5[1]))])), "zipWithPreviousAndNext");
var zipChunks = /* @__PURE__ */ __name((left3, right3, f) => {
  if (left3.length > right3.length) {
    return [pipe(left3, take3(right3.length), zipWith5(right3, f)), left2(pipe(left3, drop3(right3.length)))];
  }
  return [pipe(left3, zipWith5(pipe(right3, take3(left3.length)), f)), right2(pipe(right3, drop3(left3.length)))];
}, "zipChunks");
var Do7 = /* @__PURE__ */ succeed20({});
var bind9 = /* @__PURE__ */ dual((args2) => typeof args2[0] !== "string", (self, tag, f, options) => flatMap23(self, (k) => map33(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var bindTo9 = /* @__PURE__ */ bindTo(map33);
var let_9 = /* @__PURE__ */ let_(map33);
var channelToStream = /* @__PURE__ */ __name((self) => {
  return new StreamImpl(self);
}, "channelToStream");
var decodeText = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, encoding = "utf-8") => suspend10(() => {
  const decoder = new TextDecoder(encoding);
  return map33(self, (s) => decoder.decode(s));
}));
var encodeText = /* @__PURE__ */ __name((self) => suspend10(() => {
  const encoder = new TextEncoder();
  return map33(self, (s) => encoder.encode(s));
}), "encodeText");
var fromEventListener = /* @__PURE__ */ __name((target, type, options) => asyncPush((emit4) => acquireRelease3(sync8(() => target.addEventListener(type, emit4.single, options)), () => sync8(() => target.removeEventListener(type, emit4.single, options))), {
  bufferSize: typeof options === "object" ? options.bufferSize : void 0
}), "fromEventListener");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/groupBy.js
var GroupBySymbolKey = "effect/GroupBy";
var GroupByTypeId = /* @__PURE__ */ Symbol.for(GroupBySymbolKey);
var groupByVariance = {
  /* c8 ignore next */
  _R: /* @__PURE__ */ __name((_) => _, "_R"),
  /* c8 ignore next */
  _E: /* @__PURE__ */ __name((_) => _, "_E"),
  /* c8 ignore next */
  _K: /* @__PURE__ */ __name((_) => _, "_K"),
  /* c8 ignore next */
  _V: /* @__PURE__ */ __name((_) => _, "_V")
};
var isGroupBy = /* @__PURE__ */ __name((u) => hasProperty(u, GroupByTypeId), "isGroupBy");
var evaluate2 = /* @__PURE__ */ dual((args2) => isGroupBy(args2[0]), (self, f, options) => flatMap23(self.grouped, ([key, queue]) => f(key, flattenTake(fromQueue3(queue, {
  shutdown: true
}))), {
  concurrency: "unbounded",
  bufferSize: options?.bufferSize ?? 16
}));
var filter19 = /* @__PURE__ */ dual(2, (self, predicate) => make71(pipe(self.grouped, filterEffect((tuple5) => {
  if (predicate(tuple5[0])) {
    return pipe(succeed13(tuple5), as8(true));
  }
  return pipe(shutdown4(tuple5[1]), as8(false));
}))));
var first3 = /* @__PURE__ */ dual(2, (self, n) => make71(pipe(zipWithIndex(self.grouped), filterEffect((tuple5) => {
  const index = tuple5[1];
  const queue = tuple5[0][1];
  if (index < n) {
    return pipe(succeed13(tuple5), as8(true));
  }
  return pipe(shutdown4(queue), as8(false));
}), map33((tuple5) => tuple5[0]))));
var make71 = /* @__PURE__ */ __name((grouped3) => ({
  [GroupByTypeId]: groupByVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  grouped: grouped3
}), "make");
var groupBy3 = /* @__PURE__ */ dual((args2) => isStream(args2[0]), (self, f, options) => make71(unwrapScoped5(gen5(function* () {
  const decider = yield* make22();
  const output = yield* acquireRelease3(bounded5(options?.bufferSize ?? 16), (queue) => shutdown4(queue));
  const ref = yield* make35(/* @__PURE__ */ new Map());
  const add7 = yield* pipe(mapEffectSequential(self, f), distributedWithDynamicCallback(options?.bufferSize ?? 16, ([key, value2]) => flatMap18(_await(decider), (f2) => f2(key, value2)), (exit4) => offer3(output, exit4)));
  yield* succeed2(decider, (key, _) => pipe(get13(ref), map26((map36) => fromNullable2(map36.get(key))), flatMap18(match2({
    onNone: /* @__PURE__ */ __name(() => flatMap18(add7, ([index, queue]) => zipRight7(update4(ref, (map36) => map36.set(key, index)), pipe(offer3(output, succeed3([key, mapDequeue(queue, (exit4) => new TakeImpl(pipe(exit4, map15((tuple5) => of3(tuple5[1])))))])), as8((n) => n === index)))), "onNone"),
    onSome: /* @__PURE__ */ __name((index) => succeed13((n) => n === index), "onSome")
  }))));
  return flattenExitOption(fromQueue3(output, {
    shutdown: true
  }));
}))));
var mapEffectOptions = /* @__PURE__ */ dual((args2) => typeof args2[0] !== "function", (self, f, options) => {
  if (options?.key) {
    return evaluate2(groupByKey(self, options.key, {
      bufferSize: options.bufferSize
    }), (_, s) => mapEffectSequential(s, f));
  }
  return matchConcurrency(options?.concurrency, () => mapEffectSequential(self, f), (n) => options?.unordered ? flatMap23(self, (a) => fromEffect9(f(a)), {
    concurrency: n
  }) : mapEffectPar(self, n, f));
});
var bindEffect = /* @__PURE__ */ dual((args2) => typeof args2[0] !== "string", (self, tag, f, options) => mapEffectOptions(self, (k) => map26(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var mapDequeue = /* @__PURE__ */ __name((dequeue, f) => new MapDequeue(dequeue, f), "mapDequeue");
var MapDequeue = class extends Class3 {
  static {
    __name(this, "MapDequeue");
  }
  dequeue;
  f;
  [DequeueTypeId2] = {
    _Out: /* @__PURE__ */ __name((_) => _, "_Out")
  };
  constructor(dequeue, f) {
    super();
    this.dequeue = dequeue;
    this.f = f;
  }
  capacity() {
    return capacity5(this.dequeue);
  }
  get size() {
    return size20(this.dequeue);
  }
  unsafeSize() {
    return this.dequeue.unsafeSize();
  }
  get awaitShutdown() {
    return awaitShutdown4(this.dequeue);
  }
  isActive() {
    return this.dequeue.isActive();
  }
  get isShutdown() {
    return isShutdown4(this.dequeue);
  }
  get shutdown() {
    return shutdown4(this.dequeue);
  }
  get isFull() {
    return isFull5(this.dequeue);
  }
  get isEmpty() {
    return isEmpty18(this.dequeue);
  }
  get take() {
    return pipe(take6(this.dequeue), map26((a) => this.f(a)));
  }
  get takeAll() {
    return pipe(takeAll2(this.dequeue), map26(map7((a) => this.f(a))));
  }
  takeUpTo(max10) {
    return pipe(takeUpTo2(this.dequeue, max10), map26(map7((a) => this.f(a))));
  }
  takeBetween(min8, max10) {
    return pipe(takeBetween2(this.dequeue, min8, max10), map26(map7((a) => this.f(a))));
  }
  takeN(n) {
    return pipe(takeN2(this.dequeue, n), map26(map7((a) => this.f(a))));
  }
  poll() {
    return pipe(poll6(this.dequeue), map26(map2((a) => this.f(a))));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  commit() {
    return this.take;
  }
};
var groupByKey = /* @__PURE__ */ dual((args2) => typeof args2[0] !== "function", (self, f, options) => {
  const loop4 = /* @__PURE__ */ __name((map36, outerQueue) => readWithCause({
    onInput: /* @__PURE__ */ __name((input) => flatMap20(fromEffect5(forEach16(groupByIterable(input, f), ([key, values10]) => {
      const innerQueue = map36.get(key);
      if (innerQueue === void 0) {
        return pipe(bounded5(options?.bufferSize ?? 16), flatMap18((innerQueue2) => pipe(sync8(() => {
          map36.set(key, innerQueue2);
        }), zipRight7(offer3(outerQueue, of7([key, innerQueue2]))), zipRight7(pipe(offer3(innerQueue2, chunk3(values10)), catchSomeCause2((cause3) => isInterruptedOnly2(cause3) ? some3(_void) : none2()))))));
      }
      return catchSomeCause2(offer3(innerQueue, chunk3(values10)), (cause3) => isInterruptedOnly2(cause3) ? some3(_void) : none2());
    }, {
      discard: true
    })), () => loop4(map36, outerQueue)), "onInput"),
    onFailure: /* @__PURE__ */ __name((cause3) => fromEffect5(offer3(outerQueue, failCause14(cause3))), "onFailure"),
    onDone: /* @__PURE__ */ __name(() => fromEffect5(pipe(forEach16(map36.entries(), ([_, innerQueue]) => pipe(offer3(innerQueue, end4), catchSomeCause2((cause3) => isInterruptedOnly2(cause3) ? some3(_void) : none2())), {
      discard: true
    }), zipRight7(offer3(outerQueue, end4)))), "onDone")
  }), "loop");
  return make71(unwrapScopedWith3((scope6) => gen5(function* () {
    const map36 = /* @__PURE__ */ new Map();
    const queue = yield* unbounded5();
    yield* addFinalizer3(scope6, shutdown4(queue));
    return yield* toChannel2(self).pipe(pipeTo(loop4(map36, queue)), drain, runIn(scope6), forkIn3(scope6), as8(flattenTake(fromQueue3(queue, {
      shutdown: true
    }))));
  })));
});
var groupByIterable = /* @__PURE__ */ dual(2, (iterable, f) => {
  const builder = [];
  const iterator = iterable[Symbol.iterator]();
  const map36 = /* @__PURE__ */ new Map();
  let next4;
  while ((next4 = iterator.next()) && !next4.done) {
    const value2 = next4.value;
    const key = f(value2);
    if (map36.has(key)) {
      const innerBuilder = map36.get(key);
      innerBuilder.push(value2);
    } else {
      const innerBuilder = [value2];
      builder.push([key, innerBuilder]);
      map36.set(key, innerBuilder);
    }
  }
  return unsafeFromArray(builder.map((tuple5) => [tuple5[0], unsafeFromArray(tuple5[1])]));
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Stream.js
var Stream_exports = {};
__export(Stream_exports, {
  DefaultChunkSize: () => DefaultChunkSize2,
  Do: () => Do8,
  StreamTypeId: () => StreamTypeId4,
  accumulate: () => accumulate2,
  accumulateChunks: () => accumulateChunks2,
  acquireRelease: () => acquireRelease5,
  aggregate: () => aggregate2,
  aggregateWithin: () => aggregateWithin2,
  aggregateWithinEither: () => aggregateWithinEither2,
  as: () => as14,
  async: () => _async2,
  asyncEffect: () => asyncEffect4,
  asyncPush: () => asyncPush2,
  asyncScoped: () => asyncScoped2,
  bind: () => bind10,
  bindEffect: () => bindEffect2,
  bindTo: () => bindTo10,
  branchAfter: () => branchAfter2,
  broadcast: () => broadcast2,
  broadcastDynamic: () => broadcastDynamic2,
  broadcastedQueues: () => broadcastedQueues2,
  broadcastedQueuesDynamic: () => broadcastedQueuesDynamic2,
  buffer: () => buffer3,
  bufferChunks: () => bufferChunks2,
  catchAll: () => catchAll9,
  catchAllCause: () => catchAllCause8,
  catchSome: () => catchSome5,
  catchSomeCause: () => catchSomeCause4,
  catchTag: () => catchTag6,
  catchTags: () => catchTags5,
  changes: () => changes2,
  changesWith: () => changesWith2,
  changesWithEffect: () => changesWithEffect2,
  chunks: () => chunks2,
  chunksWith: () => chunksWith2,
  combine: () => combine17,
  combineChunks: () => combineChunks2,
  concat: () => concat4,
  concatAll: () => concatAll3,
  context: () => context10,
  contextWith: () => contextWith8,
  contextWithEffect: () => contextWithEffect6,
  contextWithStream: () => contextWithStream2,
  cross: () => cross2,
  crossLeft: () => crossLeft2,
  crossRight: () => crossRight2,
  crossWith: () => crossWith2,
  debounce: () => debounce2,
  decodeText: () => decodeText2,
  die: () => die15,
  dieMessage: () => dieMessage7,
  dieSync: () => dieSync9,
  distributedWith: () => distributedWith2,
  distributedWithDynamic: () => distributedWithDynamic2,
  drain: () => drain4,
  drainFork: () => drainFork2,
  drop: () => drop7,
  dropRight: () => dropRight4,
  dropUntil: () => dropUntil5,
  dropUntilEffect: () => dropUntilEffect3,
  dropWhile: () => dropWhile7,
  dropWhileEffect: () => dropWhileEffect3,
  either: () => either10,
  empty: () => empty45,
  encodeText: () => encodeText2,
  ensuring: () => ensuring10,
  ensuringWith: () => ensuringWith4,
  execute: () => execute2,
  fail: () => fail21,
  failCause: () => failCause17,
  failCauseSync: () => failCauseSync10,
  failSync: () => failSync11,
  filter: () => filter20,
  filterEffect: () => filterEffect2,
  filterMap: () => filterMap13,
  filterMapEffect: () => filterMapEffect2,
  filterMapWhile: () => filterMapWhile5,
  filterMapWhileEffect: () => filterMapWhileEffect2,
  finalizer: () => finalizer2,
  find: () => find4,
  findEffect: () => findEffect3,
  flatMap: () => flatMap24,
  flatten: () => flatten17,
  flattenChunks: () => flattenChunks2,
  flattenEffect: () => flattenEffect2,
  flattenExitOption: () => flattenExitOption2,
  flattenIterables: () => flattenIterables2,
  flattenTake: () => flattenTake2,
  forever: () => forever7,
  fromAsyncIterable: () => fromAsyncIterable2,
  fromChannel: () => fromChannel3,
  fromChunk: () => fromChunk2,
  fromChunkPubSub: () => fromChunkPubSub2,
  fromChunkQueue: () => fromChunkQueue2,
  fromChunks: () => fromChunks2,
  fromEffect: () => fromEffect10,
  fromEffectOption: () => fromEffectOption2,
  fromEventListener: () => fromEventListener2,
  fromIterable: () => fromIterable18,
  fromIterableEffect: () => fromIterableEffect2,
  fromIteratorSucceed: () => fromIteratorSucceed2,
  fromPubSub: () => fromPubSub4,
  fromPull: () => fromPull3,
  fromQueue: () => fromQueue4,
  fromReadableStream: () => fromReadableStream2,
  fromReadableStreamByob: () => fromReadableStreamByob2,
  fromSchedule: () => fromSchedule2,
  fromTPubSub: () => fromTPubSub2,
  fromTQueue: () => fromTQueue2,
  groupAdjacentBy: () => groupAdjacentBy2,
  groupBy: () => groupBy4,
  groupByKey: () => groupByKey2,
  grouped: () => grouped2,
  groupedWithin: () => groupedWithin2,
  haltAfter: () => haltAfter2,
  haltWhen: () => haltWhen2,
  haltWhenDeferred: () => haltWhenDeferred2,
  identity: () => identity4,
  interleave: () => interleave2,
  interleaveWith: () => interleaveWith2,
  interruptAfter: () => interruptAfter2,
  interruptWhen: () => interruptWhen3,
  interruptWhenDeferred: () => interruptWhenDeferred3,
  intersperse: () => intersperse4,
  intersperseAffixes: () => intersperseAffixes2,
  iterate: () => iterate5,
  let: () => let_10,
  make: () => make72,
  map: () => map34,
  mapAccum: () => mapAccum6,
  mapAccumEffect: () => mapAccumEffect2,
  mapBoth: () => mapBoth10,
  mapChunks: () => mapChunks2,
  mapChunksEffect: () => mapChunksEffect2,
  mapConcat: () => mapConcat2,
  mapConcatChunk: () => mapConcatChunk2,
  mapConcatChunkEffect: () => mapConcatChunkEffect2,
  mapConcatEffect: () => mapConcatEffect2,
  mapEffect: () => mapEffect8,
  mapError: () => mapError11,
  mapErrorCause: () => mapErrorCause7,
  mapInputContext: () => mapInputContext8,
  merge: () => merge11,
  mergeAll: () => mergeAll10,
  mergeEither: () => mergeEither2,
  mergeLeft: () => mergeLeft2,
  mergeRight: () => mergeRight2,
  mergeWith: () => mergeWith4,
  mergeWithTag: () => mergeWithTag2,
  mkString: () => mkString3,
  never: () => never9,
  onDone: () => onDone2,
  onEnd: () => onEnd2,
  onError: () => onError5,
  onStart: () => onStart2,
  orDie: () => orDie9,
  orDieWith: () => orDieWith6,
  orElse: () => orElse14,
  orElseEither: () => orElseEither8,
  orElseFail: () => orElseFail5,
  orElseIfEmpty: () => orElseIfEmpty2,
  orElseIfEmptyChunk: () => orElseIfEmptyChunk2,
  orElseIfEmptyStream: () => orElseIfEmptyStream2,
  orElseSucceed: () => orElseSucceed6,
  paginate: () => paginate2,
  paginateChunk: () => paginateChunk2,
  paginateChunkEffect: () => paginateChunkEffect2,
  paginateEffect: () => paginateEffect2,
  partition: () => partition12,
  partitionEither: () => partitionEither2,
  peel: () => peel2,
  pipeThrough: () => pipeThrough2,
  pipeThroughChannel: () => pipeThroughChannel2,
  pipeThroughChannelOrFail: () => pipeThroughChannelOrFail2,
  prepend: () => prepend7,
  provideContext: () => provideContext9,
  provideLayer: () => provideLayer3,
  provideService: () => provideService11,
  provideServiceEffect: () => provideServiceEffect5,
  provideServiceStream: () => provideServiceStream2,
  provideSomeContext: () => provideSomeContext4,
  provideSomeLayer: () => provideSomeLayer4,
  race: () => race6,
  raceAll: () => raceAll5,
  range: () => range5,
  rechunk: () => rechunk2,
  refineOrDie: () => refineOrDie4,
  refineOrDieWith: () => refineOrDieWith4,
  repeat: () => repeat5,
  repeatEffect: () => repeatEffect2,
  repeatEffectChunk: () => repeatEffectChunk2,
  repeatEffectChunkOption: () => repeatEffectChunkOption2,
  repeatEffectOption: () => repeatEffectOption2,
  repeatEffectWithSchedule: () => repeatEffectWithSchedule2,
  repeatEither: () => repeatEither2,
  repeatElements: () => repeatElements2,
  repeatElementsWith: () => repeatElementsWith2,
  repeatValue: () => repeatValue2,
  repeatWith: () => repeatWith2,
  retry: () => retry8,
  run: () => run5,
  runCollect: () => runCollect3,
  runCount: () => runCount2,
  runDrain: () => runDrain3,
  runFold: () => runFold2,
  runFoldEffect: () => runFoldEffect2,
  runFoldScoped: () => runFoldScoped2,
  runFoldScopedEffect: () => runFoldScopedEffect2,
  runFoldWhile: () => runFoldWhile2,
  runFoldWhileEffect: () => runFoldWhileEffect2,
  runFoldWhileScoped: () => runFoldWhileScoped2,
  runFoldWhileScopedEffect: () => runFoldWhileScopedEffect2,
  runForEach: () => runForEach2,
  runForEachChunk: () => runForEachChunk2,
  runForEachChunkScoped: () => runForEachChunkScoped2,
  runForEachScoped: () => runForEachScoped2,
  runForEachWhile: () => runForEachWhile2,
  runForEachWhileScoped: () => runForEachWhileScoped2,
  runHead: () => runHead2,
  runIntoPubSub: () => runIntoPubSub2,
  runIntoPubSubScoped: () => runIntoPubSubScoped2,
  runIntoQueue: () => runIntoQueue2,
  runIntoQueueElementsScoped: () => runIntoQueueElementsScoped2,
  runIntoQueueScoped: () => runIntoQueueScoped2,
  runLast: () => runLast2,
  runScoped: () => runScoped3,
  runSum: () => runSum2,
  scan: () => scan4,
  scanEffect: () => scanEffect2,
  scanReduce: () => scanReduce2,
  scanReduceEffect: () => scanReduceEffect2,
  schedule: () => schedule3,
  scheduleWith: () => scheduleWith2,
  scoped: () => scoped8,
  scopedWith: () => scopedWith5,
  share: () => share2,
  sliding: () => sliding10,
  slidingSize: () => slidingSize2,
  some: () => some18,
  someOrElse: () => someOrElse2,
  someOrFail: () => someOrFail2,
  split: () => split5,
  splitLines: () => splitLines3,
  splitOnChunk: () => splitOnChunk2,
  succeed: () => succeed21,
  suspend: () => suspend11,
  sync: () => sync15,
  take: () => take12,
  takeRight: () => takeRight5,
  takeUntil: () => takeUntil4,
  takeUntilEffect: () => takeUntilEffect2,
  takeWhile: () => takeWhile7,
  tap: () => tap10,
  tapBoth: () => tapBoth5,
  tapError: () => tapError8,
  tapErrorCause: () => tapErrorCause7,
  tapSink: () => tapSink2,
  throttle: () => throttle2,
  throttleEffect: () => throttleEffect2,
  tick: () => tick2,
  timeout: () => timeout5,
  timeoutFail: () => timeoutFail4,
  timeoutFailCause: () => timeoutFailCause4,
  timeoutTo: () => timeoutTo4,
  toAsyncIterable: () => toAsyncIterable2,
  toAsyncIterableEffect: () => toAsyncIterableEffect2,
  toAsyncIterableRuntime: () => toAsyncIterableRuntime2,
  toChannel: () => toChannel3,
  toPubSub: () => toPubSub3,
  toPull: () => toPull3,
  toQueue: () => toQueue3,
  toQueueOfElements: () => toQueueOfElements2,
  toReadableStream: () => toReadableStream2,
  toReadableStreamEffect: () => toReadableStreamEffect2,
  toReadableStreamRuntime: () => toReadableStreamRuntime2,
  transduce: () => transduce2,
  unfold: () => unfold6,
  unfoldChunk: () => unfoldChunk2,
  unfoldChunkEffect: () => unfoldChunkEffect2,
  unfoldEffect: () => unfoldEffect2,
  unwrap: () => unwrap5,
  unwrapScoped: () => unwrapScoped6,
  unwrapScopedWith: () => unwrapScopedWith4,
  updateService: () => updateService7,
  void: () => void_12,
  when: () => when6,
  whenCase: () => whenCase2,
  whenCaseEffect: () => whenCaseEffect2,
  whenEffect: () => whenEffect4,
  withExecutionPlan: () => withExecutionPlan4,
  withSpan: () => withSpan7,
  zip: () => zip17,
  zipAll: () => zipAll2,
  zipAllLeft: () => zipAllLeft2,
  zipAllRight: () => zipAllRight2,
  zipAllSortedByKey: () => zipAllSortedByKey2,
  zipAllSortedByKeyLeft: () => zipAllSortedByKeyLeft2,
  zipAllSortedByKeyRight: () => zipAllSortedByKeyRight2,
  zipAllSortedByKeyWith: () => zipAllSortedByKeyWith2,
  zipAllWith: () => zipAllWith2,
  zipFlatten: () => zipFlatten3,
  zipLatest: () => zipLatest2,
  zipLatestAll: () => zipLatestAll2,
  zipLatestWith: () => zipLatestWith2,
  zipLeft: () => zipLeft13,
  zipRight: () => zipRight13,
  zipWith: () => zipWith18,
  zipWithChunks: () => zipWithChunks2,
  zipWithIndex: () => zipWithIndex2,
  zipWithNext: () => zipWithNext2,
  zipWithPrevious: () => zipWithPrevious2,
  zipWithPreviousAndNext: () => zipWithPreviousAndNext2
});
var StreamTypeId4 = StreamTypeId3;
var DefaultChunkSize2 = DefaultChunkSize;
var accumulate2 = accumulate;
var accumulateChunks2 = accumulateChunks;
var acquireRelease5 = acquireRelease4;
var aggregate2 = aggregate;
var aggregateWithin2 = aggregateWithin;
var aggregateWithinEither2 = aggregateWithinEither;
var as14 = as13;
var _async2 = _async;
var asyncEffect4 = asyncEffect3;
var asyncPush2 = asyncPush;
var asyncScoped2 = asyncScoped;
var branchAfter2 = branchAfter;
var broadcast2 = broadcast;
var share2 = share;
var broadcastDynamic2 = broadcastDynamic;
var broadcastedQueues2 = broadcastedQueues;
var broadcastedQueuesDynamic2 = broadcastedQueuesDynamic;
var buffer3 = buffer2;
var bufferChunks2 = bufferChunks;
var catchAll9 = catchAll8;
var catchAllCause8 = catchAllCause7;
var catchSome5 = catchSome4;
var catchTag6 = catchTag5;
var catchTags5 = catchTags4;
var catchSomeCause4 = catchSomeCause3;
var changes2 = changes;
var changesWith2 = changesWith;
var changesWithEffect2 = changesWithEffect;
var chunks2 = chunks;
var chunksWith2 = chunksWith;
var combine17 = combine16;
var combineChunks2 = combineChunks;
var concat4 = concat3;
var concatAll3 = concatAll2;
var cross2 = cross;
var crossLeft2 = crossLeft;
var crossRight2 = crossRight;
var crossWith2 = crossWith;
var debounce2 = debounce;
var die15 = die14;
var dieSync9 = dieSync8;
var dieMessage7 = dieMessage6;
var distributedWith2 = distributedWith;
var distributedWithDynamic2 = distributedWithDynamic;
var drain4 = drain3;
var drainFork2 = drainFork;
var drop7 = drop6;
var dropRight4 = dropRight3;
var dropUntil5 = dropUntil4;
var dropUntilEffect3 = dropUntilEffect2;
var dropWhile7 = dropWhile6;
var dropWhileEffect3 = dropWhileEffect2;
var either10 = either9;
var empty45 = empty44;
var ensuring10 = ensuring9;
var ensuringWith4 = ensuringWith3;
var context10 = context9;
var contextWith8 = contextWith7;
var contextWithEffect6 = contextWithEffect5;
var contextWithStream2 = contextWithStream;
var execute2 = execute;
var fail21 = fail20;
var failSync11 = failSync10;
var failCause17 = failCause16;
var failCauseSync10 = failCauseSync9;
var filter20 = filter18;
var filterEffect2 = filterEffect;
var filterMap13 = filterMap12;
var filterMapEffect2 = filterMapEffect;
var filterMapWhile5 = filterMapWhile4;
var filterMapWhileEffect2 = filterMapWhileEffect;
var finalizer2 = finalizer;
var find4 = find3;
var findEffect3 = findEffect2;
var flatMap24 = flatMap23;
var flatten17 = flatten16;
var flattenChunks2 = flattenChunks;
var flattenEffect2 = flattenEffect;
var flattenExitOption2 = flattenExitOption;
var flattenIterables2 = flattenIterables;
var flattenTake2 = flattenTake;
var forever7 = forever6;
var fromAsyncIterable2 = fromAsyncIterable;
var fromChannel3 = fromChannel2;
var toChannel3 = toChannel2;
var fromChunk2 = fromChunk;
var fromChunkPubSub2 = fromChunkPubSub;
var fromChunkQueue2 = fromChunkQueue;
var fromChunks2 = fromChunks;
var fromEffect10 = fromEffect9;
var fromEffectOption2 = fromEffectOption;
var fromPubSub4 = fromPubSub3;
var fromTPubSub2 = fromTPubSub;
var fromIterable18 = fromIterable17;
var fromIterableEffect2 = fromIterableEffect;
var fromIteratorSucceed2 = fromIteratorSucceed;
var fromPull3 = fromPull2;
var fromQueue4 = fromQueue3;
var fromTQueue2 = fromTQueue;
var fromReadableStream2 = fromReadableStream;
var fromReadableStreamByob2 = fromReadableStreamByob;
var fromSchedule2 = fromSchedule;
var groupAdjacentBy2 = groupAdjacentBy;
var groupBy4 = groupBy3;
var groupByKey2 = groupByKey;
var grouped2 = grouped;
var groupedWithin2 = groupedWithin;
var haltAfter2 = haltAfter;
var haltWhen2 = haltWhen;
var haltWhenDeferred2 = haltWhenDeferred;
var identity4 = identityStream;
var interleave2 = interleave;
var interleaveWith2 = interleaveWith;
var intersperse4 = intersperse3;
var intersperseAffixes2 = intersperseAffixes;
var interruptAfter2 = interruptAfter;
var interruptWhen3 = interruptWhen2;
var interruptWhenDeferred3 = interruptWhenDeferred2;
var iterate5 = iterate4;
var make72 = make70;
var map34 = map33;
var mapAccum6 = mapAccum5;
var mapAccumEffect2 = mapAccumEffect;
var mapBoth10 = mapBoth9;
var mapChunks2 = mapChunks;
var mapChunksEffect2 = mapChunksEffect;
var mapConcat2 = mapConcat;
var mapConcatChunk2 = mapConcatChunk;
var mapConcatChunkEffect2 = mapConcatChunkEffect;
var mapConcatEffect2 = mapConcatEffect;
var mapEffect8 = mapEffectOptions;
var mapError11 = mapError10;
var mapErrorCause7 = mapErrorCause6;
var merge11 = merge10;
var mergeAll10 = mergeAll9;
var mergeWithTag2 = mergeWithTag;
var mergeWith4 = mergeWith3;
var mergeEither2 = mergeEither;
var mergeLeft2 = mergeLeft;
var mergeRight2 = mergeRight;
var mkString3 = mkString2;
var never9 = never8;
var onEnd2 = onEnd;
var onError5 = onError4;
var onDone2 = onDone;
var onStart2 = onStart;
var orDie9 = orDie8;
var orDieWith6 = orDieWith5;
var orElse14 = orElse13;
var orElseEither8 = orElseEither7;
var orElseFail5 = orElseFail4;
var orElseIfEmpty2 = orElseIfEmpty;
var orElseIfEmptyChunk2 = orElseIfEmptyChunk;
var orElseIfEmptyStream2 = orElseIfEmptyStream;
var orElseSucceed6 = orElseSucceed5;
var paginate2 = paginate;
var paginateChunk2 = paginateChunk;
var paginateChunkEffect2 = paginateChunkEffect;
var paginateEffect2 = paginateEffect;
var partition12 = partition11;
var partitionEither2 = partitionEither;
var peel2 = peel;
var pipeThrough2 = pipeThrough;
var pipeThroughChannel2 = pipeThroughChannel;
var pipeThroughChannelOrFail2 = pipeThroughChannelOrFail;
var prepend7 = prepend6;
var provideContext9 = provideContext8;
var provideSomeContext4 = provideSomeContext3;
var provideLayer3 = provideLayer2;
var provideService11 = provideService10;
var provideServiceEffect5 = provideServiceEffect4;
var provideServiceStream2 = provideServiceStream;
var mapInputContext8 = mapInputContext7;
var provideSomeLayer4 = provideSomeLayer3;
var race6 = race5;
var raceAll5 = raceAll4;
var range5 = range4;
var rechunk2 = rechunk;
var refineOrDie4 = refineOrDie3;
var refineOrDieWith4 = refineOrDieWith3;
var repeat5 = repeat4;
var repeatEffect2 = repeatEffect;
var repeatEffectChunk2 = repeatEffectChunk;
var repeatEffectChunkOption2 = repeatEffectChunkOption;
var repeatEffectOption2 = repeatEffectOption;
var repeatEffectWithSchedule2 = repeatEffectWithSchedule;
var repeatEither2 = repeatEither;
var repeatElements2 = repeatElements;
var repeatElementsWith2 = repeatElementsWith;
var repeatValue2 = repeatValue;
var repeatWith2 = repeatWith;
var retry8 = retry7;
var withExecutionPlan4 = withExecutionPlan3;
var run5 = run4;
var runCollect3 = runCollect2;
var runCount2 = runCount;
var runDrain3 = runDrain2;
var runFold2 = runFold;
var runFoldEffect2 = runFoldEffect;
var runFoldScoped2 = runFoldScoped;
var runFoldScopedEffect2 = runFoldScopedEffect;
var runFoldWhile2 = runFoldWhile;
var runFoldWhileEffect2 = runFoldWhileEffect;
var runFoldWhileScoped2 = runFoldWhileScoped;
var runFoldWhileScopedEffect2 = runFoldWhileScopedEffect;
var runForEach2 = runForEach;
var runForEachChunk2 = runForEachChunk;
var runForEachChunkScoped2 = runForEachChunkScoped;
var runForEachScoped2 = runForEachScoped;
var runForEachWhile2 = runForEachWhile;
var runForEachWhileScoped2 = runForEachWhileScoped;
var runHead2 = runHead;
var runIntoPubSub2 = runIntoPubSub;
var runIntoPubSubScoped2 = runIntoPubSubScoped;
var runIntoQueue2 = runIntoQueue;
var runIntoQueueElementsScoped2 = runIntoQueueElementsScoped;
var runIntoQueueScoped2 = runIntoQueueScoped;
var runLast2 = runLast;
var runScoped3 = runScoped2;
var runSum2 = runSum;
var scan4 = scan3;
var scanEffect2 = scanEffect;
var scanReduce2 = scanReduce;
var scanReduceEffect2 = scanReduceEffect;
var schedule3 = schedule2;
var scheduleWith2 = scheduleWith;
var scoped8 = scoped7;
var scopedWith5 = scopedWith4;
var sliding10 = sliding9;
var slidingSize2 = slidingSize;
var some18 = some17;
var someOrElse2 = someOrElse;
var someOrFail2 = someOrFail;
var split5 = split4;
var splitOnChunk2 = splitOnChunk;
var splitLines3 = splitLines2;
var succeed21 = succeed20;
var sync15 = sync14;
var suspend11 = suspend10;
var take12 = take11;
var takeRight5 = takeRight4;
var takeUntil4 = takeUntil3;
var takeUntilEffect2 = takeUntilEffect;
var takeWhile7 = takeWhile6;
var tap10 = tap9;
var tapBoth5 = tapBoth4;
var tapError8 = tapError7;
var tapErrorCause7 = tapErrorCause6;
var tapSink2 = tapSink;
var throttle2 = throttle;
var throttleEffect2 = throttleEffect;
var tick2 = tick;
var timeout5 = timeout4;
var timeoutFail4 = timeoutFail3;
var timeoutFailCause4 = timeoutFailCause3;
var timeoutTo4 = timeoutTo3;
var toPubSub3 = toPubSub2;
var toPull3 = toPull2;
var toQueue3 = toQueue2;
var toQueueOfElements2 = toQueueOfElements;
var toReadableStream2 = toReadableStream;
var toReadableStreamEffect2 = toReadableStreamEffect;
var toReadableStreamRuntime2 = toReadableStreamRuntime;
var toAsyncIterableRuntime2 = toAsyncIterableRuntime;
var toAsyncIterableEffect2 = toAsyncIterableEffect;
var toAsyncIterable2 = toAsyncIterable;
var transduce2 = transduce;
var unfold6 = unfold5;
var unfoldChunk2 = unfoldChunk;
var unfoldChunkEffect2 = unfoldChunkEffect;
var unfoldEffect2 = unfoldEffect;
var void_12 = void_11;
var unwrap5 = unwrap4;
var unwrapScoped6 = unwrapScoped5;
var unwrapScopedWith4 = unwrapScopedWith3;
var updateService7 = updateService6;
var when6 = when5;
var whenCase2 = whenCase;
var whenCaseEffect2 = whenCaseEffect;
var whenEffect4 = whenEffect3;
var withSpan7 = withSpan6;
var zip17 = zip16;
var zipFlatten3 = zipFlatten2;
var zipAll2 = zipAll;
var zipAllLeft2 = zipAllLeft;
var zipAllRight2 = zipAllRight;
var zipAllSortedByKey2 = zipAllSortedByKey;
var zipAllSortedByKeyLeft2 = zipAllSortedByKeyLeft;
var zipAllSortedByKeyRight2 = zipAllSortedByKeyRight;
var zipAllSortedByKeyWith2 = zipAllSortedByKeyWith;
var zipAllWith2 = zipAllWith;
var zipLatest2 = zipLatest;
var zipLatestAll2 = zipLatestAll;
var zipLatestWith2 = zipLatestWith;
var zipLeft13 = zipLeft12;
var zipRight13 = zipRight12;
var zipWith18 = zipWith17;
var zipWithChunks2 = zipWithChunks;
var zipWithNext2 = zipWithNext;
var zipWithPrevious2 = zipWithPrevious;
var zipWithPreviousAndNext2 = zipWithPreviousAndNext;
var zipWithIndex2 = zipWithIndex;
var Do8 = Do7;
var bind10 = bind9;
var bindEffect2 = bindEffect;
var bindTo10 = bindTo9;
var let_10 = let_9;
var decodeText2 = decodeText;
var encodeText2 = encodeText;
var fromEventListener2 = fromEventListener;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/Subscribable.js
var TypeId28 = /* @__PURE__ */ Symbol.for("effect/Subscribable");
var isSubscribable = /* @__PURE__ */ __name((u) => hasProperty(u, TypeId28), "isSubscribable");
var Proto3 = {
  [TypeId17]: TypeId17,
  [TypeId28]: TypeId28,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make73 = /* @__PURE__ */ __name((options) => Object.assign(Object.create(Proto3), options), "make");
var map35 = /* @__PURE__ */ dual(2, (self, f) => make73({
  get: map26(self.get, f),
  changes: map34(self.changes, f)
}));
var mapEffect9 = /* @__PURE__ */ dual(2, (self, f) => make73({
  get: flatMap18(self.get, f),
  changes: mapEffect8(self.changes, f)
}));
var unwrap6 = /* @__PURE__ */ __name((effect4) => make73({
  get: flatMap18(effect4, (s) => s.get),
  changes: unwrap5(map26(effect4, (s) => s.changes))
}), "unwrap");

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/SubscriptionRef.js
var SubscriptionRef_exports = {};
__export(SubscriptionRef_exports, {
  SubscriptionRefTypeId: () => SubscriptionRefTypeId2,
  get: () => get20,
  getAndSet: () => getAndSet7,
  getAndUpdate: () => getAndUpdate7,
  getAndUpdateEffect: () => getAndUpdateEffect3,
  getAndUpdateSome: () => getAndUpdateSome6,
  getAndUpdateSomeEffect: () => getAndUpdateSomeEffect3,
  make: () => make76,
  modify: () => modify15,
  modifyEffect: () => modifyEffect4,
  modifySome: () => modifySome6,
  modifySomeEffect: () => modifySomeEffect3,
  set: () => set13,
  setAndGet: () => setAndGet6,
  update: () => update10,
  updateAndGet: () => updateAndGet7,
  updateAndGetEffect: () => updateAndGetEffect3,
  updateEffect: () => updateEffect3,
  updateSome: () => updateSome6,
  updateSomeAndGet: () => updateSomeAndGet6,
  updateSomeAndGetEffect: () => updateSomeAndGetEffect2,
  updateSomeEffect: () => updateSomeEffect3
});

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/SynchronizedRef.js
var SynchronizedRefTypeId = SynchronizedTypeId;
var make74 = makeSynchronized;
var get18 = get12;
var getAndSet6 = getAndSet2;
var getAndUpdate6 = getAndUpdate2;
var getAndUpdateEffect2 = getAndUpdateEffect;
var getAndUpdateSome5 = getAndUpdateSome;
var getAndUpdateSomeEffect2 = getAndUpdateSomeEffect;
var modify13 = modify10;
var modifyEffect2 = modifyEffect;
var modifySome5 = modifySome;
var modifySomeEffect2 = modifySomeEffect;
var set11 = set6;
var setAndGet5 = setAndGet2;
var update9 = update3;
var updateEffect2 = updateEffect;
var updateAndGet6 = updateAndGet2;
var updateAndGetEffect2 = updateAndGetEffect;
var updateSome5 = updateSome;
var updateSomeEffect2 = updateSomeEffect;
var updateSomeAndGet5 = updateSomeAndGet;
var updateSomeAndGetEffect = updateSomeAndGetEffectSynchronized;
var unsafeMake14 = unsafeMakeSynchronized;

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/internal/subscriptionRef.js
var SubscriptionRefSymbolKey = "effect/SubscriptionRef";
var SubscriptionRefTypeId = /* @__PURE__ */ Symbol.for(SubscriptionRefSymbolKey);
var subscriptionRefVariance = {
  /* c8 ignore next */
  _A: /* @__PURE__ */ __name((_) => _, "_A")
};
var SubscriptionRefImpl = class extends Class3 {
  static {
    __name(this, "SubscriptionRefImpl");
  }
  ref;
  pubsub;
  semaphore;
  [TypeId17] = TypeId17;
  [TypeId28] = TypeId28;
  [RefTypeId2] = refVariance;
  [SynchronizedRefTypeId] = synchronizedVariance;
  [SubscriptionRefTypeId] = subscriptionRefVariance;
  constructor(ref, pubsub, semaphore) {
    super();
    this.ref = ref;
    this.pubsub = pubsub;
    this.semaphore = semaphore;
    this.get = get13(this.ref);
  }
  commit() {
    return this.get;
  }
  get;
  get changes() {
    return pipe(get13(this.ref), flatMap18((a) => map26(fromPubSub3(this.pubsub, {
      scoped: true
    }), (s) => concat3(make70(a), s))), this.semaphore.withPermits(1), unwrapScoped5);
  }
  modify(f) {
    return this.modifyEffect((a) => succeed13(f(a)));
  }
  modifyEffect(f) {
    return pipe(get13(this.ref), flatMap18(f), flatMap18(([b, a]) => pipe(set7(this.ref, a), as8(b), zipLeft7(publish2(this.pubsub, a)))), this.semaphore.withPermits(1));
  }
};
var get19 = /* @__PURE__ */ __name((self) => get13(self.ref), "get");
var make75 = /* @__PURE__ */ __name((value2) => pipe(all10([unbounded4(), make35(value2), makeSemaphore2(1)]), map26(([pubsub, ref, semaphore]) => new SubscriptionRefImpl(ref, pubsub, semaphore))), "make");
var modify14 = /* @__PURE__ */ dual(2, (self, f) => self.modify(f));
var modifyEffect3 = /* @__PURE__ */ dual(2, (self, f) => self.modifyEffect(f));
var set12 = /* @__PURE__ */ dual(2, (self, value2) => pipe(set7(self.ref, value2), zipLeft7(publish2(self.pubsub, value2)), self.semaphore.withPermits(1)));

// ../../node_modules/.pnpm/effect@3.19.16/node_modules/effect/dist/esm/SubscriptionRef.js
var SubscriptionRefTypeId2 = SubscriptionRefTypeId;
var get20 = get19;
var getAndSet7 = getAndSet3;
var getAndUpdate7 = getAndUpdate3;
var getAndUpdateEffect3 = getAndUpdateEffect2;
var getAndUpdateSome6 = getAndUpdateSome2;
var getAndUpdateSomeEffect3 = getAndUpdateSomeEffect2;
var make76 = make75;
var modify15 = modify14;
var modifyEffect4 = modifyEffect3;
var modifySome6 = modifySome2;
var modifySomeEffect3 = modifySomeEffect2;
var set13 = set12;
var setAndGet6 = setAndGet3;
var update10 = update4;
var updateEffect3 = updateEffect2;
var updateAndGet7 = updateAndGet3;
var updateAndGetEffect3 = updateAndGetEffect2;
var updateSome6 = updateSome2;
var updateSomeEffect3 = updateSomeEffect2;
var updateSomeAndGet6 = updateSomeAndGet2;
var updateSomeAndGetEffect2 = updateSomeAndGetEffect;

// Source/Types/Sandbox.ts
var SandboxNotReadyError = class extends Error {
  static {
    __name(this, "SandboxNotReadyError");
  }
  _tag = "SandboxNotReadyError";
  constructor() {
    super("window.vscode is not initialized. Preload script not executed.");
  }
};
var IPCChannelError = class extends Error {
  constructor(channel, cause3) {
    super(`IPC channel '${channel}' error: ${String(cause3)}`);
    this.channel = channel;
    this.cause = cause3;
  }
  static {
    __name(this, "IPCChannelError");
  }
  _tag = "IPCChannelError";
};
var ConfigurationNotReadyError = class extends Error {
  static {
    __name(this, "ConfigurationNotReadyError");
  }
  _tag = "ConfigurationNotReadyError";
  constructor() {
    super("Configuration not yet resolved from preload");
  }
};

// Source/Effect/IPC.ts
var IPCInvokeError = class _IPCInvokeError extends Error {
  static {
    __name(this, "IPCInvokeError");
  }
  _tag = "IPCInvokeError";
  _channel;
  _cause;
  constructor(channel, cause3) {
    super(`IPC invoke failed on channel '${channel}': ${String(cause3)}`);
    this._channel = channel;
    this._cause = cause3;
    Object.setPrototypeOf(this, _IPCInvokeError.prototype);
  }
  get name() {
    return "IPCInvokeError";
  }
  get channel() {
    return this._channel;
  }
  get cause() {
    return this._cause;
  }
};
var IPCSendError = class _IPCSendError extends Error {
  static {
    __name(this, "IPCSendError");
  }
  _tag = "IPCSendError";
  _channel;
  _cause;
  constructor(channel, cause3) {
    super(`IPC send failed on channel '${channel}': ${String(cause3)}`);
    this._channel = channel;
    this._cause = cause3;
    Object.setPrototypeOf(this, _IPCSendError.prototype);
  }
  get name() {
    return "IPCSendError";
  }
  get channel() {
    return this._channel;
  }
  get cause() {
    return this._cause;
  }
};
var IPCSubscriptionError = class _IPCSubscriptionError extends Error {
  static {
    __name(this, "IPCSubscriptionError");
  }
  _tag = "IPCSubscriptionError";
  _channel;
  _cause;
  constructor(channel, cause3) {
    super(
      `IPC subscription failed on channel '${channel}': ${String(cause3)}`
    );
    this._channel = channel;
    this._cause = cause3;
    Object.setPrototypeOf(this, _IPCSubscriptionError.prototype);
  }
  get name() {
    return "IPCSubscriptionError";
  }
  get channel() {
    return this._channel;
  }
  get cause() {
    return this._cause;
  }
};
var IPCTag = class extends Context_exports.Tag("IPC")() {
  static {
    __name(this, "IPCTag");
  }
};
var IPC = IPCTag;
var IPCTauriLive = Layer_exports.effect(
  IPC,
  Effect_exports.gen(function* () {
    const isTauriAvailable = typeof window !== "undefined" && window.__TAURI__ !== void 0;
    if (!isTauriAvailable) {
      return yield* Effect_exports.die(new SandboxNotReadyError());
    }
    const send = /* @__PURE__ */ __name((channel) => (args2) => Effect_exports.try({
      try: /* @__PURE__ */ __name(() => emit(channel, args2.length === 1 ? args2[0] : args2), "try"),
      catch: /* @__PURE__ */ __name((error2) => new IPCSendError(channel, error2), "catch")
    }).pipe(
      Effect_exports.mapError((error2) => new IPCSendError(channel, error2))
    ), "send");
    const invoke_ = /* @__PURE__ */ __name((channel) => (args2) => Effect_exports.tryPromise({
      try: /* @__PURE__ */ __name(() => {
        const invokeArgs = args2.length === 1 ? args2[0] : args2;
        return invoke(channel, invokeArgs);
      }, "try"),
      catch: /* @__PURE__ */ __name((error2) => new IPCInvokeError(channel, error2), "catch")
    }), "invoke_");
    const events = /* @__PURE__ */ __name((channel) => Stream_exports.async((emit4) => {
      let cleanup;
      listen(channel, (event) => {
        emit4.single({
          channel,
          args: [event.payload]
        });
      }).then((unlisten) => {
        cleanup = unlisten;
      }).catch((error2) => {
        emit4.fail(new IPCSubscriptionError(channel, error2));
      });
      return Effect_exports.sync(() => cleanup?.());
    }), "events");
    const once6 = /* @__PURE__ */ __name((channel) => Effect_exports.async((resume2) => {
      listen(channel, (event) => {
        resume2(
          Effect_exports.succeed({
            channel,
            args: [event.payload]
          })
        );
      }).catch((error2) => {
        resume2(
          Effect_exports.fail(new IPCSubscriptionError(channel, error2))
        );
      });
    }), "once");
    const removeAllListeners = /* @__PURE__ */ __name((channel) => Effect_exports.log(`[IPC] Remove all listeners for ${channel}`).pipe(
      Effect_exports.map(() => void 0)
    ), "removeAllListeners");
    return {
      send,
      invoke: invoke_,
      events,
      once: once6,
      removeAllListeners
    };
  })
);
var IPCElectronLive = Layer_exports.effect(
  IPC,
  Effect_exports.gen(function* () {
    const vscode = window.vscode;
    if (!vscode?.ipcRenderer) {
      return yield* Effect_exports.die(new SandboxNotReadyError());
    }
    const { ipcRenderer } = vscode;
    const send = /* @__PURE__ */ __name((channel) => (args2) => Effect_exports.sync(() => {
      ipcRenderer.send(channel, ...args2);
    }).pipe(
      Effect_exports.mapError((error2) => new IPCSendError(channel, error2))
    ), "send");
    const invoke_ = /* @__PURE__ */ __name((channel) => (args2) => Effect_exports.tryPromise({
      try: /* @__PURE__ */ __name(() => ipcRenderer.invoke(channel, ...args2), "try"),
      catch: /* @__PURE__ */ __name((error2) => new IPCInvokeError(channel, error2), "catch")
    }), "invoke_");
    const events = /* @__PURE__ */ __name((channel) => Stream_exports.async((emit4) => {
      const listener = /* @__PURE__ */ __name((_event, ...args2) => {
        emit4.single({ channel, args: args2 });
      }, "listener");
      ipcRenderer.on(channel, listener);
      return Effect_exports.sync(() => {
        ipcRenderer.removeListener(channel, listener);
      });
    }), "events");
    const once6 = /* @__PURE__ */ __name((channel) => Effect_exports.async((resume2) => {
      const listener = /* @__PURE__ */ __name((_event, ...args2) => {
        resume2(Effect_exports.succeed({ channel, args: args2 }));
      }, "listener");
      ipcRenderer.once(channel, listener);
    }), "once");
    const removeAllListeners = /* @__PURE__ */ __name((channel) => Effect_exports.sync(() => {
      ipcRenderer.removeAllListeners(channel);
    }), "removeAllListeners");
    return {
      send,
      invoke: invoke_,
      events,
      once: once6,
      removeAllListeners
    };
  })
);
var IPCMockLive = Layer_exports.succeed(IPC, {
  send: /* @__PURE__ */ __name((_channel) => (_args) => Effect_exports.void, "send"),
  invoke: /* @__PURE__ */ __name((_channel) => (_args) => Effect_exports.succeed({}), "invoke"),
  events: /* @__PURE__ */ __name((_channel) => Stream_exports.empty, "events"),
  once: /* @__PURE__ */ __name((_channel) => Effect_exports.succeed({ channel: _channel, args: [] }), "once"),
  removeAllListeners: /* @__PURE__ */ __name((_channel) => Effect_exports.void, "removeAllListeners")
});

// Source/Effect/Sandbox.ts
var Sandbox = Context_exports.GenericTag("Sandbox");
var SandboxLive = Layer_exports.effect(
  Sandbox,
  Effect_exports.gen(function* () {
    const checkReady = Effect_exports.sync(() => {
      const vscode = window.vscode;
      return vscode && typeof vscode === "object";
    });
    const getGlobals = Effect_exports.sync(() => {
      const vscode = window.vscode;
      if (!vscode) throw new SandboxNotReadyError();
      return vscode;
    }).pipe(Effect_exports.mapError(() => new SandboxNotReadyError()));
    const awaitReady = Effect_exports.async(
      (resume2) => {
        const vscode = window.vscode;
        if (vscode) {
          resume2(Effect_exports.succeed(vscode));
          return;
        }
        const handler = /* @__PURE__ */ __name(() => {
          const vscode2 = window.vscode;
          if (vscode2) {
            resume2(Effect_exports.succeed(vscode2));
          } else {
            resume2(Effect_exports.fail(new SandboxNotReadyError()));
          }
        }, "handler");
        window.addEventListener("vscode-wind-preload-ready", handler, {
          once: true
        });
        setTimeout(() => {
          resume2(Effect_exports.fail(new SandboxNotReadyError()));
        }, 3e4);
        return Effect_exports.sync(() => {
          window.removeEventListener(
            "vscode-wind-preload-ready",
            handler
          );
        });
      }
    ).pipe(
      Effect_exports.timeout("30 seconds"),
      Effect_exports.mapError(() => new SandboxNotReadyError())
    );
    const ipc = Effect_exports.gen(function* () {
      const g = yield* getGlobals;
      if (!g.ipcRenderer) {
        return yield* Effect_exports.fail(new SandboxNotReadyError());
      }
      return g.ipcRenderer;
    });
    const configuration = Effect_exports.gen(function* () {
      const g = yield* getGlobals;
      if (!g.context) {
        return yield* Effect_exports.fail(new SandboxNotReadyError());
      }
      return g.context;
    });
    const resolveConfiguration = Effect_exports.gen(function* () {
      const ctx = yield* configuration;
      return yield* Effect_exports.tryPromise({
        try: /* @__PURE__ */ __name(() => ctx.resolveConfiguration(), "try"),
        catch: /* @__PURE__ */ __name(() => new ConfigurationNotReadyError(), "catch")
      });
    }).pipe(
      Effect_exports.catchAll(
        (error2) => error2 instanceof SandboxNotReadyError ? Effect_exports.fail(new ConfigurationNotReadyError()) : Effect_exports.fail(error2)
      )
    );
    return {
      globals: getGlobals,
      isReady: checkReady,
      awaitReady,
      ipc,
      configuration,
      resolveConfiguration
    };
  })
);
var SandboxMockLive = Layer_exports.succeed(Sandbox, {
  globals: Effect_exports.die(new SandboxNotReadyError()),
  isReady: Effect_exports.succeed(false),
  awaitReady: Effect_exports.die(new SandboxNotReadyError()),
  ipc: Effect_exports.die(new SandboxNotReadyError()),
  configuration: Effect_exports.die(new SandboxNotReadyError()),
  resolveConfiguration: Effect_exports.fail(new ConfigurationNotReadyError())
});

// Source/Effect/Telemetry.ts
var TelemetryCollectionError = class extends Error {
  constructor(operation, cause3) {
    super(
      `Telemetry collection failed for '${operation}': ${String(cause3)}`
    );
    this.operation = operation;
    this.cause = cause3;
  }
  static {
    __name(this, "TelemetryCollectionError");
  }
  _tag = "TelemetryCollectionError";
};
var TelemetryTag = class extends Context_exports.Tag("Telemetry")() {
  static {
    __name(this, "TelemetryTag");
  }
};
var Telemetry = TelemetryTag;
var TelemetryLive = Layer_exports.effect(
  Telemetry,
  Effect_exports.gen(function* () {
    const metricsRef = yield* SubscriptionRef_exports.make(HashMap_exports.empty());
    const spansRef = yield* SubscriptionRef_exports.make(HashMap_exports.empty());
    const eventsRef = yield* SubscriptionRef_exports.make([]);
    const recordMetric = /* @__PURE__ */ __name((name, value2, labels) => Effect_exports.gen(function* () {
      void name;
      void value2;
      const metric = {
        name,
        value: value2,
        timestamp: Date.now(),
        labels: labels ?? {}
      };
      const currentMetrics = yield* metricsRef.get;
      const existing = HashMap_exports.get(currentMetrics, name).pipe(Effect_exports.runSync) || [];
      yield* SubscriptionRef_exports.set(
        metricsRef,
        HashMap_exports.set(
          currentMetrics,
          name,
          [...existing, metric].slice(-1e3)
        )
      );
      const currentEvents = yield* eventsRef.get;
      yield* SubscriptionRef_exports.set(
        eventsRef,
        [
          ...currentEvents,
          {
            type: "metric",
            timestamp: Date.now(),
            data: metric
          }
        ].slice(-1e4)
      );
      console.log(`[Telemetry] Metric: ${name} = ${value2}`);
    }), "recordMetric");
    const startSpan = /* @__PURE__ */ __name((name, labels) => Effect_exports.sync(() => {
      const startTime = Date.now();
      const end6 = /* @__PURE__ */ __name((success, error2) => Effect_exports.gen(function* () {
        const endTime = Date.now();
        const span4 = {
          name,
          startTime,
          endTime,
          duration: endTime - startTime,
          success,
          error: error2 ?? "",
          labels: labels ?? {}
        };
        const currentSpans = yield* spansRef.get;
        const existing = HashMap_exports.get(currentSpans, name).pipe(Effect_exports.runSync) || [];
        yield* SubscriptionRef_exports.set(
          spansRef,
          HashMap_exports.set(
            currentSpans,
            name,
            [...existing, span4].slice(-1e3)
          )
        );
        const currentEvents = yield* eventsRef.get;
        yield* SubscriptionRef_exports.set(
          eventsRef,
          [
            ...currentEvents,
            {
              type: "span",
              timestamp: Date.now(),
              data: span4
            }
          ].slice(-1e4)
        );
        console.log(
          `[Telemetry] Span: ${name} completed in ${span4.duration}ms (success: ${success})`
        );
      }), "end");
      return { end: end6 };
    }), "startSpan");
    const log4 = /* @__PURE__ */ __name((level, message, context11) => Effect_exports.gen(function* () {
      const logEntry = {
        level,
        message,
        context: context11 ?? {}
      };
      const currentEvents = yield* eventsRef.get;
      yield* SubscriptionRef_exports.set(
        eventsRef,
        [
          ...currentEvents,
          {
            type: "log",
            timestamp: Date.now(),
            data: logEntry
          }
        ].slice(-1e4)
      );
      const consoleMethod = level === "error" ? console.error : level === "warn" ? console.warn : level === "debug" ? console.debug : console.log;
      consoleMethod(
        `[Telemetry] [${level.toUpperCase()}] ${message}`,
        context11 ?? {}
      );
    }), "log");
    const events = eventsRef.changes;
    const getMetrics = /* @__PURE__ */ __name((name) => metricsRef.get.pipe(
      Effect_exports.map(
        (map36) => HashMap_exports.get(map36, name).pipe(Effect_exports.runSync) || []
      )
    ), "getMetrics");
    const getAverageDuration = /* @__PURE__ */ __name((name) => spansRef.get.pipe(
      Effect_exports.map((map36) => {
        const spans = HashMap_exports.get(map36, name).pipe(Effect_exports.runSync) || [];
        if (spans.length === 0) return 0;
        const total = spans.reduce(
          (sum4, s) => sum4 + (s.duration || 0),
          0
        );
        return total / spans.length;
      })
    ), "getAverageDuration");
    const getSuccessRate = /* @__PURE__ */ __name((name) => spansRef.get.pipe(
      Effect_exports.map((map36) => {
        const spans = HashMap_exports.get(map36, name).pipe(Effect_exports.runSync) || [];
        if (spans.length === 0) return 0;
        const successful = spans.filter((s) => s.success).length;
        return successful / spans.length;
      })
    ), "getSuccessRate");
    const flush = Effect_exports.void;
    yield* Effect_exports.log("[Telemetry] Telemetry service initialized");
    return {
      recordMetric,
      startSpan,
      log: log4,
      events,
      getMetrics,
      getAverageDuration,
      getSuccessRate,
      flush
    };
  })
);
var withSpan8 = /* @__PURE__ */ __name((name, effect4, labels) => Effect_exports.gen(function* () {
  const telemetry = yield* Telemetry;
  const span4 = yield* telemetry.startSpan(name, labels);
  return yield* effect4.pipe(
    Effect_exports.tap(() => span4.end(true)),
    Effect_exports.catchAll(
      (error2) => Effect_exports.gen(function* () {
        yield* span4.end(false, String(error2));
        return yield* Effect_exports.fail(error2);
      })
    )
  );
}), "withSpan");
var withMetric3 = /* @__PURE__ */ __name((name, effect4, labels) => Effect_exports.gen(function* () {
  const telemetry = yield* Telemetry;
  const startTime = Date.now();
  return yield* effect4.pipe(
    Effect_exports.tap(
      () => telemetry.recordMetric(
        `${name}_duration`,
        Date.now() - startTime,
        labels
      )
    ),
    Effect_exports.tapError(
      (error2) => telemetry.log("error", `${name} failed`, {
        error: String(error2)
      })
    )
  );
}), "withMetric");
var TelemetryMockLive = Layer_exports.succeed(Telemetry, {
  recordMetric: /* @__PURE__ */ __name(() => Effect_exports.void, "recordMetric"),
  startSpan: /* @__PURE__ */ __name(() => Effect_exports.succeed({ end: /* @__PURE__ */ __name(() => Effect_exports.void, "end") }), "startSpan"),
  log: /* @__PURE__ */ __name(() => Effect_exports.void, "log"),
  events: Stream_exports.empty,
  getMetrics: /* @__PURE__ */ __name(() => Effect_exports.succeed([]), "getMetrics"),
  getAverageDuration: /* @__PURE__ */ __name(() => Effect_exports.succeed(0), "getAverageDuration"),
  getSuccessRate: /* @__PURE__ */ __name(() => Effect_exports.succeed(0), "getSuccessRate"),
  flush: Effect_exports.void
});

// Source/Effect/Mountain.ts
var MountainConnectionError = class extends Error {
  static {
    __name(this, "MountainConnectionError");
  }
  _tag = "MountainConnectionError";
  cause;
  constructor(cause3) {
    super(`Failed to connect to Mountain backend: ${String(cause3)}`);
  }
};
var MountainRPCError = class extends Error {
  static {
    __name(this, "MountainRPCError");
  }
  _tag = "MountainRPCError";
  method;
  cause;
  constructor(method, cause3) {
    super(`Mountain RPC '${method}' failed: ${String(cause3)}`);
    this.method = method;
  }
};
var MountainSyncError = class extends Error {
  static {
    __name(this, "MountainSyncError");
  }
  _tag = "MountainSyncError";
  resource;
  cause;
  constructor(resource, cause3) {
    super(`Mountain sync for '${resource}' failed: ${String(cause3)}`);
    this.resource = resource;
  }
};
var MountainStateError = class extends Error {
  static {
    __name(this, "MountainStateError");
  }
  _tag = "MountainStateError";
  expected;
  actual;
  constructor(expected, actual) {
    super(`Mountain state error: expected ${expected}, got ${actual}`);
    this.expected = expected;
    this.actual = actual;
  }
};
var MountainTag = class extends Context_exports.Tag("Mountain")() {
  static {
    __name(this, "MountainTag");
  }
};
var Mountain = MountainTag;
var MountainLive = Layer_exports.effect(
  Mountain,
  Effect_exports.gen(function* () {
    const ipc = yield* IPC;
    const config2 = yield* Configuration;
    const telemetry = yield* Telemetry;
    const stateRef = yield* SubscriptionRef_exports.make({
      _tag: "Idle"
    });
    const syncEventsRef = yield* SubscriptionRef_exports.make([]);
    const retrySchedule = Schedule_exports.exponential("100 millis").pipe(
      Schedule_exports.union(Schedule_exports.spaced("5 seconds")),
      Schedule_exports.intersect(Schedule_exports.recurs(10))
    );
    const withSpanLocal = /* @__PURE__ */ __name((name, effect4) => Effect_exports.gen(function* () {
      const span4 = yield* telemetry.startSpan(name);
      return yield* effect4.pipe(
        Effect_exports.tap(() => span4.end(true)),
        Effect_exports.catchAll(
          (error2) => Effect_exports.gen(function* () {
            const errorMsg = error2 instanceof Error ? error2.message : String(error2);
            yield* span4.end(false, errorMsg);
            return yield* Effect_exports.fail(error2);
          })
        )
      );
    }), "withSpanLocal");
    const setState = /* @__PURE__ */ __name((state) => Effect_exports.gen(function* () {
      yield* SubscriptionRef_exports.modify(stateRef, () => [void 0, state]);
      yield* telemetry.log("info", `Mountain state: ${state._tag}`);
    }), "setState");
    const connectionState = stateRef.get;
    const connectionChanges = stateRef.changes;
    const connect = Effect_exports.gen(function* () {
      yield* setState({ _tag: "Connecting", attempt: 1 });
      const connectionEffect = Effect_exports.gen(function* () {
        const status3 = yield* ipc.invoke("mountain_get_status")([]).pipe(
          Effect_exports.map((result) => {
            const apiStatus = result;
            return {
              connected: apiStatus.connected ?? false,
              version: apiStatus.version ?? "unknown"
            };
          }),
          Effect_exports.mapError(
            (error2) => new MountainConnectionError(error2)
          )
        );
        if (!status3.connected) {
          yield* Effect_exports.fail(
            new MountainConnectionError("Mountain not ready")
          );
        }
        yield* setState({
          _tag: "Connected",
          version: status3.version
        });
        yield* telemetry.log(
          "info",
          `Connected to Mountain v${status3.version}`
        );
      });
      return yield* Effect_exports.retry(
        withSpanLocal("mountain_connect", connectionEffect),
        retrySchedule
      ).pipe(
        Effect_exports.catchAll(
          (error2) => Effect_exports.gen(function* () {
            const errorObj = error2 instanceof Error ? error2 : new Error(String(error2));
            yield* setState({ _tag: "Error", error: errorObj });
            yield* telemetry.log(
              "error",
              `Failed to connect: ${errorObj.message}`
            );
            yield* Effect_exports.fail(error2);
          })
        )
      );
    });
    const disconnect3 = Effect_exports.gen(function* () {
      yield* setState({ _tag: "Disconnected", reason: "manual" });
      yield* telemetry.log("info", "Disconnected from Mountain");
    });
    const rpc = /* @__PURE__ */ __name((method) => (args2) => Effect_exports.gen(function* () {
      const currentState = yield* stateRef.get;
      if (currentState._tag !== "Connected") {
        yield* connect;
      }
      const span4 = yield* telemetry.startSpan(`rpc_${method}`);
      return yield* ipc.invoke(method)(args2 ? [args2] : []).pipe(
        Effect_exports.mapError(
          (error2) => new MountainRPCError(method, error2)
        ),
        Effect_exports.tap(() => span4.end(true)),
        Effect_exports.catchAll(
          (error2) => Effect_exports.gen(function* () {
            const errorMessage = error2 instanceof Error ? error2.message : String(error2);
            yield* span4.end(false, errorMessage);
            if (errorMessage.includes("connection") || errorMessage.includes("network")) {
              yield* setState({
                _tag: "Disconnected",
                reason: "connection_lost"
              });
            }
            yield* Effect_exports.fail(error2);
          })
        )
      );
    }), "rpc");
    const sync16 = /* @__PURE__ */ __name((resourceType) => Effect_exports.gen(function* () {
      const span4 = yield* telemetry.startSpan(`sync_${resourceType}`);
      const startTime = Date.now();
      yield* telemetry.log(
        "info",
        `Starting sync for ${resourceType}`
      );
      const result = yield* Effect_exports.gen(function* () {
        switch (resourceType) {
          case "configuration": {
            const mountainConfig = yield* rpc("mountain_get_configuration")();
            const localConfig = yield* config2.get;
            const mountainHash = JSON.stringify(mountainConfig);
            const localHash = JSON.stringify(localConfig);
            if (mountainHash !== localHash) {
              yield* config2.apply(mountainConfig);
              const resource = {
                type: "configuration",
                id: "main",
                data: mountainConfig,
                timestamp: Date.now(),
                hash: mountainHash
              };
              yield* SubscriptionRef_exports.modify(syncEventsRef, (events) => [void 0, [...events, resource].slice(-1e3)]);
            }
            return {
              success: true,
              resourcesSynced: 1,
              errors: []
            };
          }
          case "services": {
            const services = yield* rpc("mountain_get_services_status")();
            const resource = {
              type: "services",
              id: "all",
              data: services,
              timestamp: Date.now(),
              hash: JSON.stringify(services)
            };
            yield* SubscriptionRef_exports.modify(syncEventsRef, (events) => [void 0, [...events, resource].slice(-1e3)]);
            return {
              success: true,
              resourcesSynced: Object.keys(services).length,
              errors: []
            };
          }
          case "state": {
            const state = yield* rpc("mountain_get_state")();
            const resource = {
              type: "state",
              id: "main",
              data: state,
              timestamp: Date.now(),
              hash: JSON.stringify(state)
            };
            yield* SubscriptionRef_exports.modify(syncEventsRef, (events) => [void 0, [...events, resource].slice(-1e3)]);
            return {
              success: true,
              resourcesSynced: 1,
              errors: []
            };
          }
          default:
            return {
              success: false,
              resourcesSynced: 0,
              errors: [
                `Unknown resource type: ${resourceType}`
              ]
            };
        }
      }).pipe(
        Effect_exports.tap(
          (result2) => span4.end(result2.success, result2.errors[0])
        ),
        Effect_exports.catchAll(
          (error2) => Effect_exports.gen(function* () {
            const errorMessage = error2 instanceof Error ? error2.message : String(error2);
            yield* span4.end(false, errorMessage);
            yield* Effect_exports.fail(
              new MountainSyncError(resourceType, error2)
            );
          })
        )
      );
      const duration3 = Date.now() - startTime;
      return {
        ...result,
        duration: duration3
      };
    }), "sync");
    const syncEvents = syncEventsRef.changes.pipe(
      Stream_exports.flatMap((events) => Stream_exports.fromIterable(events))
    );
    const version = Effect_exports.gen(function* () {
      const status3 = yield* ipc.invoke("mountain_get_status")([]).pipe(
        Effect_exports.map((result) => {
          const apiStatus = result;
          return { version: apiStatus.version ?? "unknown" };
        }),
        Effect_exports.mapError((error2) => new MountainConnectionError(error2))
      );
      return status3.version;
    });
    const healthCheck = Effect_exports.gen(function* () {
      return yield* Effect_exports.orElse(
        rpc("mountain_get_status")().pipe(
          Effect_exports.map((status3) => status3.connected === true)
        ),
        () => Effect_exports.succeed(false)
      );
    });
    const setupBackgroundSync = Effect_exports.gen(function* () {
      yield* Stream_exports.runForEach(
        connectionChanges,
        (state) => state._tag === "Connected" ? Effect_exports.gen(function* () {
          yield* telemetry.log(
            "info",
            "Starting background sync"
          );
          yield* sync16("configuration").pipe(
            Effect_exports.catchAll(
              (error2) => telemetry.log(
                "error",
                `Initial config sync failed: ${error2.message}`
              )
            )
          );
          const syncFiber = yield* Stream_exports.fromSchedule(
            Schedule_exports.spaced("5 seconds")
          ).pipe(
            Stream_exports.runForEach(
              () => sync16("configuration").pipe(
                Effect_exports.catchAll(
                  (error2) => telemetry.log(
                    "error",
                    `Periodic sync failed: ${error2.message}`
                  )
                )
              )
            ),
            Effect_exports.fork
          );
          yield* connectionChanges.pipe(
            Stream_exports.filter(
              (s) => s._tag === "Disconnected" || s._tag === "Error"
            ),
            Stream_exports.runForEach(
              () => Fiber_exports.interrupt(syncFiber)
            )
          );
        }) : Effect_exports.void
      );
    }).pipe(Effect_exports.fork);
    yield* setupBackgroundSync;
    yield* telemetry.log("info", "Mountain service initialized");
    return {
      connectionState,
      connectionChanges,
      connect,
      disconnect: disconnect3,
      rpc,
      sync: sync16,
      syncEvents,
      version,
      healthCheck
    };
  })
);
var MountainMockLive = Layer_exports.succeed(Mountain, {
  connectionState: Effect_exports.succeed({
    _tag: "Connected",
    version: "mock"
  }),
  connectionChanges: Stream_exports.empty,
  connect: Effect_exports.void,
  disconnect: Effect_exports.void,
  rpc: /* @__PURE__ */ __name(() => () => Effect_exports.succeed({}), "rpc"),
  sync: /* @__PURE__ */ __name(() => Effect_exports.succeed({
    success: true,
    resourcesSynced: 0,
    errors: [],
    duration: 0
  }), "sync"),
  syncEvents: Stream_exports.empty,
  version: Effect_exports.succeed("mock"),
  healthCheck: Effect_exports.succeed(true)
});

// Source/Effect/Configuration.ts
var ConfigFetchError = class extends Error {
  constructor(cause3) {
    super(`Failed to fetch configuration: ${String(cause3)}`);
    this.cause = cause3;
  }
  static {
    __name(this, "ConfigFetchError");
  }
  _tag = "ConfigFetchError";
};
var ConfigValidationError = class extends Error {
  constructor(issues) {
    super(`Configuration validation failed: ${issues.join(", ")}`);
    this.issues = issues;
  }
  static {
    __name(this, "ConfigValidationError");
  }
  _tag = "ConfigValidationError";
};
var ConfigApplyError = class extends Error {
  constructor(key, cause3) {
    super(`Failed to apply configuration for '${key}': ${String(cause3)}`);
    this.key = key;
    this.cause = cause3;
  }
  static {
    __name(this, "ConfigApplyError");
  }
  _tag = "ConfigApplyError";
};
var validateConfig = /* @__PURE__ */ __name((config2) => {
  const issues = [];
  if (!config2 || typeof config2 !== "object") {
    issues.push({ path: "", message: "Configuration must be an object" });
    return issues;
  }
  const cfg = config2;
  if (cfg["zoomLevel"] !== void 0) {
    if (typeof cfg["zoomLevel"] !== "number") {
      issues.push({ path: "zoomLevel", message: "Must be a number" });
    } else if (cfg["zoomLevel"] < -10 || cfg["zoomLevel"] > 10) {
      issues.push({
        path: "zoomLevel",
        message: "Must be between -10 and 10"
      });
    }
  }
  if (cfg["userEnv"] !== void 0 && typeof cfg["userEnv"] !== "object") {
    issues.push({ path: "userEnv", message: "Must be an object" });
  }
  if (cfg["workspace"] !== void 0) {
    if (typeof cfg["workspace"] !== "object" || cfg["workspace"] === null) {
      issues.push({ path: "workspace", message: "Must be an object" });
    } else {
      const ws = cfg["workspace"];
      if (ws["id"] !== void 0 && typeof ws["id"] !== "string") {
        issues.push({
          path: "workspace.id",
          message: "Must be a string"
        });
      }
      if (ws["uri"] !== void 0 && typeof ws["uri"] !== "string") {
        issues.push({
          path: "workspace.uri",
          message: "Must be a string"
        });
      }
    }
  }
  return issues;
}, "validateConfig");
var ConfigurationTag = class extends Context_exports.Tag("Configuration")() {
  static {
    __name(this, "ConfigurationTag");
  }
};
var Configuration = ConfigurationTag;
var ConfigurationLive = Layer_exports.effect(
  Configuration,
  Effect_exports.gen(function* () {
    const sandbox4 = yield* Sandbox;
    const ipc = yield* IPC;
    const configRef = yield* SubscriptionRef_exports.make(null);
    const fetch = Effect_exports.gen(function* () {
      const fromSandbox = yield* sandbox4.resolveConfiguration.pipe(
        Effect_exports.either
      );
      if (fromSandbox._tag === "Right") {
        return fromSandbox.right;
      }
      return yield* ipc.invoke("mountain_get_workbench_configuration")([]).pipe(Effect_exports.mapError((error2) => new ConfigFetchError(error2)));
    });
    const validate3 = /* @__PURE__ */ __name((config2) => Effect_exports.sync(() => validateConfig(config2)).pipe(
      Effect_exports.flatMap(
        (issues) => issues.length > 0 ? Effect_exports.fail(
          new ConfigValidationError(
            issues.map(
              (i) => `${i.path}: ${i.message}`
            )
          )
        ) : Effect_exports.succeed(config2)
      )
    ), "validate");
    const apply2 = /* @__PURE__ */ __name((config2) => Effect_exports.gen(function* () {
      if (config2.zoomLevel !== void 0) {
        yield* Effect_exports.try({
          try: /* @__PURE__ */ __name(() => {
            if (window && window.vscode) {
              window.vscode.postMessage({
                type: "setZoomLevel",
                payload: config2.zoomLevel
              });
            }
          }, "try"),
          catch: /* @__PURE__ */ __name((error2) => new ConfigApplyError("zoomLevel", error2), "catch")
        });
      }
      if (config2.userEnv) {
        for (const [key, value2] of Object.entries(config2.userEnv || {})) {
          yield* Effect_exports.try({
            try: /* @__PURE__ */ __name(() => {
              if (typeof process !== "undefined" && process.env) {
                process.env[key] = value2;
              }
            }, "try"),
            catch: /* @__PURE__ */ __name((error2) => new ConfigApplyError(key, error2), "catch")
          });
        }
      }
    }), "apply");
    const changes3 = configRef.changes.pipe(
      Stream_exports.filter((config2) => config2 !== null)
    );
    const get21 = Effect_exports.gen(function* () {
      const current2 = yield* configRef.get;
      if (!current2) {
        return yield* Effect_exports.fail(
          new ConfigurationNotReadyError()
        );
      }
      return current2;
    });
    const refresh = Effect_exports.gen(function* () {
      const config2 = yield* fetch;
      yield* SubscriptionRef_exports.set(configRef, config2);
      return config2;
    });
    yield* fetch.pipe(Effect_exports.flatMap((config2) => SubscriptionRef_exports.set(configRef, config2)));
    yield* Effect_exports.log("[Configuration] Configuration service initialized");
    return {
      get: get21,
      fetch,
      validate: validate3,
      apply: apply2,
      changes: changes3,
      refresh
    };
  })
);
var ConfigurationWithSyncLive = Layer_exports.effect(
  Configuration,
  Effect_exports.gen(function* () {
    const sandbox4 = yield* Sandbox;
    const ipc = yield* IPC;
    const mountain = yield* MountainTag;
    const configRef = yield* SubscriptionRef_exports.make(null);
    const fetch = Effect_exports.gen(function* () {
      const fromSandbox = yield* sandbox4.resolveConfiguration.pipe(
        Effect_exports.either
      );
      if (fromSandbox._tag === "Right") {
        return fromSandbox.right;
      }
      return yield* ipc.invoke("mountain_get_workbench_configuration")([]).pipe(Effect_exports.mapError((error2) => new ConfigFetchError(error2)));
    });
    const validate3 = /* @__PURE__ */ __name((config2) => Effect_exports.sync(() => validateConfig(config2)).pipe(
      Effect_exports.flatMap(
        (issues) => issues.length > 0 ? Effect_exports.fail(
          new ConfigValidationError(
            issues.map(
              (i) => `${i.path}: ${i.message}`
            )
          )
        ) : Effect_exports.succeed(config2)
      )
    ), "validate");
    const apply2 = /* @__PURE__ */ __name((config2) => Effect_exports.gen(function* () {
      if (config2.zoomLevel !== void 0) {
        yield* Effect_exports.try({
          try: /* @__PURE__ */ __name(() => {
            if (window && window.vscode) {
              window.vscode.postMessage({
                type: "setZoomLevel",
                payload: config2.zoomLevel
              });
            }
          }, "try"),
          catch: /* @__PURE__ */ __name((error2) => new ConfigApplyError("zoomLevel", error2), "catch")
        });
      }
      if (config2.userEnv) {
        for (const [key, value2] of Object.entries(config2.userEnv || {})) {
          yield* Effect_exports.try({
            try: /* @__PURE__ */ __name(() => {
              if (typeof process !== "undefined" && process.env) {
                process.env[key] = value2;
              }
            }, "try"),
            catch: /* @__PURE__ */ __name((error2) => new ConfigApplyError(key, error2), "catch")
          });
        }
      }
    }), "apply");
    const changes3 = configRef.changes.pipe(
      Stream_exports.filter((config2) => config2 !== null)
    );
    const get21 = Effect_exports.gen(function* () {
      const current2 = yield* configRef.get;
      if (!current2) {
        return yield* Effect_exports.fail(
          new ConfigurationNotReadyError()
        );
      }
      return current2;
    });
    const refresh = Effect_exports.gen(function* () {
      const config2 = yield* fetch;
      yield* SubscriptionRef_exports.set(configRef, config2);
      return config2;
    });
    yield* fetch.pipe(Effect_exports.flatMap((config2) => SubscriptionRef_exports.set(configRef, config2)));
    yield* Effect_exports.fork(
      Effect_exports.gen(function* () {
        const connectionState = yield* mountain.connectionState;
        if (connectionState._tag === "Connected") {
          yield* Effect_exports.repeat(
            Effect_exports.gen(function* () {
              const config2 = yield* mountain.rpc("mountain_get_configuration")();
              if (config2) {
                yield* validate3(config2).pipe(
                  Effect_exports.flatMap((validatedConfig) => {
                    return Effect_exports.gen(function* () {
                      const current2 = yield* configRef.get;
                      if (!current2 || JSON.stringify(current2) !== JSON.stringify(validatedConfig)) {
                        yield* SubscriptionRef_exports.set(configRef, validatedConfig);
                        yield* apply2(validatedConfig);
                      }
                    });
                  }),
                  Effect_exports.catchAll(
                    (error2) => Effect_exports.sync(() => {
                      console.error("[Configuration] Sync error:", error2);
                    })
                  )
                );
              }
            }),
            Schedule_exports.spaced("5 seconds")
          );
        }
      })
    );
    yield* Effect_exports.log("[Configuration] Configuration service with sync initialized");
    return {
      get: get21,
      fetch,
      validate: validate3,
      apply: apply2,
      changes: changes3,
      refresh
    };
  })
);
var getConfigValue = /* @__PURE__ */ __name((config2, path) => {
  const parts2 = path.split(".");
  let current2 = config2;
  for (const part of parts2) {
    if (current2 && typeof current2 === "object" && part in current2) {
      current2 = current2[part];
    } else {
      return void 0;
    }
  }
  return current2;
}, "getConfigValue");
var makeMockConfiguration = /* @__PURE__ */ __name((overrides) => {
  const mockConfig = {
    zoomLevel: 0,
    userEnv: {},
    workspace: {
      id: "mock-workspace",
      uri: "mock://workspace",
      name: "Mock Workspace"
    },
    ...overrides
  };
  return {
    get: Effect_exports.succeed(mockConfig),
    fetch: Effect_exports.succeed(mockConfig),
    validate: /* @__PURE__ */ __name((config2) => Effect_exports.sync(() => {
      const issues = validateConfig(config2);
      if (issues.length > 0) {
        return Effect_exports.fail(new ConfigValidationError(issues.map((i) => `${i.path}: ${i.message}`)));
      }
      return Effect_exports.succeed(config2);
    }).pipe(Effect_exports.flatten), "validate"),
    apply: /* @__PURE__ */ __name(() => Effect_exports.void, "apply"),
    changes: Stream_exports.empty,
    refresh: Effect_exports.succeed(mockConfig)
  };
}, "makeMockConfiguration");
var ConfigurationMock = Layer_exports.succeed(
  Configuration,
  makeMockConfiguration()
);

// Source/Effect/Environment.ts
var EnvironmentTag = class extends Context_exports.Tag("Effect/EnvironmentService")() {
  static {
    __name(this, "EnvironmentTag");
  }
};
var detectPlatform = /* @__PURE__ */ __name(() => {
  if (typeof navigator === "undefined") {
    return "web";
  }
  const platform = navigator.platform?.toLowerCase() || "";
  if (platform.includes("win")) {
    return "win32";
  }
  if (platform.includes("mac")) {
    return "darwin";
  }
  if (platform.includes("linux") || platform.includes("ubuntu")) {
    return "linux";
  }
  return "web";
}, "detectPlatform");
var detectArchitecture = /* @__PURE__ */ __name(() => {
  if (typeof navigator === "undefined") {
    return "web";
  }
  const userAgent = navigator.userAgent.toLowerCase();
  if (userAgent.includes("arm") || userAgent.includes("aarch64")) {
    return "arm64";
  }
  return "x64";
}, "detectArchitecture");
var detectLocale = /* @__PURE__ */ __name(() => {
  if (typeof navigator === "undefined") {
    return "en-US";
  }
  return navigator.language || "en-US";
}, "detectLocale");
var detectTimezone = /* @__PURE__ */ __name(() => {
  try {
    return Intl.DateTimeFormat().resolvedOptions().timeZone || "UTC";
  } catch {
    return "UTC";
  }
}, "detectTimezone");
var getUserAgent = /* @__PURE__ */ __name(() => {
  if (typeof navigator === "undefined") {
    return "Unknown";
  }
  return navigator.userAgent || "Unknown";
}, "getUserAgent");
var makeLiveEnvironment = {
  getInfo: Effect_exports.sync(() => ({
    platform: detectPlatform(),
    architecture: detectArchitecture(),
    locale: detectLocale(),
    timezone: detectTimezone(),
    userAgent: getUserAgent(),
    isSecureContext: typeof window !== "undefined" && window.isSecureContext,
    language: detectLocale().split("-")[0] || "en"
  })),
  getPlatform: Effect_exports.sync(detectPlatform),
  getArchitecture: Effect_exports.sync(detectArchitecture),
  isWindows: Effect_exports.map(Effect_exports.sync(detectPlatform), (p) => p === "win32"),
  isMac: Effect_exports.map(Effect_exports.sync(detectPlatform), (p) => p === "darwin"),
  isLinux: Effect_exports.map(Effect_exports.sync(detectPlatform), (p) => p === "linux"),
  isWeb: Effect_exports.map(Effect_exports.sync(detectPlatform), (p) => p === "web")
};
var EnvironmentLive = Layer_exports.effect(
  EnvironmentTag,
  Effect_exports.succeed(makeLiveEnvironment)
);
var makeMockEnvironment = /* @__PURE__ */ __name((overrides) => {
  const mockInfo = {
    platform: "web",
    architecture: "x64",
    locale: "en-US",
    timezone: "UTC",
    userAgent: "Mock",
    isSecureContext: true,
    language: "en",
    ...overrides
  };
  return {
    getInfo: Effect_exports.sync(() => mockInfo),
    getPlatform: Effect_exports.sync(() => mockInfo.platform),
    getArchitecture: Effect_exports.sync(() => mockInfo.architecture),
    isWindows: Effect_exports.sync(() => mockInfo.platform === "win32"),
    isMac: Effect_exports.sync(() => mockInfo.platform === "darwin"),
    isLinux: Effect_exports.sync(() => mockInfo.platform === "linux"),
    isWeb: Effect_exports.sync(() => mockInfo.platform === "web")
  };
}, "makeMockEnvironment");
var EnvironmentMock = Layer_exports.effect(
  EnvironmentTag,
  Effect_exports.succeed(makeMockEnvironment())
);

// Source/Effect/Health.ts
var HealthTag = class extends Context_exports.Tag("Effect/HealthService")() {
  static {
    __name(this, "HealthTag");
  }
};
var createServiceHealth = /* @__PURE__ */ __name((name, status3, message, responseTime, details) => ({
  serviceName: name,
  status: status3,
  message,
  lastChecked: Date.now(),
  responseTime,
  details
}), "createServiceHealth");
var createServiceHealthWithNoResponseTime = /* @__PURE__ */ __name((name, status3, message) => ({
  serviceName: name,
  status: status3,
  message,
  lastChecked: Date.now(),
  responseTime: 0,
  details: void 0
}), "createServiceHealthWithNoResponseTime");
var makeHealthChecker = /* @__PURE__ */ __name(() => ({
  checkService: /* @__PURE__ */ __name((serviceName) => Effect_exports.gen(function* () {
    const startTime = Date.now();
    switch (serviceName.toLowerCase()) {
      case "environment":
        const envTime = Date.now() - startTime;
        return Effect_exports.succeed(
          createServiceHealth(
            "Environment",
            "healthy",
            "Environment service available",
            envTime
          )
        );
      case "telemetry":
        const telemetryService = yield* TelemetryTag;
        const telemetryTime = Date.now() - startTime;
        return yield* telemetryService.log("info", "[Health] Telemetry health check").pipe(
          Effect_exports.map(
            () => createServiceHealth(
              "Telemetry",
              "healthy",
              "Telemetry service available",
              telemetryTime
            )
          ),
          Effect_exports.catchAll(
            () => Effect_exports.succeed(
              createServiceHealth(
                "Telemetry",
                "unhealthy",
                "Telemetry service error",
                telemetryTime
              )
            )
          )
        );
      case "mountain": {
        const mountain = yield* MountainTag;
        const mountainTime = Date.now() - startTime;
        return yield* mountain.version.pipe(
          Effect_exports.map(
            (version) => createServiceHealth(
              "Mountain",
              "healthy",
              `Mountain backend connected (v${version})`,
              mountainTime,
              { version }
            )
          ),
          Effect_exports.catchAll(
            (error2) => Effect_exports.succeed(
              createServiceHealth(
                "Mountain",
                "unhealthy",
                `Mountain connection failed: ${String(error2)}`,
                Date.now() - startTime
              )
            )
          )
        );
      }
      case "ipc": {
        const ipcTime = Date.now() - startTime;
        return Effect_exports.succeed(
          createServiceHealth(
            "IPC",
            "healthy",
            "IPC service available",
            ipcTime
          )
        );
      }
      case "configuration": {
        const config2 = yield* ConfigurationTag;
        const configTime = Date.now() - startTime;
        return yield* config2.get.pipe(
          Effect_exports.map(
            () => createServiceHealth(
              "Configuration",
              "healthy",
              "Configuration service available",
              configTime
            )
          ),
          Effect_exports.catchAll(
            () => Effect_exports.succeed(
              createServiceHealth(
                "Configuration",
                "unhealthy",
                "Configuration service error",
                configTime
              )
            )
          )
        );
      }
      default:
        return Effect_exports.succeed(
          createServiceHealthWithNoResponseTime(
            serviceName,
            "unknown",
            `Unknown service: ${serviceName}`
          )
        );
    }
  }), "checkService"),
  checkAllServices: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
    const env = yield* EnvironmentTag;
    const envInfo = yield* env.getInfo;
    const services = ["environment", "telemetry", "mountain", "ipc", "configuration"];
    const healthChecker = makeHealthChecker();
    const serviceHealthChecks = services.map(
      (service6) => healthChecker.checkService(service6)
    );
    const healthResults = yield* Effect_exports.all(serviceHealthChecks);
    const unhealthyCount = healthResults.filter((h) => h.status === "unhealthy").length;
    const degradedCount = healthResults.filter((h) => h.status === "degraded").length;
    let overallStatus = "healthy";
    if (unhealthyCount > 0) {
      overallStatus = "unhealthy";
    } else if (degradedCount > 0) {
      overallStatus = "degraded";
    }
    return {
      overallStatus,
      services: healthResults,
      systemInfo: {
        platform: envInfo.platform,
        architecture: envInfo.architecture,
        upSince: Date.now()
      },
      lastChecked: Date.now()
    };
  }), "checkAllServices"),
  getOverallStatus: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
    const healthChecker = makeHealthChecker();
    const systemHealth = yield* healthChecker.checkAllServices();
    return systemHealth.overallStatus;
  }), "getOverallStatus"),
  monitorService: /* @__PURE__ */ __name((serviceName, intervalMs) => Effect_exports.gen(function* () {
    yield* makeHealthChecker().checkService(serviceName).pipe(
      Effect_exports.repeat(Schedule_exports.spaced(`${intervalMs} millis`))
    );
  }), "monitorService")
}), "makeHealthChecker");
var HealthLive = Layer_exports.effect(
  HealthTag,
  Effect_exports.succeed(makeHealthChecker())
);
var makeMockHealth = /* @__PURE__ */ __name((overrides) => ({
  checkService: /* @__PURE__ */ __name((serviceName) => Effect_exports.gen(function* () {
    const defaultStatus = "healthy";
    const status3 = overrides?.[serviceName] ?? defaultStatus;
    return createServiceHealth(
      serviceName,
      status3,
      status3 === "healthy" ? "Mock service healthy" : "Mock service unhealthy",
      0
    );
  }), "checkService"),
  checkAllServices: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
    const services = ["environment", "telemetry", "mountain", "ipc", "configuration"];
    const results = services.map(
      (name) => createServiceHealth(
        name,
        overrides?.[name] ?? "healthy",
        "Mock service check",
        0
      )
    );
    return {
      overallStatus: "healthy",
      services: results,
      systemInfo: {
        platform: "mock",
        architecture: "mock",
        upSince: Date.now()
      },
      lastChecked: Date.now()
    };
  }), "checkAllServices"),
  getOverallStatus: /* @__PURE__ */ __name(() => Effect_exports.succeed("healthy"), "getOverallStatus"),
  monitorService: /* @__PURE__ */ __name(() => Effect_exports.void, "monitorService")
}), "makeMockHealth");
var HealthMock = Layer_exports.effect(HealthTag, Effect_exports.succeed(makeMockHealth()));

// Source/Effect/Bootstrap.ts
var BootstrapTag = class extends Context_exports.Tag("Effect/BootstrapService")() {
  static {
    __name(this, "BootstrapTag");
  }
};
var stage0_Environment = withSpan8(
  "stage0_environment",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    const environment3 = yield* EnvironmentTag;
    telemetry.log("info", "[Bootstrap] Stage 0: Detecting environment...");
    const envInfo = yield* environment3.getInfo;
    telemetry.log(
      "info",
      `[Bootstrap] Environment: ${envInfo.platform}/${envInfo.architecture}`
    );
    telemetry.log("info", `[Bootstrap] Locale: ${envInfo.locale}, Timezone: ${envInfo.timezone}`);
    return {
      stageName: "Environment",
      success: true,
      duration: 0,
      // Will be set by caller
      error: void 0
    };
  })
);
var stage1_Preload = withSpan8(
  "stage1_preload",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    const sandbox4 = yield* Sandbox;
    telemetry.log("info", "[Bootstrap] Stage 1: Waiting for preload...");
    void (yield* sandbox4.awaitReady);
    telemetry.log("info", "[Bootstrap] Preload ready, globals available");
    return {
      stageName: "Preload",
      success: true,
      duration: 0,
      error: void 0
    };
  })
);
var stage2_Configuration = withSpan8(
  "stage2_configuration",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    yield* (yield* Configuration).get;
    telemetry.log("info", "[Bootstrap] Stage 2: Loading configuration...");
    telemetry.log("info", "[Bootstrap] Configuration applied");
    return {
      stageName: "Configuration",
      success: true,
      duration: 0,
      error: void 0
    };
  })
);
var stage3_Services = withSpan8(
  "stage3_services",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    telemetry.log("info", "[Bootstrap] Stage 3: Connecting to Mountain backend...");
    yield* (yield* MountainTag).connect;
    telemetry.log("info", "[Bootstrap] Mountain connected");
    return {
      stageName: "Services",
      success: true,
      duration: 0,
      error: void 0
    };
  })
);
var stage4_Preparation = withSpan8(
  "stage4_preparation",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    telemetry.log("info", "[Bootstrap] Stage 4: Preparing workbench resources...");
    telemetry.log("info", "[Bootstrap] Workbench resources prepared");
    return {
      stageName: "Preparation",
      success: true,
      duration: 0,
      error: void 0
    };
  })
);
var stage5_Initialization = withSpan8(
  "stage5_initialization",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    telemetry.log("info", "[Bootstrap] Stage 5: Initializing VSCode workbench...");
    telemetry.log("info", "[Bootstrap] VSCode workbench initialized");
    yield* Effect_exports.sync(() => {
      window.dispatchEvent(
        new CustomEvent("vscode-wind-bootstrap-complete", {
          detail: { success: true }
        })
      );
    });
    return {
      stageName: "Initialization",
      success: true,
      duration: 0,
      error: void 0
    };
  })
);
var stage6_HealthCheck = withSpan8(
  "stage6_healthcheck",
  Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    const health = yield* HealthTag;
    telemetry.log("info", "[Bootstrap] Stage 6: Running health checks...");
    const systemHealth = yield* health.checkAllServices();
    telemetry.log(
      "info",
      `[Bootstrap] Health check result: ${systemHealth.overallStatus}`
    );
    if (systemHealth.overallStatus === "unhealthy") {
      telemetry.log("error", "[Bootstrap] Some services are unhealthy!");
    }
    return {
      stageName: "HealthCheck",
      success: systemHealth.overallStatus !== "unhealthy",
      duration: 0,
      error: void 0
    };
  })
);
var makeBootstrap = /* @__PURE__ */ __name(() => ({
  run: /* @__PURE__ */ __name((options) => Effect_exports.gen(function* () {
    const telemetry = yield* Telemetry;
    const startTime = Date.now();
    const { skipHealthCheck = false, debugMode = false } = options ?? {};
    telemetry.log("info", "[Bootstrap] ===============================================");
    telemetry.log("info", "[Bootstrap] Wind VSCode Workbench Bootstrap");
    telemetry.log("info", "[Bootstrap] Debug mode: " + debugMode);
    telemetry.log("info", "[Bootstrap] ===============================================");
    const stages = [
      stage0_Environment,
      stage1_Preload,
      stage2_Configuration,
      stage3_Services,
      stage4_Preparation,
      stage5_Initialization,
      ...skipHealthCheck ? [] : [stage6_HealthCheck]
    ];
    const results = [];
    for (const stage of stages) {
      const stageStartTime = Date.now();
      let result;
      try {
        const stageResult = yield* Effect_exports.suspend(() => stage);
        result = { ...stageResult, duration: Date.now() - stageStartTime };
      } catch (e) {
        const error2 = e instanceof Error ? e : new Error(String(e));
        result = {
          stageName: "Unknown",
          success: false,
          duration: Date.now() - stageStartTime,
          error: error2
        };
      }
      results.push(result);
    }
    const endTime = Date.now();
    const totalDuration = endTime - startTime;
    const allSuccess = results.every((r) => r.success);
    telemetry.log("info", "[Bootstrap] ===============================================");
    telemetry.log(
      "info",
      `[Bootstrap] ${allSuccess ? "\u2713 Bootstrap completed successfully" : "\u2717 Bootstrap failed"}`
    );
    telemetry.log("info", `[Bootstrap] Total duration: ${totalDuration}ms`);
    telemetry.log("info", "[Bootstrap] ===============================================");
    if (!allSuccess) {
      const failedStages = results.filter((r) => !r.success);
      telemetry.log("error", "[Bootstrap] Failed stages:");
      for (const failed of failedStages) {
        telemetry.log("error", `[Bootstrap]   - ${failed.stageName}: ${failed.error?.message || "Unknown error"}`);
      }
    }
    return {
      success: allSuccess,
      totalDuration,
      stages: results,
      error: allSuccess ? void 0 : new Error("Bootstrap failed")
    };
  }), "run")
}), "makeBootstrap");
var BootstrapLive = Layer_exports.effect(
  BootstrapTag,
  Effect_exports.succeed(makeBootstrap())
);
var makeMockBootstrap = /* @__PURE__ */ __name(() => ({
  run: /* @__PURE__ */ __name((options) => Effect_exports.gen(function* () {
    yield* Effect_exports.sleep("1 millis");
    return {
      success: true,
      totalDuration: 1,
      stages: [
        { stageName: "Environment", success: true, duration: 0, error: void 0 },
        { stageName: "Preload", success: true, duration: 0, error: void 0 },
        { stageName: "Configuration", success: true, duration: 0, error: void 0 },
        { stageName: "Services", success: true, duration: 0, error: void 0 },
        { stageName: "Preparation", success: true, duration: 0, error: void 0 },
        { stageName: "Initialization", success: true, duration: 0, error: void 0 },
        ...options?.skipHealthCheck ? [] : [{ stageName: "HealthCheck", success: true, duration: 0, error: void 0 }]
      ],
      error: void 0
    };
  }), "run")
}), "makeMockBootstrap");
var BootstrapMock = Layer_exports.effect(BootstrapTag, Effect_exports.succeed(makeMockBootstrap()));
var runBootstrap = /* @__PURE__ */ __name((options) => Effect_exports.gen(function* () {
  const bootstrap = yield* BootstrapTag;
  return yield* bootstrap.run(options);
}).pipe(
  Effect_exports.provide(BootstrapLive)
), "runBootstrap");

// Source/Effect/MountainSync.ts
var MountainSyncTag = class extends Context_exports.Tag("Effect/MountainSyncService")() {
  static {
    __name(this, "MountainSyncTag");
  }
};
var syncNowEffect = /* @__PURE__ */ __name((_mountain, _ipc, telemetry) => Effect_exports.gen(function* () {
  const startTime = Date.now();
  yield* telemetry.log("info", "[MountainSync] Performing sync...");
  yield* Effect_exports.sleep(10);
  return {
    success: true,
    itemsSynced: 0,
    duration: Date.now() - startTime
  };
}), "syncNowEffect");
var makeMountainSync = /* @__PURE__ */ __name((mountain, ipc, telemetry) => {
  let syncFiber = null;
  return {
    start: /* @__PURE__ */ __name((config2) => Effect_exports.gen(function* () {
      const fullConfig = {
        enabled: true,
        syncIntervalMs: 5e3,
        autoRetry: true,
        maxRetries: 3,
        batchSize: 100,
        ...config2
      };
      if (!fullConfig.enabled) {
        yield* telemetry.log("info", "[MountainSync] Sync disabled in config");
        return;
      }
      yield* telemetry.log(
        "info",
        `[MountainSync] Starting sync with ${fullConfig.syncIntervalMs}ms interval`
      );
      const startSyncing = Effect_exports.gen(function* () {
        yield* Effect_exports.forever(
          Effect_exports.gen(function* () {
            yield* Effect_exports.sleep(fullConfig.syncIntervalMs);
            const result = yield* syncNowEffect(mountain, ipc, telemetry);
            if (result.success) {
              yield* telemetry.log(
                "info",
                `[MountainSync] Synced ${result.itemsSynced} items in ${result.duration}ms`
              );
            } else if (fullConfig.autoRetry) {
              yield* telemetry.log(
                "warn",
                `[MountainSync] Sync failed, will retry: ${result.error?.message}`
              );
            }
          })
        );
      });
      syncFiber = yield* startSyncing.pipe(Effect_exports.fork);
    }), "start"),
    stop: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
      if (syncFiber) {
        yield* Fiber_exports.interrupt(syncFiber);
        syncFiber = null;
        yield* telemetry.log("info", "[MountainSync] Stopped");
      }
    }), "stop"),
    syncNow: /* @__PURE__ */ __name(() => syncNowEffect(mountain, ipc, telemetry), "syncNow"),
    getStatus: /* @__PURE__ */ __name(() => Effect_exports.sync(() => "idle"), "getStatus"),
    getStats: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
      const now2 = Date.now();
      return {
        lastSyncTime: now2,
        syncCount: 1,
        successCount: 1,
        errorCount: 0,
        itemsSynced: 0
      };
    }), "getStats"),
    pause: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
      yield* telemetry.log("info", "[MountainSync] Pausing...");
    }), "pause"),
    resume: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
      yield* telemetry.log("info", "[MountainSync] Resuming...");
    }), "resume")
  };
}, "makeMountainSync");
var MountainSyncLive = Layer_exports.effect(
  MountainSyncTag,
  Effect_exports.gen(function* () {
    const mountain = yield* MountainTag;
    const ipc = yield* IPCTag;
    const telemetry = yield* TelemetryTag;
    return makeMountainSync(mountain, ipc, telemetry);
  })
);
var makeMockMountainSync = /* @__PURE__ */ __name(() => ({
  start: /* @__PURE__ */ __name(() => Effect_exports.void, "start"),
  stop: /* @__PURE__ */ __name(() => Effect_exports.void, "stop"),
  syncNow: /* @__PURE__ */ __name(() => Effect_exports.gen(function* () {
    return {
      success: true,
      itemsSynced: 0,
      duration: 1
    };
  }), "syncNow"),
  getStatus: /* @__PURE__ */ __name(() => Effect_exports.succeed("idle"), "getStatus"),
  getStats: /* @__PURE__ */ __name(() => Effect_exports.succeed({
    lastSyncTime: Date.now(),
    syncCount: 0,
    successCount: 0,
    errorCount: 0,
    itemsSynced: 0
  }), "getStats"),
  pause: /* @__PURE__ */ __name(() => Effect_exports.void, "pause"),
  resume: /* @__PURE__ */ __name(() => Effect_exports.void, "resume")
}), "makeMockMountainSync");
var MountainSyncMock = Layer_exports.effect(
  MountainSyncTag,
  Effect_exports.succeed(makeMockMountainSync())
);

// Source/Effect/Clipboard.ts
var LiveClipboardService = {
  readText: /* @__PURE__ */ __name(() => Effect_exports.tryPromise({
    try: /* @__PURE__ */ __name(async () => {
      if (typeof navigator === "undefined" || !navigator.clipboard) {
        throw {
          _tag: "ClipboardNotAvailable",
          reason: "Clipboard API not available in this environment"
        };
      }
      return await navigator.clipboard.readText();
    }, "try"),
    catch: /* @__PURE__ */ __name((error2) => ({
      _tag: "ClipboardReadError",
      error: error2
    }), "catch")
  }), "readText"),
  writeText: /* @__PURE__ */ __name((text) => Effect_exports.tryPromise({
    try: /* @__PURE__ */ __name(async () => {
      if (typeof navigator === "undefined" || !navigator.clipboard) {
        throw {
          _tag: "ClipboardNotAvailable",
          reason: "Clipboard API not available in this environment"
        };
      }
      await navigator.clipboard.writeText(text);
    }, "try"),
    catch: /* @__PURE__ */ __name((error2) => ({
      _tag: "ClipboardWriteError",
      error: error2
    }), "catch")
  }), "writeText"),
  // Placeholder implementations for remaining methods
  readHTML: /* @__PURE__ */ __name(() => Effect_exports.fail({
    _tag: "ClipboardFormatNotSupported",
    format: "HTML"
  }), "readHTML"),
  writeHTML: /* @__PURE__ */ __name(() => Effect_exports.fail({
    _tag: "ClipboardFormatNotSupported",
    format: "HTML"
  }), "writeHTML"),
  readImage: /* @__PURE__ */ __name(() => Effect_exports.fail({
    _tag: "ClipboardFormatNotSupported",
    format: "Image"
  }), "readImage"),
  writeImage: /* @__PURE__ */ __name(() => Effect_exports.fail({
    _tag: "ClipboardFormatNotSupported",
    format: "Image"
  }), "writeImage"),
  hasText: /* @__PURE__ */ __name(() => Effect_exports.succeed(false), "hasText"),
  clear: /* @__PURE__ */ __name(() => Effect_exports.void, "clear")
};
var ClipboardServiceTag = class extends Context_exports.Tag("Application/ClipboardService")() {
  static {
    __name(this, "ClipboardServiceTag");
  }
};
var LiveClipboardServiceLayer = Layer_exports.succeed(
  ClipboardServiceTag,
  LiveClipboardService
);
var MockClipboardService = {
  readText: /* @__PURE__ */ __name(() => Effect_exports.succeed("mock clipboard text"), "readText"),
  writeText: /* @__PURE__ */ __name((_text) => Effect_exports.void, "writeText"),
  readHTML: /* @__PURE__ */ __name(() => Effect_exports.succeed(""), "readHTML"),
  writeHTML: /* @__PURE__ */ __name(() => Effect_exports.void, "writeHTML"),
  readImage: /* @__PURE__ */ __name(() => Effect_exports.succeed(new Blob()), "readImage"),
  writeImage: /* @__PURE__ */ __name(() => Effect_exports.void, "writeImage"),
  hasText: /* @__PURE__ */ __name(() => Effect_exports.succeed(true), "hasText"),
  clear: /* @__PURE__ */ __name(() => Effect_exports.void, "clear")
};
var MockClipboardServiceLayer = Layer_exports.succeed(
  ClipboardServiceTag,
  MockClipboardService
);
export {
  BootstrapLive,
  BootstrapMock,
  BootstrapTag,
  ClipboardServiceTag,
  ConfigApplyError,
  ConfigFetchError,
  ConfigValidationError,
  Configuration,
  ConfigurationLive,
  EnvironmentLive,
  EnvironmentMock,
  EnvironmentTag,
  HealthLive,
  HealthMock,
  HealthTag,
  IPC,
  IPCElectronLive,
  IPCInvokeError,
  IPCMockLive,
  IPCSendError,
  IPCSubscriptionError,
  IPCTauriLive,
  LiveClipboardServiceLayer,
  MockClipboardServiceLayer,
  Mountain,
  MountainConnectionError,
  MountainLive,
  MountainMockLive,
  MountainRPCError,
  MountainStateError,
  MountainSyncError,
  MountainSyncLive,
  MountainSyncMock,
  MountainSyncTag,
  Sandbox,
  SandboxLive,
  SandboxMockLive,
  Telemetry,
  TelemetryCollectionError,
  TelemetryLive,
  TelemetryMockLive,
  runBootstrap,
  withMetric3 as withMetric,
  withSpan8 as withSpan
};
//# sourceMappingURL=index.js.map
